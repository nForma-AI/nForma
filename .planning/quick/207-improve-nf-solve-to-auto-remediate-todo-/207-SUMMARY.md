---
phase: quick-207
plan: 01
subsystem: testing
tags: [nf-solve, formal-verification, test-stubs, behavioral-tests, auto-remediation]

requires:
  - phase: quick-140
    provides: nf-solve solver loop and autoClose function
provides:
  - Behavioral test strategy using recipe import_hint and template_boilerplate
  - autoClose integration with _implement-stubs.cjs for TODO stub upgrade
affects: [nf-solve, formal-test-sync, generated-stubs]

tech-stack:
  added: []
  patterns: [try-catch-require-with-source-grep-fallback, dry-run-flag]

key-files:
  created: []
  modified:
    - .planning/formal/generated-stubs/_implement-stubs.cjs
    - bin/nf-solve.cjs
    - bin/nf-solve.test.cjs

key-decisions:
  - "Behavioral strategy wraps require() in try/catch to handle modules with TUI side effects, falling back to source-grep pattern"
  - "autoClose uses spawnSync directly (not spawnTool) since _implement-stubs.cjs lives outside SCRIPT_DIR"

patterns-established:
  - "try/catch require + source-grep fallback for behavioral test generation of modules with side effects"

requirements-completed: [QUICK-207]

duration: 23min
completed: 2026-03-07
---

# Quick 207: Improve nf:solve auto-remediation Summary

**Behavioral test stub generation now uses recipe import_hint and template_boilerplate for import-and-call tests, with autoClose dispatching _implement-stubs.cjs after formal-test-sync stub creation**

## Performance

- **Duration:** 23 min
- **Started:** 2026-03-07T15:33:45Z
- **Completed:** 2026-03-07T15:56:50Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Behavioral strategy in _implement-stubs.cjs now reads recipe.import_hint to generate proper require() calls instead of file-exists checks
- Uses recipe.template_boilerplate for import-and-call test templates when template type matches
- autoClose() in nf-solve.cjs now dispatches _implement-stubs.cjs after formal-test-sync generates stubs
- Added --dry-run flag to _implement-stubs.cjs for debugging
- Added 2 new test cases (TC-AUTOCLOSE-STUBS-1, TC-AUTOCLOSE-STUBS-2)

## Task Commits

Each task was committed atomically:

1. **Task 1: Upgrade _implement-stubs.cjs behavioral strategy** - `3925230f` (feat)
2. **Task 2: Wire _implement-stubs.cjs into autoClose and add test coverage** - `b727a7b3` (feat)

## Files Created/Modified
- `.planning/formal/generated-stubs/_implement-stubs.cjs` - Upgraded behavioral strategy with import_hint, template_boilerplate, try/catch require, --dry-run flag
- `bin/nf-solve.cjs` - Added _implement-stubs.cjs dispatch in autoClose() after F->T stub generation
- `bin/nf-solve.test.cjs` - Added TC-AUTOCLOSE-STUBS-1 and TC-AUTOCLOSE-STUBS-2 tests

## Decisions Made
- Behavioral strategy wraps require() in try/catch because some modules (e.g., nForma.cjs) launch TUI on import. Falls back to source-grep verification if require fails.
- autoClose uses spawnSync directly with absolute path rather than spawnTool, since _implement-stubs.cjs lives in .planning/formal/generated-stubs/ (outside SCRIPT_DIR/bin/).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The _implement-stubs.cjs behavioral strategy is ready for use in future nf:solve runs
- All TODO stubs are currently already implemented (272 skipped, 0 needing upgrade)
- When new behavioral stubs are generated by formal-test-sync, they will be automatically upgraded during autoClose

---
*Phase: quick-207*
*Completed: 2026-03-07*
