---
phase: 01-hook-enforcement
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - STOP-05

must_haves:
  truths:
    - "STOP-05 requirement description matches the actual implementation: stop hook reads transcript JSONL as the authoritative source, with no fast-path pre-check"
    - "REQUIREMENTS.md STOP-05 checkbox is checked and the description does not promise last_assistant_message fast-path behavior"
    - "STATE.md Decisions records the explicit design decision to omit the fast-path in favor of JSONL-only verification"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated STOP-05 entry — description revised to match implementation, checkbox checked"
      contains: "STOP-05"
    - path: ".planning/STATE.md"
      provides: "Design decision entry: fast-path omitted for robustness"
      contains: "STOP-05"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "hooks/qgsd-stop.js"
      via: "STOP-05 description accurately describes what main() does"
      pattern: "STOP-05.*transcript JSONL"
---

<objective>
Close the STOP-05 verification gap by aligning the requirement description with the implemented behavior, recording the design decision, and marking the requirement satisfied.

Purpose: The verifier found that REQUIREMENTS.md STOP-05 promises a `last_assistant_message` fast-path optimization that was never implemented. The implementation is correct and all 9 test cases pass — the gap is a documentation mismatch, not a code defect. Multi-model quorum (Claude + Codex + Gemini, OpenCode unavailable) reached consensus that option (b) — document the omission — produces better long-term code quality: the fast-path would rely on substring matching of prose text, is not the authoritative source of truth, and adds complexity for negligible performance gain.

Output: Updated REQUIREMENTS.md (STOP-05 description revised + checked) and STATE.md (design decision recorded).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hook-enforcement/01-VERIFICATION.md
@hooks/qgsd-stop.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise STOP-05 in REQUIREMENTS.md to match implementation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, find the STOP-05 line:

    ```
    - [x] **STOP-05**: Stop hook uses `last_assistant_message` as fast-path check before parsing full JSONL
    ```

    Replace it with:

    ```
    - [x] **STOP-05**: Stop hook reads transcript JSONL as the authoritative source of quorum evidence — no fast-path pre-check (design decision: last_assistant_message substring matching is not a reliable signal; JSONL parse is synchronous and correct for all transcript sizes)
    ```

    The checkbox must remain `[x]` (the requirement is now satisfied as redefined).

    Also update the last-updated footer at the bottom of REQUIREMENTS.md. Find the line:
    ```
    *Last updated: 2026-02-20 — META-01/02/03 marked complete after Plan 01-03 (hook enforcement + CLAUDE.md R4 structural note)*
    ```
    Replace it with:
    ```
    *Last updated: 2026-02-20 — STOP-05 revised: fast-path omitted by design; JSONL-only verification is authoritative (gap closure Plan 01-06)*
    ```
  </action>
  <verify>
    ```bash
    grep "STOP-05" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
    ```
    Output must include "authoritative source" and must NOT include "fast-path check before parsing".

    ```bash
    grep -c "\[x\]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
    ```
    Count must remain 17 (all Phase 1 requirements checked — STOP-05 stays checked).
  </verify>
  <done>STOP-05 in REQUIREMENTS.md describes JSONL-only verification, is checked [x], and does not promise last_assistant_message pre-check behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Record design decision in STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
    In `.planning/STATE.md`, find the Decisions section. Append the following decision entry after the last existing decision bullet (the line about "Task 1 produces no git commit..."):

    ```
    - [Phase 01-hook-enforcement gap closure]: STOP-05 fast-path omitted by design — last_assistant_message substring matching is not a reliable signal (Claude could summarize results in prose without naming tool prefixes); transcript JSONL is the authoritative and sole source of quorum evidence. Requirement revised to match implementation. (quorum consensus: Claude + Codex + Gemini; OpenCode unavailable)
    ```

    Also update the last activity line to reflect the gap closure:
    Find:
    ```
    Last activity: 2026-02-20 — Plan 01-05 complete (QGSD hooks installed globally, integration checkpoint auto-approved)
    ```
    Replace with:
    ```
    Last activity: 2026-02-20 — Plan 01-06 complete (STOP-05 gap closure: requirement revised to match JSONL-only implementation)
    ```
  </action>
  <verify>
    ```bash
    grep "STOP-05" /Users/jonathanborduas/code/QGSD/.planning/STATE.md
    ```
    Must return a line containing "fast-path omitted by design".

    ```bash
    grep "01-06" /Users/jonathanborduas/code/QGSD/.planning/STATE.md
    ```
    Must return the updated Last activity line.
  </verify>
  <done>STATE.md Decisions section contains the STOP-05 design decision rationale. Last activity reflects Plan 01-06 completion.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Confirm STOP-05 is checked and describes JSONL-only verification
grep "STOP-05" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md

# Confirm all 17 Phase 1 requirements remain checked
grep -c "\[x\]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md

# Confirm design decision recorded in STATE.md
grep "STOP-05" /Users/jonathanborduas/code/QGSD/.planning/STATE.md

# Confirm hook source code unchanged (the gap closure is documentation only)
node --check /Users/jonathanborduas/code/QGSD/hooks/qgsd-stop.js && echo "hook syntax OK"
node --test /Users/jonathanborduas/code/QGSD/hooks/qgsd-stop.test.js
```

All 9 hook tests must still pass. Hook source must be unmodified.
</verification>

<success_criteria>
- REQUIREMENTS.md STOP-05 is checked [x] and describes "authoritative source" JSONL-only verification
- REQUIREMENTS.md STOP-05 does NOT mention "fast-path check before parsing full JSONL"
- Total checked requirements in REQUIREMENTS.md remains 17
- STATE.md Decisions contains the STOP-05 design decision with rationale
- hooks/qgsd-stop.js is NOT modified (gap closure is documentation only)
- All 9 tests in hooks/qgsd-stop.test.js still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-hook-enforcement/01-06-SUMMARY.md`
</output>
