---
phase: 01-hook-enforcement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03

must_haves:
  truths:
    - "CLAUDE.md R4 (discuss-phase pre-filter) describes that quorum auto-resolves questions before escalating to user — this behavior is structurally enforced by the hooks built in Plans 01 and 02"
    - "CLAUDE.md R4 decision table includes the auto-resolution mechanism and its consensus/no-consensus branches"
    - "STATE.md records that META-01/02/03 are satisfied by hook enforcement, not behavioral instruction alone"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated R4 that explicitly describes quorum auto-resolution before user escalation"
      contains: "auto-resolved"
    - path: ".planning/STATE.md"
      provides: "META behavior note in Accumulated Context > Decisions"
      contains: "META-01"
  key_links:
    - from: "CLAUDE.md R4"
      to: "discuss-phase workflow"
      via: "structural hook enforcement (Plans 01-02)"
      pattern: "discuss-phase"
---

<objective>
Update CLAUDE.md R4 to explicitly describe how quorum auto-resolves discuss-phase questions before escalation, and record in STATE.md that this META behavior is satisfied structurally via the hooks rather than by instruction alone.

Purpose: META-01/02/03 require that this repo's own GSD planning uses quorum to auto-answer questions (only escalating unresolved ones). R4 already defines the behavior, but it does not explicitly name the auto-resolution decision table columns or the "assumptions before questions" output requirement. This plan makes the policy unambiguous so that the hooks built in Plans 01 and 02 correctly satisfy it.

Output: Updated CLAUDE.md (R4 section) and updated STATE.md (Decisions block).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md R4 to explicitly name the auto-resolution behavior</name>
  <files>CLAUDE.md</files>
  <action>
    Read the existing CLAUDE.md R4 section. R4 currently describes the discuss-phase pre-filter with a four-step process and a decision table. It instructs Claude to answer questions independently, query models, and apply a decision table with three outcomes.

    The current R4 decision table has these outcome rows:
    - CONSENSUS reached → Skip the question. Record it as an assumption.
    - No consensus → Run deliberation (R3.3, up to 3 rounds).
    - Still no consensus after deliberation → Present the question to the user.

    And the current R4 also has:
    - Step 4: Show user ONLY questions where consensus failed
    - Step 5: Precede those questions with auto-resolved assumptions

    The existing text already captures the behavior. The only required update is to add an explicit note that makes the structural enforcement connection clear: that /gsd:discuss-phase is in the UserPromptSubmit hook allowlist (so Claude receives quorum instructions before processing it) and is also gate-checked by the Stop hook.

    Add the following note as a new paragraph at the END of the R4 section (after step 5), before the next section:

    ```
    > **Structural enforcement note:** `/gsd:discuss-phase` is in the QGSD hook allowlist. The UserPromptSubmit hook injects quorum instructions before Claude processes this command, and the Stop hook verifies quorum evidence before Claude delivers the filtered question list. This makes the R4 pre-filter structurally enforced — not instruction-dependent.
    ```

    This is a small, targeted addition. Do NOT rewrite R4 or modify any other section. The existing R4 logic (steps 1-5, the decision table, the "auto-resolved questions as assumptions" requirement) already satisfies META-01/02/03. The addition simply makes the enforcement mechanism explicit.

    Important: preserve all existing markdown formatting in CLAUDE.md exactly. Only append the note paragraph at the end of R4, before the next `---` divider or heading.
  </action>
  <verify>
    ```bash
    grep -n "Structural enforcement note" CLAUDE.md
    ```
    Expected: one line found in the R4 section.

    ```bash
    grep -n "auto-resolved\|assumptions" CLAUDE.md | head -10
    ```
    Expected: both terms present in R4 section.

    Verify no other sections were modified:
    ```bash
    git diff CLAUDE.md | head -40
    ```
    Expected: diff shows only the one added paragraph in R4 section.
  </verify>
  <done>
    - CLAUDE.md R4 has the structural enforcement note appended
    - No other sections in CLAUDE.md are modified
    - The note references /gsd:discuss-phase, the UserPromptSubmit hook, and the Stop hook
    - File is valid markdown (no broken headings or formatting)
  </done>
</task>

<task type="auto">
  <name>Task 2: Record META behavior in STATE.md Decisions block</name>
  <files>.planning/STATE.md</files>
  <action>
    Read the existing .planning/STATE.md. Locate the `### Decisions` section under `## Accumulated Context`.

    Add the following decision entry to the bottom of the existing Decisions bullet list:

    ```
    - META behavior (META-01/02/03): discuss-phase question auto-resolution is satisfied structurally — /gsd:discuss-phase is in the QGSD hook allowlist, so quorum runs before output delivery. Only questions without consensus are escalated to the user; auto-resolved questions are presented as assumptions first. This is enforced by hooks, not by behavioral instruction.
    ```

    Do not modify any other section of STATE.md. The existing entries (Architecture, Hook installation, Fail-open, Global install only) stay exactly as-is.
  </action>
  <verify>
    ```bash
    grep -n "META-01\|META behavior" .planning/STATE.md
    ```
    Expected: the new decision entry is found.

    ```bash
    git diff .planning/STATE.md | head -20
    ```
    Expected: diff shows only the one new bullet in the Decisions section.
  </verify>
  <done>
    - STATE.md Decisions section has the META behavior entry
    - Entry references META-01/02/03 by ID
    - No other STATE.md sections modified
  </done>
</task>

</tasks>

<verification>
```bash
# Both files have the expected content
grep "Structural enforcement note" CLAUDE.md && echo "CLAUDE.md: OK"
grep "META-01" .planning/STATE.md && echo "STATE.md: OK"

# Verify diff scope is minimal
git diff --stat CLAUDE.md .planning/STATE.md
```
Expected output: CLAUDE.md shows +3-5 lines in diff, STATE.md shows +3-5 lines. No other files modified.
</verification>

<success_criteria>
- CLAUDE.md R4 section has structural enforcement note appended (references hooks, /gsd:discuss-phase)
- STATE.md Decisions block has META-01/02/03 entry
- No other content in either file is modified
- git diff shows minimal, targeted changes only in R4 and Decisions sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-hook-enforcement/01-03-SUMMARY.md`
</output>
