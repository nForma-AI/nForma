---
phase: 12-version-publish
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified: []
autonomous: true
requirements:
  - RLS-03
  - RLS-04

must_haves:
  truths:
    - "Git tag v0.2.0 exists in the local repository"
    - "Git tag v0.2.0 has been pushed to the remote (origin)"
    - "qgsd@0.2.0 is live on the npm registry"
    - "npm view qgsd@0.2.0 returns package metadata including version, description, and dist.tarball"
    - "The tag points to the version bump commit from Plan 01, not to any npm publish commit"
  artifacts:
    - path: ".git/refs/tags/v0.2.0"
      provides: "Local git tag v0.2.0"
      contains: "SHA of the chore(12-01) commit"
  key_links:
    - from: "git tag v0.2.0"
      to: "remote origin"
      via: "git push origin v0.2.0"
      pattern: "v0.2.0"
    - from: "npm publish"
      to: "npm registry"
      via: "package.json version 0.2.0 + prepublishOnly runs npm run build:hooks"
      pattern: "qgsd@0.2.0"
---

<objective>
Create git tag v0.2.0 on the release commit from Plan 01, push it to remote, then publish qgsd@0.2.0 to the npm registry. After publish, verify the package is live.

Purpose: Ship qgsd@0.2.0 so `npx qgsd@0.2.0` resolves from the npm registry. The tag records the exact source commit that corresponds to the published artifact.
Output: Tag v0.2.0 on remote, qgsd@0.2.0 on npm, npm view confirmation.

IMPORTANT ORDERING: The tag MUST be created before `npm publish` is run, and it MUST point to the Plan 01 version-bump commit. npm publish itself does not create a commit — so there is no risk of the tag pointing to the wrong commit as long as no new commit is made between tagging and publishing.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-version-publish/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and push git tag v0.2.0</name>
  <files></files>
  <action>
    Tag the current HEAD (which is the chore(12-01) commit from Plan 01) with v0.2.0, then push the tag to the remote.

    PRECONDITION CHECK: Confirm HEAD is the Plan 01 commit before tagging:
      git log --oneline -1
    The message must contain "chore(12-01): bump version to 0.2.0". If it does not, STOP and report — do not create the tag on the wrong commit.

    If precondition passes, run:
      git tag v0.2.0
      git push origin v0.2.0

    The tag is a lightweight tag pointing to HEAD. An annotated tag is not required here — lightweight is correct for this release workflow.

    After pushing, verify:
      git tag -l v0.2.0
      (must output "v0.2.0")
      git ls-remote origin refs/tags/v0.2.0
      (must output a SHA + refs/tags/v0.2.0 line)
  </action>
  <verify>
    git tag -l v0.2.0 outputs "v0.2.0"
    git ls-remote origin refs/tags/v0.2.0 returns a non-empty line with the tag SHA
  </verify>
  <done>Tag v0.2.0 exists locally and on the remote origin pointing to the chore(12-01) commit (RLS-03 satisfied)</done>
</task>

<task type="auto">
  <name>Task 2: Publish qgsd@0.2.0 to npm</name>
  <files></files>
  <action>
    Publish the package to the public npm registry. This step is irreversible — verify the tag and version one more time before running publish.

    PRE-PUBLISH CHECKLIST (verify each before proceeding):
    1. `cat /Users/jonathanborduas/code/QGSD/package.json | grep '"version"'` → must show "0.2.0"
    2. `git tag -l v0.2.0` → must show "v0.2.0"
    3. `npm whoami` → must return a logged-in npm username (not an error). If not logged in, run `npm login` before publish.

    If all checks pass, run:
      npm publish --access public

    The `prepublishOnly` script in package.json runs `npm run build:hooks` automatically — hooks/dist/ will be rebuilt as part of the publish step. This is correct behavior; the dist was already rebuilt in Phase 11 so the rebuild will be a no-op diff, but running it again is safe.

    The publish command will:
    - Bundle the files listed in package.json "files" array: bin/, commands/, get-shit-done/, agents/, hooks/dist/, scripts/, templates/
    - Upload as qgsd@0.2.0 to the public npm registry
    - Output a "+" line on success: `+ qgsd@0.2.0`

    After publish succeeds, run the verification step.
  </action>
  <verify>
    npm view qgsd@0.2.0 version
    (must output "0.2.0")
    npm view qgsd@0.2.0 dist.tarball
    (must output a tarball URL containing "qgsd-0.2.0")
  </verify>
  <done>qgsd@0.2.0 is live on the npm registry — `npm view qgsd@0.2.0` returns package metadata (RLS-04 satisfied)</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md, REQUIREMENTS.md, and STATE.md to mark Phase 12 complete</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Mark the phase and all RLS requirements as complete across planning documents.

    In ROADMAP.md:
    - Change `[ ] **Phase 12: Version & Publish**` to `[x] **Phase 12: Version & Publish**`
    - Update the Phase 12 progress table row: change "Not started" to "Complete" and add "2026-02-21" date
    - Update Phase 12 Plans count from "0/TBD" to "2/2"

    In REQUIREMENTS.md:
    - Change `- [ ] **RLS-01**:` to `- [x] **RLS-01**:`
    - Change `- [ ] **RLS-02**:` to `- [x] **RLS-02**:`
    - Change `- [ ] **RLS-03**:` to `- [x] **RLS-03**:`
    - Change `- [ ] **RLS-04**:` to `- [x] **RLS-04**:`
    - Update traceability table: change all four RLS rows from "Pending" to "Complete"

    In STATE.md:
    - Update "Current Position" section: Phase 12, Plan 02 complete (2/2 plans done — Phase 12 COMPLETE)
    - Update "Status" line: Phase 12 complete — RLS-01..04 all satisfied; qgsd@0.2.0 published to npm
    - Update "Last activity": 2026-02-21 — Phase 12 Plan 02 complete; tag v0.2.0 pushed, npm publish succeeded
    - Update progress bar: 12/13 phases complete (92%) — increment from 77%
    - Update "Stopped at": Completed 12-02-PLAN.md — git tag v0.2.0 pushed to remote, qgsd@0.2.0 published to npm registry; v0.2 milestone complete. Phase 13 (Circuit Breaker Oscillation Resolution Mode) is already complete.

    Commit all three files:
      git add /Users/jonathanborduas/code/QGSD/.planning/ROADMAP.md /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md /Users/jonathanborduas/code/QGSD/.planning/STATE.md
      git commit -m "docs(12-02): mark Phase 12 complete — qgsd@0.2.0 published"
  </action>
  <verify>
    grep "RLS-04" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[x\]"
    (must match — confirms all RLS requirements marked complete)
    grep "Phase 12" /Users/jonathanborduas/code/QGSD/.planning/ROADMAP.md | grep "\[x\]"
    (must match — confirms phase marked complete)
  </verify>
  <done>ROADMAP.md shows Phase 12 [x] complete, REQUIREMENTS.md shows RLS-01..04 all [x], STATE.md shows current position at Phase 12 plan 02 complete. Docs commit exists.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `git tag -l v0.2.0` outputs "v0.2.0"
2. `git ls-remote origin refs/tags/v0.2.0` returns non-empty (tag is on remote)
3. `npm view qgsd@0.2.0 version` outputs "0.2.0"
4. `npm view qgsd@0.2.0 dist.tarball` outputs a URL containing "qgsd-0.2.0"
5. `grep '\[x\].*RLS-04' .planning/REQUIREMENTS.md` matches
</verification>

<success_criteria>
- Git tag v0.2.0 exists and is pushed to remote (RLS-03 satisfied)
- `npm view qgsd@0.2.0` returns package metadata — qgsd@0.2.0 is live on npm (RLS-04 satisfied)
- ROADMAP.md, REQUIREMENTS.md, and STATE.md reflect Phase 12 complete
- All 4 RLS requirements closed; all 8 v0.3 requirements (CL-01, CL-02, BLD-01, BLD-02, RLS-01..04) are satisfied
</success_criteria>

<output>
After completion, create `/Users/jonathanborduas/code/QGSD/.planning/phases/12-version-publish/12-02-SUMMARY.md`
</output>
