---
phase: 11-changelog-build
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - hooks/dist/qgsd-circuit-breaker.js
  - hooks/dist/qgsd-stop.js
  - hooks/dist/qgsd-prompt.js
  - hooks/dist/config-loader.js
  - hooks/dist/gsd-check-update.js
  - hooks/dist/gsd-statusline.js
autonomous: true
requirements:
  - BLD-01
  - BLD-02

must_haves:
  truths:
    - "hooks/dist/ contains rebuilt files that match current source in hooks/"
    - "npm test exits with 0 failures across all four test suites"
    - "The dist rebuild captures Phase 13 changes to buildBlockReason() (commit graph table + R5 reference + module.exports)"
  artifacts:
    - path: "hooks/dist/qgsd-circuit-breaker.js"
      provides: "Circuit breaker hook with Phase 13 buildBlockReason() and module.exports"
      contains: "buildBlockReason"
    - path: "hooks/dist/qgsd-stop.js"
      provides: "Stop hook quorum gate"
      contains: "hasArtifactCommit"
    - path: "hooks/dist/config-loader.js"
      provides: "Shared config loader with circuit_breaker sub-key validation"
      contains: "circuit_breaker"
  key_links:
    - from: "hooks/qgsd-circuit-breaker.js"
      to: "hooks/dist/qgsd-circuit-breaker.js"
      via: "npm run build:hooks (scripts/build-hooks.js copyFileSync)"
      pattern: "diff.*empty"
    - from: "npm test"
      to: "0 failures"
      via: "node --test on all four test files"
      pattern: "pass.*141"
---

<objective>
Rebuild hooks/dist/ from current source and validate that npm test passes with zero failures across all test suites.

Purpose: BLD-01 and BLD-02 gate Phase 12 (version bump and npm publish). The dist files are what actually get installed on user machines via npm — stale dist means Phase 13 changes (buildBlockReason commit graph, module.exports) never reach users.
Output: hooks/dist/ synchronized with source; npm test reports 0 failures.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-changelog-build/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild hooks/dist/ via npm run build:hooks</name>
  <files>
    hooks/dist/qgsd-circuit-breaker.js
    hooks/dist/qgsd-stop.js
    hooks/dist/qgsd-prompt.js
    hooks/dist/config-loader.js
    hooks/dist/gsd-check-update.js
    hooks/dist/gsd-statusline.js
  </files>
  <action>
    Run the build script from the repo root:

      npm run build:hooks

    This executes `node scripts/build-hooks.js` which copies all six files from hooks/ to hooks/dist/:
    - gsd-check-update.js
    - gsd-statusline.js
    - qgsd-prompt.js
    - qgsd-stop.js
    - config-loader.js
    - qgsd-circuit-breaker.js

    The build script uses fs.copyFileSync — no bundling, pure copy. The current hooks/dist/ is stale:
    the dist/qgsd-circuit-breaker.js does NOT have Phase 13 changes (buildBlockReason with commit
    graph table, require.main guard, module.exports). After rebuilding, dist must match source exactly.

    After the build, verify each critical file matches source:
      diff hooks/qgsd-circuit-breaker.js hooks/dist/qgsd-circuit-breaker.js
      diff hooks/qgsd-stop.js hooks/dist/qgsd-stop.js
      diff hooks/config-loader.js hooks/dist/config-loader.js
      diff hooks/qgsd-prompt.js hooks/dist/qgsd-prompt.js

    All diffs must produce no output (empty = identical).

    Also confirm Phase 13 content is now in dist:
      grep -c "buildBlockReason" hooks/dist/qgsd-circuit-breaker.js
      grep -c "module.exports" hooks/dist/qgsd-circuit-breaker.js
      grep -c "Oscillation Resolution Mode" hooks/dist/qgsd-circuit-breaker.js

    Commit the rebuilt dist files:
      git add hooks/dist/
      git commit -m "build(11-02): rebuild hooks/dist/ from current source"

    Note: hooks/dist/ is gitignored by default — check .gitignore first. If dist is gitignored,
    force-add it:
      git add -f hooks/dist/

    Check with: `git check-ignore -v hooks/dist/qgsd-circuit-breaker.js`
    If gitignored, use: `git add -f hooks/dist/`
  </action>
  <verify>
    Run these checks to confirm dist matches source:
    1. `diff hooks/qgsd-circuit-breaker.js hooks/dist/qgsd-circuit-breaker.js` — empty output
    2. `diff hooks/qgsd-stop.js hooks/dist/qgsd-stop.js` — empty output
    3. `diff hooks/config-loader.js hooks/dist/config-loader.js` — empty output
    4. `grep "module.exports" hooks/dist/qgsd-circuit-breaker.js` — should match
    5. `grep "Oscillation Resolution Mode" hooks/dist/qgsd-circuit-breaker.js` — should match
    6. `grep "commit graph\|commit_window_snapshot" hooks/dist/qgsd-circuit-breaker.js` — should match
  </verify>
  <done>
    hooks/dist/ contains all six hook files rebuilt from current source. hooks/dist/qgsd-circuit-breaker.js contains Phase 13 changes (commit graph rendering, Oscillation Resolution Mode reference, module.exports). All diffs between source and dist are empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run npm test and confirm 0 failures</name>
  <files></files>
  <action>
    Run the full test suite:

      npm test

    This runs:
      node --test hooks/qgsd-stop.test.js hooks/config-loader.test.js get-shit-done/bin/gsd-tools.test.cjs hooks/qgsd-circuit-breaker.test.js

    Expected result: 141 tests passing, 0 failing across all four suites:
    - hooks/qgsd-stop.test.js: 19 tests
    - hooks/config-loader.test.js: 10 tests (including TC-CB1 through TC-CB8)
    - get-shit-done/bin/gsd-tools.test.cjs: 81 tests (Phase 10 confirmed this count)
    - hooks/qgsd-circuit-breaker.test.js: 31 tests (15 Phase 6 + 4 Phase 7 CB-TC + 3 Phase 13 CB-TC-BR + Phase 10 additions)

    Note: Phase 10 STATE.md confirmed 141 total tests passing. If the count has changed due to
    Phase 13 additions, accept the new count as long as 0 tests fail.

    If any test fails:
    1. Read the failure output carefully
    2. Identify which test file and test name
    3. Check if the failure is in the circuit breaker test (likely CB-TC17 or CB-TC-BR assertions)
    4. Do NOT modify tests to make them pass — fix the source if the source is wrong
    5. If a Phase 13 test is failing against the source, it means dist was not correctly rebuilt
       (run `diff hooks/qgsd-circuit-breaker.js hooks/dist/qgsd-circuit-breaker.js` again)

    After confirming 0 failures, commit the test result evidence:
      git add -f hooks/dist/  (if not already staged)

    Create a final commit for Phase 11 completion if Task 1 commit does not already cover dist:
      git commit -m "test(11-02): npm test passes 141/141 — BLD-02 satisfied"

    (If all tests pass and Task 1 already committed the dist rebuild, no additional commit needed.)
  </action>
  <verify>
    `npm test` exit code is 0. Output shows no failures. All four test files report pass.
  </verify>
  <done>
    npm test exits with code 0. 0 test failures across all four suites. BLD-02 is satisfied.
  </done>
</task>

</tasks>

<verification>
1. `diff hooks/qgsd-circuit-breaker.js hooks/dist/qgsd-circuit-breaker.js` — empty (identical)
2. `diff hooks/qgsd-stop.js hooks/dist/qgsd-stop.js` — empty
3. `diff hooks/config-loader.js hooks/dist/config-loader.js` — empty
4. `npm test` exits 0 with no failures
5. `grep "module.exports" hooks/dist/qgsd-circuit-breaker.js` — match found (Phase 13 change in dist)
6. `grep "Oscillation Resolution Mode" hooks/dist/qgsd-circuit-breaker.js` — match found
</verification>

<success_criteria>
BLD-01: hooks/dist/ rebuilt from current source — all six files in HOOKS_TO_COPY present and identical to source.
BLD-02: npm test passes with 0 failures across all four test suites.
</success_criteria>

<output>
After completion, create `.planning/phases/11-changelog-build/11-02-SUMMARY.md` following the standard summary template.
</output>
