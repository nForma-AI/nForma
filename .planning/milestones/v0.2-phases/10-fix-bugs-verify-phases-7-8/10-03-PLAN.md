---
phase: 10-fix-bugs-verify-phases-7-8
plan: "03"
type: verify
wave: 2
depends_on:
  - "10-01"
files_modified:
  - .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md
autonomous: true
requirements:
  - INST-08
  - INST-09
  - INST-10
  - RECV-01

must_haves:
  truths:
    - "08-VERIFICATION.md exists and has status: passed or status: failed (never missing)"
    - "INST-08 verified: settings.json PreToolUse hook registered after install; and after uninstall, PreToolUse entry is removed"
    - "INST-09 verified: fresh install writes circuit_breaker: { oscillation_depth: 3, commit_window: 6 } to qgsd.json"
    - "INST-10 verified: reinstall with existing circuit_breaker block adds missing sub-keys without overwriting present values"
    - "RECV-01 verified: --reset-breaker with git root resolution deletes .claude/circuit-breaker-state.json from project root"
  artifacts:
    - path: ".planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md"
      provides: "Independent gsd-verifier VERIFICATION.md for Phase 8"
      contains: "INST-08"
  key_links:
    - from: "08-VERIFICATION.md"
      to: "bin/install.js"
      via: "source inspection of PreToolUse registration and uninstall removal"
      pattern: "qgsd-circuit-breaker"
    - from: "08-VERIFICATION.md"
      to: "bin/install.js --reset-breaker handler"
      via: "source inspection of git rev-parse usage"
      pattern: "rev-parse"
---

<objective>
Spawn the gsd-verifier agent to independently verify that Phase 8 (Installer Integration) satisfies requirements INST-08, INST-09, INST-10, and RECV-01 — including the 3 bug fixes applied by Plan 10-01.

Purpose: The v0.2 audit found Phase 8 was executed but never independently verified, and the integration checker identified 3 gaps (INST-08 uninstall gap, RECV-01 path mismatch, INST-10 sub-key backfill). Plan 10-01 fixed all three bugs. This plan produces the formal verification document that confirms the fixes are correct and all Phase 8 requirements are now satisfied.

Output: .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-integration/08-01-PLAN.md
@.planning/phases/08-installer-integration/08-01-SUMMARY.md
@.planning/v0.2-MILESTONE-AUDIT.md
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 8 — Installer Integration (including Plan 10-01 bug fixes)</name>
  <files>.planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md</files>
  <action>
Act as the gsd-verifier agent. Independently verify that bin/install.js satisfies all 4 Phase 8 requirements. These requirements include the 3 bugs fixed by Plan 10-01 — verify the fixed behavior, not the original buggy behavior.

**Requirements to verify:**

INST-08: Installer registers PreToolUse circuit breaker hook in ~/.claude/settings.json alongside existing hooks; AND after uninstall, the PreToolUse hook entry is removed (bug fix: uninstall() now removes the PreToolUse entry)
INST-09: Installer writes default circuit_breaker config block { oscillation_depth: 3, commit_window: 6 } to qgsd.json on first install
INST-10: Reinstall (idempotent) adds missing circuit_breaker config block AND missing individual sub-keys without overwriting user-modified values (bug fix: now backfills individual missing sub-keys)
RECV-01: --reset-breaker CLI flag resolves state file path from git root (not raw process.cwd()), deletes .claude/circuit-breaker-state.json, and logs confirmation (bug fix: now uses git rev-parse --show-toplevel)

**Verification methodology:**

For INST-08 (registration):
- Read bin/install.js: confirm PreToolUse hook registration block exists in the install path (lines ~1714-1724)
- Grep: grep -n "PreToolUse\|qgsd-circuit-breaker" bin/install.js
- Expected: At least one occurrence in the install section (around line 1714) that pushes to settings.hooks.PreToolUse

For INST-08 (uninstall — bug fix from Plan 10-01):
- Read bin/install.js uninstall() function body (lines ~917-1153)
- Grep for PreToolUse removal in the uninstall function:
  node -e "
  const src = require('fs').readFileSync('bin/install.js', 'utf8');
  // Find uninstall function body
  const uninstallStart = src.indexOf('function uninstall(');
  const uninstallEnd = src.indexOf('\nfunction ', uninstallStart + 1);
  const uninstallBody = src.slice(uninstallStart, uninstallEnd > 0 ? uninstallEnd : src.length);
  console.log('has PreToolUse removal:', uninstallBody.includes('PreToolUse'));
  console.log('has circuit-breaker filter:', uninstallBody.includes('qgsd-circuit-breaker'));
  "
- Expected: both true

For INST-09:
- Read bin/install.js fresh install path: confirm the qgsdConfig object written on first install contains circuit_breaker: { oscillation_depth: 3, commit_window: 6 }
- Grep: grep -A5 "INST-09" bin/install.js
- Expected: oscillation_depth: 3 and commit_window: 6 appear in the fresh install config block

For INST-10 (bug fix from Plan 10-01 — sub-key backfill):
- Read bin/install.js INST-10 block: confirm the else branch checks individual sub-keys
  node -e "
  const src = require('fs').readFileSync('bin/install.js', 'utf8');
  console.log('has sub-key backfill:', src.includes('oscillation_depth === undefined'));
  console.log('has commit_window backfill:', src.includes('commit_window === undefined'));
  "
- Expected: both true
- Verify logic: a user config with only { oscillation_depth: 5 } would enter the else branch, keep oscillation_depth: 5, and add commit_window: 6. A config with both keys set skips the entire block.

For RECV-01 (bug fix from Plan 10-01 — git root resolution):
- Read bin/install.js --reset-breaker handler: confirm it uses git rev-parse --show-toplevel
  node -e "
  const src = require('fs').readFileSync('bin/install.js', 'utf8');
  const resetBlock = src.match(/if \(hasResetBreaker\)([\s\S]*?)process\.exit\(0\)/);
  if (resetBlock) {
    const block = resetBlock[0];
    console.log('uses git rev-parse:', block.includes('rev-parse'));
    console.log('uses projectRoot:', block.includes('projectRoot'));
    console.log('state file from projectRoot:', block.includes('path.join(projectRoot'));
    console.log('fallback to cwd:', block.includes('process.cwd()'));
  }
  "
- Expected: first three true; last also true (confirming fallback behavior is preserved)

**Functional test for RECV-01:**
Run the actual --reset-breaker command to confirm it works from a subdirectory simulation:
  node -e "
  const {spawnSync} = require('child_process');
  const os = require('os'); const path = require('path'); const fs = require('fs');
  // Create a temp git repo
  const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'qgsd-recv01-'));
  spawnSync('git', ['init'], {cwd: tmp});
  spawnSync('git', ['commit', '--allow-empty', '-m', 'init'], {cwd: tmp, env: {...process.env, GIT_AUTHOR_NAME:'t', GIT_AUTHOR_EMAIL:'t@t', GIT_COMMITTER_NAME:'t', GIT_COMMITTER_EMAIL:'t@t'}});
  // Write state file at git root
  fs.mkdirSync(path.join(tmp, '.claude'));
  const stateFile = path.join(tmp, '.claude', 'circuit-breaker-state.json');
  fs.writeFileSync(stateFile, JSON.stringify({active:true}));
  // Create subdirectory and run reset from there
  const subdir = path.join(tmp, 'src');
  fs.mkdirSync(subdir);
  const result = spawnSync('node', [path.resolve('bin/install.js'), '--reset-breaker'], {cwd: subdir, encoding: 'utf8'});
  const cleared = !fs.existsSync(stateFile);
  console.log('state cleared from subdir:', cleared);
  console.log('stdout includes cleared:', result.stdout.includes('cleared') || result.stdout.includes('Circuit breaker'));
  fs.rmSync(tmp, {recursive:true});
  "
Expected: state cleared from subdir: true, stdout includes cleared message.

**Run test suite:**
- Run: npm test
- Expected: 138 tests pass

**Write the VERIFICATION.md:**

Create .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md with this structure:

```
---
phase: 08-installer-integration
verified: {today's date}
verifier: gsd-verifier (spawned from Phase 10 Plan 03)
status: passed|failed
requirements_verified: [INST-08, INST-09, INST-10, RECV-01]
bug_fixes_verified:
  - "INST-08 uninstall: uninstall() now removes PreToolUse hook (Plan 10-01)"
  - "RECV-01 path mismatch: --reset-breaker now uses git root (Plan 10-01)"
  - "INST-10 sub-key backfill: reinstall now backfills missing sub-keys (Plan 10-01)"
---

# Phase 8 Verification Report

## Requirements

| Req ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| INST-08 (install) | ... | PASS/FAIL | ... |
| INST-08 (uninstall fix) | ... | PASS/FAIL | ... |
| INST-09 | ... | PASS/FAIL | ... |
| INST-10 (sub-key fix) | ... | PASS/FAIL | ... |
| RECV-01 (path fix) | ... | PASS/FAIL | ... |

## Test Suite

| Suite | Tests | Status |
|-------|-------|--------|
| npm test | 138/138 | PASS |

## Key Evidence

[grep outputs, node -e outputs, functional test results]

## Bug Fixes Verified

[For each of the 3 bugs from audit: confirm fix is in place]

## Verdict

PASSED (N/4 requirements verified) or FAILED with details.
```

Set status: passed only if ALL 4 requirements (including bug fix behaviors) pass.
  </action>
  <verify>
Run: ls .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md
Expected: File exists.

Run: grep "status:" .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md | head -1
Expected: status: passed or status: failed.

Run: grep "INST-08\|INST-09\|INST-10\|RECV-01" .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md | wc -l
Expected: 4 or more lines (each requirement appears at least once).
  </verify>
  <done>08-VERIFICATION.md exists in the Phase 10 output directory. It contains verification results for all 4 Phase 8 requirements (INST-08, INST-09, INST-10, RECV-01) with a top-level status field (passed or failed), a per-requirement table with evidence, and explicit confirmation that the 3 bug fixes from Plan 10-01 are in place.</done>
</task>

</tasks>

<verification>
Run: ls -la .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md
Expected: File exists with nonzero size.

Run: grep -c "PASS\|FAIL" .planning/phases/10-fix-bugs-verify-phases-7-8/08-VERIFICATION.md
Expected: 4 or more (each requirement has a verdict).
</verification>

<success_criteria>
- 08-VERIFICATION.md exists in .planning/phases/10-fix-bugs-verify-phases-7-8/
- All 4 Phase 8 requirements are explicitly verified (INST-08, INST-09, INST-10, RECV-01)
- Each requirement has a PASS or FAIL verdict with actual evidence
- The 3 bug fixes from Plan 10-01 are explicitly confirmed in the verification evidence
- Top-level status field reflects overall result
- npm test evidence included (138 tests expected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-bugs-verify-phases-7-8/10-03-SUMMARY.md`
</output>
