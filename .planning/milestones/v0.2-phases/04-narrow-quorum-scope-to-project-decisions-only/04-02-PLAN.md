---
phase: 04-narrow-quorum-scope-to-project-decisions-only
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - hooks/qgsd-prompt.js
autonomous: true
requirements:
  - SCOPE-04

must_haves:
  truths:
    - "The injected quorum instructions include an explicit instruction for Claude to include <!-- GSD_DECISION --> in its final output when delivering a project decision"
    - "The decision marker instruction names the exact token <!-- GSD_DECISION --> and explains when to include it (final output only, not intermediate messages)"
    - "The DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK constant includes the decision marker instruction as a new numbered step"
    - "The existing injection mechanism (hookSpecificOutput.additionalContext) is unchanged"
  artifacts:
    - path: "hooks/qgsd-prompt.js"
      provides: "Updated DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK with decision marker step"
      contains: "GSD_DECISION"
  key_links:
    - from: "hooks/qgsd-prompt.js"
      to: "hooks/qgsd-stop.js DECISION_MARKER constant"
      via: "Identical token '<!-- GSD_DECISION -->' used in both files"
      pattern: "GSD_DECISION"
---

<objective>
Update the UserPromptSubmit hook to instruct Claude to include the decision marker `<!-- GSD_DECISION -->` in its final output when delivering a project decision, providing the behavioral backstop signal that complements the structural artifact commit detection.

Purpose: Without this injection, the Stop hook's GUARD 5 `hasDecisionMarker()` backstop has no behavioral input — Claude won't know to include the token. This is especially critical for `/gsd:verify-work` and other commands where no file artifact is committed.

Output: Updated `hooks/qgsd-prompt.js` with decision marker instruction added to the injected quorum context.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-narrow-quorum-scope-to-project-decisions-only/04-RESEARCH.md
@hooks/qgsd-prompt.js
@.planning/phases/04-narrow-quorum-scope-to-project-decisions-only/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decision marker instruction to UserPromptSubmit injection</name>
  <files>hooks/qgsd-prompt.js</files>
  <action>
    Update `DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK` in hooks/qgsd-prompt.js to include a new numbered step instructing Claude to include the decision marker `<!-- GSD_DECISION -->` in its final output.

    The current fallback is:
    ```
    QUORUM REQUIRED (structural enforcement — Stop hook will verify)

    Before presenting any planning output to the user, you MUST:
      1. Call mcp__codex-cli__review with the full plan content
      2. Call mcp__gemini-cli__gemini with the full plan content
      3. Call mcp__opencode__opencode with the full plan content
      4. Present all model responses, resolve any concerns, then deliver your final output

    Fail-open: if a model is UNAVAILABLE (quota/error), note it and proceed with available models.
    The Stop hook reads the transcript — skipping quorum will block your response.
    ```

    Update it to add step 5:
    ```
    QUORUM REQUIRED (structural enforcement — Stop hook will verify)

    Before presenting any planning output to the user, you MUST:
      1. Call mcp__codex-cli__review with the full plan content
      2. Call mcp__gemini-cli__gemini with the full plan content
      3. Call mcp__opencode__opencode with the full plan content
      4. Present all model responses, resolve any concerns, then deliver your final output
      5. Include the token <!-- GSD_DECISION --> somewhere in your FINAL output (not in intermediate messages or status updates — only when you are delivering the completed plan, research, verification report, or filtered question list to the user)

    Fail-open: if a model is UNAVAILABLE (quota/error), note it and proceed with available models.
    The Stop hook reads the transcript — skipping quorum will block your response.
    ```

    IMPORTANT: The token must match EXACTLY: `<!-- GSD_DECISION -->` — same value as the DECISION_MARKER constant in qgsd-stop.js (defined in Plan 01). Do not paraphrase or alter the token.

    The quorum_instructions field in qgsd.json (config file) is user-customizable. The DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK is only used when the config has no quorum_instructions field. Users who have customized their config will need to add the decision marker instruction manually — this is acceptable (documented in the existing fail-open philosophy).

    Do not change the injection mechanism, the cmdPattern logic, or any other part of qgsd-prompt.js.
  </action>
  <verify>
    1. Inspect the updated DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK to confirm step 5 is present with the exact token `<!-- GSD_DECISION -->`.
    2. Confirm the injected text includes the phrase "only when you are delivering the completed plan" (or equivalent intent-clarifying language) so Claude understands NOT to include it in intermediate messages.
    3. Run the existing config-loader tests to confirm no regressions:
       ```bash
       node --test hooks/config-loader.test.js
       ```
    4. Run the stop hook tests to confirm no regressions:
       ```bash
       node --test hooks/qgsd-stop.test.js
       ```
    (Neither test file imports qgsd-prompt.js, but running them confirms no shared-module breakage.)
  </verify>
  <done>
    - DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK contains step 5 with exact token `<!-- GSD_DECISION -->`
    - The instruction specifies "FINAL output only" to prevent marker inclusion in intermediate messages
    - node --test hooks/config-loader.test.js passes
    - node --test hooks/qgsd-stop.test.js passes (19/19)
  </done>
</task>

</tasks>

<verification>
Verify the updated injection:

```bash
node -e "const p = require('./hooks/qgsd-prompt.js'); " 2>&1 || true
grep -c "GSD_DECISION" hooks/qgsd-prompt.js
```

Expected: grep returns 1 (token appears exactly once in the file).

Verify no test regressions:
```bash
node --test hooks/qgsd-stop.test.js
node --test hooks/config-loader.test.js
```

Both must exit 0 with all tests passing.
</verification>

<success_criteria>
- hooks/qgsd-prompt.js contains `<!-- GSD_DECISION -->` in DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK
- Step 5 clarifies: token goes in FINAL output, not intermediate messages
- All existing tests pass
- Injection mechanism (hookSpecificOutput.additionalContext) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrow-quorum-scope-to-project-decisions-only/04-02-SUMMARY.md`
</output>
