---
phase: 14-activity-tracking
plan: 04
type: execute
wave: 3
depends_on:
  - 14-02
  - 14-03
files_modified:
  - /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
  - /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
autonomous: true
requirements:
  - ACT-04

must_haves:
  truths:
    - "resume-work reads .planning/current-activity.json and displays the interrupted activity state before presenting options"
    - "resume-work routes to the correct recovery command based on sub_activity: e.g., 'was in debug round 2 of plan 14-03' → suggest /qgsd:execute-phase 14"
    - "resume-work gracefully handles missing current-activity.json (no file = no mid-workflow interruption) and continues its normal flow"
    - "resume-work shows the interrupted phase, plan, sub_activity, and timestamp to the user in the PROJECT STATUS display"
  artifacts:
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md"
      provides: "resume-work with activity-aware recovery routing"
      contains: "activity-get"
    - path: "/Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md"
      provides: "get-shit-done version with activity routing"
      contains: "activity-get"
  key_links:
    - from: "resume-project.md initialize step"
      to: "gsd-tools.cjs activity-get"
      via: "Bash call during initialization to read current-activity.json"
      pattern: "activity-get"
    - from: "resume-project.md present_status step"
      to: "activity data"
      via: "Display of interrupted activity in PROJECT STATUS block"
      pattern: "sub_activity"
    - from: "resume-project.md determine_next_action step"
      to: "recovery routing table"
      via: "sub_activity value determines suggested recovery command"
      pattern: "executing_plan.*execute-phase"
---

<objective>
Update `resume-project.md` to read `.planning/current-activity.json` and route the user to the exact interrupted workflow stage. After this plan, "resuming" after a mid-debug-loop computer restart shows the user exactly what was interrupted and the right command to recover.

Purpose: This is the payoff of the entire phase — all the activity tracking in Plans 14-01 through 14-03 feeds into this single read. Resume-work gains surgical precision instead of relying on SUMMARY.md last-committed state.

Output: Updated `resume-project.md` in both installed and source locations. The initialize step reads activity-get, present_status displays it, and determine_next_action routes based on sub_activity.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity-get read and routing table to resume-project.md (qgsd version)</name>
  <files>/Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md</files>
  <action>
    Read the current file at `/Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md`.

    Make these three targeted additions:

    **Addition 1 — In `initialize` step**, after the existing INIT bash call and before "If `state_exists` is true", add:

    ```bash
    # Check for mid-workflow interruption
    ACTIVITY=$(node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs activity-get)
    ```

    Then parse ACTIVITY: if the result is `{}` or empty, set `HAS_ACTIVITY=false`. Otherwise `HAS_ACTIVITY=true` and extract `activity`, `sub_activity`, `phase`, `plan`, `debug_round`, `quorum_round`, `checkpoint`, `updated` fields.

    **Addition 2 — In `present_status` step**, add a new section inside the PROJECT STATUS box output, between "Last activity" and the closing box border:

    ```
    [If HAS_ACTIVITY is true:]

    ⚡ Mid-workflow interruption detected:
        Activity:  [activity] / [sub_activity]
        Phase:     [phase if present]
        Plan:      [plan if present]
        [If debug_round present:] Debug round: [debug_round]/3
        [If quorum_round present:] Quorum round: [quorum_round]
        [If checkpoint present:] Checkpoint: [checkpoint]
        Interrupted: [updated timestamp]
    ```

    **Addition 3 — In `determine_next_action` step**, add this routing block as the FIRST priority (before "If interrupted agent exists"):

    ```
    **If HAS_ACTIVITY is true (mid-workflow interruption):**
    Route based on `activity` + `sub_activity`:

    | sub_activity | Recovery |
    |---|---|
    | executing_plan | `/qgsd:execute-phase {phase}` — executor will skip completed plans and resume from the interrupted plan |
    | checkpoint_verify | `/qgsd:execute-phase {phase}` — re-enter checkpoint:verify for plan {plan} |
    | debug_loop | `/qgsd:execute-phase {phase}` — re-enter debug loop (was on round {debug_round}) |
    | awaiting_human_verify | `/qgsd:execute-phase {phase}` — checkpoint is waiting for human approval |
    | verifying_phase | `/qgsd:execute-phase {phase}` — verifier was running, re-trigger phase verification |
    | researching | `/qgsd:plan-phase {phase}` — researcher was running, re-trigger with --research flag |
    | planning | `/qgsd:plan-phase {phase}` — planner was running, re-trigger plan-phase |
    | checking_plan | `/qgsd:plan-phase {phase}` — checker was running, re-trigger plan-phase |
    | quorum | `/qgsd:plan-phase {phase}` — QUORUM was in progress (round {quorum_round}), re-trigger plan-phase |
    | oscillation_diagnosis | `/qgsd:execute-phase {phase}` — oscillation resolution quorum was running |
    | awaiting_approval | `/qgsd:execute-phase {phase}` — unified solution is ready, user approval is needed |
    | executing | `/qgsd:quick` — quick task execution was in progress |
    | planning (activity=quick) | `/qgsd:quick` — quick task planning was in progress |

    Present the matched recovery option to the user as the PRIMARY action.
    ```

    **Addition 4 — In `offer_options` step**, when HAS_ACTIVITY is true, add as option 1 (before "Execute phase"):

    ```
    [If HAS_ACTIVITY is true:]
    1. Resume interrupted workflow: {recovery command from routing table}
       (Was in: {activity}/{sub_activity} at {updated})
    ```

    Format all additions as prose narrative within the existing step structure — not as new separate steps. The additions are read as instructions to Claude at resume time.
  </action>
  <verify>
    `grep "activity-get" /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md` returns a match in the initialize step.
    `grep "sub_activity" /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md` returns matches in present_status and determine_next_action.
    `grep "executing_plan" /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md` returns a match in the routing table.
    The file is valid markdown with no broken structure.
  </verify>
  <done>
    resume-project.md reads current-activity.json during initialization, displays the interrupted state in PROJECT STATUS, and routes to the correct recovery command in determine_next_action and offer_options.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync activity routing to get-shit-done source resume-project.md and commit</name>
  <files>/Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md</files>
  <action>
    Read the current file at `/Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md`.

    Apply the same 4 additions as Task 1, but use `node ~/.claude/qgsd/bin/gsd-tools.cjs activity-get` (matching the existing `~/.claude/` path style in this file).

    All routing table entries, present_status display, and offer_options additions are identical — only the binary path differs.

    Verify by diffing the two files: only the path prefix difference should exist, not content differences in the additions.

    Commit both files:
    ```bash
    node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs commit "feat(14-04): add activity-aware recovery routing to resume-project workflow" \
      --files /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md \
             /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    ```

    After committing, update REQUIREMENTS.md to mark ACT-01 through ACT-07 as complete:
    ```bash
    node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs requirements mark-complete ACT-01,ACT-02,ACT-03,ACT-04,ACT-05,ACT-06,ACT-07
    ```

    Commit REQUIREMENTS.md:
    ```bash
    node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs commit "docs(14-04): mark ACT-01..07 complete in REQUIREMENTS.md" \
      --files .planning/REQUIREMENTS.md
    ```
  </action>
  <verify>
    `grep "activity-get" /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md` returns a match.
    `grep -c "sub_activity" /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md` returns >= 3 matches.
    `grep "ACT-01" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md` shows `[x]` marking.
    `git log --oneline -3` shows commits for both files and REQUIREMENTS.md.
  </verify>
  <done>
    Both resume-project.md files are updated with activity routing. REQUIREMENTS.md marks ACT-01..07 complete. Phase 14 is feature-complete: all workflows write activity, resume-work reads it and routes to recovery.
  </done>
</task>

</tasks>

<verification>
1. `grep "activity-get" /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md` — match in initialize step
2. `grep "executing_plan\|checkpoint_verify\|debug_loop" /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md` — routing table entries present
3. `grep "\[x\] \*\*ACT-01\*\*" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md` — requirement marked complete
4. Both resume-project.md locations updated and committed
5. A manual simulation: create `.planning/current-activity.json` with `{"activity":"execute_phase","sub_activity":"debug_loop","phase":14,"plan":"14-03-PLAN.md","debug_round":2,"updated":"2026-02-21T10:30:00Z"}` → run resume-project workflow mentally → it should display the interruption and offer `/qgsd:execute-phase 14`
</verification>

<success_criteria>
- resume-project.md reads current-activity.json during initialization
- PROJECT STATUS box shows interrupted activity (activity, sub_activity, phase, plan, debug_round, timestamp) when file exists
- determine_next_action routing table maps every sub_activity to a recovery command
- Missing current-activity.json is handled gracefully (empty object = no mid-workflow interruption)
- ACT-04 satisfied: resume-work routes to exact recovery point with context restoration
- All ACT-01..07 marked complete in REQUIREMENTS.md
- Both installed (qgsd) and source (get-shit-done) versions committed
</success_criteria>

<output>
After completion, create `.planning/phases/14-activity-tracking/14-04-SUMMARY.md`
</output>
