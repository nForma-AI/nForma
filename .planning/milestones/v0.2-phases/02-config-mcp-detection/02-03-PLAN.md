---
phase: 02-config-mcp-detection
plan: 03
type: auto
wave: 1
depends_on: []
files_modified:
  - bin/install.js
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-05

must_haves:
  truths:
    - "When install.js runs on a machine with ~/.claude.json containing mcpServers, the written qgsd.json has required_models entries derived from detected server names"
    - "When no matching servers are found in ~/.claude.json, qgsd.json is written with hardcoded default prefixes and the installer warns the user"
    - "When ~/.claude.json is missing or unparseable, qgsd.json is written with hardcoded default prefixes without crashing"
    - "When qgsd.json already exists, the installer skips writing it entirely — user's manual edits are never overwritten (MCP-05)"
    - "Server names containing 'codex' map to tool_prefix 'mcp__<serverName>__'"
    - "Server names containing 'gemini' map to tool_prefix 'mcp__<serverName>__'"
    - "Server names containing 'opencode' map to tool_prefix 'mcp__<serverName>__'"
    - "Case-insensitive matching: 'Codex-CLI' matches keyword 'codex'"
    - "quorum_instructions in the written qgsd.json is GENERATED from detected required_models (not copied verbatim from template) — behavioral instructions and structural enforcement reference the same tool names"
  artifacts:
    - path: "bin/install.js"
      provides: "Installer with MCP auto-detection, detected config writing, and generated quorum_instructions"
      contains: "buildRequiredModelsFromMcp"
  key_links:
    - from: "bin/install.js buildRequiredModelsFromMcp()"
      to: "~/.claude.json mcpServers"
      via: "fs.readFileSync"
      pattern: "\\.claude\\.json"
    - from: "bin/install.js buildQuorumInstructions()"
      to: "detectedModels required_models"
      via: "generates tool names from detected tool_prefix values"
      pattern: "buildQuorumInstructions"
    - from: "bin/install.js"
      to: "qgsd.json write path"
      via: "buildRequiredModelsFromMcp() + buildQuorumInstructions() results written together"
      pattern: "buildRequiredModelsFromMcp"
---

<objective>
Add MCP auto-detection to bin/install.js: on first install, read ~/.claude.json to find Codex, Gemini, and OpenCode server names, derive tool prefixes, and write them into the qgsd.json config. Fall back to hardcoded defaults if no servers are detected. Never overwrite an existing qgsd.json.

Purpose: Users should not have to manually configure quorum model prefixes. Detection runs once at install time and writes the correct prefixes so the hooks work out of the box.
Output: bin/install.js updated with buildRequiredModelsFromMcp() and detection-aware config write.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-mcp-detection/2-RESEARCH.md
@templates/qgsd.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildRequiredModelsFromMcp() to installer</name>
  <files>bin/install.js</files>
  <action>
    In bin/install.js, make the following changes:

    1. Add the KEYWORD_MAP constant near the top of the file (after the existing color constants and before the first function), outside of any function:

       const QGSD_KEYWORD_MAP = {
         codex:    { keywords: ['codex'],    defaultPrefix: 'mcp__codex-cli__'  },
         gemini:   { keywords: ['gemini'],   defaultPrefix: 'mcp__gemini-cli__' },
         opencode: { keywords: ['opencode'], defaultPrefix: 'mcp__opencode__'   },
       };

    2. Add the buildRequiredModelsFromMcp() function near the top of the file (after QGSD_KEYWORD_MAP, before the main install functions). This function reads ~/.claude.json and returns a required_models object:

       function buildRequiredModelsFromMcp() {
         const claudeJsonPath = path.join(os.homedir(), '.claude.json');
         let mcpServers = {};

         try {
           if (fs.existsSync(claudeJsonPath)) {
             const d = JSON.parse(fs.readFileSync(claudeJsonPath, 'utf8'));
             mcpServers = d.mcpServers || {};
           }
         } catch (e) {
           console.warn(`  ${yellow}⚠${reset} Could not read ~/.claude.json: ${e.message}`);
         }

         const requiredModels = {};
         let anyDetected = false;

         for (const [modelKey, { keywords, defaultPrefix }] of Object.entries(QGSD_KEYWORD_MAP)) {
           const matched = Object.keys(mcpServers).find(serverName =>
             keywords.some(kw => serverName.toLowerCase().includes(kw))
           );
           if (matched) {
             requiredModels[modelKey] = { tool_prefix: `mcp__${matched}__`, required: true };
             anyDetected = true;
             console.log(`  ${green}✓${reset} Detected ${modelKey} MCP server: ${matched} → prefix: mcp__${matched}__`);
           } else {
             requiredModels[modelKey] = { tool_prefix: defaultPrefix, required: true };
             console.warn(`  ${yellow}⚠${reset} No ${modelKey} MCP server found in ~/.claude.json; using default prefix: ${defaultPrefix}`);
           }
         }

         if (!anyDetected) {
           console.warn(`  ${yellow}⚠${reset} No quorum MCP servers detected — using hardcoded defaults. Edit ~/.claude/qgsd.json to configure.`);
         }

         return requiredModels;
       }

    3. Add the buildQuorumInstructions() function after buildRequiredModelsFromMcp(). This generates
       the quorum_instructions string from the DETECTED model prefixes so the injected behavioral
       instructions name the same tools the Stop hook structurally enforces:

       function buildQuorumInstructions(requiredModels) {
         function toolName(key, prefix) {
           if (key === 'codex')    return prefix + 'review';
           if (key === 'gemini')   return prefix + 'gemini';
           if (key === 'opencode') return prefix + 'opencode';
           return prefix + key;
         }
         const required = Object.entries(requiredModels).filter(([, def]) => def.required);
         const steps = required.map(([key, def], i) =>
           `  ${i + 1}. Call ${toolName(key, def.tool_prefix)} with the full plan content`
         ).join('\n');
         return (
           'QUORUM REQUIRED (structural enforcement — Stop hook will verify)\n\n' +
           'Before presenting any planning output to the user, you MUST:\n' +
           steps + '\n' +
           `  ${required.length + 1}. Present all model responses, resolve concerns, then deliver final output\n\n` +
           'Fail-open: if a model is UNAVAILABLE (quota/error), note it and proceed with available models.\n' +
           'The Stop hook reads the transcript — skipping quorum will block your response.'
         );
       }

    4. Locate the existing qgsd.json write block in install.js (around lines 1605-1613). It currently copies the template file unchanged:

       const qgsdConfigPath = path.join(targetDir, 'qgsd.json');
       if (!fs.existsSync(qgsdConfigPath)) {
         const qgsdConfigTemplatePath = path.join(__dirname, '..', 'templates', 'qgsd.json');
         if (fs.existsSync(qgsdConfigTemplatePath)) {
           fs.copyFileSync(qgsdConfigTemplatePath, qgsdConfigPath);
           console.log(`  ${green}✓${reset} Wrote QGSD default config (~/.claude/qgsd.json)`);
         }
       }

       Replace this block with a detection-aware version that generates quorum_instructions from
       detected prefixes (NOT verbatim template copy):

       const qgsdConfigPath = path.join(targetDir, 'qgsd.json');
       if (!fs.existsSync(qgsdConfigPath)) {
         // Build config with auto-detected MCP prefixes
         const detectedModels = buildRequiredModelsFromMcp();
         const qgsdConfig = {
           quorum_commands: [
             'plan-phase', 'new-project', 'new-milestone',
             'discuss-phase', 'verify-work', 'research-phase',
           ],
           fail_mode: 'open',
           required_models: detectedModels,
           // Generated from detected prefixes — behavioral instructions match structural enforcement
           quorum_instructions: buildQuorumInstructions(detectedModels),
         };

         fs.writeFileSync(qgsdConfigPath, JSON.stringify(qgsdConfig, null, 2) + '\n', 'utf8');
         console.log(`  ${green}✓${reset} Wrote QGSD config with detected MCP prefixes (~/.claude/qgsd.json)`);
       } else {
         console.log(`  ${dim}↳ ~/.claude/qgsd.json already exists — skipping (user config preserved)${reset}`);
       }

    IMPORTANT constraints:
    - The existing qgsdConfigPath guard (if !fs.existsSync) MUST remain — MCP-05 requires no overwrite on reinstall
    - console.warn() is acceptable in install.js (it writes to stderr in Node.js, not stdout, and the installer is not a hook — stdout is not the decision channel here)
    - quorum_instructions MUST be generated from detectedModels (not copied from template) so behavioral
      instructions (UserPromptSubmit injection) name the same tools as the structural enforcement (Stop hook).
      This prevents the mismatch where renamed servers produce wrong tool names in the injected prompt.
    - The template templates/qgsd.json retains its hardcoded quorum_instructions as a reference/fallback
      for users who manually create qgsd.json without using the installer.
    - No new npm dependencies — only fs, path, os (all already imported in install.js)
    - The QGSD_KEYWORD_MAP constant must be named QGSD_KEYWORD_MAP (not KEYWORD_MAP) to avoid potential collision with any existing constants in the 1660-line install.js
  </action>
  <verify>
    1. grep -n "buildRequiredModelsFromMcp" bin/install.js — returns at least 2 matches (definition + call)
    2. grep -n "buildQuorumInstructions" bin/install.js — returns at least 2 matches (definition + call)
    3. grep -n "QGSD_KEYWORD_MAP" bin/install.js — returns at least 2 matches (definition + use)
    4. grep -n "fs.existsSync(qgsdConfigPath)" bin/install.js — returns a match (skip-if-exists guard still present)
    5. node -e "const f = require('./bin/install.js')" — no parse error (syntax check)
       Note: require will execute the file; pass --help flag to suppress interactive prompts: node bin/install.js --help 2>&1 | head -5
    6. grep -n "quorum_instructions.*buildQuorumInstructions" bin/install.js — returns a match (instructions are generated, not template copy)
  </verify>
  <done>buildRequiredModelsFromMcp() is defined and called in the qgsd.json write path. Existing files are never overwritten. Detection falls back to hardcoded defaults if ~/.claude.json missing or no servers match.</done>
</task>

</tasks>

<verification>
Overall Plan 03 checks:
1. grep -n "buildRequiredModelsFromMcp" bin/install.js — at least 2 matches
2. grep -n "buildQuorumInstructions" bin/install.js — at least 2 matches
3. grep -n "QGSD_KEYWORD_MAP" bin/install.js — at least 1 match
4. node bin/install.js --help exits cleanly (no syntax error)
5. The if (!fs.existsSync(qgsdConfigPath)) guard is still present — no overwrite on reinstall
6. console.warn is used for warnings (appropriate for CLI output in installer, not hook)
7. No "quorum_instructions.*template" verbatim copy in the write path (instructions generated from detected models)
</verification>

<success_criteria>
- buildRequiredModelsFromMcp() reads ~/.claude.json mcpServers
- buildQuorumInstructions(requiredModels) generates instructions text from detected tool_prefix values
- Keyword matching is case-insensitive (serverName.toLowerCase().includes(kw))
- Detected server name → tool_prefix = "mcp__" + serverName + "__"
- No match for a model → hardcoded default prefix used + warning logged
- ~/.claude.json missing/malformed → all models fall back to defaults + warning logged
- qgsd.json written only when it does not already exist (MCP-05 preserved)
- Existing qgsd.json is never overwritten; installer logs "user config preserved" message
- quorum_instructions in written qgsd.json names the same tools as required_models (no mismatch)
- No new npm dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-mcp-detection/02-03-SUMMARY.md`
</output>
