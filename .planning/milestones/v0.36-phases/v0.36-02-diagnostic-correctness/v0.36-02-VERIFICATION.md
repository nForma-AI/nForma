---
phase: v0.36-02-diagnostic-correctness
verified: 2026-03-14T23:15:00Z
status: passed
score: 3/3 must-haves verified
gaps: []
---

# Phase v0.36-02: Diagnostic Correctness Verification Report

**Phase Goal:** The solve diagnostic engine produces accurate residuals -- no false zeros from missing files, no leaked unscoped results, no stale classification cache hits
**Verified:** 2026-03-14T23:15:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `nf:solve --focus AUTH` propagates the focus filter to all 19 layers -- every layer either filters its output to the focus set or marks results with `scoped: false` | VERIFIED | 17 `scoped: focusSet ? false : undefined` instances in nf-solve.cjs + 1 in sweepPtoF.cjs; sweepFtoT filters gapsList by focusSet (line 742); sweepRtoD and sweepRtoF already filter by focusSet; TC-FOCUS-1 through TC-FOCUS-4 all pass |
| 2 | When `check-results.ndjson` or `requirements.json` is missing, affected layers report `residual: -1` (unknown) instead of `residual: 0` -- preventing false convergence signals | VERIFIED | 12 missing-file return paths confirmed as `residual: -1` with `missing:` prefix reasons at lines 1103, 1144, 1281, 1291, 1305, 1394, 1659, 1666, 1810, 1874, 1881, 1906; runner=none exception at line 851 correctly remains `residual: 0`; TC-MISSING-1 through TC-MISSING-4 all pass |
| 3 | D-to-C classification cache keys are SHA-256 content hashes of claim text -- changing doc content at the same line number invalidates the cached verdict and triggers reclassification | VERIFIED | `itemKey()` at solve-tui.cjs:1408 uses `crypto.createHash('sha256')` for dtoc and dtor categories; 16-char hex truncation; ctor/ttor unchanged (file paths); old-format key cleanup at line 1338; `itemKey` exported at line 1488; TC-CLASS-1 through TC-CLASS-8 all pass |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/nf-solve.cjs` | scoped flag on 17 non-filterable sweeps + focusSet filter on sweepFtoT + 12 missing-file residual:-1 returns | VERIFIED | 17 scoped references, focusSet filtering in sweepFtoT, 12 residual:-1 returns confirmed |
| `bin/sweepPtoF.cjs` | Accepts focusSet option, includes scoped flag in detail | VERIFIED | Line 60: `scoped: focusSet ? false : undefined` |
| `bin/solve-tui.cjs` | SHA-256 content-hash itemKey, crypto import, old-key migration cleanup | VERIFIED | crypto at line 24, itemKey at line 1408, migration cleanup at line 1338 |
| `bin/nf-solve.test.cjs` | 16 new tests covering TC-FOCUS, TC-MISSING, TC-CLASS | VERIFIED | 8 TC-CLASS + 4 TC-FOCUS + 4 TC-MISSING = 16 tests, all passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| nf-solve.cjs sweep functions | downstream JSON output | `scoped: focusSet ? false : undefined` in detail object | WIRED | 17 sweep functions include scoped flag; integration test TC-FOCUS-1 confirms appearance in JSON output |
| nf-solve.cjs missing-file paths | computeResidual() | `residual: -1` sentinel | WIRED | computeResidual handles -1 via `(layer.residual >= 0 ? layer.residual : 0)` -- -1 correctly excluded from total |
| solve-tui.cjs itemKey() | classifyWithHaiku() | cache key lookup/write | WIRED | itemKey exported and used in classification cache reads/writes; old-key cleanup runs during classifyWithHaiku() |
| nf-solve.test.cjs | solve-tui.cjs | `require('./solve-tui.cjs').itemKey` | WIRED | Import at test line 1160; itemKey exported at solve-tui.cjs line 1488 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DIAG-01 | Plan 01 | Solve diagnostic engine propagates --focus filter to all 19 layers | SATISFIED | 17+1 scoped flags, sweepFtoT filter, TC-FOCUS tests pass |
| DIAG-02 | Plan 01 | Solve diagnostic engine returns residual=-1 when formal artifact files are missing | SATISFIED | 12 missing-file returns changed to -1, runner=none exception preserved, TC-MISSING tests pass |
| CLASS-01 | Plan 02 | D-to-C classification cache uses content-hash keys (SHA-256) | SATISFIED | itemKey uses crypto.createHash('sha256'), old-key cleanup present, TC-CLASS tests pass |

No orphaned requirements found -- REQUIREMENTS.md maps exactly DIAG-01, DIAG-02, and CLASS-01 to v0.36-02, and all three are covered by plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No blocker or warning anti-patterns found in modified files |

### Human Verification Required

### 1. Focus Filter Integration with Real Focus Term

**Test:** Run `node bin/nf-solve.cjs --json --report-only --fast --max-iterations=1 --focus=AUTH` and inspect the JSON output layers
**Expected:** Layers like c_to_f, t_to_c, d_to_c include `"scoped": false` in their detail; layers r_to_f and r_to_d show filtered requirement counts
**Why human:** Integration test TC-FOCUS-1 uses a nonexistent focus term; verifying with a real focus term confirms end-to-end behavior with actual data

### 2. Classification Cache Reclassification After Content Edit

**Test:** Edit a doc file claim at the same line number, then run the TUI classification; check that the old cached verdict is replaced
**Expected:** The item gets reclassified (Haiku call made) rather than returning the stale cached result
**Why human:** Requires live Haiku API call and manual doc editing to trigger the cache miss path

---

_Verified: 2026-03-14T23:15:00Z_
_Verifier: Claude (nf-verifier)_
