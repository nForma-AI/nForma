---
phase: v0.36-04-architecture-parallelism
plan: 02
subsystem: infra
tags: [dag, wave-parallel, remediation, performance]

# Dependency graph
requires:
  - phase: v0.36-01
    provides: shared layer-constants and gate-score-utils infrastructure
provides:
  - wave-parallel remediation dispatch via bin/solve-wave-dag.cjs
  - dependency-ordered wave computation with RAM-budget-aware splitting
  - wave timing and speedup ratio in remediation output
affects: [solve-remediate, nf-solve]

# Tech tracking
tech-stack:
  added: []
  patterns: [topological-sort wave grouping, sequential chain compaction, wave timing comparison]

key-files:
  created: [bin/solve-wave-dag.cjs]
  modified: [commands/nf/solve-remediate.md]

key-decisions:
  - "Sequential chain compaction: consecutive single-layer waves merged into one wave with sequential:true flag to keep total <= 6 waves"
  - "Gate chain (hazard_model -> l1_to_l3 -> l3_to_tc -> per_model_gates) runs sequentially within a single wave"
  - "Longest-path assignment for topological ordering ensures correct dependency depth"

patterns-established:
  - "Wave DAG pattern: dependency-ordered wave computation with MAX_PER_WAVE=3 splitting and chain compaction"
  - "Wave timing pattern: per-wave start_ms/duration_ms plus speedup_ratio comparing parallel vs sequential"

requirements-completed: [PERF-01]

# Metrics
duration: 4min
completed: 2026-03-14
---

# Phase v0.36-04 Plan 02: Wave-Parallel Remediation Dispatch Summary

**Dependency DAG module (bin/solve-wave-dag.cjs) computes topologically-ordered wave groupings, and solve-remediate.md dispatches layers in parallel within each wave with timing comparison**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-14T23:16:36Z
- **Completed:** 2026-03-14T23:20:39Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Created bin/solve-wave-dag.cjs with 16-layer dependency DAG and computeWaves() function
- Replaced 13-step sequential dispatch in solve-remediate.md with wave-parallel system
- Added wave timing with speedup ratio (parallel vs sequential comparison) to output contract

## Task Commits

Each task was committed atomically:

1. **Task 1: Create bin/solve-wave-dag.cjs** - `dcaa34c8` (feat)
2. **Task 2: Update solve-remediate.md — wave-parallel dispatch** - `75d3619a` (feat)
3. **Task 3: Add wave timing comparison to remediation output** - `e9bbc0a3` (feat)

## Files Created/Modified
- `bin/solve-wave-dag.cjs` - Dependency DAG module with computeWaves(), getLayerDeps(), LAYER_DEPS adjacency list, and CLI mode
- `commands/nf/solve-remediate.md` - Replaced sequential dispatch with wave-parallel, added wave_timing and speedup_ratio to output contract

## Decisions Made
- Sequential chain compaction: consecutive single-layer waves merged into one wave with `sequential: true` to keep total wave count <= 6
- Gate chain (hazard_model -> l1_to_l3 -> l3_to_tc -> per_model_gates) runs sequentially within its compacted wave since each depends on the previous
- Longest-path assignment (not Kahn's BFS level) for correct topological depth calculation
- Used l1_to_l3 (not l1_to_l2/l2_to_l3) per STRUCT-01 collapse in the DAG

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave DAG module ready for integration with nf-solve orchestrator
- solve-remediate.md dispatch pattern updated; existing layer reference sections (3a-3m) unchanged
- Wave timing output enables performance benchmarking of parallel vs sequential remediation

## Self-Check: PASSED

All files exist, all commit hashes verified (dcaa34c8, 75d3619a, e9bbc0a3).

---
*Phase: v0.36-04-architecture-parallelism*
*Completed: 2026-03-14*
