---
phase: v0.36-04-architecture-parallelism
verified: 2026-03-14T23:45:00Z
status: passed
score: 2/2 must-haves verified
---

# Phase v0.36-04: Architecture Parallelism Verification Report

**Phase Goal:** The L2 structural blocker is resolved (Gate B scores above zero) and remediation dispatch runs in parallel waves instead of sequential steps
**Verified:** 2026-03-14T23:45:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | L2 (Semantics) layer is collapsed to a 2-layer architecture with Gate A evaluating L1-to-L3 directly -- Gate B no longer reports a permanent zero score (it now checks requirement backing) | VERIFIED | LAYER_KEYS has 18 entries, includes `l1_to_l3`, excludes `l1_to_l2` and `l2_to_l3`. Gate B `wiring_purpose_score: 0.96875` in gate-b-abstraction.json. Gate A relationship in build-layer-manifest.cjs: `{ from: 'L1', to: 'L3' }`. No old key references remain in bin/, commands/, hooks/, or test/ directories. |
| 2 | Solve remediation dispatches layers in dependency-ordered waves (max 6 waves) via solve-wave-dag.cjs -- independent layers within a wave run in parallel | VERIFIED | `computeWaves()` with all 16 layers active produces exactly 6 waves. Waves 1-5 have 2-3 layers each (parallel). Wave 6 is a compacted sequential chain (hazard_model -> l1_to_l3 -> l3_to_tc -> per_model_gates). `solve-remediate.md` imports `computeWaves` from `solve-wave-dag.cjs` and dispatches in wave-parallel with timing output (speedup_ratio, wave_timing, total_wall_ms). |

**Score:** 2/2 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/layer-constants.cjs` | 18-key LAYER_KEYS with l1_to_l3, no l1_to_l2/l2_to_l3 | VERIFIED | 18 keys confirmed via runtime check |
| `bin/solve-wave-dag.cjs` | DAG module exporting computeWaves, getLayerDeps, LAYER_DEPS | VERIFIED | 167 lines, 16-layer DAG, topological sort with chain compaction, CLI mode |
| `bin/nf-solve.cjs` | sweepL1toL3 replaces sweepL1toL2, sweepL2toL3 removed | VERIFIED | `sweepL1toL3` at line 2264, removal comment at line 2293, 6 references to `l1_to_l3` |
| `bin/compute-per-model-gates.cjs` | Gate B evaluates requirement backing (purpose check) | VERIFIED | Path 1 at line 183: "any model with non-empty requirements passes Gate B", wiring_purpose_score computed at line 318 |
| `bin/build-layer-manifest.cjs` | Schema v2, Gate A: L1->L3, collapsed_layers metadata | VERIFIED | schema_version '2', Gate A `{ from: 'L1', to: 'L3' }`, collapsed_layers present |
| `.planning/formal/gates/gate-b-abstraction.json` | wiring_purpose_score > 0 | VERIFIED | Score: 0.96875 (192 total, 186 grounded, 6 orphaned) |
| `commands/nf/solve-remediate.md` | Wave-parallel dispatch using computeWaves, wave timing in output contract | VERIFIED | `require(_nfBin('solve-wave-dag.cjs'))` at line 106, wave_timing/speedup_ratio in output contract, constraint #4 updated |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `solve-remediate.md` | `solve-wave-dag.cjs` | `require(_nfBin('solve-wave-dag.cjs'))` | WIRED | Line 106, computeWaves called with residualVector |
| `layer-constants.cjs` | `nf-solve.cjs` | LAYER_KEYS import | WIRED | nf-solve.cjs uses l1_to_l3 in sweepL1toL3 and residual assembly |
| `compute-per-model-gates.cjs` | `gate-b-abstraction.json` | --aggregate --write-per-model | WIRED | gate-b-abstraction.json has schema_version 2 and wiring_purpose_score |
| `solve-wave-dag.cjs` | LAYER_DEPS | Internal DAG uses l1_to_l3 | WIRED | Line 34: `l1_to_l3: ['hazard_model']` -- consistent with STRUCT-01 collapse |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| STRUCT-01 | Plan 01 | L2 collapsed to 2-layer architecture, Gate B unblocked from permanent zero | SATISFIED | LAYER_KEYS=18, l1_to_l3 replaces l1_to_l2, Gate B score 0.969 |
| PERF-01 | Plan 02 | Solve remediation dispatches in dependency-ordered waves (max 6) | SATISFIED | computeWaves produces 6 waves, solve-remediate.md uses wave-parallel dispatch |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No anti-patterns detected |

No TODO/FIXME/PLACEHOLDER markers found in `bin/solve-wave-dag.cjs`. No stub implementations detected. The `sweepL2toL3` removal is documented with a comment at line 2293 of nf-solve.cjs (informational, not a stub).

### Human Verification Required

### 1. Wave-Parallel Remediation End-to-End

**Test:** Run a full `nf:solve` remediation cycle on a codebase with multiple residual layers and observe wave dispatch behavior.
**Expected:** Layers within the same wave dispatch as parallel Agent calls (up to 3 concurrent). Cross-wave dependencies are respected (e.g., f_to_t does not start until r_to_f completes). Wave timing output shows speedup_ratio > 1.0.
**Why human:** Parallel dispatch behavior requires observing actual Agent calls at runtime. Static analysis confirms the workflow text is correct but cannot verify Claude follows the parallel dispatch instructions.

### 2. Gate B Score Accuracy

**Test:** Run `node bin/compute-per-model-gates.cjs --aggregate` and verify Gate B score matches the 6 orphaned models that lack requirement backing.
**Expected:** 186/192 pass = 0.96875 score. The 6 orphaned entries should be identifiable models without requirements.
**Why human:** Verifying the correctness of which specific models are orphaned requires domain knowledge of the formal model registry.

---

_Verified: 2026-03-14T23:45:00Z_
_Verifier: Claude (nf-verifier)_
