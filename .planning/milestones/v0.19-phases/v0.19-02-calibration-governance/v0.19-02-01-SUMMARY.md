---
plan: v0.19-02-01
phase: v0.19-02
status: complete
completed: 2026-02-27
commit: bc7b9bc
requirements: [CALIB-01, CALIB-04]
---

# Summary: v0.19-02-01 — Policy Data Layer

## What Was Built

Calibration governance policy data layer: three new files providing the foundation for cold-start mode in run-prism.cjs.

## Key Files

### Created
- `formal/policy.yaml` — Cold-start thresholds (min_ci_runs:5, min_quorum_rounds:10, min_days:1), steady_state.mode:warn, conservative_priors with YAML-comment documentation of fallthrough logic (CALIB-04)
- `formal/policy.schema.json` — JSON Schema draft-07 validation contract; requires cold_start/steady_state/conservative_priors; additionalProperties:false on cold_start and steady_state
- `bin/read-policy.cjs` — exports readPolicy(path); regex-based flat YAML parser; throws descriptive errors on missing file or missing required fields (CALIB-01)
- `bin/read-policy.test.cjs` — 7 unit tests (RED-GREEN-REFACTOR); covers: loads, missing file, missing field, all fields parsed, integer constraint, float min_days, conservative_priors fields

## Test Results

- `node --test bin/read-policy.test.cjs`: 7/7 pass, 0 failures
- `npm test`: 389/389 pass, 0 failures (no regressions)

## Decisions

- Regex-based YAML parsing: avoids external dependencies; sufficient for flat key:value structure in policy.yaml (per RESEARCH.md recommendation)
- `policy.yaml` committed to repo (not gitignored) — it is default config, not a runtime artifact
- Schema uses `additionalProperties: true` on conservative_priors (allows future extension without schema breakage)

## Self-Check: PASSED

All verification commands from plan confirmed passing:
1. `node -e "...includes('cold_start:')..."` — OK
2. `JSON.parse(policy.schema.json)` — valid JSON
3. `grep -c 'min_ci_runs|...'` — 8 (≥ 5)
4. `ls formal/policy.yaml formal/policy.schema.json bin/read-policy.cjs bin/read-policy.test.cjs` — all 4 exist
5. `grep 'conservative_priors' formal/policy.yaml | grep -c 'tp_rate|unavail|fallthrough'` — 2+
