---
phase: v0.19-05
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/validate-traces.test.cjs
  - bin/run-prism.test.cjs
  - formal/tla/QGSDMCPEnv.tla
  - formal/tla/MCMCPEnv.cfg
autonomous: true
requirements:
  - MCPENV-02
  - MCPENV-03
  - MCPENV-04

must_haves:
  truths:
    - "validate-traces.test.cjs has failing tests for mcp_call field validation before any implementation starts"
    - "run-prism.test.cjs has failing tests for per-slot UNAVAIL rate extraction before any implementation starts"
    - "QGSDMCPEnv.tla stub exists with module header so TLC can parse the file structure"
    - "MCMCPEnv.cfg exists with model configuration for TLC to use with QGSDMCPEnv.tla"
  artifacts:
    - path: "bin/validate-traces.test.cjs"
      provides: "Failing test stubs for MCP field validation"
      contains: "mcp_call missing request_id"
    - path: "bin/run-prism.test.cjs"
      provides: "Failing test stubs for per-slot UNAVAIL rate extraction"
      contains: "extracts per-slot UNAVAIL rates from scoreboard"
    - path: "formal/tla/QGSDMCPEnv.tla"
      provides: "TLA+ module stub for MCP environment spec"
      contains: "MODULE QGSDMCPEnv"
    - path: "formal/tla/MCMCPEnv.cfg"
      provides: "TLC configuration for QGSDMCPEnv.tla"
      contains: "SPECIFICATION"
  key_links:
    - from: "bin/validate-traces.test.cjs"
      to: "bin/validate-traces.cjs"
      via: "node --test require"
      pattern: "require.*validate-traces"
    - from: "bin/run-prism.test.cjs"
      to: "bin/run-prism.cjs"
      via: "node --test require or spawn"
      pattern: "run-prism|readMCPAvailabilityRates"
---

<objective>
Wave 0 Nyquist test scaffolding: commit failing tests for MCPENV-03 and MCPENV-04 before any implementation begins. Also create the TLA+ stub files needed for MCPENV-02.

Purpose: The VALIDATION.md mandates Wave 0 scaffolding before implementation. Failing tests define the exact contract each implementation plan must satisfy, preventing drift and enabling fast feedback.
Output: Three failing test groups + two new TLA+ stub files. npm test will have failing tests; TLA+ module parses but has no behavior.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.19-05-mcp-environment-model/v0.19-05-RESEARCH.md
@.planning/phases/v0.19-05-mcp-environment-model/v0.19-05-VALIDATION.md
@bin/validate-traces.test.cjs
@bin/run-prism.test.cjs
@formal/tla/QGSDQuorum_xstate.tla
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 — Failing tests for validate-traces.cjs MCP field validation (MCPENV-03)</name>
  <files>bin/validate-traces.test.cjs</files>
  <action>
    Append three new failing test cases to bin/validate-traces.test.cjs using the existing node:test pattern already in the file.

    Test 1 — "validate-traces emits error for mcp_call missing request_id":
    - Create a temp JSONL conformance event file containing one event: `{"action":"mcp_call","peer":"codex-1","outcome":"success","attempt":1}` (no request_id)
    - Spawn validate-traces.cjs against it (use spawnSync or exec pattern matching the existing test pattern in the file)
    - Assert: process exits with code 1 (divergence found) OR stdout/stderr contains "request_id"
    - Mark with todo() if the behavior cannot be asserted without implementation, causing RED state

    Test 2 — "validate-traces accepts mcp_call with all required fields":
    - Create a temp JSONL event: `{"action":"mcp_call","request_id":"round1:codex-1:1","peer":"codex-1","outcome":"success","attempt":1}`
    - Assert: no MCP field validation errors for this event
    - Mark with todo() if needed for RED state

    Test 3 — "validate-traces ignores MCP metadata fields for non-mcp_call actions":
    - Create a temp JSONL event: `{"action":"quorum_start","slots_available":4}` (no MCP fields)
    - Assert: no MCP field validation errors (non-mcp_call actions are exempt)
    - Mark with todo() if needed for RED state

    Follow the EXACT test pattern already used in bin/validate-traces.test.cjs (look at how existing tests import and invoke validate-traces.cjs before writing). Group under a describe block: "MCP field validation (MCPENV-03)".

    IMPORTANT: These tests MUST be in RED state (failing) after this commit. If the tests would accidentally pass against the current validate-traces.cjs (which has no MCP validation), use `assert.fail('not yet implemented')` as a placeholder inside each test body.
  </action>
  <verify>
    `npm test -- bin/validate-traces.test.cjs` — the three new "mcp_" tests fail (RED state), all existing tests continue to pass.
    Confirm by checking test output: existing tests show "pass", new MCP tests show "fail" or "todo".
  </verify>
  <done>
    bin/validate-traces.test.cjs contains 3 new failing test cases under "MCP field validation (MCPENV-03)". Running `npm test -- bin/validate-traces.test.cjs --grep "mcp_"` shows 3 failing tests. All previously passing tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Wave 0 — Failing tests for run-prism.cjs per-slot UNAVAIL rate extraction (MCPENV-04)</name>
  <files>bin/run-prism.test.cjs</files>
  <action>
    Append three new failing test cases to bin/run-prism.test.cjs using the existing node:test and spawnSync/fixture pattern in the file.

    Test 1 — "run-prism extracts per-slot UNAVAIL rates from scoreboard":
    - Create a mock scoreboard fixture with 10 rounds: slot "codex-1" has 2 UNAVAIL, 8 TP; slot "gemini-1" has 1 UNAVAIL, 9 TP
    - Assert that readMCPAvailabilityRates (or equivalent exported function) returns `{ "codex-1": 0.8, "gemini-1": 0.9 }` (availability = 1 - unavail/total)
    - Since readMCPAvailabilityRates does not yet exist, assert.fail('not yet implemented')

    Test 2 — "run-prism calibrates mcp-availability.pm with empirical rates":
    - Verify that run-prism.cjs with PRISM_BIN=prism sentinel and a model flag (or relevant env) would call PRISM with per-slot availability constants derived from scoreboard
    - Focus on the rate injection: mock scoreboard → rates extracted → PRISM -const flags set correctly
    - assert.fail('not yet implemented') until implementation

    Test 3 — "run-prism falls back to priors when scoreboard missing":
    - Call readMCPAvailabilityRates with a path to a non-existent scoreboard
    - Assert: returns null (falls back to priors)
    - assert.fail('not yet implemented') until implementation

    Follow the EXACT pattern used in the file for importing/requiring run-prism functions. Group tests under describe: "MCP availability calibration (MCPENV-04)".

    These tests MUST be in RED state after this commit. The function readMCPAvailabilityRates does not yet exist in run-prism.cjs.
  </action>
  <verify>
    `npm test -- bin/run-prism.test.cjs` — three new "MCP availability calibration" tests fail, all existing tests pass.
    Confirm by grep of output for "mcp" test names showing fail/todo.
  </verify>
  <done>
    bin/run-prism.test.cjs contains 3 new failing tests under "MCP availability calibration (MCPENV-04)". Running `npm test -- bin/run-prism.test.cjs --grep "mcp"` shows 3 failing tests. All previously passing run-prism tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — TLA+ module stub for QGSDMCPEnv.tla and MCMCPEnv.cfg</name>
  <files>formal/tla/QGSDMCPEnv.tla, formal/tla/MCMCPEnv.cfg</files>
  <action>
    Create formal/tla/QGSDMCPEnv.tla as a minimal TLA+ stub that parses without error. Model it on the structure of existing specs in formal/tla/ (e.g., QGSDQuorum.tla or QGSDDeliberation.tla).

    QGSDMCPEnv.tla stub contents:
    ```
    ---- MODULE QGSDMCPEnv ----
    (*
     * formal/tla/QGSDMCPEnv.tla
     * MCPENV-02: Models MCP server call behavior as a nondeterministic
     * environment process. Verifies quorum fault-tolerance under arbitrary
     * MCP failures (timeout, unavailability, reorder).
     *
     * Wave 0 stub: module header only. Implementation in v0.19-05 Plan 02.
     *)
    EXTENDS Naturals, FiniteSets, TLC

    CONSTANTS NumSlots, MaxRetries, QuorumThreshold

    VARIABLES
      slotStatus,    \* [1..NumSlots -> {"AVAILABLE", "UNAVAILABLE"}]
      callState,     \* [1..NumSlots -> {"PENDING", "SUCCESS", "FAIL", "TIMEOUT"}]
      quorumPhase    \* "COLLECTING" | "DELIBERATING" | "DECIDED"

    vars == <<slotStatus, callState, quorumPhase>>

    (* Implementation: stub only — see Plan 02 for full spec *)
    Init == TRUE
    Next == UNCHANGED vars

    Spec == Init /\ [][Next]_vars

    ====
    ```

    Create formal/tla/MCMCPEnv.cfg stub:
    ```
    \* MCMCPEnv.cfg — TLC configuration for QGSDMCPEnv.tla
    \* Wave 0 stub. Full configuration in Plan 02.
    SPECIFICATION Spec
    CONSTANTS
      NumSlots = 2
      MaxRetries = 2
      QuorumThreshold = 1
    CHECK_DEADLOCK FALSE
    ```

    These stubs let the TLA+ toolchain parse the module without error. The real implementation will be added in Plan 02.
  </action>
  <verify>
    File exists: `ls formal/tla/QGSDMCPEnv.tla formal/tla/MCMCPEnv.cfg`
    Both files contain the stub content.
    If tlc is available: `cd formal/tla && tlc QGSDMCPEnv.tla -config MCMCPEnv.cfg 2>&1 | head -5` — should parse without syntax errors (stub behavior is trivially satisfied).
  </verify>
  <done>
    formal/tla/QGSDMCPEnv.tla exists with MODULE header and stub variables. formal/tla/MCMCPEnv.cfg exists with SPECIFICATION Spec and NumSlots=2, MaxRetries=2, QuorumThreshold=1. Both files are parseable TLA+ syntax.
  </done>
</task>

</tasks>

<verification>
Run full test suite after Wave 0: `npm test`
Expected outcome:
- All previously passing tests still pass (zero regressions)
- 3 new MCP field validation tests in validate-traces.test.cjs are in failing/todo state
- 3 new MCP calibration tests in run-prism.test.cjs are in failing/todo state
- formal/tla/QGSDMCPEnv.tla and formal/tla/MCMCPEnv.cfg exist and are syntactically valid

The failing test count in npm test output should increase by 6 from baseline.
</verification>

<success_criteria>
- bin/validate-traces.test.cjs has 3 new failing tests under "MCP field validation (MCPENV-03)"
- bin/run-prism.test.cjs has 3 new failing tests under "MCP availability calibration (MCPENV-04)"
- formal/tla/QGSDMCPEnv.tla exists with valid TLA+ syntax
- formal/tla/MCMCPEnv.cfg exists with SPECIFICATION reference
- npm test: previously-passing tests all still pass
</success_criteria>

<output>
After completion, create `.planning/phases/v0.19-05-mcp-environment-model/v0.19-05-01-SUMMARY.md`
</output>
