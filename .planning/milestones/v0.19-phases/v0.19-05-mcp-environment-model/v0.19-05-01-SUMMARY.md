---
phase: v0.19-05
plan: "01"
status: complete
completed: 2026-02-27
---

# Plan 01 Summary — Wave 0 Nyquist Test Scaffolding

## What Was Built

Added 6 failing test stubs (Wave 0 / RED state) and 2 TLA+ stub files required for subsequent plans to implement against.

## Tasks Completed

### Task 1 — Failing tests for validate-traces.cjs MCP field validation (MCPENV-03)
Added 3 RED tests to `bin/validate-traces.test.cjs`:
- `validate-traces emits error for mcp_call missing request_id`
- `validate-traces accepts mcp_call with all required fields`
- `validate-traces ignores MCP metadata fields for non-mcp_call actions`

All 3 use `assert.fail()` to guarantee RED state. Will turn GREEN in Plan 03 when `validateMCPMetadata` is implemented.

### Task 2 — Failing tests for run-prism.cjs per-slot UNAVAIL rate extraction (MCPENV-04)
Added 3 RED tests to `bin/run-prism.test.cjs`:
- `run-prism extracts per-slot UNAVAIL rates from scoreboard`
- `run-prism calibrates mcp-availability.pm with empirical rates`
- `run-prism falls back to priors when scoreboard missing`

All 3 use `assert.fail()` to guarantee RED state. Will turn GREEN in Plan 04 when `readMCPAvailabilityRates` is implemented.

### Task 3 — TLA+ module stubs (MCPENV-02)
Created `formal/tla/QGSDMCPEnv.tla` with MODULE header, CONSTANTS, VARIABLES, and stub Init/Next/Spec.
Created `formal/tla/MCMCPEnv.cfg` with SPECIFICATION Spec, NumSlots=2, MaxRetries=2, QuorumThreshold=1.

## Test Results

- Pre-existing tests: 32 pass, 0 fail (no regressions)
- New MCP tests: 6 fail (intended RED state)
- Both TLA+ stub files are syntactically valid

## Commits

- `dc0bae5` test(v0.19-05-01): Wave 0 scaffolding — 6 RED tests + TLA+ stubs (MCPENV-02,03,04)

## Key Files

- `bin/validate-traces.test.cjs` — extended with 3 MCP field validation tests
- `bin/run-prism.test.cjs` — extended with 3 MCP calibration tests
- `formal/tla/QGSDMCPEnv.tla` — new stub
- `formal/tla/MCMCPEnv.cfg` — new stub

## Self-Check: PASSED

All must_haves satisfied:
- validate-traces.test.cjs has 3 failing tests for mcp_call field validation
- run-prism.test.cjs has 3 failing tests for per-slot UNAVAIL rate extraction
- QGSDMCPEnv.tla stub exists with MODULE header parseable by TLC
- MCMCPEnv.cfg exists with SPECIFICATION Spec and model constants
