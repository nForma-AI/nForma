# v0.19-03-01 SUMMARY

**Phase:** v0.19-03-01 (Liveness Fairness — Plan 01)
**Completed:** 2026-02-27
**Requirement:** LIVE-01
**Commit:** 8678950

## What Was Built

8 new `formal/spec/<surface>/invariants.md` files created under the new `formal/spec/` directory tree. Each file documents liveness properties with explicit fairness assumption declarations and QGSD-specific realism rationales.

## Files Created

| File | Property | Fairness |
|------|----------|---------|
| `formal/spec/quorum/invariants.md` | EventualConsensus | WF_vars on 4 actions (Decide, StartQuorum, AnyCollectVotes, AnyDeliberate) |
| `formal/spec/deliberation/invariants.md` | ProtocolTerminates | WF_vars on 3 actions (Escalate, ReceiveApprove, ImprovementAccepted) |
| `formal/spec/prefilter/invariants.md` | PreFilterTerminates | WF_vars on 2 composite actions (EvaluateAndResolve, EscalateToUser) |
| `formal/spec/recruiting/invariants.md` | FullRecruitment | Per-slot WF_vars (TrySubSlot, TryApiSlot) + Finish |
| `formal/spec/breaker/invariants.md` | MonitoringReachable | WF_vars on 4 actions (OscillationDetected, ResetBreaker, DisableBreaker, EnableBreaker) |
| `formal/spec/convergence/invariants.md` | ConvergenceEventuallyResolves | WF_vars on 1 action (HaikuReturnsYES) |
| `formal/spec/oscillation/invariants.md` | AlgorithmTerminates | WF_vars on 2 actions (EvaluateFlag, CollapseRuns) |
| `formal/spec/account-manager/invariants.md` | IdleReachable | WF_vars on 9 actions (OAuthSuccess/Fail, SaveOk/Fail, SwapOk/Fail, RemoveOk/Fail, ResetError) |

## Decisions Made

- Property names match `.cfg` PROPERTY/PROPERTIES lines exactly (case-sensitive grep-verified)
- Each rationale references a specific QGSD component (run-quorum.cjs, call-quorum-slot.cjs, qgsd-circuit-breaker.js, hooks/qgsd-prompt.js) rather than generic boilerplate
- MCsafety.cfg has PROPERTY lines (AllTransitionsValid, DeliberationMonotone) but these are safety invariant checks, not liveness — no formal/spec/safety/ directory created (not in scope per LIVE-01)
- MCaccount-manager.cfg uses `PROPERTIES` (plural) keyword — documented correctly

## Verification

- `grep -r "^## " formal/spec/` returns 8 headers (one per surface) ✓
- `grep -r "WF_vars" formal/spec/ | wc -l` returns 8 ✓
- `npm test` 396/396 pass, 0 fail (doc-only changes, no regressions) ✓

## LIVE-01 Status

LIVE-01 SATISFIED: Every TLA+ surface with a liveness PROPERTY entry in its .cfg has a companion `formal/spec/<surface>/invariants.md` with:
- Property name matching the .cfg PROPERTY line exactly ✓
- Fairness assumption declaring WF_vars with specific actions from .tla Spec definition ✓
- QGSD-specific realism rationale referencing actual QGSD components ✓
