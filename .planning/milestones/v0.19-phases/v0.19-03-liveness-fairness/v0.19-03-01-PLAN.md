---
phase: v0.19-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formal/spec/quorum/invariants.md
  - formal/spec/deliberation/invariants.md
  - formal/spec/prefilter/invariants.md
  - formal/spec/recruiting/invariants.md
  - formal/spec/breaker/invariants.md
  - formal/spec/convergence/invariants.md
  - formal/spec/oscillation/invariants.md
  - formal/spec/account-manager/invariants.md
autonomous: true
requirements:
  - LIVE-01

must_haves:
  truths:
    - "Every TLA+ surface with a liveness PROPERTY entry in its .cfg has a companion formal/spec/<surface>/invariants.md"
    - "Each invariants.md entry names the property exactly as it appears in the .cfg PROPERTY line"
    - "Each entry declares the fairness assumption (WF_vars/SF_vars) matching the Spec definition in the .tla file"
    - "Each entry includes a realism rationale referencing QGSD's specific execution context (e.g., dispatcher loop, sequential processing)"
  artifacts:
    - path: "formal/spec/quorum/invariants.md"
      provides: "Fairness declaration for EventualConsensus"
      contains: "## EventualConsensus"
    - path: "formal/spec/deliberation/invariants.md"
      provides: "Fairness declaration for ProtocolTerminates"
      contains: "## ProtocolTerminates"
    - path: "formal/spec/prefilter/invariants.md"
      provides: "Fairness declaration for PreFilterTerminates"
      contains: "## PreFilterTerminates"
    - path: "formal/spec/recruiting/invariants.md"
      provides: "Fairness declaration for FullRecruitment"
      contains: "## FullRecruitment"
    - path: "formal/spec/breaker/invariants.md"
      provides: "Fairness declaration for MonitoringReachable"
      contains: "## MonitoringReachable"
    - path: "formal/spec/convergence/invariants.md"
      provides: "Fairness declaration for ConvergenceEventuallyResolves"
      contains: "## ConvergenceEventuallyResolves"
    - path: "formal/spec/oscillation/invariants.md"
      provides: "Fairness declaration for AlgorithmTerminates"
      contains: "## AlgorithmTerminates"
    - path: "formal/spec/account-manager/invariants.md"
      provides: "Fairness declaration for IdleReachable"
      contains: "## IdleReachable"
  key_links:
    - from: "formal/tla/MCliveness.cfg"
      to: "formal/spec/quorum/invariants.md"
      via: "PROPERTY EventualConsensus matches ## EventualConsensus header"
      pattern: "## EventualConsensus"
    - from: "formal/tla/MCdeliberation.cfg"
      to: "formal/spec/deliberation/invariants.md"
      via: "PROPERTY ProtocolTerminates matches ## ProtocolTerminates header"
      pattern: "## ProtocolTerminates"
    - from: "formal/tla/MCrecruiting-liveness.cfg"
      to: "formal/spec/recruiting/invariants.md"
      via: "PROPERTIES FullRecruitment matches ## FullRecruitment header"
      pattern: "## FullRecruitment"
---

<objective>
Create formal/spec/<surface>/invariants.md files for all 8 TLA+ surfaces that declare liveness properties. Each file explicitly documents: (1) the property name matching the .cfg PROPERTY line exactly, (2) the fairness assumption (WF_vars/SF_vars actions) matching the Spec definition in the .tla file, and (3) a QGSD-specific realism rationale explaining why that assumption holds.

Purpose: Make liveness properties operationally meaningful (LIVE-01). Currently fairness assumptions are embedded in Spec definitions but nowhere documented in human-readable form with operational justification. This documentation is also required by the run-tlc.cjs detection logic added in Plan 02.

Output: 8 invariants.md files under formal/spec/<surface>/ — one per surface that has a liveness property in its .cfg file.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.19-03-liveness-fairness/v0.19-03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formal/spec directory structure and quorum + deliberation invariants.md</name>
  <files>
    formal/spec/quorum/invariants.md
    formal/spec/deliberation/invariants.md
  </files>
  <action>
Create `formal/spec/` directory tree and write the first two invariants.md files.

**Quorum surface** — from `formal/tla/QGSDQuorum.tla`:
- Property in MCliveness.cfg: `PROPERTY EventualConsensus`
- Spec definition: `EventualConsensus == <>(phase = "DECIDED")`
- Fairness in Spec: `WF_vars(Decide) /\ WF_vars(StartQuorum) /\ WF_vars(AnyCollectVotes) /\ WF_vars(AnyDeliberate)` (4 actions)
- Source line reference: QGSDQuorum.tla, lines 141 and 148–152

Write `formal/spec/quorum/invariants.md`:
```markdown
# Liveness Fairness Declarations: quorum

**Spec source:** `formal/tla/QGSDQuorum.tla`
**Config:** `formal/tla/MCliveness.cfg`

## EventualConsensus

**Property:** `EventualConsensus == <>(phase = "DECIDED")`
**Config line:** `PROPERTY EventualConsensus` (MCliveness.cfg)
**Fairness assumption:** WF_vars on 4 actions: Decide, StartQuorum, AnyCollectVotes, AnyDeliberate
**Realism rationale:** The QGSD quorum orchestrator in `bin/run-quorum.cjs` processes slot worker responses sequentially via a wave-barrier loop. If a vote response is pending in the queue, the Decide or Deliberate action will become enabled and, by weak fairness, must eventually fire. The AnyCollectVotes and AnyDeliberate composite actions cover the existentially quantified set of voting slots — once any responsive slot's response arrives, the action is enabled. In a deployed system, the event loop guarantees that enabled I/O actions are not permanently skipped; WF captures this guarantee.

**Source:** `formal/tla/QGSDQuorum.tla`, lines 141, 148–152
```

**Deliberation surface** — from `formal/tla/QGSDDeliberation.tla`:
- Property in MCdeliberation.cfg: `PROPERTY ProtocolTerminates`
- Spec definition: `ProtocolTerminates == <>(round > MaxDeliberationRounds \/ voteState = "CONSENSUS" \/ voteState = "ESCALATED")`
- Fairness in Spec: `WF_vars(Escalate) /\ WF_vars(ReceiveApprove) /\ WF_vars(ImprovementAccepted)` (3 actions)
- Source line reference: QGSDDeliberation.tla, lines 114, 118–121

Write `formal/spec/deliberation/invariants.md`:
```markdown
# Liveness Fairness Declarations: deliberation

**Spec source:** `formal/tla/QGSDDeliberation.tla`
**Config:** `formal/tla/MCdeliberation.cfg`

## ProtocolTerminates

**Property:** `ProtocolTerminates == <>(round > MaxDeliberationRounds \/ voteState = "CONSENSUS" \/ voteState = "ESCALATED")`
**Config line:** `PROPERTY ProtocolTerminates` (MCdeliberation.cfg)
**Fairness assumption:** WF_vars on 3 actions: Escalate, ReceiveApprove, ImprovementAccepted
**Realism rationale:** The R3 deliberation protocol in QGSD's quorum orchestrator runs a bounded loop (up to 10 rounds per R3.3). Once the round counter exceeds MaxDeliberationRounds or a terminal vote is received, the Escalate or ReceiveApprove action becomes enabled. By weak fairness, this enabled action must eventually fire — models the fact that the round-dispatch loop will not indefinitely skip a terminal outcome. ImprovementAccepted covers the R3.6 iterative improvement path, which also terminates by round bound.

**Source:** `formal/tla/QGSDDeliberation.tla`, lines 114, 118–121
```
  </action>
  <verify>
    Run: `ls formal/spec/quorum/invariants.md formal/spec/deliberation/invariants.md`
    Both files exist.
    Run: `grep "## EventualConsensus" formal/spec/quorum/invariants.md && grep "## ProtocolTerminates" formal/spec/deliberation/invariants.md`
    Both property headers present.
  </verify>
  <done>
    formal/spec/quorum/invariants.md exists with ## EventualConsensus header, WF_vars declaration, QGSD-specific rationale referencing run-quorum.cjs wave-barrier loop.
    formal/spec/deliberation/invariants.md exists with ## ProtocolTerminates header, WF_vars declaration, rationale referencing 10-round bound.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prefilter, recruiting, breaker, convergence, oscillation, account-manager invariants.md</name>
  <files>
    formal/spec/prefilter/invariants.md
    formal/spec/recruiting/invariants.md
    formal/spec/breaker/invariants.md
    formal/spec/convergence/invariants.md
    formal/spec/oscillation/invariants.md
    formal/spec/account-manager/invariants.md
  </files>
  <action>
Create the remaining 6 invariants.md files.

**PreFilter surface** — from `formal/tla/QGSDPreFilter.tla`:
- Property in MCprefilter.cfg: `PROPERTY PreFilterTerminates`
- Spec definition: `PreFilterTerminates == <>(filterPhase = "AUTO_RESOLVED" \/ filterPhase = "USER_NEEDED")`
- Fairness: `WF_vars(\E agree \in BOOLEAN : EvaluateAndResolve(agree)) /\ WF_vars(EscalateToUser)` (2 composite actions)
- Source: QGSDPreFilter.tla lines 101, 105–107

Write `formal/spec/prefilter/invariants.md`:
```markdown
# Liveness Fairness Declarations: prefilter

**Spec source:** `formal/tla/QGSDPreFilter.tla`
**Config:** `formal/tla/MCprefilter.cfg`

## PreFilterTerminates

**Property:** `PreFilterTerminates == <>(filterPhase = "AUTO_RESOLVED" \/ filterPhase = "USER_NEEDED")`
**Config line:** `PROPERTY PreFilterTerminates` (MCprefilter.cfg)
**Fairness assumption:** WF_vars on 2 composite actions: EvaluateAndResolve (existentially quantified over BOOLEAN), EscalateToUser
**Realism rationale:** The R4 pre-filter in `hooks/qgsd-prompt.js` runs voting rounds in a loop until consensus or escalation. EvaluateAndResolve covers the agreement/disagreement outcome for any Boolean vote result — once an outcome is enabled (a voting slot responds), by weak fairness it must eventually fire. EscalateToUser fires when all deliberation rounds exhaust without consensus; the bounded loop in R4 (capped at 3 rounds) guarantees this action eventually becomes enabled, and WF ensures it fires.

**Source:** `formal/tla/QGSDPreFilter.tla`, lines 101, 105–107
```

**Recruiting surface** — from `formal/tla/QGSDRecruiting.tla`:
- Property in MCrecruiting-liveness.cfg: `PROPERTIES FullRecruitment` (note: .cfg uses PROPERTIES plural)
- Spec definition: `FullRecruitment == EnoughResponsive => <>(Cardinality(recruited) = MaxSize)`
- Fairness: `WF_vars(Finish) /\ \A ss \in SubSlots : WF_vars(TrySubSlot(ss)) /\ \A sa \in ApiSlots : WF_vars(TryApiSlot(sa))` (per-slot fairness)
- Source: QGSDRecruiting.tla lines 122, 129–131

Write `formal/spec/recruiting/invariants.md`:
```markdown
# Liveness Fairness Declarations: recruiting

**Spec source:** `formal/tla/QGSDRecruiting.tla`
**Config:** `formal/tla/MCrecruiting-liveness.cfg`

## FullRecruitment

**Property:** `FullRecruitment == EnoughResponsive => <>(Cardinality(recruited) = MaxSize)`
**Config line:** `PROPERTIES FullRecruitment` (MCrecruiting-liveness.cfg)
**Fairness assumption:** WF_vars on Finish plus per-slot WF_vars for each SubSlot (TrySubSlot) and ApiSlot (TryApiSlot)
**Realism rationale:** The QGSD quorum slot-trying loop in `bin/call-quorum-slot.cjs` dispatches sub-slots and API slots sequentially. For each responsive slot, the TrySubSlot or TryApiSlot action eventually fires (guaranteed by the slot dispatcher's sequential try loop). WF captures that no responsive slot is permanently skipped. Finish fires once MaxSize slots are recruited; by WF it must eventually fire when enabled. When EnoughResponsive holds (at least MaxSize slots are responsive), termination is guaranteed by these fairness conditions.

**Source:** `formal/tla/QGSDRecruiting.tla`, lines 122, 129–131
```

**Breaker surface** — from `formal/tla/QGSDCircuitBreaker.tla`:
- Property in MCbreaker.cfg: `PROPERTY MonitoringReachable`
- Spec definition: `MonitoringReachable == <>(active = FALSE /\ disabled = FALSE)`
- Fairness: `WF_vars(OscillationDetected) /\ WF_vars(ResetBreaker) /\ WF_vars(DisableBreaker) /\ WF_vars(EnableBreaker)` (4 actions)
- Source: QGSDCircuitBreaker.tla lines 77, 80–84

Write `formal/spec/breaker/invariants.md`:
```markdown
# Liveness Fairness Declarations: breaker

**Spec source:** `formal/tla/QGSDCircuitBreaker.tla`
**Config:** `formal/tla/MCbreaker.cfg`

## MonitoringReachable

**Property:** `MonitoringReachable == <>(active = FALSE /\ disabled = FALSE)`
**Config line:** `PROPERTY MonitoringReachable` (MCbreaker.cfg)
**Fairness assumption:** WF_vars on 4 actions: OscillationDetected, ResetBreaker, DisableBreaker, EnableBreaker
**Realism rationale:** The circuit breaker in `hooks/qgsd-circuit-breaker.js` progresses through states (monitoring → active → disabled) via explicit user or Claude actions. OscillationDetected fires when the run-collapse algorithm detects 3+ alternating commit groups; ResetBreaker fires after oscillation resolution (explicitly called via `npx qgsd --reset-breaker`). By weak fairness, once any terminal action (Reset or Enable) is enabled, it must eventually fire — models the fact that the resolution protocol, once invoked, will run to completion in QGSD's sequential hook execution context.

**Source:** `formal/tla/QGSDCircuitBreaker.tla`, lines 77, 80–84
```

**Convergence surface** — from `formal/tla/QGSDConvergence.tla`:
- Property in MCconvergence.cfg: `PROPERTY ConvergenceEventuallyResolves`
- Spec definition: `ConvergenceEventuallyResolves == <>(logWritten = TRUE)`
- Fairness: `WF_vars(HaikuReturnsYES)` (1 action)
- Source: QGSDConvergence.tla lines 84–85, 88–89

Write `formal/spec/convergence/invariants.md`:
```markdown
# Liveness Fairness Declarations: convergence

**Spec source:** `formal/tla/QGSDConvergence.tla`
**Config:** `formal/tla/MCconvergence.cfg`

## ConvergenceEventuallyResolves

**Property:** `ConvergenceEventuallyResolves == <>(logWritten = TRUE)`
**Config line:** `PROPERTY ConvergenceEventuallyResolves` (MCconvergence.cfg)
**Fairness assumption:** WF_vars on 1 action: HaikuReturnsYES
**Realism rationale:** The convergence check in QGSD's oscillation-resolution mode uses the Haiku reviewer (claude-haiku-4-5-20251001) to classify GENUINE vs REFINEMENT. HaikuReturnsYES fires when the Haiku classifier returns a YES verdict. By weak fairness, if the Haiku call is enabled (circuit breaker active, Haiku available), it must eventually complete and fire the action — models the fact that the HTTP call to the Haiku endpoint, once initiated, will not be permanently stuck. In QGSD's deployment, Haiku is a reliable low-latency endpoint with a 10s timeout ensuring completion.

**Source:** `formal/tla/QGSDConvergence.tla`, lines 84–85, 88–89
```

**Oscillation surface** — from `formal/tla/QGSDOscillation.tla`:
- Property in MCoscillation.cfg: `PROPERTY AlgorithmTerminates`
- Spec definition: `AlgorithmTerminates == <>(algorithmDone = TRUE)`
- Fairness: `WF_vars(EvaluateFlag) /\ WF_vars(CollapseRuns)` (2 actions)
- Source: QGSDOscillation.tla lines 136–137, 140–142

Write `formal/spec/oscillation/invariants.md`:
```markdown
# Liveness Fairness Declarations: oscillation

**Spec source:** `formal/tla/QGSDOscillation.tla`
**Config:** `formal/tla/MCoscillation.cfg`

## AlgorithmTerminates

**Property:** `AlgorithmTerminates == <>(algorithmDone = TRUE)`
**Config line:** `PROPERTY AlgorithmTerminates` (MCoscillation.cfg)
**Fairness assumption:** WF_vars on 2 actions: EvaluateFlag, CollapseRuns
**Realism rationale:** The run-collapse algorithm in `hooks/qgsd-circuit-breaker.js` processes commit groups from a bounded history window (CommitWindow). CollapseRuns fires to group consecutive commits on the same file set; EvaluateFlag fires to evaluate the final oscillation flag once all runs are collapsed. Both actions become enabled as commits are processed and the window advances. By weak fairness, once a commit-processing action is enabled (new commit in window), it must eventually fire — models the sequential PreToolUse hook execution where each invocation processes at least one more commit entry before completing.

**Source:** `formal/tla/QGSDOscillation.tla`, lines 136–137, 140–142
```

**AccountManager surface** — from `formal/tla/QGSDAccountManager.tla`:
- Property in MCaccount-manager.cfg: `PROPERTY IdleReachable`
- Spec definition: `IdleReachable == <>(state = "IDLE")`
- Fairness: `WF_vars` on 9 actions: OAuthSuccess, OAuthFail, SaveOk, SaveFail, SwapOk, SwapFail, RemoveOk, RemoveFail, ResetError
- Source: QGSDAccountManager.tla lines 194–207

Write `formal/spec/account-manager/invariants.md`:
```markdown
# Liveness Fairness Declarations: account-manager

**Spec source:** `formal/tla/QGSDAccountManager.tla`
**Config:** `formal/tla/MCaccount-manager.cfg`

## IdleReachable

**Property:** `IdleReachable == <>(state = "IDLE")`
**Config line:** `PROPERTY IdleReachable` (MCaccount-manager.cfg)
**Fairness assumption:** WF_vars on 9 actions: OAuthSuccess, OAuthFail, SaveOk, SaveFail, SwapOk, SwapFail, RemoveOk, RemoveFail, ResetError
**Realism rationale:** The account manager in QGSD's credential management system processes OAuth flows and key operations sequentially. Each operation (OAuth, Save, Swap, Remove) must eventually complete — either succeeding or failing — before the state machine can progress toward IDLE. By weak fairness, once any operation's completion action is enabled (the external OAuth/API call returns), it must eventually fire. The 10s timeout on all API calls in QGSD's credential manager (MCTimeout constant) bounds the time any action remains pending, ensuring WF holds in the deployed context.

**Source:** `formal/tla/QGSDAccountManager.tla`, lines 194–207
```
  </action>
  <verify>
    Run: `ls formal/spec/prefilter/invariants.md formal/spec/recruiting/invariants.md formal/spec/breaker/invariants.md formal/spec/convergence/invariants.md formal/spec/oscillation/invariants.md formal/spec/account-manager/invariants.md`
    All 6 files exist.
    Run: `grep -r "^## " formal/spec/`
    Should show: EventualConsensus, ProtocolTerminates, PreFilterTerminates, FullRecruitment, MonitoringReachable, ConvergenceEventuallyResolves, AlgorithmTerminates, IdleReachable.
    Run: `grep -r "WF_vars\|SF_vars" formal/spec/ | wc -l`
    Should be >= 8 lines (each invariants.md has at least one fairness declaration).
  </verify>
  <done>
    All 8 formal/spec/<surface>/invariants.md files exist.
    Each file has: a ## header matching the PROPERTY name in the corresponding .cfg file exactly, a Fairness assumption line declaring WF_vars/SF_vars with the specific actions from the .tla Spec definition, and a Realism rationale referencing a specific QGSD component (e.g., run-quorum.cjs, hooks/qgsd-prompt.js, call-quorum-slot.cjs).
    `grep -r "^## " formal/spec/` returns 8 headers (one per surface).
    `npm test` passes (396+ tests green, no regressions from doc-only changes).
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls formal/spec/*/invariants.md` — 8 files listed
2. `grep -r "^## " formal/spec/` — 8 property headers, each matching its .cfg PROPERTY line
3. `grep -r "WF_vars" formal/spec/` — all 8 files reference WF_vars (no SF_vars in this codebase based on research)
4. `npm test` — full suite passes (396+ tests, doc changes should not break tests)
</verification>

<success_criteria>
LIVE-01 satisfied: Every TLA+ surface with a liveness PROPERTY in its .cfg has a companion formal/spec/<surface>/invariants.md with:
- Property name matching the .cfg PROPERTY line exactly (case-sensitive)
- Fairness assumption declaring WF_vars actions that match the .tla Spec definition
- QGSD-specific realism rationale (not generic boilerplate)
All 8 files exist and npm test is green.
</success_criteria>

<output>
After completion, create `.planning/phases/v0.19-03-liveness-fairness/v0.19-03-01-SUMMARY.md`
</output>
