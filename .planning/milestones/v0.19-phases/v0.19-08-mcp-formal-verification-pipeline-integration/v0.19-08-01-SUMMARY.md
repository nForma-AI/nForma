---
phase: v0.19-08
plan: 01
status: complete
completed: 2026-02-28
---

# Summary: v0.19-08-01 — Wire MCMCPEnv into TLC Pipeline

## What Was Built

Registered `QGSDMCPEnv.tla` (the MCP environment model) into the automated TLC pipeline so the `EventualDecision` liveness property is formally verified with documented fairness assumptions. Closes MCPENV-02.

## Tasks Completed

| Task | Status | Files |
|------|--------|-------|
| Task 1: Create invariants.md + update SURFACE_MAP and VALID_CONFIGS | DONE | `formal/spec/mcp-calls/invariants.md`, `bin/run-tlc.cjs` |
| Task 2: Add MCMCPEnv step to run-formal-verify.cjs + CI + unit tests | DONE | `bin/run-formal-verify.cjs`, `.github/workflows/formal-verify.yml`, `bin/run-tlc.test.cjs` |

## Key Files Created / Modified

- **`formal/spec/mcp-calls/invariants.md`** (created) — EventualDecision fairness declaration with WF_vars(QuorumProcessOutcomes), WF_vars(QuorumDecide), WF_vars(TimeoutAction), per-operator rationale, and realism rationale grounded in finite timeout bounds.
- **`bin/run-tlc.cjs`** — SURFACE_MAP entry `'MCMCPEnv': 'mcp-calls'` added; VALID_CONFIGS updated to `['MCsafety', 'MCliveness', 'MCMCPEnv']`.
- **`bin/run-formal-verify.cjs`** — STEPS entry `{ tool: 'tla', id: 'tla:mcp-environment', ... args: ['MCMCPEnv'] }` added after `tla:account-manager`.
- **`.github/workflows/formal-verify.yml`** — CI step `node bin/run-tlc.cjs MCMCPEnv` added after MCliveness step.
- **`bin/run-tlc.test.cjs`** — Two new tests: `detectLivenessProperties returns [] for MCMCPEnv` (3-part assertion), `SURFACE_MAP contains MCMCPEnv -> mcp-calls entry`.

## Verification Results

- `detectLivenessProperties('MCMCPEnv', cfgPath, specDir)` returns `[]` (all 3 checks passed)
- `npm test` — 648 tests passing, 0 failures
- Commit: `c5ebab0b` (feat(MCPENV-02))

## Self-Check: PASSED
