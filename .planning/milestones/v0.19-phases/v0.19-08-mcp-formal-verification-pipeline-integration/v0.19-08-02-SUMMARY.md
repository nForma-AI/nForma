---
phase: v0.19-08
plan: 02
status: complete
completed: 2026-02-28
---

# Summary: v0.19-08-02 — Fix PRISM mcp-availability + Wire into Pipeline

## What Was Built

Fixed three defects in `run-prism.cjs` blocking MCPENV-04, and wired the PRISM mcp-availability model into the formal verification pipeline and CI workflow.

## Tasks Completed

| Task | Status | Files |
|------|--------|-------|
| Task 1: Fix module.exports + composite-key filter + unit tests | DONE | `bin/run-prism.cjs`, `bin/run-prism.test.cjs` |
| Task 2: Add PRISM mcp-availability step to run-formal-verify.cjs + CI | DONE | `bin/run-formal-verify.cjs`, `.github/workflows/formal-verify.yml` |

## Key Changes

- **`bin/run-prism.cjs`** — Fixed module.exports: wrapped `process.exit()` in `require.main === module` guard; moved `module.exports = { readMCPAvailabilityRates }` to top-level (always reachable). Added composite-key filter (`slot.includes(':') || slot.includes('/')`) INSIDE `readMCPAvailabilityRates` function body before rates object is built — with `process.stderr.write('[run-prism] Skipping composite key...')` logging.
- **`bin/run-prism.test.cjs`** — Added 3 new MCPENV-04 tests: (1) `readMCPAvailabilityRates` accessible via require, (2) composite-key filter excludes keys from returned rates object, (3) spawn integration test confirming stderr log and clean `-const` arg names.
- **`bin/run-formal-verify.cjs`** — Added `{ tool: 'prism', id: 'prism:mcp-availability', args: ['--model=mcp-availability'] }` STEPS entry.
- **`.github/workflows/formal-verify.yml`** — Added CI step `node bin/run-prism.cjs --model=mcp-availability` with `PRISM_BIN: /tmp/prism/bin/prism` env.

## Verification Results

- `require('./bin/run-prism.cjs').readMCPAvailabilityRates` is a function — PASS
- Composite-key filter inside function: `readMCPAvailabilityRates({ 'codex-1': 'TP', 'claude-1:provider/model': 'TP' })` returns `{ 'codex-1': 1 }` — PASS
- `run-prism.test.cjs` — 17 tests, 0 failures (`ℹ fail 0`)
- CI steps confirmed: `grep "mcp-availability\|MCMCPEnv" .github/workflows/formal-verify.yml` returns both new steps
- Commit: `e6639781` (feat(MCPENV-04))

## Self-Check: PASSED
