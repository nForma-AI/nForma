---
phase: v0.19-01
plan: 02
type: execute
wave: 2
depends_on:
  - v0.19-01-01
files_modified:
  - bin/run-tlc.cjs
  - bin/run-oscillation-tlc.cjs
  - bin/run-breaker-tlc.cjs
  - bin/run-protocol-tlc.cjs
  - bin/run-account-manager-tlc.cjs
  - bin/run-alloy.cjs
  - bin/run-audit-alloy.cjs
  - bin/run-transcript-alloy.cjs
  - bin/run-installer-alloy.cjs
  - bin/run-account-pool-alloy.cjs
  - bin/run-prism.cjs
  - bin/run-oauth-rotation-prism.cjs
  - bin/validate-traces.cjs
autonomous: true
requirements:
  - UNIF-01

must_haves:
  truths:
    - "Every runner script (TLC, Alloy, PRISM, trace validator) calls writeCheckResult() before process.exit()"
    - "writeCheckResult() calls are wrapped in try/catch so runner still exits correctly on NDJSON write failure"
    - "Each runner passes correct formalism: run-tlc.cjs/run-*-tlc.cjs use 'tla', run-*-alloy.cjs use 'alloy', run-prism.cjs/run-oauth-rotation-prism.cjs use 'prism', validate-traces.cjs uses 'trace'"
    - "run-audit-alloy.cjs and run-installer-alloy.cjs include the spec name in metadata (not in tool field)"
    - "Generate steps (xstate-to-tla.cjs, generate-formal-specs.cjs) and wasm-dot steps are NOT modified — they do not write check results"
  artifacts:
    - path: "bin/run-tlc.cjs"
      provides: "TLA+ runner with writeCheckResult call"
      contains: "writeCheckResult"
    - path: "bin/run-prism.cjs"
      provides: "PRISM runner with writeCheckResult call"
      contains: "writeCheckResult"
    - path: "bin/validate-traces.cjs"
      provides: "Trace validator with writeCheckResult call"
      contains: "writeCheckResult"
  key_links:
    - from: "bin/run-tlc.cjs"
      to: "bin/write-check-result.cjs"
      via: "require('./write-check-result.cjs')"
      pattern: "require.*write-check-result"
    - from: "bin/validate-traces.cjs"
      to: "bin/write-check-result.cjs"
      via: "require('./write-check-result.cjs')"
      pattern: "require.*write-check-result"
---

<objective>
Wire writeCheckResult() into all 13 runner scripts so each appends exactly one NDJSON entry after each check run.

Purpose: UNIF-01 requires all FV checkers (TLC runners x5, Alloy runners x5, PRISM runners x2, trace validator x1) to emit a normalized JSON line. This plan performs that wiring across all 13 scripts.
Output: 13 modified runner scripts, each importing and calling writeCheckResult() with correct tool/formalism/result values.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/phases/v0.19-01-unified-verdict-format/v0.19-01-RESEARCH.md
@.planning/phases/v0.19-01-unified-verdict-format/v0.19-01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire writeCheckResult into TLC runner scripts (5 scripts)</name>
  <files>
    bin/run-tlc.cjs
    bin/run-oscillation-tlc.cjs
    bin/run-breaker-tlc.cjs
    bin/run-protocol-tlc.cjs
    bin/run-account-manager-tlc.cjs
  </files>
  <action>
For each of the 5 TLC runner scripts, add a writeCheckResult() call before every process.exit() call that exits after a tool run (NOT before early-exit prerequisite checks like Java/JAR missing — those should also write a fail result as per the pattern below).

Add at the top of each file (after existing requires):
  const { writeCheckResult } = require('./write-check-result.cjs');

Before each process.exit() call (including error exits), add a try/catch block:
  try {
    writeCheckResult({
      tool:      '<script-name-without-cjs>',   // e.g. 'run-tlc'
      formalism: 'tla',
      result:    passed ? 'pass' : 'fail',
      metadata:  {},   // add { config: configName } for run-tlc.cjs where configName is the MCsafety/MCliveness arg
    });
  } catch (err) {
    process.stderr.write('[<script-name>] Warning: failed to write check result: ' + err.message + '\n');
  }

SPECIFIC instructions per script:

bin/run-tlc.cjs:
- tool: 'run-tlc'
- formalism: 'tla'
- metadata: { config: configArg } where configArg is the first CLI argument (MCsafety, MCliveness, etc.)
- Add writeCheckResult before EVERY process.exit() (prerequisite failures = fail, normal exit = pass/fail based on TLC exit code)

bin/run-oscillation-tlc.cjs:
- tool: 'run-oscillation-tlc'
- formalism: 'tla'
- metadata: { config: configArg } (MCoscillation, MCconvergence)
- Same pattern: before every exit

bin/run-breaker-tlc.cjs:
- tool: 'run-breaker-tlc'
- formalism: 'tla'
- metadata: {}
- Same pattern

bin/run-protocol-tlc.cjs:
- tool: 'run-protocol-tlc'
- formalism: 'tla'
- metadata: { config: configArg } (MCdeliberation, MCprefilter)
- Same pattern

bin/run-account-manager-tlc.cjs:
- tool: 'run-account-manager-tlc'
- formalism: 'tla'
- metadata: {}
- Same pattern

IMPORTANT: Read each file before editing to understand its current exit structure. Each file may have multiple process.exit() calls. Add writeCheckResult before each one, with result: 'fail' for prerequisite-check exits, and result based on actual tool run success for the main exit.

NOTE: Do NOT modify generate-formal-specs.cjs or xstate-to-tla.cjs — those are generation steps, not check steps.
  </action>
  <verify>
    node -e "const { writeCheckResult } = require('./bin/run-tlc.cjs'); console.log('no')" 2>&1 || true
    grep -l "writeCheckResult" bin/run-tlc.cjs bin/run-oscillation-tlc.cjs bin/run-breaker-tlc.cjs bin/run-protocol-tlc.cjs bin/run-account-manager-tlc.cjs
  </verify>
  <done>All 5 TLC runner scripts contain 'writeCheckResult' and 'write-check-result'. Running node -e "require('./bin/run-tlc.cjs')" does not crash on require. grep confirms writeCheckResult present in all 5 files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire writeCheckResult into Alloy, PRISM, and trace runner scripts (8 scripts)</name>
  <files>
    bin/run-alloy.cjs
    bin/run-audit-alloy.cjs
    bin/run-transcript-alloy.cjs
    bin/run-installer-alloy.cjs
    bin/run-account-pool-alloy.cjs
    bin/run-prism.cjs
    bin/run-oauth-rotation-prism.cjs
    bin/validate-traces.cjs
  </files>
  <action>
For each of the 8 remaining runner scripts, apply the same writeCheckResult() wiring pattern as Task 1. Add require at top, add try/catch call before every process.exit().

SPECIFIC instructions per script:

bin/run-alloy.cjs:
- tool: 'run-alloy'
- formalism: 'alloy'
- metadata: {}

bin/run-audit-alloy.cjs:
- tool: 'run-audit-alloy'
- formalism: 'alloy'
- metadata: { spec: specArg } where specArg is the --spec=NAME argument parsed from process.argv (e.g. 'scoreboard-recompute', 'availability-parsing')
- This script is called multiple times with different --spec args; include spec in metadata to disambiguate NDJSON entries

bin/run-transcript-alloy.cjs:
- tool: 'run-transcript-alloy'
- formalism: 'alloy'
- metadata: { spec: specArg } (--spec=transcript-scan)

bin/run-installer-alloy.cjs:
- tool: 'run-installer-alloy'
- formalism: 'alloy'
- metadata: { spec: specArg } (--spec=install-scope or --spec=taxonomy-safety)

bin/run-account-pool-alloy.cjs:
- tool: 'run-account-pool-alloy'
- formalism: 'alloy'
- metadata: {}

bin/run-prism.cjs:
- tool: 'run-prism'
- formalism: 'prism'
- metadata: {}
- NOTE: run-prism.cjs has a PRISM_BIN sentinel (process.env.PRISM_BIN === 'prism') for test mode. The writeCheckResult call should still execute even in test mode.

bin/run-oauth-rotation-prism.cjs:
- tool: 'run-oauth-rotation-prism'
- formalism: 'prism'
- metadata: {}

bin/validate-traces.cjs:
- tool: 'validate-traces'
- formalism: 'trace'
- metadata: {}
- This script may have a different exit pattern (may not call process.exit directly, may return exit codes). Read the file structure first. Add writeCheckResult before all final exit paths.

Read each file before editing. Apply the same try/catch wrapping pattern for every process.exit() call.
  </action>
  <verify>
    grep -l "writeCheckResult" bin/run-alloy.cjs bin/run-audit-alloy.cjs bin/run-transcript-alloy.cjs bin/run-installer-alloy.cjs bin/run-account-pool-alloy.cjs bin/run-prism.cjs bin/run-oauth-rotation-prism.cjs bin/validate-traces.cjs
  </verify>
  <done>All 8 scripts contain 'writeCheckResult'. grep confirms presence in all 8 files. node -e "require('./bin/run-alloy.cjs')" and other requires complete without syntax errors (note: most scripts call process.exit at top level so test via grep, not require).</done>
</task>

</tasks>

<verification>
Confirm all 13 runners are wired:
  grep -l "writeCheckResult" bin/run-tlc.cjs bin/run-oscillation-tlc.cjs bin/run-breaker-tlc.cjs bin/run-protocol-tlc.cjs bin/run-account-manager-tlc.cjs bin/run-alloy.cjs bin/run-audit-alloy.cjs bin/run-transcript-alloy.cjs bin/run-installer-alloy.cjs bin/run-account-pool-alloy.cjs bin/run-prism.cjs bin/run-oauth-rotation-prism.cjs bin/validate-traces.cjs
Must return all 13 file paths.

Confirm generate scripts are NOT modified:
  grep -l "writeCheckResult" bin/xstate-to-tla.cjs bin/generate-formal-specs.cjs 2>/dev/null
Must return no matches (these files must not contain writeCheckResult).

Confirm formalism values are correct (spot check):
  grep -A3 "writeCheckResult" bin/run-tlc.cjs | grep "formalism"    # should show 'tla'
  grep -A3 "writeCheckResult" bin/run-prism.cjs | grep "formalism"  # should show 'prism'
  grep -A3 "writeCheckResult" bin/validate-traces.cjs | grep "formalism"  # should show 'trace'
</verification>

<success_criteria>
- All 13 runner scripts import and call writeCheckResult() before all process.exit() paths
- Correct formalism per runner: tla (5 scripts), alloy (5 scripts), prism (2 scripts), trace (1 script)
- run-audit-alloy.cjs, run-transcript-alloy.cjs, run-installer-alloy.cjs include spec in metadata
- All calls wrapped in try/catch
- xstate-to-tla.cjs and generate-formal-specs.cjs are untouched
</success_criteria>

<output>
After completion, create .planning/phases/v0.19-01-unified-verdict-format/v0.19-01-02-SUMMARY.md
</output>
