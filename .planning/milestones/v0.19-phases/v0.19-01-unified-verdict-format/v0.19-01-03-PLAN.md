---
phase: v0.19-01
plan: 03
type: execute
wave: 3
depends_on:
  - v0.19-01-01
  - v0.19-01-02
files_modified:
  - bin/run-formal-verify.cjs
  - .github/workflows/formal-verify.yml
  - .gitignore
  - package.json
autonomous: true
requirements:
  - UNIF-02
  - UNIF-03
  - UNIF-04

must_haves:
  truths:
    - "run-formal-verify.cjs truncates formal/check-results.ndjson at the start of runOnce() before any steps execute"
    - "run-formal-verify.cjs summary section reads from check-results.ndjson and emits a NDJSON-based count line alongside the existing results[] summary"
    - "CI workflow formal-verify.yml contains a 'Check NDJSON exit code' step that runs node bin/check-results-exit.cjs after run-formal-verify.cjs"
    - "formal/check-results.ndjson is listed in .gitignore"
    - "bin/write-check-result.test.cjs and bin/check-results-exit.test.cjs are added to the npm test script in package.json"
    - "npm test passes with all new test files included"
  artifacts:
    - path: "bin/run-formal-verify.cjs"
      provides: "Orchestrator that initializes NDJSON and reads it for summary"
      contains: "check-results.ndjson"
    - path: ".github/workflows/formal-verify.yml"
      provides: "CI workflow with check-results-exit step"
      contains: "check-results-exit"
    - path: ".gitignore"
      provides: "Excludes runtime-generated check-results.ndjson"
      contains: "check-results.ndjson"
    - path: "package.json"
      provides: "npm test includes write-check-result.test.cjs and check-results-exit.test.cjs"
      contains: "write-check-result.test.cjs"
  key_links:
    - from: "bin/run-formal-verify.cjs"
      to: "formal/check-results.ndjson"
      via: "fs.writeFileSync at runOnce() start (truncate)"
      pattern: "writeFileSync.*check-results"
    - from: "bin/run-formal-verify.cjs"
      to: "formal/check-results.ndjson"
      via: "fs.readFileSync at summary phase"
      pattern: "readFileSync.*check-results"
    - from: ".github/workflows/formal-verify.yml"
      to: "bin/check-results-exit.cjs"
      via: "run: node bin/check-results-exit.cjs"
      pattern: "check-results-exit"
---

<objective>
Wire run-formal-verify.cjs to initialize and read check-results.ndjson, add CI exit gate, gitignore the runtime file, and register new test files in npm test.

Purpose: UNIF-02 (orchestrator generates ndjson as canonical output), UNIF-03 (triage reads from ndjson), UNIF-04 (CI gate). This plan completes the integration by connecting the orchestrator and CI to the infrastructure built in Plans 01 and 02.
Output: Modified run-formal-verify.cjs (init + summary read), updated CI workflow with exit gate, .gitignore entry, package.json test registration.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/phases/v0.19-01-unified-verdict-format/v0.19-01-RESEARCH.md
@.planning/phases/v0.19-01-unified-verdict-format/v0.19-01-01-SUMMARY.md
@.planning/phases/v0.19-01-unified-verdict-format/v0.19-01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run-formal-verify.cjs — init NDJSON + summary read</name>
  <files>
    bin/run-formal-verify.cjs
  </files>
  <action>
Read bin/run-formal-verify.cjs in full before editing.

Make TWO targeted edits:

EDIT 1 — NDJSON initialization at runOnce() start (UNIF-02):
In the runOnce() function, after `results.length = 0;` (the existing reset), add:
  const ndjsonPath = path.join(__dirname, '..', 'formal', 'check-results.ndjson');
  fs.writeFileSync(ndjsonPath, '', 'utf8');  // truncate — fresh run

This ensures each runOnce() call starts with a clean file. Watch mode already calls runOnce() in a loop, so this mirrors the existing `results.length = 0` reset.

EDIT 2 — NDJSON-based summary (UNIF-03):
In the summary section of runOnce(), AFTER the existing summary loop (the `for (const r of results)` block and the HR separator that follows), ADD (do not replace) a new block that reads from the NDJSON file:

  // NDJSON-based summary (UNIF-03)
  try {
    const ndjsonLines = fs.readFileSync(ndjsonPath, 'utf8')
      .split('\n').filter(l => l.trim().length > 0);
    const checkResults   = ndjsonLines.map(l => JSON.parse(l));
    const ndjsonFailed   = checkResults.filter(r => r.result === 'fail').length;
    const ndjsonPassed   = checkResults.filter(r => r.result === 'pass').length;
    const ndjsonOther    = checkResults.length - ndjsonFailed - ndjsonPassed;
    process.stdout.write(
      TAG + ' check-results.ndjson: ' + ndjsonPassed + ' pass, ' +
      ndjsonFailed + ' fail' +
      (ndjsonOther > 0 ? ', ' + ndjsonOther + ' warn/inconclusive' : '') + '\n'
    );
  } catch (err) {
    process.stderr.write(TAG + ' Warning: could not read check-results.ndjson: ' + err.message + '\n');
  }

IMPORTANT: Keep the existing `results[]` in-memory array and its summary loop intact — the NDJSON summary is ADDITIVE. Do not remove any existing code. Only add these two new blocks.

Also ensure 'fs' is already required at the top of the file (it should be — check and add if somehow missing).
  </action>
  <verify>
    node -e "const src = require('fs').readFileSync('bin/run-formal-verify.cjs','utf8'); console.log(src.includes('writeFileSync') && src.includes('check-results.ndjson') && src.includes('readFileSync') ? 'WIRED' : 'MISSING')"
  </verify>
  <done>Output is 'WIRED'. run-formal-verify.cjs contains both fs.writeFileSync (truncate at start) and fs.readFileSync (summary read) referencing check-results.ndjson. Existing results[] loop is still present.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow gate, gitignore, and package.json test registration</name>
  <files>
    .github/workflows/formal-verify.yml
    .gitignore
    package.json
  </files>
  <action>
THREE targeted edits:

EDIT 1 — CI workflow (.github/workflows/formal-verify.yml):
Read the file. After the final existing step ("Run full FV pipeline via master runner"), ADD a new step:
  - name: Check NDJSON exit code (fail if any result=fail in check-results.ndjson)
    env:
      PRISM_BIN: /tmp/prism/bin/prism
    run: node bin/check-results-exit.cjs

Also add 'bin/check-results-exit.cjs' and 'bin/write-check-result.cjs' to BOTH the push.paths and pull_request.paths trigger lists (so CI re-runs when these files change).

EDIT 2 — .gitignore:
Read the file. Find the block for formal/ directory entries. Add after the existing formal/ entries:
  formal/check-results.ndjson

EDIT 3 — package.json test script:
Read the file. Find the "test" script value. The current test script ends with `bin/check-spec-sync.test.cjs`. Append the two new test files to the end of the --test argument list:
  ... bin/check-spec-sync.test.cjs bin/write-check-result.test.cjs bin/check-results-exit.test.cjs

Be careful to preserve the existing test script exactly — only append the two new file paths before the closing quote.
  </action>
  <verify>
    grep "check-results-exit" .github/workflows/formal-verify.yml
    grep "check-results.ndjson" .gitignore
    grep "write-check-result.test.cjs" package.json
    grep "check-results-exit.test.cjs" package.json
  </verify>
  <done>
    CI workflow contains 'node bin/check-results-exit.cjs' step.
    .gitignore contains 'formal/check-results.ndjson'.
    package.json test script includes both 'bin/write-check-result.test.cjs' and 'bin/check-results-exit.test.cjs'.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
Run the full npm test suite to confirm all existing tests still pass and the two new test files are included and passing:
  npm test

If npm test fails, diagnose which test(s) are failing and fix the root cause. Common issues:
- A runner script was edited and now has a syntax error (fix the syntax error)
- The test script in package.json was incorrectly modified (restore correct format)
- The NDJSON file path in write-check-result.cjs conflicts with test isolation (ensure CHECK_RESULTS_PATH env var is used in tests)

Do NOT mark this task done until npm test exits 0.
  </action>
  <verify>npm test</verify>
  <done>npm test exits 0. All tests pass including the two new test files (write-check-result.test.cjs and check-results-exit.test.cjs visible in test output).</done>
</task>

</tasks>

<verification>
Final integration check — confirm all four UNIF requirements are satisfied:

UNIF-01: All runners emit NDJSON
  grep -l "writeCheckResult" bin/run-tlc.cjs bin/run-oscillation-tlc.cjs bin/run-breaker-tlc.cjs bin/run-protocol-tlc.cjs bin/run-account-manager-tlc.cjs bin/run-alloy.cjs bin/run-audit-alloy.cjs bin/run-transcript-alloy.cjs bin/run-installer-alloy.cjs bin/run-account-pool-alloy.cjs bin/run-prism.cjs bin/run-oauth-rotation-prism.cjs bin/validate-traces.cjs
  (must return 13 files)

UNIF-02: run-formal-verify.cjs initializes ndjson
  node -e "const s = require('fs').readFileSync('bin/run-formal-verify.cjs','utf8'); console.log(s.includes('writeFileSync') ? 'OK' : 'MISSING')"

UNIF-03: Summary reads from ndjson
  node -e "const s = require('fs').readFileSync('bin/run-formal-verify.cjs','utf8'); console.log(s.includes('readFileSync') && s.includes('check-results.ndjson') ? 'OK' : 'MISSING')"

UNIF-04: CI gate exists
  grep "check-results-exit" .github/workflows/formal-verify.yml

All four must succeed. Then: npm test (0 exit).
</verification>

<success_criteria>
- run-formal-verify.cjs truncates check-results.ndjson at runOnce() start (UNIF-02)
- run-formal-verify.cjs reads and reports NDJSON counts in summary section (UNIF-03)
- formal-verify.yml has check-results-exit.cjs as a final CI step (UNIF-04)
- formal/check-results.ndjson is in .gitignore
- npm test exits 0 with all tests including the 2 new test files
</success_criteria>

<output>
After completion, create .planning/phases/v0.19-01-unified-verdict-format/v0.19-01-03-SUMMARY.md
</output>
