---
plan: v0.19-10-01
status: complete
wave: 1
---

# Summary: v0.19-10-01 — TDD RED phase

## What was built

Added one new failing test to `bin/run-prism.test.cjs` that documents the CALIB-04 contract:
`run-prism.cjs` must read `conservative_priors.tp_rate` and `conservative_priors.unavail` from
`formal/policy.yaml` via `readPolicy()` — not hardcoded `PRISM_PRIOR_TP = 0.85` / `PRISM_PRIOR_UNAVAIL = 0.15` constants.

## RED anchor

The wiring assertion (`assert.doesNotMatch` on `/const PRISM_PRIOR_TP\s*=/`) fails immediately
against current `run-prism.cjs` because the source contains `const PRISM_PRIOR_TP = 0.85` at line 108.

## Test results

- Before plan: 17 pass, 0 fail
- After plan:  17 pass, 1 fail (new CALIB-04 test — expected RED)

## Commit

`test(v0.19-10): RED — policy.yaml conservative_priors not wired to run-prism.cjs constants`

## Self-Check: PASSED

- New test exists: `policy.yaml conservative_priors values are used as PRISM constants when no scoreboard`
- Failure message confirms: `run-prism.cjs must not define hardcoded const PRISM_PRIOR_TP`
- 17 pre-existing tests still pass (no regressions)
