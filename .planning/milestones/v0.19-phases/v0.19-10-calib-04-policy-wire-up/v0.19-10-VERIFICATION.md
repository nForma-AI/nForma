---
phase: v0.19-10
status: passed
verified: 2026-02-28
---

# Verification: Phase v0.19-10 — CALIB-04 Policy Wire-Up

## Goal

Wire `read-policy.cjs` `conservative_priors` to `run-prism.cjs` runtime constants so `policy.yaml`
changes take effect at runtime.

## Must-Haves Verification

### From Plan 01 (TDD RED)

- [x] **New test named `policy.yaml conservative_priors values are used as PRISM constants when no scoreboard` exists in `bin/run-prism.test.cjs`**
  - Verified: `grep "policy.yaml conservative_priors values are used" bin/run-prism.test.cjs` → found

- [x] **New test FAILS (RED) against pre-Plan-02 code**
  - Verified: After Plan 01 commit, test output showed 17 pass, 1 fail with message `run-prism.cjs must not define hardcoded const PRISM_PRIOR_TP`

- [x] **All 6+ pre-existing tests still pass after Plan 01**
  - Verified: 17 pass, 1 fail (all pre-existing tests green)

### From Plan 02 (GREEN implementation)

- [x] **`bin/run-prism.cjs` contains no reference to `const PRISM_PRIOR_TP` or `const PRISM_PRIOR_UNAVAIL` as assignments**
  - Verified: `grep -c "PRISM_PRIOR_TP|PRISM_PRIOR_UNAVAIL" bin/run-prism.cjs` → 0

- [x] **`bin/run-prism.cjs` uses `policy.conservative_priors.tp_rate` and `policy.conservative_priors.unavail` in fallback block**
  - Verified: 3 references to `policy.conservative_priors` in `bin/run-prism.cjs`

- [x] **All 18 tests in `bin/run-prism.test.cjs` pass GREEN (including CALIB-04 test)**
  - Verified: `node --test bin/run-prism.test.cjs` → 18 pass, 0 fail

- [x] **First test updated from hardcoded `0.85`/`0.15` to policy-read values**
  - Verified: `grep "tp_rate=0\.85|unavail=0\.15" bin/run-prism.test.cjs` → 0 lines

- [x] **Changing `formal/policy.yaml` conservative_priors changes runtime PRISM constants**
  - Verified structurally: `run-prism.cjs` now reads `policy.conservative_priors.tp_rate` and
    `policy.conservative_priors.unavail` from the `policy` object (loaded from `policy.yaml` via
    `readPolicy()` at module load). The wiring is direct — no intermediate copy or cache.

## Commits

1. `test(v0.19-10): RED — policy.yaml conservative_priors not wired to run-prism.cjs constants`
2. `feat(v0.19-10): wire readPolicy() conservative_priors to run-prism.cjs fallback constants`
3. `test(v0.19-10): GREEN — update prior assertions to read from policy.yaml`

## Requirement Traceability

- **CALIB-04**: Fully satisfied — `conservative_priors.tp_rate` and `conservative_priors.unavail`
  from `formal/policy.yaml` are now the authoritative source for the PRISM fallback constants.

## Pre-existing Failures (not introduced by this phase)

- `bin/secrets.test.cjs` `syncToClaudeJson` tests: 11 failures pre-phase, 5 post-phase (improvement).
  None related to CALIB-04 or `run-prism.cjs`.

## Status

**PASSED** — All must-haves satisfied. Phase goal achieved.
