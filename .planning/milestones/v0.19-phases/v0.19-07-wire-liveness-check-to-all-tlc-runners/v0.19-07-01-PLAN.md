---
plan: v0.19-07-01
title: RED Tests — Liveness Wiring Stubs for All Four TLC Runners
phase: v0.19-07
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/run-oscillation-tlc.test.cjs
  - bin/run-breaker-tlc.test.cjs
  - bin/run-protocol-tlc.test.cjs
  - bin/run-account-manager-tlc.test.cjs
autonomous: true
requirements:
  - LIVE-02
must_haves:
  truths:
    - "All 4 runner test files contain failing tests asserting detectLivenessProperties import and call"
    - "New tests are RED (failing) before any runner source changes"
    - "Existing passing tests remain green after adding RED stubs"
  artifacts:
    - path: "bin/run-oscillation-tlc.test.cjs"
      provides: "RED liveness-wiring assertions for run-oscillation-tlc.cjs"
    - path: "bin/run-breaker-tlc.test.cjs"
      provides: "RED liveness-wiring assertions for run-breaker-tlc.cjs"
    - path: "bin/run-protocol-tlc.test.cjs"
      provides: "RED liveness-wiring assertions for run-protocol-tlc.cjs"
    - path: "bin/run-account-manager-tlc.test.cjs"
      provides: "RED liveness-wiring assertions for run-account-manager-tlc.cjs"
  key_links:
    - from: "bin/run-oscillation-tlc.test.cjs"
      to: "bin/run-oscillation-tlc.cjs"
      via: "fs.readFileSync source inspection"
      pattern: "readFileSync.*run-oscillation-tlc\\.cjs"
    - from: "bin/run-breaker-tlc.test.cjs"
      to: "bin/run-breaker-tlc.cjs"
      via: "fs.readFileSync source inspection"
      pattern: "readFileSync.*run-breaker-tlc\\.cjs"
---

# Plan v0.19-07-01: RED Tests — Liveness Wiring Stubs for All Four TLC Runners

<objective>
Add failing test cases to all 4 TLC runner test files that assert `detectLivenessProperties` is imported and called with the correct arguments. These tests must be RED before the implementation plan (v0.19-07-02) makes them GREEN.

Purpose: Establish the TDD RED baseline so the implementation plan has well-defined acceptance criteria.
Output: 12 new failing tests across 4 test files, all existing tests still passing.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="W0-01" type="auto">
  <name>Task W0-01: Add RED liveness-wiring tests to run-oscillation-tlc.test.cjs</name>
  <files>bin/run-oscillation-tlc.test.cjs</files>
  <action>
Append three new test cases to the existing file `bin/run-oscillation-tlc.test.cjs`:

```js
test('run-oscillation-tlc.cjs requires run-tlc.cjs and destructures detectLivenessProperties', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-oscillation-tlc.cjs'), 'utf8');
  // Must require run-tlc.cjs
  assert.match(src, /require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-oscillation-tlc.cjs must require ./run-tlc.cjs');
  // Must destructure detectLivenessProperties from it
  assert.match(src, /\{\s*detectLivenessProperties\s*\}\s*=\s*require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-oscillation-tlc.cjs must destructure detectLivenessProperties from ./run-tlc.cjs');
});

test('run-oscillation-tlc.cjs calls detectLivenessProperties with configName and cfgPath', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-oscillation-tlc.cjs'), 'utf8');
  assert.match(src, /detectLivenessProperties\s*\(\s*configName\s*,\s*cfgPath\s*\)/,
    'run-oscillation-tlc.cjs must call detectLivenessProperties(configName, cfgPath)');
});

test('run-oscillation-tlc.cjs has inconclusive writeCheckResult call in source', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-oscillation-tlc.cjs'), 'utf8');
  assert.match(src, /result\s*:\s*['"]inconclusive['"]/,
    'run-oscillation-tlc.cjs must have result=inconclusive in writeCheckResult call');
});
```

Run `node --test bin/run-oscillation-tlc.test.cjs` — these three new tests MUST be RED (failing). Existing tests must remain GREEN.

Commit: `test(LIVE-02): add RED liveness-wiring stubs for run-oscillation-tlc`
  </action>
  <verify>
`node --test bin/run-oscillation-tlc.test.cjs` — the three new tests fail (RED), existing tests still pass.
  </verify>
  <done>Three new RED tests in run-oscillation-tlc.test.cjs. Existing tests green. Committed.</done>
</task>

<task id="W0-02" type="auto">
  <name>Task W0-02: Add RED liveness-wiring tests to run-breaker-tlc.test.cjs, run-protocol-tlc.test.cjs, run-account-manager-tlc.test.cjs</name>
  <files>bin/run-breaker-tlc.test.cjs, bin/run-protocol-tlc.test.cjs, bin/run-account-manager-tlc.test.cjs</files>
  <action>
Append the same three-test pattern to each of the three remaining test files. Adjust the `readFileSync` path and description string per file.

For `bin/run-breaker-tlc.test.cjs`:
```js
test('run-breaker-tlc.cjs requires run-tlc.cjs and destructures detectLivenessProperties', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-breaker-tlc.cjs'), 'utf8');
  assert.match(src, /require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-breaker-tlc.cjs must require ./run-tlc.cjs');
  assert.match(src, /\{\s*detectLivenessProperties\s*\}\s*=\s*require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-breaker-tlc.cjs must destructure detectLivenessProperties from ./run-tlc.cjs');
});

test('run-breaker-tlc.cjs calls detectLivenessProperties with configName and cfgPath', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-breaker-tlc.cjs'), 'utf8');
  assert.match(src, /detectLivenessProperties\s*\(\s*configName\s*,\s*cfgPath\s*\)/,
    'run-breaker-tlc.cjs must call detectLivenessProperties(configName, cfgPath)');
});

test('run-breaker-tlc.cjs has inconclusive writeCheckResult call in source', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-breaker-tlc.cjs'), 'utf8');
  assert.match(src, /result\s*:\s*['"]inconclusive['"]/,
    'run-breaker-tlc.cjs must have result=inconclusive in writeCheckResult call');
});
```

For `bin/run-protocol-tlc.test.cjs`:
```js
test('run-protocol-tlc.cjs requires run-tlc.cjs and destructures detectLivenessProperties', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-protocol-tlc.cjs'), 'utf8');
  assert.match(src, /require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-protocol-tlc.cjs must require ./run-tlc.cjs');
  assert.match(src, /\{\s*detectLivenessProperties\s*\}\s*=\s*require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-protocol-tlc.cjs must destructure detectLivenessProperties from ./run-tlc.cjs');
});

test('run-protocol-tlc.cjs calls detectLivenessProperties with configName and cfgPath', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-protocol-tlc.cjs'), 'utf8');
  assert.match(src, /detectLivenessProperties\s*\(\s*configName\s*,\s*cfgPath\s*\)/,
    'run-protocol-tlc.cjs must call detectLivenessProperties(configName, cfgPath)');
});

test('run-protocol-tlc.cjs has inconclusive writeCheckResult call in source', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-protocol-tlc.cjs'), 'utf8');
  assert.match(src, /result\s*:\s*['"]inconclusive['"]/,
    'run-protocol-tlc.cjs must have result=inconclusive in writeCheckResult call');
});
```

For `bin/run-account-manager-tlc.test.cjs`:
```js
test('run-account-manager-tlc.cjs requires run-tlc.cjs and destructures detectLivenessProperties', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-account-manager-tlc.cjs'), 'utf8');
  assert.match(src, /require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-account-manager-tlc.cjs must require ./run-tlc.cjs');
  assert.match(src, /\{\s*detectLivenessProperties\s*\}\s*=\s*require\(['"]\.\/run-tlc\.cjs['"]\)/,
    'run-account-manager-tlc.cjs must destructure detectLivenessProperties from ./run-tlc.cjs');
});

test('run-account-manager-tlc.cjs calls detectLivenessProperties with configName and cfgPath', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-account-manager-tlc.cjs'), 'utf8');
  assert.match(src, /detectLivenessProperties\s*\(\s*configName\s*,\s*cfgPath\s*\)/,
    'run-account-manager-tlc.cjs must call detectLivenessProperties(configName, cfgPath)');
});

test('run-account-manager-tlc.cjs has inconclusive writeCheckResult call in source', () => {
  const fs = require('fs');
  const src = fs.readFileSync(path.join(__dirname, 'run-account-manager-tlc.cjs'), 'utf8');
  assert.match(src, /result\s*:\s*['"]inconclusive['"]/,
    'run-account-manager-tlc.cjs must have result=inconclusive in writeCheckResult call');
});
```

Commit: `test(LIVE-02): add RED liveness-wiring stubs for breaker, protocol, account-manager TLC runners`
  </action>
  <verify>
```
node --test bin/run-breaker-tlc.test.cjs bin/run-protocol-tlc.test.cjs bin/run-account-manager-tlc.test.cjs
```
The three new tests in each file are RED. Existing tests remain green.
  </verify>
  <done>9 new RED tests across 3 test files. Existing tests green. Committed.</done>
</task>

</tasks>

<verification>
1. `node --test bin/run-oscillation-tlc.test.cjs` — 3 new tests RED, existing tests green
2. `node --test bin/run-breaker-tlc.test.cjs bin/run-protocol-tlc.test.cjs bin/run-account-manager-tlc.test.cjs` — 9 new tests RED (3 per file), existing tests green
3. Total: 12 new RED tests across 4 test files
</verification>

<success_criteria>
- All 4 runner test files contain exactly 3 new failing tests each (12 total)
- New tests assert: (1) import destructuring of detectLivenessProperties, (2) call with configName+cfgPath, (3) result=inconclusive path in source
- Zero regressions in previously-passing tests
- Both commits pushed, tests confirmed RED
</success_criteria>

<output>
After completion, create `.planning/phases/v0.19-07-wire-liveness-check-to-all-tlc-runners/v0.19-07-01-SUMMARY.md`
</output>
