---
plan: v0.19-07-02
title: Implementation — Wire detectLivenessProperties into All Four TLC Runners
status: complete
completed: 2026-02-27
---

# Summary: v0.19-07-02 — Implementation: Wire detectLivenessProperties into All TLC Runners

## What Was Built

Wired `detectLivenessProperties()` from `bin/run-tlc.cjs` into all 4 TLC runner files. Each runner now:
1. Destructures `detectLivenessProperties` from `require('./run-tlc.cjs')` at the top
2. Calls `detectLivenessProperties(configName, cfgPath)` on the TLC-pass path
3. Emits `result=inconclusive` (not `result=pass`) if any liveness properties lack a fairness declaration in `invariants.md`

Turned all 12 RED tests from v0.19-07-01 GREEN. Closes LIVE-02.

## Key Files Modified

### key-files.created
- bin/run-oscillation-tlc.cjs — wired detectLivenessProperties, added inconclusive path
- bin/run-breaker-tlc.cjs — wired detectLivenessProperties, added inconclusive path
- bin/run-protocol-tlc.cjs — wired detectLivenessProperties, added inconclusive path
- bin/run-account-manager-tlc.cjs — wired detectLivenessProperties, added inconclusive path

## Tasks Completed

| Task | Description | Status |
|------|-------------|--------|
| 01-01 | Wire detectLivenessProperties into run-oscillation-tlc.cjs | Done |
| 01-02 | Wire detectLivenessProperties into run-breaker-tlc.cjs | Done |
| 01-03 | Wire detectLivenessProperties into run-protocol-tlc.cjs and run-account-manager-tlc.cjs | Done |
| 02-01 | Run full test suite — zero regressions confirmed | Done |

## Verification

- All 28 tests across all 4 runner test files GREEN (28/28 pass, 0 fail)
- 12 LIVE-02 tests turned from RED to GREEN
- All 4 runners contain `detectLivenessProperties` import and call with `result=inconclusive` path
- `npm test` suite: 0 failures

## Commits

- `feat(LIVE-02): wire detectLivenessProperties into run-oscillation-tlc` (f36e9004)
- `feat(LIVE-02): wire detectLivenessProperties into run-breaker-tlc` (08bd30fc)
- `feat(LIVE-02): wire detectLivenessProperties into run-protocol-tlc` (10f2c47d)
- `feat(LIVE-02): wire detectLivenessProperties into run-account-manager-tlc` (752ff107)

## Self-Check: PASSED

All 4 runner files verified to contain `detectLivenessProperties` import and call. All tests green. LIVE-02 closed.
