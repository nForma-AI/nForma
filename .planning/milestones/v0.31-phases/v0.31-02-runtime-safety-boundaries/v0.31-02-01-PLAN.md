---
phase: v0.31-02-runtime-safety-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/nf-circuit-breaker.js
  - hooks/dist/nf-circuit-breaker.js
  - hooks/dist/nf-circuit-breaker.test.js
autonomous: true
requirements: [BRKR-01]
formal_artifacts:
  - create:
    - path: ".planning/formal/evidence/oscillation-signatures.json"
      type: "json"
      description: "Cross-session oscillation signature persistence file"

must_haves:
  truths:
    - "When circuit breaker fires, the oscillation trigger pattern (file set, alternation count, time window) is written to .planning/formal/evidence/oscillation-signatures.json as a structured JSON file that future sessions can read to preemptively detect the same signature"
    - "The breaker eventually returns to monitoring state after resolution (formal: MonitoringReachable) -- when breaker clears, the evidence signature is marked with resolved_at timestamp"
    - "Preemptive evidence detection on session startup warns on stderr for known unresolved signatures but does NOT block tool calls (warn-only, not block)"
    - "Evidence file is capped at 50 entries sorted by recency; signatures older than 30 days are pruned on read"
    - "Evidence file read/write failures are fail-open -- never block tool calls because evidence is unavailable"
  artifacts:
    - path: "hooks/nf-circuit-breaker.js"
      provides: "writeEvidenceSignature and checkPreemptiveEvidence functions for cross-session oscillation detection"
      contains: "writeEvidenceSignature"
    - path: "hooks/dist/nf-circuit-breaker.test.js"
      provides: "Tests for evidence write, read, preemptive detection, cap at 50, resolved_at marking, fail-open on missing file"
      contains: "oscillation-signatures"
  key_links:
    - from: "hooks/nf-circuit-breaker.js (writeEvidenceSignature)"
      to: ".planning/formal/evidence/oscillation-signatures.json"
      via: "fs.writeFileSync after oscillation detection in PreToolUse handler"
      pattern: "oscillation-signatures\\.json"
    - from: "hooks/nf-circuit-breaker.js (checkPreemptiveEvidence)"
      to: ".planning/formal/evidence/oscillation-signatures.json"
      via: "fs.readFileSync on startup for preemptive detection"
      pattern: "checkPreemptiveEvidence"
    - from: "hooks/nf-circuit-breaker.js (PostToolUse Haiku convergence)"
      to: ".planning/formal/evidence/oscillation-signatures.json"
      via: "Mark resolved_at when Haiku YES verdict clears breaker"
      pattern: "resolved_at"
---

<objective>
Add cross-session oscillation signature persistence to the circuit breaker hook. When oscillation is detected, write a structured evidence file to `.planning/formal/evidence/oscillation-signatures.json` with the file set hash, pattern hash, alternation count, and time window. On session startup, read the evidence file to warn about known unresolved signatures. When breaker clears (Haiku YES or manual reset), mark the signature as resolved.

Purpose: Satisfies BRKR-01 -- oscillation signatures persist across sessions so future sessions can preemptively detect recurring patterns. Maintains MonitoringReachable invariant by marking signatures resolved when breaker clears.
Output: Modified circuit breaker hook with evidence persistence, extended tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.31-02-runtime-safety-boundaries/v0.31-02-RESEARCH.md
@.planning/formal/spec/breaker/invariants.md
@hooks/nf-circuit-breaker.js
@hooks/dist/nf-circuit-breaker.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evidence write/read/resolve functions and wire into circuit breaker</name>
  <files>hooks/nf-circuit-breaker.js</files>
  <action>
Add three functions to hooks/nf-circuit-breaker.js:

1. **writeEvidenceSignature(gitRoot, fileSet, fileSets, fileSetHash, patternHash)** — Writes/updates an entry in `.planning/formal/evidence/oscillation-signatures.json`. Schema:
   ```json
   { "schema_version": 1, "signatures": [{ "id": "sig_{fileSetHash}", "file_set_hash": "...", "pattern_hash": "...", "files": [...], "alternation_count": N, "time_window": { "first_seen": "...", "last_seen": "..." }, "resolved_at": null, "resolved_by_commit": null, "session_id": "..." }] }
   ```
   - If file exists, parse it; if entry with same file_set_hash exists, update last_seen and increment alternation_count; otherwise push new entry.
   - Cap at 50 entries sorted by last_seen descending. Use fs.mkdirSync recursive. Wrap entirely in try/catch with stderr warning on failure (fail-open).

2. **checkPreemptiveEvidence(gitRoot, fileSets)** — Reads evidence file, checks current file sets against known unresolved signatures (resolved_at === null). Prune entries older than 30 days on read. Returns matching signature or null. Wrapped in try/catch returning null (fail-open). Use existing makeFileSetHash to hash each file set for comparison.

3. **markEvidenceResolved(gitRoot, fileSetHash, commit)** — Reads evidence file, finds entry by file_set_hash, sets resolved_at to now and resolved_by_commit. Fail-open.

Wire into existing code paths:
- After oscillation detection (where writeState is called, around line 677): call `writeEvidenceSignature(gitRoot, result.fileSet, fileSets, fileSetHash, patternHash)`.
- At the top of PreToolUse handler (after gitRoot check, before state check, around line 600): call `checkPreemptiveEvidence(gitRoot, fileSets)` — but note we need fileSets here. Move the `getCommitHashes` + `getCommitFileSets` calls BEFORE the state check, or alternatively call checkPreemptiveEvidence only when state is NOT active (i.e., before detection, as a preemptive warning). Best approach: call it right before `detectOscillation` using the already-fetched fileSets, and if a match is found, emit a stderr warning with the signature details (NOT a deny decision — warn only per research recommendation).
- In PostToolUse Haiku YES branch (around line 578): after clearing state file, call `markEvidenceResolved(gitRoot, makeFileSetHash(activeEntry.files), resolvedCommit)`.

Export the three new functions in module.exports for testing.

IMPORTANT: Do NOT change the existing oscillation detection algorithm or the deny/allow decision logic. These additions are purely additive.
  </action>
  <verify>
Run `npx vitest run hooks/dist/nf-circuit-breaker.test.js --bail 1` to confirm existing tests still pass. Then run `npx vitest run` for the full suite.
  </verify>
  <done>
hooks/nf-circuit-breaker.js has writeEvidenceSignature, checkPreemptiveEvidence, and markEvidenceResolved functions. Evidence is written on oscillation detection, checked preemptively before detection, and marked resolved on Haiku YES. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for evidence persistence, sync to dist, and install</name>
  <files>hooks/dist/nf-circuit-breaker.test.js, hooks/dist/nf-circuit-breaker.js</files>
  <action>
Extend hooks/dist/nf-circuit-breaker.test.js with tests for the new evidence functions. Test cases:

1. **writeEvidenceSignature creates new file** — Call with a temp dir, verify JSON written with schema_version 1, one signature entry with correct fields.
2. **writeEvidenceSignature updates existing entry** — Write once, write again with same fileSetHash, verify alternation_count incremented and last_seen updated.
3. **writeEvidenceSignature caps at 50 entries** — Write 55 entries, verify only 50 remain, sorted by last_seen descending.
4. **writeEvidenceSignature is fail-open** — Call with read-only dir, verify no throw (returns silently).
5. **checkPreemptiveEvidence returns match** — Write an unresolved signature, call check with matching file set, verify non-null return.
6. **checkPreemptiveEvidence returns null for resolved** — Write a resolved signature (resolved_at set), call check, verify null.
7. **checkPreemptiveEvidence returns null on missing file** — Call on empty dir, verify null (fail-open).
8. **checkPreemptiveEvidence prunes old entries** — Write entry with last_seen > 30 days ago, call check, verify pruned.
9. **markEvidenceResolved sets resolved_at and resolved_by_commit** — Write unresolved, call markResolved, read back, verify fields set.

Import the new functions from the module.exports. Use vitest `describe`/`it` blocks matching the existing test file patterns. Use `fs.mkdtempSync` for temp dirs.

After tests pass, sync the modified hook to dist and run install:
```bash
cp hooks/nf-circuit-breaker.js hooks/dist/nf-circuit-breaker.js
node bin/install.js --claude --global
```
  </action>
  <verify>
Run `npx vitest run hooks/dist/nf-circuit-breaker.test.js` — all new and existing tests pass. Run `npx vitest run` for full suite. Verify `diff hooks/nf-circuit-breaker.js hooks/dist/nf-circuit-breaker.js` shows no differences.
  </verify>
  <done>
9 new evidence persistence tests pass. Hook synced to hooks/dist/ and installed globally. Full test suite green.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run hooks/dist/nf-circuit-breaker.test.js` — all tests pass including new evidence tests
- `npx vitest run` — full suite green, no regressions
- `grep 'writeEvidenceSignature' hooks/nf-circuit-breaker.js` — function exists and is called
- `grep 'checkPreemptiveEvidence' hooks/nf-circuit-breaker.js` — function exists and is called
- `grep 'markEvidenceResolved' hooks/nf-circuit-breaker.js` — function exists and is called in PostToolUse handler
- `diff hooks/nf-circuit-breaker.js hooks/dist/nf-circuit-breaker.js` — no differences (synced)
</verification>

<success_criteria>
- Circuit breaker writes oscillation signature to `.planning/formal/evidence/oscillation-signatures.json` on detection
- Preemptive check warns (stderr) for known unresolved signatures before detection
- Haiku YES clears both state file AND marks evidence signature as resolved
- Evidence file capped at 50, old entries pruned, all operations fail-open
- All tests pass, hook synced and installed
</success_criteria>

<output>
After completion, create `.planning/phases/v0.31-02-runtime-safety-boundaries/v0.31-02-01-SUMMARY.md`
</output>
