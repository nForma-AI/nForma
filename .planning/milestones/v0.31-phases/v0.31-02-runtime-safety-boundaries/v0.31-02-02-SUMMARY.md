---
phase: v0.31-02-runtime-safety-boundaries
plan: 02
subsystem: quorum
tags: [latency, timeout, providers, quorum-dispatch]

requires:
  - phase: v0.31-02-01
    provides: preemptive-check evidence file schema
provides:
  - latency_budget_ms field on provider entries in providers.json
  - per-slot latency budget enforcement in call-quorum-slot.cjs
  - latency budget test suite (13 tests)
affects: [v0.31-02-03, quorum-dispatch, providers]

tech-stack:
  added: []
  patterns: [latency-budget-priority-over-timeout]

key-files:
  created:
    - bin/call-quorum-slot-latency.test.cjs
  modified:
    - bin/call-quorum-slot.cjs
    - bin/providers.json
    - package.json

key-decisions:
  - "latency_budget_ms takes priority over --timeout arg and quorum_timeout_ms when present"
  - "latency_budget_ms=0 or negative treated as not set (falls back to existing logic)"
  - "claude-5 and claude-6 set as example slots with latency_budget_ms=15000"

patterns-established:
  - "latency-budget-priority: latency_budget_ms is a hard ceiling that always wins when > 0"

requirements-completed: [LTCY-01]

duration: 7min
completed: 2026-03-08
---

# Phase v0.31-02 Plan 02: Per-Slot Latency Budget Summary

**Per-slot latency_budget_ms enforcement in quorum dispatch with priority over all other timeout sources**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-08T16:20:21Z
- **Completed:** 2026-03-08T16:27:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added latency_budget_ms enforcement to call-quorum-slot.cjs effective timeout computation
- Added latency_budget_ms field to all 12 providers in providers.json (null for most, 15000 for claude-5/claude-6)
- Created 13-test suite covering priority logic, edge cases (0, negative, undefined), and providers.json field presence

## Task Commits

Each task was committed atomically:

1. **Task 1: Add latency_budget_ms enforcement to call-quorum-slot.cjs** - `e350b993` (feat)
2. **Task 2: Add latency_budget_ms to providers.json and add tests** - `cfe3206c` (feat)

## Files Created/Modified
- `bin/call-quorum-slot.cjs` - Added latency_budget_ms priority in effective timeout computation + stderr log
- `bin/providers.json` - Added latency_budget_ms field to all 12 provider entries
- `bin/call-quorum-slot-latency.test.cjs` - 13 unit tests for latency budget priority logic
- `package.json` - Registered new test file in test:ci script

## Decisions Made
- latency_budget_ms takes priority over --timeout arg and quorum_timeout_ms when present; this is a hard ceiling for the user
- Values of 0 or negative are treated as "not set" to prevent accidental instant-kill on slots
- claude-5 and claude-6 chosen as example slots since they have the lowest existing quorum_timeout_ms values (10s and 8s)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- latency_budget_ms infrastructure ready for v0.31-02-03 (if applicable)
- TIMEOUT telemetry already supported via existing error classification in catch handler
- Backward compatible: slots without latency_budget_ms behave identically to before

---
*Phase: v0.31-02-runtime-safety-boundaries*
*Completed: 2026-03-08*
