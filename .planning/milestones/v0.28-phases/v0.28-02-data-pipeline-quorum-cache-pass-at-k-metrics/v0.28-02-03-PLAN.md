---
phase: v0.28-02-data-pipeline-quorum-cache-pass-at-k-metrics
plan: 03
type: execute
wave: 3
depends_on: ["v0.28-02-02"]
files_modified:
  - hooks/nf-stop.js
  - hooks/dist/nf-stop.js
  - bin/verify-quorum-health.cjs
  - bin/verify-quorum-health.test.cjs
autonomous: true
requirements: [PASSK-01, PASSK-02]
formal_artifacts: none

must_haves:
  truths:
    - "verify-quorum-health.cjs reports pass@1, pass@3, and pass@k rates computed from conformance event history"
    - "Quorum still reaches a DECIDED state on every run where at least one slot responds, whether results come from cache or live dispatch (formal: EventualConsensus invariant preserved)"
  artifacts:
    - path: "hooks/nf-stop.js"
      provides: "pass_at_k field on quorum_complete conformance events"
      contains: "pass_at_k"
    - path: "bin/verify-quorum-health.cjs"
      provides: "pass@k rate computation and reporting"
      contains: "pass_at_1"
    - path: "bin/verify-quorum-health.test.cjs"
      provides: "Tests for pass@k reporting"
      contains: "pass_at_k"
  key_links:
    - from: "hooks/nf-stop.js"
      to: ".planning/telemetry/conformance-events.jsonl"
      via: "appendConformanceEvent with pass_at_k field"
      pattern: "pass_at_k"
    - from: "bin/verify-quorum-health.cjs"
      to: ".planning/telemetry/conformance-events.jsonl"
      via: "reads conformance events to compute pass@k rates"
      pattern: "pass_at_k|pass_at_1|pass_at_3"
---

<objective>
Add pass@k consensus efficiency tracking: nf-stop.js records how many rounds it took to reach consensus, and verify-quorum-health.cjs computes and reports pass@1, pass@3, and average-k rates.

Purpose: Quantifies consensus efficiency to enable data-driven tuning of maxDeliberation. Provides visibility into whether the quorum typically agrees on first round or requires deliberation.
Output: pass_at_k field on quorum_complete events, pass@k reporting section in verify-quorum-health.cjs.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.28-02-data-pipeline-quorum-cache-pass-at-k-metrics/v0.28-02-RESEARCH.md
@.planning/phases/v0.28-02-data-pipeline-quorum-cache-pass-at-k-metrics/v0.28-02-02-SUMMARY.md
@hooks/nf-stop.js
@bin/verify-quorum-health.cjs
@bin/verify-quorum-health.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pass_at_k field to quorum_complete events in nf-stop.js</name>
  <files>
    hooks/nf-stop.js
    hooks/dist/nf-stop.js
  </files>
  <action>
In `hooks/nf-stop.js`:

1. **Add `countDeliberationRounds(currentTurnLines)` function**: Count the number of distinct dispatch rounds by scanning assistant messages for Task tool_use blocks that reference `nf-quorum-slot-worker`. Count unique dispatch groups (one group per assistant message that contains at least one slot-worker Task call). This counts dispatch rounds, not individual slot calls. Return `Math.max(1, rounds)` — at least round 1 even if no explicit dispatch is detected (handles pre-existing events gracefully).

Implementation detail: Iterate `currentTurnLines`, parse each JSON entry. For entries with `type === 'assistant'`, check if `entry.message.content` (array) contains any `tool_use` block where `block.name === 'Task'` and `JSON.stringify(block.input || {}).includes('nf-quorum-slot-worker')`. If so, increment round counter (once per assistant message, not per tool_use block — multiple parallel dispatches in one message = one round).

2. **Add pass_at_k to quorum_complete events**: In the existing code path where a `quorum_complete` conformance event is appended (on successful quorum approval), add `pass_at_k: countDeliberationRounds(currentTurnLines)` to the event object.

3. **Cache-hit events**: For cache-hit quorum_complete events (added in Plan 02), set `pass_at_k: 0` to distinguish cached results from actual round-based decisions.

After editing, copy: `cp hooks/nf-stop.js hooks/dist/nf-stop.js`
Then run: `node bin/install.js --claude --global`
  </action>
  <verify>
Run: `node -c hooks/nf-stop.js` — syntax valid.
Run: `grep -c 'pass_at_k' hooks/nf-stop.js` — returns at least 2 (function + event field).
Run: `grep -c 'countDeliberationRounds' hooks/nf-stop.js` — returns at least 2 (definition + call).
Run: `diff hooks/nf-stop.js hooks/dist/nf-stop.js` — no differences.
Run: `node bin/install.js --claude --global 2>&1 | tail -5` — install succeeds.
  </verify>
  <done>
nf-stop.js counts deliberation rounds per quorum and records pass_at_k on every quorum_complete conformance event. Cache-hit events get pass_at_k: 0. Hook synced to dist and globally installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pass@k reporting to verify-quorum-health.cjs with tests</name>
  <files>
    bin/verify-quorum-health.cjs
    bin/verify-quorum-health.test.cjs
  </files>
  <action>
In `bin/verify-quorum-health.cjs`:

1. **Add `computePassAtKRates(conformanceEventsPath)` function**: Read the JSONL file, filter for `action === 'quorum_complete'` and `outcome === 'APPROVE'` events that have a `pass_at_k` field (skip pre-PASSK events and cache_hit events where pass_at_k === 0). Compute:
   - `total`: count of qualifying events
   - `pass_at_1`: fraction where `pass_at_k === 1`
   - `pass_at_3`: fraction where `pass_at_k <= 3`
   - `avg_k`: average pass_at_k value
   Return `{ total, pass_at_1, pass_at_3, avg_k }`. All rates as floats 0.0-1.0.

2. **Add reporting section**: In the main output flow (where the tool prints its analysis), add a `## Pass@k Consensus Efficiency` section that displays:
   - `pass@1: XX.X%` (first-round consensus rate)
   - `pass@3: XX.X%` (consensus within 3 rounds)
   - `avg rounds: X.X`
   - `total quorum events: N`
   If no events have pass_at_k data, print "No pass@k data available yet (requires quorum runs after v0.28-02)."

3. **Export `computePassAtKRates`** so tests can import it.

In `bin/verify-quorum-health.test.cjs`:

Add tests for pass@k reporting:

1. **computePassAtKRates with mixed events** — create temp JSONL with 3 quorum_complete events (pass_at_k: 1, 2, 1), verify pass_at_1 === 2/3, pass_at_3 === 1.0, avg_k === 4/3.
2. **computePassAtKRates skips cache hits** — include events with pass_at_k: 0, verify they are excluded from rates.
3. **computePassAtKRates skips pre-PASSK events** — events without pass_at_k field are excluded.
4. **computePassAtKRates empty file** — returns total: 0, rates: 0.
5. **Full script output includes pass@k section** — spawn the script with a temp conformance file containing pass_at_k events, verify stdout contains "pass@1" and "pass@3".
  </action>
  <verify>
Run: `node --test bin/verify-quorum-health.test.cjs` — all tests pass (existing + new).
Run: `node -c bin/verify-quorum-health.cjs` — syntax valid.
Run: `grep -c 'pass_at_1\|pass_at_3\|pass_at_k' bin/verify-quorum-health.cjs` — returns at least 5.
Run: `npm run test:ci 2>&1 | tail -10` — full suite passes, no regressions.
  </verify>
  <done>
verify-quorum-health.cjs computes and reports pass@1, pass@3, and average-k rates from conformance event history. Tests verify computation accuracy, cache-hit exclusion, backward compatibility with pre-PASSK events, and end-to-end script output.
  </done>
</task>

</tasks>

<verification>
- `node --test bin/verify-quorum-health.test.cjs` — all pass@k tests pass
- `node -c hooks/nf-stop.js && node -c bin/verify-quorum-health.cjs` — syntax valid
- `npm run test:ci` — no regressions
- `grep 'pass_at_k' hooks/nf-stop.js` — field present on quorum_complete events
- `grep 'pass_at_1' bin/verify-quorum-health.cjs` — reporting present
</verification>

<success_criteria>
pass_at_k field recorded on every quorum_complete conformance event. verify-quorum-health.cjs reports pass@1, pass@3, and average-k rates. All existing and new tests pass. EventualConsensus invariant preserved — pass@k is observational metadata that does not affect quorum decision flow.
</success_criteria>

<output>
After completion, create `.planning/phases/v0.28-02-data-pipeline-quorum-cache-pass-at-k-metrics/v0.28-02-03-SUMMARY.md`
</output>
