---
phase: v0.28-01-foundation-hook-profiles-de-sloppify
plan: 01
subsystem: hooks
tags: [config, hooks, profiles, shouldRunHook]

# Dependency graph
requires: []
provides:
  - HOOK_PROFILE_MAP constant mapping minimal/standard/strict to hook sets
  - shouldRunHook() helper for profile-based hook gating
  - hook_profile validation in config-loader validateConfig()
  - hook_profile default in DEFAULT_CONFIG
affects: [v0.28-01-02, v0.28-01-03]

# Tech tracking
tech-stack:
  added: []
  patterns: [profile-map-with-Set, shouldRunHook-guard-pattern]

key-files:
  created: []
  modified:
    - hooks/config-loader.js
    - hooks/config-loader.test.js

key-decisions:
  - "standard and strict share identical hook sets -- behavioral difference handled in individual hooks not profile map"
  - "nf-check-update excluded from profile map -- SessionStart hook runs independently"

patterns-established:
  - "Hook profile gating via shouldRunHook(hookBasename, profile) returning boolean"
  - "Profile validation with stderr warning and fallback to standard"

requirements-completed: [PROF-01, PROF-04]

# Metrics
duration: 2min
completed: 2026-03-06
---

# Phase v0.28-01 Plan 01: Hook Profile Infrastructure Summary

**HOOK_PROFILE_MAP with 3 profiles (minimal/standard/strict), shouldRunHook() guard, and hook_profile config validation in config-loader.js**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-06T12:47:02Z
- **Completed:** 2026-03-06T12:48:46Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added HOOK_PROFILE_MAP constant with minimal (2 hooks), standard (10 hooks), and strict (10 hooks) profiles
- Added shouldRunHook() function that gates hook execution by profile with fallback to standard
- Added hook_profile to DEFAULT_CONFIG and validateConfig() with stderr warning on invalid values
- Added 18 new tests covering profile validation, shouldRunHook behavior, and HOOK_PROFILE_MAP structure

## Task Commits

Each task was committed atomically:

1. **Task 1: Add HOOK_PROFILE_MAP, shouldRunHook(), hook_profile to DEFAULT_CONFIG and validateConfig()** - `3b6a65bd` (feat)
2. **Task 2: Add config-loader tests for hook_profile, shouldRunHook(), and HOOK_PROFILE_MAP** - `114a8b5b` (test)

## Files Created/Modified
- `hooks/config-loader.js` - Added HOOK_PROFILE_MAP, shouldRunHook(), hook_profile default + validation, updated exports
- `hooks/config-loader.test.js` - Added 18 tests: 4 validation, 9 shouldRunHook, 5 structure

## Decisions Made
- standard and strict profiles share identical hook sets -- the behavioral difference (strict expands quorum scope) is handled inside nf-prompt.js and nf-stop.js, not by the profile map
- nf-check-update is excluded from all profiles -- it is a SessionStart hook that runs independently of the profile system

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- shouldRunHook() and HOOK_PROFILE_MAP are exported and ready for consumption by Plan 02 (hook guard integration)
- All 43 config-loader tests pass

---
*Phase: v0.28-01-foundation-hook-profiles-de-sloppify*
*Completed: 2026-03-06*
