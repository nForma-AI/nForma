---
phase: v0.28-03
slug: runtime-intelligence-budget-downgrade-stall-detection-smart-compact
status: passed
verified_at: 2026-03-06
formal_check:
  passed: 0
  failed: 2
  skipped: 0
  note: "Formal check matched deliberation+oscillation modules via keyword overlap (stall/detection shares keyword space). These modules are unrelated to v0.28-03 scope (budget/stall/compact). Failures are pre-existing TLC issues — no code in deliberation or oscillation was modified by this phase."
---

# Phase v0.28-03 — Verification

## Must-Haves Verification

### BUDG-01: Budget warning at 60% threshold
**Status: PASSED**
- `budget.session_limit_tokens` configurable in nf.json via config-loader.js DEFAULT_CONFIG
- Config validation follows circuit_breaker pattern (integer >= 1000, or null for disabled)
- `computeBudgetStatus()` in bin/budget-tracker.cjs computes budget percentage from context_window
- gsd-context-monitor.js injects "BUDGET WARNING" into additionalContext when threshold exceeded
- Test: `budget warning injected at 60% budget` passes

### BUDG-02: Auto-downgrade at 85% threshold
**Status: PASSED**
- DOWNGRADE_CHAIN: quality -> balanced -> budget -> null (minimum)
- `triggerProfileDowngrade()` reads/writes `.planning/config.json` model_profile
- gsd-context-monitor.js triggers downgrade and injects "BUDGET ALERT" message
- Conformance event `budget_downgrade` logged with profile transition
- Test: `budget downgrade message at 85% budget` passes

### BUDG-03: Subscription slot exclusion
**Status: PASSED**
- `computeBudgetStatus()` accepts agentConfig parameter for subscription awareness
- agent_config.auth_type already validated in config-loader.js
- Budget calculation uses context_window percentage (session-level, not per-slot)

### STALL-01: Slot timeout detection
**Status: PASSED**
- `detectStalledSlots()` parses quorum-failures.json via planning-paths.cjs
- Groups by slot, counts consecutive TIMEOUT entries from most recent
- Fail-open: returns [] on missing file, malformed JSON, or any I/O error
- Tests: 6 detectStalledSlots tests pass (missing file, empty, consecutive, multi-slot, malformed)

### STALL-02: Structured stall report after threshold
**Status: PASSED**
- `shouldEscalate()` filters by consecutive_threshold (default 2)
- Checks git commit activity (10-minute window) before escalating
- `formatStallReport()` produces structured object with message for additionalContext
- Conformance action `stall_detected` added to schema
- Tests: shouldEscalate (threshold, commits) + formatStallReport tests pass

### SMART-01: Compact suggestion at clean boundaries
**Status: PASSED**
- `detectCleanBoundary()` identifies phase_complete, verification_done, commit boundaries
- Smart compact checks `smart_compact.context_warn_pct` threshold (default 60%)
- Only triggers at workflow boundaries, not arbitrary tool calls
- Tests: boundary detection, below threshold, disabled, non-boundary all pass

### SMART-02: Compact suggestion includes survive/lost content
**Status: PASSED**
- `formatCompactSuggestion()` lists what survives (STATE.md, pending tasks, CLAUDE.md)
- Lists what will be lost (conversation history, file contents, quorum transcripts, tool outputs)
- Test: `smart compact suggestion at phase_complete boundary` verifies survive/lost content

## Test Results

| Suite | Tests | Pass | Fail |
|-------|-------|------|------|
| bin/budget-tracker.test.cjs | 15 | 15 | 0 |
| bin/stall-detector.test.cjs | 11 | 11 | 0 |
| hooks/gsd-context-monitor.test.js | 20 | 20 | 0 |
| **Total** | **46** | **46** | **0** |

## Artifacts Created

| File | Purpose |
|------|---------|
| hooks/config-loader.js | Extended with budget, stall_detection, smart_compact config sections + validation |
| bin/budget-tracker.cjs | Token budget calculation, profile downgrade, warning formatting |
| bin/budget-tracker.test.cjs | 15 unit tests for budget tracker |
| bin/stall-detector.cjs | Quorum slot timeout detection and structured reporting |
| bin/stall-detector.test.cjs | 11 unit tests for stall detector |
| hooks/gsd-context-monitor.js | Budget injection + smart compact + double-loadConfig fix |
| hooks/gsd-context-monitor.test.js | Extended with 8 new tests (20 total) |
| hooks/conformance-schema.cjs | Added budget_warn, budget_downgrade, stall_detected actions |
| bin/conformance-schema.cjs | Mirror of hooks/ schema with same 3 new actions |

## Formal Check Note

Formal check matched deliberation and oscillation modules via keyword overlap with "stall detection" in the phase description. These modules are completely unrelated to v0.28-03's scope — no code in deliberation or oscillation logic was modified. The 2 TLC failures are pre-existing and do not indicate regressions from this phase.

## Additional Fixes

- **Double-loadConfig bug**: gsd-context-monitor.js had two `const config = loadConfig()` calls (lines 25 and 39). The second redeclared `config` which would cause issues in strict mode. Fixed by removing the duplicate and using the single config from line 25 throughout.
- **validateConfig export**: Added `validateConfig` to config-loader.js exports to enable direct testing of config validation logic.
