---
phase: v0.30-01-dynamic-model-selection
plan: 02
subsystem: observability
tags: [tokens, cost-estimation, dashboard, cli, jsonl]

requires:
  - phase: v0.30-01-dynamic-model-selection
    provides: "nf-token-collector.js writes token-usage.jsonl records"
provides:
  - "Token usage dashboard CLI with slot aggregation and cost estimation"
  - "/nf:tokens slash command for interactive dashboard access"
affects: [budget-tracker, cost-control, model-selection]

tech-stack:
  added: []
  patterns: ["JSONL line-by-line parsing with fail-open", "Hardcoded cost table with subscription slot handling"]

key-files:
  created:
    - bin/token-dashboard.cjs
    - bin/token-dashboard.test.cjs
    - commands/nf/tokens.md
  modified:
    - package.json

key-decisions:
  - "Hardcoded cost table instead of fetching live pricing -- keeps zero-dependency constraint"
  - "Copilot displays 'subscription' instead of '$0.00' to avoid user confusion"
  - "Cache read tokens charged at 10% of input rate for cost estimation"

patterns-established:
  - "Subscription-based slot display: check COST_PER_M[family].subscription === true, render 'subscription' in cost column"
  - "Slot family extraction: strip trailing -N from slot names for aggregation"

requirements-completed: [TOKN-03]

duration: 3min
completed: 2026-03-07
---

# Phase v0.30-01 Plan 02: Token Dashboard Summary

**Token usage dashboard CLI aggregating JSONL cost data by slot family and session with subscription-aware display**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-07T16:29:27Z
- **Completed:** 2026-03-07T16:32:16Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Token dashboard parses token-usage.jsonl with fail-open handling for missing/malformed files
- Aggregation by slot family (strips -N suffix) and by session with cache hit percentage
- Cost estimation using hardcoded rate table, copilot shows "subscription" not "$0.00"
- /nf:tokens slash command registered and test file added to test:ci

## Task Commits

Each task was committed atomically:

1. **Task 1: Create token dashboard CLI with aggregation and cost estimation** - `9d54daa2` (feat)
2. **Task 2: Create /nf:tokens command and register tests** - `4ce23b53` (feat)

## Files Created/Modified
- `bin/token-dashboard.cjs` - CLI tool: parses JSONL, aggregates by slot/session, estimates costs, formats dashboard table
- `bin/token-dashboard.test.cjs` - 14 unit tests covering parsing, aggregation, cost estimation, and display formatting
- `commands/nf/tokens.md` - Slash command definition for /nf:tokens
- `package.json` - Added token-dashboard.test.cjs to test:ci script

## Decisions Made
- Hardcoded cost table (COST_PER_M) instead of live API pricing to maintain zero-dependency constraint
- Copilot displays "subscription" instead of "$0.00" to avoid user confusion about zero-cost display
- Cache read tokens charged at 10% of input rate for cost estimation
- Used node:test and node:assert for tests consistent with existing project patterns

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Token dashboard is functional and ready for use via /nf:tokens
- Budget tracker (bin/budget-tracker.cjs) can reference dashboard cost estimation in future integration
- Plan 03 can build on this for budget-aware model routing

---
*Phase: v0.30-01-dynamic-model-selection*
*Completed: 2026-03-07*
