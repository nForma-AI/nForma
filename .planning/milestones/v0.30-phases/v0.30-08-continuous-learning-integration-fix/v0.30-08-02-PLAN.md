---
phase: v0.30-08-continuous-learning-integration-fix
plan: 02
type: execute
wave: 2
depends_on:
  - v0.30-08-01
files_modified:
  - .planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md
autonomous: true
requirements:
  - LRNG-01
  - LRNG-02
  - LRNG-03
  - LRNG-04
formal_artifacts: none

must_haves:
  truths:
    - "Phase v0.30-04 has a VERIFICATION.md that validates LRNG-01..04 satisfaction with evidence"
    - "node bin/install.js --claude --global registers nf-session-end.js as a SessionEnd hook in settings.json — re-install does not silently drop it"
    - "skill-extractor.cjs is invoked automatically by nf-session-end.js during session end processing (not orphaned)"
    - "context_retrieval_enabled config key uses config-loader two-layer merge (DEFAULT_CONFIG -> global -> project) instead of direct .claude/nf.json read"
    - "Uninstall/reinstall lifecycle preserves correct SessionEnd hook state at each step"
  artifacts:
    - path: ".planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md"
      provides: "LRNG-01..04 verification with command evidence"
      contains: "LRNG-01"
      min_lines: 40
  key_links:
    - from: ".planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md"
      to: "bin/install.js"
      via: "Verification evidence commands"
      pattern: "install\\.js"
    - from: ".planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md"
      to: "hooks/nf-session-end.js"
      via: "Verification evidence for skill-extractor wiring"
      pattern: "skill-extractor"
---

<objective>
Produce VERIFICATION.md for phase v0.30-04 that validates LRNG-01..04 satisfaction with concrete evidence from the v0.30-08 fixes.

Purpose: The v0.30 milestone audit found that v0.30-04 had no VERIFICATION.md. This plan creates one that proves all four LRNG requirements are satisfied AFTER the v0.30-08-01 fixes are applied.

Output: VERIFICATION.md file in the v0.30-04 phase directory with command output evidence.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v0.30-MILESTONE-AUDIT.md
@.planning/phases/v0.30-08-continuous-learning-integration-fix/v0.30-08-01-SUMMARY.md
@bin/install.js
@hooks/nf-session-end.js
@hooks/config-loader.js
@bin/quorum-slot-dispatch.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VERIFICATION.md for v0.30-04 with LRNG-01..04 evidence</name>
  <files>.planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md</files>
  <action>
Create a VERIFICATION.md file in the v0.30-04 phase directory. This file must verify the state AFTER all v0.30-08 fixes are applied.

**Structure:**
Use the standard VERIFICATION.md format with frontmatter (phase, verified_date, status, requirements_checked).

**For each requirement, run actual commands and capture evidence:**

**LRNG-01 (Auto error extraction at session end):**
- Run `node bin/install.js --claude --global` and capture output showing SessionEnd hook registration
- Run `grep 'nf-session-end' ~/.claude/settings.json` to prove the hook is registered
- Run `node bin/install.js --claude --global` again to prove idempotency (no duplicate)
- Run `grep -c 'learning-extractor\|extractErrors' hooks/nf-session-end.js` to show error extraction code exists
- Status: SATISFIED with evidence

**LRNG-02 (User correction capture):**
- Run `grep -c 'correction\|extractCorrections' hooks/nf-session-end.js` to show correction extraction exists
- Run `grep -c 'appendEntry.*correction' hooks/nf-session-end.js` to show persistence
- Status: SATISFIED with evidence

**LRNG-03 (Quorum-validated skill extraction):**
- Run `grep -c 'skill-extractor\|skill_candidates' hooks/nf-session-end.js` to prove wiring exists
- Run `grep -c 'clusterByTags\|generateCandidates' hooks/nf-session-end.js` to show extraction functions called
- Run `grep -c 'pending_validation' hooks/nf-session-end.js` to show candidate status tagging
- Status: SATISFIED with evidence

**LRNG-04 (Failure catalog with confidence):**
- Run `grep -c 'confidence\|failure' bin/memory-store.cjs` to show confidence field support
- Run `grep -c 'extractErrors\|failures' hooks/nf-session-end.js` to show failure extraction
- Status: SATISFIED with evidence

**Additional cross-cutting evidence:**
- Run `npm test` to show full test suite passes
- Run `npx vitest run hooks/dist/nf-session-end.test.js` to show SessionEnd hook tests pass
- Run `grep 'context_retrieval_enabled' hooks/config-loader.js` to show config convention compliance

**Uninstall/reinstall lifecycle test (cross-cutting):**
This validates the full install lifecycle, not just initial registration:
1. Run `node bin/install.js --claude --global --uninstall` and capture output
2. Run `grep -c 'nf-session-end' ~/.claude/settings.json` — verify returns 0 (hook removed)
3. Run `node bin/install.js --claude --global` and capture output
4. Run `grep -c 'nf-session-end' ~/.claude/settings.json` — verify returns at least 1 (hook restored)
Include the command output from all 4 steps as evidence in the VERIFICATION.md.

**Format:** Each requirement section should have: Requirement ID, Description, Evidence (command + output), Verdict (SATISFIED/UNSATISFIED).

**Note:** This VERIFICATION.md covers the POST-fix state. Reference the v0.30-MILESTONE-AUDIT.md for the pre-fix state and the v0.30-08-01 plan for what was fixed.
  </action>
  <verify>
File exists: `test -f .planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md`
Contains all 4 requirement IDs: `grep -c 'LRNG-0[1-4]' .planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md` returns 4 or more.
Contains evidence: `grep -c 'SATISFIED' .planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md` returns 4 (one per requirement).
Contains lifecycle test: `grep -c 'uninstall' .planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md` returns at least 1.
  </verify>
  <done>VERIFICATION.md exists in v0.30-04 phase directory, validates all four LRNG requirements with command evidence, includes uninstall/reinstall lifecycle test, and shows SATISFIED status for each.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/v0.30-04-continuous-learning/v0.30-04-VERIFICATION.md` exists and is well-formed
2. All four LRNG-01..04 requirements are individually verified with evidence
3. Evidence includes actual command output (not hypothetical)
4. No requirement is marked SATISFIED without supporting evidence
5. Uninstall/reinstall lifecycle test is included with evidence from all 4 steps
</verification>

<success_criteria>
- VERIFICATION.md exists with evidence for all 4 LRNG requirements
- Each requirement has concrete command output proving satisfaction
- Uninstall/reinstall lifecycle test validates full hook lifecycle
- The v0.30 milestone audit gap "Phase 04 has no VERIFICATION.md" is closed
</success_criteria>

<output>
After completion, create `.planning/phases/v0.30-08-continuous-learning-integration-fix/v0.30-08-02-SUMMARY.md`
</output>
