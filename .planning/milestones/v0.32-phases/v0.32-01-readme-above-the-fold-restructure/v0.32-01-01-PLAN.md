---
phase: v0.32-01-readme-above-the-fold-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
formal_artifacts: none
requirements: [RDME-01, RDME-02, RDME-03, RDME-04, RDME-06, RDME-07]

must_haves:
  truths:
    - "The TUI section with a hero screenshot is visible without expanding any collapsible element"
    - "'Who This Is For' lists 3-4 concrete problems (blind spots, context rot, oscillation, manual tracking) a reader can self-identify with"
    - "A 'With vs. Without' comparison table appears directly after 'Who This Is For' showing Claude Code alone vs. with nForma"
    - "A 'By the Numbers' section shows at least 5 quantified metrics (agents, commands, requirements, hooks, milestones)"
    - "Nav bar contains anchor links for TUI, Configuration, and Star History sections"
  artifacts:
    - path: "README.md"
      provides: "Restructured above-the-fold content with TUI hero, value props, comparison, metrics, changelog, and expanded nav"
      contains: "## Terminal UI"
  key_links:
    - from: "Nav bar anchor links"
      to: "Section headings"
      via: "GitHub auto-generated heading anchors"
      pattern: "\\[.*TUI.*\\]\\(#terminal-ui\\).*\\[.*Configuration.*\\]\\(#configuration-reference\\).*\\[.*Star History.*\\]\\(#star-history\\)"
---

<objective>
Restructure the top portion of README.md so a first-time visitor immediately sees what nForma does, who it helps, how it compares to Claude Code alone, and what the TUI looks like -- all without scrolling or expanding collapsible sections.

Purpose: The current README buries the TUI inside a collapsed `<details>` block, has a single-sentence "Who This Is For", lacks any comparison or metrics section, and the nav bar omits key sections. This plan promotes visual and value-prop content above the fold.

Output: A restructured README.md with 5 new/expanded sections above the fold and an expanded nav bar.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.32-01-readme-above-the-fold-restructure/v0.32-01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README above-the-fold with new sections</name>
  <files>README.md</files>
  <action>
Restructure the top of README.md by adding 5 new/expanded sections between the centered header div and "Why I Built nForma". The target structure for the top of the file is:

1. **Existing centered header** (title, badges, install command, terminal.svg) -- keep unchanged
2. **Expanded nav bar** (inside the centered div, replacing the current nav line) -- add TUI, Configuration, Star History anchors:
   ```
   [Why nForma](#why-i-built-nforma) · [TUI](#terminal-ui) · [How It Works](#how-it-works) · [Features](#features) · [Commands](#commands) · [Configuration](#configuration-reference) · [Star History](#star-history) · [User Guide](docs/USER-GUIDE.md)
   ```
   **Note on `#configuration-reference` anchor:** GitHub generates anchors from `<summary>` text inside `<details>` blocks, but behavior can be inconsistent. If "Configuration Reference" is rendered via `<details><summary>` rather than a standard `## heading`, the anchor may not resolve. After writing, verify this anchor resolves correctly (see verify block). If it does not, either add a manual anchor `<a id="configuration-reference"></a>` before the details block, or change the nav link to point to the nearest valid heading (e.g., `#configuration`).
3. **`---` separator** (existing)
4. **`## Terminal UI`** section (RDME-01) -- Extract TUI content from the `<details><summary>Agent Manager TUI</summary>` block in Getting Started (lines ~133-177). Place it as a visible top-level section. Include:
   - One-line description: "A full-featured keyboard-navigable terminal interface for managing your quorum agents -- add, edit, reorder, health-check, and rotate keys without touching config files."
   - Hero image using HTML img tag for size control on GitHub: `<img src="docs/assets/tui-agents.png" alt="Agent Manager TUI" width="720">` (use HTML `<img>` with `width="720"` to prevent oversized rendering on GitHub -- per quorum consensus)
   - Module table (Agents, Reqs, Config, Sessions, Solve) with what-it-does column
   - Navigation instructions line
   - Do NOT include the extra screenshots (tui-reqs.png, tui-session-claude.png) or the git clone/install instructions -- those belong in Getting Started. Keep this section concise as a hero showcase.

5. **`## Who This Is For`** (RDME-02) -- Replace the single-sentence version (line ~48-50) with problem-framing bullets. Use this exact structure:
   ```markdown
   ## Who This Is For

   If you use Claude Code and have hit any of these walls:

   - **Single-model blind spots** -- One model misses edge cases another would catch
   - **Context rot** -- Quality degrades as your context window fills with accumulated garbage
   - **Oscillation loops** -- The agent ping-pongs between two files, fixing one and breaking the other
   - **Manual tracking** -- You're the one remembering what's done, what's next, and what broke

   nForma fixes these structurally, not with better prompts.
   ```

6. **`## With vs. Without`** (RDME-03) -- New section immediately after Who This Is For. Use a GFM comparison table:
   ```markdown
   ## With vs. Without

   | Capability | Claude Code Alone | With nForma |
   |------------|:-----------------:|:-----------:|
   | Planning reviewed before execution | Manual review | 5-model quorum consensus |
   | Ping-pong loop detection | None | Automatic circuit breaker |
   | Context window freshness | Degrades over session | Fresh 200k per task |
   | Requirements traceability | Ad-hoc notes | Formal, auditable, gate-scored |
   | Test coverage verification | Manual | Pre-execution test mapping |
   | Session memory across restarts | Lost | STATE.md + auto-resume |
   ```

7. **`## By the Numbers`** (RDME-04) -- New section after With vs. Without. Use a compact 2x3 grid table with at least 5 metrics:
   ```markdown
   ## By the Numbers

   | | |
   |---|---|
   | **5+** quorum agents | **30+** slash commands |
   | **287** tracked requirements | **7** lifecycle hooks |
   | **15+** formal verification models | **31** milestones shipped |
   ```

8. **`## What's New in v0.32`** (RDME-06) -- New section after By the Numbers. Frame items honestly -- the README overhaul is the v0.32 deliverable; gate scoring and solve loops were recently shipped (v0.31 era) and are highlighted here as recent additions readers may have missed:
   ```markdown
   ## What's New in v0.32

   - **README overhaul** -- Restructured for immediate clarity; TUI, metrics, and comparisons above the fold
   - **Gate scoring TUI** -- Recently shipped: browse requirement gate scores directly in the TUI (F2 module)
   - **Solve feedback loops** -- Recently shipped: TUI Solve module now closes all diagnostic-to-remediation loops
   - **31 milestones shipped** -- From v0.1 through v0.31, covering quorum, formal verification, agent harness, and more
   ```

9. **`---` separator** before "Why I Built nForma"

After the new sections, the rest of the README continues unchanged: "Why I Built nForma", "Getting Started", etc.

**Critical detail for Getting Started:** After extracting TUI content, replace the `<details><summary>Agent Manager TUI</summary>...</details>` block in Getting Started with a brief cross-reference:
```markdown
<details>
<summary><strong>Agent Manager TUI</strong></summary>

See [Terminal UI](#terminal-ui) above for the full TUI overview.

To launch:

```bash
# Requires a local clone
git clone https://github.com/nForma-AI/nForma.git
cd nForma && npm install

node bin/nForma.cjs
```

</details>
```

Keep the launch instructions in Getting Started since they require a local clone (not relevant to the hero section). Keep the `--cwd` flag note and extra screenshots (tui-reqs, tui-session-claude) -- move those to this abbreviated cross-reference section if they add value, or drop the extra screenshots since they're secondary.

**What NOT to change:** Everything from "How It Works" onward stays exactly as-is. Do not modify Features, Commands, Configuration, Star History, or License sections.

**Use `--` (double dash) for em-dashes, NOT unicode em-dash characters** -- match the existing README style.
  </action>
  <verify>
Run these structural checks:
```bash
# RDME-01: TUI section is a visible heading, not inside details
grep -n "^## Terminal UI" README.md
# Confirm it's NOT inside a details block (no <details> before it without closing)

# RDME-01: Hero image uses HTML img tag with width constraint
grep -q '<img src="docs/assets/tui-agents.png"' README.md && echo "RDME-01 img: PASS" || echo "RDME-01 img: FAIL"

# RDME-02: Who This Is For has 3-4 problem bullets
grep -c "blind spots\|context rot\|oscillation\|tracking" README.md | awk '{print ($1>=3) ? "RDME-02: PASS" : "RDME-02: FAIL"}'

# RDME-03: Comparison table exists
grep -q "Claude Code Alone" README.md && echo "RDME-03: PASS" || echo "RDME-03: FAIL"

# RDME-04: By the Numbers with 5+ metrics
grep -q "By the Numbers" README.md && echo "RDME-04: PASS" || echo "RDME-04: FAIL"

# RDME-06: What's New section with honest framing
grep -q "What's New" README.md && echo "RDME-06: PASS" || echo "RDME-06: FAIL"
grep -q "Recently shipped" README.md && echo "RDME-06 framing: PASS" || echo "RDME-06 framing: FAIL"

# RDME-07: Nav bar has TUI, Configuration, Star History
grep "Terminal UI\|TUI" README.md | grep -q "Configuration" && grep "Star History" README.md | head -1 && echo "RDME-07: check nav bar manually"

# RDME-07: Verify #configuration-reference anchor resolves
# Check if Configuration Reference is a standard heading or inside <details><summary>
if grep -q "^## Configuration Reference" README.md || grep -q "^### Configuration Reference" README.md; then
  echo "ANCHOR-CHECK: standard heading found, anchor will resolve"
elif grep -q '<summary>.*Configuration Reference.*</summary>' README.md; then
  echo "ANCHOR-CHECK: WARNING — Configuration Reference is inside <details><summary>, anchor may not resolve on GitHub"
  echo "ACTION NEEDED: Add <a id=\"configuration-reference\"></a> before the <details> block"
fi

# Regression: no code was changed
npm test
```
  </verify>
  <done>
README.md contains: (1) visible Terminal UI section with hero screenshot using HTML img tag (width="720") not inside any details block, (2) Who This Is For with 3-4 problem-framing bullets mentioning blind spots/context rot/oscillation/tracking, (3) With vs. Without comparison table with "Claude Code Alone" and "With nForma" columns, (4) By the Numbers section with 5+ quantified metrics, (5) What's New in v0.32 changelog section with honest framing (README overhaul as v0.32 deliverable, gate scoring and solve loops framed as "Recently shipped"), (6) expanded nav bar with TUI/Configuration/Star History anchor links and verified #configuration-reference anchor resolution. Getting Started TUI details block replaced with cross-reference to new section.
  </done>
</task>

</tasks>

<verification>
1. All 6 requirement IDs (RDME-01 through RDME-04, RDME-06, RDME-07) are addressed
2. The TUI hero screenshot appears without expanding any collapsible element, rendered at width=720 via HTML img tag
3. Section ordering follows: Nav bar -> Terminal UI -> Who This Is For -> With vs. Without -> By the Numbers -> What's New -> separator -> Why I Built nForma -> (rest unchanged)
4. No existing content below "Why I Built nForma" was modified (except Getting Started TUI cross-reference)
5. All nav bar anchor links resolve to actual section headings (including #configuration-reference verified against details/summary edge case)
6. What's New section honestly frames which items are v0.32 deliverables vs. recently shipped
7. `npm test` passes (no code changes, regression guard)
</verification>

<success_criteria>
- A first-time visitor sees TUI screenshot (properly sized at 720px), problem framing, comparison table, metrics, and changelog without scrolling past the fold or expanding any collapsible
- Nav bar provides quick access to TUI, Configuration, and Star History sections, with anchor resolution verified
- What's New section accurately represents what shipped when
- All existing README content below the new sections is preserved intact
- `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/v0.32-01-readme-above-the-fold-restructure/v0.32-01-01-SUMMARY.md`
</output>
