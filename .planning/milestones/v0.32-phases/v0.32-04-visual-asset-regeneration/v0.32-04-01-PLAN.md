---
phase: v0.32-04-visual-asset-regeneration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tui-screenshot.tape
  - .gitignore
  - docs/assets/tui-agents.png
  - docs/assets/tui-agents-health.png
  - docs/assets/tui-agents-scoreboard.png
  - docs/assets/tui-config.png
  - docs/assets/tui-modal-add-agent.png
  - docs/assets/tui-modal-settings.png
  - docs/assets/tui-reqs.png
  - docs/assets/tui-reqs-coverage.png
  - docs/assets/tui-session-claude.png
  - docs/assets/tui-sessions.png
  - docs/assets/tui-solve.png
autonomous: false
requirements:
  - VIS-01
  - VIS-02
formal_artifacts: none

must_haves:
  truths:
    - "All TUI screenshots embedded in README and User Guide are regenerated and match current TUI output"
    - "VHS tape runs successfully with zsh shell, explicit PATH export, and CLAUDECODE unset"
    - "Running the VHS tape twice produces identical screenshot output for static-content screenshots (health and session-claude excluded due to live data variance)"
  artifacts:
    - path: "scripts/tui-screenshot.tape"
      provides: "Hardened VHS tape with dynamic Claude Code path resolution"
      contains: "ls -d"
    - path: "docs/assets/tui-agents.png"
      provides: "TUI hero screenshot for README"
    - path: "docs/assets/tui-agents-health.png"
      provides: "Health view screenshot"
    - path: "docs/assets/tui-agents-scoreboard.png"
      provides: "Scoreboard screenshot"
    - path: "docs/assets/tui-config.png"
      provides: "Config module screenshot"
    - path: "docs/assets/tui-modal-add-agent.png"
      provides: "Add Agent modal screenshot"
    - path: "docs/assets/tui-modal-settings.png"
      provides: "Settings modal screenshot"
    - path: "docs/assets/tui-reqs.png"
      provides: "Reqs module screenshot"
    - path: "docs/assets/tui-reqs-coverage.png"
      provides: "Coverage view screenshot"
    - path: "docs/assets/tui-session-claude.png"
      provides: "Claude session screenshot"
    - path: "docs/assets/tui-sessions.png"
      provides: "Sessions module screenshot"
    - path: "docs/assets/tui-solve.png"
      provides: "Solve module screenshot"
  key_links:
    - from: "scripts/tui-screenshot.tape"
      to: "docs/assets/tui-*.png"
      via: "VHS Screenshot directive"
      pattern: "Screenshot docs/assets/tui-"
    - from: "README.md"
      to: "docs/assets/tui-agents.png"
      via: "HTML img tag"
      pattern: "tui-agents\\.png"
    - from: "docs/USER-GUIDE.md"
      to: "docs/assets/tui-*.png"
      via: "markdown image references"
      pattern: "tui-.*\\.png"
---

<objective>
Harden the VHS tape for CI reliability, regenerate all 11 TUI screenshots, verify deterministic output, and clean up orphaned SVG files.

Purpose: Phases v0.32-01 through v0.32-03 restructured README and User Guide content. The TUI also gained a Gate Scoring page (quick-242). Screenshots must be regenerated to reflect the current state. The VHS tape has a hardcoded Claude Code version path that breaks on updates.

Output: 11 regenerated PNG screenshots in docs/assets/, a hardened VHS tape with dynamic path resolution, and no orphaned SVG files in the repository root.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.32-04-visual-asset-regeneration/v0.32-04-RESEARCH.md
@scripts/tui-screenshot.tape
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden VHS tape and clean up orphaned SVGs</name>
  <files>scripts/tui-screenshot.tape, .gitignore</files>
  <action>
1. In `scripts/tui-screenshot.tape`, replace the hardcoded PATH export line (line 16):
   ```
   Type@500ms "export PATH=$PATH:/Users/jonathanborduas/Library/Application\ Support/Claude/claude-code/2.1.51/ && unset CLAUDECODE"
   ```
   With a dynamic version that resolves the latest installed Claude Code version:
   ```
   Type@500ms "export PATH=$PATH:$(ls -d ~/Library/Application\\ Support/Claude/claude-code/*/ 2>/dev/null | tail -1) && unset CLAUDECODE"
   ```
   This prevents breakage when Claude Code auto-updates to a new version.

2. Update the tape header comment from "Captures 10 screenshots" to "Captures 11 screenshots" (line 3) to match the actual count of Screenshot directives in the file.

3. Delete the 5 orphaned SVG files in the repository root:
   - `tui-agents.svg`
   - `tui-config.svg`
   - `tui-reqs.svg`
   - `tui-sessions.svg`
   - `tui-solve.svg`
   These are artifacts from a prior SVG generation approach (quick-213) and are not referenced anywhere.

4. Add `tui-*.svg` to `.gitignore` (after the existing `*.png` / `!docs/assets/*.png` block around line 120-121) to prevent future SVG artifacts from being tracked:
   ```
   # VHS SVG byproducts (keep PNGs in docs/assets/ instead)
   tui-*.svg
   ```

Do NOT change any sleep times, navigation sequences, or Screenshot directives -- those are already tuned.
  </action>
  <verify>
- `grep 'ls -d' scripts/tui-screenshot.tape` returns the dynamic path line
- `grep '2.1.51' scripts/tui-screenshot.tape` returns NO matches (hardcoded version removed)
- `ls tui-*.svg 2>/dev/null` in project root returns nothing (orphaned SVGs deleted)
- `grep 'tui-\*\.svg' .gitignore` returns the new gitignore entry
- `grep 'Captures 11' scripts/tui-screenshot.tape` returns match
  </verify>
  <done>VHS tape uses dynamic Claude Code path resolution, orphaned SVGs are deleted, and .gitignore prevents future SVG accumulation.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate all TUI screenshots and verify determinism</name>
  <files>docs/assets/tui-agents.png, docs/assets/tui-agents-health.png, docs/assets/tui-agents-scoreboard.png, docs/assets/tui-config.png, docs/assets/tui-modal-add-agent.png, docs/assets/tui-modal-settings.png, docs/assets/tui-reqs.png, docs/assets/tui-reqs-coverage.png, docs/assets/tui-session-claude.png, docs/assets/tui-sessions.png, docs/assets/tui-solve.png</files>
  <action>
1. Pre-flight: Verify VHS is installed: `which vhs` should return `/opt/homebrew/bin/vhs`. Verify the dynamic PATH in the tape resolves correctly: `ls -d ~/Library/Application\ Support/Claude/claude-code/*/ 2>/dev/null | tail -1` should return a valid directory containing `claude`.

2. Run the VHS tape (first pass -- regenerate all screenshots):
   ```bash
   cd /Users/jonathanborduas/code/QGSD && vhs scripts/tui-screenshot.tape
   ```
   This takes approximately 60 seconds. VHS will output all 11 PNG files to `docs/assets/`.

3. Verify all 11 PNG files exist after the run:
   ```bash
   for f in tui-agents tui-agents-health tui-agents-scoreboard tui-config tui-modal-add-agent tui-modal-settings tui-reqs tui-reqs-coverage tui-session-claude tui-sessions tui-solve; do
     test -f "docs/assets/${f}.png" && echo "OK: ${f}.png" || echo "MISSING: ${f}.png"
   done
   ```

4. Copy all PNGs to /tmp for determinism comparison:
   ```bash
   cp docs/assets/tui-*.png /tmp/vhs-run1/
   ```

5. Run the VHS tape a second time:
   ```bash
   vhs scripts/tui-screenshot.tape
   ```

6. Compare outputs for determinism (exclude tui-agents-health.png which contains live latency data that varies):
   ```bash
   for f in tui-agents tui-agents-scoreboard tui-config tui-modal-add-agent tui-modal-settings tui-reqs tui-reqs-coverage tui-sessions tui-solve; do
     cmp "docs/assets/${f}.png" "/tmp/vhs-run1/${f}.png" && echo "MATCH: ${f}.png" || echo "DIFFERS: ${f}.png"
   done
   ```
   Note: `tui-session-claude.png` is also excluded from the byte-identical check because it launches a real Claude Code session whose output timing may vary. The determinism check covers the 9 screenshots with static TUI content.

7. Verify all PNG references in README.md and USER-GUIDE.md resolve to existing files:
   ```bash
   grep -oP 'docs/assets/tui-[a-z-]+\.png' README.md docs/USER-GUIDE.md | while IFS=: read -r src ref; do
     test -f "$ref" && echo "OK: $ref (from $src)" || echo "MISSING: $ref (from $src)"
   done
   ```

If tui-session-claude.png fails to capture (blank or partial), increase the sleep before its Screenshot directive from 12s to 18s in `scripts/tui-screenshot.tape` and re-run the tape once more.
  </action>
  <verify>
- `vhs scripts/tui-screenshot.tape` exits with code 0
- All 11 PNG files exist in `docs/assets/`
- At least 9 of 11 PNGs (excluding health and session-claude) are byte-identical across two runs
- All PNG references in README.md and USER-GUIDE.md resolve to existing files on disk
  </verify>
  <done>All 11 TUI screenshots are regenerated reflecting current TUI output. Determinism is verified for static-content screenshots. All document references resolve to real files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify screenshot quality</name>
  <what-built>Regenerated all 11 TUI screenshots via VHS tape automation with hardened path resolution.</what-built>
  <how-to-verify>
1. Open README.md in GitHub preview or local markdown viewer
2. Verify the hero screenshot (tui-agents.png) renders correctly at width=720 and shows the current Agents module layout
3. Verify tui-agents-health.png in the Observability section shows the Health view with colored status indicators
4. Verify tui-solve.png in the Observability section shows the Solve module with the S-shaped ASCII art (not old T-shape)
5. Open docs/USER-GUIDE.md and spot-check 3-4 screenshots:
   - tui-reqs.png should show the Reqs module with Gate Scoring as a menu item (added in quick-242)
   - tui-config.png should show the Config module
   - tui-sessions.png should show the Sessions module
   - tui-modal-add-agent.png should show the Add Agent modal dialog
6. Confirm no screenshots appear blank, corrupted, or show outdated content
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues with any screenshots</resume-signal>
</task>

</tasks>

<verification>
- All 11 PNG files in docs/assets/ are freshly generated (check file modification timestamps)
- VHS tape contains no hardcoded Claude Code version numbers
- No orphaned tui-*.svg files in repository root
- .gitignore contains tui-*.svg entry
- Determinism verified for static screenshots across two consecutive runs
- All screenshot references in README.md and USER-GUIDE.md point to existing files
</verification>

<success_criteria>
1. All TUI screenshots embedded in README and User Guide are regenerated and match current TUI output
2. VHS tape runs successfully with zsh shell, explicit PATH export, and CLAUDECODE unset
3. Running the VHS tape twice produces identical screenshot output for static-content screenshots (health and session-claude excluded due to live data variance)
4. No orphaned SVG files remain in repository root
</success_criteria>

<output>
After completion, create `.planning/phases/v0.32-04-visual-asset-regeneration/v0.32-04-01-SUMMARY.md`
</output>
