---
phase: v0.32-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/user-guide-overhaul.test.cjs
  - docs/USER-GUIDE.md
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
formal_artifacts: none

must_haves:
  truths:
    - "Each major User Guide feature section includes at least one TUI screenshot cross-referenced to the feature description"
    - "The Getting Started walkthrough proceeds step-by-step from install through first command with screenshots at each stage"
  artifacts:
    - path: "test/user-guide-overhaul.test.cjs"
      provides: "Structural assertions for GUIDE-01 and GUIDE-02"
      min_lines: 40
    - path: "docs/USER-GUIDE.md"
      provides: "Complete User Guide with screenshots and Getting Started walkthrough"
      contains: "## Getting Started"
  key_links:
    - from: "docs/USER-GUIDE.md"
      to: "docs/assets/tui-*.png"
      via: "HTML img tags with relative paths"
      pattern: '<img src="assets/tui-'
    - from: "docs/USER-GUIDE.md"
      to: "docs/assets/terminal.svg"
      via: "HTML img tag for install screenshot"
      pattern: '<img src="assets/terminal.svg"'
    - from: "test/user-guide-overhaul.test.cjs"
      to: "docs/USER-GUIDE.md"
      via: "fs.readFileSync assertion target"
      pattern: "USER-GUIDE.md"
---

<objective>
Update the User Guide with TUI screenshots cross-referenced to feature sections and a new Getting Started walkthrough with step-by-step screenshots.

Purpose: A new user can follow the User Guide from install to first successful quorum command using screenshots as visual anchors.
Output: Updated `docs/USER-GUIDE.md` with embedded screenshots and Getting Started section; `test/user-guide-overhaul.test.cjs` with structural assertions.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.32-03-user-guide-overhaul/v0.32-03-RESEARCH.md
@docs/USER-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structural test scaffolding for User Guide overhaul</name>
  <files>test/user-guide-overhaul.test.cjs</files>
  <action>
Create `test/user-guide-overhaul.test.cjs` using `node:test` and `node:assert` (project standard).

The test file reads `docs/USER-GUIDE.md` via `fs.readFileSync` and asserts:

**GUIDE-01 assertions (screenshot cross-references):**
- At least 6 distinct `<img src="assets/tui-` references exist in the User Guide (the plan embeds 4 in feature sections + 4 in Getting Started = 8 total tui- references; threshold of 6 guards against accidental removal while allowing minor adjustments)
- Every image `src="assets/..."` referenced in the User Guide exists on disk in `docs/assets/`
- Screenshots are distributed across the document (spread > 50 lines between first and last), not clustered in one section

**GUIDE-02 assertions (Getting Started walkthrough):**
- A `## Getting Started` section exists
- Getting Started appears before `## Workflow Diagrams` in the document
- Contains `### Step 1` and `### Step 2` (step-by-step structure)
- Getting Started section contains at least 2 `<img` tags (screenshots at each stage)
- Getting Started content mentions both "install" (or "npx") and "quorum" (or "mcp-setup")

**ToC assertion:**
- Every `## ` heading in the User Guide has a corresponding anchor link in the Table of Contents section
- Each ToC link matches the pattern `[text](#slug)` where the slug is derived from the heading text by: lowercasing, replacing spaces with hyphens, and stripping non-alphanumeric/hyphen characters. Assert that the slug in the ToC link matches the expected slug computed from the heading text (catches malformed anchors that would pass a simple existence check but produce broken links)

Use the exact test code from the RESEARCH.md Validation Architecture section as a starting point, then add the ToC checks.

Run tests -- they MUST FAIL (red phase) since the User Guide has no screenshots or Getting Started section yet.
  </action>
  <verify>
Run `node --test test/user-guide-overhaul.test.cjs` -- tests should execute and all FAIL (expected: the User Guide has 0 screenshots and no Getting Started section). Confirm the test file has at least 7 test cases covering the assertions above (including the ToC slug format check).
  </verify>
  <done>
`test/user-guide-overhaul.test.cjs` exists with 7+ test cases for GUIDE-01 and GUIDE-02. All tests fail against the current User Guide (confirming the assertions are correct and the guide needs updating).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Getting Started walkthrough and embed TUI screenshots in User Guide</name>
  <files>docs/USER-GUIDE.md</files>
  <action>
Edit `docs/USER-GUIDE.md` to satisfy both GUIDE-01 and GUIDE-02. All changes are Markdown/HTML only -- no code files.

**GUIDE-02: Add Getting Started section (insert after Table of Contents, before Workflow Diagrams):**

Create `## Getting Started` with 4 subsections:

1. `### Step 1: Install nForma` -- Show the install command (`npx @nforma.ai/nforma@latest`), explain what the installer does (prompts for runtime and location), embed `<img src="assets/terminal.svg" alt="nForma Install" width="600">` with italic caption. Recommend "Claude Code" + "Global" for first-time setup.

2. `### Step 2: Set Up Your Quorum` -- Explain what a quorum is in one sentence ("nForma uses multiple AI models to cross-check each other's work -- this group is called a quorum"). Tell user to run `/nf:mcp-setup` to configure MCP servers. Embed `<img src="assets/tui-modal-add-agent.png" alt="Add Agent Modal" width="600">` showing the agent setup modal. Then show `<img src="assets/tui-agents.png" alt="Agent Manager" width="600">` as the result after setup.

3. `### Step 3: Your First Command` -- Show running `/nf:new-project` or `/nf:quick` as first real command. Embed `<img src="assets/tui-reqs.png" alt="Requirements View" width="600">` to show what nForma produces (requirements tracking).

4. `### Step 4: Start a Project` -- Brief paragraph pointing to the Usage Examples section for full project lifecycle. Embed `<img src="assets/tui-session-claude.png" alt="Active Project Session" width="600">` to show what an active project session looks like, giving the user a visual goal. Caption: "*An active Claude Code session managed by nForma, showing project context and quorum status.*"

**GUIDE-01: Embed TUI screenshots in existing feature sections:**

Use the screenshot-to-section mapping from RESEARCH.md. Add each screenshot with `<img src="assets/..." alt="..." width="600">` followed by an italic caption line. Place screenshots AFTER the textual description, not before.

- **Configuration Reference** section: Add `<img src="assets/tui-config.png" alt="Configuration TUI" width="600">` after the introductory paragraph (before the schema block). Caption: "*The TUI Config module lets you edit provider keys, timeouts, and export/import settings without touching JSON files.*"

- **Command Reference** section: Add `<img src="assets/tui-agents-scoreboard.png" alt="Agent Scoreboard" width="600">` after the introductory paragraph or command listing. Caption: "*The Agent Scoreboard shows quorum slot health, response times, and pass rates for each configured model.*" This screenshot directly shows command outputs and strengthens GUIDE-01 coverage in this section.

- **Troubleshooting** section: Add `<img src="assets/tui-solve.png" alt="Solve Diagnostics" width="600">` after the section heading paragraph. Caption: "*The Solve module surfaces formal verification gaps, circuit breaker state, and actionable diagnostics.*"

- **Usage Examples > Resuming After a Break** subsection: Add `<img src="assets/tui-sessions.png" alt="Session Manager" width="600">` near the resume-work example. Caption: "*The Sessions module shows active and recent Claude Code sessions with context summaries.*"

- **Workflow Diagrams > Planning Agent Coordination** subsection: Add `<img src="assets/tui-reqs-coverage.png" alt="Requirements Coverage" width="600">` after the planning diagram. Caption: "*The Requirements Coverage view tracks which requirements are addressed by which plans.*"

**Update Table of Contents:**
Add `- [Getting Started](#getting-started)` as the FIRST entry in the ToC (before Workflow Diagrams). Verify all `## ` headings have ToC entries with correctly formatted anchor slugs (lowercase, spaces replaced with hyphens, non-alphanumeric characters stripped).

**Anti-patterns to avoid (per RESEARCH.md):**
- Do NOT add every screenshot to every section (each screenshot appears once)
- Do NOT place screenshots without preceding context text
- Always use `width="600"` (not full-width)
- Use relative paths from `docs/` (e.g., `assets/tui-config.png`, NOT `docs/assets/tui-config.png`)
  </action>
  <verify>
Run `node --test test/user-guide-overhaul.test.cjs` -- ALL tests must PASS. Additionally verify:
- `grep -c '<img src="assets/' docs/USER-GUIDE.md` returns at least 9 (Getting Started screenshots + feature section screenshots including Command Reference)
- `grep '## Getting Started' docs/USER-GUIDE.md` returns a match
- All referenced image files exist: for each `src="assets/..."` in the file, confirm the file exists in `docs/assets/`
  </verify>
  <done>
`docs/USER-GUIDE.md` contains a new Getting Started section with 4 steps and at least 4 screenshots (Steps 1-4), plus at least 5 TUI screenshots cross-referenced in feature sections (including Command Reference). All `test/user-guide-overhaul.test.cjs` tests pass. Table of Contents is updated with Getting Started entry and all anchor slugs are correctly formatted.
  </done>
</task>

</tasks>

<verification>
1. `node --test test/user-guide-overhaul.test.cjs` -- all tests pass
2. `grep -c '<img' docs/USER-GUIDE.md` -- at least 9 img tags total
3. Every `src="assets/..."` reference in the User Guide resolves to an existing file in `docs/assets/`
4. `## Getting Started` appears before `## Workflow Diagrams` in the document
5. Table of Contents includes Getting Started link with correct anchor slug format
</verification>

<success_criteria>
- Each major User Guide feature section (Configuration Reference, Command Reference, Troubleshooting, Workflow Diagrams, Usage Examples) includes at least one TUI screenshot cross-referenced to the feature description
- The Getting Started walkthrough proceeds step-by-step from install through first command with screenshots at each stage (Steps 1-4, with screenshots in all 4 steps)
- All structural tests pass (including ToC slug format validation)
- No broken image references
</success_criteria>

<output>
After completion, create `.planning/phases/v0.32-03-user-guide-overhaul/v0.32-03-01-SUMMARY.md`
</output>
