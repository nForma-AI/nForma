---
phase: v0.24-02-dispatch-reliability
plan: 03
type: execute
wave: 2
depends_on: [v0.24-02-02]
files_modified: []
autonomous: false
requirements: [DISP-01, DISP-02, DISP-03]
formal_artifacts: none

must_haves:
  truths:
    - "Before building the dispatch list, qgsd-prompt.js runs a fast health probe (<3s) per provider and excludes all slots on providers that fail the probe -- observable via dispatch log showing excluded: provider X down"
    - "Slots whose available_at_iso timestamp in the scoreboard is in the future are excluded from the dispatch list -- a slot marked unavailable until 14:30 is not dispatched at 14:25"
    - "The dispatch list is ordered by descending recent success rate from scoreboard slot stats -- the slot with the highest success rate appears first in DISPATCH_LIST"
    - "When all preferred slots are excluded (all providers down or cooling), dispatch falls back gracefully to any remaining slot rather than failing with zero candidates"
  artifacts:
    - path: "hooks/qgsd-prompt.js"
      provides: "Complete dispatch reliability implementation"
      exports: ["getAvailableSlots", "sortBySuccessRate", "mapRiskLevelToCount", "parseQuorumSizeFlag"]
  key_links:
    - from: "bin/dispatch-availability-window.test.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "All structural tests passing GREEN"
      pattern: "getAvailableSlots"
    - from: "bin/dispatch-success-rate-ordering.test.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "All structural tests passing GREEN"
      pattern: "sortBySuccessRate"
---

<objective>
Run the complete acceptance test suite and have the user verify the dispatch reliability features work in a live quorum round.

Purpose: Confirm all three DISP requirements are met end-to-end -- health probing, availability filtering, and success rate ordering -- with observable evidence.
Output: All tests GREEN, human-verified dispatch behavior.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-02-dispatch-reliability/v0.24-02-02-SUMMARY.md
@hooks/qgsd-prompt.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full acceptance test suite</name>
  <files></files>
  <action>
    Run ALL test suites related to v0.24-02 and confirm everything passes:

    1. DISP-02 tests:
       ```bash
       node --test bin/dispatch-availability-window.test.cjs 2>&1
       ```
       Expected: ALL tests GREEN (structural + unit)

    2. DISP-03 tests:
       ```bash
       node --test bin/dispatch-success-rate-ordering.test.cjs 2>&1
       ```
       Expected: ALL tests GREEN (structural + unit)

    3. Existing provider-mapping tests (regression):
       ```bash
       node --test bin/provider-mapping.test.cjs 2>&1
       ```
       Expected: ALL 17 tests GREEN (no regression)

    4. Existing retry backoff tests (regression):
       ```bash
       node --test bin/call-quorum-slot-retry.test.cjs 2>&1
       ```
       Expected: ALL tests GREEN (no regression)

    5. Full npm test suite:
       ```bash
       npm test 2>&1 | tail -20
       ```
       Expected: No new failures

    6. Verify install sync is current:
       ```bash
       diff hooks/qgsd-prompt.js hooks/dist/qgsd-prompt.js && echo "dist SYNCED"
       diff hooks/qgsd-prompt.js ~/.claude/hooks/qgsd-prompt.js && echo "installed SYNCED"
       ```

    If any test fails, diagnose and fix before proceeding to human verification.
  </action>
  <verify>
    All test commands above exit with code 0 (no failures).
    Total expected test count: DISP-02 tests + DISP-03 tests + 17 provider-mapping + retry backoff tests.
  </verify>
  <done>
    - All DISP-01 structural test GREEN (spawnSync + check-provider-health present)
    - All DISP-02 structural and unit tests GREEN
    - All DISP-03 structural and unit tests GREEN
    - All existing provider-mapping tests GREEN (no regression)
    - All existing retry backoff tests GREEN (no regression)
    - npm test passes without new failures
    - hooks/dist/ and ~/.claude/hooks/ copies are synced
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of dispatch reliability</name>
  <files>hooks/qgsd-prompt.js</files>
  <action>
Human verification checkpoint. All implementation was completed in Plans 01-02.
This task validates the full dispatch reliability pipeline end-to-end.

What was built: Complete dispatch reliability for v0.24-02:
- DISP-01: Health probe trigger runs check-provider-health.cjs (under 3s) before dispatch, excludes DOWN providers
- DISP-02: Availability window filtering reads scoreboard and excludes cooling-down slots
- DISP-03: Success rate ordering sorts dispatch list by descending tp/(tp+fn) from scoreboard
- SC-4: Graceful fallback ensures at least one slot is always dispatched

How to verify:

Test 1: Observe dispatch ordering in a live quorum round
1. Run any planning command: /qgsd:plan-phase v0.24-03
2. Observe the DISPATCH_LIST in the quorum instructions injected into context
3. Verify: slots are ordered (highest success rate first) -- check the DISPATCH_LIST task order matches what the scoreboard would predict

Test 2: Observe availability window filtering
1. Create or modify .planning/quorum-scoreboard.json.
   NOTE: This file may not exist before the first quorum run. If it does not exist, create it with this minimal structure:
   ```json
   {
     "meta": { "updated_at": "2026-03-02T00:00:00Z" },
     "availability": {},
     "slots": {}
   }
   ```
2. In the `availability` section, add an entry with a future timestamp for a test slot:
   ```json
   "codex-1": { "available_at_iso": "2026-03-03T23:59:59Z", "reason": "test", "set_at": "2026-03-02T00:00:00Z" }
   ```
3. Run a planning command
4. Verify: codex-1 does NOT appear in DISPATCH_LIST (or shows "SKIP (COOLING DOWN)")
5. Remove the test entry afterward

Test 3: Verify graceful fallback (optional)
1. If all providers happen to be down, verify dispatch still proceeds with at least one slot
2. This can be observed by checking the quorum instructions -- they should never show zero dispatch slots

Resume signal: Type "approved" if dispatch ordering is observable and correct. Describe any issues otherwise.
  </action>
  <verify>Human observation of dispatch ordering in live quorum round -- slots ordered by success rate, cooling-down slots excluded, at least one slot always dispatched.</verify>
  <done>Human approves that dispatch list ordering matches scoreboard success rates, availability windows are respected, and graceful fallback prevents zero-candidate dispatch.</done>
</task>

</tasks>

<verification>
All success criteria from ROADMAP v0.24-02 met:
1. Health probe runs and excludes DOWN providers (observable in SKIP note)
2. Availability windows filter future-timestamp slots (testable by editing scoreboard)
3. Dispatch ordered by success rate (observable in DISPATCH_LIST order)
4. Graceful fallback prevents zero-candidate dispatch (guaranteed by code guard)
</verification>

<success_criteria>
- All automated tests pass GREEN with zero failures
- No regression in existing test suites
- Human verification confirms dispatch ordering is observable
- All 4 ROADMAP success criteria for v0.24-02 are met
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-02-dispatch-reliability/v0.24-02-03-SUMMARY.md`
</output>
