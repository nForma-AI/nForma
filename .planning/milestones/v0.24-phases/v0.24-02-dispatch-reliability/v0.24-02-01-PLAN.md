---
phase: v0.24-02-dispatch-reliability
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/dispatch-availability-window.test.cjs
  - bin/dispatch-success-rate-ordering.test.cjs
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03]
formal_artifacts: none

must_haves:
  truths:
    - "bin/dispatch-availability-window.test.cjs exists and runs under node --test without syntax errors"
    - "bin/dispatch-success-rate-ordering.test.cjs exists and runs under node --test without syntax errors"
    - "Availability window structural tests are RED: test asserts hooks/qgsd-prompt.js exports or contains a getAvailableSlots function that filters out slots whose available_at_iso is in the future -- fails because the function does not exist yet"
    - "Success rate ordering structural tests are RED: test asserts hooks/qgsd-prompt.js exports or contains a sortBySuccessRate function that sorts slots by descending tp/(tp+fn) from scoreboard -- fails because the function does not exist yet"
    - "DISP-01 structural test is RED: test asserts hooks/qgsd-prompt.js contains both 'spawnSync' and 'check-provider-health' strings confirming health probe integration -- fails because triggerHealthProbe does not exist yet"
    - "Availability window unit tests are GREEN: pure function that given a slot list and a mock scoreboard object with available_at_iso timestamps correctly filters out future-timestamp slots and includes past-timestamp and missing-data slots"
    - "Success rate ordering unit tests are GREEN: pure function that given a slot list and a mock scoreboard object with tp/fn stats correctly sorts by descending success rate with 0.5 default for missing data"
    - "Graceful fallback unit test is GREEN: pure function returns at least one slot when all slots would otherwise be filtered out"
  artifacts:
    - path: "bin/dispatch-availability-window.test.cjs"
      provides: "Unit + structural tests for DISP-01 health probe and DISP-02 availability window filtering"
      min_lines: 80
    - path: "bin/dispatch-success-rate-ordering.test.cjs"
      provides: "Unit + structural tests for DISP-03 success rate ordering"
      min_lines: 80
  key_links:
    - from: "bin/dispatch-availability-window.test.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "fs.readFileSync structural check for getAvailableSlots function and spawnSync/check-provider-health integration"
      pattern: "qgsd-prompt\\.js|getAvailableSlots|available_at|spawnSync|check-provider-health"
    - from: "bin/dispatch-success-rate-ordering.test.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "fs.readFileSync structural check for sortBySuccessRate function"
      pattern: "qgsd-prompt\\.js|sortBySuccessRate|success.*rate"
    - from: "bin/dispatch-availability-window.test.cjs"
      to: ".planning/quorum-scoreboard.json"
      via: "Mock scoreboard structure matching real availability section format"
      pattern: "available_at_iso|availability"
    - from: "bin/dispatch-success-rate-ordering.test.cjs"
      to: ".planning/quorum-scoreboard.json"
      via: "Mock scoreboard structure matching real slots section format with composite keys"
      pattern: "slots|tp|fn|success|split.*:"
---

<objective>
Write failing test scaffolding BEFORE any implementation starts. Tests assert behaviors that
will exist AFTER Plan 02 executes -- intentionally RED now, turn GREEN when implementation completes.

Purpose: Enforce TDD discipline for DISP-01 (health probe integration), DISP-02 (availability window filtering) and DISP-03 (success rate ordering). Plan 02 cannot be considered complete until these tests pass.
Output: Two test files -- both RED on structural tests, GREEN on pure function unit tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-02-dispatch-reliability/v0.24-02-RESEARCH.md
@hooks/qgsd-prompt.js
@.planning/quorum-scoreboard.json (read availability and slots sections for real data structure)
@bin/provider-mapping.test.cjs (pattern reference for test file structure)
</context>

<feature>
  <name>TDD Test Scaffolding for v0.24-02 Dispatch Reliability</name>
  <files>bin/dispatch-availability-window.test.cjs, bin/dispatch-success-rate-ordering.test.cjs</files>
  <behavior>
    Two test files using node:test (native -- matches existing bin/*.test.cjs pattern).
    Use require('node:test') and require('node:assert/strict').

    CRITICAL DATA STRUCTURE NOTE: The actual quorum-scoreboard.json uses:
    - `availability` section (NOT `slots`) for availability windows, keyed by slot name (e.g., "codex-1")
    - `availability[slotName].available_at_iso` (ISO 8601 UTC string, e.g., "2026-02-28T16:47:57.332Z")
    - `slots` section for per-slot stats, keyed as `slotName:modelId` (e.g., "claude-1:deepseek-ai/DeepSeek-V3.2")
    - Each slot entry has: `slot`, `model`, `score`, `tp`, `tn`, `fp`, `fn`, `impr`
    - A single slot name may appear multiple times with different models (e.g., "claude-1:deepseek-ai/DeepSeek-V3.2" and "claude-1:unknown")
    - For success rate aggregation: extract slot name from composite key using `key.split(':')[0]`, then sum tp and fn across ALL entries sharing that slot name

    FILE 1: bin/dispatch-availability-window.test.cjs (DISP-01 + DISP-02 tests)

    STRUCTURAL TESTS (RED until Plan 02 complete):

    1. getAvailableSlots function exists in qgsd-prompt.js:
       - Read hooks/qgsd-prompt.js as string
       - Assert: file contains 'getAvailableSlots' function name
       - RED now (no availability filtering logic in qgsd-prompt.js)

    2. getAvailableSlots is exported for testing:
       - Assert: file contains 'getAvailableSlots' in module.exports block
       - RED now

    3. Availability filtering integrated into dispatch flow:
       - Assert: file contains call to getAvailableSlots between skipSet filtering and dynamic step generation
       - RED now

    4. DISP-01 health probe integration (NEW):
       - Read hooks/qgsd-prompt.js as string
       - Assert: file contains BOTH 'spawnSync' AND 'check-provider-health' strings
       - This transforms DISP-01 from "assumed correct" to "verified integrated"
       - RED now (triggerHealthProbe function does not exist yet)

    UNIT TESTS (GREEN immediately -- pure function tests with inline reference implementation):

    5. Filter excludes future-timestamp slots:
       - Inline: getAvailableSlots function that reads availability section
       - Mock scoreboard: { availability: { "slot-a": { available_at_iso: futureDate }, "slot-b": { available_at_iso: pastDate } } }
       - Input slots: [{slot:"slot-a"}, {slot:"slot-b"}, {slot:"slot-c"}]
       - Assert: returns only slot-b and slot-c (slot-a excluded because future, slot-c included because no data)
       - GREEN immediately

    6. Filter includes all slots when no scoreboard exists:
       - Mock: scoreboard = null (file not found)
       - Assert: returns all input slots unchanged (fail-open)
       - GREEN immediately

    7. Filter includes slot when available_at_iso is malformed:
       - Mock: { availability: { "slot-a": { available_at_iso: "not-a-date" } } }
       - Assert: slot-a included (fail-open on parse error)
       - GREEN immediately

    8. Filter includes slot when availability section missing:
       - Mock: {} (no availability key)
       - Assert: all slots included
       - GREEN immediately

    FILE 2: bin/dispatch-success-rate-ordering.test.cjs (DISP-03 tests)

    STRUCTURAL TESTS (RED until Plan 02 complete):

    1. sortBySuccessRate function exists in qgsd-prompt.js:
       - Read hooks/qgsd-prompt.js as string
       - Assert: file contains 'sortBySuccessRate' function name
       - RED now

    2. sortBySuccessRate is exported for testing:
       - Assert: file contains 'sortBySuccessRate' in module.exports block
       - RED now

    3. Success rate sorting integrated into dispatch flow:
       - Assert: file contains call to sortBySuccessRate after availability filtering
       - RED now

    4. Graceful fallback logic exists:
       - Assert: file contains logic ensuring at least one slot remains after all filtering
       - Pattern: check for "length === 0" or "fallback" or similar guard
       - RED now

    UNIT TESTS (GREEN immediately):

    5. Sort by descending success rate:
       - Inline sortBySuccessRate that reads slots section and computes tp/(tp+fn)
       - EXPLICIT MOCK FIXTURE -- use composite keys matching real scoreboard format:
         ```javascript
         const mockScoreboard = {
           slots: {
             'slot-a:modelX': { slot: 'slot-a', model: 'modelX', tp: 8, fn: 2 },
             'slot-b:modelY': { slot: 'slot-b', model: 'modelY', tp: 5, fn: 5 },
             'slot-c:modelZ': { slot: 'slot-c', model: 'modelZ', tp: 9, fn: 1 }
           }
         };
         // slot-a rate = 8/(8+2) = 0.8, slot-b rate = 5/(5+5) = 0.5, slot-c rate = 9/(9+1) = 0.9
         ```
       - Input: [{slot:"slot-a"}, {slot:"slot-b"}, {slot:"slot-c"}]
       - Assert: output order is slot-c, slot-a, slot-b
       - GREEN immediately

    6. Default to 0.5 for slots with no scoreboard data:
       - Mock: empty slots section
       - Input: [{slot:"slot-a"}, {slot:"slot-b"}]
       - Assert: both get 0.5 rate, order preserved (stable sort)
       - GREEN immediately

    7. Aggregate tp/fn across multiple model entries for same slot:
       - EXPLICIT MOCK FIXTURE with multi-model composite keys:
         ```javascript
         const mockScoreboard = {
           slots: {
             'slot-a:model1': { slot: 'slot-a', model: 'model1', tp: 8, fn: 2 },
             'slot-a:model2': { slot: 'slot-a', model: 'model2', tp: 3, fn: 1 }
           }
         };
         // Aggregation: extract slot name via key.split(':')[0]
         // const allModelsForSlot = Object.entries(scoreboard.slots)
         //   .filter(([k]) => k.startsWith(slotName + ':'))
         //   .map(([_, v]) => v);
         // Sum tp: 8+3=11, sum fn: 2+1=3
         // Aggregate rate for 'slot-a' = 11/(11+3) = 0.786
         ```
       - Assert: slot-a aggregate rate = (8+3)/(8+3+2+1) = 11/14 approx 0.786
       - GREEN immediately

    8. Fail-open: return original order on scoreboard read error:
       - Mock: scoreboard = null
       - Assert: returns slots in original order
       - GREEN immediately

    9. Graceful fallback: at least one slot when all filtered:
       - Inline: graceful fallback function that adds back first original slot if filtered list empty
       - Assert: never returns empty array
       - GREEN immediately

    Read paths for source files (NOT installed copies):
    - hooks/qgsd-prompt.js
  </behavior>
  <implementation>
    Follow exact pattern from bin/provider-mapping.test.cjs:
    - require('node:test') with test() blocks
    - require('node:assert/strict') for assertions
    - require('node:fs') for readFileSync
    - require('node:path') for path.resolve

    For structural tests:
      const content = fs.readFileSync(path.resolve(__dirname, '..', 'hooks', 'qgsd-prompt.js'), 'utf8');
      assert.ok(content.includes('getAvailableSlots'), 'getAvailableSlots function not found');

    For the DISP-01 structural test (in dispatch-availability-window.test.cjs):
      // DISP-01: Verify health probe integration exists
      assert.ok(content.includes('spawnSync'), 'spawnSync not found -- health probe trigger missing');
      assert.ok(content.includes('check-provider-health'), 'check-provider-health not found -- health probe integration missing');

    For unit tests with inline reference functions:
      // Reference implementation matching the RESEARCH.md pattern
      function getAvailableSlots(slots, scoreboard) {
        if (!scoreboard || !scoreboard.availability) return slots;
        const now = Date.now();
        return slots.filter(s => {
          const avail = scoreboard.availability[s.slot];
          if (!avail || !avail.available_at_iso) return true;
          try {
            return new Date(avail.available_at_iso).getTime() <= now;
          } catch (_) { return true; }
        });
      }

    For sortBySuccessRate inline reference (slot name extraction via key.split):
      // Reference implementation using composite key slot name extraction
      function sortBySuccessRate(slots, scoreboard) {
        if (!scoreboard || !scoreboard.slots) return [...slots];
        const getRate = (slotName) => {
          // Extract slot name from composite keys: "slotName:modelId"
          const entries = Object.entries(scoreboard.slots)
            .filter(([k]) => k.split(':')[0] === slotName)
            .map(([_, v]) => v);
          if (entries.length === 0) return 0.5; // default for unknown
          const totalTp = entries.reduce((sum, e) => sum + (e.tp || 0), 0);
          const totalFn = entries.reduce((sum, e) => sum + (e.fn || 0), 0);
          return (totalTp + totalFn) === 0 ? 0.5 : totalTp / (totalTp + totalFn);
        };
        return [...slots].sort((a, b) => getRate(b.slot) - getRate(a.slot));
      }

    Use fail-open guard pattern from v0.24-01:
      let mod = null;
      try { mod = require('../hooks/qgsd-prompt.js'); } catch(e) {}
      // Each structural test checks the raw file content, not the module

    RED/GREEN commit sequence (single commit):
      Run: node --test bin/dispatch-availability-window.test.cjs bin/dispatch-success-rate-ordering.test.cjs
      Confirm: structural tests FAIL, unit tests PASS
      Commit: test(v0.24-02-01): add failing test scaffolding for availability window and success rate ordering
  </implementation>
</feature>

<verification>
Run tests and confirm RED/GREEN split:
```bash
node --test bin/dispatch-availability-window.test.cjs bin/dispatch-success-rate-ordering.test.cjs 2>&1 | grep -E "^(#|not ok|ok )" | head -30
```

Expected output: structural tests show "not ok" (failing), unit tests show "ok" (passing).
Confirm no syntax errors by verifying the command exits (even with test failures -- exit code 1 is expected).
</verification>

<success_criteria>
- bin/dispatch-availability-window.test.cjs exists, at least 80 lines
- bin/dispatch-success-rate-ordering.test.cjs exists, at least 80 lines
- Both files run without syntax error under node --test
- Structural tests fail (RED): getAvailableSlots exists, exported, integrated; sortBySuccessRate exists, exported, integrated; graceful fallback logic; DISP-01 spawnSync+check-provider-health integration
- Unit tests pass (GREEN): availability filtering, success rate sorting, aggregation, fail-open, graceful fallback
- Commit message: test(v0.24-02-01): add failing test scaffolding for availability window and success rate ordering
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-02-dispatch-reliability/v0.24-02-01-SUMMARY.md`
</output>
