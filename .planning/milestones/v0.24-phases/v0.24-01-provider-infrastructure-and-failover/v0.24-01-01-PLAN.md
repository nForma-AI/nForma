---
phase: v0.24-01-provider-infrastructure-and-failover
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/call-quorum-slot-retry.test.cjs
  - bin/provider-mapping.test.cjs
autonomous: true
requirements: [FAIL-01, FAIL-02]
formal_artifacts: none

must_haves:
  truths:
    - "bin/call-quorum-slot-retry.test.cjs exists and runs under node --test without syntax errors"
    - "bin/provider-mapping.test.cjs exists and runs under node --test without syntax errors"
    - "Retry backoff tests are RED: test asserts call-quorum-slot.cjs retries up to 2 times with delays of 1s then 3s before recording UNAVAIL — fails because retry logic does not exist yet"
    - "Provider mapping tests are RED: test asserts every entry in providers.json has a provider field — fails because the field does not exist yet"
    - "Provider-skip tests are RED: test asserts qgsd-prompt.js skips all slots for a DOWN provider in a single dispatch decision — fails because provider-skip logic does not exist yet"
    - "Structural test verifies probe-quorum-slots.cjs resolves each slot to its backing provider without ambiguity — RED because provider field not in providers.json yet"
  artifacts:
    - path: "bin/call-quorum-slot-retry.test.cjs"
      provides: "Unit tests for retry-with-backoff logic in call-quorum-slot.cjs"
      min_lines: 80
    - path: "bin/provider-mapping.test.cjs"
      provides: "Structural + unit tests for provider field mapping and provider-skip dispatch"
      min_lines: 80
  key_links:
    - from: "bin/call-quorum-slot-retry.test.cjs"
      to: "bin/call-quorum-slot.cjs"
      via: "fs.readFileSync structural check + mocked subprocess"
      pattern: "readFileSync.*call-quorum-slot\\.cjs|retryWithBackoff|exponential"
    - from: "bin/provider-mapping.test.cjs"
      to: "bin/providers.json"
      via: "JSON.parse(fs.readFileSync)"
      pattern: "providers\\.json|provider"
    - from: "bin/provider-mapping.test.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "fs.readFileSync structural check"
      pattern: "qgsd-prompt\\.js|PROVIDER DOWN"
---

<objective>
Write failing test scaffolding BEFORE any implementation starts. Tests assert behaviors that
will exist AFTER Plans 02 and 03 execute — intentionally RED now, turn GREEN when
implementation completes.

Purpose: Enforce TDD discipline. Plans 02 and 03 are not considered complete until these tests pass.
Output: Two test files — both RED on structural/behavioral tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-01-provider-infrastructure-and-failover/v0.24-01-RESEARCH.md
@bin/call-quorum-slot.cjs
@bin/providers.json
@hooks/qgsd-prompt.js
</context>

<feature>
  <name>TDD Test Scaffolding for v0.24-01 Provider Infrastructure and Failover</name>
  <files>bin/call-quorum-slot-retry.test.cjs, bin/provider-mapping.test.cjs</files>
  <behavior>
    Two test files using node:test (native — matches existing bin/*.test.cjs pattern).
    Use require('node:test') and require('node:assert/strict').

    FILE 1: bin/call-quorum-slot-retry.test.cjs (FAIL-01 tests)

    STRUCTURAL TESTS (RED until Plan 02 complete):

    1. Retry function exists:
       - Read bin/call-quorum-slot.cjs as string
       - Assert: file contains 'retryWithBackoff' or 'retry' function
       - RED now (no retry logic in call-quorum-slot.cjs)

    2. Backoff delays present:
       - Assert: file contains numeric literals 1000 and 3000 (the 1s and 3s delays)
       - RED now

    3. Max retries constant:
       - Assert: file contains a max retry count of 2 (MAX_RETRIES = 2 or similar)
       - RED now

    4. Retry only on transient errors:
       - Assert: file contains logic to distinguish retryable vs non-retryable errors
       - Assert: CLI_SYNTAX errors are NOT retried (structural check for pattern like 'CLI_SYNTAX' or 'spawn error' exclusion)
       - RED now

    BEHAVIORAL TESTS (unit tests using mocked timer — will be GREEN when retry function is extracted and testable):

    5. Backoff calculation:
       - Inline a reference backoff function: delay(attempt) = attempt === 0 ? 1000 : 3000
       - Assert delay(0) === 1000, delay(1) === 3000
       - GREEN immediately (pure function test)

    FILE 2: bin/provider-mapping.test.cjs (FAIL-02 tests)

    STRUCTURAL TESTS (RED until Plan 03 complete):

    1. Every slot has provider field:
       - Read bin/providers.json, parse JSON
       - For each entry in .providers array, assert entry.provider is a non-empty string
       - RED now (no provider field exists)

    2. Provider field values are canonical:
       - Assert: every provider value matches /^[a-z][a-z0-9-]*$/ (lowercase kebab-case)
       - RED now

    3. Provider-skip logic in qgsd-prompt.js:
       - Read hooks/qgsd-prompt.js as string
       - Assert: file contains 'PROVIDER DOWN' or 'provider-cache' reference
       - RED now (no provider-skip logic)

    4. probe-quorum-slots resolves to provider:
       - Read bin/probe-quorum-slots.cjs as string
       - Assert: file references 'provider' field from providers.json (provider-level resolution)
       - RED now

    5. Multiple slots share provider:
       - Read bin/providers.json, group entries by provider field
       - Assert: at least one provider maps to 2+ slots (verifying the "all slots on that provider" grouping)
       - RED now (no provider field)

    UNIT TESTS (GREEN immediately):

    6. Provider grouping function:
       - Inline: function groupByProvider(providers) { return providers.reduce((acc, p) => { (acc[p.provider] = acc[p.provider] || []).push(p.name); return acc; }, {}); }
       - Test: groupByProvider([{name:'a',provider:'x'},{name:'b',provider:'x'},{name:'c',provider:'y'}]) deep equals {x:['a','b'], y:['c']}
       - GREEN immediately

    7. Provider-skip filter:
       - Inline: function skipDownProviders(slots, downProviders) { return slots.filter(s => !downProviders.has(s.provider)); }
       - Test: skipDownProviders([{name:'a',provider:'x'},{name:'b',provider:'y'}], new Set(['x'])) deep equals [{name:'b',provider:'y'}]
       - GREEN immediately

    Read paths for source files (NOT installed copies):
    - bin/call-quorum-slot.cjs
    - bin/providers.json
    - hooks/qgsd-prompt.js
    - bin/probe-quorum-slots.cjs
  </behavior>
  <implementation>
    Follow exact pattern from bin/roadmapper-formal-integration.test.cjs and other bin/*.test.cjs files:
    - require('node:test') with test() blocks
    - require('node:assert/strict') for assertions
    - require('node:fs') for readFileSync
    - require('node:path') for path.resolve

    For structural tests:
      const content = fs.readFileSync(path.resolve(__dirname, 'call-quorum-slot.cjs'), 'utf8');
      assert.ok(content.includes('retryWithBackoff'), 'Retry function not found');

    For provider JSON tests:
      const providers = JSON.parse(fs.readFileSync(path.resolve(__dirname, 'providers.json'), 'utf8')).providers;
      for (const p of providers) {
        assert.ok(typeof p.provider === 'string' && p.provider.length > 0, `Missing provider for ${p.name}`);
      }

    RED/GREEN commit sequence (single commit):
      Run: node --test bin/call-quorum-slot-retry.test.cjs bin/provider-mapping.test.cjs
      Confirm: structural tests FAIL, unit tests PASS
      Commit: test(v0.24-01-01): add failing test scaffolding for retry backoff and provider mapping
  </implementation>
</feature>

<verification>
Run tests and confirm RED/GREEN split:
```bash
node --test bin/call-quorum-slot-retry.test.cjs bin/provider-mapping.test.cjs 2>&1 | grep -E "^(#|not ok|ok )" | head -30
```

Expected output: structural tests show "not ok" (failing), unit tests show "ok" (passing).
Confirm no syntax errors by verifying the command exits (even with test failures — exit code 1 is expected).
</verification>

<success_criteria>
- bin/call-quorum-slot-retry.test.cjs exists, at least 80 lines
- bin/provider-mapping.test.cjs exists, at least 80 lines
- Both files run without syntax error under node --test
- Structural tests fail (RED): retry function, backoff delays, provider field, provider-skip logic
- Unit tests pass (GREEN): backoff calculation, provider grouping, provider-skip filter
- Commit message: test(v0.24-01-01): add failing test scaffolding for retry backoff and provider mapping
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-01-provider-infrastructure-and-failover/v0.24-01-01-SUMMARY.md`
</output>
