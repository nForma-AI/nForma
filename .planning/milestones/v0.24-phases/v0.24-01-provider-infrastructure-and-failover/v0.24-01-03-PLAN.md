---
phase: v0.24-01-provider-infrastructure-and-failover
plan: 03
type: implementation
wave: 1
depends_on: [01]
files_modified:
  - bin/providers.json
  - hooks/qgsd-prompt.js
  - hooks/dist/qgsd-prompt.js
autonomous: true
requirements: [FAIL-02]
formal_artifacts: none

must_haves:
  truths:
    - "providers.json contains an explicit slot-to-provider mapping for every slot in quorum_active — running node bin/probe-quorum-slots.cjs resolves each slot to its backing provider without ambiguity"
    - "Every entry in providers.json has a provider field that is a non-empty lowercase kebab-case string matching the canonical provider name"
    - "When a provider health probe returns DOWN, all slots backed by that provider are skipped in a single dispatch decision — no per-slot probing of a known-dead provider"
    - "qgsd-prompt.js reads the provider health cache (~/.claude/qgsd-provider-cache.json) and providers.json to identify DOWN providers and skip all their slots"
    - "Quorum still reaches a DECIDED state on every run where at least one provider with responsive slots remains available (EventualConsensus preserved under partial provider failure)"
  artifacts:
    - path: "bin/providers.json"
      provides: "Explicit provider field on every slot entry"
      min_lines: 380
    - path: "hooks/qgsd-prompt.js"
      provides: "Provider-aware dispatch skip logic"
      min_lines: 430
  key_links:
    - from: "hooks/qgsd-prompt.js"
      to: "bin/providers.json"
      via: "findProviders or JSON.parse"
      pattern: "providers\\.json|provider"
    - from: "hooks/qgsd-prompt.js"
      to: "~/.claude/qgsd-provider-cache.json"
      via: "fs.readFileSync"
      pattern: "qgsd-provider-cache|provider-cache"
---

<objective>
Implement FAIL-02: add explicit slot-to-provider mapping in providers.json and integrate
provider-level health skip into qgsd-prompt.js dispatch. When a provider health probe returns
DOWN, ALL slots backed by that provider are skipped in a single dispatch decision, eliminating
per-slot probing of known-dead providers.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/v0.24-01-provider-infrastructure-and-failover/v0.24-01-RESEARCH.md
@bin/providers.json
@hooks/qgsd-prompt.js
@bin/check-provider-health.cjs
</context>

<feature>
  <name>Provider Field in providers.json + Provider-Aware Dispatch Skip</name>
  <files>bin/providers.json, hooks/qgsd-prompt.js, hooks/dist/qgsd-prompt.js</files>
  <behavior>
    TASK 1: Add provider field to providers.json

    Add a "provider" field to every entry in the providers array. The provider value is a
    canonical lowercase kebab-case identifier for the backing infrastructure provider:

    - codex-1, codex-2 → "openai"
    - gemini-1, gemini-2 → "google"
    - opencode-1 → "xai" (backed by Grok on xAI)
    - copilot-1 → "github"
    - claude-1 → "akashml" (DeepSeek-V3.2 via AkashML)
    - claude-2 → "akashml" (MiniMax-M2.5 via AkashML)
    - claude-3 → "together-xyz" (Qwen3-Coder via Together.xyz)
    - claude-4 → "fireworks" (Kimi-K2 via Fireworks)
    - claude-5 → "together-xyz" (Llama-4-Maverick via Together.xyz)
    - claude-6 → "fireworks" (GLM-5 via Fireworks)

    Place the provider field right after the "name" field for each entry for readability.

    TASK 2: Provider-aware dispatch skip in qgsd-prompt.js

    Add a getDownProviderSlots(cwd) function that:
    1. Reads the provider health cache at ~/.claude/qgsd-provider-cache.json
    2. Reads providers.json (using existing findProviders pattern from call-quorum-slot.cjs)
    3. For each DOWN provider in the cache, collects all slot names from providers.json that
       have that provider's baseUrl
    4. Returns an array of slot names to skip

    MATCHING LOGIC:
    The cache is keyed by baseUrl (e.g., "https://api.akashml.com/v1"). The providers.json
    entries for HTTP-based slots (claude-1 through claude-6) don't have a baseUrl — their
    backing provider's URL is in ~/.claude.json mcpServers env.

    However, providers.json entries DO have display_provider. The mapping from cache baseUrl
    to provider field:
    - Check-provider-health.cjs extracts hostname from baseUrl (e.g., "akashml" from "api.akashml.com")
    - Match this against the "provider" field in providers.json

    For subprocess CLI providers (codex, gemini, opencode, copilot), the provider cache does
    not cover them (no HTTP endpoint). Their health is already handled by the existing
    getRecentlyTimedOutSlots function. So the provider-skip logic only needs to cover the
    claude-1..6 HTTP-backed slots.

    ALTERNATIVE simpler approach: Rather than matching hostnames, read the cache entries
    directly and match by iterating providers.json entries that share the same provider field
    value. Build a mapping:
      providerToSlots = groupBy(providers, p => p.provider)
      For each cache entry (baseUrl → {healthy: false}):
        hostname = extractHostname(baseUrl) → e.g., "akashml"
        match providers.json entries where provider field starts with or contains hostname
        Add all matched slot names to skip list

    INTEGRATION in qgsd-prompt.js:
    In the existing code, after getRecentlyTimedOutSlots(cwd), call getDownProviderSlots(cwd):

    const recentTimeouts = getRecentlyTimedOutSlots(cwd);
    const providerSkips = getDownProviderSlots(cwd);
    const allSkips = [...new Set([...recentTimeouts, ...providerSkips])];

    For the skipNote generation:
    - If providerSkips.length > 0, add: 'SKIP (PROVIDER DOWN): [slots] — entire provider unreachable.\n'
    - Keep existing timeout skip note for recentTimeouts

    TASK 3: Install sync

    After editing hooks/qgsd-prompt.js:
    cp hooks/qgsd-prompt.js hooks/dist/qgsd-prompt.js
    node bin/install.js --claude --global
  </behavior>
  <implementation>
    STEP 1: Edit bin/providers.json
    Add "provider": "<value>" after the "name" field for each of the 12 entries.

    STEP 2: Edit hooks/qgsd-prompt.js

    a) Add findProviders() function (borrow pattern from call-quorum-slot.cjs lines 123-147):
       function findProviders() {
         const searchPaths = [
           path.join(__dirname, '..', 'bin', 'providers.json'),
           path.join(os.homedir(), '.claude', 'qgsd-bin', 'providers.json'),
         ];
         for (const p of searchPaths) {
           try { if (fs.existsSync(p)) return JSON.parse(fs.readFileSync(p, 'utf8')).providers; } catch (_) {}
         }
         return null;
       }

    b) Add getDownProviderSlots() function:
       function getDownProviderSlots() {
         try {
           const cachePath = path.join(os.homedir(), '.claude', 'qgsd-provider-cache.json');
           if (!fs.existsSync(cachePath)) return [];
           const cache = JSON.parse(fs.readFileSync(cachePath, 'utf8'));
           if (!cache || !cache.entries) return [];

           const providers = findProviders();
           if (!providers) return [];

           // Build hostname → provider field mapping
           const downHostnames = [];
           const now = Date.now();
           for (const [baseUrl, entry] of Object.entries(cache.entries)) {
             const ttl = entry.healthy ? 180000 : 300000; // match check-provider-health.cjs TTL
             if (now - entry.cachedAt >= ttl) continue; // expired
             if (entry.healthy) continue; // UP — not skipping

             try {
               const hostname = new URL(baseUrl).hostname.replace(/^api\./, '').replace(/\.com$|\.ai$|\.xyz$/, '');
               downHostnames.push(hostname);
             } catch (_) {}
           }

           if (downHostnames.length === 0) return [];

           // Find slots backed by down providers
           const skipSlots = [];
           for (const p of providers) {
             if (!p.provider) continue;
             for (const dh of downHostnames) {
               if (p.provider.includes(dh) || dh.includes(p.provider)) {
                 skipSlots.push(p.name);
                 break;
               }
             }
           }
           return skipSlots;
         } catch (_) { return []; } // fail-open
       }

    c) In the dispatch section (around line 344), integrate the provider skip:
       Replace:
         const recentTimeouts = getRecentlyTimedOutSlots(cwd);
         const skipNote = recentTimeouts.length > 0 ? ... : '';
       With:
         const recentTimeouts = getRecentlyTimedOutSlots(cwd);
         const providerSkips = getDownProviderSlots();
         const allSkipSlots = [...new Set([...recentTimeouts, ...providerSkips])];
         let skipNote = '';
         if (recentTimeouts.length > 0) {
           skipNote += `SKIP (TIMEOUT < 30min ago): [${recentTimeouts.join(', ')}] — do NOT dispatch these slots.\n`;
         }
         if (providerSkips.length > 0) {
           skipNote += `SKIP (PROVIDER DOWN): [${providerSkips.join(', ')}] — entire provider unreachable, skip all backed slots.\n`;
         }

    d) Also filter the cappedSlots list to exclude skipped slots BEFORE building dynamicSteps:
       After the orderedSlots.slice() call, add:
         const skipSet = new Set(allSkipSlots);
         cappedSlots = cappedSlots.filter(s => !skipSet.has(s.slot));

    STEP 3: Install sync
       cp hooks/qgsd-prompt.js hooks/dist/qgsd-prompt.js && node bin/install.js --claude --global

    Commit message: feat(v0.24-01-03): add provider field to providers.json + provider-aware dispatch skip (FAIL-02)
  </implementation>
</feature>

<verification>
Run the Wave 0 tests:
```bash
node --test bin/provider-mapping.test.cjs 2>&1 | grep -E "^(#|not ok|ok )" | head -20
```

Expected: structural tests for provider field and provider-skip logic should now PASS (GREEN).

Verify providers.json has provider field for all entries:
```bash
node -e "const p=require('./bin/providers.json').providers; p.forEach(e => console.log(e.name, '->', e.provider))"
```

Verify probe-quorum-slots resolves each slot:
```bash
node -e "const p=require('./bin/providers.json').providers; const g={}; p.forEach(e => {(g[e.provider]=g[e.provider]||[]).push(e.name)}); console.log(JSON.stringify(g,null,2))"
```
</verification>

<success_criteria>
- Every entry in providers.json has a "provider" field (non-empty, lowercase kebab-case)
- Provider grouping: codex-1,codex-2 → openai; gemini-1,gemini-2 → google; claude-1,claude-2 → akashml; claude-3,claude-5 → together-xyz; claude-4,claude-6 → fireworks; opencode-1 → xai; copilot-1 → github
- qgsd-prompt.js contains getDownProviderSlots() function
- qgsd-prompt.js reads ~/.claude/qgsd-provider-cache.json for provider health status
- DOWN provider → all backed slots skipped in dispatch
- hooks/dist/qgsd-prompt.js synced and install.js run
- bin/provider-mapping.test.cjs structural tests pass GREEN
- Commit message: feat(v0.24-01-03): add provider field to providers.json + provider-aware dispatch skip (FAIL-02)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-01-provider-infrastructure-and-failover/v0.24-01-03-SUMMARY.md`
</output>
