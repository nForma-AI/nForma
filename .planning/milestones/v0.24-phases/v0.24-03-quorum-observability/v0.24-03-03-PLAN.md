---
phase: v0.24-03-quorum-observability
plan: 03
type: execute
wave: 1
depends_on: [v0.24-03-01]
files_modified:
  - bin/update-scoreboard.cjs
  - hooks/qgsd-prompt.js
autonomous: true
requirements: [OBS-02, OBS-03]
formal_artifacts: none

must_haves:
  truths:
    - "The scoreboard tracks quorum delivery rate -- after N quorum rounds, running /qgsd:health shows the percentage of rounds that achieved the target vote count (e.g., 3/3: 87%, 2/3: 13%)"
    - "Each slot has a flakiness score computed from recent UNAVAIL/timeout frequency -- a slot with 3 timeouts in the last 10 rounds has a visibly higher flakiness score than one with 0 timeouts"
    - "High-flakiness slots are deprioritized in dispatch ordering -- observable by comparing dispatch position before and after a slot accumulates failures"
    - "Flakiness uses a trailing 10-round window per slot — not first-10, not lifetime average, but the most recent 10 rounds that slot participated in"
    - "Delivery stats and flakiness are recomputed automatically during merge-wave — no separate manual call needed"
  artifacts:
    - path: "bin/update-scoreboard.cjs"
      provides: "computeDeliveryStats and computeFlakiness functions; extended emptyData schema with delivery_stats and per-slot flakiness"
      contains: "computeDeliveryStats"
    - path: "hooks/qgsd-prompt.js"
      provides: "Flakiness-aware dispatch ordering: sort by flakiness ascending, then success rate descending"
      contains: "flakiness"
  key_links:
    - from: "bin/update-scoreboard.cjs"
      to: ".planning/quorum-scoreboard.json"
      via: "JSON read-modify-write in merge-wave with delivery_stats and flakiness computation"
      pattern: "delivery_stats|computeDeliveryStats|computeFlakiness"
    - from: "hooks/qgsd-prompt.js"
      to: ".planning/quorum-scoreboard.json"
      via: "Read scoreboard slots flakiness_score for dispatch ordering"
      pattern: "flakiness_score|flakiness"
    - from: "bin/update-scoreboard.cjs"
      to: "hooks/qgsd-prompt.js"
      via: "Scoreboard writes flakiness_score that prompt.js reads for sorting"
      pattern: "flakiness_score"
---

<objective>
Implement quorum delivery rate tracking and per-slot flakiness scoring in the scoreboard, then
wire flakiness-aware dispatch ordering into qgsd-prompt.js so high-flakiness slots are
deprioritized.

Purpose: OBS-02 (delivery rate tracking) and OBS-03 (flakiness scoring + dispatch ordering).
These complete the observability foundation that v0.24-04 self-healing will consume.
Output: Extended update-scoreboard.cjs with delivery stats and flakiness; modified qgsd-prompt.js
with flakiness-aware sort.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-03-quorum-observability/v0.24-03-RESEARCH.md
@.planning/phases/v0.24-03-quorum-observability/v0.24-03-01-SUMMARY.md
@bin/update-scoreboard.cjs
@hooks/qgsd-prompt.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scoreboard with delivery stats and flakiness scoring</name>
  <files>bin/update-scoreboard.cjs</files>
  <action>
    Extend bin/update-scoreboard.cjs with three additions:

    1. EXTEND emptyData() — add delivery_stats and per-slot flakiness fields:

    In the emptyData() function (around line 144), add to the returned object:
    - delivery_stats: { total_rounds: 0, target_vote_count: 3, achieved_by_outcome: {} }

    The existing slots: {} object will be extended at runtime with flakiness_score and
    recent_verdicts fields per slot (no schema change to emptyData needed for slots).

    2. ADD computeDeliveryStats(data) function:

    function computeDeliveryStats(data):
    - Iterate over data.rounds
    - For each round, count the number of valid votes in round.votes (excluding failures)
    - Group rounds by vote count, compute percentages
    - Store result in data.delivery_stats with structure:
      { total_rounds, target_vote_count: 3, achieved_by_outcome: { '3_votes': {count, pct}, '2_votes': {count, pct}, ... } }
    - pct MUST be a JSON number, not a string. Since .toFixed(1) returns a string, use
      parseFloat(((count / total) * 100).toFixed(1)) or Math.round(count / total * 1000) / 10
      to ensure pct is stored as a number (e.g., 87.2 not "87.2")
    - Handle empty rounds array gracefully (total_rounds: 0, empty achieved_by_outcome)
    - Return data for chaining

    Vote-counting pseudocode — iterate over a round's votes object and count valid entries
    (excluding failures) regardless of whether keys are model-keyed (e.g., votes.claude) or
    slot-keyed (e.g., votes['gemini-1:gemini-2.0-flash']):

      function countValidVotes(votes) {
        let count = 0;
        for (const key of Object.keys(votes)) {
          const v = votes[key];
          if (v && v !== '' && v !== 'UNAVAIL' && v !== 'TIMEOUT') count++;
        }
        return count;
      }

    Use this pattern inside computeDeliveryStats when iterating data.rounds — for each round,
    call countValidVotes(round.votes) to get the vote count for that round. This handles both
    key formats uniformly because it iterates all keys without inspecting key structure.

    3. ADD computeFlakiness(data) function:

    function computeFlakiness(data, windowSize = 10):
    - Build a set of all unique slot names from all rounds (both model-keyed and slot-keyed votes)
    - For each slot, collect the last windowSize rounds where that slot has a vote
    - Count failures in that window: votes that are 'UNAVAIL', 'TIMEOUT', empty string '',
      null/undefined, or rounds where the slot's verdict is absent when the slot was in the
      dispatch list
    - Compute flakiness_score = failures / window.length (0.0 = reliable, 1.0 = always fails)
    - Store in data: create or update data.slots entries. For model-keyed slots (no ':' separator),
      use the model name as slot key. For slot-keyed entries (containing ':'), use the slot name
      portion (before ':').
    - Each slot entry gets: flakiness_score (number), recent_verdicts (array of
      {round: round_num, verdict: vote_value} for the window)
    - If a slot has fewer than 1 round, flakiness_score = 0.0
    - Return data for chaining

    IMPORTANT: flakiness_score precision — store as a JavaScript number (not string) in the
    scoreboard JSON. For example, 0.3 not '0.30'. Display formatting with .toFixed(2) is for
    log output and stderr display only, never for the stored value.

    4. INTEGRATE into merge-wave:

    In the mergeWave() function (around line 594), AFTER the existing recomputeStats(data) and
    recomputeSlots(data) calls, add:
    - computeDeliveryStats(data);
    - computeFlakiness(data);

    This ensures delivery stats and flakiness are automatically recomputed every time merge-wave
    runs — no separate CLI command needed.

    5. EXPORT for testing:

    bin/update-scoreboard.cjs has NO existing module.exports block. CREATE one at the bottom
    of the file, using the same guard pattern as hooks/qgsd-prompt.js (line 612):

      if (typeof module !== 'undefined') {
        module.exports = { computeDeliveryStats, computeFlakiness, emptyData };
      }

    This guard ensures the exports are only set when the file is require()d by tests, not
    when run as a CLI script (where main() calls process.exit() before reaching this line).

    IMPORTANT constraints:
    - Do NOT modify existing recomputeStats or recomputeSlots behavior
    - Do NOT modify existing emptyData model stats structure
    - Fail-open: if computeDeliveryStats or computeFlakiness throws, catch and log to stderr,
      do not interrupt merge-wave
    - Use existing loadData/saveData patterns for atomic JSON writes
    - Count both model-keyed votes (e.g., votes.claude) and slot-keyed votes (e.g., votes['gemini-1:model-id'])
      when computing delivery stats — a round's total vote count is the count of all non-empty,
      non-UNAVAIL, non-TIMEOUT votes regardless of key format
  </action>
  <verify>
    Run the TDD structural tests from Plan 01:
    ```bash
    node --test bin/scoreboard-observability.test.cjs 2>&1 | grep -E "(not ok|ok )" | head -20
    ```
    Structural tests for computeDeliveryStats and computeFlakiness should now pass (GREEN).

    Verify exports:
    ```bash
    node -e "const m = require('./bin/update-scoreboard.cjs'); console.log(typeof m.computeDeliveryStats, typeof m.computeFlakiness)"
    ```
    Should output: function function
  </verify>
  <done>
    - computeDeliveryStats function exists in bin/update-scoreboard.cjs
    - computeFlakiness function exists in bin/update-scoreboard.cjs
    - emptyData() includes delivery_stats object
    - merge-wave calls both functions after recomputeStats/recomputeSlots
    - Both functions are exported via module.exports (newly created block with typeof guard)
    - Delivery stats compute correct percentages from round vote counts
    - countValidVotes excludes 'UNAVAIL', 'TIMEOUT', empty string, null/undefined
    - computeFlakiness counts 'UNAVAIL', 'TIMEOUT', '', null/undefined as failures
    - pct values are stored as JSON numbers (parseFloat or Math.round pattern), not strings
    - Flakiness uses trailing 10-round window, returns score 0.0-1.0
    - flakiness_score is stored as a number (e.g., 0.3, not '0.30') in the scoreboard JSON
    - Integration test gate: All structural + unit tests from Plan 01 pass GREEN (0 failures): node --test bin/quorum-telemetry.test.cjs bin/scoreboard-observability.test.cjs
  </done>
</task>

<task type="auto">
  <name>Task 2: Add flakiness-aware dispatch ordering to qgsd-prompt.js</name>
  <files>hooks/qgsd-prompt.js</files>
  <action>
    Modify hooks/qgsd-prompt.js to incorporate flakiness scores into dispatch ordering.

    1. MODIFY sortBySuccessRate function (around line 215) to also read flakiness scores:

    The existing sortBySuccessRate reads scoreboard.slots to compute success rates per slot.
    Extend it to ALSO read flakiness_score from each slot entry and use it as the PRIMARY
    sort key, with success rate as the SECONDARY tiebreaker.

    Updated sort logic:
    - Primary: flakiness_score ascending (lower = more reliable = dispatched first)
    - Secondary: success rate descending (higher = better = dispatched first)
    - Default flakiness_score to 0 if not present (treat unknown slots as reliable — fail-open)

    Implementation approach — modify the existing sorted = [...slots].sort() comparator:

    Replace:
      const sorted = [...slots].sort((a, b) => getRate(b.slot) - getRate(a.slot));

    With:
      const getFlakiness = (slotName) => {
        // Look up flakiness_score from scoreboard slots entries for this slot name
        const entries = Object.entries(scoreboard.slots)
          .filter(([k]) => k === slotName || k.startsWith(slotName + ':'));
        if (entries.length === 0) return 0; // fail-open: unknown = reliable
        // Use the max flakiness across any model for this slot
        return Math.max(...entries.map(([_, v]) => v.flakiness_score ?? 0));
      };

      const sorted = [...slots].sort((a, b) => {
        const flakDiff = getFlakiness(a.slot) - getFlakiness(b.slot);
        if (flakDiff !== 0) return flakDiff;
        return getRate(b.slot) - getRate(a.slot);
      });

    2. UPDATE stderr logging to include flakiness:

    After the sort, update the existing console.error line to include flakiness info:
      console.error('[qgsd-dispatch] DISPATCH ORDER (flakiness,rate): [' +
        sorted.map(s => `${s.slot}(f=${getFlakiness(s.slot).toFixed(2)},r=${getRate(s.slot).toFixed(3)})`).join(', ') + ']');

    3. EXPORT: Add sortBySuccessRate to module.exports if not already present (it already is
    at line 617, so no change needed).

    IMPORTANT constraints:
    - Do NOT rename sortBySuccessRate — it's referenced by tests and other code. The function
      now sorts by flakiness + success rate but keeps its original name for backward compatibility.
    - Fail-open: if reading flakiness_score fails for any reason, default to 0 (treat as reliable)
    - Do NOT modify getAvailableSlots or any other function
    - The overall try/catch in sortBySuccessRate already returns [...slots] on any error — this
      provides the fail-open safety net

    4. INSTALL: After modifying hooks/qgsd-prompt.js, sync to dist and install:
    ```bash
    cp hooks/qgsd-prompt.js hooks/dist/
    node bin/install.js --claude --global
    ```
  </action>
  <verify>
    Run the TDD tests:
    ```bash
    node --test bin/scoreboard-observability.test.cjs 2>&1 | grep -E "(not ok|ok )"
    ```
    The flakiness-aware sorting structural test should now pass (GREEN).

    Verify installed copy matches source:
    ```bash
    diff hooks/qgsd-prompt.js ~/.claude/hooks/qgsd-prompt.js
    ```
    Should show no differences (or only expected installer modifications).
  </verify>
  <done>
    - sortBySuccessRate in hooks/qgsd-prompt.js sorts by flakiness ascending, then success rate descending
    - Flakiness score read from scoreboard.slots entries with fail-open default of 0
    - Dispatch ordering log line includes flakiness values
    - Installed copy at ~/.claude/hooks/ matches source
    - All structural tests in bin/scoreboard-observability.test.cjs pass (GREEN)
    - Integration test gate: All structural + unit tests from Plan 01 pass GREEN (0 failures): node --test bin/quorum-telemetry.test.cjs bin/scoreboard-observability.test.cjs
  </done>
</task>

</tasks>

<verification>
```bash
# Run all OBS-02 and OBS-03 tests
node --test bin/scoreboard-observability.test.cjs 2>&1 | tail -5

# Verify computeDeliveryStats exists
grep -c 'computeDeliveryStats' bin/update-scoreboard.cjs

# Verify computeFlakiness exists
grep -c 'computeFlakiness' bin/update-scoreboard.cjs

# Verify flakiness in qgsd-prompt.js
grep -c 'flakiness' hooks/qgsd-prompt.js

# Verify installed copy is in sync
diff hooks/qgsd-prompt.js ~/.claude/hooks/qgsd-prompt.js 2>/dev/null | head -5
```
</verification>

<success_criteria>
- All tests in bin/scoreboard-observability.test.cjs pass (0 failures)
- computeDeliveryStats and computeFlakiness exported from update-scoreboard.cjs
- merge-wave automatically calls both functions
- Delivery stats correctly compute percentages per vote-count outcome
- pct values stored as JSON numbers (not strings from .toFixed)
- countValidVotes excludes UNAVAIL, TIMEOUT, empty string, null/undefined
- computeFlakiness counts UNAVAIL, TIMEOUT, '', null/undefined as failures
- Flakiness uses trailing 10-round window, returns 0.0-1.0
- flakiness_score stored as number in JSON, .toFixed(2) used for display only
- sortBySuccessRate in qgsd-prompt.js uses flakiness as primary sort key
- Dispatch ordering prefers low-flakiness slots
- hooks/qgsd-prompt.js synced to hooks/dist/ and installed globally
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-03-quorum-observability/v0.24-03-03-SUMMARY.md`
</output>
