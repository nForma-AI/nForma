---
phase: v0.24-04-self-healing-consensus
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/self-healing.test.cjs
  - bin/health-auto-adjust.test.cjs
autonomous: true
requirements:
  - HEAL-01
  - HEAL-02
formal_artifacts: none

must_haves:
  truths:
    - "self-healing.test.cjs contains RED tests for HEAL-01: computeEarlyEscalation, readEarlyEscalationThreshold, remainingRounds calculation, threshold comparison logic"
    - "health-auto-adjust.test.cjs contains RED tests for HEAL-02: suggestMaxDeliberation, applyMaxDeliberationUpdate (machine file, config file), rollback on failure"
    - "All RED tests fail with descriptive assertion messages when run against current codebase (functions do not yet exist)"
    - "Existing tests (quorum-consensus-gate.test.cjs, verify-quorum-health.test.cjs) remain GREEN and unmodified"
  artifacts:
    - path: "bin/self-healing.test.cjs"
      provides: "TDD RED scaffolding for HEAL-01 early escalation"
      contains: "computeEarlyEscalation|readEarlyEscalationThreshold"
    - path: "bin/health-auto-adjust.test.cjs"
      provides: "TDD RED scaffolding for HEAL-02 auto-adjust"
      contains: "suggestMaxDeliberation|applyMaxDeliberationUpdate"
  key_links:
    - from: "bin/self-healing.test.cjs"
      to: "bin/quorum-consensus-gate.cjs"
      via: "require with fail-open guard"
      pattern: "require.*quorum-consensus-gate"
    - from: "bin/health-auto-adjust.test.cjs"
      to: "bin/verify-quorum-health.cjs"
      via: "require with fail-open guard"
      pattern: "require.*verify-quorum-health"
---

<objective>
Create TDD RED test scaffolding for both HEAL-01 (early escalation) and HEAL-02 (auto-adjust maxDeliberation) requirements before any implementation begins.

Purpose: TDD discipline requires failing tests to exist BEFORE implementation. These tests define the behavioral contract for Plans 02 and 03, and cannot turn GREEN until those plans execute.
Output: Two test files with RED tests that define expected function signatures, I/O contracts, and edge cases for self-healing consensus features.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-04-self-healing-consensus/v0.24-04-RESEARCH.md
@bin/quorum-consensus-gate.cjs
@bin/quorum-consensus-gate.test.cjs
@bin/verify-quorum-health.cjs
@bin/verify-quorum-health.test.cjs
@src/machines/qgsd-workflow.machine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HEAL-01 TDD RED test scaffolding (self-healing.test.cjs)</name>
  <files>bin/self-healing.test.cjs</files>
  <action>
Create bin/self-healing.test.cjs with node:test and node:assert (strict). Use the established fail-open guard pattern from v0.24-01/03:

```javascript
let mod = null;
try { mod = require('./quorum-consensus-gate.cjs'); } catch(e) {}
```

Write these test groups (all RED initially -- functions do not exist yet):

**Group 1: computeEarlyEscalation(slotRates, minQuorum, remainingRounds, threshold)**
- Test: returns { shouldEscalate: true, probability, threshold } when P(consensus | remainingRounds) < threshold (0.10)
  - Use known slot rates where P is very low (e.g., all rates = 0.1, minQuorum = 3, remainingRounds = 1)
  - Assert shouldEscalate === true, assert probability < 0.10
- Test: returns { shouldEscalate: false, probability, threshold } when P(consensus | remainingRounds) >= threshold
  - Use known slot rates where P is high (e.g., all rates = 0.9, minQuorum = 2, remainingRounds = 5)
  - Assert shouldEscalate === false, assert probability >= 0.10
- Test: remainingRounds correctly influences probability (more remaining = higher P)
  - Same rates but remainingRounds = 1 vs remainingRounds = 5; latter should have higher P
- Test: threshold parameter is respected (0.10 default, but accepts 0.50 for aggressive escalation)
  - Same rates, threshold = 0.50 triggers escalation, threshold = 0.01 does not
- Test: returns shouldEscalate: true when remainingRounds = 0 and no consensus achieved

**Group 2: readEarlyEscalationThreshold(configPaths)**
- Test: returns 0.10 when no config files exist (default)
- Test: reads workflow.early_escalation_threshold from .planning/config.json if present
  - Use fs.mkdtempSync to create temp dir with config.json containing { "workflow": { "early_escalation_threshold": 0.25 } }
- Test: invalid threshold (negative, > 1.0) falls back to default 0.10
- Test: fail-open on JSON parse error returns 0.10

Each test uses the fail-open guard: `assert.ok(mod, 'Module not loaded')` as first check, then `assert.ok(typeof mod.computeEarlyEscalation === 'function', 'computeEarlyEscalation not exported')`.

Existing GREEN tests in quorum-consensus-gate.test.cjs MUST NOT be modified. New tests import from the same module but test new exported functions.
  </action>
  <verify>
Run `node --test bin/self-healing.test.cjs 2>&1` -- all tests should FAIL (RED) because computeEarlyEscalation and readEarlyEscalationThreshold do not exist yet. Exit code should be non-zero. Count failing tests to confirm at least 9 RED tests exist.

Run `node --test bin/quorum-consensus-gate.test.cjs 2>&1` -- existing tests MUST still be GREEN.
  </verify>
  <done>
bin/self-healing.test.cjs exists with at least 9 RED tests covering computeEarlyEscalation (5 tests) and readEarlyEscalationThreshold (4 tests). All tests fail cleanly with assertion messages (not crashes). Existing tests remain GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HEAL-02 TDD RED test scaffolding (health-auto-adjust.test.cjs)</name>
  <files>bin/health-auto-adjust.test.cjs</files>
  <action>
Create bin/health-auto-adjust.test.cjs with node:test and node:assert (strict). Use fail-open guard:

```javascript
let mod = null;
try { mod = require('./verify-quorum-health.cjs'); } catch(e) {}
```

Note: verify-quorum-health.cjs currently has NO module.exports (it only runs as CLI). The RED tests will fail because functions are not exported yet. Plan 03 will add exports.

Write these test groups (all RED initially):

**Group 1: suggestMaxDeliberation(pPerRound, targetConfidence)**
- Test: returns correct k for known pPerRound and target 0.95
  - pPerRound = 0.72, targetConfidence = 0.95 -> k = ceil(log(0.05)/log(0.28)) = ceil(2.35) = 3... verify via manual calc: k = ceil(log(1 - 0.95) / log(1 - 0.72)) = ceil(2.35) = 3
  - Assert result === 3
- Test: returns 1 when pPerRound = 1.0 (always succeeds)
- Test: returns Infinity or very large number when pPerRound = 0 (never succeeds) -- handle edge case
- Test: respects different target levels (0.90 vs 0.99 produce different k values)

**Group 2: applyMaxDeliberationUpdate(newValue, options)**
- Test: updates maxDeliberation in a temp copy of machine.ts
  - Create temp dir, write mock machine.ts with `maxDeliberation:    7`
  - Call applyMaxDeliberationUpdate(9, { machineFile: tempPath })
  - Read file back, verify it now contains `maxDeliberation:    9`
- Test: updates workflow.maxDeliberation in config.json if it exists
  - Create temp config.json with { "workflow": {} }
  - Call applyMaxDeliberationUpdate(9, { configFile: tempConfigPath })
  - Read file back, verify workflow.maxDeliberation === 9
- Test: skips config.json update if file does not exist (no error thrown)
- Test: rollback on failure restores original files
  - Mock spec generation to throw, verify machine.ts reverted to original value after failed apply

**Group 3: Integration (recommendAndApply flow)**
- Test: when pActual < targetConfidence, returns { recommended: true, suggestedValue: k, applied: false } without --auto-apply
- Test: when pActual >= targetConfidence, returns { recommended: false } (no action needed)

Each test uses fail-open guard: `assert.ok(mod, 'Module not loaded')` then `assert.ok(typeof mod.suggestMaxDeliberation === 'function', 'suggestMaxDeliberation not exported')`.

Use fs.mkdtempSync for all file operations. Clean up in afterEach.
  </action>
  <verify>
Run `node --test bin/health-auto-adjust.test.cjs 2>&1` -- all tests should FAIL (RED) because suggestMaxDeliberation and applyMaxDeliberationUpdate are not exported from verify-quorum-health.cjs. Exit code non-zero. Count at least 9 RED tests.

Run `node --test bin/verify-quorum-health.test.cjs 2>&1` -- existing tests MUST still be GREEN.
  </verify>
  <done>
bin/health-auto-adjust.test.cjs exists with at least 9 RED tests covering suggestMaxDeliberation (4 tests), applyMaxDeliberationUpdate (4 tests), and recommendAndApply flow (2 tests). All tests fail cleanly. Existing tests remain GREEN.
  </done>
</task>

</tasks>

<verification>
1. `node --test bin/self-healing.test.cjs` -- all tests RED (non-zero exit)
2. `node --test bin/health-auto-adjust.test.cjs` -- all tests RED (non-zero exit)
3. `node --test bin/quorum-consensus-gate.test.cjs` -- existing tests GREEN
4. `node --test bin/verify-quorum-health.test.cjs` -- existing tests GREEN
5. No files outside bin/self-healing.test.cjs and bin/health-auto-adjust.test.cjs were modified
</verification>

<success_criteria>
- Two new test files exist with at least 18 total RED tests (9 per file)
- All RED tests fail with descriptive assertion messages, not crashes or import errors
- Existing test suites remain GREEN and untouched
- Test contracts define function signatures matching RESEARCH.md architecture
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-04-self-healing-consensus/v0.24-04-01-SUMMARY.md`
</output>
