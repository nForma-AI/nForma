---
phase: v0.24-05-slot-worker-thin-passthrough
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified: []
autonomous: false
requirements: [DISP-04, DISP-05]
formal_artifacts: none

must_haves:
  truths:
    - "Per-worker token usage drops below 5k tokens measured by comparing before/after on a real quorum round"
    - "All existing quorum flows (Mode A pure question, Mode A with artifact, Mode B execution review, R3.6 improvement iteration) produce identical verdicts when run through the refactored path"
    - "The quorum reaches DECIDED state with the refactored worker — EventualConsensus formal invariant preserved"
  artifacts: []
  key_links:
    - from: "agents/qgsd-quorum-slot-worker.md"
      to: "bin/quorum-slot-dispatch.cjs"
      via: "Real quorum dispatch end-to-end"
      pattern: "quorum-slot-dispatch\\.cjs"
---

<objective>
Validate the full refactored quorum pipeline end-to-end by running a real quorum dispatch
and confirming that the thin passthrough worker produces correct results with dramatically
reduced token usage.

Purpose: Final validation that DISP-04 and DISP-05 are complete — the refactored path must
produce identical result block format and reach consensus.
Output: Human-verified quorum dispatch confirming token reduction and correctness.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/v0.24-05-slot-worker-thin-passthrough/v0.24-05-03-SUMMARY.md
@agents/qgsd-quorum-slot-worker.md
@bin/quorum-slot-dispatch.cjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify real quorum dispatch with refactored worker</name>
  <files>agents/qgsd-quorum-slot-worker.md, bin/quorum-slot-dispatch.cjs</files>
  <action>
Human verification checkpoint. All implementation was completed in Plans 01-03.
This task validates the full refactored quorum pipeline end-to-end.

What was built: Complete quorum slot worker refactoring — prompt construction and output
parsing moved from Haiku agent into bin/quorum-slot-dispatch.cjs. Agent reduced to thin
passthrough (1 Bash call, verbatim output). All unit tests pass.

How to verify:
1. Run a real Mode A quorum dispatch:
   /qgsd:quorum "Does the current slot worker agent spec look correct for thin passthrough?"

2. Observe the parallel worker UI. Each worker should show exactly 1 tool use (Bash call to
   quorum-slot-dispatch.cjs). No file reads, no additional Bash calls for prompt construction.

3. Check the result blocks: each worker should return a structured YAML result block
   (slot, round, verdict, reasoning, raw) — NOT a prose response.

4. Verify token usage: in the parallel worker UI, each slot worker should show token counts
   under 5k (previously 22-25k). The total quorum cost should drop by ~80%.

5. Verify consensus is reached: the quorum should produce a DECIDED state (CONSENSUS or
   ESCALATED — either is valid as long as it completes).

6. If Mode A works, optionally test Mode B by running a verification quorum:
   /qgsd:quorum "Run npm test and verify all tests pass"

Resume signal: Type "approved" if quorum completes with correct result format and reduced
token usage. Describe any issues otherwise.
  </action>
  <verify>Human observation of quorum dispatch — token usage under 5k per worker, correct result block format, DECIDED state reached.</verify>
  <done>Real quorum dispatch completes with refactored worker producing correct YAML result blocks, per-worker token usage under 5k, and consensus reached.</done>
</task>

</tasks>

<verification>
Human observes:
1. Each worker uses exactly 1 Bash call (no file reads, no prompt construction)
2. Result blocks are structured YAML matching the spec
3. Token usage per worker is under 5k
4. Quorum reaches DECIDED state
</verification>

<success_criteria>
- Real quorum dispatch completes successfully with refactored worker
- Each worker makes exactly 1 Bash call per dispatch
- Per-worker token usage is under 5k tokens (measured from parallel UI)
- Result block format matches structured YAML spec (slot, round, verdict, reasoning, raw)
- Quorum reaches DECIDED state (EventualConsensus preserved)
- Human approves the verification
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-05-slot-worker-thin-passthrough/v0.24-05-04-SUMMARY.md`
</output>
