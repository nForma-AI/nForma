---
phase: v0.26-04
slug: health-dashboard
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-03
---

# Phase v0.26-04 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.26-04` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.26-04` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.26-04`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in) |
| **Config file** | none — uses `node --test` directly |
| **Quick run command** | `node --test bin/manage-agents.test.cjs` |
| **Full suite command** | `node --test bin/manage-agents.test.cjs` |
| **Estimated runtime** | ~30 seconds |
| **CI pipeline** | none — local only |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/manage-agents.test.cjs`
- **After every plan wave:** Run `node --test bin/manage-agents.test.cjs`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.26-04-01-T1 | 01 | 1 | DASH-01, DASH-02, DASH-03 | unit | `node --test bin/manage-agents.test.cjs` | ✅ | ⬜ pending |
| v0.26-04-01-T2 | 01 | 1 | DASH-01, DASH-02, DASH-03 | unit | `node -e "const j = require('./.formal/unit-test-coverage.json'); ['DASH-01','DASH-02','DASH-03'].forEach(d => { if (!j[d].covered) throw new Error(d) })"` | ✅ | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

- Test file `bin/manage-agents.test.cjs` already exists with 181 passing tests
- Test runner `node --test` is a Node.js built-in (no install needed)
- Pure function exports (`buildDashboardLines`, `formatTimestamp`, `liveDashboard`) already available in `_pure` namespace

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-03

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.26-04`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | `node --test bin/manage-agents.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
