---
phase: v0.26-05
status: passed
verified: 2026-03-04
verifier: inline
formal_check: null
---

# Phase v0.26-05: Architecture Enforcement — Verification

## Phase Goal
QGSD structurally prevents LLM SDK bundling

## Must-Haves Verification

### Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No LLM SDK packages (openai, anthropic, @google/generative-ai, etc.) appear in QGSD's package.json dependencies | PASSED | package.json has zero npm dependencies. `grep -c '"dependencies"' package.json` returns 0. No node_modules with LLM SDKs. |
| 2 | All Haiku/Sonnet/Opus calls in QGSD source use raw HTTPS requests, never direct SDK imports | PASSED | `node bin/check-bundled-sdks.cjs` scans 106 files, finds 0 violations. update-scoreboard.cjs and validate-requirements-haiku.cjs refactored from `@anthropic-ai/sdk` to raw `https.request` calls to `api.anthropic.com/v1/messages`. Pattern matches hooks/qgsd-circuit-breaker.js (lines 253-281). |

### Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| bin/check-bundled-sdks.cjs | PRESENT | Architecture linter — scans bin/ and hooks/ for 7 forbidden SDKs. Detects require(), require.resolve(), ESM import. Exits 1 on violations, 0 on clean. |
| bin/check-bundled-sdks.test.cjs | PRESENT | 17 tests: SDK detection (6), false-positive filtering (3), scope filtering (6), error handling (1), integration (1). All pass. |
| bin/update-scoreboard.cjs | MODIFIED | classifyWithHaiku refactored: removed @anthropic-ai/sdk import, added raw https.request with 15s timeout, fail-open on errors. |
| bin/validate-requirements-haiku.cjs | MODIFIED | Removed module-level Anthropic SDK guard. Added callHaikuAPI() helper using raw HTTPS with 30s timeout. Preserved mockCall testing path. |

### Key Links

| Link | Status | Evidence |
|------|--------|----------|
| check-bundled-sdks.cjs → codebase scan | VERIFIED | Scans all .js/.cjs/.mjs files in bin/ and hooks/, excludes tests, node_modules, docs, .formal |
| update-scoreboard.cjs → api.anthropic.com | VERIFIED | Raw https.request to hostname: 'api.anthropic.com', path: '/v1/messages' with x-api-key header |
| validate-requirements-haiku.cjs → api.anthropic.com | VERIFIED | callHaikuAPI helper uses same raw HTTPS pattern |

## Requirement Coverage

| ID | Description | Status |
|----|-------------|--------|
| ARCH-10 | No bundled LLM SDKs, all calls via raw HTTPS | COVERED (17 linter tests + 0 violations on codebase scan) |

## Formal Verification

No formal scope matched this phase. The linter is a static analysis tool; no TLA+ specifications are involved.

## Score

**2/2 must-haves verified. ARCH-10 requirement covered. Phase goal achieved.**
