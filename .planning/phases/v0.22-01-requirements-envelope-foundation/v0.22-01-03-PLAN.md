---
phase: v0.22-01
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - .planning/REQUIREMENTS.md
  - formal/requirements.json
autonomous: false
requirements:
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "Running bin/aggregate-requirements.cjs on .planning/REQUIREMENTS.md produces formal/requirements.json with all 5 ENV requirements"
    - "Running bin/validate-requirements-haiku.cjs on the aggregated envelope detects any semantic issues and presents them to the user"
    - "After user approves validation results, formal/requirements.json gains a frozen_at timestamp"
    - "Re-running bin/aggregate-requirements.cjs on the frozen envelope is refused with an error"
    - "npm test includes both new test files and they pass"
  artifacts:
    - path: "formal/requirements.json"
      provides: "Frozen requirements envelope with all ENV-01..05 requirements"
    - path: "package.json"
      provides: "Updated test script including new test files"
  key_links:
    - from: "bin/aggregate-requirements.cjs"
      to: "formal/requirements.json"
      via: "end-to-end pipeline: parse -> validate -> write"
      pattern: "requirements\\.json"
    - from: "bin/validate-requirements-haiku.cjs"
      to: "formal/requirements.json"
      via: "reads envelope, validates, freezes"
      pattern: "requirements\\.json"
---

<objective>
Wire the aggregation and validation pipeline end-to-end: run aggregation on the real REQUIREMENTS.md, validate with Haiku, present findings to user, and freeze the envelope upon approval. Register new tests in npm test.

Purpose: This is the integration plan that proves ENV-01 and ENV-02 work together on real data. The user sees and approves the validation results before the envelope is frozen.
Output: Frozen `formal/requirements.json` + updated `package.json` test script
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.22-01-requirements-envelope-foundation/v0.22-01-RESEARCH.md
@.planning/phases/v0.22-01-requirements-envelope-foundation/v0.22-01-01-SUMMARY.md
@.planning/phases/v0.22-01-requirements-envelope-foundation/v0.22-01-02-SUMMARY.md
@bin/aggregate-requirements.cjs
@bin/validate-requirements-haiku.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run aggregation pipeline and register tests in npm test</name>
  <files>formal/requirements.json, package.json</files>
  <action>
1. **Run aggregation on real REQUIREMENTS.md:**
   ```bash
   node bin/aggregate-requirements.cjs
   ```
   Verify output:
   - `formal/requirements.json` exists
   - Contains all 5 ENV requirements (ENV-01 through ENV-05)
   - Each requirement has id, text, category, phase, status, provenance
   - `schema_version` is "1"
   - `frozen_at` is null (not yet frozen)

2. **Verify determinism:**
   ```bash
   cp formal/requirements.json /tmp/run1.json
   node bin/aggregate-requirements.cjs
   diff formal/requirements.json /tmp/run1.json
   ```
   Output must be identical (same content_hash).

3. **Register new test files in package.json:**
   Edit `package.json` scripts.test to append:
   - `bin/aggregate-requirements.test.cjs`
   - `bin/validate-requirements-haiku.test.cjs`
   after the existing test file list.

4. **Run full test suite:**
   ```bash
   npm test
   ```
   All existing + new tests must pass.
  </action>
  <verify>
node bin/aggregate-requirements.cjs && cat formal/requirements.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('reqs:', d.requirements.length, 'frozen:', d.frozen_at)" && npm test
  </verify>
  <done>
formal/requirements.json exists with 5 requirements, frozen_at is null, deterministic output confirmed, all tests pass including new test files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate with Haiku and present findings for user approval</name>
  <what-built>
The requirements aggregation pipeline has produced formal/requirements.json from .planning/REQUIREMENTS.md.

Now running Haiku semantic validation to check for duplicates, contradictions, and ambiguity across all 5 ENV requirements.
  </what-built>
  <how-to-verify>
1. Run the Haiku validator:
   ```bash
   node bin/validate-requirements-haiku.cjs
   ```

2. Review the findings presented:
   - CONFIRMED FINDINGS (appeared in 2+ of 3 Haiku passes)
   - Each finding shows: type, severity, affected requirement IDs, description, suggested resolution

3. For each finding, decide:
   - **Accept**: The finding is valid -- update REQUIREMENTS.md to resolve it
   - **Dismiss**: The finding is a false positive -- no action needed

4. If all findings are resolved or dismissed, approve to proceed with freezing.

5. If ANTHROPIC_API_KEY is not set or SDK unavailable, validation will be skipped (fail-open). You can still choose to freeze.
  </how-to-verify>
  <resume-signal>Type "approved" to freeze the envelope, or describe issues to address first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Freeze the requirements envelope</name>
  <files>formal/requirements.json</files>
  <action>
After user approval of validation results:

1. **Freeze the envelope:**
   ```bash
   node bin/validate-requirements-haiku.cjs --freeze
   ```
   Or if validation was skipped:
   ```bash
   node -e "const v = require('./bin/validate-requirements-haiku.cjs'); v.freezeEnvelope('formal/requirements.json').then(r => console.log(r))"
   ```

2. **Verify freeze:**
   - Read `formal/requirements.json` and confirm `frozen_at` is a valid ISO timestamp
   - Confirm re-running `node bin/aggregate-requirements.cjs` is refused with "Envelope is frozen" error

3. **Commit the frozen envelope:**
   ```bash
   git add formal/requirements.json
   git commit -m "feat(v0.22-01): freeze requirements envelope with 5 ENV requirements"
   ```
  </action>
  <verify>
node -e "const d=JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); console.log('frozen_at:', d.frozen_at); if(!d.frozen_at) process.exit(1)"
  </verify>
  <done>
formal/requirements.json has a frozen_at timestamp. Re-running aggregation is refused. Envelope is immutable.
  </done>
</task>

</tasks>

<verification>
1. `formal/requirements.json` exists with 5 requirements and frozen_at timestamp
2. `node bin/aggregate-requirements.cjs` refuses to overwrite (frozen guard)
3. `npm test` passes with all tests including new test files
4. User has reviewed and approved Haiku validation findings
</verification>

<success_criteria>
- formal/requirements.json contains all 5 ENV requirements with correct fields
- Haiku validation findings presented to user and resolved
- Envelope frozen with frozen_at timestamp after user approval
- Re-aggregation blocked by frozen guard
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.22-01-requirements-envelope-foundation/v0.22-01-03-SUMMARY.md`
</output>
