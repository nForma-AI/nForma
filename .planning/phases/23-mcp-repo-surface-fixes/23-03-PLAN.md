---
phase: 23-mcp-repo-surface-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/claude-mcp-server/Makefile
  - /Users/jonathanborduas/code/codex-mcp-server/Makefile
  - /Users/jonathanborduas/code/copilot-mcp-server/Makefile
  - /Users/jonathanborduas/code/openhands-mcp-server/Makefile
  - /Users/jonathanborduas/code/claude-mcp-server/CHANGELOG.md
  - /Users/jonathanborduas/code/codex-mcp-server/CHANGELOG.md
  - /Users/jonathanborduas/code/copilot-mcp-server/CHANGELOG.md
  - /Users/jonathanborduas/code/openhands-mcp-server/CHANGELOG.md
  - /Users/jonathanborduas/code/claude-mcp-server/CLAUDE.md
  - /Users/jonathanborduas/code/codex-mcp-server/CLAUDE.md
  - /Users/jonathanborduas/code/gemini-mcp-server/CLAUDE.md
autonomous: true
requirements:
  - STD-07
  - STD-09

must_haves:
  truths:
    - "All 4 Gen1 repos have a Makefile with lint, format, test, build, clean, dev as separate named targets"
    - ".PHONY declaration in each Gen1 Makefile lists all 6 targets"
    - "All 6 repos have a CHANGELOG.md file (4 Gen1 repos get new ones; gemini and opencode already have them)"
    - "All 6 repos have a CLAUDE.md file (claude, codex, gemini get new ones; copilot, opencode, openhands already have them)"
  artifacts:
    - path: "/Users/jonathanborduas/code/claude-mcp-server/Makefile"
      provides: "Full 6-target Makefile"
      contains: ".PHONY: lint format test build clean dev"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/Makefile"
      provides: "Full 6-target Makefile"
      contains: ".PHONY: lint format test build clean dev"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/CHANGELOG.md"
      provides: "Initial changelog"
      contains: "## [1.4.0]"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/CLAUDE.md"
      provides: "QGSD operational policy"
      contains: "CLAUDE OPERATING POLICY"
    - path: "/Users/jonathanborduas/code/gemini-mcp-server/CLAUDE.md"
      provides: "QGSD operational policy"
      contains: "CLAUDE OPERATING POLICY"
  key_links:
    - from: "/Users/jonathanborduas/code/claude-mcp-server/Makefile"
      to: "npm scripts"
      via: "make targets delegate to npm run"
      pattern: "npm run"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/CLAUDE.md"
      to: "operational policy"
      via: "copied from copilot-mcp-server/CLAUDE.md"
      pattern: "CLAUDE OPERATING POLICY"
---

<objective>
Write full 6-target Makefiles to the 4 Gen1 repos (replacing their lint-only stub) and create the missing CHANGELOG.md and CLAUDE.md files across repos that lack them.

Purpose: STD-07 requires a comprehensive Makefile for developer experience parity with Gen2 repos. STD-09 requires both CHANGELOG.md and CLAUDE.md in every repo. The Gen2 repos (gemini, opencode) are the reference — their Makefile and CLAUDE.md are already correct.

Current state:
- Makefiles: claude/codex/copilot/openhands have a stub with only `.PHONY: lint` and a combined lint+format target. gemini and opencode already have full 6-target Makefiles (no changes needed there).
- CHANGELOG.md: claude/codex/copilot/openhands are missing. gemini and opencode already have them.
- CLAUDE.md: claude/codex/gemini are missing. copilot/opencode/openhands already have them.

Output: 4 Makefiles updated; 4 CHANGELOG.md files created; 3 CLAUDE.md files created.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-mcp-repo-surface-fixes/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full 6-target Makefile for 4 Gen1 repos (STD-07)</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/Makefile
    /Users/jonathanborduas/code/codex-mcp-server/Makefile
    /Users/jonathanborduas/code/copilot-mcp-server/Makefile
    /Users/jonathanborduas/code/openhands-mcp-server/Makefile
  </files>
  <action>
    Replace the existing Makefile in each of the 4 Gen1 repos with the full 6-target version. The current files only have `.PHONY: lint` with a combined lint+format target — replace the entire file.

    IMPORTANT: Makefile targets use a TAB character for indentation (not spaces). If the Write tool produces spaces, the Makefile will be broken. Use a literal tab before each `npm run` line.

    Write the following content to all 4 Makefiles (identical across claude/codex/copilot/openhands):

    ```makefile
    .PHONY: lint format test build clean dev

    lint:
    	npm run lint

    format:
    	npm run format

    test:
    	npm test

    build:
    	npm run build

    clean:
    	npm run clean

    dev:
    	npm run dev
    ```

    This matches the pattern from gemini-mcp-server and opencode-mcp-server (the Gen2 reference implementations). Each target delegates to the corresponding npm script, which already exists in each repo's package.json.

    After writing, verify the Makefile uses real tab characters:
    ```bash
    cat -A /Users/jonathanborduas/code/claude-mcp-server/Makefile | grep npm
    ```
    The `^I` symbol in `cat -A` output confirms a tab character. If spaces appear instead, rewrite using a method that preserves tabs.
  </action>
  <verify>
    For each of the 4 repos (claude, codex, copilot, openhands):
    1. `grep -c "^[[:space:]]npm run" /Users/jonathanborduas/code/{repo}/Makefile` returns 5 (lint, format, build, clean, dev use npm run; test uses npm test directly)
    2. `grep ".PHONY" /Users/jonathanborduas/code/{repo}/Makefile` shows `lint format test build clean dev` — all 6 targets listed
    3. `cd /Users/jonathanborduas/code/{repo} && make lint` exits 0 (lint target works)

    Quick combined check:
    ```bash
    for repo in claude-mcp-server codex-mcp-server copilot-mcp-server openhands-mcp-server; do
      echo -n "$repo .PHONY: "; grep ".PHONY" /Users/jonathanborduas/code/$repo/Makefile
    done
    ```
    All 4 must show `lint format test build clean dev`.
  </verify>
  <done>
    All 4 Gen1 repos have a full 6-target Makefile with .PHONY listing all targets. Each target delegates to the corresponding npm command. Tab indentation is correct. `make lint` runs without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md and CLAUDE.md for missing repos (STD-09)</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/CHANGELOG.md
    /Users/jonathanborduas/code/codex-mcp-server/CHANGELOG.md
    /Users/jonathanborduas/code/copilot-mcp-server/CHANGELOG.md
    /Users/jonathanborduas/code/openhands-mcp-server/CHANGELOG.md
    /Users/jonathanborduas/code/claude-mcp-server/CLAUDE.md
    /Users/jonathanborduas/code/codex-mcp-server/CLAUDE.md
    /Users/jonathanborduas/code/gemini-mcp-server/CLAUDE.md
  </files>
  <action>
    Create CHANGELOG.md and CLAUDE.md files where they are missing. Do NOT modify repos that already have these files.

    **Repos already compliant — do NOT touch:**
    - CHANGELOG.md: gemini-mcp-server (exists), opencode-mcp-server (exists)
    - CLAUDE.md: copilot-mcp-server (exists), opencode-mcp-server (exists), openhands-mcp-server (exists)

    **CHANGELOG.md — create in claude, codex, copilot, openhands:**

    All 4 repos are at version 1.4.0. Write this content to each:

    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    ## [Unreleased]

    ## [1.4.0] - 2026-02-22

    - Initial public release
    - MCP server implementation with tool support
    ```

    Write to:
    - /Users/jonathanborduas/code/claude-mcp-server/CHANGELOG.md
    - /Users/jonathanborduas/code/codex-mcp-server/CHANGELOG.md
    - /Users/jonathanborduas/code/copilot-mcp-server/CHANGELOG.md
    - /Users/jonathanborduas/code/openhands-mcp-server/CHANGELOG.md

    **CLAUDE.md — create in claude, codex, gemini:**

    Read the source from copilot-mcp-server which has the correct QGSD operational policy:
    ```bash
    cat /Users/jonathanborduas/code/copilot-mcp-server/CLAUDE.md
    ```
    Copy that exact content to:
    - /Users/jonathanborduas/code/claude-mcp-server/CLAUDE.md
    - /Users/jonathanborduas/code/codex-mcp-server/CLAUDE.md
    - /Users/jonathanborduas/code/gemini-mcp-server/CLAUDE.md
  </action>
  <verify>
    1. For each of the 4 repos getting CHANGELOG.md (claude, codex, copilot, openhands):
       - `ls /Users/jonathanborduas/code/{repo}/CHANGELOG.md` exits 0
       - `grep "1.4.0" /Users/jonathanborduas/code/{repo}/CHANGELOG.md` finds the version entry

    2. For each of the 3 repos getting CLAUDE.md (claude, codex, gemini):
       - `ls /Users/jonathanborduas/code/{repo}/CLAUDE.md` exits 0
       - `grep "CLAUDE OPERATING POLICY" /Users/jonathanborduas/code/{repo}/CLAUDE.md` finds the header

    3. All-repos check:
    ```bash
    for repo in claude-mcp-server codex-mcp-server copilot-mcp-server gemini-mcp-server opencode-mcp-server openhands-mcp-server; do
      echo -n "$repo CHANGELOG: "; ls /Users/jonathanborduas/code/$repo/CHANGELOG.md 2>/dev/null && echo OK || echo MISSING
      echo -n "$repo CLAUDE.md: "; ls /Users/jonathanborduas/code/$repo/CLAUDE.md 2>/dev/null && echo OK || echo MISSING
    done
    ```
    All 12 lines must show OK.
  </verify>
  <done>
    All 6 repos have CHANGELOG.md. All 6 repos have CLAUDE.md. Content of new CHANGELOG.md files includes version 1.4.0 entry. CLAUDE.md files contain the QGSD operational policy header.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `for repo in claude-mcp-server codex-mcp-server copilot-mcp-server openhands-mcp-server; do grep ".PHONY" /Users/jonathanborduas/code/$repo/Makefile; done` — all 4 show all 6 targets
2. `for repo in claude-mcp-server codex-mcp-server copilot-mcp-server gemini-mcp-server opencode-mcp-server openhands-mcp-server; do ls /Users/jonathanborduas/code/$repo/CHANGELOG.md /Users/jonathanborduas/code/$repo/CLAUDE.md; done` — all 12 files exist (no error output)
</verification>

<success_criteria>
- STD-07 satisfied: claude/codex/copilot/openhands each have a Makefile with .PHONY listing lint/format/test/build/clean/dev as 6 separate named targets with correct tab indentation; `make lint` succeeds
- STD-09 satisfied: All 6 repos have CHANGELOG.md (4 created, 2 pre-existing); all 6 repos have CLAUDE.md (3 created, 3 pre-existing)
</success_criteria>

<output>
After completion, create `.planning/phases/23-mcp-repo-surface-fixes/23-03-SUMMARY.md`
</output>
