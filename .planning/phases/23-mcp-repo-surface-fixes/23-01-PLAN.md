---
phase: 23-mcp-repo-surface-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/openhands-mcp-server/src/index.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/src/server.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/src/__tests__/index.test.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/package.json
  - /Users/jonathanborduas/code/openhands-mcp-server/package-lock.json
  - /Users/jonathanborduas/code/claude-mcp-server/src/index.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/index.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/index.ts
autonomous: true
requirements:
  - STD-01
  - STD-03

must_haves:
  truths:
    - "openhands-mcp-server package.json name is 'openhands-mcp-server', not 'codex-mcp-server'"
    - "openhands-mcp-server class name is OpenHandsMcpServer in server.ts, index.ts, and test file"
    - "openhands-mcp-server server config name field reads 'openhands-mcp-server'"
    - "openhands-mcp-server package-lock.json root name is 'openhands-mcp-server'"
    - "openhands-mcp-server package.json has engines, publishConfig, prepublishOnly, license MIT, author tuannvm"
    - "All 4 Gen1 repos (claude, codex, copilot, openhands) read version dynamically via createRequire + pkg.version"
    - "No hardcoded version string '0.0.6' remains in any of the 4 Gen1 index.ts files"
  artifacts:
    - path: "/Users/jonathanborduas/code/openhands-mcp-server/src/server.ts"
      provides: "OpenHandsMcpServer class export"
      contains: "export class OpenHandsMcpServer"
    - path: "/Users/jonathanborduas/code/openhands-mcp-server/src/index.ts"
      provides: "openhands server entry point with correct identity and dynamic version"
      contains: "openhands-mcp-server"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/index.ts"
      provides: "claude server entry point with dynamic version"
      contains: "createRequire"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/index.ts"
      provides: "codex server entry point with dynamic version"
      contains: "createRequire"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/index.ts"
      provides: "copilot server entry point with dynamic version"
      contains: "createRequire"
  key_links:
    - from: "/Users/jonathanborduas/code/openhands-mcp-server/src/index.ts"
      to: "OpenHandsMcpServer"
      via: "import from ./server.js"
      pattern: "OpenHandsMcpServer"
    - from: "/Users/jonathanborduas/code/openhands-mcp-server/src/index.ts"
      to: "pkg.version"
      via: "createRequire + ../package.json"
      pattern: "createRequire"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/index.ts"
      to: "pkg.version"
      via: "createRequire + ../package.json"
      pattern: "pkg\\.version"
---

<objective>
Fix the openhands-mcp-server identity rename (all CodexMcpServer references → OpenHandsMcpServer) and apply dynamic version reading from package.json to all 4 Gen1 repos (claude, codex, copilot, openhands). Also apply package.json metadata fields (engines/publishConfig/prepublishOnly/license/author) to openhands while touching its package.json for the rename.

Purpose: STD-01 closes a broken identity where the openhands repo still declares itself as codex-mcp-server in 7 locations. STD-03 ensures version strings auto-update from package.json at runtime. The openhands metadata fields are bundled here because Plan 02 applies those same fields to claude/codex/copilot — having openhands touched separately by both plans would cause a write conflict.

Output: openhands-mcp-server fully renamed with correct metadata; all 4 Gen1 index.ts files using createRequire + pkg.version pattern.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-mcp-repo-surface-fixes/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: openhands-mcp-server identity rename + dynamic version + package.json metadata (STD-01, STD-03, STD-06)</name>
  <files>
    /Users/jonathanborduas/code/openhands-mcp-server/src/server.ts
    /Users/jonathanborduas/code/openhands-mcp-server/src/index.ts
    /Users/jonathanborduas/code/openhands-mcp-server/src/__tests__/index.test.ts
    /Users/jonathanborduas/code/openhands-mcp-server/package.json
    /Users/jonathanborduas/code/openhands-mcp-server/package-lock.json
  </files>
  <action>
    Apply ALL of the following changes to the openhands-mcp-server repo at /Users/jonathanborduas/code/openhands-mcp-server/.

    Read each file first to confirm exact content before editing.

    **1. src/server.ts:**
    Find `export class CodexMcpServer` and change it to `export class OpenHandsMcpServer`. No other changes.

    **2. src/index.ts — full replacement:**
    Replace the entire file with:
    ```typescript
    #!/usr/bin/env node

    import chalk from 'chalk';
    import { OpenHandsMcpServer } from './server.js';
    import { createRequire } from 'module';

    const require = createRequire(import.meta.url);
    const pkg = require('../package.json');

    const SERVER_CONFIG = {
      name: 'openhands-mcp-server',
      version: pkg.version,
    };

    async function main(): Promise<void> {
      try {
        const server = new OpenHandsMcpServer(SERVER_CONFIG);
        await server.start();
      } catch (error) {
        console.error(chalk.red('Failed to start server:'), error);
        process.exit(1);
      }
    }

    main();
    ```
    Read the current file first to check if there is any content beyond the main() call. If there is, preserve it after main().

    **3. src/__tests__/index.test.ts:**
    Read the file first to confirm exact line content. Then:
    - Replace every occurrence of `CodexMcpServer` with `OpenHandsMcpServer` (there are 3 occurrences: the import, the constructor call, and the instanceof check)

    **4. package.json — update and add fields:**

    Update existing fields:
    - `"name"`: `"codex-mcp-server"` → `"openhands-mcp-server"`
    - `"description"`: `"MCP server wrapper for OpenAI Codex CLI"` → `"MCP server for OpenHands CLI integration"`
    - In `"bin"` object: rename key `"codex-mcp-server"` → `"openhands-mcp-server"` (value `"dist/index.js"` unchanged)
    - `"repository.url"`: change `codex-mcp-server` → `openhands-mcp-server` in the URL
    - `"bugs.url"`: change `codex-mcp-server` → `openhands-mcp-server` in the URL
    - `"homepage"`: change `codex-mcp-server` → `openhands-mcp-server` in the URL
    - `"author"`: `""` → `"tuannvm"`
    - `"license"`: `"ISC"` → `"MIT"`

    Add new fields (not currently present):
    - In `"scripts"` object, add: `"prepublishOnly": "npm run build"`
    - Add top-level `"engines": { "node": ">=18.0.0" }`
    - Add top-level `"publishConfig": { "access": "public" }`

    Do NOT change: `"version"`, `"keywords"`, dependencies, devDependencies, `"type"`, `"main"`, `"files"`, `"directories"`.

    Note: src/utils/command.ts has comments about "codex CLI" — this refers to the OpenAI Codex CLI tool (the external tool this server wraps). Do NOT change those. Only rename class identity references.

    **5. After editing package.json, run:**
    ```bash
    cd /Users/jonathanborduas/code/openhands-mcp-server && npm install
    ```
    This regenerates package-lock.json with the correct name at the root `"name"` field.
  </action>
  <verify>
    1. `grep -rn "CodexMcpServer" /Users/jonathanborduas/code/openhands-mcp-server/src/` returns zero results
    2. `node -e "const p=require('/Users/jonathanborduas/code/openhands-mcp-server/package.json'); console.log(p.name, p.engines, p.publishConfig, p.scripts.prepublishOnly)"` shows `openhands-mcp-server`, `{node:'>=18.0.0'}`, `{access:'public'}`, `npm run build`
    3. `grep "codex-mcp-server" /Users/jonathanborduas/code/openhands-mcp-server/package.json` returns zero results
    4. `grep "createRequire\|pkg.version" /Users/jonathanborduas/code/openhands-mcp-server/src/index.ts` shows both lines
    5. `grep '"name"' /Users/jonathanborduas/code/openhands-mcp-server/package-lock.json | head -1` shows `"openhands-mcp-server"`
    6. `cd /Users/jonathanborduas/code/openhands-mcp-server && npm test` passes
  </verify>
  <done>
    openhands-mcp-server: zero CodexMcpServer references in src/; package.json name/bin/description/repository/bugs/homepage all say openhands-mcp-server; engines/publishConfig/prepublishOnly added; license MIT, author tuannvm; package-lock.json regenerated; npm test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dynamic version in claude-mcp-server, codex-mcp-server, copilot-mcp-server (STD-03)</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/src/index.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/index.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/index.ts
  </files>
  <action>
    Apply the createRequire dynamic version pattern to the 3 remaining Gen1 repos. Each currently has `version: '0.0.6'` hardcoded in SERVER_CONFIG. Read each file first before editing.

    For each of the 3 repos, update src/index.ts:

    **Add these lines after the existing imports (before the SERVER_CONFIG const):**
    ```typescript
    import { createRequire } from 'module';

    const require = createRequire(import.meta.url);
    const pkg = require('../package.json');
    ```

    **Change SERVER_CONFIG version field:**
    - Before: `version: '0.0.6',`
    - After: `version: pkg.version,`

    The class names and server names for each repo are already correct — do NOT change them:
    - claude-mcp-server: imports `ClaudeMcpServer`, server name `'claude-mcp-server'`
    - codex-mcp-server: imports `CodexMcpServer`, server name `'codex-mcp-server'` (correct — this is the codex CLI wrapper)
    - copilot-mcp-server: imports `CopilotMcpServer`, server name `'copilot-mcp-server'`

    Only add the createRequire lines and change the version value. No other changes.

    The `createRequire` pattern is confirmed working in ESM + TypeScript from gemini-mcp-server and opencode-mcp-server. The tsconfig.json files in all 3 repos already have `resolveJsonModule: true`.
  </action>
  <verify>
    For each of the 3 repos (claude-mcp-server, codex-mcp-server, copilot-mcp-server):
    1. `grep "createRequire" /Users/jonathanborduas/code/{repo}/src/index.ts` shows the import and const lines
    2. `grep "pkg.version" /Users/jonathanborduas/code/{repo}/src/index.ts` shows the version assignment
    3. `grep "'0.0.6'" /Users/jonathanborduas/code/{repo}/src/index.ts` returns zero results
    4. `cd /Users/jonathanborduas/code/{repo} && npm run build` exits 0 (TypeScript compiles without errors)
  </verify>
  <done>
    All 3 repos (claude, codex, copilot) have createRequire + pkg.version in index.ts. No hardcoded '0.0.6' version string in any of them. npm run build succeeds in all 3.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `grep -rn "'0.0.6'" /Users/jonathanborduas/code/claude-mcp-server/src/ /Users/jonathanborduas/code/codex-mcp-server/src/ /Users/jonathanborduas/code/copilot-mcp-server/src/ /Users/jonathanborduas/code/openhands-mcp-server/src/` — must return zero results
2. `grep -n "CodexMcpServer" /Users/jonathanborduas/code/openhands-mcp-server/src/index.ts /Users/jonathanborduas/code/openhands-mcp-server/src/server.ts /Users/jonathanborduas/code/openhands-mcp-server/src/__tests__/index.test.ts` — must return zero results
3. `grep '"name"' /Users/jonathanborduas/code/openhands-mcp-server/package.json` — must show `"openhands-mcp-server"`
4. `node -e "const p=require('/Users/jonathanborduas/code/openhands-mcp-server/package.json'); console.log(!!p.engines, !!p.publishConfig, !!p.scripts.prepublishOnly)"` — must print `true true true`
</verification>

<success_criteria>
- STD-01 satisfied: Zero CodexMcpServer references in openhands src/; package.json name, bin key, description, repository/bugs/homepage URLs all say openhands-mcp-server; package-lock.json regenerated to match
- STD-03 satisfied: All 4 Gen1 repos (claude, codex, copilot, openhands) use createRequire + pkg.version; no hardcoded '0.0.6' version strings remain
- STD-06 (partial): openhands package.json has engines node>=18, prepublishOnly, publishConfig access:public, license MIT, author tuannvm
- npm test passes in openhands-mcp-server; npm run build passes in all 4 Gen1 repos
</success_criteria>

<output>
After completion, create `.planning/phases/23-mcp-repo-surface-fixes/23-01-SUMMARY.md`
</output>
