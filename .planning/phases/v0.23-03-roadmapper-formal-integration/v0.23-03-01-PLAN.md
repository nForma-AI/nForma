---
phase: v0.23-03-roadmapper-formal-integration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/roadmapper-formal-integration.test.cjs
autonomous: true
requirements: [WFI-05, ENF-03]
formal_artifacts: none

must_haves:
  truths:
    - "bin/roadmapper-formal-integration.test.cjs exists and runs under node --test without syntax errors"
    - "Structural tests for gsd-tools goal regex are RED (failing) because qgsd-core/bin/gsd-tools.cjs still has \\*\\*Goal:\\*\\* (colon inside asterisks)"
    - "Structural tests for plan-phase Step 4.5 source field are RED (failing) because qgsd-core/workflows/plan-phase.md still uses .phase_name not .goal // .phase_name"
    - "Structural tests for execute-phase verify_phase_goal are RED (failing) because qgsd-core/workflows/execute-phase.md still has .goal // empty, missing '/' separator, and unidirectional match"
    - "Structural tests for new-milestone.md formal scan block are RED (failing) because qgsd-core/workflows/new-milestone.md has no formal scope scan yet"
    - "Unit tests for keyword-match algorithm (standardized version) are GREEN from the start"
  artifacts:
    - path: "bin/roadmapper-formal-integration.test.cjs"
      provides: "Structural tests for all 5 changed files + unit tests for keyword-match algorithm"
      min_lines: 60
  key_links:
    - from: "bin/roadmapper-formal-integration.test.cjs"
      to: "qgsd-core/bin/gsd-tools.cjs"
      via: "fs.readFileSync"
      pattern: "readFileSync.*gsd-tools\\.cjs"
    - from: "bin/roadmapper-formal-integration.test.cjs"
      to: "qgsd-core/workflows/plan-phase.md"
      via: "fs.readFileSync"
      pattern: "readFileSync.*plan-phase\\.md"
    - from: "bin/roadmapper-formal-integration.test.cjs"
      to: "qgsd-core/workflows/execute-phase.md"
      via: "fs.readFileSync"
      pattern: "readFileSync.*execute-phase\\.md"
    - from: "bin/roadmapper-formal-integration.test.cjs"
      to: "qgsd-core/workflows/new-milestone.md"
      via: "fs.readFileSync"
      pattern: "readFileSync.*new-milestone\\.md"
---

<objective>
Write failing structural tests BEFORE any implementation starts. Tests assert content patterns
that will exist AFTER Plans 02 and 03 execute — intentionally RED now, turn GREEN when
implementation and install complete.

Purpose: Enforce TDD discipline. Plans 02 and 03 are not considered complete until all
structural tests pass. Unit tests for keyword-match algorithm run GREEN immediately.
Output: bin/roadmapper-formal-integration.test.cjs — structural tests RED, unit tests GREEN.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.23-03-roadmapper-formal-integration/v0.23-03-RESEARCH.md
</context>

<feature>
  <name>Test Scaffolding for v0.23-03 Roadmapper Formal Integration</name>
  <files>bin/roadmapper-formal-integration.test.cjs</files>
  <behavior>
    Single test file using node:test (native, no jest/mocha — matches existing bin/*.test.cjs pattern).
    Use require('node:test') and require('node:assert/strict').

    STRUCTURAL TESTS (RED until Plans 02-03 complete):

    1. gsd-tools.cjs goal regex fix (ISSUE-2):
       - Read qgsd-core/bin/gsd-tools.cjs as string
       - Assert: file does NOT contain /\*\*Goal:\*\*/ (old broken regex)
       - Assert: file DOES contain /\*\*Goal\*\*:/ at least 3 times
       - This is RED now (source still has old pattern)

    2. plan-phase.md Step 4.5 source field (ISSUE-1 part 1):
       - Read qgsd-core/workflows/plan-phase.md as string
       - Assert: contains '.goal // .phase_name' in Step 4.5 block (not just '.phase_name')
       - This is RED now (source still uses .phase_name)

    3. execute-phase.md verify_phase_goal standardization (ISSUE-1 part 2):
       - Read qgsd-core/workflows/execute-phase.md as string
       - Assert: contains '.goal // .phase_name' (not '.goal // empty')
       - Assert: contains "tr ' -/'" (not "tr ' -'" — must have slash)
       - Assert: contains bidirectional match pattern: grep -qF "$KEYWORD" || echo "$KEYWORD" | grep -qF
       - This is RED now (source still has old patterns)

    4. new-milestone.md formal scope scan block (WFI-05):
       - Read qgsd-core/workflows/new-milestone.md as string
       - Assert: contains 'formal/spec' (scan block exists)
       - Assert: contains 'FORMAL_SPEC_CONTEXT' (variable is built)
       - Assert: contains 'invariants.md' (roadmapper injection references invariants)
       - This is RED now (new-milestone.md has no formal scan yet)

    5. qgsd-roadmapper.md formal_context handling:
       - Read qgsd-core/agents/qgsd-roadmapper.md as string
       - Assert: contains 'formal_context' (agent has formal context section)
       - Assert: contains 'invariants' (agent knows to use invariants)
       - This is RED now (roadmapper has no formal context section)

    UNIT TESTS (GREEN immediately — pure functions, no I/O):

    6. Keyword-match algorithm (standardized):
       Inline the standard algorithm as JS:
       function matchKeyword(descLower, moduleLower) {
         const keywords = descLower.split(/[ \-\/]/).filter(k => k.length > 0);
         return keywords.some(kw => moduleLower.includes(kw) || kw.includes(moduleLower));
       }

       Cases:
       - matchKeyword('quorum gate', 'quorum') -> true  (module in keyword)
       - matchKeyword('tui navigation', 'tui-nav') -> false (hyphenated module — split gives 'tui','nav'; 'nav'.includes('tui-nav') false; 'tui-nav'.includes('nav') true -> TRUE)
       - matchKeyword('quorum gate', 'breaker') -> false
       - matchKeyword('tui/navigation design', 'navigation') -> true (slash separator splits 'tui','navigation','design')
       - matchKeyword('', 'quorum') -> false (empty source)
       - matchKeyword('quorum', '') -> false (empty module)

    7. Fail-open: empty FORMAL_SPEC_CONTEXT skips scan:
       - empty array -> matches = 0, no injection (no error)
       - missing formal/spec/ directory -> FORMAL_SPEC_CONTEXT stays [] (represented as: code path produces empty array when dir absent)

    Read paths for source files (NOT installed copies — implementation edits source):
    - qgsd-core/bin/gsd-tools.cjs
    - qgsd-core/workflows/plan-phase.md
    - qgsd-core/workflows/execute-phase.md
    - qgsd-core/workflows/new-milestone.md
    - qgsd-core/agents/qgsd-roadmapper.md

    Use path.resolve(__dirname, '../qgsd-core/...') for portable paths relative to bin/.
  </behavior>
  <implementation>
    Follow exact pattern from bin/execute-phase-formal-gate.test.cjs:
    - require('node:test') with test() blocks
    - require('node:assert/strict') for assertions
    - require('node:fs') for readFileSync
    - require('node:path') for path.resolve

    For structural tests:
      const content = fs.readFileSync(path.resolve(__dirname, '../qgsd-core/...'), 'utf8');
      assert.ok(content.includes('expected-pattern'), 'Pattern not found: expected-pattern');

    For the regex check (gsd-tools):
      assert.ok(!content.includes('**Goal:**'), 'Old broken regex still present');
      const count = (content.match(/\*\*Goal\*\*:/g) || []).length;
      assert.ok(count >= 3, `Expected >=3 occurrences of **Goal**: fix, got ${count}`);

    RED/GREEN commit sequence (single commit):
      Run: node --test bin/roadmapper-formal-integration.test.cjs
      Confirm: structural tests FAIL (files not yet updated), unit tests PASS
      Commit: test(v0.23-03-01): add failing structural tests + green unit tests for roadmapper formal integration
  </implementation>
</feature>

<verification>
Run tests and confirm RED/GREEN split:
```bash
node --test bin/roadmapper-formal-integration.test.cjs 2>&1 | grep -E "^(#|not ok|ok )" | head -30
```

Expected output: structural tests show "not ok" (failing), unit tests show "ok" (passing).
Confirm no syntax errors by verifying the command exits (even with test failures — exit code 1 is expected).
</verification>

<success_criteria>
- bin/roadmapper-formal-integration.test.cjs exists, at least 60 lines
- File runs without syntax error under node --test
- Structural tests (5 groups) fail: gsd-tools regex, plan-phase field, execute-phase standardization, new-milestone scan, roadmapper agent
- Unit tests (2 groups: keyword-match + fail-open) pass GREEN
- Commit message: test(v0.23-03-01): add failing structural tests + green unit tests for roadmapper formal integration
</success_criteria>

<output>
After completion, create `.planning/phases/v0.23-03-roadmapper-formal-integration/v0.23-03-01-SUMMARY.md`
</output>
