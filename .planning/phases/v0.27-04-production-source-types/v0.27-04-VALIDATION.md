---
phase: v0.27-04
slug: production-source-types
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-04
---

# Phase v0.27-04 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.27-04` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.27-04` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.27-04`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | none — uses `node --test` directly |
| **Quick run command** | `node --test bin/observe-handlers-production.test.cjs --test-reporter spec` |
| **Full suite command** | `npm run test:ci` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | npm run test:ci (existing) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/observe-handlers-production.test.cjs --test-reporter spec`
- **After every plan wave:** Run `npm run test:ci`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | OBS-03 | unit | `node --test bin/observe-handlers-production.test.cjs -x` | TBD | ⬜ pending |
| {N}-01-02 | 01 | 1 | OBS-04 | unit | `node --test bin/observe-handlers-production.test.cjs -x` | TBD | ⬜ pending |
| {N}-01-03 | 01 | 1 | OBS-05 | unit | `node --test bin/observe-handlers-production.test.cjs -x` | TBD | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/observe-handlers-production.test.cjs` — test stubs for OBS-03, OBS-04, OBS-05
- [ ] Test fixtures: sample Prometheus, Grafana, Elasticsearch JSON responses

*If none required: "Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed."*

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.27-04`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | {N} | — | — | — | scaffold |
| 1 | {N} | {command} | {N} | {N} | ✅ sampled |

**Phase validation complete:** pending
