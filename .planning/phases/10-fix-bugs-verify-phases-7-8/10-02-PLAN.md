---
phase: 10-fix-bugs-verify-phases-7-8
plan: "02"
type: verify
wave: 2
depends_on:
  - "10-01"
files_modified:
  - .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md
autonomous: true
requirements:
  - ENFC-01
  - ENFC-02
  - ENFC-03
  - CONF-06
  - CONF-07
  - CONF-08
  - CONF-09

must_haves:
  truths:
    - "07-VERIFICATION.md exists and has status: passed or status: failed (never missing)"
    - "ENFC-01 verified: qgsd-circuit-breaker.js emits hookSpecificOutput.permissionDecision:deny when state.active and command is not read-only"
    - "ENFC-02 verified: block reason names the oscillating file set, confirms circuit breaker active, lists allowed ops"
    - "ENFC-03 verified: block reason instructs root cause analysis and explicitly tells user to manually commit"
    - "CONF-06 verified: DEFAULT_CONFIG.circuit_breaker.oscillation_depth=3 present in config-loader.js"
    - "CONF-07 verified: DEFAULT_CONFIG.circuit_breaker.commit_window=6 present in config-loader.js"
    - "CONF-08 verified: validateConfig() handles invalid sub-key values with fallback + stderr warning"
    - "CONF-09 verified: circuit_breaker shallow merge behavior documented in templates/qgsd.json"
  artifacts:
    - path: ".planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md"
      provides: "Independent gsd-verifier VERIFICATION.md for Phase 7"
      contains: "ENFC-01"
  key_links:
    - from: "07-VERIFICATION.md"
      to: "hooks/qgsd-circuit-breaker.js"
      via: "grep verification of permissionDecision:deny"
      pattern: "permissionDecision.*deny"
    - from: "07-VERIFICATION.md"
      to: "hooks/config-loader.js"
      via: "grep verification of circuit_breaker in DEFAULT_CONFIG"
      pattern: "circuit_breaker.*oscillation_depth"
---

<objective>
Spawn the gsd-verifier agent to independently verify that Phase 7 (Enforcement & Config Integration) satisfies requirements ENFC-01, ENFC-02, ENFC-03, CONF-06, CONF-07, CONF-08, and CONF-09.

Purpose: The v0.2 audit found Phase 7 was executed but never independently verified — SUMMARY.md claims delivery but no VERIFICATION.md confirms it. This plan produces the formal verification document that closes the orphaned requirement gap.

Output: .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-enforcement-config-integration/07-01-PLAN.md
@.planning/phases/07-enforcement-config-integration/07-01-SUMMARY.md
@.planning/phases/07-enforcement-config-integration/07-02-PLAN.md
@.planning/phases/07-enforcement-config-integration/07-02-SUMMARY.md
@hooks/config-loader.js
@hooks/qgsd-circuit-breaker.js
@templates/qgsd.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 7 — Enforcement & Config Integration</name>
  <files>.planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md</files>
  <action>
Act as the gsd-verifier agent. Independently verify that the Phase 7 implementation (hooks/config-loader.js, hooks/qgsd-circuit-breaker.js, and templates/qgsd.json) satisfies every requirement assigned to Phase 7.

**Requirements to verify:**

CONF-06: qgsd.json schema extended with circuit_breaker.oscillation_depth (integer, default: 3)
CONF-07: qgsd.json schema extended with circuit_breaker.commit_window (integer, default: 6)
CONF-08: Circuit breaker config values validated on load; invalid values fall back to defaults with stderr warning
CONF-09: Two-layer config merge (global + project) applies to circuit_breaker settings identically to existing merge behavior — document the shallow merge limitation in templates/qgsd.json (added by Plan 10-01)
ENFC-01: When circuit breaker is active, hook returns hookSpecificOutput.permissionDecision:'deny' blocking Bash execution
ENFC-02: Block reason names the oscillating file set, confirms circuit breaker is active, and lists allowed operations (read-only Bash)
ENFC-03: Block reason instructs Claude to perform root cause analysis and map dependencies before resuming; explicitly instructs the user to manually commit the fix

**Verification methodology for each requirement:**

For CONF-06 and CONF-07:
- Read hooks/config-loader.js: confirm DEFAULT_CONFIG contains circuit_breaker: { oscillation_depth: 3, commit_window: 6 }
- Run: node -e "const {DEFAULT_CONFIG} = require('./hooks/config-loader'); console.log(JSON.stringify(DEFAULT_CONFIG.circuit_breaker))"
- Expected: {"oscillation_depth":3,"commit_window":6}

For CONF-08:
- Read hooks/config-loader.js validateConfig() function: confirm it contains a circuit_breaker branch that validates both sub-keys independently
- Run: node -e "
const {loadConfig} = require('./hooks/config-loader');
const os = require('os'); const path = require('path'); const fs = require('fs');
const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'qgsd-test-'));
fs.mkdirSync(path.join(tmp, '.claude'));
fs.writeFileSync(path.join(tmp, '.claude', 'qgsd.json'), JSON.stringify({circuit_breaker: {oscillation_depth: 'bad', commit_window: -1}}));
let stderr = '';
const orig = process.stderr.write.bind(process.stderr);
process.stderr.write = (s) => { stderr += s; return true; };
const cfg = loadConfig(tmp);
process.stderr.write = orig;
fs.rmSync(tmp, {recursive:true});
console.log('oscillation_depth fallback:', cfg.circuit_breaker.oscillation_depth === 3);
console.log('commit_window fallback:', cfg.circuit_breaker.commit_window === 6);
console.log('stderr has WARNING:', stderr.includes('WARNING'));
"
- Expected: all three true

For CONF-09:
- Read templates/qgsd.json _comment: confirm it documents that circuit_breaker uses shallow merge (added by Plan 10-01)
- Run: node -e "const t = JSON.parse(require('fs').readFileSync('templates/qgsd.json', 'utf8')); console.log(t._comment.some(l => l.includes('circuit_breaker') && l.includes('shallow')))"
- Expected: true

For ENFC-01:
- Read hooks/qgsd-circuit-breaker.js: confirm the active-state branch outputs hookSpecificOutput with permissionDecision: 'deny' for non-read-only commands
- Run: node -e "
const {execSync, spawnSync} = require('child_process');
const os = require('os'); const path = require('path'); const fs = require('fs');
const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'qgsd-cb-'));
spawnSync('git', ['init'], {cwd: tmp});
spawnSync('git', ['commit', '--allow-empty', '-m', 'init'], {cwd: tmp, env: {...process.env, GIT_AUTHOR_NAME:'t', GIT_AUTHOR_EMAIL:'t@t', GIT_COMMITTER_NAME:'t', GIT_COMMITTER_EMAIL:'t@t'}});
fs.mkdirSync(path.join(tmp, '.claude'));
fs.writeFileSync(path.join(tmp, '.claude', 'circuit-breaker-state.json'), JSON.stringify({active:true, file_set:['src/foo.js'], activated_at: new Date().toISOString(), commit_window_snapshot:[]}));
const payload = JSON.stringify({tool_name:'Bash', tool_input:{command:'echo test'}, cwd: tmp});
const result = spawnSync('node', ['hooks/qgsd-circuit-breaker.js'], {input: payload, encoding: 'utf8'});
const output = JSON.parse(result.stdout);
console.log('has deny:', output.hookSpecificOutput.permissionDecision === 'deny');
fs.rmSync(tmp, {recursive:true});
"
- Expected: has deny: true

For ENFC-02:
- Same test as ENFC-01 but check the permissionDecisionReason content:
- Run: (extend the ENFC-01 test) check that reason includes file set names, 'CIRCUIT BREAKER', read-only ops list
- Expected: permissionDecisionReason includes 'CIRCUIT BREAKER', 'src/foo.js', 'git log'

For ENFC-03:
- Same test but check reason includes root cause instruction and reset command:
- Expected: permissionDecisionReason includes 'root cause', 'npx qgsd --reset-breaker', 'manually'

**Run the test suite:**
- Run: npm test
- Expected: 138 tests pass (all existing tests from Phases 1-7, with CB-TC7 updated + CB-TC16-19 from Phase 7)

**Write the VERIFICATION.md:**

Create .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md with this structure:

```
---
phase: 07-enforcement-config-integration
verified: {today's date}
verifier: gsd-verifier (spawned from Phase 10 Plan 02)
status: passed|failed
requirements_verified: [ENFC-01, ENFC-02, ENFC-03, CONF-06, CONF-07, CONF-08, CONF-09]
---

# Phase 7 Verification Report

## Requirements

| Req ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| CONF-06 | ... | PASS/FAIL | ... |
| CONF-07 | ... | PASS/FAIL | ... |
| CONF-08 | ... | PASS/FAIL | ... |
| CONF-09 | ... | PASS/FAIL | ... |
| ENFC-01 | ... | PASS/FAIL | ... |
| ENFC-02 | ... | PASS/FAIL | ... |
| ENFC-03 | ... | PASS/FAIL | ... |

## Test Suite

| Suite | Tests | Status |
|-------|-------|--------|
| npm test | 138/138 | PASS |

## Key Evidence

[grep outputs, node -e outputs confirming each requirement]

## Verdict

PASSED (N/7 requirements verified) or FAILED with details.
```

Set status: passed only if ALL 7 requirements pass. Set status: failed with specific failures listed if any requirement does not pass.
  </action>
  <verify>
Run: ls .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md
Expected: File exists.

Run: grep "status:" .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md | head -1
Expected: status: passed (or status: failed with detail — either is acceptable, the file MUST exist).

Run: grep "ENFC-01\|ENFC-02\|ENFC-03\|CONF-06\|CONF-07\|CONF-08\|CONF-09" .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md | wc -l
Expected: 7 or more lines (each requirement appears at least once).
  </verify>
  <done>07-VERIFICATION.md exists in the Phase 10 output directory. It contains verification results for all 7 Phase 7 requirements (ENFC-01..03, CONF-06..09) with a top-level status field (passed or failed), a per-requirement table, and actual command output as evidence.</done>
</task>

</tasks>

<verification>
Run: ls -la .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md
Expected: File exists with nonzero size.

Run: grep -c "PASS\|FAIL" .planning/phases/10-fix-bugs-verify-phases-7-8/07-VERIFICATION.md
Expected: 7 or more (each requirement has a PASS or FAIL verdict).
</verification>

<success_criteria>
- 07-VERIFICATION.md exists in .planning/phases/10-fix-bugs-verify-phases-7-8/
- All 7 Phase 7 requirements are explicitly verified (ENFC-01, ENFC-02, ENFC-03, CONF-06, CONF-07, CONF-08, CONF-09)
- Each requirement has a PASS or FAIL verdict with actual evidence (command output, grep result, or test count)
- Top-level status field reflects overall result
- npm test evidence included (138 tests expected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-bugs-verify-phases-7-8/10-02-SUMMARY.md`
</output>
