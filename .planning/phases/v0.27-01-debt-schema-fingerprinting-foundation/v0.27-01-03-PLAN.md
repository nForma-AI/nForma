---
phase: v0.27-01
plan: 03
type: tdd
wave: 2
depends_on:
  - v0.27-01-01
files_modified:
  - bin/debt-ledger.cjs
  - bin/debt-ledger.test.cjs
  - bin/debt-retention.cjs
  - bin/debt-retention.test.cjs
  - bin/qgsd-debt.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - DEBT-04

must_haves:
  truths:
    - "Resolved entries older than max_age are archived to .formal/debt-archive.jsonl when retention runs"
    - ".formal/debt.json exists with validated schema and ajv validation passes"
  artifacts:
    - path: "bin/debt-ledger.cjs"
      provides: "Atomic read/write operations for debt ledger"
      exports: ["readDebtLedger", "writeDebtLedger"]
    - path: "bin/debt-ledger.test.cjs"
      provides: "Unit tests for ledger I/O including fail-open and atomicity"
      min_lines: 50
    - path: "bin/debt-retention.cjs"
      provides: "Retention policy: archive resolved entries older than max_age"
      exports: ["applyRetentionPolicy", "writeArchive"]
    - path: "bin/debt-retention.test.cjs"
      provides: "Unit tests for retention cutoff logic and JSONL archival"
      min_lines: 60
    - path: "bin/qgsd-debt.test.cjs"
      provides: "Integration test: create entry, validate, transition, archive"
      min_lines: 40
  key_links:
    - from: "bin/debt-ledger.cjs"
      to: ".formal/debt.json"
      via: "read/write with atomic temp+rename"
      pattern: "writeFileSync.*\\.tmp"
    - from: "bin/debt-retention.cjs"
      to: "bin/debt-ledger.cjs"
      via: "reads ledger, splits active/archived"
      pattern: "applyRetentionPolicy"
    - from: "bin/debt-retention.cjs"
      to: ".formal/debt-archive.jsonl"
      via: "appends archived entries as JSONL"
      pattern: "appendFileSync"
    - from: "bin/qgsd-debt.test.cjs"
      to: "bin/validate-debt-entry.cjs"
      via: "validates entries from Plan 01"
      pattern: "validateDebtEntry"
---

<objective>
Implement ledger I/O (atomic read/write), retention policy (archive resolved entries older than max_age to JSONL), and an integration test proving the complete debt lifecycle works end-to-end.

Purpose: The ledger I/O layer provides fail-open persistence for all downstream phases. The retention policy prevents unbounded growth of debt.json — a critical pitfall identified in research. The integration test validates that schema, state machine, fingerprinting, ledger, and retention all compose correctly.

Output: Ledger I/O module (bin/debt-ledger.cjs), retention module (bin/debt-retention.cjs), integration test (bin/qgsd-debt.test.cjs), and an empty seed .formal/debt.json.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.27-01-debt-schema-fingerprinting-foundation/v0.27-01-RESEARCH.md
@.planning/phases/v0.27-01-debt-schema-fingerprinting-foundation/v0.27-01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ledger I/O and retention policy (TDD)</name>
  <files>
    bin/debt-ledger.cjs
    bin/debt-ledger.test.cjs
    bin/debt-retention.cjs
    bin/debt-retention.test.cjs
  </files>
  <action>
RED: Create bin/debt-ledger.test.cjs with tests FIRST:
- Reading nonexistent file returns empty ledger with schema_version "1" and empty debt_entries (fail-open)
- Reading corrupt JSON file returns empty ledger (fail-open, no crash, logs error)
- Writing creates parent directory if needed (recursive mkdir)
- Write is atomic: uses temp file (.tmp) + rename (not direct write)
- Reading back a written ledger returns identical data (round-trip)
- Schema version field is preserved across read/write cycles
- last_updated is set on write
- Empty debt_entries array writes correctly

Create bin/debt-retention.test.cjs with tests FIRST:
- Entry with status=open is NEVER archived regardless of age (test with 365-day-old open entry)
- Entry with status=acknowledged is NEVER archived regardless of age
- Entry with status=resolving is NEVER archived regardless of age
- Entry with status=resolved AND resolved_at older than cutoff IS archived
- Entry with status=resolved AND resolved_at newer than cutoff is kept active
- Default max_age is 90 days
- Custom max_age parameter is honored (test with 30 days)
- Archived entries are written in JSONL format (one JSON object per line, no outer array)
- JSONL append does not overwrite existing archive content
- Entry without resolved_at falls back to last_seen for cutoff calculation (per research)
- Empty ledger returns empty active and empty archived arrays

Run all tests — they MUST fail.

GREEN: Create bin/debt-ledger.cjs:
- readDebtLedger(ledgerPath): reads .formal/debt.json, returns parsed JSON. If file missing or corrupt, return empty ledger structure { schema_version: "1", created_at: now, last_updated: now, debt_entries: [] }. Log errors to stderr (fail-open).
- writeDebtLedger(ledgerPath, ledger): writes ledger to path atomically (write to .tmp, then rename). Creates parent dir with recursive mkdir if needed. Sets last_updated to now. Uses JSON.stringify with 2-space indent.
- CommonJS module.exports: { readDebtLedger, writeDebtLedger }

Create bin/debt-retention.cjs:
- applyRetentionPolicy(ledger, maxAgeDays = 90): iterates debt_entries, splits into active (keep) and archived (remove) arrays. Only archives entries where status === "resolved" AND (resolved_at || last_seen) is older than cutoff. Returns { active, archived }.
- writeArchive(archivedEntries, archivePath): appends entries to .formal/debt-archive.jsonl as one JSON object per line. Uses fs.appendFileSync. Creates parent dir if needed.
- CommonJS module.exports: { applyRetentionPolicy, writeArchive }

Seed file: Create .formal/debt.json with empty ledger structure (schema_version "1", empty debt_entries).

Run all tests — they MUST pass.
  </action>
  <verify>node --test bin/debt-ledger.test.cjs && node --test bin/debt-retention.test.cjs</verify>
  <done>
- Ledger I/O is fail-open (corrupt/missing files do not crash)
- Writes are atomic (temp + rename prevents corruption on crash)
- Retention correctly archives only resolved entries older than max_age
- Non-resolved entries are never archived regardless of age
- Archive file uses JSONL format (one entry per line)
- .formal/debt.json exists with valid empty ledger structure
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test — complete debt lifecycle</name>
  <files>
    bin/qgsd-debt.test.cjs
  </files>
  <action>
Create bin/qgsd-debt.test.cjs as an integration test proving the complete debt lifecycle:

1. Create a temporary directory for test isolation (use fs.mkdtempSync)
2. Create a new debt entry using fingerprintIssue (from Plan 02) to generate fingerprint
3. Validate the entry with validateDebtEntry (from Plan 01) — assert valid
4. Write to ledger using writeDebtLedger — assert file created
5. Read back using readDebtLedger — assert round-trip identity
6. Transition status: open -> acknowledged using transitionDebtEntry — assert success
7. Transition status: acknowledged -> resolving — assert success
8. Transition status: resolving -> resolved — assert success, resolved_at added
9. Attempt invalid transition: resolved -> open — assert failure with error message
10. Set resolved_at to 120 days ago, apply retention policy with 90-day max_age — assert entry moved to archived
11. Write archive using writeArchive — assert .jsonl file created with correct format
12. Verify active ledger no longer contains the archived entry
13. Clean up temp directory

Import all modules:
- bin/validate-debt-entry.cjs (validateDebtEntry)
- bin/debt-state-machine.cjs (canTransition, transitionDebtEntry)
- bin/fingerprint-issue.cjs (fingerprintIssue)
- bin/fingerprint-drift.cjs (fingerprintDrift)
- bin/debt-ledger.cjs (readDebtLedger, writeDebtLedger)
- bin/debt-retention.cjs (applyRetentionPolicy, writeArchive)

Also add a drift lifecycle test:
1. Create drift entry using fingerprintDrift with formal_parameter_key
2. Validate with validateDebtEntry
3. Transition through full lifecycle
4. Verify fingerprint stability (same key produces same fingerprint)

Use node:test framework with describe/it blocks. Use assert.strictEqual and assert.deepStrictEqual.
  </action>
  <verify>node --test bin/qgsd-debt.test.cjs</verify>
  <done>
- Integration test proves complete lifecycle: create -> validate -> write -> read -> transition -> archive
- Both issue and drift fingerprinting work within the lifecycle
- Invalid transitions are rejected at integration level
- Retention correctly archives old resolved entries
- All modules compose without type mismatches or missing fields
  </done>
</task>

</tasks>

<verification>
- `node --test bin/debt-ledger.test.cjs` — all tests pass
- `node --test bin/debt-retention.test.cjs` — all tests pass
- `node --test bin/qgsd-debt.test.cjs` — integration test passes
- `.formal/debt.json` exists with valid empty ledger structure
- `npm test` still passes (no regressions)
</verification>

<success_criteria>
- Ledger I/O provides fail-open atomic persistence
- Retention policy archives resolved entries older than 90 days (configurable) to JSONL
- Integration test validates the complete debt lifecycle end-to-end
- All Phase v0.27-01 modules compose correctly: schema + validation + state machine + fingerprinting + ledger + retention
</success_criteria>

<output>
After completion, create `.planning/phases/v0.27-01-debt-schema-fingerprinting-foundation/v0.27-01-03-SUMMARY.md`
</output>
