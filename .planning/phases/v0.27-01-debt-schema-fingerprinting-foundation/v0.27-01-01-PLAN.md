---
phase: v0.27-01
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .formal/debt.schema.json
  - bin/validate-debt-entry.cjs
  - bin/validate-debt-entry.test.cjs
  - bin/debt-state-machine.cjs
  - bin/debt-state-machine.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - DEBT-01
  - DEBT-03

must_haves:
  truths:
    - ".formal/debt.json schema is defined with validated fields (id, fingerprint, title, occurrences, first_seen, last_seen, environments, status, formal_ref, source_entries) and ajv-free validation passes"
    - "Debt status transitions enforce the state machine (open -> acknowledged -> resolving -> resolved) and reject invalid transitions (e.g., resolved -> open)"
  artifacts:
    - path: ".formal/debt.schema.json"
      provides: "JSON Schema draft-07 defining debt entry and ledger structure"
      contains: "draft-07"
    - path: "bin/validate-debt-entry.cjs"
      provides: "Runtime validation for debt entries and ledger"
      exports: ["validateDebtEntry", "validateDebtLedger"]
    - path: "bin/validate-debt-entry.test.cjs"
      provides: "Unit tests for schema validation"
      min_lines: 80
    - path: "bin/debt-state-machine.cjs"
      provides: "State transition guard and transition function"
      exports: ["canTransition", "transitionDebtEntry", "ALLOWED_TRANSITIONS"]
    - path: "bin/debt-state-machine.test.cjs"
      provides: "Unit tests for state machine transitions"
      min_lines: 60
  key_links:
    - from: "bin/validate-debt-entry.cjs"
      to: ".formal/debt.schema.json"
      via: "validation logic mirrors schema constraints"
      pattern: "status.*enum.*open.*acknowledged.*resolving.*resolved"
    - from: "bin/debt-state-machine.cjs"
      to: "bin/validate-debt-entry.cjs"
      via: "state machine operates on validated debt entries"
      pattern: "ALLOWED_TRANSITIONS"
---

<objective>
Create the debt entry JSON Schema definition, runtime validation functions, and state machine enforcement for debt status transitions.

Purpose: Establishes the data contract that all downstream phases (observe, dedup, solve) depend on. Without a validated schema and enforced state machine, the debt ledger would accept garbage data and allow invalid status transitions.

Output: Schema file (.formal/debt.schema.json), validation module (bin/validate-debt-entry.cjs), state machine module (bin/debt-state-machine.cjs), and comprehensive test suites for both.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.27-01-debt-schema-fingerprinting-foundation/v0.27-01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debt schema definition and runtime validation (TDD)</name>
  <files>
    .formal/debt.schema.json
    bin/validate-debt-entry.cjs
    bin/validate-debt-entry.test.cjs
  </files>
  <action>
RED: Create bin/validate-debt-entry.test.cjs with comprehensive tests FIRST:
- Valid entry passes validation (all required fields present, correct types)
- Missing required fields rejected (test each: id, fingerprint, title, occurrences, first_seen, last_seen, environments, status, source_entries)
- Invalid status enum rejected (e.g., "invalid", "closed")
- Invalid timestamp format rejected (non-ISO8601 strings)
- Empty environments array rejected
- Empty source_entries array rejected
- last_seen < first_seen rejected
- Valid formal_ref (string or null) accepted
- Invalid environments values rejected (e.g., "prod" instead of "production")
- Ledger validation: schema_version must be "1", debt_entries must be array, chains to entry validation
- Additional properties on entry rejected (additionalProperties: false behavior)

Run tests — they MUST fail (no implementation yet).

GREEN: Create .formal/debt.schema.json with JSON Schema draft-07:
- Required fields: id, fingerprint, title, occurrences, first_seen, last_seen, environments, status, source_entries
- Optional fields: formal_ref (type: ["string", "null"])
- id pattern: ^[a-z0-9]{8}-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{12}$
- fingerprint pattern: ^[a-z0-9]{16,64}$
- status enum: ["open", "acknowledged", "resolving", "resolved"]
- environments items enum: ["production", "staging", "development", "test", "local"]
- source_entries items require: source_type, source_id, observed_at
- source_type enum: ["github", "sentry", "sentry-feedback", "prometheus", "grafana", "logstash", "bash"]
- additionalProperties: false on both entry and source_entry

Create bin/validate-debt-entry.cjs implementing validateDebtEntry(entry) and validateDebtLedger(ledger):
- validateDebtEntry returns true on valid, or array of error strings
- Check all required fields, types, patterns, enums per schema
- Enforce last_seen >= first_seen
- Validate each source_entry has required fields
- validateDebtLedger checks schema_version="1", debt_entries is array, validates each entry
- Use CommonJS module.exports pattern (project standard)
- Use isValidISO8601() helper (regex for ISO8601 date-time)

Run tests — they MUST pass.

REFACTOR: Clean up any duplication between schema constants and validation code. Ensure enum values are defined once and referenced.
  </action>
  <verify>node --test bin/validate-debt-entry.test.cjs</verify>
  <done>
- .formal/debt.schema.json exists with draft-07 schema covering all DEBT-01 fields
- validateDebtEntry accepts valid entries, rejects invalid ones with specific error messages
- validateDebtLedger validates the container structure and chains to entry validation
- All unit tests pass (10+ test cases covering valid, missing fields, invalid types, edge cases)
  </done>
</task>

<task type="auto">
  <name>Task 2: Debt state machine enforcement (TDD)</name>
  <files>
    bin/debt-state-machine.cjs
    bin/debt-state-machine.test.cjs
  </files>
  <action>
RED: Create bin/debt-state-machine.test.cjs with tests FIRST:
- Valid forward transitions: open->acknowledged, acknowledged->resolving, resolving->resolved (all return true/success)
- Acknowledged->open allowed (revert for clarification per research)
- Invalid backward transitions: resolved->open, resolved->acknowledged, resolved->resolving (all rejected)
- Invalid skip transitions: open->resolving, open->resolved, acknowledged->resolved (all rejected)
- No-op transitions: open->open, acknowledged->acknowledged, resolving->resolving, resolved->resolved (all rejected)
- Terminal state: resolved has no outbound transitions (test all 3 targets)
- transitionDebtEntry returns { success: true, entry: {...} } on valid transition
- transitionDebtEntry returns { success: false, error: string } on invalid transition
- Updated entry has new status field
- resolved_at timestamp added when transitioning to resolved
- Invalid target status (e.g., "invalid") rejected
- Test all 16 state pairs (4x4 matrix) for complete coverage

Run tests — they MUST fail.

GREEN: Create bin/debt-state-machine.cjs:
- ALLOWED_TRANSITIONS map: open->["acknowledged"], acknowledged->["resolving","open"], resolving->["resolved"], resolved->[]
- canTransition(fromStatus, toStatus) — returns boolean, rejects same-status no-ops
- transitionDebtEntry(entry, newStatus) — validates transition, returns success/error object, adds resolved_at on terminal transition
- CommonJS module.exports: { canTransition, transitionDebtEntry, ALLOWED_TRANSITIONS }

Run tests — they MUST pass.
  </action>
  <verify>node --test bin/debt-state-machine.test.cjs</verify>
  <done>
- canTransition correctly allows only valid forward transitions (plus acknowledged->open revert)
- transitionDebtEntry returns structured success/error results
- resolved is terminal state with no outbound transitions
- All 16 state pairs tested, invalid ones rejected
- resolved_at timestamp added on transition to resolved
  </done>
</task>

</tasks>

<verification>
- `node --test bin/validate-debt-entry.test.cjs` — all tests pass
- `node --test bin/debt-state-machine.test.cjs` — all tests pass
- `.formal/debt.schema.json` exists and is valid JSON
- Both modules export documented functions
</verification>

<success_criteria>
- Debt entry schema is fully defined with all DEBT-01 fields
- Runtime validation catches invalid entries before persistence
- State machine enforces DEBT-03 transition rules with no loopholes
- Test coverage includes all valid transitions, all invalid transitions, edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/v0.27-01-debt-schema-fingerprinting-foundation/v0.27-01-01-SUMMARY.md`
</output>
