---
phase: v0.21-06-operational-signals
plan: "04"
subsystem: formal-verification
tags: [poisson-binomial, consensus-probability, quorum-gate, availability]

requires:
  - phase: v0.21-06-03
    provides: PRISM infrastructure and scoreboard reading
provides:
  - quorum-consensus-gate.cjs with checkConsensusGate, computeConsensusProbability, poissonBinomialCDF exports
  - Closed-form consensus probability computation (no Java/PRISM dependency)
  - Pre-quorum gating with configurable threshold
affects: [quorum.md, execute-phase.md]

tech-stack:
  added: []
  patterns: [Poisson binomial distribution via recursive DP, fail-open prior rates pattern]

key-files:
  created:
    - bin/quorum-consensus-gate.cjs
    - bin/quorum-consensus-gate.test.cjs
  modified: []

key-decisions:
  - "Closed-form Poisson binomial instead of live PRISM invocation -- sub-millisecond execution"
  - "Conservative prior rates (0.85 per slot for 4 slots) when scoreboard is missing"
  - "Default threshold 0.70 configurable via workflow.consensus_probability_threshold in config.json"

patterns-established:
  - "Poisson binomial DP: dp[j] = dp[j]*(1-p_i) + dp[j-1]*p_i, backwards sweep"
  - "Prior rates fallback: 0.85 per slot when scoreboard is empty/missing"

requirements-completed: [SIG-04]

duration: 3min
completed: 2026-03-01
---

# Plan 04: Poisson Binomial Consensus Probability Gate Summary

**quorum-consensus-gate.cjs computes P(consensus_reached) using closed-form Poisson binomial DP, deferring quorum rounds when probability < configurable threshold**

## Performance

- **Duration:** 3 min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Poisson binomial distribution computation via O(n^2) recursive DP (sub-millisecond execution)
- Consensus probability gate returning proceed/defer with structured message
- Configurable threshold from config.json (default 0.70)
- Conservative prior rates (0.85) when scoreboard data is missing

## Task Commits

1. **Task 1+2: quorum-consensus-gate.cjs + tests** - `7bccdd9a` (feat)

## Files Created/Modified
- `bin/quorum-consensus-gate.cjs` - Consensus probability gate with poissonBinomialCDF, computeConsensusProbability, checkConsensusGate
- `bin/quorum-consensus-gate.test.cjs` - 10 unit tests covering all branches including performance (<10ms assertion)

## Decisions Made
- Closed-form DP instead of PRISM invocation for sub-millisecond pre-quorum gating
- Conservative priors (0.85) when no scoreboard data -- proceed by default, fail-open
- Threshold default 0.70 -- high enough to prevent weak quorums, low enough to not block healthy systems

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## Next Phase Readiness
- Consensus probability gate ready for integration into quorum dispatch workflow
- All 4 phase plans complete

---
*Phase: v0.21-06-operational-signals*
*Completed: 2026-03-01*
