---
phase: v0.19-11
slug: unif-03-summary-fix-checkbox-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-28
---

# Phase v0.19-11 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.19-11`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.19-11`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | none — uses `node --test` directly |
| **Quick run command** | `node --test bin/run-formal-verify.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds |
| **CI pipeline** | `.github/workflows/formal-verify.yml` — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/run-formal-verify.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.19-11-01-W0 | 01 | 0 | UNIF-03 | unit (RED scaffold) | `node --test bin/run-formal-verify.test.cjs` | ✅ existing | ⬜ pending |
| v0.19-11-01-01 | 01 | 1 | UNIF-03 | unit (GREEN) | `node --test bin/run-formal-verify.test.cjs` | ✅ existing | ⬜ pending |
| v0.19-11-02-01 | 02 | 1 | UNIF-03 | smoke (grep) | `grep -c "^\- \[ \]" .planning/REQUIREMENTS.md` (expect 0) | ✅ existing | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/run-formal-verify.test.cjs` — Add UNIF-03 RED test asserting `ci:trace-redaction` and `ci:trace-schema-drift` appear in `--only=ci` output. Test must FAIL before STEPS entries are added.

**Test to add (RED — fails until fix):**
```javascript
test('UNIF-03: --only=ci filter resolves ci:trace-redaction and ci:trace-schema-drift steps', () => {
  const result = spawnSync(process.execPath, [RUN_FV, '--only=ci'], {
    encoding: 'utf8',
    timeout: 30000,
  });
  assert.doesNotMatch(result.stderr || '', /Unknown --only value/i);
  const output = (result.stdout || '') + (result.stderr || '');
  assert.ok(output.includes('ci:trace-redaction'), 'ci:trace-redaction must be in STEPS — UNIF-03');
  assert.ok(output.includes('ci:trace-schema-drift'), 'ci:trace-schema-drift must be in STEPS — UNIF-03');
});
```

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.

*All phase behaviors have automated verification coverage.*

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 30s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.19-11`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | `node --test bin/run-formal-verify.test.cjs` | — | — | scaffold |
| 1 | 2 | `node --test bin/run-formal-verify.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
