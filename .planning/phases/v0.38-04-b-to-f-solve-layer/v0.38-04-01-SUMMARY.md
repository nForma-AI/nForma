# Plan v0.38-04-01 Summary

**Phase:** v0.38-04 — B-to-F Solve Layer
**Plan:** 01 — Layer Registration & Diagnostic Sweep
**Status:** Complete
**Duration:** ~5 min

## What Was Built

Registered `b_to_f` as the 20th solve layer and implemented the `sweepBtoF()` diagnostic sweep function that classifies failing tests against formal model coverage into three buckets:
- `covered_reproduced` (0 residual) — model exists and reproduces the bug
- `covered_not_reproduced` (+1 residual) — model exists but doesn't reproduce
- `not_covered` (+1 residual) — no formal model covers the test's requirements

## Key Files

### Created
- `test/b-to-f-sweep.test.cjs` — 14 unit tests for layer registration, wave DAG, and classification logic

### Modified
- `bin/layer-constants.cjs` — Added `b_to_f` as 20th entry in `LAYER_KEYS` array
- `bin/solve-wave-dag.cjs` — Added `b_to_f: ['t_to_c']` to `LAYER_DEPS`
- `bin/nf-solve.cjs` — Added `sweepBtoF()`, `classifyFailingTest()`, wired into `computeResidual()`, added B->F row to result table rendering

## Self-Check: PASSED

- [x] `LAYER_KEYS.length === 20` and `LAYER_KEYS[19] === 'b_to_f'`
- [x] `LAYER_DEPS.b_to_f` equals `['t_to_c']`
- [x] `computeWaves` places b_to_f after t_to_c in execution order
- [x] `sweepBtoF` exported from nf-solve.cjs
- [x] `classifyFailingTest` returns correct classification for all three buckets
- [x] B->F row renders in solver output
- [x] All 14 tests pass

## Decisions Made
- Used `test_requirement_map` from traceability matrix for test-to-requirement mapping (t_to_c detail doesn't expose individual test file paths)
- Classification happens at the test→requirement→model level, not at per-failure level
- Sorting: not_covered first, then covered_not_reproduced, then bug_id tiebreaker for deterministic output
- Wave compaction: b_to_f and t_to_c merge into a sequential wave when they're the only active layers (expected behavior from existing compaction algorithm)
