---
phase: v0.38-04
status: passed
verified: 2026-03-18
must_haves_score: 4/4
formal_check: null
---

# Phase v0.38-04 Verification: B-to-F Solve Layer

## Goal
The solve pipeline autonomously identifies bugs that formal models should explain but don't, and routes them to model creation or refinement.

## Must-Haves Verification

### 1. b_to_f exists as the 20th layer key in layer-constants.cjs
**Status:** PASSED
- `LAYER_KEYS.length === 20`
- `LAYER_KEYS[19] === 'b_to_f'`
- Verified via: `node --test test/b-to-f-sweep.test.cjs` (3 tests)

### 2. solve-wave-dag.cjs includes b_to_f with dependency on t_to_c and correct wave position
**Status:** PASSED
- `LAYER_DEPS.b_to_f` equals `['t_to_c']`
- `computeWaves` places b_to_f after t_to_c in flattened execution order
- Verified via: `node --test test/b-to-f-sweep.test.cjs` (3 tests)

### 3. nf-solve.cjs diagnostic sweep computes b_to_f residual by classifying failing tests
**Status:** PASSED
- `classifyFailingTest` returns `not_covered` when test has no requirement mapping or no formal models
- `classifyFailingTest` returns `covered_not_reproduced` when models exist but no reproduction record
- `classifyFailingTest` returns `covered_reproduced` when bug-model-gaps has reproduced status
- Residual = not_covered_count + covered_not_reproduced_count (covered_reproduced contributes 0)
- Verified via: `node --test test/b-to-f-sweep.test.cjs` (8 tests)

### 4. solve-remediate.md dispatches close-formal-gaps for not_covered gaps and model refinement for blind spots
**Status:** PASSED
- Section 3a-extra exists in solve-remediate.md
- Phase 1 dispatches `/nf:close-formal-gaps` for not_covered (max 3/cycle)
- Phase 2 dispatches `/nf:model-driven-fix` for blind spots (max 2/cycle)
- Verified via: `node --test test/b-to-f-remediate.test.cjs` (7 tests)

## Requirement Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| BTF-01 | Complete | `LAYER_KEYS[19] === 'b_to_f'`, array length 20 |
| BTF-02 | Complete | `LAYER_DEPS.b_to_f === ['t_to_c']`, correct wave position |
| BTF-03 | Complete | `sweepBtoF()` with three-bucket classification, wired into computeResidual |
| BTF-04 | Complete | Section 3a-extra with dispatch caps (3 not_covered, 2 blind spots) |

## Test Results

| Test File | Tests | Pass | Fail |
|-----------|-------|------|------|
| test/b-to-f-sweep.test.cjs | 14 | 14 | 0 |
| test/b-to-f-remediate.test.cjs | 7 | 7 | 0 |
| **Total** | **21** | **21** | **0** |

## Formal Check
No formal modules matched this phase. Formal verification skipped.
