---
plan: "03-01"
phase: "03-installer-distribution"
title: "Package Identity + Changelog"
requirements: ["INST-01", "INST-02", "SYNC-01", "SYNC-02", "SYNC-03", "SYNC-04"]
wave: 1
---

# Plan 03-01: Package Identity + Changelog

## Objective

Update `package.json` to publish QGSD as a standalone `qgsd` npm package and add a QGSD v0.1.0 entry to the existing `CHANGELOG.md` documenting GSD version compatibility and the SYNC-04 audit result.

## Pre-conditions

- Phase 2 complete (hooks/config-loader.js, qgsd-stop.js, qgsd-prompt.js all written and tested)
- `package.json` currently named `get-shit-done-cc` v1.20.5
- `CHANGELOG.md` exists at repo root (Keep-a-Changelog format)

## Tasks

### Task 1: Update package.json

Edit `/Users/jonathanborduas/code/QGSD/package.json` with the following changes:

1. Change `"name"` from `"get-shit-done-cc"` to `"qgsd"`
2. Change `"version"` from `"1.20.5"` to `"0.1.0"`
3. Add `"qgsd"` to the `"bin"` object: `"qgsd": "bin/install.js"` (keep existing `"get-shit-done-cc"` entry)
4. Add `"templates"` to the `"files"` array
5. Add `"peerDependencies"` object: `{ "get-shit-done-cc": ">=1.20.0" }`

Final `package.json` diff:
```json
{
  "name": "qgsd",
  "version": "0.1.0",
  "description": "Multi-model quorum enforcement for GSD planning commands via Claude Code hooks.",
  "bin": {
    "qgsd": "bin/install.js",
    "get-shit-done-cc": "bin/install.js"
  },
  "files": [
    "bin",
    "commands",
    "get-shit-done",
    "agents",
    "hooks/dist",
    "scripts",
    "templates"
  ],
  "peerDependencies": {
    "get-shit-done-cc": ">=1.20.0"
  },
  ...
}
```

Note: Keep all other fields (description, keywords, author, license, repository, engines, dependencies, devDependencies, scripts) unchanged.

### Task 2: Add QGSD v0.1.0 entry to CHANGELOG.md

Edit `/Users/jonathanborduas/code/QGSD/CHANGELOG.md`.

Insert a new `## [0.1.0] - 2026-02-20` section after `## [Unreleased]` and before the first existing GSD version entry. The section must include:

1. A note that this is the initial QGSD release
2. GSD compatibility: `get-shit-done-cc >= 1.20.0`
3. SYNC-04 audit result
4. Files added by QGSD (what installs into ~/.claude/)
5. A maintenance note about quorum_commands duplication (SYNC-02 process note)

Section content:
```markdown
## [0.1.0] - 2026-02-20

### QGSD — Initial Release

QGSD adds multi-model quorum enforcement to GSD via Claude Code hooks. It installs
alongside GSD without modifying any GSD source files.

**GSD compatibility:** `get-shit-done-cc >= 1.20.0`

**Files installed into `~/.claude/` by QGSD:**
- `hooks/qgsd-stop.js` — Stop hook: reads transcript JSONL, blocks if quorum evidence missing
- `hooks/qgsd-prompt.js` — UserPromptSubmit hook: injects quorum instructions before planning commands
- `hooks/config-loader.js` — Shared config loader: two-layer merge (global + per-project qgsd.json)
- `qgsd.json` — Quorum config with MCP-auto-detected tool prefixes

**SYNC-04 audit (no GSD source modifications):**
QGSD adds only the files listed above. Zero imports from GSD internals
(`get-shit-done/`, `commands/`, `agents/`, `bin/`). GSD source is unmodified.

**SYNC-02 maintenance note:**
When GSD adds a new planning command, update `quorum_commands` in three places:
`hooks/config-loader.js` (DEFAULT_CONFIG), `bin/install.js` (qgsd config write block),
and `templates/qgsd.json`. Then cut a QGSD patch release.
```

## Acceptance Criteria

- [ ] `package.json` `name` is `"qgsd"`
- [ ] `package.json` `version` is `"0.1.0"`
- [ ] `package.json` `bin` contains both `"qgsd"` and `"get-shit-done-cc"` entries pointing to `bin/install.js`
- [ ] `package.json` `files` includes `"templates"`
- [ ] `package.json` has `peerDependencies` with `"get-shit-done-cc": ">=1.20.0"`
- [ ] `CHANGELOG.md` has `## [0.1.0] - 2026-02-20` section before first GSD version entry
- [ ] CHANGELOG.md entry includes GSD compatibility range, SYNC-04 audit note, SYNC-02 maintenance note
- [ ] `node -e "require('./package.json')"` succeeds (valid JSON)

## Commit

```
feat(03-01): package identity — rename to qgsd, add peerDeps, update CHANGELOG
```

Files: `package.json`, `CHANGELOG.md`
