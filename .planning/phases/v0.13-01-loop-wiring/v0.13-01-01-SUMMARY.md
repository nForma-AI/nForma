---
phase: v0.13-01
plan: 01
type: execute
wave: 1
status: complete
requirements-completed:
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - STATE-01
files-modified:
  - get-shit-done/workflows/transition.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/transition.md
  - get-shit-done/workflows/audit-milestone.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/audit-milestone.md
---

# Plan v0.13-01-01 — Summary

**Phase:** v0.13-01 Loop Wiring
**Plan:** 01 — Wire Route B audit gate + Gap Closure detection in transition.md; auto-spawn and STATE.md update in audit-milestone.md
**Status:** Complete

## What Was Built

Two surgical edits to existing workflow markdown files wired the autonomous milestone execution loop.

### Task 1: transition.md Route B — Audit Gate (LOOP-01 + LOOP-02)

**File:** `get-shit-done/workflows/transition.md` (+ installed copy)

Replaced the Route B section (previously invoked `/qgsd:complete-milestone` directly) with a two-path structure:

- **Step 1:** Detect Gap Closure phase using `grep -c '\*\*Gap Closure:\*\*'` on the completed phase's ROADMAP.md entry
- **Step 2a (IS_GAP_CLOSURE=1+, LOOP-02):** Re-audit path — both yolo and interactive sub-paths invoke `/qgsd:audit-milestone {version}` with re-audit messaging
- **Step 2b (IS_GAP_CLOSURE=0, LOOP-01):** Primary completion path — both yolo and interactive sub-paths invoke `/qgsd:audit-milestone {version}` with primary audit messaging

No direct invocation of `/qgsd:complete-milestone` remains in Route B. Complete-milestone is downstream of audit passing (handled inside audit-milestone's offer_next passed block).

### Task 2: audit-milestone.md — STATE.md Update + plan-milestone-gaps Auto-Spawn (STATE-01 + LOOP-03)

**File:** `get-shit-done/workflows/audit-milestone.md` (+ installed copy)

Two insertions:

**Insertion A (STATE-01):** Added Step 6b immediately after Step 6 (MILESTONE-AUDIT.md write), before Step 7 (Present Results):
```bash
node ~/.claude/qgsd/bin/gsd-tools.cjs state record-session \
  --stopped-at "Milestone ${MILESTONE_VERSION} audit: ${AUDIT_STATUS} (${REQUIREMENTS_SATISFIED}/${REQUIREMENTS_TOTAL} requirements)" \
  --resume-file "None"
```

**Insertion B (LOOP-03):** In the `<offer_next>` gaps_found block, replaced the text suggestion (`/qgsd:plan-milestone-gaps`) with an auto-Task spawn:
```
Task(
  prompt="Run /qgsd:plan-milestone-gaps workflow. ...",
  subagent_type="general-purpose",
  description="Plan milestone gaps: {version}"
)
```

## Preservation Checks

- `plan_exists_not_executed` pre-routing check in audit-milestone.md: **UNCHANGED** — pre-routing check still fires first; LOOP-03 auto-spawn only applies when `missing_no_plan` phases exist
- `tech_debt` block in audit-milestone.md: **UNCHANGED** — remains a human-facing option (A/B choice), no auto-spawn added
- Route A in transition.md (is_last_phase: false): **UNCHANGED**
- Steps 1-5 in audit-milestone.md: **UNCHANGED**
- Step 7 (Present Results) in audit-milestone.md: **UNCHANGED** — Step 6b inserted before it, not replacing it

## Verification Results

All checks passed:

1. `grep -n "audit-milestone" get-shit-done/workflows/transition.md` — 4 matches in Route B (yolo+primary, interactive+primary, yolo+re-audit, interactive+re-audit)
2. `grep -n "complete-milestone" get-shit-done/workflows/transition.md` — 0 matches (Route B no longer invokes complete-milestone directly)
3. `grep -c "Gap Closure" get-shit-done/workflows/transition.md` — 4 matches (IS_GAP_CLOSURE detection + comments present)
4. `diff get-shit-done/workflows/transition.md ~/.claude/qgsd/workflows/transition.md` — only pre-existing tilde vs absolute path differences (no new diffs)
5. `grep -n "state record-session" get-shit-done/workflows/audit-milestone.md` — 1 match in Step 6b
6. `grep -n "general-purpose" get-shit-done/workflows/audit-milestone.md` — 1 match in gaps_found offer_next block
7. `grep -n "plan_exists_not_executed" get-shit-done/workflows/audit-milestone.md` — 3 matches (pre-routing check intact)
8. `grep -n "tech_debt" ... | grep "plan-milestone-gaps"` — 0 matches (tech_debt block untouched)
9. `grep -n "## 7. Present Results"` — present (Step 6b inserted before it, not replacing it)
10. `diff get-shit-done/workflows/audit-milestone.md ~/.claude/qgsd/workflows/audit-milestone.md` — only pre-existing tilde vs absolute path differences (no new diffs)

## Requirements Closed

| ID | Requirement | Status |
|----|-------------|--------|
| LOOP-01 | Last-phase transition calls audit-milestone before complete-milestone (primary path) | CLOSED |
| LOOP-02 | Gap Closure marker detection routes to audit-milestone with re-audit messaging | CLOSED |
| LOOP-03 | audit-milestone auto-spawns plan-milestone-gaps Task when gaps_found + missing_no_plan | CLOSED |
| STATE-01 | audit-milestone updates STATE.md via gsd-tools state record-session after Step 6 | CLOSED |

## Decisions

- Used `subagent_type="general-purpose"` for the plan-milestone-gaps Task spawn (no dedicated `qgsd-plan-milestone-gaps` subagent type registered; no `model=` parameter to avoid resolve-model errors on unregistered roles)
- STATE.md update placed at end of Step 6 (before Step 7) per research recommendation — ensures STATE.md is updated regardless of routing path taken in Step 7
- Both LOOP-01 and LOOP-02 paths invoke the same `/qgsd:audit-milestone` destination; distinction is in the message text only
