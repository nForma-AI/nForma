---
phase: v0.9-08-post-v0.9-install-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/v0.9-08-post-v0.9-install-sync/v0.9-08-01-SUMMARY.md
autonomous: true
requirements: []
goal: The model="haiku" quorum dispatch flag added in quick-110 is present in the installed runtime at ~/.claude/qgsd/workflows/, closing the source/runtime drift record introduced post-v0.9.
success_criteria:
  - qgsd-core/workflows/plan-phase.md contains model="haiku" at all quorum dispatch sites
  - qgsd-core/workflows/discuss-phase.md contains model="haiku" at all quorum dispatch sites
  - qgsd-core/workflows/execute-phase.md contains model="haiku" at all quorum dispatch sites
  - qgsd-core/workflows/quick.md contains model="haiku" at all quorum dispatch sites
  - ~/.claude/qgsd/workflows/plan-phase.md contains model="haiku" at all quorum dispatch sites
  - ~/.claude/qgsd/workflows/discuss-phase.md contains model="haiku" at all quorum dispatch sites
  - ~/.claude/qgsd/workflows/execute-phase.md contains model="haiku" at all quorum dispatch sites
  - ~/.claude/qgsd/workflows/quick.md contains model="haiku" at all quorum dispatch sites
  - node bin/install.js --claude --global exits 0
  - SUMMARY.md documents the verification outcome with exact per-file match counts

must_haves:
  truths:
    - "Source workflows contain model=\"haiku\" at every qgsd-quorum-slot-worker dispatch site"
    - "Installed runtime workflows contain model=\"haiku\" at every qgsd-quorum-slot-worker dispatch site"
    - "install.js --claude --global runs cleanly (exit 0)"
    - "Phase has a SUMMARY.md documenting the verification result"
  artifacts:
    - path: "qgsd-core/workflows/plan-phase.md"
      provides: "Source workflow — 1 dispatch site (line 283)"
      contains: "model=\"haiku\""
    - path: "qgsd-core/workflows/discuss-phase.md"
      provides: "Source workflow — 2 dispatch sites (lines 208, 256)"
      contains: "model=\"haiku\""
    - path: ".planning/phases/v0.9-08-post-v0.9-install-sync/v0.9-08-01-SUMMARY.md"
      provides: "Phase completion record"
  key_links:
    - from: "qgsd-core/workflows/"
      to: "~/.claude/qgsd/workflows/"
      via: "node bin/install.js --claude --global"
      pattern: "copyWithPathReplacement"
---

<objective>
Formally verify that the installed runtime at ~/.claude/qgsd/workflows/ contains the model="haiku" quorum dispatch flag introduced in quick-110, then run an idempotent install sync to close the phase gap record.

Purpose: Phase v0.9-08 was opened to close a post-v0.9 source/runtime drift concern. Research (RESEARCH.md) confirmed the drift does not exist — the installed runtime already reflects quick-110 and quick-111 changes. This plan executes the formal verification and idempotent sync to mark the gap officially closed.

Output: A verified, re-synced runtime and a SUMMARY.md artifact documenting the finding.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/v0.9-08-post-v0.9-install-sync/RESEARCH.md

Key finding from RESEARCH.md: NO DRIFT EXISTS. The installed runtime already has model="haiku" at all dispatch sites. Installed file timestamps (2026-02-26 19:47 UTC) postdate quick-110 (11:39 UTC) and quick-111 (12:12 UTC) commits. This plan is a formal closure step, not a bug fix.

Pitfall to avoid: Path-expansion diffs (~/.claude/ vs /Users/jonathanborduas/.claude/) are expected installer behavior (copyWithPathReplacement), not functional drift. Only verify functional content.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify source and installed runtime both contain model="haiku"</name>
  <files>none (read-only verification)</files>
  <action>
Run the following grep commands to confirm model="haiku" is present at every dispatch site in both source and installed copies. Capture the output for the SUMMARY.md.

Source verification:
```
grep -n 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/quick.md
```

Installed runtime verification:
```
grep -n 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/plan-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md
grep -n 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/quick.md
```

Expected results (from RESEARCH.md):
- plan-phase.md: 1 match at line 283 in both source and installed
- discuss-phase.md: 2 matches (lines 208, 256) in both source and installed
- execute-phase.md: 2 matches (lines 405, 435) in both source and installed
- quick.md: 2 matches (lines 257, 397) in both source and installed

If any file returns 0 matches in source: STOP — the source has drifted (unexpected, needs investigation).
If any file returns 0 matches in installed: proceed to Task 2 anyway — the install sync will fix it.
  </action>
  <verify>All 4 source files and all 4 installed files return at least 1 grep match for model="haiku".</verify>
  <done>8 grep commands all return matches. No file has 0 results. Verification output captured.</done>
</task>

<task type="auto">
  <name>Task 2: Run idempotent install sync and write SUMMARY.md</name>
  <files>.planning/phases/v0.9-08-post-v0.9-install-sync/v0.9-08-01-SUMMARY.md</files>
  <action>
Run the install sync command from the repo root:
```
cd /Users/jonathanborduas/code/QGSD && node bin/install.js --claude --global
```

The install is idempotent — safe to run even when already synced. It performs a clean install (removes dest, copies fresh), so the result will always be the latest source state.

After the install completes (exit 0), re-run the installed-copy grep checks from Task 1 to confirm the installed runtime still shows model="haiku" post-sync. This closes the loop: source verified, install run, installed re-verified.

Then write v0.9-08-01-SUMMARY.md documenting:
- Phase goal and finding (no drift existed)
- Task 1 grep output (line numbers where model="haiku" appears)
- Task 2 install command output (exit code, summary line)
- Post-install re-verification result
- Confirmation that the gap record is closed
- Decision recorded: install sync is idempotent and safe to run as a formal closure step even when no drift exists

Format: standard SUMMARY.md template (phase, plan, date, outcome, tasks, decisions, artifacts).
  </action>
  <verify>
1. `node bin/install.js --claude --global` exits 0
2. Post-install grep of /Users/jonathanborduas/.claude/qgsd/workflows/plan-phase.md returns model="haiku"
3. v0.9-08-01-SUMMARY.md exists at .planning/phases/v0.9-08-post-v0.9-install-sync/
  </verify>
  <done>install.js exits 0, post-install installed runtime grep confirms model="haiku" at all sites, SUMMARY.md written documenting the full verification outcome.</done>
</task>

</tasks>

<verification>
Final state check after both tasks complete:

```bash
# Source integrity
grep -c 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-phase.md    # expect 1
grep -c 'model="haiku"' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md # expect 2

# Installed runtime integrity
grep -c 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/plan-phase.md    # expect 1
grep -c 'model="haiku"' /Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md # expect 2

# SUMMARY artifact exists
ls /Users/jonathanborduas/code/QGSD/.planning/phases/v0.9-08-post-v0.9-install-sync/v0.9-08-01-SUMMARY.md
```

All 5 checks must pass.
</verification>

<success_criteria>
- All source workflow files contain model="haiku" at every qgsd-quorum-slot-worker dispatch site (confirmed by grep)
- All installed workflow files at ~/.claude/qgsd/workflows/ contain model="haiku" at every dispatch site (confirmed by grep)
- node bin/install.js --claude --global exits 0
- v0.9-08-01-SUMMARY.md exists and documents the verification outcome
- The post-v0.9 install sync gap record is formally closed
</success_criteria>

<output>
After completion, the SUMMARY.md is written as part of Task 2. No separate step needed.

Artifact: `.planning/phases/v0.9-08-post-v0.9-install-sync/v0.9-08-01-SUMMARY.md`
</output>
