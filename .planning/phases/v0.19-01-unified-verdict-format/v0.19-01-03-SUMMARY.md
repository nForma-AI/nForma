# Plan v0.19-01-03 Summary — Orchestrator Integration, CI Gate, gitignore, npm test

**Phase:** v0.19-01 (Unified Verdict Format)
**Plan:** 03
**Status:** DONE
**Completed:** 2026-02-27

## What Was Built

Connected the orchestrator and CI to the NDJSON infrastructure built in Plans 01 and 02.

### Files Modified

**bin/run-formal-verify.cjs** (UNIF-02 + UNIF-03):
- Edit 1: Added NDJSON truncate at `runOnce()` start, right after `results.length = 0`:
  ```javascript
  const ndjsonPath = path.join(__dirname, '..', 'formal', 'check-results.ndjson');
  fs.writeFileSync(ndjsonPath, '', 'utf8');  // truncate — fresh run
  ```
- Edit 2: Added NDJSON-based summary block after the final HR separator in the summary section — reads back check-results.ndjson and emits `N pass, N fail` count line

**.github/workflows/formal-verify.yml** (UNIF-04):
- Added `bin/write-check-result.cjs` and `bin/check-results-exit.cjs` to both `push.paths` and `pull_request.paths` trigger lists
- Added final CI step: `Check NDJSON exit code (fail if any result=fail in check-results.ndjson)` that runs `node bin/check-results-exit.cjs`

**.gitignore**:
- Added `formal/check-results.ndjson` under PRISM-generated constants block with comment

**package.json**:
- Appended `bin/write-check-result.test.cjs bin/check-results-exit.test.cjs` to npm test script

**bin/run-prism.cjs** (PRISM-02 — pre-existing test failures fixed):
- Implemented scoreboard injection: reads `.planning/quorum-scoreboard.json` (from `process.cwd()`), computes aggregate `tp_rate` and `unavail` from rounds.votes (excluding 'claude'), injects as `-const tp_rate=X -const unavail=Y`
- Falls back to conservative priors (0.85/0.15) with stderr warning when no scoreboard found
- Caller `-const` overrides take precedence (same pattern as `run-oauth-rotation-prism.cjs`)

**formal/tla/guards/qgsd-workflow.json** (local-only — directory is gitignored):
- Added `unanimityMet: "successCount = polledCount"` guard mapping
- Fixed pre-existing `check-spec-sync` test failure

## Verification Results

- UNIF-01: All 13 runners emit NDJSON — 13/13 confirmed
- UNIF-02: run-formal-verify.cjs truncates at runOnce() start — WIRED
- UNIF-03: Summary reads from check-results.ndjson — WIRED
- UNIF-04: CI gate step exists — confirmed
- npm test: 389/389 pass (0 failures)

## Pre-existing Issues Fixed

1. **run-prism.test.cjs (4 tests)**: Tests existed since commit 889b0c1 but the implementation (scoreboard injection) was never added. Fixed by implementing PRISM-02 in run-prism.cjs.
2. **check-spec-sync.test.cjs (1 test)**: `unanimityMet` guard missing from `formal/tla/guards/qgsd-workflow.json`. Fixed locally (directory is gitignored).

## Commit

- `f2a3556` — feat(v0.19-01-03): orchestrator NDJSON integration, CI gate, gitignore, npm test

## Self-Check

- [x] UNIF-02: run-formal-verify.cjs initializes ndjson — CONFIRMED
- [x] UNIF-03: Summary reads from ndjson — CONFIRMED
- [x] UNIF-04: CI gate exists in formal-verify.yml — CONFIRMED
- [x] formal/check-results.ndjson in .gitignore — CONFIRMED
- [x] npm test exits 0, 389 tests pass — CONFIRMED
- [x] Both new test files included in npm test script — CONFIRMED
