---
phase: v0.33-05-tla-plus-meta-verification
plan: 02
type: execute
wave: 2
depends_on: [v0.33-05-01]
files_modified:
  - bin/run-oscillation-tlc.cjs
  - bin/run-oscillation-tlc.test.cjs
  - bin/requirement-map.cjs
autonomous: true
requirements: [FV-02, FV-03]
formal_artifacts: none

must_haves:
  truths:
    - "TLC verifies the safety property: no layer oscillates more than once (oscillation_count[layer] <= 1 for all layers) with zero counterexamples"
    - "TLC verifies the liveness property: solve loop eventually converges (converged = TRUE) under explicit fairness assumptions, passing the liveness fairness CI lint"
    - "The convergence liveness property uses explicit fairness declarations -- consistent with the formal ConvergenceEventuallyResolves invariant pattern (weak fairness on enabling actions ensures the check eventually completes)"
  artifacts:
    - path: "bin/run-oscillation-tlc.cjs"
      provides: "TLC runner with MCsolve-convergence config support"
      contains: "MCsolve-convergence"
    - path: "bin/run-oscillation-tlc.test.cjs"
      provides: "Tests for new config validation"
      contains: "MCsolve-convergence"
    - path: "bin/requirement-map.cjs"
      provides: "FV-01/02/03 requirement ID mapping"
      contains: "tla:solve-convergence"
  key_links:
    - from: "bin/run-oscillation-tlc.cjs"
      to: ".planning/formal/tla/NFSolveConvergence.tla"
      via: "specFileName resolution for MCsolve-convergence"
      pattern: "NFSolveConvergence\\.tla"
    - from: "bin/run-oscillation-tlc.cjs"
      to: "bin/requirement-map.cjs"
      via: "getRequirementIds('tla:solve-convergence')"
      pattern: "tla:solve-convergence"
    - from: "bin/run-oscillation-tlc.cjs"
      to: ".planning/formal/spec/solve-convergence/invariants.md"
      via: "detectLivenessProperties reads invariants.md for fairness check"
      pattern: "detectLivenessProperties"
  consumers:
    - artifact: "bin/run-oscillation-tlc.cjs (extended)"
      consumed_by: "bin/run-formal-verify.cjs"
      integration: "Already wired -- run-formal-verify.cjs calls run-oscillation-tlc.cjs"
      verify_pattern: "run-oscillation-tlc"
---

<objective>
Wire NFSolveConvergence.tla into the existing TLC runner infrastructure and execute TLC model checking to verify both safety (FV-02: oscillation bounded) and liveness (FV-03: eventual convergence) properties with zero counterexamples.

Purpose: Proves that Option C prevents unbounded oscillation (safety) and the outer solve loop eventually converges (liveness) under fairness assumptions.
Output: Extended runner + requirement map, passing TLC verification, CI lint compliance.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/v0.33-05-tla-plus-meta-verification/v0.33-05-RESEARCH.md
@.planning/phases/v0.33-05-tla-plus-meta-verification/v0.33-05-01-SUMMARY.md

@bin/run-oscillation-tlc.cjs
@bin/run-oscillation-tlc.test.cjs
@bin/requirement-map.cjs
@bin/run-tlc.cjs
@bin/check-liveness-fairness.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend run-oscillation-tlc.cjs, requirement-map.cjs, and tests for MCsolve-convergence</name>
  <files>
    bin/run-oscillation-tlc.cjs
    bin/run-oscillation-tlc.test.cjs
    bin/requirement-map.cjs
  </files>
  <action>
**run-oscillation-tlc.cjs** -- Make these 4 targeted changes per RESEARCH.md Example 4:

1. Add to CHECK_ID_MAP:
   `'MCsolve-convergence': 'tla:solve-convergence'`

2. Add to PROPERTY_MAP:
   `'MCsolve-convergence': 'Outer solve loop convergence under Option C'`

3. Add to VALID_CONFIGS array:
   `'MCsolve-convergence'` (append after 'MCconvergence')

4. Update specFileName resolution (around line 131-133). Change from the ternary:
   ```javascript
   const specFileName = configName === 'MCoscillation'
     ? 'NFOscillation.tla'
     : 'NFConvergence.tla';
   ```
   To a lookup map:
   ```javascript
   const SPEC_FILE_MAP = {
     'MCoscillation': 'NFOscillation.tla',
     'MCconvergence': 'NFConvergence.tla',
     'MCsolve-convergence': 'NFSolveConvergence.tla',
   };
   const specFileName = SPEC_FILE_MAP[configName];
   ```
   This is cleaner than nested ternaries. The workers value stays as `const workers = '1'` (all configs need single-worker for liveness).

**requirement-map.cjs** -- Add one entry per RESEARCH.md Example 5:
   `'tla:solve-convergence': ['FV-01', 'FV-02', 'FV-03']`

**run-oscillation-tlc.test.cjs** -- Add 4 new tests at the end of the file:

1. `test('MCsolve-convergence is a valid config (no error exit)')` -- spawn with `--config=MCsolve-convergence` using bogus JAVA_HOME so it fails at Java check (not config validation). Assert stderr does NOT match /Unknown config/.

2. `test('MCsolve-convergence maps to NFSolveConvergence.tla spec file')` -- read source, check SPEC_FILE_MAP contains the mapping.

3. `test('All VALID_CONFIGS have entries in SPEC_FILE_MAP and CHECK_ID_MAP')` -- read the source file, extract VALID_CONFIGS entries, SPEC_FILE_MAP keys, and CHECK_ID_MAP keys. Assert that every config in VALID_CONFIGS has a corresponding entry in both SPEC_FILE_MAP and CHECK_ID_MAP. This catches desync when new configs are added to one map but not others.

4. `test('requirement-map.cjs has tla:solve-convergence entry with FV-01, FV-02, FV-03')` -- require requirement-map.cjs and assert getRequirementIds('tla:solve-convergence') deep-equals ['FV-01', 'FV-02', 'FV-03'].
  </action>
  <verify>
- `node --test bin/run-oscillation-tlc.test.cjs` -- all tests pass
- `grep -q "MCsolve-convergence" bin/run-oscillation-tlc.cjs`
- `grep -q "NFSolveConvergence.tla" bin/run-oscillation-tlc.cjs`
- `grep -q "tla:solve-convergence" bin/requirement-map.cjs`
- `grep -q "FV-01" bin/requirement-map.cjs`
- `node -e "const {getRequirementIds}=require('./bin/requirement-map.cjs'); const ids=getRequirementIds('tla:solve-convergence'); console.log(ids); process.exit(ids.length===3 && ids.includes('FV-01') && ids.includes('FV-02') && ids.includes('FV-03') ? 0 : 1)"`
  </verify>
  <done>
run-oscillation-tlc.cjs accepts MCsolve-convergence as a valid config, maps it to NFSolveConvergence.tla, and writes check results with tla:solve-convergence check_id. requirement-map.cjs maps tla:solve-convergence to [FV-01, FV-02, FV-03]. All existing and new tests pass, including the VALID_CONFIGS/SPEC_FILE_MAP/CHECK_ID_MAP consistency test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run TLC verification and confirm safety + liveness pass with zero counterexamples</name>
  <files>
    bin/run-oscillation-tlc.cjs
    .planning/formal/tla/NFSolveConvergence.tla
  </files>
  <action>
Pre-flight: Verify Java >= 17 is available and tla2tools.jar exists:
```bash
java --version
test -f .planning/formal/tla/tla2tools.jar && echo "JAR found"
```

If tla2tools.jar is missing, download it:
```bash
curl -L https://github.com/tlaplus/tlaplus/releases/download/v1.8.0/tla2tools.jar -o .planning/formal/tla/tla2tools.jar
```

SANY parse pre-flight -- run the TLA+ parser BEFORE full model checking to catch syntax errors early:
```bash
java -cp .planning/formal/tla/tla2tools.jar tla2.SANY.SANY .planning/formal/tla/NFSolveConvergence.tla
```
If SANY reports parse errors, fix them in NFSolveConvergence.tla before proceeding. This avoids wasting a full TLC run on a syntax error.

Run TLC verification:
```bash
node bin/run-oscillation-tlc.cjs MCsolve-convergence
```

Expected output: TLC explores the state space and reports:
- INVARIANT OscillationBounded: no violations (zero counterexamples)
- PROPERTY EventualConvergence: no violations
- Exit code 0

If TLC reports a counterexample:
- For OscillationBounded violation: The blocking rule is firing too late. Ensure the block triggers when oscillation_count reaches 1 (using `>= 1`), not after incrementing to 2. This task is AUTHORIZED to modify NFSolveConvergence.tla directly to fix the issue -- document the change rationale in a TLA+ comment and re-run SANY + TLC. Do not require plan 01 reopening.
- For EventualConvergence violation: Check that WF_vars fairness is declared on both RunSession and CheckConvergence. Ensure the convergence condition includes blocked layers (converged when all layers have residual=0 OR are blocked). This task is AUTHORIZED to modify NFSolveConvergence.tla directly -- document the change rationale and re-run.
- For state space explosion (timeout > 120s): Reduce MaxSessions to 4 or MaxResidual to 2 in the .cfg file.
- For any spec modification: re-run SANY parse check before re-running TLC.

After TLC passes, run the liveness-fairness CI lint:
```bash
node bin/check-liveness-fairness.cjs
```

This must report `pass` (not `inconclusive`) for the solve-convergence surface. If it reports `inconclusive`, verify:
1. The invariants.md file is at `.planning/formal/spec/solve-convergence/invariants.md` (Pitfall 5)
2. The section header is exactly `## EventualConvergence` (Pitfall 4)
3. The config name is `MCsolve-convergence.cfg`

Also verify the check result was written:
```bash
tail -1 .planning/formal/check-results.ndjson | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(d.check_id, d.result, d.requirement_ids)"
```

Expected: `tla:solve-convergence pass ['FV-01','FV-02','FV-03']`
  </action>
  <verify>
- `java -cp .planning/formal/tla/tla2tools.jar tla2.SANY.SANY .planning/formal/tla/NFSolveConvergence.tla` exits 0 (no parse errors)
- `node bin/run-oscillation-tlc.cjs MCsolve-convergence` exits 0
- `node bin/check-liveness-fairness.cjs` passes (no inconclusive for solve-convergence)
- Check result NDJSON contains tla:solve-convergence with result=pass
- TLC output shows zero counterexamples for both OscillationBounded invariant and EventualConvergence property
  </verify>
  <done>
TLC verifies OscillationBounded safety property (no layer oscillates more than once) with zero counterexamples. TLC verifies EventualConvergence liveness property (solve loop eventually converges) with zero counterexamples under weak fairness. The liveness-fairness CI lint passes for the solve-convergence surface. Check result NDJSON records tla:solve-convergence as pass with FV-01, FV-02, FV-03 requirement IDs. If any spec fixes were needed, they are documented in TLA+ comments within NFSolveConvergence.tla.
  </done>
</task>

</tasks>

<verification>
- SANY parses NFSolveConvergence.tla without errors
- `node bin/run-oscillation-tlc.cjs MCsolve-convergence` exits 0 with zero counterexamples
- `node bin/check-liveness-fairness.cjs` shows pass for solve-convergence surface
- `node --test bin/run-oscillation-tlc.test.cjs` all tests pass
- `node -e "const {getRequirementIds}=require('./bin/requirement-map.cjs'); console.log(getRequirementIds('tla:solve-convergence'))"` returns [FV-01, FV-02, FV-03]
- Check result NDJSON has tla:solve-convergence pass entry
</verification>

<success_criteria>
- TLC reports zero counterexamples for safety (OscillationBounded) and liveness (EventualConvergence)
- Liveness-fairness CI lint passes for solve-convergence surface (not inconclusive)
- All runner tests pass including new MCsolve-convergence tests and VALID_CONFIGS consistency test
- Requirement map correctly wires FV-01, FV-02, FV-03
</success_criteria>

<output>
After completion, create `.planning/phases/v0.33-05-tla-plus-meta-verification/v0.33-05-02-SUMMARY.md`
</output>
