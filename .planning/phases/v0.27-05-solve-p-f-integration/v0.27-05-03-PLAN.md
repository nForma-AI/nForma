---
phase: v0.27-05
plan: 03
type: execute
wave: 2
depends_on:
  - v0.27-05-01
  - v0.27-05-02
files_modified:
  - bin/qgsd-solve.cjs
  - bin/qgsd-solve-ptoF-integration.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - PF-01
  - PF-02
  - PF-03
  - PF-04
  - PF-05

must_haves:
  truths:
    - "bin/qgsd-solve.cjs computeResidual() includes p_to_f in its residual vector by calling sweepPtoF(), and the p_to_f residual is included in the total sum"
    - "bin/qgsd-solve.cjs autoClose() calls autoClosePtoF() when residual.p_to_f.residual > 0"
    - "The solve report table includes a P -> F (Prod->Formal) row showing the residual count and health indicator"
    - "The solve report detail section shows P->F divergent entries, parameter updates dispatched, investigations flagged, and skipped unlinked count when p_to_f residual > 0"
    - "node bin/qgsd-solve.cjs --report-only runs successfully with the P->F layer included and outputs p_to_f in JSON mode"
    - "End-to-end integration test verifies the full flow: seed debt.json with acknowledged entries → run sweepPtoF → verify residual → run autoClosePtoF → verify state transitions and dispatch"
  artifacts:
    - path: "bin/qgsd-solve.cjs"
      provides: "Extended solve with P->F layer (8-layer sweep)"
      exports: ["computeResidual (with p_to_f)", "autoClose (with P->F dispatch)"]
    - path: "bin/qgsd-solve-ptoF-integration.test.cjs"
      provides: "End-to-end integration test for P->F feedback loop"
      min_lines: 80
  key_links:
    - from: "bin/qgsd-solve.cjs"
      to: "bin/sweepPtoF.cjs"
      via: "require and call sweepPtoF() in computeResidual()"
      pattern: "sweepPtoF"
    - from: "bin/qgsd-solve.cjs"
      to: "bin/autoClosePtoF.cjs"
      via: "require and call autoClosePtoF() in autoClose()"
      pattern: "autoClosePtoF"
    - from: "bin/qgsd-solve-ptoF-integration.test.cjs"
      to: "bin/sweepPtoF.cjs"
      via: "direct import for integration testing"
      pattern: "sweepPtoF"
    - from: "bin/qgsd-solve-ptoF-integration.test.cjs"
      to: "bin/autoClosePtoF.cjs"
      via: "direct import for integration testing"
      pattern: "autoClosePtoF"
---

<objective>
Wire the P->F layer into the existing qgsd-solve.cjs orchestrator (computeResidual, autoClose, report formatting) and create an end-to-end integration test verifying the complete feedback loop.

Purpose: Completes the P->F integration by connecting sweepPtoF and autoClosePtoF into the 8-layer solve pipeline. Addresses all five requirements (PF-01 through PF-05) at the integration level — Plan 01 and Plan 02 built the components, this plan wires them together.

Output: Updated bin/qgsd-solve.cjs with P->F layer and bin/qgsd-solve-ptoF-integration.test.cjs.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.27-05-solve-p-f-integration/v0.27-05-RESEARCH.md
@.planning/phases/v0.27-05-solve-p-f-integration/v0.27-05-CONTEXT.md
@.planning/phases/v0.27-05-solve-p-f-integration/v0.27-05-01-SUMMARY.md
@.planning/phases/v0.27-05-solve-p-f-integration/v0.27-05-02-SUMMARY.md
@bin/qgsd-solve.cjs
@bin/sweepPtoF.cjs
@bin/autoClosePtoF.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire P->F into qgsd-solve.cjs computeResidual, autoClose, and report</name>
  <files>
    bin/qgsd-solve.cjs
  </files>
  <action>
Modify bin/qgsd-solve.cjs to integrate the P->F layer. Follow the existing 7-layer pattern exactly.

**1. Add require at top (near other requires):**
```js
const { sweepPtoF } = require('./sweepPtoF.cjs');
const { autoClosePtoF } = require('./autoClosePtoF.cjs');
```

**2. Update computeResidual() function:**
- Add `const p_to_f = sweepPtoF();` after `const d_to_c = sweepDtoC();`
- Add `(p_to_f.residual >= 0 ? p_to_f.residual : 0)` to the total sum
- Add `p_to_f,` to the return object (after d_to_c)
- Update the JSDoc comment: "Computes residual vector for all 8 layer transitions" (was 7)

**3. Update autoClose() function:**
- Add a new block after the D->C block:
```js
// P->F divergence: dispatch parameter updates or flag investigations
if (residual.p_to_f && residual.p_to_f.residual > 0) {
  const result = autoClosePtoF(residual.p_to_f);
  for (const action of result.actions_taken) {
    actions.push(action);
  }
}
```

**4. Update formatReport() function:**
- Add a new row to the residual vector table:
```js
{ label: 'P -> F (Prod->Formal)', residual: finalResidual.p_to_f.residual },
```
- Add a new detail section for P->F (after D->C detail):
```js
if (finalResidual.p_to_f && finalResidual.p_to_f.residual > 0) {
  lines.push('## P -> F (Production -> Formal)');
  const detail = finalResidual.p_to_f.detail;
  if (detail.divergent_entries && detail.divergent_entries.length > 0) {
    lines.push('Divergent entries:');
    for (const ent of detail.divergent_entries) {
      lines.push(`  - ${ent.id}: ${ent.formal_ref} (measured: ${ent.measured}, expected: ${ent.expected})`);
    }
  }
  if (detail.skipped_unlinked > 0) {
    lines.push(`Skipped (waiting for formal link): ${detail.skipped_unlinked}`);
  }
  lines.push('');
}
```

**5. Update file header comment:**
- Change "Layer transitions (7):" to "Layer transitions (8):"
- Add: `//   P->F: Acknowledged production debt entries diverging from formal model thresholds`

**6. Guard against missing p_to_f (backward compat):**
- In formatReport, guard the p_to_f row: `residual: finalResidual.p_to_f ? finalResidual.p_to_f.residual : -1`
- This handles cases where old cached residual objects lack the p_to_f key

Do NOT refactor or restructure existing sweep functions. Only add the P->F wiring.
  </action>
  <verify>node bin/qgsd-solve.cjs --report-only 2>&1 | head -30</verify>
  <done>
- computeResidual() returns p_to_f in residual vector with correct total
- autoClose() dispatches autoClosePtoF when p_to_f residual > 0
- Report table shows P -> F row with residual count and health indicator
- Report detail section shows divergent entries and skipped unlinked count
- --report-only runs without error and includes P->F in output
- JSON mode (--json) includes p_to_f object in output
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test for P->F feedback loop</name>
  <files>
    bin/qgsd-solve-ptoF-integration.test.cjs
  </files>
  <action>
Create bin/qgsd-solve-ptoF-integration.test.cjs — an integration test that exercises the full P->F feedback loop using real modules but mock filesystem.

**Setup:**
- Create a temp directory with:
  - `.formal/debt.json` seeded with test entries:
    - Entry A: status="acknowledged", issue_type="drift", formal_ref="spec:safety/MCsafety.cfg:MaxDeliberation", meta.measured_value=10
    - Entry B: status="acknowledged", issue_type="issue", formal_ref="requirement:REL-01", meta.measured_value="failing"
    - Entry C: status="open", issue_type="drift", formal_ref="spec:safety/MCsafety.cfg:Timeout"
    - Entry D: status="acknowledged", issue_type="drift", formal_ref=null (unlinked)
  - `.formal/spec/safety/MCsafety.cfg` with `MaxDeliberation = 5` (differs from measured_value=10)
  - `.formal/requirements.json` with a few requirement entries

**Test 1: sweepPtoF produces correct residual**
- Call sweepPtoF with ledgerPath and specDir pointing to temp dir
- Assert residual=1 (only Entry A diverges — numeric threshold, measured != expected)
- Assert Entry C (open) is not in divergent_entries
- Assert Entry D (null formal_ref) is in skipped_unlinked
- Assert Entry B (issue type, non-numeric formal_ref) is not in divergent_entries (requirement refs return null from extractFormalExpected)

**Test 2: autoClosePtoF dispatches correctly**
- Use mock spawnTool that returns { ok: true }
- Call autoClosePtoF with the residual from Test 1
- Assert Entry A transitions acknowledged → resolving → resolved
- Assert spawnTool was called once with args containing "MaxDeliberation" and "10"
- Assert actions_taken includes "Dispatched parameter update"

**Test 3: Investigation track for issue entries**
- Seed debt.json with only Entry B (acknowledged issue)
- sweepPtoF should find it divergent if extractFormalExpected returns a value
- If not (requirement refs return null), verify Entry B is in skipped_unlinked instead
- Alternatively: create a spec entry for REL-01 and test investigation flag

**Test 4: Freeze semantics**
- After autoClosePtoF transitions an entry to resolving, verify that:
  - canTransition(entry.status, 'open') === false (cannot revert to open)
  - The entry status in the written ledger reflects the final state

**Test 5: Full pipeline smoke**
- Call sweepPtoF → autoClosePtoF → verify final ledger state
- Read debt.json from temp dir after autoClosePtoF completes
- Assert: Entry A is resolved, Entry B is unchanged or flagged, Entry C still open, Entry D still acknowledged

**Cleanup:** Remove temp directory after each test.

Use Node.js built-in test runner (node:test) and assert module. Follow existing test patterns in bin/debt-dedup-integration.test.cjs for temp dir setup.
  </action>
  <verify>node --test bin/qgsd-solve-ptoF-integration.test.cjs</verify>
  <done>
- Integration test exercises full P->F feedback loop: read → sweep → auto-close → verify
- Correct entries processed (acknowledged only, open filtered, unlinked skipped)
- Parameter update track dispatches and resolves entries
- Investigation track flags entries without auto-remediation
- Freeze semantics verified (resolving entries immune to invalid transitions)
- All integration tests pass
  </done>
</task>

</tasks>

<verification>
- `node bin/qgsd-solve.cjs --report-only` — runs without error, P->F row visible in report
- `node bin/qgsd-solve.cjs --report-only --json` — JSON output includes p_to_f key
- `node --test bin/qgsd-solve-ptoF-integration.test.cjs` — all integration tests pass
- Existing solve tests still pass (no regressions in 7 existing layers)
</verification>

<success_criteria>
- P->F layer fully integrated into qgsd-solve.cjs 8-layer sweep pipeline
- Report output includes P->F residual count, health indicator, and detail section
- End-to-end integration test covers the complete feedback loop from debt.json to remediation
- Zero regressions in existing solve functionality
</success_criteria>

<output>
After completion, create `.planning/phases/v0.27-05-solve-p-f-integration/v0.27-05-03-SUMMARY.md`
</output>
