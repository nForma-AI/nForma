---
phase: v0.25-02
slug: property-annotations
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-03
---

# Phase v0.25-02 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.25-02` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.25-02` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.25-02`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `node --test` (`.test.cjs` files) |
| **Config file** | none — project uses `node --test` directly |
| **Quick run command** | `node --test test/extract-annotations.test.cjs` |
| **Full suite command** | `node bin/extract-annotations.cjs --validate` |
| **Estimated runtime** | ~5 seconds |
| **CI pipeline** | .github/workflows/ — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `grep -rc '@requirement' formal/tla/ formal/alloy/ formal/prism/*.props 2>/dev/null | tail -3`
- **After every plan wave:** Run `node bin/extract-annotations.cjs --validate`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.25-02-01-01 | 01 | 1 | ANNOT-01 | smoke | `for f in formal/tla/QGSD*.tla formal/tla/TUINavigation.tla; do echo "$f: $(grep -c '@requirement' "$f")"; done` | N/A | ⬜ pending |
| v0.25-02-01-02 | 01 | 1 | ANNOT-01 | smoke | `grep -c '@requirement' formal/tla/QGSDQuorum.tla formal/tla/QGSDMCPEnv.tla formal/tla/TUINavigation.tla formal/tla/QGSDQuorum_xstate.tla` | N/A | ⬜ pending |
| v0.25-02-02-01 | 02 | 1 | ANNOT-02 | smoke | `for f in formal/alloy/*.als; do echo "$f: $(grep -c '@requirement' "$f")"; done` | N/A | ⬜ pending |
| v0.25-02-02-02 | 02 | 1 | ANNOT-03 | smoke | `for f in formal/prism/*.props; do echo "$f: $(grep -c '@requirement' "$f")"; done` | N/A | ⬜ pending |
| v0.25-02-03-01 | 03 | 2 | ANNOT-04 | unit | `node --test test/extract-annotations.test.cjs` | TBD | ⬜ pending |
| v0.25-02-03-02 | 03 | 2 | ANNOT-04 | integration | `node bin/extract-annotations.cjs --validate` | TBD | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

Wave 1 tasks (annotation placement) are verified by grep counts — no test framework required.
Wave 2 tasks (parser) create their own test infrastructure (test/extract-annotations.test.cjs) as part of the plan.

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-03

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.25-02`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 4 | grep counts | — | — | ⬜ pending |
| 2 | 2 | `node --test + --validate` | — | — | ⬜ pending |

**Phase validation complete:** {pending / YYYY-MM-DD HH:MM}
