---
phase: v0.12-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/run-alloy.test.cjs
  - bin/export-prism-constants.test.cjs
  - bin/generate-petri-net.test.cjs
  - formal/alloy/.gitkeep
  - formal/prism/.gitkeep
  - formal/petri/.gitkeep
  - .gitignore
autonomous: true
requirements: [ALY-02, PRM-02, PRM-03, PET-01, PET-02, PET-03]

must_haves:
  truths:
    - "Running `node --test bin/run-alloy.test.cjs` exits non-zero (RED) with at least 1 failing stub test"
    - "Running `node --test bin/export-prism-constants.test.cjs` exits non-zero (RED) with at least 1 failing stub test"
    - "Running `node --test bin/generate-petri-net.test.cjs` exits non-zero (RED) with at least 1 failing stub test"
    - "formal/alloy/, formal/prism/, and formal/petri/ directories exist in the repo (tracked via .gitkeep)"
    - "org.alloytools.alloy.dist.jar and rates.const are in .gitignore"
  artifacts:
    - path: "bin/run-alloy.test.cjs"
      provides: "Failing stubs for ALY-02 error paths (JAVA_HOME bad, JAR missing)"
      min_lines: 30
    - path: "bin/export-prism-constants.test.cjs"
      provides: "Failing stubs for PRM-02/03 (no scoreboard, < 30 rounds warning, conservative priors)"
      min_lines: 40
    - path: "bin/generate-petri-net.test.cjs"
      provides: "Failing stubs for PET-01/02/03 (DOT output shape, deadlock warning, SVG path)"
      min_lines: 30
    - path: "formal/alloy/.gitkeep"
      provides: "Tracked empty directory for Alloy JAR and spec"
    - path: "formal/prism/.gitkeep"
      provides: "Tracked empty directory for PRISM model and rates.const"
    - path: "formal/petri/.gitkeep"
      provides: "Tracked empty directory for DOT and SVG output"
  key_links:
    - from: "bin/run-alloy.test.cjs"
      to: "bin/run-alloy.cjs"
      via: "spawnSync(process.execPath, [RUN_ALLOY], ...)"
      pattern: "spawnSync.*run-alloy\\.cjs"
    - from: "bin/export-prism-constants.test.cjs"
      to: "bin/export-prism-constants.cjs"
      via: "require() or spawnSync for testable pure functions"
      pattern: "export-prism-constants"
    - from: "bin/generate-petri-net.test.cjs"
      to: "bin/generate-petri-net.cjs"
      via: "require('...generate-petri-net.cjs')._pure for unit-testable logic"
      pattern: "generate-petri-net"
---

<objective>
Create Wave 0 test scaffolding — RED stub test files for all three static analysis CLI scripts, plus formal/ directory structure and .gitignore entries for large JARs.

Purpose: The Nyquist validation strategy requires test stubs committed before any implementation. These stubs define the expected behavior contracts for run-alloy.cjs, export-prism-constants.cjs, and generate-petri-net.cjs. All stubs must fail (RED) because the implementation files do not yet exist.

Output: Three failing test files in bin/, three .gitkeep files in formal/, and updated .gitignore entries.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.12-03-static-analysis-suite/v0.12-03-RESEARCH.md
@.planning/phases/v0.12-03-static-analysis-suite/v0.12-03-VALIDATION.md
@bin/run-tlc.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/run-alloy.test.cjs — RED stubs for ALY-02 error paths</name>
  <files>bin/run-alloy.test.cjs</files>
  <action>
Create `bin/run-alloy.test.cjs` following the exact same pattern as `bin/run-tlc.test.cjs`. All tests use `spawnSync(process.execPath, [RUN_ALLOY], ...)` to invoke the not-yet-existing `bin/run-alloy.cjs`. Tests MUST fail (RED) because the implementation file does not exist yet — spawnSync on a missing file returns error state.

Include these 4 stub tests:
1. `'exits non-zero and prints JAVA_HOME error when JAVA_HOME points to nonexistent path'` — env: `{ JAVA_HOME: '/nonexistent/java/path' }`, assert status 1, stderr matches `/JAVA_HOME|java/i`
2. `'exits non-zero and prints Alloy JAR download URL when JAR not found'` — needs valid Java (detect via `/usr/libexec/java_home` or `JAVA_HOME` env, skip if unavailable); assert status 1, stderr matches `/org\.alloytools|alloy.*jar|github\.com\/AlloyTools/i`
3. `'exits non-zero with descriptive message for unknown --model value'` — use `--model=bogus` (or an appropriate invalid flag if run-alloy has no such flag; in that case, use missing ALS file path); assert status 1
4. `'exits non-zero listing download URL in error when both JAR and ALS are missing'` — no JAVA_HOME override, just run with no files present; assert status 1, stderr contains download guidance

File header:
```
#!/usr/bin/env node
'use strict';
// bin/run-alloy.test.cjs
// Wave 0 RED stubs for bin/run-alloy.cjs error paths.
// All tests check error conditions only — no Java or Alloy JAR invocation.
// Requirements: ALY-02
```

Use `const { test } = require('node:test')` and `const assert = require('node:assert')`.
The file for `RUN_ALLOY` is `path.join(__dirname, 'run-alloy.cjs')`.

Important: since `run-alloy.cjs` does not exist yet, `spawnSync(process.execPath, [RUN_ALLOY])` will return `{ status: 1, error: ... }` or similar non-zero exit. Tests 1, 3, and 4 will fail (RED) because they attempt to match stderr patterns from a script that doesn't exist — that is the intended RED state.
  </action>
  <verify>node --test bin/run-alloy.test.cjs 2>&amp;1 | tail -20 — expect at least 1 failing test (RED state confirmed)</verify>
  <done>File exists; running it produces at least 1 failure (test framework exit code non-zero), proving RED state. No test accidentally passes on a missing implementation file.</done>
</task>

<task type="auto">
  <name>Task 2: Create bin/export-prism-constants.test.cjs and bin/generate-petri-net.test.cjs — RED stubs for PRM-02/03 and PET-01/02/03</name>
  <files>bin/export-prism-constants.test.cjs, bin/generate-petri-net.test.cjs</files>
  <action>
Create two test stub files. Both must fail (RED) because the implementation files do not exist yet. Use `node:test` and `node:assert` throughout. Use `spawnSync` for process-level error tests.

**bin/export-prism-constants.test.cjs** (PRM-02, PRM-03):

File header:
```
#!/usr/bin/env node
'use strict';
// bin/export-prism-constants.test.cjs
// Wave 0 RED stubs for bin/export-prism-constants.cjs
// Tests cover: no scoreboard file, < 30 rounds warning, conservative priors.
// Requirements: PRM-02, PRM-03
```

Include 4 stubs that use `spawnSync(process.execPath, [EXPORT_PRISM], ...)`:
1. `'exits non-zero when no scoreboard file found'` — run with `cwd` set to a temp dir that has no `.planning/` directory; assert status 1, stderr matches `/scoreboard|no.*scoreboard/i`
2. `'exits 0 and writes rates.const when scoreboard has >= 30 rounds per slot'` — create a temp `.planning/quorum-scoreboard.json` with 35 rounds per slot (TP votes); assert status 0, stdout matches `/written|rates\.const/i`
3. `'emits WARNING to stderr when any slot has < 30 rounds'` — scoreboard with only 5 rounds for "gemini"; assert stderr matches `/WARNING.*gemini|fewer.*30|conservative/i`
4. `'uses conservative priors (0.85) for slots with < 30 rounds'` — verify output rates.const contains `0.85` for the low-round slot

For stubs 2-4: since the implementation doesn't exist, `spawnSync` will fail with non-zero exit for ALL tests — that is the intended RED state. Write the assertions as if the implementation exists (for documentation); they will fail on the missing binary.

**bin/generate-petri-net.test.cjs** (PET-01, PET-02, PET-03):

File header:
```
#!/usr/bin/env node
'use strict';
// bin/generate-petri-net.test.cjs
// Wave 0 RED stubs for bin/generate-petri-net.cjs
// Tests cover: DOT output structure, WASM SVG render, deadlock warning.
// Requirements: PET-01, PET-02, PET-03
```

Include 4 stubs:
1. `'exits 0 and writes quorum-petri-net.dot on success'` — run in a temp dir; assert status 0, dot file exists (will fail RED — no implementation)
2. `'DOT output contains place nodes (circle shape) and transition nodes (rect shape)'` — after run, read dot file content; assert matches `/shape=circle/` and `/shape=rect/`; will fail RED
3. `'exits 0 and writes quorum-petri-net.svg when @hpcc-js/wasm-graphviz is installed'` — assert svg file exists; will fail RED
4. `'emits structural deadlock WARNING to stderr when min_quorum_size > available_slots'` — this is pure logic in the implementation; test will fail RED since binary doesn't exist

For the _pure export pattern: once implementation exists, tests 1-4 can be updated to use `require('./generate-petri-net.cjs')._pure.buildDot(slots, min)` for unit tests. The stubs use spawnSync for now.
  </action>
  <verify>node --test bin/export-prism-constants.test.cjs 2>&amp;1 | tail -5; node --test bin/generate-petri-net.test.cjs 2>&amp;1 | tail -5 — both must show at least 1 failure</verify>
  <done>Both test files exist; both produce at least 1 failing test (RED). Test count matches expectations (4 tests each).</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold formal/ directories and update .gitignore</name>
  <files>formal/alloy/.gitkeep, formal/prism/.gitkeep, formal/petri/.gitkeep, .gitignore</files>
  <action>
1. Create three tracked placeholder files:
   - `formal/alloy/.gitkeep` — empty file (git tracks the directory)
   - `formal/prism/.gitkeep` — empty file
   - `formal/petri/.gitkeep` — empty file

2. Update `.gitignore` to exclude large JARs and generated output files. Add these entries if not already present:
```
# Alloy 6 JAR (large binary — download separately, see VERIFICATION_TOOLS.md)
formal/alloy/org.alloytools.alloy.dist.jar

# PRISM-generated constants file (generated by bin/export-prism-constants.cjs)
formal/prism/rates.const

# Petri Net generated outputs (generated by bin/generate-petri-net.cjs)
formal/petri/quorum-petri-net.dot
formal/petri/quorum-petri-net.svg
```

Note: `formal/tla/tla2tools.jar` should already be gitignored from v0.12-02. Verify it's present and don't duplicate it.

Verify `formal/` directory structure exists with `ls formal/` after creation.
  </action>
  <verify>ls formal/alloy formal/prism formal/petri; grep 'org.alloytools.alloy.dist.jar' .gitignore</verify>
  <done>All three .gitkeep files exist; .gitignore contains entries for org.alloytools.alloy.dist.jar, rates.const, quorum-petri-net.dot, and quorum-petri-net.svg. Running `git status` shows only new tracked files (no large binary noise).</done>
</task>

</tasks>

<verification>
After all tasks:
1. `node --test bin/run-alloy.test.cjs` — exits non-zero (RED), at least 1 failing test
2. `node --test bin/export-prism-constants.test.cjs` — exits non-zero (RED), at least 1 failing test
3. `node --test bin/generate-petri-net.test.cjs` — exits non-zero (RED), at least 1 failing test
4. `ls formal/alloy/.gitkeep formal/prism/.gitkeep formal/petri/.gitkeep` — all three exist
5. `grep 'org.alloytools' .gitignore` — entry present
6. `npm test` — existing test suite still passes (new test files fail, but existing green tests unaffected); confirm no regression in currently-passing tests
</verification>

<success_criteria>
Wave 0 scaffold is complete when:
- Three RED test stubs exist in bin/ covering ALY-02, PRM-02, PRM-03, PET-01, PET-02, PET-03
- formal/alloy/, formal/prism/, formal/petri/ directories tracked via .gitkeep files
- .gitignore updated for large JARs and generated output files
- No regression in existing npm test green tests
</success_criteria>

<output>
After completion, create `.planning/phases/v0.12-03-static-analysis-suite/v0.12-03-01-SUMMARY.md`
</output>
