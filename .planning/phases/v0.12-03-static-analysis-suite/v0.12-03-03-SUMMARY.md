# v0.12-03-03 SUMMARY — PRISM Model + export-prism-constants.cjs

**Phase:** v0.12-03-static-analysis-suite
**Plan:** 03
**Completed:** 2026-02-25
**Duration:** ~5 min

## What Was Built

### Files Created
- `formal/prism/quorum.pm` — PRISM DTMC model with dtmc keyword, const double tp_rate/unavail, 3-state quorum_convergence module (s=0/1/2), absorbing DECIDED state, comments for -const CLI override pattern
- `bin/export-prism-constants.cjs` — scoreboard reader with MIN_ROUNDS=30 threshold (PRM-03), conservative priors TP=0.85/UNAVAIL=0.15, WARNING to stderr for low-data slots, writes rates.const, exports `_pure = { computeSlotRates, buildConstLines }`

## Verification

- `grep 'dtmc' formal/prism/quorum.pm` → matches
- `grep 'const double' formal/prism/quorum.pm` → 2+ matches (tp_rate, unavail)
- `node bin/export-prism-constants.cjs 2>&1` → "Written: formal/prism/rates.const" on real scoreboard (430 rounds, all slots > 30 rounds)
- `cat formal/prism/rates.const | grep 'const double'` → 8 per-slot declarations (4 slots × tp_ + unavail_)
- `node --test bin/export-prism-constants.test.cjs` → 4/4 GREEN

## Real Scoreboard Results (2026-02-25)

| Slot | Rounds | TP Rate | UNAVAIL Rate | Prior Used? |
|------|--------|---------|--------------|-------------|
| gemini | 144 | 0.3958 | 0.5417 | No |
| opencode | 148 | 0.7297 | 0.1892 | No |
| copilot | 141 | 0.6454 | 0.2695 | No |
| codex | 134 | 0.0299 | 0.8955 | No |

All slots have > 30 rounds — empirical rates used for all.

## Requirements Satisfied

| Requirement | Status |
|-------------|--------|
| PRM-01: quorum.pm DTMC model | COMPLETE |
| PRM-02: export-prism-constants.cjs writes rates.const | COMPLETE |
| PRM-03: 30-round threshold + conservative priors + WARNING | COMPLETE |
| bin/export-prism-constants.test.cjs GREEN | COMPLETE |

## Key Decisions

- `require.main === module` guard ensures main I/O logic does not run when the module is required for _pure testing
- `computeSlotRates` counts only TP and TP+ as APPROVE (not TN, FP, FN)
- UNAVAIL is tracked as its own rate class (not just "not TP") — enables independent modeling in PRISM
- Slots list excludes Claude intentionally — Claude's self-vote is structural
