# v0.12-03-04 SUMMARY — generate-petri-net.cjs + VERIFICATION_TOOLS.md + package.json

**Phase:** v0.12-03-static-analysis-suite
**Plan:** 04
**Completed:** 2026-02-25
**Duration:** ~6 min

## What Was Built

### Files Created
- `bin/generate-petri-net.cjs` — DOT generator + WASM SVG renderer. Pure `buildDot(slots, minQuorum)` function exported via `_pure`. Structural deadlock warning (PET-03) fires when MIN_QUORUM_SIZE > QUORUM_SLOTS.length (does not exit 1). WASM render via dynamic `await import('@hpcc-js/wasm-graphviz')`. `require.main === module` guard.
- `VERIFICATION_TOOLS.md` — Single Java 17 prerequisite documented for TLA+, Alloy, PRISM. PRISM install note (shell script, not java -jar). Petri Net noted as Java-free.

### Files Modified
- `package.json` — devDependencies: added `@hpcc-js/wasm-graphviz@^1.21.0`. test script: added `bin/run-alloy.test.cjs bin/export-prism-constants.test.cjs bin/generate-petri-net.test.cjs`

## Verification

- `node bin/generate-petri-net.cjs` → exits 0; DOT and SVG written to formal/petri/
- `grep 'shape=circle' formal/petri/quorum-petri-net.dot` → matches
- `grep 'shape=rect' formal/petri/quorum-petri-net.dot` → matches
- `file formal/petri/quorum-petri-net.svg` → SVG XML file
- `node --test bin/generate-petri-net.test.cjs` → 4/4 GREEN
- `grep 'Java 17' VERIFICATION_TOOLS.md` → 3+ matches
- `grep 'run-alloy.test.cjs' package.json` → present in test script
- `npm test` → 284/284 passing (was 272; +12 from 3 new test suites)

## Requirements Satisfied

| Requirement | Status |
|-------------|--------|
| PET-01: DOT file written | COMPLETE |
| PET-02: SVG via @hpcc-js/wasm-graphviz | COMPLETE |
| PET-03: Structural deadlock warning (exit 0) | COMPLETE |
| VERIFICATION_TOOLS.md with Java 17 | COMPLETE |
| package.json test script includes all 3 new suites | COMPLETE |
| npm test exits 0 — no regressions | COMPLETE |

## Phase v0.12-03 Final Checklist

| Req | File | Status |
|-----|------|--------|
| ALY-01 | formal/alloy/quorum-votes.als | COMPLETE (Plan 02) |
| ALY-02 | bin/run-alloy.cjs | COMPLETE (Plan 02) |
| PRM-01 | formal/prism/quorum.pm | COMPLETE (Plan 03) |
| PRM-02 | bin/export-prism-constants.cjs | COMPLETE (Plan 03) |
| PRM-03 | 30-round threshold + conservative priors | COMPLETE (Plan 03) |
| PET-01 | formal/petri/quorum-petri-net.dot | COMPLETE (Plan 04) |
| PET-02 | formal/petri/quorum-petri-net.svg | COMPLETE (Plan 04) |
| PET-03 | Structural deadlock WARNING, not exit 1 | COMPLETE (Plan 04) |

## Key Decisions

- `require.main === module` guard — same pattern as export-prism-constants.cjs; ensures _pure functions can be required in tests without triggering I/O
- WASM render failure → exit 1; DOT write failure is not caught (fs.writeFileSync throws naturally)
- `@hpcc-js/wasm-graphviz` is ESM-only; `require()` throws ERR_REQUIRE_ESM — dynamic `await import()` is mandatory
- Deadlock fires when MIN_QUORUM_SIZE > QUORUM_SLOTS.length; with N=5 and min=3, deadlock never fires in current config
