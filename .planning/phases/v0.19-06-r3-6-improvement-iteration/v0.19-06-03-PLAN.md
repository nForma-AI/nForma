---
phase: v0.19-06
plan: "03"
type: execute
wave: 2
depends_on:
  - v0.19-06-02
files_modified:
  - qgsd-core/workflows/plan-phase.md
  - qgsd-core/workflows/quick.md
autonomous: true
requirements:
  - IMPR-03
  - IMPR-04

must_haves:
  truths:
    - "~/.claude/qgsd/workflows/plan-phase.md contains the R3.6 outer loop (step 8.5)"
    - "~/.claude/qgsd/workflows/quick.md contains the R3.6 outer loop (step 5.7)"
    - "Both installed files have zero CLAUDE.md file-read instructions (Plan 02 changes are captured)"
    - "Installed plan-phase.md and quick.md match source files after Plan 02 changes"
  artifacts:
    - path: "~/.claude/qgsd/workflows/plan-phase.md"
      provides: "Installed plan-phase workflow with R3.6 loop + no CLAUDE.md references"
      contains: "improvement_iteration"
    - path: "~/.claude/qgsd/workflows/quick.md"
      provides: "Installed quick workflow with R3.6 loop + no CLAUDE.md references"
      contains: "improvement_iteration"
  key_links:
    - from: "qgsd-core/workflows/plan-phase.md"
      to: "~/.claude/qgsd/workflows/plan-phase.md"
      via: "node bin/install.js --claude --global (reads from qgsd-core/workflows/ not hooks/dist/)"
      pattern: "improvement_iteration"
    - from: "qgsd-core/workflows/quick.md"
      to: "~/.claude/qgsd/workflows/quick.md"
      via: "node bin/install.js --claude --global"
      pattern: "improvement_iteration"
---

<objective>
Install the updated qgsd-core/workflows/plan-phase.md and quick.md (with R3.6 loops from steps 8.5 and 5.7, and CLAUDE.md references already removed by Plan 02) to ~/.claude/qgsd/workflows/ so the live runtime uses the complete implementation.

Purpose: The R3.6 loops exist in source. Plan 02 removed CLAUDE.md references. This plan syncs the updated source to the installed location so both IMPR-03 (plan-phase R3.6) and IMPR-04 (quick R3.6 + no CLAUDE.md) are live.
Output: Installed plan-phase.md and quick.md at ~/.claude/qgsd/workflows/ reflecting all Phase v0.19-06 changes.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.19-06-r3-6-improvement-iteration/v0.19-06-RESEARCH.md
@.planning/phases/v0.19-06-r3-6-improvement-iteration/v0.19-06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify R3.6 loop completeness in source files, then install</name>
  <files>
    qgsd-core/workflows/plan-phase.md
    qgsd-core/workflows/quick.md
  </files>
  <action>
**Step 1: Verify R3.6 loop presence in source files**

Before installing, confirm the R3.6 loops are present and complete in both source files.

For plan-phase.md — grep for the key markers:
```bash
grep -n "improvement_iteration\|QUORUM_IMPROVEMENTS_START\|R3\.6" qgsd-core/workflows/plan-phase.md | head -20
```
Expected: lines showing `improvement_iteration = 0`, `LOOP (while improvement_iteration <= 10)`, QUORUM_IMPROVEMENTS_START parsing, and the four route conditions (BLOCKED, ESCALATED, APPROVED+empty, APPROVED+non-empty).

For quick.md — grep for the key markers:
```bash
grep -n "improvement_iteration\|QUORUM_IMPROVEMENTS_START\|R3\.6\|Step 5\.7" qgsd-core/workflows/quick.md | head -20
```
Expected: Step 5.7 heading, `improvement_iteration = 0`, loop, route conditions.

Also confirm Plan 02 changes are present (no CLAUDE.md references):
```bash
grep -n "CLAUDE.md" qgsd-core/workflows/plan-phase.md qgsd-core/workflows/quick.md
```
Expected: no output (plan-phase.md was not in Plan 02's file list — it has no CLAUDE.md reference; quick.md had one removed in Plan 02).

**Step 2: Run installer**

```bash
node bin/install.js --claude --global
```

The installer reads from qgsd-core/workflows/ (NOT hooks/dist/) and writes to ~/.claude/qgsd/workflows/. Per MEMORY.md install sync pattern, this is the correct command.

**Step 3: Verify installed files**

Confirm R3.6 loop present in installed plan-phase.md:
```bash
grep -n "improvement_iteration" ~/.claude/qgsd/workflows/plan-phase.md | head -5
```
Expected: lines showing `improvement_iteration = 0` and `improvement_iteration += 1`.

Confirm R3.6 loop present in installed quick.md:
```bash
grep -n "improvement_iteration" ~/.claude/qgsd/workflows/quick.md | head -5
```
Expected: lines showing `improvement_iteration = 0` and `improvement_iteration += 1`.

Confirm no CLAUDE.md references in installed files:
```bash
grep -n "CLAUDE.md" ~/.claude/qgsd/workflows/plan-phase.md ~/.claude/qgsd/workflows/quick.md ~/.claude/qgsd/agents/qgsd-executor.md ~/.claude/qgsd/agents/qgsd-quorum-worker.md 2>/dev/null
```
Expected: no output. (The installer also copies agent files from agents/ to ~/.claude/agents/ — verify those too if installer scope includes agents.)

Note: if installer does NOT copy agents/ automatically, manually sync the modified agent files:
```bash
cp agents/qgsd-executor.md ~/.claude/agents/
cp agents/qgsd-phase-researcher.md ~/.claude/agents/
cp agents/qgsd-plan-checker.md ~/.claude/agents/
cp agents/qgsd-planner.md ~/.claude/agents/
cp agents/qgsd-quorum-worker.md ~/.claude/agents/
```
Then re-verify with grep.
  </action>
  <verify>
    1. `grep -n "improvement_iteration" ~/.claude/qgsd/workflows/plan-phase.md` — shows at least 2 matches
    2. `grep -n "improvement_iteration" ~/.claude/qgsd/workflows/quick.md` — shows at least 2 matches
    3. `grep -rn "CLAUDE.md" ~/.claude/qgsd/workflows/ ~/.claude/agents/ 2>/dev/null` — no output
    4. `npm test` — all tests pass (install doesn't break anything)
  </verify>
  <done>Both plan-phase.md and quick.md are installed to ~/.claude/qgsd/workflows/ with R3.6 loop present (improvement_iteration variable visible via grep) and zero CLAUDE.md file-read references across all installed workflow and agent files.</done>
</task>

<task type="auto">
  <name>Task 2: Final integration check — verify all IMPR requirements are closed</name>
  <files></files>
  <action>
Perform a final cross-requirement verification pass to confirm all four IMPR requirements are now closed.

**IMPR-01 check:**
```bash
node -e "
const fs = require('fs');
const content = fs.readFileSync('bin/gsd-quorum-slot-worker-improvements.test.cjs', 'utf8');
console.log('IMPR-01 test file exists:', content.length > 0);
console.log('Line count:', content.split('\n').length);
"
```
And: `npm test -- 2>&1 | grep -E "gsd-quorum-slot-worker-improvements|PASSED|FAILED" | head -5`

**IMPR-02 check:**
```bash
node -e "
const fs = require('fs');
const content = fs.readFileSync('bin/quorum-improvements-signal.test.cjs', 'utf8');
console.log('IMPR-02 test file exists:', content.length > 0);
"
```
And: `npm test -- 2>&1 | grep -E "quorum-improvements-signal|PASSED|FAILED" | head -5`

**IMPR-03 check:**
```bash
grep -c "improvement_iteration" ~/.claude/qgsd/workflows/plan-phase.md
```
Expected: >= 4 (initialization, loop condition, increment, break condition checks).

**IMPR-04 check (combined):**
```bash
# R3.6 loop in quick
grep -c "improvement_iteration" ~/.claude/qgsd/workflows/quick.md
# CLAUDE.md removal audit
grep -rn "CLAUDE.md" qgsd-core/workflows/ agents/ commands/ 2>/dev/null | grep -v "^Binary" || echo "CLEAN"
```
Expected: quick.md count >= 4; grep returns "CLEAN" or no output.

**Final npm test run:**
```bash
npm test
```
Expected: exits 0, all tests pass including the three new test files from Plans 01 and 02.

Report the final test count and any failures. If any test fails, diagnose and fix before completing.
  </action>
  <verify>
    `npm test` exits 0. All four IMPR requirement checks pass per the verification steps above.
  </verify>
  <done>All four IMPR requirements are verified closed: IMPR-01 (slot-worker tests passing), IMPR-02 (signal emission tests passing), IMPR-03 (plan-phase R3.6 loop installed), IMPR-04 (quick R3.6 loop installed + zero CLAUDE.md references). npm test exits 0.</done>
</task>

</tasks>

<verification>
1. `grep -c "improvement_iteration" ~/.claude/qgsd/workflows/plan-phase.md` — returns >= 4
2. `grep -c "improvement_iteration" ~/.claude/qgsd/workflows/quick.md` — returns >= 4
3. `grep -rn "CLAUDE.md" ~/.claude/qgsd/workflows/ ~/.claude/agents/ 2>/dev/null` — no output
4. `npm test` — exits 0, all tests pass including gsd-quorum-slot-worker-improvements, quorum-improvements-signal, and claude-md-references tests
</verification>

<success_criteria>
- Installed plan-phase.md has R3.6 loop (improvement_iteration present)
- Installed quick.md has R3.6 loop (improvement_iteration present)
- Zero CLAUDE.md file-read references in installed workflows and agents
- npm test exits 0 — all IMPR test coverage passes
- IMPR-01, IMPR-02, IMPR-03, IMPR-04 all verified closed
</success_criteria>

<output>
After completion, create `.planning/phases/v0.19-06-r3-6-improvement-iteration/v0.19-06-03-SUMMARY.md`
</output>
