---
phase: v0.10-02-presets-and-cloning
verified: 2026-02-25T00:00:00Z
status: human_needed
score: 2/4 automated truths verified
human_verification:
  - test: "Preset selector in addAgent/editAgent flow"
    expected: "Base URL step shows AkashML / Together.xyz / Fireworks.ai / Custom options via inquirer list prompt; selecting a preset auto-fills URL without manual typing"
    why_human: "TTY-interactive inquirer list prompt — cannot drive programmatically"
  - test: "Probe retry/cancel loop on provider failure"
    expected: "If probe fails (bad URL), user sees error and retry/cancel prompt — no partial slot written on cancel"
    why_human: "Requires live network probe failure simulation; no programmatic control"
  - test: "Clone slot flow — new name, no key copied"
    expected: "Clone slot option copies provider URL and model config to new slot name; new slot shows [no key]; original key is not copied to clone"
    why_human: "Requires real ~/.claude.json write + keytar operations"
  - test: "Keytar isolation — clone keytar account is independent"
    expected: "Original slot's keytar account is never read or written during clone; clone starts with no key configured"
    why_human: "Requires keytar runtime + real keychain state"
---

# Phase v0.10-02: Presets and Cloning Verification Report

**Phase Goal:** Users can select a named provider preset (AkashML / Together.xyz / Fireworks.ai) in addAgent/editAgent instead of manually typing a URL, and can clone an existing slot with keytar isolation
**Verified:** 2026-02-25T00:00:00Z
**Status:** human_needed (automated checks PASSED for pure functions; 4 interactive flows require human TTY testing)
**Re-verification:** No — initial verification (retroactive, created in v0.10-07-03)

---

## Goal Achievement

### Observable Truths (Derived from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Preset selector replaces free-text URL input in addAgent() and editAgent() | ? NEEDS HUMAN (code wired) | `buildPresetChoices()` at lines ~250-260 returns inquirer list choices for AkashML / Together.xyz / Fireworks.ai / Custom; wired into `addAgent()` and `editAgent()` at commit 146e751 |
| 2 | Probe preflight runs before slot write — no "Save anyway" escape | ? NEEDS HUMAN (code wired) | `probeWithRetryOrCancel()` placed after `probeProviderUrl()` (commit 146e751) replaces the saveAnyway confirm; loops until healthy or user cancels; no partial slot on cancel |
| 3 | Clone slot option copies provider/model config without copying API key | ? NEEDS HUMAN (code wired) | `buildCloneEntry()` at commit cfacfce explicitly excludes `ANTHROPIC_API_KEY`; copies only `ANTHROPIC_BASE_URL`, `CLAUDE_DEFAULT_MODEL`, `CLAUDE_MCP_TIMEOUT_MS`, `PROVIDER_SLOT`; `cloneSlot()` at commit a61facc uses `buildCloneEntry()` as sole copying gate |
| 4 | Clone keytar isolation — source slot's keytar account is never read during clone | ? NEEDS HUMAN (code wired) | `cloneSlot()` derives new keytar account from `newName` only; source slot's `keytarAccount` is never passed to `keytar.getPassword()`; see commit a61facc; `buildCloneEntry()` summary states this invariant explicitly |

Automated score: 2/4 pure function implementations verified via 18 unit tests (buildPresetChoices, findPresetForUrl, buildCloneEntry).
Human score: 0/4 interactive flows confirmed in this run. The checkpoint:human-verify gate in SUMMARY-02 was flagged as pending with no recorded approval.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/manage-agents.cjs` `PROVIDER_PRESETS` constant | AkashML / Together.xyz / Fireworks.ai URLs at module scope | VERIFIED | Added at commit cfacfce adjacent to `CCR_CONFIG_PATH` constant; 3 named providers + Custom sentinel |
| `bin/manage-agents.cjs` `buildPresetChoices()` | Pure function returning inquirer list choices | VERIFIED | Commit cfacfce; exported via `_pure`; returns 3 preset entries + Separator + `__custom__` |
| `bin/manage-agents.cjs` `findPresetForUrl()` | Pure function — reverse-map URL to preset value | VERIFIED | Commit cfacfce; exported via `_pure`; falls back to `__custom__` for unknown/null/undefined |
| `bin/manage-agents.cjs` `buildCloneEntry()` | Pure function — clone config without ANTHROPIC_API_KEY | VERIFIED | Commit cfacfce; exported via `_pure`; ANTHROPIC_API_KEY exclusion is structurally enforced (field not listed) |
| `bin/manage-agents.cjs` `probeWithRetryOrCancel()` | Async retry/cancel loop | VERIFIED (code only) | Commit 146e751; replaces `saveAnyway` confirm in both `addAgent()` and `editAgent()` |
| `bin/manage-agents.cjs` `cloneSlot()` | Async clone flow with keytar isolation | VERIFIED (code only) | Commit a61facc; uses `buildCloneEntry()` exclusively; new keytar account from `newName` only |
| `bin/manage-agents.test.cjs` | Unit tests for 3 pure functions | VERIFIED | Commits 2dea655 (18 RED stubs) + cfacfce (18 GREEN); test suite: 66 tests total after this phase |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `addAgent()` | `buildPresetChoices()` | inquirer list prompt for base URL | WIRED | Commit 146e751; free-text input replaced by preset list |
| `editAgent()` | `findPresetForUrl()` + `buildPresetChoices()` | pre-selected current preset | WIRED | Commit 146e751; `findPresetForUrl(currentUrl)` passed to inquirer default |
| `addAgent()` / `editAgent()` | `probeWithRetryOrCancel()` | before slot write — blocks on failure | WIRED | Commit 146e751; `saveAnyway` confirm removed; loop returns false on cancel (no write) |
| `mainMenu()` | `cloneSlot()` | `action === 'clone-slot'` dispatch | WIRED | Commit a61facc; "3. Clone slot" entry with dispatch handler |
| `cloneSlot()` | `buildCloneEntry()` | sole entry builder — gates what fields are copied | WIRED | Commit a61facc; `buildCloneEntry(sourceName, newName, sourceEntry)` is the single copying gate |
| `buildCloneEntry()` | `writeClaudeJson()` | new slot mcpServer entry | WIRED | `cloneSlot()` calls `buildCloneEntry()` → `writeClaudeJson()`; no keytar read from source |

---

### Requirements Coverage

| Requirement | Derived From | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PRST-01 | ROADMAP v0.10-02 SC1 + SC2 | Preset selector replaces free-text URL; probe preflight before slot write | SATISFIED (code) / NEEDS HUMAN (UI) | `buildPresetChoices()` wired into `addAgent()`/`editAgent()`; `probeWithRetryOrCancel()` replaces `saveAnyway`; commits 2dea655, cfacfce, 146e751 |
| PRST-02 | ROADMAP v0.10-02 SC3 + SC4 | Clone slot with keytar isolation; new slot shows [no key]; source key unaffected | SATISFIED (code) / NEEDS HUMAN (UI) | `buildCloneEntry()` excludes `ANTHROPIC_API_KEY`; `cloneSlot()` never reads source keytar account; commits 2dea655, cfacfce, a61facc |

No orphaned requirements — both PRST-01 and PRST-02 are claimed by all two plans and have corresponding implementation.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| N/A | — | No TODO/FIXME/placeholder in implementing code | None | — |
| N/A | — | No stub `return null`/`return {}` in integration functions | None | — |
| N/A | — | `buildCloneEntry()` structurally excludes ANTHROPIC_API_KEY — no conditional guard needed | None | — |

---

### Human Verification Required

The following human tests must be confirmed with a real TTY:

#### 1. Preset Selector — addAgent Flow (PRST-01)

**Test:** Run `node bin/manage-agents.cjs`; select "2. Add agent"; enter a test slot name (e.g., "test-preset-1").
**Expected:** Next prompt is an inquirer LIST showing "AkashML (api.akashml.com/v1)", "Together.xyz (api.together.xyz/v1)", "Fireworks.ai (api.fireworks.ai/inference/v1)", and "Custom...". Selecting AkashML auto-fills the URL without any typing required; probe runs automatically.
**Why human:** TTY-interactive inquirer `list` prompt with arrow-key selection — cannot drive programmatically.

#### 2. Probe Retry/Cancel Loop (PRST-01)

**Test:** Select "2. Add agent", enter test slot name, select "Custom", enter `https://invalid-host-does-not-exist.example.com/v1`.
**Expected:** After probe timeout/failure, options shown are "Retry probe" and "Cancel — do not write slot" (NOT a "Save anyway" option). Selecting Cancel results in no new slot being written.
**Why human:** Requires live network probe failure simulation; loop requires user input for each iteration.

#### 3. Clone Slot Flow (PRST-02)

**Test:** Select "3. Clone slot"; select an existing configured slot; enter new name (e.g., "test-clone-1"); answer No to API key prompt.
**Expected:** Success confirmation shows "[no key]". Running "1. List agents" confirms new slot appears with "[no key]" badge. Original slot is unchanged.
**Why human:** Requires real `~/.claude.json` write + keytar operations.

#### 4. Keytar Isolation (PRST-02)

**Test:** Clone a slot that has a key configured. Confirm the clone prompt asks for a NEW key (does not auto-populate from the source slot's keytar entry). Skip the key for the clone. Confirm the source slot's key is unaffected (check with "7. Check agent health" on the source slot).
**Why human:** Requires keytar runtime + real keychain state; isolation can only be confirmed by observing the absence of source-key propagation.

---

### Commits Verified

The following commits are referenced in SUMMARY files and confirmed present in `git log`:

| Commit | Description | Plan |
|--------|-------------|------|
| `2dea655` | test(v0.10-02-01): add failing stubs for buildPresetChoices, findPresetForUrl, buildCloneEntry | 01 |
| `cfacfce` | feat(v0.10-02-01): implement buildPresetChoices, findPresetForUrl, buildCloneEntry | 01 |
| `146e751` | feat(v0.10-02-02): add probeWithRetryOrCancel + preset selector in addAgent/editAgent | 02 |
| `a61facc` | feat(v0.10-02-02): add cloneSlot + wire Clone slot into mainMenu | 02 |

---

## Summary

Phase v0.10-02 goal achievement is **structurally complete**. Every component required by PRST-01 and PRST-02 is:

1. Implemented with substantive, non-stub code in `bin/manage-agents.cjs`
2. Wired end-to-end (preset selector → probeWithRetryOrCancel → write; clone selector → buildCloneEntry → writeClaudeJson)
3. Tested with 18 passing unit tests for the three pure functions (buildPresetChoices, findPresetForUrl, buildCloneEntry)
4. Committed with traceable commit hashes (4 commits across 2 plans)

The `human_needed` status reflects that 4 TTY-interactive flows (preset selector display, probe retry/cancel loop, clone-slot flow, keytar isolation) cannot be verified programmatically without a terminal driver. The SUMMARY-02 human-verify checkpoint was flagged as pending; no approval was recorded in the audit trail.

---

_Verified: 2026-02-25T00:00:00Z_
_Verifier: Claude (qgsd-verifier, v0.10-07-03)_
