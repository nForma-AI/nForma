---
phase: v0.10-02-presets-and-cloning
plan: 01
subsystem: ui
tags: [inquirer, manage-agents, provider-presets, tdd, pure-functions]

# Dependency graph
requires:
  - phase: v0.10-01-foundation
    provides: readQgsdJson, writeQgsdJson, and display helper pure functions + _pure export pattern
provides:
  - PROVIDER_PRESETS constant hardcoding AkashML, Together.xyz, Fireworks.ai URLs
  - buildPresetChoices() — builds inquirer list choices for provider selector
  - findPresetForUrl() — reverse lookup URL to preset value or '__custom__'
  - buildCloneEntry() — builds cloned slot env without ANTHROPIC_API_KEY
affects:
  - v0.10-02-02 (Plan 02 consumes all three pure functions)

# Tech tracking
tech-stack:
  added: []
  patterns: [TDD wave 0/1, pure functions exported via _pure, PROVIDER_PRESETS constant]

key-files:
  created: []
  modified:
    - bin/manage-agents.cjs
    - bin/manage-agents.test.cjs

key-decisions:
  - "PROVIDER_PRESETS is a hardcoded constant at module scope (adjacent to CCR_CONFIG_PATH constant) — user-extensible presets deferred to v0.11+"
  - "buildPresetChoices() uses inquirer.Separator() between presets and Custom — matches inquirer list UX pattern"
  - "buildCloneEntry() explicitly excludes ANTHROPIC_API_KEY (keytar isolation) — only copies ANTHROPIC_BASE_URL, CLAUDE_DEFAULT_MODEL, CLAUDE_MCP_TIMEOUT_MS, and overrides PROVIDER_SLOT"
  - "Removed redundant second _pure destructure at line 219 in test file — merged into unified top-level destructure per plan spec"

patterns-established:
  - "Pure function pattern: domain logic extracted into stateless functions, exported via _pure for testability"
  - "Provider preset table pattern: { name, value, label } entries with buildPresetChoices() for inquirer rendering"

requirements-completed:
  - PRST-01
  - PRST-02

# Metrics
duration: 12min
completed: 2026-02-24
---

# Phase v0.10-02-01: Pure Helper Functions Summary

**PROVIDER_PRESETS constant + buildPresetChoices, findPresetForUrl, buildCloneEntry pure functions implemented via TDD (18 new tests green)**

## Performance

- **Duration:** 12 min
- **Started:** 2026-02-24T00:00:00Z
- **Completed:** 2026-02-24T00:12:00Z
- **Tasks:** 2 (Wave 0 stubs + Wave 1 implementation)
- **Files modified:** 2

## Accomplishments
- Added `PROVIDER_PRESETS` constant with exact URLs from MEMORY.md Provider Map (AkashML, Together.xyz, Fireworks.ai)
- `buildPresetChoices()` returns inquirer list choices: 3 presets + Separator + Custom `__custom__` sentinel
- `findPresetForUrl()` reverse-maps URLs to preset values, falls back to `__custom__` for unknown/null/undefined
- `buildCloneEntry()` builds cloned slot config copying provider/model env vars, explicitly excluding `ANTHROPIC_API_KEY`
- All 3 functions exported via `module.exports._pure`
- Test suite grew from 48 → 66 tests (18 new), all green

## Task Commits

1. **Task 1 (Wave 0): Failing test stubs** - `2dea655` (test)
2. **Task 2 (Wave 1): Implementation** - `cfacfce` (feat)

## Files Created/Modified
- `bin/manage-agents.cjs` - Added PROVIDER_PRESETS constant + 3 pure functions + updated _pure export
- `bin/manage-agents.test.cjs` - Added 18 test cases; merged duplicate _pure destructure into unified top-level destructure

## Decisions Made
- Placed `PROVIDER_PRESETS` constant adjacent to `CCR_CONFIG_PATH` at top of file for discoverability
- Merged redundant second destructure (line 219) into the top-level destructure — plan spec implied this organization

## Deviations from Plan

None - plan executed exactly as written. One minor structural fix: the test file had a second `const { slotToFamily, ... } = _pure` destructure at line 219 that was made redundant by adding the new names to the top-level destructure. Removed the duplicate to avoid redeclaration.

## Issues Encountered
None

## Next Phase Readiness
- All 3 pure functions ready for Plan 02 integration
- `buildPresetChoices()` ready to replace free-text base URL input in `addAgent()` and `editAgent()`
- `buildCloneEntry()` ready for use in `cloneSlot()` function
- Full test suite (260 total) green

---
*Phase: v0.10-02-presets-and-cloning*
*Completed: 2026-02-24*
