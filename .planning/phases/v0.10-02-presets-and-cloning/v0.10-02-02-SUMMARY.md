---
phase: v0.10-02-presets-and-cloning
plan: 02
subsystem: ui
tags: [inquirer, manage-agents, provider-presets, clone-slot, probe-retry]

# Dependency graph
requires:
  - phase: v0.10-02-01
    provides: buildPresetChoices, findPresetForUrl, buildCloneEntry pure functions
provides:
  - probeWithRetryOrCancel() — retry/cancel loop replacing saveAnyway confirm
  - preset selector in addAgent() replacing free-text ANTHROPIC_BASE_URL input
  - preset selector in editAgent() with pre-selection from current URL
  - cloneSlot() — full clone flow with keytar isolation
  - mainMenu() "3. Clone slot" entry with dispatch handler
affects:
  - future phases modifying manage-agents.cjs

# Tech tracking
tech-stack:
  added: []
  patterns: [probeWithRetryOrCancel retry loop, preset selector UX pattern, clone-with-keytar-isolation]

key-files:
  created: []
  modified:
    - bin/manage-agents.cjs

key-decisions:
  - "saveAnyway confirm fully removed from both addAgent() and editAgent() — replaced by probeWithRetryOrCancel() loop"
  - "cloneSlot() uses buildCloneEntry() (pure function) — no direct env copy, ANTHROPIC_API_KEY excluded structurally"
  - "cloneSlot() reads source slot config but NEVER reads source keytar key — new keytarAccount derived only from newName"
  - "mainMenu renumbered: Clone slot=3, Edit=4, Remove=5, Reorder=6, Health=7, AddSub=8, EditSub=9, CCR=10, Update=11"
  - "editAgent() preset list includes (remove base URL) option as first choice with __remove__ sentinel"

patterns-established:
  - "probeWithRetryOrCancel pattern: caller passes URL + key, function loops internally, returns bool — caller returns on false"
  - "Preset selector UX: list with 3 named providers + Separator + Custom escape hatch with follow-up input"
  - "Clone isolation pattern: buildCloneEntry() is the single gating function for what fields are copied to new slot"

requirements-completed:
  - PRST-01
  - PRST-02

# Metrics
duration: 15min
completed: 2026-02-24
---

# Phase v0.10-02-02: Integration Tasks Summary

**Provider preset selector + probe retry/cancel loop + clone slot flow wired into manage-agents.cjs interactive flows**

## Performance

- **Duration:** 15 min
- **Started:** 2026-02-24T00:12:00Z
- **Completed:** 2026-02-24T00:27:00Z
- **Tasks:** 2 automated + 1 human-verify checkpoint
- **Files modified:** 1

## Accomplishments
- `probeWithRetryOrCancel()` added after `probeProviderUrl()` — loops until healthy or user cancels, no "Save anyway" escape
- `addAgent()` now uses `buildPresetChoices()` list prompt for base URL; free-text input removed
- `editAgent()` now uses `buildPresetChoices()` list with `findPresetForUrl()` pre-selection; remove option added; `saveAnyway` removed
- `cloneSlot()` implemented: source selector → new name input → `buildCloneEntry()` → optional key prompt (keytar-isolated) → write
- "3. Clone slot" entry wired into `mainMenu()` with full renumbering (old 3→4, 4→5, etc.)
- All automated tests green: 66 unit tests + 260 full suite

## Task Commits

1. **Task 1: probeWithRetryOrCancel + preset selector** - `146e751` (feat)
2. **Task 2: cloneSlot + mainMenu wiring** - `a61facc` (feat)

## Files Created/Modified
- `bin/manage-agents.cjs` - probeWithRetryOrCancel, preset selector in addAgent/editAgent, cloneSlot, mainMenu wiring

## Decisions Made
- `editAgent()` preset list includes `(remove base URL)` as first option with `__remove__` sentinel — keeps parity with original blank=remove behavior
- `probeWithRetryOrCancel()` placed immediately after `probeProviderUrl()` (line 144) for locality

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## Human Verification Gate (checkpoint:human-verify)

**Pending** — automated tasks complete. Manual verification required:

Run: `node bin/manage-agents.cjs`

**Test 1 — Preset selector (PRST-01):**
1. Select "2. Add agent"
2. Enter a test slot name (e.g. "test-preset-1")
3. Confirm the next prompt is a LIST showing AkashML, Together.xyz, Fireworks.ai, and Custom
4. Select AkashML — confirm no URL typing required; probe runs automatically

**Test 2 — Probe retry/cancel (PRST-01):**
1. Select "Add agent", enter test slot name, select "Custom", enter "https://invalid-host-does-not-exist.example.com/v1"
2. After probe timeout, confirm options are "Retry probe" and "Cancel — do not write slot" (NOT "Save anyway")
3. Select Cancel — confirm no slot is written

**Test 3 — Clone slot (PRST-02):**
1. Select "3. Clone slot"
2. Select an existing slot, enter new name (e.g. "test-clone-1")
3. Answer No to API key prompt
4. Confirm success shows "[no key]"
5. "1. List agents" — confirm new slot appears with [no key]

**Test 4 — Keytar isolation (PRST-02):**
1. Clone a slot that has a key configured
2. Confirm clone prompt asks for NEW key (does not auto-populate from source)
3. Skip key — confirm source slot key unaffected

## Next Phase Readiness
- PRST-01 and PRST-02 automation-complete; pending human-verify checkpoint approval
- After checkpoint approval, phase v0.10-02 is fully complete

---
*Phase: v0.10-02-presets-and-cloning*
*Completed: 2026-02-24*
