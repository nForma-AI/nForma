---
phase: v0.30-01-dynamic-model-selection
plan: 01
subsystem: hooks
tags: [task-classifier, thinking-budget, model-routing, quorum-dispatch]

# Dependency graph
requires: []
provides:
  - "Task complexity classifier (bin/task-classifier.cjs) with classifyTask, getModelRecommendation, readTaskEnvelope, COMPLEXITY_MAP"
  - "Thinking budget scaling config keys in DEFAULT_CONFIG"
  - "THINKING BUDGET directive injection in quorum dispatch"
  - "TIER_SLOT_MAP slot filtering in nf-prompt.js"
affects: [v0.30-01-03-anti-oscillation, quorum-dispatch]

# Tech tracking
tech-stack:
  added: []
  patterns: [task-complexity-classification, tier-based-slot-filtering, thinking-budget-injection]

key-files:
  created:
    - bin/task-classifier.cjs
    - bin/task-classifier.test.cjs
  modified:
    - hooks/config-loader.js
    - hooks/dist/config-loader.js
    - hooks/nf-prompt.js
    - hooks/dist/nf-prompt.js
    - .gitignore

key-decisions:
  - "Slot filtering uses TIER_SLOT_MAP with >= 2 slot minimum to preserve quorum diversity"
  - "Classification inserted after DISP-03 sort and before cache check to affect both cached key computation and slot dispatch"
  - "Thinking budget directive appended to instructions after model overrides but before command pattern check"

patterns-established:
  - "Task classification fail-open: null envelope defaults to moderate, missing classifier does not crash nf-prompt.js"
  - "TIER_SLOT_MAP mapping: haiku->budget slots, sonnet->budget+codex, opus->claude+codex"
  - "COMPLEXITY_BUDGET_KEY mapping: trivial->exploration, simple->review, complex->architecture (moderate has no override)"

requirements-completed: [TOKN-02, TOKN-04]

# Metrics
duration: 3min
completed: 2026-03-07
---

# Phase v0.30-01 Plan 01: Task Classifier and Thinking Budget Summary

**Task complexity classifier routing trivial/simple/moderate/complex tasks to model tiers with THINKING BUDGET injection and TIER_SLOT_MAP slot filtering in quorum dispatch**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-07T16:29:26Z
- **Completed:** 2026-03-07T16:32:30Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Created bin/task-classifier.cjs with 4-level complexity classification (trivial/simple/moderate/complex) and model tier recommendations
- Added thinking_budget_scaling, model_routing_enabled, model_routing_cooldown_rounds, model_routing config keys to DEFAULT_CONFIG
- Integrated classifier into nf-prompt.js quorum dispatch: THINKING BUDGET directive + TIER_SLOT_MAP slot filtering with >= 2 minimum
- 21 unit tests passing for classifier; 25 nf-prompt tests passing (no regressions)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create task complexity classifier with config keys** - `008559e0` (feat)
2. **Task 2: Integrate classifier into nf-prompt.js quorum dispatch with slot filtering** - `b41ef411` (feat)

## Files Created/Modified
- `bin/task-classifier.cjs` - Task complexity classifier with classifyTask, getModelRecommendation, readTaskEnvelope, COMPLEXITY_MAP
- `bin/task-classifier.test.cjs` - 21 unit tests covering all classification rules and config overrides
- `hooks/config-loader.js` - Added thinking_budget_scaling, model_routing_enabled, model_routing_cooldown_rounds, model_routing to DEFAULT_CONFIG
- `hooks/dist/config-loader.js` - Synced copy
- `hooks/nf-prompt.js` - Integrated classifier with THINKING BUDGET directive, TIER_SLOT_MAP slot filtering, task-classification.json output
- `hooks/dist/nf-prompt.js` - Synced copy
- `.gitignore` - Added .planning/task-classification.json exclusion

## Decisions Made
- Slot filtering uses TIER_SLOT_MAP with >= 2 slot minimum to preserve quorum diversity -- if filtering would leave fewer than 2 slots, falls back to all activeSlots
- Classification inserted after DISP-03 sort and before cache check so filtered slot list affects cache key computation
- _nfClassification stored via `var` for access in outer scope (thinking budget injection happens after instructions are fully built)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing test failures: hooks-sync verification (missing nf-post-edit-format.js and nf-console-guard.js in HOOKS_TO_COPY), config-loader HPM-TC3/HPM-TC4 (hook count 12 vs expected 10). Both confirmed pre-existing via git stash test.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Classifier and injection are in place; Plan 03 (anti-oscillation cooldown) can build on model_routing_cooldown_rounds config key
- TOKN-04 is partial until Plan 03 delivers the cooldown safety net

---
*Phase: v0.30-01-dynamic-model-selection*
*Completed: 2026-03-07*

## Self-Check: PASSED
