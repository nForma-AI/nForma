---
phase: 31-merge-gen2-branches-and-phase24-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - .planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STD-02

must_haves:
  truths:
    - ".planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md exists with status: passed"
    - "REQUIREMENTS.md v0.4 Requirements (Complete) section contains [x] STD-02 entry"
    - "REQUIREMENTS.md traceability table has STD-02 row with status Complete"
  artifacts:
    - path: ".planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md"
      provides: "Phase 24 verification gate confirming all 4 repos ported to Gen2"
      contains: "passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements registry with STD-02 marked complete"
      contains: "STD-02"
  key_links:
    - from: "24-VERIFICATION.md status"
      to: "STD-02 Complete in REQUIREMENTS.md"
      via: "Phase 31 gap closure"
      pattern: "passed"
---

<objective>
Create Phase 24 VERIFICATION.md confirming all 4 Gen1→Gen2 ports are complete and production-stable (codex and copilot now on main after Plan 31-01). Mark STD-02 as complete in REQUIREMENTS.md.

Purpose: Phase 24 ran all 4 plans and produced SUMMARY.md files but the verification gate was never triggered. This plan closes that gap by retrospectively verifying the Gen2 architecture is in place across all 4 repos and recording the result.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Verify Gen2 architecture on all 4 repos and create 24-VERIFICATION.md</name>
  <files>.planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md</files>
  <action>
Run the following checks on each of the 4 repos to gather evidence for the VERIFICATION.md:

**claude-mcp-server:**
```bash
ls /Users/jonathanborduas/code/claude-mcp-server/src/tools/
# Expected: registry.ts, claude.tool.ts, review.tool.ts, simple-tools.ts, index.ts
cd /Users/jonathanborduas/code/claude-mcp-server && git log --oneline -1
```

**codex-mcp-server (now on main after Plan 31-01):**
```bash
ls /Users/jonathanborduas/code/codex-mcp-server/src/tools/
# Expected: registry.ts, codex.tool.ts, review.tool.ts, simple-tools.ts, index.ts
cd /Users/jonathanborduas/code/codex-mcp-server && git log --oneline -1 && git branch --show-current
```

**copilot-mcp-server (now on main after Plan 31-01):**
```bash
ls /Users/jonathanborduas/code/copilot-mcp-server/src/tools/
# Expected: registry.ts, ask.tool.ts, explain.tool.ts, suggest.tool.ts, simple-tools.ts, index.ts
cd /Users/jonathanborduas/code/copilot-mcp-server && git log --oneline -1 && git branch --show-current
```

**openhands-mcp-server:**
```bash
ls /Users/jonathanborduas/code/openhands-mcp-server/src/tools/
# Expected: registry.ts, review.tool.ts, simple-tools.ts, index.ts
cd /Users/jonathanborduas/code/openhands-mcp-server && git log --oneline -1
```

Also verify no Gen1 files remain on any repo:
```bash
ls /Users/jonathanborduas/code/claude-mcp-server/src/tools/definitions.ts 2>/dev/null && echo "FAIL: Gen1 file present" || echo "PASS: No Gen1 definitions.ts"
ls /Users/jonathanborduas/code/codex-mcp-server/src/tools/definitions.ts 2>/dev/null && echo "FAIL: Gen1 file present" || echo "PASS: No Gen1 definitions.ts"
ls /Users/jonathanborduas/code/copilot-mcp-server/src/tools/definitions.ts 2>/dev/null && echo "FAIL: Gen1 file present" || echo "PASS: No Gen1 definitions.ts"
ls /Users/jonathanborduas/code/openhands-mcp-server/src/tools/definitions.ts 2>/dev/null && echo "FAIL: Gen1 file present" || echo "PASS: No Gen1 definitions.ts"
```

Write the verification results to `.planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md` with the following structure:

```markdown
---
phase: 24
status: passed
verified: 2026-02-22
verifier: qgsd-executor (Phase 31 gap closure)
requirements: [STD-02]
---

# Phase 24 Verification: Gen1→Gen2 Architecture Port

## Goal

All 4 Gen1 MCP server repos (claude, codex, copilot, openhands) ported to Gen2 per-tool architecture with registry.ts dispatch. STD-02 is production-stable.

## Success Criteria

| # | Criterion | Result |
|---|-----------|--------|
| 1 | claude-mcp-server main: registry.ts + per-tool files present, no definitions.ts | PASS |
| 2 | codex-mcp-server main: registry.ts + per-tool files present, no definitions.ts | PASS |
| 3 | copilot-mcp-server main: registry.ts + per-tool files present, no definitions.ts | PASS |
| 4 | openhands-mcp-server main: registry.ts + per-tool files present, no definitions.ts | PASS |

## Evidence

[Fill in the actual git log output and file listings from the bash commands above]

## Test Results from Phase 24 Plans

| Repo | Tests | Result | Plan |
|------|-------|--------|------|
| claude-mcp-server | 62 | PASSED | 24-01 (commit fd4fbcc on main) |
| codex-mcp-server | 77 | PASSED | 24-02 (commit 568a199, now on main via Phase 31) |
| copilot-mcp-server | 58 | PASSED | 24-03 (commit a091ae6, now on main via Phase 31) |
| openhands-mcp-server | 13 | PASSED | 24-04 (commit 7a9040b on main) |

## Requirements Traceability

| Requirement | Satisfied | Evidence |
|-------------|-----------|----------|
| STD-02 | Yes | All 4 repos have Gen2 src/tools/registry.ts on main branch |

## Gap Closure Note

Phase 24 ran all 4 plans and produced SUMMARY.md files but the verification gate was not triggered at the time. Additionally, codex-mcp-server and copilot-mcp-server Gen2 work was on feature branches (fix/progress-after-done and feat/02-error-handling-and-resilience respectively). Phase 31 Plan 01 merged both to main before this verification ran.

## Verdict

**PASSED** — STD-02 satisfied. All 4 repos have Gen2 architecture on main branch.
```

Fill in the actual evidence from the bash command outputs before writing the file.
  </action>
  <verify>
```bash
grep "^status:" /Users/jonathanborduas/code/QGSD/.planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md
```
Expected: `status: passed`

```bash
grep "PASSED" /Users/jonathanborduas/code/QGSD/.planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md | wc -l
```
Expected: at least 5 lines (4 criteria + overall verdict)
  </verify>
  <done>24-VERIFICATION.md exists with status: passed, covering all 4 repos. All Gen2 architecture criteria verified with real filesystem evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add STD-02 to REQUIREMENTS.md v0.4 Complete section</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md`. Locate the `## v0.4 Requirements (Complete)` section. It currently has:
- `### Observability (OBS)` with OBS-01–04
- `### Standardization (STD)` with STD-10

Add STD-02 to the Standardization (STD) subsection:

```markdown
- [x] **STD-02**: All 4 Gen1 MCP server repos (claude, codex, copilot, openhands) ported to Gen2 per-tool architecture (`*.tool.ts` + `registry.ts`) with Gen1 files (`definitions.ts`, `handlers.ts`) removed — all repos on main branch
```

Insert it BEFORE the STD-10 entry (STD-02 comes before STD-10 numerically).

Also update the Traceability table. Add a row for STD-02 AFTER the OBS-04 row and BEFORE the STD-10 row:

```
| STD-02 | Phase 24 + Phase 31 (gap closure) | Complete |
```

Also update the Coverage block:
- Change `v0.4 requirements completed: 5 (OBS-01–04, STD-10)` to `v0.4 requirements completed: 6 (OBS-01–04, STD-02, STD-10)`
- Update `Last updated:` line to: `*Last updated: 2026-02-22 — STD-02 marked Complete (Phase 31 gap closure)*`

Do NOT modify v0.5 requirements or any other section.
  </action>
  <verify>
```bash
grep -n "STD-02" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
```
Expected: at least 3 matching lines — one in the v0.4 Complete section checkbox, one in the traceability table, one in the coverage note.
  </verify>
  <done>REQUIREMENTS.md v0.4 Requirements (Complete) section contains [x] STD-02 in the Standardization (STD) subsection. Traceability table has STD-02 row with status Complete. Coverage count reflects 6 completed v0.4 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Commit Phase 24 VERIFICATION.md and updated REQUIREMENTS.md</name>
  <files>.planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Stage and commit both files:

```bash
git -C /Users/jonathanborduas/code/QGSD add \
  .planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md \
  .planning/REQUIREMENTS.md
git -C /Users/jonathanborduas/code/QGSD commit -m "feat(phase-31): create Phase 24 VERIFICATION.md, mark STD-02 complete"
```
  </action>
  <verify>
```bash
git -C /Users/jonathanborduas/code/QGSD log --oneline -1
```
Expected: commit message contains "phase-31" and "STD-02"
  </verify>
  <done>24-VERIFICATION.md and REQUIREMENTS.md committed to QGSD repo.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Phase 24 VERIFICATION.md status:
   ```bash
   grep "^status:" .planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md
   ```
   Expected: `status: passed`

2. STD-02 in REQUIREMENTS.md:
   ```bash
   grep "STD-02" .planning/REQUIREMENTS.md
   ```
   Expected: 3+ lines (checkbox, traceability, coverage)

3. All 4 repos have Gen2 on main:
   ```bash
   ls /Users/jonathanborduas/code/claude-mcp-server/src/tools/registry.ts
   ls /Users/jonathanborduas/code/codex-mcp-server/src/tools/registry.ts
   ls /Users/jonathanborduas/code/copilot-mcp-server/src/tools/registry.ts
   ls /Users/jonathanborduas/code/openhands-mcp-server/src/tools/registry.ts
   ```
   Expected: all 4 files present
</verification>

<success_criteria>
Plan 31-02 is complete when:
- .planning/phases/24-gen1-to-gen2-architecture-port/24-VERIFICATION.md exists with status: passed
- REQUIREMENTS.md has [x] STD-02 in the v0.4 Requirements (Complete) Standardization (STD) subsection
- STD-02 traceability row shows Phase 24 + Phase 31 gap closure, status Complete
- Coverage count reflects 6 completed v0.4 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/31-merge-gen2-branches-and-phase24-verification/31-02-SUMMARY.md`
</output>
