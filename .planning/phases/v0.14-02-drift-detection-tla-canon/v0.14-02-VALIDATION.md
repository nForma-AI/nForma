---
phase: v0.14-02
slug: drift-detection-tla-canon
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-26
---

# Phase v0.14-02 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.14-02`.
> Governs feedback sampling during `/qgsd:execute-phase v0.14-02`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | none — test files listed explicitly in package.json |
| **Quick run command** | `node --test bin/check-spec-sync.test.cjs bin/xstate-to-tla.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds (full), ~3 seconds (quick) |
| **CI pipeline** | `.github/workflows/formal-verify.yml` — exists |

---

## Nyquist Sampling Rate

- **After every task commit:** Run `node --test bin/check-spec-sync.test.cjs bin/xstate-to-tla.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 15 seconds

---

## Wave 0 Scaffolding (required before Wave 1 implementation)

The following test files must exist before implementation begins:

| File | Status | Action |
|------|--------|--------|
| `bin/check-spec-sync.test.cjs` | Missing | Create with basic "exits 0 when in sync" test |
| `bin/xstate-to-tla.test.cjs` | Exists | No action — add output-path assertion during TASK-00 |
| `bin/run-formal-verify.test.cjs` | Exists | No action |

Wave 0 complete when: `node --test bin/check-spec-sync.test.cjs` exits 0 with at least one passing test.

---

## Per-Task Test Matrix

| Task | Test Type | Test File | What it verifies |
|------|-----------|-----------|-----------------|
| TASK-00: BROKEN-01 output path fix | Unit | `bin/xstate-to-tla.test.cjs` | `--dry` output references `QGSDQuorum_xstate.tla`, not `QGSDQuorum.tla` |
| TASK-01: Wire check-spec-sync into npm test | Integration smoke | `bin/check-spec-sync.test.cjs` | `spawnSync('node', ['bin/check-spec-sync.cjs'])` exits 0 on clean repo |
| TASK-02: AST walk extraction | Unit | `bin/check-spec-sync.test.cjs` | Injecting a drift (removing a state from TLA+ fixture) causes exit 1 |
| TASK-03: Orphaned spec detection | Unit | `bin/check-spec-sync.test.cjs` | A TLA+ state not in XState machine triggers orphan failure |
| TASK-04: CI path triggers | Manual | Visual diff of `formal-verify.yml` | 5 new paths present in both push: and pull_request: blocks |
| TASK-05: Remove continue-on-error | Manual | Visual diff of `formal-verify.yml` | `continue-on-error: true` absent from master runner step |

---

## Wave Structure

| Wave | Tasks | Dependencies |
|------|-------|-------------|
| Wave 1 | TASK-00, TASK-04, TASK-05 | None — these are independent file edits |
| Wave 2 | TASK-01, TASK-02, TASK-03 | Wave 1 TASK-00 (canonical TLA+ model must be resolved first) |

Within Wave 2: TASK-02 (AST extraction) must complete before TASK-03 (orphan detection) since both modify `check-spec-sync.cjs`. TASK-01 (test wrapper) can proceed in parallel with TASK-02/03 or precede them.
