---
phase: v0.40-02-workflow-modifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/nf/solve-diagnose.md
autonomous: true
requirements:
  - ROOT-02
formal_artifacts: none

must_haves:
  truths:
    - "solve-diagnose Step 0f dispatches parallel nf-quorum-slot-worker Tasks on root cause diagnosis"
    - "output_contract includes root_cause_verdict field after quorum completes"
    - "Quorum vote uses existing dispatch pattern from quick.md Step 5.7 (no new infrastructure)"
    - "BLOCK from any external voter halts remediation (CE-2 absolute)"
    - "APPROVE from all valid voters allows remediation to proceed"
    - "Quorum unavailability is handled per tiered fallback (fail-open when all slots UNAVAIL)"
    - "Quorum vote fires AFTER hypothesis measurement (Step 0e) and BEFORE diagnostic sweep (Step 1)"
  artifacts:
    - path: "commands/nf/solve-diagnose.md"
      provides: "Step 0f: root cause quorum vote after hypothesis synthesis"
      contains: "nf-quorum-slot-worker"
  key_links:
    - from: "commands/nf/solve-diagnose.md (Step 0f)"
      to: "quorum.md R3 protocol"
      via: "Mode A artifact review dispatch"
      pattern: "mode: A"
    - from: "commands/nf/solve-diagnose.md (Step 0f)"
      to: "output_contract"
      via: "root_cause_verdict field addition"
      pattern: "root_cause_verdict"
    - from: "commands/nf/solve-diagnose.md (Step 0f)"
      to: "Step 1 diagnostic sweep"
      via: "Gate — BLOCK prevents Step 1 from running"
      pattern: "BLOCK.*halt\\|bail"
---

<objective>
Add a root cause quorum vote step to solve-diagnose.md. A new Step 0f dispatches parallel nf-quorum-slot-worker Tasks to vote on whether the root cause diagnosis (from hypothesis measurement in Step 0e) is sound and complete before the diagnostic sweep in Step 1 proceeds. This prevents incorrect root causes from cascading through all remediation layers.

Purpose: Enforce multi-model consensus on the causal chain before remediation begins. A single model's diagnosis can be wrong; quorum consensus gates high-stakes remediation decisions.
Output: Modified commands/nf/solve-diagnose.md with Step 0f quorum vote, updated output_contract.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanbordumas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.40-02-workflow-modifications/v0.40-02-RESEARCH.md
@commands/nf/solve-diagnose.md
@commands/nf/quorum.md
@core/references/quorum-dispatch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0f root cause quorum vote to solve-diagnose.md</name>
  <files>commands/nf/solve-diagnose.md</files>
  <action>
Read commands/nf/solve-diagnose.md to understand the current step structure. The current steps are:
- Step 0: Legacy migration
- Step 0b: Config audit
- Step 0c: Load observe targets
- Step 0d: Inline observe refresh + debt load
- Step 0e: Hypothesis measurement collection
- Step 1: Initial diagnostic sweep (+ heatmap + classification + FSM detection)

Insert a new **Step 0f: Root Cause Quorum Vote** between Step 0e and Step 1. This step gates the diagnostic sweep on multi-model consensus about the root cause diagnosis.

**New Step 0f content:**

```
### Step 0f: Root Cause Quorum Vote (ROOT-02)

After hypothesis measurement (Step 0e), dispatch a quorum vote on the root cause diagnosis before proceeding to the diagnostic sweep. This prevents incorrect root causes from cascading through remediation layers.

**Skip condition:** If Step 0e produced no hypothesis measurements (hypothesis_measurements is null or empty), skip this step:
```
Log: "Step 0f: No hypothesis measurements — skipping quorum vote (nothing to gate)"
```
Set `root_cause_verdict = "SKIPPED_NO_HYPOTHESES"` and proceed to Step 1.

**Quorum preflight:**

```bash
PREFLIGHT=$(node "$HOME/.claude/nf-bin/quorum-preflight.cjs" --all)
```

Parse PREFLIGHT JSON to get `$MAX_QUORUM_SIZE` and active slot names from `team` keys.

Compute fan-out: `FAN_OUT_COUNT = 3` (medium risk). `$DISPATCH_LIST` = first `FAN_OUT_COUNT - 1` slot names from team keys.

**Build diagnostic context for voters:**

Create a compact diagnostic summary from the hypothesis measurement results (Step 0e output). Include:
- hypothesis_measurements verdicts (VIOLATED/CONFIRMED/UNMEASURABLE counts)
- Top violated hypotheses (if any) with their assumption text and measurement data
- The baseline residual vector summary (if available from a prior run)

Write this context to `.planning/solve/diagnose-hypothesis-log.json` (create directory if needed).

**Dispatch quorum workers:**

For each slot in `$DISPATCH_LIST`, dispatch a parallel `nf-quorum-slot-worker` Task:

```yaml
slot: <slotName>
round: 1
timeout_ms: 30000
repo_dir: <absolute path to project root>
mode: A
question: "Review the hypothesis measurement results and diagnostic context below. Is the root cause diagnosis sound, complete, and ready for the diagnostic sweep and subsequent remediation? Vote APPROVE if the diagnosis is actionable and sufficient. Vote BLOCK if the diagnosis is incomplete, contradictory, or likely to lead remediation astray."
artifact_path: ".planning/solve/diagnose-hypothesis-log.json"
review_context: "This is a diagnosis gate (ROOT-02). Evaluate whether the identified root causes from hypothesis measurement are sufficient and accurate. BLOCK is absolute — any BLOCK prevents the diagnostic sweep from running."
request_improvements: false
```

Dispatch as parallel sibling `nf-quorum-slot-worker` Tasks with `model="haiku", max_turns=100`.

**Evaluate consensus per CE-1/CE-2/CE-3:**
- CE-1: Claude's position is advisory only (not counted in vote tally)
- CE-2: Any BLOCK from a valid (non-UNAVAIL) external voter prevents consensus (BLOCK is absolute)
- CE-3: Consensus = 100% of valid external voters agree (unanimity)

Deliberate up to 10 rounds per R3.3 if votes are split.

**Route on verdict:**

- **APPROVED:** Log `"Step 0f: Root cause diagnosis APPROVED by quorum — proceeding to diagnostic sweep"`. Set `root_cause_verdict = "APPROVED"`. Proceed to Step 1.

- **BLOCKED:** Log `"Step 0f: Root cause diagnosis BLOCKED by quorum — halting remediation"`. Set `root_cause_verdict = "BLOCKED"`. Set `status = "bail"` and `reason = "diagnosis_blocked_by_quorum"`. Return the output_contract immediately — do NOT proceed to Step 1. The orchestrator will handle the blocked diagnosis (log and exit gracefully).

- **ESCALATED:** Log `"Step 0f: Root cause diagnosis ESCALATED — requires human review"`. Set `root_cause_verdict = "ESCALATED"`. Set `status = "bail"` and `reason = "diagnosis_escalated"`. Return the output_contract immediately.

**Fail-open:** If quorum preflight fails (no slots available), or all slots return UNAVAIL, log: `"Step 0f: Quorum unavailable — proceeding without consensus (fail-open)"`. Set `root_cause_verdict = "QUORUM_UNAVAILABLE"`. Proceed to Step 1.
```

**Then update the output_contract** in the `<output_contract>` section at the top of the file. Add `root_cause_verdict` to the JSON schema:

```json
{
  "status": "ok" | "bail" | "error",
  "reason": null | "zero_residual" | "diagnostic_script_failed" | "diagnosis_blocked_by_quorum" | "diagnosis_escalated" | "...",
  "root_cause_verdict": "APPROVED" | "BLOCKED" | "ESCALATED" | "SKIPPED_NO_HYPOTHESES" | "QUORUM_UNAVAILABLE",
  "baseline_residual": { },
  "open_debt": [ ],
  "heatmap": { },
  "issues": { },
  "fsm_candidates": [],
  "targets": null | { },
  "hypothesis_measurements": null | { }
}
```

**Critical constraints:**
- Step 0f MUST be after Step 0e (hypothesis measurement) — it votes on the measurement results
- Step 0f MUST be before Step 1 (diagnostic sweep) — it gates remediation
- BLOCK is absolute per CE-2: never override, rationalize, or suppress a BLOCK vote
- Quorum dispatch uses parallel sibling Task() calls (per R3.2), NOT sequential direct MCP tool calls
- All quorum operations are fail-open when quorum infrastructure is unavailable
- The quorum vote is mandatory for all diagnoses in v0.40-02 (no complexity gate — that is v2 QRUM-01)
  </action>
  <verify>
1. `grep -n 'Step 0f' commands/nf/solve-diagnose.md` — confirms new step exists
2. `grep -n 'ROOT-02' commands/nf/solve-diagnose.md` — confirms requirement traceability
3. `grep -n 'nf-quorum-slot-worker' commands/nf/solve-diagnose.md` — confirms quorum dispatch
4. `grep -n 'root_cause_verdict' commands/nf/solve-diagnose.md` — confirms verdict field
5. `grep -n 'CE-2' commands/nf/solve-diagnose.md` — confirms BLOCK-is-absolute reference
6. `grep -n 'mode: A' commands/nf/solve-diagnose.md` — confirms Mode A dispatch
7. `grep -n 'diagnosis_blocked_by_quorum' commands/nf/solve-diagnose.md` — confirms bail reason
8. `grep -n 'QUORUM_UNAVAILABLE\|fail-open' commands/nf/solve-diagnose.md` — confirms fail-open handling
9. Verify Step 0f is between Step 0e and Step 1: `grep -n 'Step 0[ef]\|Step 1:' commands/nf/solve-diagnose.md` — Step 0f line number should be between Step 0e and Step 1
  </verify>
  <done>
Step 0f exists in commands/nf/solve-diagnose.md between Step 0e (hypothesis measurement) and Step 1 (diagnostic sweep). It dispatches parallel nf-quorum-slot-worker Tasks for root cause quorum vote. BLOCK halts remediation (CE-2 absolute). APPROVE proceeds to diagnostic sweep. Quorum unavailability is fail-open. output_contract includes root_cause_verdict field with all possible values documented.
  </done>
</task>

</tasks>

<verification>
1. commands/nf/solve-diagnose.md contains Step 0f between Step 0e and Step 1
2. Step 0f dispatches parallel nf-quorum-slot-worker Tasks (not sequential MCP calls)
3. BLOCK verdict halts remediation with status "bail" and appropriate reason
4. APPROVE verdict allows Step 1 to proceed
5. Quorum unavailability results in fail-open (proceed without consensus)
6. output_contract includes root_cause_verdict field
7. No existing steps were broken or removed
8. Quorum dispatch follows R3 protocol (Mode A, CE-1/CE-2/CE-3, R3.3 deliberation)
</verification>

<success_criteria>
- solve-diagnose has Step 0f that dispatches quorum vote on root cause after hypothesis measurement (ROOT-02)
- BLOCK from any voter prevents diagnostic sweep and remediation
- APPROVE from all voters allows diagnostic sweep to proceed
- Quorum uses established R3 protocol (Mode A, parallel Task dispatch, CE-2 absolute BLOCK)
- Fail-open when quorum infrastructure is unavailable
- output_contract documents root_cause_verdict field
</success_criteria>

<output>
After completion, create `.planning/phases/v0.40-02-workflow-modifications/v0.40-02-02-SUMMARY.md`
</output>
