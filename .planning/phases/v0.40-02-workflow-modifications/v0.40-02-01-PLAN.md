---
phase: v0.40-02-workflow-modifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/quick.md
  - ~/.claude/nf/workflows/quick.md
autonomous: true
requirements:
  - INTENT-01
  - INTENT-02
  - INTENT-03
formal_artifacts: none

must_haves:
  truths:
    - "/nf:quick derives APPROACH block automatically from task description text (non-modal)"
    - ".claude/scope-contract.json is written with branch name as key and scope patterns as value"
    - "Planner receives APPROACH block in its task context"
    - "Approach derivation is fail-open: Haiku unavailability or parse failure produces a generic fallback contract"
    - "Concurrent quick tasks have independent scope contract entries (branch-keyed, no collision)"
    - "Workflow sync verified: core/workflows/quick.md matches ~/.claude/nf/workflows/quick.md"
  artifacts:
    - path: "core/workflows/quick.md"
      provides: "Step 0.5: approach derivation + scope contract write before planner spawn"
      contains: "scope-contract.json"
    - path: "~/.claude/nf/workflows/quick.md"
      provides: "Synced installed copy of quick.md workflow"
  key_links:
    - from: "core/workflows/quick.md (Step 0.5)"
      to: ".claude/scope-contract.json"
      via: "JSON write with branch name as key"
      pattern: "scope-contract\\.json"
    - from: "core/workflows/quick.md (Step 0.5)"
      to: "core/workflows/quick.md (Step 5 planner spawn)"
      via: "APPROACH block passed as planner context"
      pattern: "APPROACH"
    - from: "core/workflows/quick.md (Step 0.5)"
      to: "Haiku subagent"
      via: "Task() dispatch for approach derivation"
      pattern: "Task\\("
---

<objective>
Add approach declaration and scope contract generation to the `/nf:quick` workflow. A new Step 0.5 derives an APPROACH block (what will be done, what is out of scope) from the task description via a Haiku subagent, writes it to `.claude/scope-contract.json` keyed by branch name, and passes it to the planner as context. This is the scope contract that Phase v0.40-03's scope guard will consume.

Purpose: Eliminate surprise scope creep by making task intent explicit before execution begins. The scope contract is the data source for Phase v0.40-03's scope guard hook.
Output: Modified core/workflows/quick.md with Step 0.5, synced installed copy.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.40-02-workflow-modifications/v0.40-02-RESEARCH.md
@core/workflows/quick.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0.5 (approach derivation + scope contract write) to quick.md</name>
  <files>core/workflows/quick.md</files>
  <action>
Read core/workflows/quick.md to understand the current step structure. Insert a new Step 0.5 between Step 2.5 (handle branching) and Step 3 (create task directory). The step numbering "0.5" is intentional — it executes after branching (so branch name is known) but before directory creation and planner spawn.

**New Step 0.5: Approach Declaration + Scope Contract**

Add the following step after Step 2.5 and before Step 3:

```
**Step 2.7: Derive approach and write scope contract (INTENT-01, INTENT-02, INTENT-03)**

This step is automatic (non-modal per INTENT-03). No user dialog or confirmation.

1. **Derive approach via Haiku subagent:**

Spawn a Haiku subagent to analyze the task description and derive a structured approach:

\```
Task(
  subagent_type="general-purpose",
  model="haiku",
  description="Derive approach for quick task",
  prompt="
You are deriving a task scope contract from a quick task description.

## Task Description
${DESCRIPTION}

## Your Task
Analyze the description and produce a JSON object with:
{
  \"approach\": \"[One sentence: what will be done]\",
  \"out_of_scope\": [\"[item 1]\", \"[item 2]\", ...]
}

Guidelines:
- approach: One sentence, imperative voice, specific outcome
- out_of_scope: Literal items explicitly NOT in scope. Include at least 1-2 items.
- Be conservative — when uncertain, mark something as out-of-scope

Respond with ONLY the JSON object, no markdown fencing or extra text.
"
)
\```

Parse the Haiku response as JSON. Store as `$APPROACH_BLOCK`.

**Fail-open:** If Haiku is unavailable, response is empty, or JSON parsing fails, use fallback:
\```json
{
  "approach": "Complete the task as described",
  "out_of_scope": []
}
\```

Log: `"Step 2.7: Approach derived — ${APPROACH_BLOCK.approach}"`
If fallback was used, log: `"Step 2.7: Approach derivation fell back to generic (Haiku unavailable or parse error)"`

2. **Write scope contract to .claude/scope-contract.json (INTENT-02):**

Determine the branch name: use `$CREATED_BRANCH` if set (from Step 2.5), otherwise use `$current_branch`.

Read existing `.claude/scope-contract.json` if it exists. If it does not exist or is not valid JSON, start with an empty object `{}`. This preserves concurrent quick task entries (keyed by branch name).

Add/update the entry for this task's branch:

\```json
{
  "${branch_name}": {
    "task_id": ${next_num},
    "task_description": "${DESCRIPTION}",
    "approach": "${APPROACH_BLOCK.approach}",
    "out_of_scope": ${JSON.stringify(APPROACH_BLOCK.out_of_scope)},
    "branches_affected": ["${branch_name}"],
    "created_at": "${new Date().toISOString()}",
    "planner_model": "${planner_model}",
    "created_by": "quick-orchestrator"
  }
}
\```

Write the merged object back to `.claude/scope-contract.json`.

**Fail-open:** If the write fails (permission error, disk full), log a warning and proceed. The scope contract is informational in v0.40-02 — the scope guard (v0.40-03) is not yet active.

Log: `"Step 2.7: Scope contract written to .claude/scope-contract.json (key: ${branch_name})"`

3. **Store APPROACH_BLOCK for planner context:**

Store `$APPROACH_BLOCK` for use in Step 5 (planner spawn). The planner prompt in Step 5 must include the approach block so the planner knows the declared scope.
```

**Then modify the Step 5 planner spawn prompt** to include the approach block. In the `<planning_context>` section of the planner Task prompt, add after the `**Description:** ${DESCRIPTION}` line:

```
**Approach (auto-derived, INTENT-01):**
- What: ${APPROACH_BLOCK.approach}
- Out of scope: ${APPROACH_BLOCK.out_of_scope.join(', ')}
```

This ensures the planner receives the declared scope and can plan within it.

**Critical constraints:**
- Step 2.7 MUST execute SYNCHRONOUSLY before Step 3 (directory creation) and Step 5 (planner spawn). The scope contract must exist before execution begins (per Pitfall 4 in RESEARCH.md).
- The scope contract file uses branch name as key to prevent concurrent task collision (per Pitfall 1 in RESEARCH.md).
- Approach derivation is non-modal (INTENT-03): no AskUserQuestion, no confirmation dialog.
- All operations are fail-open: Haiku unavailability, JSON parse failure, file write failure all produce warnings, never abort the quick task.
  </action>
  <verify>
1. `grep -n 'Step 2.7' core/workflows/quick.md` — confirms new step exists
2. `grep -n 'scope-contract.json' core/workflows/quick.md` — confirms contract write reference
3. `grep -n 'APPROACH_BLOCK' core/workflows/quick.md` — confirms approach variable usage
4. `grep -n 'INTENT-01' core/workflows/quick.md` — confirms requirement traceability
5. `grep -n 'INTENT-02' core/workflows/quick.md` — confirms requirement traceability
6. `grep -n 'INTENT-03' core/workflows/quick.md` — confirms requirement traceability
7. `grep -n 'non-modal\|no.*dialog\|no.*confirmation' core/workflows/quick.md` — confirms non-modal declaration
8. `grep -n 'fail-open\|Fail-open' core/workflows/quick.md` — confirms fail-open semantics for approach derivation
9. Verify the Step 5 planner prompt includes `APPROACH_BLOCK` reference: `grep -A5 'planning_context' core/workflows/quick.md | grep -i approach`
  </verify>
  <done>
Step 2.7 exists in core/workflows/quick.md between branching (Step 2.5) and directory creation (Step 3). It spawns a Haiku subagent to derive approach (INTENT-01), writes scope contract to .claude/scope-contract.json with branch key (INTENT-02), is fully non-modal (INTENT-03), and passes the approach block to the planner in Step 5. All operations are fail-open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync quick.md workflow to installed location and verify</name>
  <files>~/.claude/nf/workflows/quick.md</files>
  <action>
After Task 1 modifies core/workflows/quick.md, sync the updated workflow to the installed location. Per project memory, edits to installed workflows MUST be synced both ways:

1. Copy the updated workflow:
```bash
cp core/workflows/quick.md ~/.claude/nf/workflows/quick.md
```

2. Run the installer to ensure all hooks and workflows are in sync:
```bash
node bin/install.js --claude --global
```

3. Verify the sync:
```bash
diff core/workflows/quick.md ~/.claude/nf/workflows/quick.md
```
The diff should return empty (files identical).

4. Verify the scope contract success_criteria in quick.md was updated. The existing success_criteria section at the bottom of quick.md should include new checklist items for:
- [ ] Step 2.7 derives APPROACH block from task description (non-modal)
- [ ] .claude/scope-contract.json is written with branch name as key
- [ ] Approach block is passed to planner in Step 5 context
- [ ] Approach derivation is fail-open (Haiku unavailability produces generic fallback)

If these items are not in the success_criteria, add them.
  </action>
  <verify>
1. `diff core/workflows/quick.md ~/.claude/nf/workflows/quick.md` — returns empty (files synced)
2. `grep 'scope-contract' ~/.claude/nf/workflows/quick.md` — confirms installed copy has scope contract reference
3. `grep 'Step 2.7' ~/.claude/nf/workflows/quick.md` — confirms installed copy has new step
4. `grep 'APPROACH' ~/.claude/nf/workflows/quick.md` — confirms approach block reference in installed copy
  </verify>
  <done>
Installed workflow at ~/.claude/nf/workflows/quick.md matches core/workflows/quick.md exactly. Installer ran successfully. Success criteria updated to include scope contract and approach derivation checklist items.
  </done>
</task>

</tasks>

<verification>
1. core/workflows/quick.md contains Step 2.7 with approach derivation + scope contract write
2. Step 2.7 is positioned between Step 2.5 (branching) and Step 3 (directory creation)
3. Approach derivation spawns Haiku subagent (non-modal, no user dialog)
4. Scope contract is written to .claude/scope-contract.json with branch name as key
5. Planner in Step 5 receives APPROACH block in its context
6. All operations are fail-open (Haiku unavailability, parse errors, write failures)
7. Installed copy at ~/.claude/nf/workflows/quick.md matches core/workflows/quick.md
8. No existing quick.md steps were broken or removed
</verification>

<success_criteria>
- /nf:quick derives APPROACH block automatically from task description via Haiku (INTENT-01)
- .claude/scope-contract.json is written with branch name as key (INTENT-02)
- Approach derivation is non-modal — no user dialog or confirmation (INTENT-03)
- Planner receives APPROACH block in its context
- Fail-open on all error paths (Haiku unavailable, parse error, file write error)
- core/workflows/quick.md and ~/.claude/nf/workflows/quick.md are identical
</success_criteria>

<output>
After completion, create `.planning/phases/v0.40-02-workflow-modifications/v0.40-02-01-SUMMARY.md`
</output>
