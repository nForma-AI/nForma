---
phase: 29-restore-mcp-status-v2-and-checkbox-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/qgsd/mcp-status.md
  - ~/.claude/commands/qgsd/mcp-status.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
gap_closure: true

must_haves:
  truths:
    - "commands/qgsd/mcp-status.md has 125 lines (v2: 10 agents, identity polling, scoreboard UNAVAIL)"
    - "~/.claude/commands/qgsd/mcp-status.md is identical to source (diff is clean)"
    - "REQUIREMENTS.md shows [x] for OBS-01, OBS-02, OBS-03, OBS-04"
    - "REQUIREMENTS.md traceability table shows Complete for OBS-01 through OBS-04"
    - "STD checkboxes (STD-01, 03, 05, 06, 07, 09) remain [x] — not regressed"
  artifacts:
    - path: "commands/qgsd/mcp-status.md"
      provides: "v2 mcp-status slash command (10 agents, scoreboard-aware)"
      min_lines: 125
      contains: "mcp__claude-glm__identity"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OBS-01–04 marked complete"
      contains: "[x] **OBS-01**"
  key_links:
    - from: "commands/qgsd/mcp-status.md"
      to: "~/.claude/commands/qgsd/mcp-status.md"
      via: "cp source to installed path"
      pattern: "diff.*mcp-status.*SYNC OK"
    - from: ".planning/REQUIREMENTS.md"
      to: "OBS-01–04 checkboxes"
      via: "direct line edit [ ] to [x]"
      pattern: "\\[x\\] \\*\\*OBS-0[1234]\\*\\*"
---

<objective>
Restore commands/qgsd/mcp-status.md from its regressed v1 state (103 lines, 4 agents) back to the verified v2 state (125 lines, 10 agents, scoreboard-aware) that exists in HEAD, sync it to the installed path, and mark OBS-01 through OBS-04 as complete in REQUIREMENTS.md.

Purpose: Phase 28 execution accidentally overwrote mcp-status.md with an older v1 copy that was embedded in 28-01-PLAN.md as context. The v2 is intact in git at HEAD (commit 29a8236). This plan is a surgical recovery — git checkout restores v2, cp syncs it, REQUIREMENTS.md gets 4 checkbox flips.

Output: mcp-status.md at 125 lines in both source and installed paths; REQUIREMENTS.md with OBS-01–04 marked [x] and traceability showing Complete.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-restore-mcp-status-v2-and-checkbox-cleanup/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore mcp-status.md v2 from HEAD and sync to installed path</name>
  <files>
    commands/qgsd/mcp-status.md
    ~/.claude/commands/qgsd/mcp-status.md
  </files>
  <action>
    The working tree has v1 (103 lines). HEAD has v2 (125 lines, commit 29a8236). The fix is a single git command.

    Execute in order:

    1. Verify current state before touching anything:
       ```bash
       git -C /Users/jonathanborduas/code/QGSD status commands/qgsd/mcp-status.md
       # Expected: "modified:   commands/qgsd/mcp-status.md"

       git -C /Users/jonathanborduas/code/QGSD show HEAD:commands/qgsd/mcp-status.md | wc -l
       # Expected: 125 (v2 is in HEAD)
       ```

    2. Restore v2 from HEAD (discards the v1 working-tree overwrite):
       ```bash
       git -C /Users/jonathanborduas/code/QGSD checkout -- commands/qgsd/mcp-status.md
       ```
       CRITICAL: Use `git checkout --` (double dash) — restores file only, does NOT switch branches.

    3. Verify source restoration:
       ```bash
       wc -l /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
       # Expected: 125
       grep "mcp__claude-glm__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
       # Expected: match (confirms 10-agent v2)
       ```

    4. Copy to installed path:
       ```bash
       cp /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md \
          ~/.claude/commands/qgsd/mcp-status.md
       ```

    5. Verify installed copy matches source:
       ```bash
       diff /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md \
            ~/.claude/commands/qgsd/mcp-status.md && echo "SYNC OK"
       wc -l ~/.claude/commands/qgsd/mcp-status.md
       # Expected: 125
       ```

    Do NOT use `git restore` (version-dependent syntax). Do NOT manually reconstruct v2 content. Do NOT touch any other command files.
  </action>
  <verify>
    ```bash
    wc -l /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
    # Output: 125

    wc -l ~/.claude/commands/qgsd/mcp-status.md
    # Output: 125

    grep "mcp__claude-glm__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
    # Output: non-empty (v2 has 10 agents including claude-glm)

    diff /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md \
         ~/.claude/commands/qgsd/mcp-status.md && echo "SYNC OK"
    # Output: SYNC OK

    git -C /Users/jonathanborduas/code/QGSD status commands/qgsd/mcp-status.md
    # Output: nothing (clean — file matches HEAD)
    ```
  </verify>
  <done>Both source (commands/qgsd/mcp-status.md) and installed (~/.claude/commands/qgsd/mcp-status.md) are 125 lines, contain "mcp__claude-glm__identity", are identical to each other, and git status shows the source file as clean (not modified).</done>
</task>

<task type="auto">
  <name>Task 2: Mark OBS-01–04 complete in REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Update exactly 8 lines in .planning/REQUIREMENTS.md — 4 checkbox lines and 4 traceability rows. Do NOT touch any STD-* lines (they are already [x]).

    Read the current file first to confirm line positions match research. Then make the following changes:

    **Checkbox section — change [ ] to [x] for OBS-01 through OBS-04:**

    Before:
    ```
    - [ ] **OBS-01**: User can run `/qgsd:mcp-status` to see all connected MCPs with name, version, current model, and availability
    - [ ] **OBS-02**: Status display shows health state (available / quota-exceeded / error) derived from scoreboard data
    - [ ] **OBS-03**: Status shows available models for each agent (from `identity` tool response)
    - [ ] **OBS-04**: Status shows recent UNAVAIL count per agent from quorum scoreboard
    ```

    After:
    ```
    - [x] **OBS-01**: User can run `/qgsd:mcp-status` to see all connected MCPs with name, version, current model, and availability
    - [x] **OBS-02**: Status display shows health state (available / quota-exceeded / error) derived from scoreboard data
    - [x] **OBS-03**: Status shows available models for each agent (from `identity` tool response)
    - [x] **OBS-04**: Status shows recent UNAVAIL count per agent from quorum scoreboard
    ```

    **Traceability table — change Pending to Complete for OBS-01 through OBS-04:**

    Before:
    ```
    | OBS-01 | Phase 29 (gap closure) | Pending |
    | OBS-02 | Phase 29 (gap closure) | Pending |
    | OBS-03 | Phase 29 (gap closure) | Pending |
    | OBS-04 | Phase 29 (gap closure) | Pending |
    ```

    After:
    ```
    | OBS-01 | Phase 29 (gap closure) | Complete |
    | OBS-02 | Phase 29 (gap closure) | Complete |
    | OBS-03 | Phase 29 (gap closure) | Complete |
    | OBS-04 | Phase 29 (gap closure) | Complete |
    ```

    Also update the footer timestamp line at the bottom of REQUIREMENTS.md:
    ```
    *Last updated: 2026-02-22 — v0.4 OBS-01–04 marked Complete (Phase 29 gap closure)*
    ```

    Use the Write tool to write the full updated file — read it first, make the 8 targeted changes, write back. Do NOT use sed. Do NOT change any other lines.

    Guard: After writing, run:
    ```bash
    grep "OBS-0[1234]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[ \]"
    # Expected: no output (all 4 are now [x])

    grep "STD-0[135679]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[ \]"
    # Expected: no output (STD checkboxes remain [x], not regressed)
    ```
  </action>
  <verify>
    ```bash
    grep "OBS-0[1234]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
    # All 4 lines show [x]

    grep "OBS-0[1234]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[ \]"
    # No output (none remain unchecked)

    grep "Phase 29 (gap closure)" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
    # All 4 rows show Complete, not Pending

    grep "STD-0[135679]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[x\]"
    # All 6 lines show [x] (unchanged)
    ```
  </verify>
  <done>REQUIREMENTS.md has [x] for OBS-01, OBS-02, OBS-03, OBS-04. Traceability table shows Complete for all 4. STD checkboxes are untouched. No other lines were modified.</done>
</task>

</tasks>

<verification>
Run all checks in sequence after both tasks complete:

```bash
# 1. mcp-status.md source is v2
wc -l /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
# Expected: 125

# 2. mcp-status.md installed is v2
wc -l ~/.claude/commands/qgsd/mcp-status.md
# Expected: 125

# 3. Source and installed are identical
diff /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md \
     ~/.claude/commands/qgsd/mcp-status.md && echo "SYNC OK"

# 4. v2 marker present in both
grep "mcp__claude-glm__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
grep "mcp__claude-glm__identity" ~/.claude/commands/qgsd/mcp-status.md

# 5. Source file is clean in git
git -C /Users/jonathanborduas/code/QGSD status commands/qgsd/mcp-status.md
# Expected: no output (clean)

# 6. OBS checkboxes all [x]
grep "OBS-0[1234]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[ \]"
# Expected: no output

# 7. STD checkboxes not regressed
grep "STD-0[135679]" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "\[ \]"
# Expected: no output

# 8. Traceability shows Complete
grep "Phase 29 (gap closure)" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
# Expected: 4 rows, all showing Complete
```
</verification>

<success_criteria>
1. commands/qgsd/mcp-status.md is 125 lines (v2 content, 10 agents, scoreboard-aware)
2. ~/.claude/commands/qgsd/mcp-status.md is identical to source (SYNC OK)
3. git status shows commands/qgsd/mcp-status.md as clean (matches HEAD)
4. REQUIREMENTS.md has [x] for OBS-01, OBS-02, OBS-03, OBS-04
5. REQUIREMENTS.md traceability table shows Complete for all 4 OBS requirements
6. STD-01, STD-03, STD-05, STD-06, STD-07, STD-09 remain [x] (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/29-restore-mcp-status-v2-and-checkbox-cleanup/29-01-SUMMARY.md`
</output>
