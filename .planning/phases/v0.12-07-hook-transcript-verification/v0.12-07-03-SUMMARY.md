---
phase: v0.12-07
plan: 03
status: complete
commit: 5b065f3
date: 2026-02-25
---

# Plan 03 Summary — Runner Implementation (run-transcript-alloy.cjs)

## What Was Built

- **bin/run-transcript-alloy.cjs** — Alloy 6 runner for transcript-scan.als, mirroring run-audit-alloy.cjs

## Test Results

All 4 tests from Plan 01 turned GREEN:

```
✔ exits non-zero and prints JAVA_HOME error when JAVA_HOME points to nonexistent path
✔ exits non-zero and prints Alloy JAR download URL when JAR not found (skip — JAR present)
✔ exits non-zero with descriptive message for unknown --spec value
✔ exits non-zero and lists valid spec (transcript-scan) in error for invalid spec
tests 4, pass 4, fail 0
```

## Alloy Verification

```
node bin/run-transcript-alloy.cjs --spec transcript-scan
EXIT: 0
```

Alloy ran all 4 check commands on openjdk 25.0.1 with no "Counterexample" in output.
All invariants verified: BoundaryCorrect, PairingUnique, NoDuplicateCounting, SuccessCountNeverExceedsMinSize.

Output shows `---Trace---` sections — these are Alloy 6 witness/satisfying instances, not counterexamples. No `Counterexample` keyword in stdout; exit 0 confirmed.

## npm test

306/308 pass. 2 pre-existing failures unrelated to v0.12-07:
- `run-audit-alloy.test.cjs` JAR-not-found test (JAR is present, skip should fire but doesn't — pre-existing)
- `run-tlc.test.cjs` tla2tools.jar test — pre-existing

All 4 new transcript tests pass GREEN in isolation and in full suite.

## Runner Structure

- `VALID_SPECS = ['transcript-scan']`
- JAVA_HOME guard → exit 1 with `[run-transcript-alloy] JAVA_HOME is set but...`
- JAR check → exit 1 with download URL
- `spawnSync java -jar ... exec --output - --type text --quiet`
- `/Counterexample/i` scan on stdout → exit 1 if found
- Propagate Alloy exit code if non-zero

## GAP-4 Status

CLOSED. The transcript scanning algorithm (getCurrentTurnLines, wasSlotCalledSuccessfully) is formally verified by Alloy 6 bounded model checking across all 4 correctness invariants.
