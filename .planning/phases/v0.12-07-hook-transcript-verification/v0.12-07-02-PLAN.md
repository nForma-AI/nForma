---
phase: v0.12-07
plan: 02
type: execute
wave: 1
depends_on:
  - v0.12-07-01
files_modified:
  - formal/alloy/transcript-scan.als
autonomous: true
requirements:
  - GAP-4

must_haves:
  truths:
    - "formal/alloy/transcript-scan.als exists with module transcript_scan declaration"
    - "Sig hierarchy has Entry (abstract, idx: one Int), HumanMessage extends Entry, ToolUse extends Entry (useId: one Id), ToolResult extends Entry (resultId: one Id, isError: one Bool)"
    - "Facts UniqueIndices and ContiguousIndices enforce ordered sequence semantics"
    - "Four assert+check statements cover all GAP-4 invariants"
    - "CeilingEnforced is named SuccessCountNeverExceedsMinSize with explicit upper-bound comment"
    - "When Java is available, node bin/run-transcript-alloy.cjs (Plan 03) exits 0 with no Counterexample in output"
  artifacts:
    - path: "formal/alloy/transcript-scan.als"
      provides: "Alloy 6 formal spec for transcript scanning invariants"
      min_lines: 90
      contains: "module transcript_scan"
  key_links:
    - from: "formal/alloy/transcript-scan.als BoundaryCorrect"
      to: "hooks/qgsd-stop.js getCurrentTurnLines"
      via: "models backward scan for last HumanMessage (max idx)"
      pattern: "BoundaryCorrect|boundaryIdx"
    - from: "formal/alloy/transcript-scan.als PairingUnique"
      to: "hooks/qgsd-stop.js wasSlotCalledSuccessfully step 1+2"
      via: "lone quantifier on resultId = useId matching"
      pattern: "PairingUnique|lone"
    - from: "formal/alloy/transcript-scan.als SuccessCountNeverExceedsMinSize"
      to: "hooks/qgsd-stop.js line 453 break"
      via: "#successfulResults <= Config.minSize (upper bound invariant)"
      pattern: "SuccessCountNeverExceedsMinSize|minSize"
---

<objective>
Author the Alloy 6 formal specification for the qgsd-stop.js transcript scanning algorithm.

Purpose: Formally verify four correctness invariants of the transcript scan algorithm using Alloy 6 bounded model checking. Every invariant name mirrors the GAP-4 requirement exactly. The spec uses integer idx fields (not Alloy seq) per project convention established in scoreboard-recompute.als and availability-parsing.als.
Output: formal/alloy/transcript-scan.als
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/phases/v0.12-07-hook-transcript-verification/v0.12-07-RESEARCH.md
@formal/alloy/scoreboard-recompute.als
@hooks/qgsd-stop.js
@.planning/phases/v0.12-07-hook-transcript-verification/v0.12-07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author formal/alloy/transcript-scan.als — four GAP-4 invariants</name>
  <files>formal/alloy/transcript-scan.als</files>
  <action>
Create formal/alloy/transcript-scan.als following the structure of scoreboard-recompute.als. Full spec content:

```alloy
-- formal/alloy/transcript-scan.als
-- Handwritten — not generated from XState.
-- Source: hooks/qgsd-stop.js (getCurrentTurnLines, wasSlotCalledSuccessfully)
--
-- Models the QGSD qgsd-stop.js transcript scanning algorithm:
--   getCurrentTurnLines: backward scan for last human message boundary
--   wasSlotCalledSuccessfully: two-pass tool_use/tool_result ID matching
--
-- Assertions:
--   BoundaryCorrect:              last human message boundary is the highest-idx HumanMessage
--   PairingUnique:                each tool_use_id matches at most one tool_result (and vice versa)
--   NoDuplicateCounting:          successCount equals the cardinality of the non-duplicate result set
--   SuccessCountNeverExceedsMinSize: successCount (upper bound) never exceeds Config.minSize
--                                  Models the early-break at qgsd-stop.js:453 (if successCount >= minSize break)
--
-- Scope: 5 Entry, 5 Id, 2 Bool, 4 Int (4-bit: [-8..7], covers idx 0..4 and minSize 1..5)
--
-- qgsd-stop.js variable mapping:
--   JSONL entry            → Entry (abstract sig, idx: one Int)
--   isHumanMessage()=true  → HumanMessage extends Entry
--   tool_use block.id      → ToolUse extends Entry { useId: one Id }
--   tool_result block      → ToolResult extends Entry { resultId: one Id, isError: one Bool }
--   block.is_error=true    → isError = True
--   toolUseIds (Set)       → domain of ToolUse.useId relation
--   successCount           → #successfulResults
--   minSize config value   → Config.minSize

module transcript_scan

-- ── Shared vocabulary ────────────────────────────────────────────────────────

-- Abstract ID atom: models tool_use block.id / tool_result block.tool_use_id
-- Alloy has no string type; ID matching is structural (same atom = same ID)
abstract sig Id {}

-- Boolean values for isError field
abstract sig Bool {}
one sig True, False extends Bool {}

-- ── Entry sig hierarchy ──────────────────────────────────────────────────────

-- Abstract base: every transcript entry has an integer index
-- idx models the line position in the current-turn JSONL window
abstract sig Entry {
  idx: one Int
}

-- Human-authored user message (isHumanMessage() = true in qgsd-stop.js)
-- Distinguishes from tool_result-only user entries by having text content blocks
sig HumanMessage extends Entry {}

-- Assistant tool_use block (block.type = 'tool_use', block.id = useId)
sig ToolUse extends Entry {
  useId: one Id
}

-- User tool_result block (block.type = 'tool_result', block.tool_use_id = resultId)
sig ToolResult extends Entry {
  resultId: one Id,
  isError:  one Bool
}

-- Config: holds minSize (the required ceiling for successCount)
-- Models: config.quorum.minSize from qgsd-stop.js line 437
one sig Config {
  minSize: one Int
}

-- ── Ordering facts ───────────────────────────────────────────────────────────

-- No two entries share the same index slot
fact UniqueIndices {
  all disj e1, e2: Entry | e1.idx != e2.idx
}

-- Indices form a contiguous 0..N-1 range (no gaps)
fact ContiguousIndices {
  Entry.idx = { i: Int | 0 <= i and i < #Entry }
}

-- ── Computed values (fun) ────────────────────────────────────────────────────

-- The boundary index: the highest idx among all HumanMessage entries
-- Models: lastUserIdx = max of all matching indices in getCurrentTurnLines
fun boundaryIdx : Int {
  max[HumanMessage.idx]
}

-- Successful results: non-error ToolResult entries that are paired with a ToolUse
-- Models: tool_result entries that increment successCount in wasSlotCalledSuccessfully
fun successfulResults : set ToolResult {
  { tr: ToolResult | tr.isError = False and
    (some tu: ToolUse | tr.resultId = tu.useId) }
}

-- successCount: the number of non-error paired results
-- Models: the successCount variable in qgsd-stop.js lines 444-463
fun successCount : Int {
  #successfulResults
}

-- ── Predicates (pred) ────────────────────────────────────────────────────────

-- BoundaryCorrect: no HumanMessage exists after the boundary index
-- Models: getCurrentTurnLines correctly identifies the last human turn
pred BoundaryCorrect {
  no h: HumanMessage | h.idx > boundaryIdx
}

-- PairingUnique: each ToolUse maps to at most one ToolResult, and vice versa
-- Models: wasSlotCalledSuccessfully two-pass ID matching (no fan-out, no fan-in)
pred PairingUnique {
  -- Each tool_use_id matches at most one tool_result (no fan-out)
  all tu: ToolUse | lone tr: ToolResult | tr.resultId = tu.useId
  -- Each tool_result matches at most one tool_use (no fan-in)
  all tr: ToolResult | lone tu: ToolUse | tr.resultId = tu.useId
}

-- NoDuplicateCounting: successCount equals the cardinality of the deduplicated result set
-- Alloy sets are duplicate-free by construction — this makes the no-double-counting
-- contract explicit: the count is exactly the number of unique successful results.
pred NoDuplicateCounting {
  successCount = #successfulResults
}

-- SuccessCountNeverExceedsMinSize: successCount is bounded above by Config.minSize
-- UPPER BOUND invariant — models the early-break ceiling at qgsd-stop.js line 453:
--   if (successCount >= minSize) break;
-- The break prevents successCount from growing past minSize, so successCount <= minSize.
-- This is NOT the approval threshold (that is successCount >= minSize at line 466).
pred SuccessCountNeverExceedsMinSize {
  Config.minSize > 0 => successCount <= Config.minSize
}

-- ── Assertions (assert + check) ──────────────────────────────────────────────

-- BoundaryCorrect: if at least one HumanMessage exists, boundary is the last one
assert BoundaryCorrectCheck {
  some HumanMessage => BoundaryCorrect
}

-- PairingUnique: the ID matching relation is functional in both directions
assert PairingUniqueCheck {
  PairingUnique
}

-- NoDuplicateCounting: set cardinality = no duplicate counting (tautological but explicit)
assert NoDuplicateCountingCheck {
  NoDuplicateCounting
}

-- SuccessCountNeverExceedsMinSize: ceiling enforcement holds universally
assert SuccessCountNeverExceedsMinSizeCheck {
  SuccessCountNeverExceedsMinSize
}

-- ── Check commands ───────────────────────────────────────────────────────────
-- Scope: 5 Entry total (HumanMessage + ToolUse + ToolResult subtypes share this budget)
--        5 Id atoms (allows non-injective ID assignments for boundary testing)
--        2 Bool (True, False — exact cardinality)
--        4 Int (4-bit integers: [-8..7], covers idx 0..4 and minSize 1..5 without overflow)

check BoundaryCorrectCheck             for 5 Entry, 5 Id, 2 Bool, 4 Int
check PairingUniqueCheck               for 5 Entry, 5 Id, 2 Bool, 4 Int
check NoDuplicateCountingCheck         for 5 Entry, 5 Id, 2 Bool, 4 Int
check SuccessCountNeverExceedsMinSizeCheck for 5 Entry, 5 Id, 2 Bool, 4 Int
```

Key design decisions embedded in comments:
1. `lone` quantifier (not `one`) for PairingUnique — "at most one" not "exactly one" (Pitfall 2)
2. `HumanMessage` and `ToolResult` are disjoint sig subtypes — never conflated (Pitfall 3)
3. Integer `idx` field, not Alloy `seq` — avoids scope limitation (Pitfall 1 + research anti-pattern)
4. `SuccessCountNeverExceedsMinSize` name (not `CeilingEnforced`) — clarifies upper-bound semantics (Copilot deliberation outcome)
5. `fun` for computed values, `pred` for boolean conditions, `assert`+`check` for universal invariants (Pitfall 5)
  </action>
  <verify>
If Java available: JAVA_HOME=$(java_home 2>/dev/null || echo $JAVA_HOME) node bin/run-transcript-alloy.cjs --spec transcript-scan
If Java not available: confirm file exists with correct structure:
  - grep "module transcript_scan" formal/alloy/transcript-scan.als
  - grep "SuccessCountNeverExceedsMinSize" formal/alloy/transcript-scan.als
  - grep "check.*for 5 Entry" formal/alloy/transcript-scan.als | wc -l (must be 4)
  </verify>
  <done>formal/alloy/transcript-scan.als exists with module declaration, 4 assert+check invariants, SuccessCountNeverExceedsMinSize (not CeilingEnforced), integer idx ordering facts, lone quantifiers in PairingUnique, and correct scope bounds</done>
</task>

</tasks>

<verification>
1. File exists: ls -la formal/alloy/transcript-scan.als
2. Module declared: grep "module transcript_scan" formal/alloy/transcript-scan.als
3. All 4 assertions present: grep "^assert\|^check" formal/alloy/transcript-scan.als | wc -l (must be 8: 4 asserts + 4 checks)
4. Correct invariant names: grep "BoundaryCorrect\|PairingUnique\|NoDuplicateCounting\|SuccessCountNeverExceedsMinSize" formal/alloy/transcript-scan.als
5. No CeilingEnforced (old name rejected by Copilot): ! grep -q "CeilingEnforced" formal/alloy/transcript-scan.als
6. lone quantifier present: grep "lone" formal/alloy/transcript-scan.als
7. Scope correct: grep "for 5 Entry, 5 Id, 2 Bool, 4 Int" formal/alloy/transcript-scan.als | wc -l (must be 4)
</verification>

<success_criteria>
- formal/alloy/transcript-scan.als exists with correct Alloy 6 module structure
- 4 assert+check pairs covering all GAP-4 invariants
- SuccessCountNeverExceedsMinSize used (not CeilingEnforced) with explicit upper-bound comment referencing qgsd-stop.js:453
- Integer idx ordering via UniqueIndices + ContiguousIndices facts (not Alloy seq)
- lone quantifier in PairingUnique (not one — avoids Pitfall 2)
- When Java available: Alloy reports no counterexamples for all 4 checks
</success_criteria>

<output>
After completion, create .planning/phases/v0.12-07-hook-transcript-verification/v0.12-07-02-SUMMARY.md
</output>
