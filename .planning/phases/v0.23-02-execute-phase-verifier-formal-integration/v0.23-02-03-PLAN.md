---
phase: v0.23-02-execute-phase-verifier-formal-integration
plan: 03
type: execute
wave: 1
depends_on: [v0.23-02-01]
files_modified:
  - agents/qgsd-verifier.md
  - qgsd-core/templates/verification-report.md
  - qgsd-core/references/verification-patterns.md
autonomous: true
requirements: [WFI-04, ENF-01, ENF-02]
formal_artifacts: none

must_haves:
  truths:
    - "qgsd-verifier agent uses actual TLC/Alloy/PRISM stdout/exit-code from FORMAL_CHECK_RESULT as evidence — VERIFICATION.md shows pass/fail counts from real tool output, not LLM estimation"
    - "When FORMAL_CHECK_RESULT.failed > 0, verifier sets status: counterexample_found in VERIFICATION.md with counterexamples array — no must_haves can pass under this status"
    - "When FORMAL_CHECK_RESULT shows all skipped (failed==0, passed==0), verifier writes a skip note but does NOT set counterexample_found — fail-open preserved"
    - "When receiving counterexample_override reason in re-verification prompt, verifier writes counterexample_override: frontmatter with acknowledged_at timestamp, reason, and override_by: user, then sets status to passed"
  artifacts:
    - path: "agents/qgsd-verifier.md"
      provides: "formal_context parsing in Steps 9-10 + counterexample_found status logic + override detection"
      contains: "counterexample_found"
    - path: "qgsd-core/templates/verification-report.md"
      provides: "Updated VERIFICATION.md template with formal_check: and counterexample_override: frontmatter fields"
      contains: "counterexample_override"
    - path: "qgsd-core/references/verification-patterns.md"
      provides: "counterexample_found status documented"
      contains: "counterexample_found"
  key_links:
    - from: "agents/qgsd-verifier.md Step 9"
      to: "VERIFICATION.md status field"
      via: "FORMAL_CHECK_RESULT.failed > 0 check"
      pattern: "failed.*0.*counterexample_found"
    - from: "agents/qgsd-verifier.md"
      to: "VERIFICATION.md formal_check: frontmatter"
      via: "Write tool — formal_check: {passed, failed, skipped, counterexamples} block"
      pattern: "formal_check"
---

<objective>
Update the qgsd-verifier agent to parse FORMAL_CHECK_RESULT from its prompt, treat failed>0 as
a hard failure setting status:counterexample_found, and handle the continuation re-verification
prompt that writes the counterexample_override audit trail. Update the verification-report template
and verification-patterns reference to document the new status and frontmatter fields.

Purpose: Satisfies WFI-04 (verifier uses real tool output), ENF-01 (counterexample_found status),
ENF-02 (override audit trail written by verifier).
Output: Updated agents/qgsd-verifier.md, verification-report.md, verification-patterns.md.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.23-02-execute-phase-verifier-formal-integration/v0.23-02-RESEARCH.md
@agents/qgsd-verifier.md
@qgsd-core/templates/verification-report.md
@qgsd-core/references/verification-patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update qgsd-verifier.md with formal_context parsing and counterexample_found status</name>
  <files>agents/qgsd-verifier.md</files>
  <action>
Read agents/qgsd-verifier.md fully. Locate Step 9 (Determine Overall Status) and Step 10 (Structure
Gap Output). Make two targeted insertions:

**Insertion 1: Add formal_context parsing step before Step 9**

Insert a new section between the end of Step 8 (Human Verification) and Step 9:

```markdown
## Step 8.5: Parse Formal Check Result

If a `<formal_context>` block was provided in the prompt, parse `FORMAL_CHECK_RESULT`:

- If `FORMAL_CHECK_RESULT` is absent or the formal_context says "No formal scope matched" or
  "tooling unavailable": set `FORMAL_RESULT = null`. Skip formal section in VERIFICATION.md.

- If `FORMAL_CHECK_RESULT` is present JSON: parse `passed`, `failed`, `skipped`, `counterexamples`.

Route:
- `failed > 0`: **HARD FAILURE** — set `FORMAL_STATUS = counterexample_found`. Record the
  `counterexamples` array. No must_haves can pass under this condition.
- `failed == 0` AND `passed == 0` AND `skipped > 0`: set `FORMAL_STATUS = tooling_absent`.
  Include skip warning in VERIFICATION.md. Continue normal must_haves verification.
- `passed > 0` AND `failed == 0`: set `FORMAL_STATUS = passed`. Include pass counts as evidence.

**The verifier does NOT re-run bin/run-formal-check.cjs.** The result was pre-computed by
execute-phase and passed in the prompt. Use the passed JSON as ground truth.
```

**Insertion 2: Update Step 9 to include counterexample_found status**

In Step 9 (Determine Overall Status), add the new status BEFORE the others (counterexample_found
takes priority):

```markdown
**Status: counterexample_found** — FORMAL_CHECK_RESULT.failed > 0 (from Step 8.5). Formal model
checker found a counterexample. Set status: counterexample_found. Include formal_check: section
in VERIFICATION.md frontmatter with passed/failed/skipped counts and counterexamples array.
This status prevents workflow advancement unless user provides explicit override.

**Override re-verification:** If the prompt contains "User acknowledged counterexample block with
reason: '...'" (this is the continuation prompt from execute-phase's override path):
- Do NOT set counterexample_found.
- Write counterexample_override: to VERIFICATION.md frontmatter:
  ```yaml
  counterexample_override:
    acknowledged_at: {ISO timestamp — use current time}
    reason: {reason string from prompt}
    override_by: user
  ```
- Set status: passed (if all other must_haves checks pass) or gaps_found (if other checks fail).
- Preserve ALL existing VERIFICATION.md content. Only add the counterexample_override: field and
  update the status: field.
```

**Insertion 3: Update VERIFICATION.md output template in Step 10 / output section**

Find the VERIFICATION.md frontmatter block in the verifier's output section. Add formal_check and
counterexample_override as optional fields:

```yaml
formal_check:  # Only when FORMAL_STATUS is not null
  passed: N
  failed: N
  skipped: N
  counterexamples: []  # Only when failed > 0
counterexample_override:  # Only when user provided override acknowledgment
  acknowledged_at: YYYY-MM-DDTHH:MM:SSZ
  reason: "user reason string"
  override_by: user
```

Add a "Formal Verification" subsection to the VERIFICATION.md body template:

```markdown
## Formal Verification

{If FORMAL_STATUS == counterexample_found:}
**Status: COUNTEREXAMPLE FOUND**
Formal model checker reported failures. Workflow blocked pending user override.
| Module:Tool | Result |
|-------------|--------|
| {module}:{tool} | COUNTEREXAMPLE |

{If FORMAL_STATUS == tooling_absent:}
**Status: TOOLING ABSENT (SKIP)**
Java or model checker binaries not available. Formal check skipped.
No formal properties verified — this is not a failure, it is a tooling gap.

{If FORMAL_STATUS == passed:}
**Status: PASSED**
| Checks | Passed | Skipped | Failed |
|--------|--------|---------|--------|
| Total  | {N} | {N} | 0 |

{If FORMAL_STATUS == null (no formal scope):}
Omit this section entirely.
```
  </action>
  <verify>
```bash
grep -n "counterexample_found\|FORMAL_CHECK_RESULT\|FORMAL_STATUS\|counterexample_override\|Step 8.5" agents/qgsd-verifier.md | head -20
```
Must show: Step 8.5 section, FORMAL_CHECK_RESULT parsing, counterexample_found status in Step 9, counterexample_override write instruction.
  </verify>
  <done>agents/qgsd-verifier.md has: (1) Step 8.5 formal_context parsing section, (2) counterexample_found as first status in Step 9, (3) override re-verification handling in Step 9, (4) formal_check: and counterexample_override: fields in VERIFICATION.md output template. grep confirms all 4 patterns present.</done>
</task>

<task type="auto">
  <name>Task 2: Update verification-report template and verification-patterns reference</name>
  <files>qgsd-core/templates/verification-report.md, qgsd-core/references/verification-patterns.md</files>
  <action>
**File 1: qgsd-core/templates/verification-report.md**

Read the file fully. The current template's frontmatter shows:
```yaml
status: passed | gaps_found | human_needed
```

Update the status line to:
```yaml
status: passed | gaps_found | human_needed | counterexample_found
```

Add the formal_check and counterexample_override fields to the frontmatter template block (after
the `score:` line):

```yaml
formal_check:  # Only if formal check ran
  passed: N
  failed: N
  skipped: N
  counterexamples:  # Only if failed > 0
    - "module:tool"
counterexample_override:  # Only if user provided override
  acknowledged_at: YYYY-MM-DDTHH:MM:SSZ
  reason: "Reason user provided"
  override_by: user
```

In the Guidelines section, add counterexample_found to the Status values list:
```markdown
- `counterexample_found` — Formal model checker found a counterexample; workflow blocked pending
  user override acknowledgment
```

The existing "Formal Verification" section near the bottom of the template references
`run-formal-verify --only=tla` (an older command). Update it to reference `run-formal-check.cjs`
and the `FORMAL_CHECK_RESULT` schema. Replace the old template section:

```markdown
## Formal Verification

**Command:** `node bin/run-formal-check.cjs --modules={module_list}`
**Completed:** {ISO_TIMESTAMP or "skipped — tooling absent"}
**Overall Status:** {passed | counterexample_found | tooling_absent}

### Results

| Passed | Failed | Skipped | Counterexamples |
|--------|--------|---------|-----------------|
| {N} | {N} | {N} | {list or none} |

**Conclusion:** {passed → "All formal properties verified" | counterexample_found → "Formal violation found — override required" | tooling_absent → "Tooling unavailable — not a failure"}
```

**File 2: qgsd-core/references/verification-patterns.md**

Read the file. Find the section that documents valid status values (likely near a table of statuses
or a list). Add counterexample_found to the status list with description:

```markdown
### counterexample_found

Set when `FORMAL_CHECK_RESULT.failed > 0` from `bin/run-formal-check.cjs`.

**Meaning:** Formal model checker (TLC/Alloy/PRISM) found a counterexample — the formal model's
invariants were violated. This is a hard block on workflow advancement.

**Fields added to VERIFICATION.md:**
- `formal_check:` — pass/fail/skip counts and counterexamples array
- `counterexample_override:` — present only when user has acknowledged and overridden

**Override path:** execute-phase prompts user for acknowledgment reason → continuation verifier
writes `counterexample_override: { acknowledged_at, reason, override_by: user }` → status
changes to `passed`. No silent bypass path.

**Fail-open distinction:** All-skipped results (tooling absent) do NOT produce counterexample_found.
Only `failed > 0` triggers this status.
```

If verification-patterns.md does not have a section for status values, add this documentation
near the top of the file (after any existing header/intro section).
  </action>
  <verify>
```bash
grep -n "counterexample_found\|counterexample_override\|formal_check" qgsd-core/templates/verification-report.md | head -20
grep -n "counterexample_found" qgsd-core/references/verification-patterns.md | head -10
```
Both files must show counterexample_found and related fields.
  </verify>
  <done>verification-report.md status line includes counterexample_found; formal_check: and counterexample_override: frontmatter fields present; Formal Verification template section updated to reference run-formal-check.cjs. verification-patterns.md documents counterexample_found status with override path and fail-open distinction.</done>
</task>

</tasks>

<verification>
Structural tests from Plan 01 now pass for verifier:
```bash
node --test bin/verifier-formal-context.test.cjs 2>&1 | grep -E "pass|fail|ok|not ok"
```
Both structural tests (counterexample_found in verifier, FORMAL_CHECK_RESULT reference) should be
GREEN after this plan (read from agents/qgsd-verifier.md).
</verification>

<success_criteria>
- agents/qgsd-verifier.md has Step 8.5 formal_context parsing section (≥15 lines)
- agents/qgsd-verifier.md Step 9 includes counterexample_found as first status option
- agents/qgsd-verifier.md has override re-verification path (counterexample_override: write)
- qgsd-core/templates/verification-report.md status line includes counterexample_found
- qgsd-core/templates/verification-report.md has formal_check: and counterexample_override: fields
- qgsd-core/references/verification-patterns.md documents counterexample_found status
- All grep checks pass: counterexample_found in all 3 files
</success_criteria>

<output>
After completion, create `.planning/phases/v0.23-02-execute-phase-verifier-formal-integration/v0.23-02-03-SUMMARY.md`
</output>
