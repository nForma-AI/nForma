---
phase: v0.23-02-execute-phase-verifier-formal-integration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/execute-phase-formal-gate.test.cjs
  - bin/verifier-formal-context.test.cjs
autonomous: true
requirements: [WFI-03, WFI-04, ENF-01, ENF-02, ENF-03]
formal_artifacts: none

must_haves:
  truths:
    - "Both test files exist and run under node --test without syntax errors"
    - "Tests for execute-phase formal gate are RED (failing) because qgsd-core/workflows/execute-phase.md does not yet contain run-formal-check.cjs invocation"
    - "Tests for verifier formal context are RED (failing) because agents/qgsd-verifier.md does not yet contain counterexample_found status"
    - "Unit tests for module extraction and fail-open logic are GREEN from the start"
  artifacts:
    - path: "bin/execute-phase-formal-gate.test.cjs"
      provides: "Structural assertions for execute-phase formal gate + unit tests for extraction logic"
      min_lines: 40
    - path: "bin/verifier-formal-context.test.cjs"
      provides: "Structural assertions for verifier formal context + unit tests for counterexample_found routing"
      min_lines: 30
  key_links:
    - from: "bin/execute-phase-formal-gate.test.cjs"
      to: "~/.claude/qgsd/workflows/execute-phase.md"
      via: "fs.readFileSync"
      pattern: "readFileSync.*execute-phase\\.md"
    - from: "bin/verifier-formal-context.test.cjs"
      to: "agents/qgsd-verifier.md"
      via: "fs.readFileSync"
      pattern: "readFileSync.*qgsd-verifier\\.md"
---

<objective>
Write failing structural tests before any implementation starts. Tests assert content patterns
that will exist AFTER Plans 02 and 03 execute — they are intentionally RED now and turn GREEN
when implementation completes.

Purpose: Enforce TDD discipline. Implementations in Plans 02-03 cannot be considered complete
until these tests pass.
Output: Two test files; structural tests RED (failing), unit tests GREEN.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.23-02-execute-phase-verifier-formal-integration/v0.23-02-RESEARCH.md
</context>

<feature>
  <name>Test Scaffolding for v0.23-02 Formal Gate</name>
  <files>bin/execute-phase-formal-gate.test.cjs, bin/verifier-formal-context.test.cjs</files>
  <behavior>
    Both test files use node:test (no jest/mocha — project pattern confirmed in existing tests).

    bin/execute-phase-formal-gate.test.cjs:
    - STRUCTURAL (RED until Plan 02): execute-phase.md contains "run-formal-check.cjs" invocation
    - STRUCTURAL (RED until Plan 02): execute-phase.md contains "FORMAL_CHECK_RESULT" pattern
    - STRUCTURAL (RED until Plan 02): execute-phase.md contains "counterexample_found" status routing
    - STRUCTURAL (RED until Plan 02): execute-phase.md contains "counterexample_override" override path
    - UNIT (GREEN now): Module extraction from bash array format works correctly
      Cases: single entry "{\"module\":\"quorum\",\"path\":\"...\"}" -> "quorum"
             two entries -> "quorum,breaker"
             empty array -> "" (triggers skip)
    - UNIT (GREEN now): Fail-open — empty FORMAL_SPEC_CONTEXT sets FORMAL_CHECK_RESULT=null

    bin/verifier-formal-context.test.cjs:
    - STRUCTURAL (RED until Plan 03): agents/qgsd-verifier.md contains "counterexample_found" status
    - STRUCTURAL (RED until Plan 03): agents/qgsd-verifier.md contains "FORMAL_CHECK_RESULT" reference
    - UNIT (GREEN now): FORMAL_CHECK_RESULT with failed>0 maps to counterexample_found
      Cases: {passed:0,failed:1,skipped:0} -> "counterexample_found"
             {passed:2,failed:0,skipped:0} -> "passed"
    - UNIT (GREEN now): FORMAL_CHECK_RESULT with all skipped does NOT map to counterexample_found
      Cases: {passed:0,failed:0,skipped:3} -> not "counterexample_found"

    Read paths:
    - execute-phase.md: read from "~/.claude/qgsd/workflows/execute-phase.md" (installed copy)
      Note: Plan 02 installs the updated file; until then structural tests FAIL (correct — RED)
    - qgsd-verifier.md: read from "agents/qgsd-verifier.md" (source; installer syncs from agents/)
      Note: Plan 03 updates the source; Plan 04 installs; structural tests for verifier read source
      CORRECTION: Read from ~/.claude/agents/qgsd-verifier.md (installed copy) to match install flow
  </behavior>
  <implementation>
    Use node:test and node:assert (native, no dependencies). Follow exact pattern from existing
    tests such as bin/run-tlc.test.cjs or bin/verify-formal-results.test.cjs — use require('node:test')
    and require('node:assert').

    For unit tests that don't read files, inline the status-determination logic as a pure function:
    function getFormalStatus(result) {
      if (result === null) return 'skip';
      if (result.failed > 0) return 'counterexample_found';
      if (result.passed === 0 && result.skipped > 0) return 'tooling_absent';
      return 'passed';
    }
    Test this inline function — this tests the LOGIC, not the file.

    For module extraction unit test, inline the bash-equivalent JS logic:
    function extractModules(ctxArray) {
      return ctxArray.map(e => {
        const m = e.match(/"module":"([^"]+)"/);
        return m ? m[1] : null;
      }).filter(Boolean).join(',');
    }

    RED/GREEN commit sequence:
    1. Create both files with all tests → run → confirm structural tests RED, unit tests GREEN
    2. Commit: test(v0.23-02-01): add failing structural tests and green unit tests for formal gate
  </implementation>
</feature>

<verification>
Run tests and confirm RED/GREEN split:
```bash
node --test bin/execute-phase-formal-gate.test.cjs bin/verifier-formal-context.test.cjs 2>&1 | grep -E "^#|pass|fail|ok|not ok"
```

Expected: structural tests FAIL (execute-phase.md and qgsd-verifier.md not yet updated), unit tests PASS.
</verification>

<success_criteria>
- bin/execute-phase-formal-gate.test.cjs exists, ≥40 lines, 4 structural tests + 2 unit tests
- bin/verifier-formal-context.test.cjs exists, ≥30 lines, 2 structural tests + 2 unit tests
- Structural tests fail (RED) with "Pattern not found" or assertion error
- Unit tests pass (GREEN): module extraction and status-mapping functions correct
- node --test runs both files without syntax error
</success_criteria>

<output>
After completion, create `.planning/phases/v0.23-02-execute-phase-verifier-formal-integration/v0.23-02-01-SUMMARY.md`
</output>
