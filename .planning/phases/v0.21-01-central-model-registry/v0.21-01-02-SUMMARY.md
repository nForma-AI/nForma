---
plan: v0.21-01-02
status: complete
completed: 2026-03-01
---

# v0.21-01-02 Summary: ARCH-01 Registry Initialization

## Objective Achieved
Implemented registry initialization (ARCH-01): created `initialize-model-registry.cjs` that scans `formal/` and produces `model-registry.json`, and wired `generate-formal-specs.cjs` to update the registry after every spec write.

## What Was Built

### Files Created
- `bin/initialize-model-registry.cjs` — 87-line idempotent initialization script
- `formal/model-registry.json` — Initial registry with 21 entries (11 TLA+, 7 Alloy, 3 PRISM)

### Files Modified
- `bin/generate-formal-specs.cjs` — Added `updateModelRegistry()` helper (38 lines) + wired after `.tla`/`.als`/`.pm` writes

## Key Behaviors
- Idempotent: exits 0 silently if registry already exists
- Scans `formal/tla/`, `formal/alloy/`, `formal/prism/` — skips TTrace artifacts and `.cfg`/`.props` files
- Keys are relative paths from ROOT (e.g., `formal/tla/QGSDQuorum.tla`) — no `./` or `/` prefix
- `update_source` is `"generate"` for xstate-derived files, `"manual"` for others
- `generate-formal-specs.cjs` atomically updates registry after each `.tla`/`.als`/`.pm` write (version++)
- Fail-open: if registry missing, `generate-formal-specs.cjs` warns and skips (doesn't fail)

## Test Results
- `node --test bin/initialize-model-registry.test.cjs`: 3/3 GREEN
- `node --test bin/model-registry.test.cjs`: 4/4 GREEN
- Combined suite: 7/7 GREEN

## Self-Check: PASSED
- `formal/model-registry.json` exists with 21 entries (all relative paths, no TTrace files)
- Running generate-formal-specs.cjs increments version and sets update_source=generate for xstate files
- All Wave 1 tests pass
