---
plan: v0.21-01-03
status: complete
completed: 2026-03-01
---

# v0.21-01-03 Summary: ARCH-02 promote-model.cjs

## Objective Achieved
Implemented `promote-model.cjs` with atomic tmp+rename merge and duplicate-name validation (ARCH-02).

## What Was Built

### Files Created
- `bin/promote-model.cjs` — 175-line CLI tool for atomic PROPERTY promotion

## Key Behaviors
- CLI: `node bin/promote-model.cjs <proposed.tla> <target.tla> [--source-id <id>]`
- Extracts PROPERTY names using canonical regex `/^PROPERTY\s+(\w+)/gm`
- Rejects duplicate PROPERTY names with exit 1 + descriptive stderr
- Merges PROPERTY blocks before `====` end marker (or appends if no end marker)
- Writes spec atomically: tmp file + `fs.renameSync`
- Updates `formal/model-registry.json` atomically: version++, update_source=plan-promote, source_id recorded
- Finds registry by walking up to `formal/` ancestor of target path (handles out-of-project tmpDir in tests)
- Fail-open: missing registry produces warning but not failure

## Test Results
- `node --test bin/promote-model.test.cjs`: 6/6 GREEN

## Integration Verified
- Single property merge: Existing + NewInvariant both appear in merged file, before `====`
- Multiple property merge: all 3 properties merged cleanly
- Duplicate rejection: `Existing` property detected in both proposed and target, exit 1
- Atomic rename: no `.tmp.` files remain after operation
- Registry version: 1 → 2 after promotion
- source_id: recorded in registry entry

## Self-Check: PASSED
- bin/promote-model.cjs exists (175 lines)
- 6/6 promote-model tests GREEN
- No tmp files left in formal/tla/
- Registry update_source=plan-promote, version++, source_id correct
