# Phase v0.29-06 Validation

## Success Criteria Verification

### 1. sweepL1toL2 reports correct unexplained_breakdown values
**PASS** -- Before fix: `instrumentation_bug: 0, model_gap: 0, genuine_violation: 0` (always zeros due to reading flat `data.instrumentation_bug` which did not exist). After fix: `instrumentation_bug: 6371, model_gap: 0, genuine_violation: 0` -- matches gate-a's `unexplained_counts` output.

### 2. Gate exit code 1 produces valid residual scores
**PASS** -- All three sweep functions return real residual scores instead of `{ residual: -1, error: true }`:
- `sweepL1toL2`: residual 2, grounding_score 0.829
- `sweepL2toL3`: residual 0, gate_b_score 1.0
- `sweepL3toTC`: residual 0, gate_c_score 1.0

### 3. All 14 REQUIREMENTS.md checkboxes show [x] Complete
**PASS** -- No unchecked checkboxes found in `.planning/REQUIREMENTS.md`. All entries already marked `[x]`.

## Files Modified
- `bin/nf-solve.cjs` -- Fixed `sweepL1toL2` data contract (lines 1968-1970) and exit-code guards in all three gate sweep functions

## Syntax Check
`node -c bin/nf-solve.cjs` -- PASS
