---
phase: v0.37-04-quorum-precedents
plan: 02
status: complete
started: 2026-03-16
completed: 2026-03-16
---

## Summary

Extended `bin/quorum-slot-dispatch.cjs` to load precedents from the JSON database, match by keyword relevance, prune stale entries, and inject up to 3 relevant past decisions into both Mode A and Mode B dispatch prompts.

## Tasks Completed

| # | Task | Status |
|---|------|--------|
| 1 | Add precedent loading, matching, formatting, and injection to quorum-slot-dispatch.cjs | Complete |
| 2 | Extend quorum-slot-dispatch test suite for precedent behaviors | Complete |

## Key Files

### Modified
- `bin/quorum-slot-dispatch.cjs` — Added `loadPrecedents`, `matchPrecedentsByKeywords`, `formatPrecedentsSection`; wired into main() and both prompt builders
- `bin/quorum-slot-dispatch.test.cjs` — 26 new tests (83 total, all pass)

## Results

- Precedent loading: fail-open with cache (same pattern as requirements loading)
- Keyword matching: reuses existing `extractKeywords()`, outcome keywords weighted 2x
- TTL pruning: inline 90-day check in matching function (avoids cross-module dependency)
- Prompt injection: `=== PAST QUORUM PRECEDENTS ===` section appears after requirements section in both Mode A and Mode B
- Truncation: question capped at 120 chars, outcome at 150 chars to prevent prompt bloat
- All 83 tests pass with zero regressions

## Decisions

- Inlined TTL check in `matchPrecedentsByKeywords` rather than importing from `extract-precedents.cjs` to avoid circular dependency (same 90-day logic, 5 lines)
- Used cache map keyed by projectRoot (same pattern as requirementsCache)
