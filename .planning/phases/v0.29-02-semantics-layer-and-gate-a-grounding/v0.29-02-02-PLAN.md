---
phase: v0.29-02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/mismatch-register.cjs
  - bin/mismatch-register.test.cjs
  - .planning/formal/semantics/mismatch-register.jsonl
  - bin/observed-fsm.cjs
  - bin/observed-fsm.test.cjs
  - .planning/formal/semantics/observed-fsm.json
autonomous: true
requirements:
  - SEM-02
  - SEM-04
formal_artifacts: none

must_haves:
  truths:
    - "Running a trace comparison produces JSONL mismatch entries tracking every L2-model vs L1-trace disagreement with resolution status (open/explained/bug)"
    - "Running the operational state machine derivation produces an observed-behavior FSM from traces comparable to hand-written TLA+/XState models"
  artifacts:
    - path: "bin/mismatch-register.cjs"
      provides: "Mismatch register builder detecting L2-vs-L1 trace disagreements"
      min_lines: 80
    - path: "bin/mismatch-register.test.cjs"
      provides: "Tests for mismatch register: JSONL format, resolution status, classification"
      min_lines: 40
    - path: ".planning/formal/semantics/mismatch-register.jsonl"
      provides: "JSONL ledger of L2-vs-L1 disagreements"
      contains: "resolution"
    - path: "bin/observed-fsm.cjs"
      provides: "Observed FSM derivation from trace replay"
      min_lines: 80
    - path: "bin/observed-fsm.test.cjs"
      provides: "Tests for observed FSM: transition extraction, model comparison, coverage metrics"
      min_lines: 40
    - path: ".planning/formal/semantics/observed-fsm.json"
      provides: "Trace-derived state machine for comparison"
      contains: "transitions"
  key_links:
    - from: "bin/mismatch-register.cjs"
      to: "bin/validate-traces.cjs"
      via: "imports mapToXStateEvent for event mapping"
      pattern: "mapToXStateEvent"
    - from: "bin/mismatch-register.cjs"
      to: ".planning/formal/semantics/mismatch-register.jsonl"
      via: "writes JSONL mismatch entries (one JSON object per line, NOT a JSON array)"
      pattern: "mismatch-register\\.jsonl"
    - from: "bin/observed-fsm.cjs"
      to: "bin/validate-traces.cjs"
      via: "imports mapToXStateEvent for trace-to-XState event mapping"
      pattern: "mapToXStateEvent"
    - from: "bin/observed-fsm.cjs"
      to: "dist/machines/nf-workflow.machine.js"
      via: "imports XState machine for actor replay and model comparison"
      pattern: "nf-workflow\\.machine"
    - from: "bin/observed-fsm.cjs"
      to: ".planning/formal/semantics/observed-fsm.json"
      via: "writes observed FSM as JSON data structure (NOT an XState definition)"
      pattern: "observed-fsm\\.json"
---

<objective>
Build the mismatch register (SEM-02) and observed FSM derivation (SEM-04) -- two trace-replay-based scripts that compare Layer 1 evidence against Layer 2 operational models.

Purpose: These scripts use the existing XState trace infrastructure (mapToXStateEvent, nf-workflow.machine.js) to detect where the L2 model disagrees with L1 traces and to derive an observed-behavior state machine from actual trace data. Both feed directly into Gate A grounding score.
Output: mismatch-register.jsonl, observed-fsm.json, two bin/ scripts with tests
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.29-02-semantics-layer-and-gate-a-grounding/v0.29-02-RESEARCH.md
@.planning/formal/evidence/event-vocabulary.json
@.planning/formal/evidence/trace-corpus-stats.json
@.planning/formal/evidence/state-candidates.json
@.planning/formal/.divergences.json
@bin/validate-traces.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mismatch register tracking L2-vs-L1 disagreements as JSONL ledger (SEM-02)</name>
  <files>
    bin/mismatch-register.cjs
    bin/mismatch-register.test.cjs
    .planning/formal/semantics/mismatch-register.jsonl
  </files>
  <action>
Create bin/mismatch-register.cjs following the existing bin/ CJS script pattern.

The script must:
1. Read conformance-events.jsonl using planning-paths.cjs for path resolution
2. Read .planning/formal/evidence/event-vocabulary.json for vocabulary validation
3. Import mapToXStateEvent from bin/validate-traces.cjs (already exported in v0.29-01-03)
4. Import createActor and nfWorkflowMachine from dist/machines/nf-workflow.machine.js
5. For each conformance event:
   a. Map to XState event via mapToXStateEvent
   b. If unmappable (returns null), skip -- this is an instrumentation gap, not a mismatch
   c. Create a fresh XState actor, send the event, compare expected vs actual state
   d. Apply H1 methodology: mid-session events (event.phase !== 'IDLE' AND action !== 'quorum_start') are methodology_skip, NOT mismatches
   e. For genuine state disagreements, create a mismatch entry
6. Also incorporate existing .divergences.json entries (10 known divergences) -- classify each as "explained" if they match the H1 methodology skip pattern, "open" otherwise

**JSONL format (one JSON object per line, file must NOT start with '['):**
```
{"id":"MISMATCH-001","timestamp":"...","l2_source":"nf-workflow.machine.ts","l1_trace_ref":{"event_index":5,"session":"session-001"},"expected_state":"DECIDED","actual_state":"IDLE","divergence_type":"state_mismatch","resolution":"open","resolution_notes":null,"classification":null}
```

Resolution values: "open" | "explained" (known methodology limitation) | "bug" (genuine implementation bug)

**Output file:** .planning/formal/semantics/mismatch-register.jsonl (JSONL, NOT JSON array)
Also write a summary to stdout (or --json for machine-readable):
```json
{ "total_mismatches": N, "by_resolution": { "open": N, "explained": N, "bug": N }, "by_divergence_type": {...} }
```

**CRITICAL:** Use fs.writeFileSync for initial generation (overwrites). Future phases can use fs.appendFileSync for incremental updates. The JSONL format enables append-only updates without rewriting the whole file.

Create bin/mismatch-register.test.cjs using node:test and node:assert:
- Unit test: JSONL entry generation produces valid JSON per line (parse each line independently)
- Unit test: H1 methodology skip correctly classifies mid-session events as non-mismatches
- Unit test: resolution status defaults to "open" for new mismatches
- Integration test: reads real conformance events, produces JSONL file where each line is valid JSON
- Test that file does NOT start with '[' (must be JSONL, not JSON array)

Run the script to generate initial mismatch-register.jsonl.
  </action>
  <verify>
node bin/mismatch-register.cjs && node --test bin/mismatch-register.test.cjs && node -e "const fs = require('fs'); const content = fs.readFileSync('.planning/formal/semantics/mismatch-register.jsonl','utf8'); const lines = content.trim().split('\\n').filter(l => l.trim()); console.log('Mismatch entries:', lines.length); if (lines.length > 0) { console.assert(!content.startsWith('['), 'Must be JSONL not JSON array'); const first = JSON.parse(lines[0]); console.assert('resolution' in first, 'Must have resolution field'); console.assert(['open','explained','bug'].includes(first.resolution), 'Resolution must be open/explained/bug'); }"
  </verify>
  <done>
mismatch-register.jsonl exists as JSONL (one JSON object per line, NOT JSON array). Each entry has id, l2_source, l1_trace_ref, expected/actual state, divergence_type, resolution (open/explained/bug), and classification. H1 methodology skip prevents false positives from mid-session events. Existing .divergences.json entries are incorporated. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create observed FSM derivation from trace replay (SEM-04)</name>
  <files>
    bin/observed-fsm.cjs
    bin/observed-fsm.test.cjs
    .planning/formal/semantics/observed-fsm.json
  </files>
  <action>
Create bin/observed-fsm.cjs following the existing bin/ CJS script pattern.

The script must:
1. Read conformance-events.jsonl using planning-paths.cjs
2. Read .planning/formal/evidence/event-vocabulary.json
3. Import mapToXStateEvent from bin/validate-traces.cjs
4. Import createActor and nfWorkflowMachine from dist/machines/nf-workflow.machine.js
5. Derive an observed FSM by replaying all mappable events:
   a. For each event, create a fresh XState actor, record beforeState, send mapped event, record afterState
   b. Build adjacency map: { fromState: { eventType: { toState, count } } }
   c. Track total events processed, events skipped (unmappable), and events per state
6. Extract the hand-written model's states and transitions from nfWorkflowMachine definition for comparison
7. Produce a structural diff:
   - missing_in_observed: transitions in the hand-written model that no trace exercises
   - missing_in_model: transitions observed in traces but not defined in the model (should be rare/zero with fresh actor replay)
   - matching: transitions that both agree on
8. Track TWO coverage metrics (per research Pitfall 4):
   - model_coverage: what % of hand-written model transitions are exercised by traces
   - vocabulary_coverage: what % of conformance events map to known vocabulary actions

**Output format (observed-fsm.json) -- JSON data structure, NOT an XState machine definition:**
```json
{
  "schema_version": "1",
  "generated": "ISO timestamp",
  "observed_transitions": { "IDLE": { "QUORUM_START": { "to_state": "quorum.collecting", "count": N } } },
  "states_observed": ["IDLE", "quorum.collecting", ...],
  "model_comparison": {
    "missing_in_observed": [...],
    "missing_in_model": [...],
    "matching": [...]
  },
  "coverage": {
    "model_coverage": 0.XX,
    "vocabulary_coverage": 0.XX,
    "total_events": N,
    "mapped_events": N,
    "unmapped_events": N
  }
}
```

Create bin/observed-fsm.test.cjs using node:test and node:assert:
- Unit test: small inline event array produces correct adjacency map (3 events -> expected transitions)
- Unit test: model comparison correctly identifies missing transitions
- Unit test: coverage metrics are computed correctly (mapped/total)
- Integration test: reads real traces, verifies states_observed is non-empty
- Test that vocabulary_coverage < 1.0 (we know 5,649 events are unmapped)
- Test that output is JSON object, NOT an XState machine definition (no "initial" or "states" top-level XState keys)

Run the script to generate initial observed-fsm.json.
  </action>
  <verify>
node bin/observed-fsm.cjs && node --test bin/observed-fsm.test.cjs && node -e "const fs = require('fs'); const fsm = JSON.parse(fs.readFileSync('.planning/formal/semantics/observed-fsm.json','utf8')); console.log('States observed:', fsm.states_observed.length, 'Model coverage:', fsm.coverage.model_coverage, 'Vocab coverage:', fsm.coverage.vocabulary_coverage); console.assert(fsm.states_observed.length > 0, 'Must have observed states'); console.assert(fsm.coverage.vocabulary_coverage < 1.0, 'Vocab coverage must be < 1.0 (unmapped events exist)'); console.assert(!('initial' in fsm), 'Must NOT be an XState machine definition');"
  </verify>
  <done>
observed-fsm.json exists as a JSON data structure (not XState definition) with observed transitions, states, model comparison (missing_in_observed/missing_in_model/matching), and two coverage metrics (model_coverage + vocabulary_coverage). vocabulary_coverage is < 1.0 reflecting the 5,649 unmapped events. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. mismatch-register.jsonl is JSONL format (one JSON per line, does NOT start with '[')
2. Each mismatch entry has resolution field (open/explained/bug)
3. H1 methodology prevents false-positive mismatches from mid-session events
4. observed-fsm.json is a JSON data structure with states + transitions + coverage metrics
5. observed-fsm.json is NOT an XState machine definition (no "initial"/"states" top-level keys)
6. vocabulary_coverage < 1.0 reflecting known unmapped events
7. Both scripts import mapToXStateEvent from validate-traces.cjs (single source of truth)
8. All tests pass: node --test bin/mismatch-register.test.cjs && node --test bin/observed-fsm.test.cjs
</verification>

<success_criteria>
- Mismatch register captures all L2-vs-L1 disagreements as JSONL with resolution tracking
- Observed FSM derives state machine from traces with structural diff against hand-written model
- Two distinct coverage metrics prevent the completeness illusion (model_coverage vs vocabulary_coverage)
- Both scripts follow existing bin/ CJS pattern (PROJECT_ROOT, --json, exit codes)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/v0.29-02-semantics-layer-and-gate-a-grounding/v0.29-02-02-SUMMARY.md`
</output>
