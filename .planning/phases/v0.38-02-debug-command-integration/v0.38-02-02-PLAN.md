---
phase: v0.38-02-debug-command-integration
plan: 02
type: execute
wave: 2
depends_on:
  - v0.38-02-01
files_modified:
  - commands/nf/debug.md
  - test/debug-verdict-reporting.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - DBG-03
  - DBG-04

must_haves:
  truths:
    - "The NEXT STEP result table includes a FORMAL row showing model verdict (reproduced / not-reproduced / no-model)"
    - "When no model covers the failure, debug Step G appends an entry to bug-model-gaps.json for future model creation"
  artifacts:
    - path: "commands/nf/debug.md"
      provides: "Debug workflow with FORMAL row in result table and gap persistence in Step G"
      contains: "FORMAL"
    - path: "test/debug-verdict-reporting.test.cjs"
      provides: "Unit tests for FORMAL row rendering and gap persistence logic"
      min_lines: 40
  key_links:
    - from: "commands/nf/debug.md"
      to: ".planning/formal/bug-model-gaps.json"
      via: "Step G writes no-coverage entries when verdict is no-model"
      pattern: "bug-model-gaps"
    - from: "commands/nf/debug.md"
      to: "bin/debug-formal-context.cjs"
      via: "Step E reads $FORMAL_VERDICT and $FORMAL_CONTEXT from Step A.5"
      pattern: "FORMAL_VERDICT"
  consumers:
    - artifact: "FORMAL row in result table"
      consumed_by: "User reading /nf:debug output"
      integration: "Visual row between worker rows and CONSENSUS row"
      verify_pattern: "FORMAL"
    - artifact: "bug-model-gaps.json entries from Step G"
      consumed_by: "Phase v0.38-03 model refinement workflow"
      integration: "Reads gaps with status no_coverage to prioritize model creation"
      verify_pattern: "no_coverage"
---

<objective>
Add FORMAL verdict row to the NEXT STEP result table and implement gap persistence in Step G when no formal model covers the failure.

Purpose: Surface formal model verdicts alongside worker recommendations so users see model-backed evidence, and track coverage gaps so Phase v0.38-03 knows which bugs need new formal models.
Output: Updated debug.md with FORMAL row and Step G gap persistence, unit tests for verdict rendering and gap entry creation.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.38-02-debug-command-integration/v0.38-02-RESEARCH.md
@.planning/phases/v0.38-02-debug-command-integration/v0.38-02-01-SUMMARY.md
@commands/nf/debug.md
@bin/debug-formal-context.cjs
@bin/formal-scope-scan.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FORMAL row to NEXT STEP result table in Step E and gap persistence in Step G</name>
  <files>commands/nf/debug.md</files>
  <action>
Modify `commands/nf/debug.md` to add the FORMAL verdict row and gap persistence logic. This builds on Plan 01's Step A.5 which already provides $FORMAL_VERDICT and $FORMAL_CONTEXT.

**Step E modification (Render NEXT STEP table):**

Update the result table template to include a FORMAL row between the worker rows and the CONSENSUS row. The table in Step E currently shows:

```
| Model        | Confidence   | Next Step                                   |
|--------------|--------------|---------------------------------------------|
| Gemini       | [confidence] | [next_step]                                 |
| ...          |              |                                             |
| CONSENSUS    | [HIGH/MED/-] | [consensus step or "No consensus"]          |
```

Insert a FORMAL row before CONSENSUS. Here are the exact row renderings for each verdict:

- **reproduced**: `| FORMAL | HIGH (model) | Model {name} reproduced bug. Constraint: {top_constraint} |`
  where `{name}` is `$FORMAL_CONTEXT.models.find(m => m.reproduced).name` and `{top_constraint}` is `$FORMAL_CONTEXT.constraints[0].text` (or "see model output" if constraints array is empty)

- **not-reproduced**: `| FORMAL | LOW (model) | Models exist but none reproduced. Bug may be environmental. |`

- **no-model**: `| FORMAL | N/A | No formal model covers this failure. Gap tracked in bug-model-gaps.json. |`

**Divergence note (after the table):**

Add a conditional note when FORMAL and CONSENSUS disagree. Use this concrete detection logic:

If $FORMAL_VERDICT == "reproduced" AND `$FORMAL_CONTEXT.constraints[0].text` is NOT found as a case-insensitive substring in `$CONSENSUS_ROOT_CAUSE`, render:

```
**Note:** Formal model reproduction suggests a constraint violation (`{$FORMAL_CONTEXT.constraints[0].text}`), but workers attribute the bug to a different cause. Consider: is the constraint too strict, or is the worker diagnosis incomplete?
```

If either condition is false (verdict is not "reproduced", or the top constraint text IS found in the consensus root cause), do NOT render the note.

**Step G modification (Execute or escalate):**

After the existing consensus-handling logic in Step G, add a conditional for gap persistence. This MUST be implemented as an inline `node` heredoc block (consistent with existing Step patterns in debug.md), NOT as a separate helper script:

```
### Gap Persistence (formal model coverage tracking)

If $FORMAL_VERDICT is "no-model":

  node << 'GAPEOF'
  const fs = require('fs');
  const crypto = require('crypto');
  const gapPath = '.planning/formal/bug-model-gaps.json';
  const description = process.env.ARGUMENTS || '';
  const bundle = process.env.BUNDLE || '';
  const consensusRoot = process.env.CONSENSUS_ROOT_CAUSE || 'no consensus';
  const consensusStep = process.env.CONSENSUS_NEXT_STEP || 'no consensus';

  let data;
  try {
    data = JSON.parse(fs.readFileSync(gapPath, 'utf8'));
  } catch {
    data = { gaps: [] };
  }

  const bugId = crypto.createHash('sha256').update(description).digest('hex').slice(0, 8);
  if (data.gaps.some(g => g.bug_id === bugId)) {
    console.log('Gap already tracked: bug_id=' + bugId);
    process.exit(0);
  }

  data.gaps.push({
    bug_id: bugId,
    description: description.slice(0, 500),
    failure_context: bundle.slice(0, 500),
    timestamp: new Date().toISOString(),
    status: 'no_coverage',
    worker_consensus_root_cause: consensusRoot,
    worker_consensus_next_step: consensusStep
  });

  try {
    fs.writeFileSync(gapPath, JSON.stringify(data, null, 2));
    console.log('Gap tracked: bug_id=' + bugId + ' — no formal model coverage for this failure.');
  } catch (e) {
    console.error('Failed to persist gap (fail-open):', e.message);
  }
  GAPEOF
```

Use the existing `hashBugId` pattern from `bin/formal-scope-scan.cjs` (SHA-256, 8-char hex) for consistency with Phase v0.38-01.

**Important:**
- The FORMAL row MUST always appear in the table (even when verdict is "no-model") — per research recommendation, it signals "we checked" vs "we didn't check"
- Gap persistence MUST happen in Step G (not Step A.5) because it needs the worker consensus results
- Use fail-open: if bug-model-gaps.json read/write fails, log to stderr and continue
  </action>
  <verify>
Read commands/nf/debug.md and confirm:
- FORMAL row template exists in Step E table between worker rows and CONSENSUS
- Three verdict cases are handled with exact row text (reproduced/HIGH, not-reproduced/LOW, no-model/N/A)
- Divergence note uses concrete logic: $FORMAL_VERDICT == "reproduced" AND constraints[0].text NOT found in $CONSENSUS_ROOT_CAUSE
- Step G includes inline node heredoc for gap persistence when verdict is no-model
- Gap entry schema matches Phase v0.38-01 pattern (bug_id, description, status, timestamp)
Run `grep -c 'FORMAL' commands/nf/debug.md` — returns at least 5 (row template + verdict cases + constraints block from Plan 01).
Run `grep -c 'bug-model-gaps' commands/nf/debug.md` — returns at least 1.
Run `grep 'no_coverage' commands/nf/debug.md` — confirms gap status value.
  </verify>
  <done>
NEXT STEP table includes FORMAL row with exact verdict-appropriate text for all three cases (HIGH/LOW/N/A). Step G uses inline node heredoc to append no_coverage entry to bug-model-gaps.json when verdict is no-model. Divergence note renders only when verdict is "reproduced" AND top constraint text is not a substring of consensus root cause.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for FORMAL verdict rendering and gap persistence</name>
  <files>test/debug-verdict-reporting.test.cjs</files>
  <action>
Create `test/debug-verdict-reporting.test.cjs` using node:test framework to validate the verdict rendering and gap persistence logic from debug-formal-context.cjs helpers and the debug workflow.

**Enumerated test cases (11 tests):**

1. `assembleFormalContext('no models match')` returns `{ verdict: 'no-model', constraints: [], models: [] }` — mock runBugModeMatching to return empty array
2. `assembleFormalContext('models exist, all pass')` returns `{ verdict: 'not-reproduced' }` — mock runBugModeMatching to return models, runModelCheckers to return all-pass
3. `assembleFormalContext('model checker fails')` returns `{ verdict: 'reproduced' }` with constraints populated — mock runModelCheckers to return a failure
4. `formatVerdictSummary('reproduced', models)` returns string containing "HIGH (model)" and the reproducing model name
5. `formatVerdictSummary('not-reproduced', models)` returns string containing "LOW (model)" and environmental note
6. `formatVerdictSummary('no-model', [])` returns string containing "N/A" and gap tracking note
7. `persistBugGap(entry)` appends to bug-model-gaps.json — write to temp dir, verify entry exists with correct schema (bug_id, description, timestamp, status: "no_coverage", worker_consensus_root_cause, worker_consensus_next_step)
8. `persistBugGap(duplicate)` does NOT re-append — call twice with same description, verify gaps array length is 1
9. `persistBugGap(corrupted JSON file)` fail-open — pre-write invalid JSON to gap file, call persist, verify it creates fresh `{ gaps: [...] }` structure
10. `formatConstraintBlock([5 constraints])` returns block with only 3 constraint lines (sliced)
11. `formatConstraintBlock([])` returns empty string

For tests 7-9 (gap persistence), implement a small `persistBugGap(description, bundle, consensusRoot, consensusStep, gapPath)` helper function within the test file that mirrors the inline node heredoc logic from Step G. This allows unit testing the persistence logic without spawning subprocesses. Use `os.tmpdir()` + unique subdirectory for file I/O, clean up after each test.

For tests 1-6 and 10-11, import functions directly from `bin/debug-formal-context.cjs` via `require()`. Use mock/stub approach for formal-scope-scan and model-constrained-fix dependencies.
  </action>
  <verify>
Run `node --test test/debug-verdict-reporting.test.cjs` — all tests pass.
Verify test count: `node --test test/debug-verdict-reporting.test.cjs 2>&1 | grep -E 'tests|pass|fail'` shows >= 11 tests, 0 failures.
  </verify>
  <done>
test/debug-verdict-reporting.test.cjs contains 11 enumerated tests covering: 3 verdict classification cases, 3 formatVerdictSummary cases, 3 gap persistence cases (append, dedup, fail-open), and 2 formatConstraintBlock cases (slicing, empty). All tests pass.
  </done>
</task>

</tasks>

<verification>
- `node --test test/debug-verdict-reporting.test.cjs` passes all 11 tests
- `commands/nf/debug.md` contains FORMAL row in Step E result table with exact text for all three verdicts
- `commands/nf/debug.md` contains inline node heredoc for gap persistence in Step G for no-model verdict
- Divergence note uses concrete constraint-substring detection logic
- Gap entries use SHA-256 bug_id consistent with Phase v0.38-01 pattern
- Deduplication prevents duplicate gap entries
</verification>

<success_criteria>
- NEXT STEP result table includes FORMAL row showing model verdict for all three cases with exact row text
- Step G appends no_coverage entry to bug-model-gaps.json via inline node heredoc when no model covers the failure
- Gap entry schema is compatible with Phase v0.38-01 and Phase v0.38-03 expectations
- Divergence note only renders when reproduced verdict AND constraint not found in consensus
- All 11 tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/v0.38-02-debug-command-integration/v0.38-02-02-SUMMARY.md`
</output>
