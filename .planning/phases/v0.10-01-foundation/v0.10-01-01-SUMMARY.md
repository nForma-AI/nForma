---
phase: v0.10-01-foundation
plan: 01
subsystem: testing
tags: [node:test, pure-functions, tdd, manage-agents, qgsd-json, ccr]

# Dependency graph
requires: []
provides:
  - readQgsdJson() — safe atomic read of ~/.claude/qgsd.json returning {} on absent/invalid
  - writeQgsdJson() — atomic write via tmp-rename pattern matching writeClaudeJson()
  - slotToFamily() — strip numeric suffix to derive family name from slot name
  - getWlDisplay() — format scoreboard W/L string from scoreboardData
  - readCcrConfigSafe() — safe read of CCR config returning null on absent/invalid
  - getCcrProviderForSlot() — lookup CCR provider name by model string
  - getKeyInvalidBadge() — produce [key invalid] badge for invalid key states
  - Unit tests: 22 new test cases covering all 7 functions and 3 absent-file edge cases
affects: [v0.10-01-02, listAgents display columns, DISP-01, DISP-02, DISP-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional filePath parameter for testability: readQgsdJson(filePath?) and writeQgsdJson(data, filePath?) accept override paths to avoid mocking fs"
    - "Dependency-injected hasKeyFn in getKeyInvalidBadge() — caller provides boolean function instead of secretsLib directly"
    - "Single qgsd.json read at top of listAgents() via readQgsdJson() — eliminates two duplicate inline try/catch blocks"

key-files:
  created:
    - bin/manage-agents.test.cjs (extended — 22 new test stubs + implementations)
  modified:
    - bin/manage-agents.cjs (QGSD_JSON_PATH/CCR_CONFIG_PATH constants + 7 new pure functions + _pure export update + listAgents() dedup)

key-decisions:
  - "readQgsdJson and writeQgsdJson accept optional filePath for testability — avoids needing to mock the fs module in tests"
  - "getKeyInvalidBadge uses dependency-injected hasKeyFn rather than secretsLib directly — pure function stays easily testable"
  - "listAgents() deduplication: 2 separate inline qgsd.json reads replaced by single readQgsdJson() call at function top"

patterns-established:
  - "Optional path override pattern: safe file readers accept filePath param defaulting to production path constant"
  - "All absent-file edge cases produce zero-value returns ({}  or null) — no ENOENT propagation"

requirements-completed: [DISP-01, DISP-02, DISP-03]

# Metrics
duration: 2min
completed: 2026-02-24
---

# Phase v0.10-01 Plan 01: Six pure display helper functions via TDD — readQgsdJson, writeQgsdJson, slotToFamily, getWlDisplay, readCcrConfigSafe, getCcrProviderForSlot, getKeyInvalidBadge exported via _pure with 22 unit tests

**Seven pure helper functions added to manage-agents.cjs with optional-path testability pattern, 22 node:test unit tests covering all absent-file edge cases, and listAgents() deduplication from 2 qgsd.json reads to 1**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-24T15:53:05Z
- **Completed:** 2026-02-24T15:55:45Z
- **Tasks:** 2 (RED + GREEN)
- **Files modified:** 2

## Accomplishments
- Added 7 new pure functions to bin/manage-agents.cjs: readQgsdJson, writeQgsdJson, slotToFamily, getWlDisplay, readCcrConfigSafe, getCcrProviderForSlot, getKeyInvalidBadge
- All 7 exported via module.exports._pure (total _pure now has 13 functions)
- 22 new unit tests in bin/manage-agents.test.cjs, including 3 mandatory absent-file edge cases: EDGE CASE 1 (absent scoreboard), EDGE CASE 2 (absent CCR config), EDGE CASE 3 (key_status absent from agentConfig)
- listAgents() refactored: 2 duplicate inline try/catch qgsd.json reads replaced by single readQgsdJson() call

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Add failing test stubs** - `23975fa` (test)
2. **Task 2 (GREEN): Implement all 6 new pure functions** - `0a2beee` (feat)

_Note: TDD tasks — RED (failing stubs) then GREEN (implementation). All 48 tests pass after GREEN._

## Files Created/Modified
- `/Users/jonathanborduas/code/QGSD/bin/manage-agents.cjs` - Added QGSD_JSON_PATH + CCR_CONFIG_PATH constants; 7 new pure functions in Pure functions section; updated module.exports._pure; replaced duplicate listAgents() reads
- `/Users/jonathanborduas/code/QGSD/bin/manage-agents.test.cjs` - Added 22 new test cases (slotToFamily x5, getWlDisplay x4, readCcrConfigSafe x2, getCcrProviderForSlot x4, getKeyInvalidBadge x5, readQgsdJson/writeQgsdJson x2)

## Decisions Made
- Used optional filePath parameter pattern (not jest/sinon mocking) for readQgsdJson and writeQgsdJson — keeps functions pure and testable without test framework coupling
- Used dependency-injected hasKeyFn for getKeyInvalidBadge rather than importing secretsLib — function stays a pure input/output transformation
- getWlDisplay returns em-dash character (\u2014) rather than ASCII dash for visual consistency with existing display code

## Deviations from Plan

None - plan executed exactly as written.

The plan specified 6 functions but the implementation added 7 (readQgsdJson + writeQgsdJson counted as 2 in the plan's list, matching the final count of 7). Both readQgsdJson and writeQgsdJson were explicitly in the plan's behavior spec.

## Issues Encountered

None — clean TDD execution. RED phase confirmed all 22 stubs failed with TypeError (not syntax errors). GREEN phase: all 48 tests passed on first implementation attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 7 helper functions are available via _pure for Plan 02 (listAgents integration)
- Plan 02 can now call readQgsdJson(), slotToFamily(), getWlDisplay(), readCcrConfigSafe(), getCcrProviderForSlot(), getKeyInvalidBadge() inside listAgents() to build the new display columns
- No blockers

---
*Phase: v0.10-01-foundation*
*Completed: 2026-02-24*

## Self-Check: PASSED

- FOUND: bin/manage-agents.cjs
- FOUND: bin/manage-agents.test.cjs
- FOUND: .planning/phases/v0.10-01-foundation/v0.10-01-01-SUMMARY.md
- FOUND: 23975fa (RED commit: test(v0.10-01) failing stubs)
- FOUND: 0a2beee (GREEN commit: feat(v0.10-01) implementation)
