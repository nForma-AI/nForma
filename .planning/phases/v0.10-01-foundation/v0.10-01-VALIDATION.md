---
phase: v0.10-01
slug: foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-24
---

# Phase v0.10-01 — Validation Strategy

> Generated by orchestrator during `/qgsd:plan-phase v0.10-01`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.10-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (built-in Node.js test runner) |
| **Config file** | none — Wave 0 adds test stubs to existing file |
| **Quick run command** | `node --test bin/manage-agents.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~2 seconds |
| **CI pipeline** | none (local only) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/manage-agents.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** ~2 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.10-01-01-01 | 01 | 0 | DISP-01, DISP-02, DISP-03 | unit (stub) | `node --test bin/manage-agents.test.cjs` | ✅ exists | ⬜ pending |
| v0.10-01-01-02 | 01 | 1 | DISP-01, DISP-02, DISP-03 | unit | `node --test bin/manage-agents.test.cjs` | ✅ exists | ⬜ pending |
| v0.10-01-01-03 | 01 | 1 | DISP-01, DISP-02, DISP-03 | unit | `node --test bin/manage-agents.test.cjs` | ✅ exists | ⬜ pending |
| v0.10-01-02-01 | 02 | 2 | DISP-01, DISP-02, DISP-03 | manual | `node bin/manage-agents.cjs` → list | ✅ N/A | ⬜ pending |
| v0.10-01-02-02 | 02 | 2 | DISP-01, DISP-02, DISP-03 | unit | `node --test bin/manage-agents.test.cjs` | ✅ exists | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/manage-agents.test.cjs` — add stubs for: `slotToFamily`, `readCcrConfigSafe`, `getCcrProviderForSlot`, `getWlDisplay`, `readQgsdJson`, `writeQgsdJson`; cover 3 absent-file edge cases (no scoreboard, no CCR config, no `key_status`)

*Wave 0 adds to the existing test file — no new file creation required.*

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/qgsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| W/L column displays correctly in terminal with ANSI color | DISP-01 | ANSI rendering is visual; automated tests verify logic only | Run `node bin/manage-agents.cjs`, choose List agents, confirm W/L column shows wins and losses per slot |
| CCR column shows provider name for slots whose model appears in CCR config | DISP-02 | Requires live `~/.claude-code-router/config.json` on target machine | Run `node bin/manage-agents.cjs`, confirm CCR column shows `akashml` / `together` / `—` per slot |
| `[key invalid]` badge appears for slot with `key_status.status: "invalid"` in `qgsd.json` | DISP-03 | Requires manual `qgsd.json` edit to inject `key_status` | Manually set `key_status.status = "invalid"` for one slot in `~/.claude/qgsd.json`, run `node bin/manage-agents.cjs` → list, confirm badge appears; restore after test |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test stubs cover all new pure functions
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 5s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.10-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 3 | `node --test bin/manage-agents.test.cjs` | — | — | ⬜ pending |
| 2 | 2 | `npm test` | — | — | ⬜ pending |

**Phase validation complete:** pending
