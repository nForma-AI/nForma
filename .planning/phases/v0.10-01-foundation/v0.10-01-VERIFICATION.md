---
phase: v0.10-01-foundation
verified: 2026-02-24T17:30:00Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Run listAgents() and inspect W/L column"
    expected: "Table header shows W/L column after Upd. Slots from claude family show values like '114W/0L' when .planning/quorum-scoreboard.json is present. When scoreboard file is absent, all W/L cells show the em-dash character with no crash."
    why_human: "Terminal rendering of ANSI columns and em-dash character requires visual inspection. Automated tests verify logic only; actual column layout and padding can only be confirmed live."
  - test: "Run listAgents() and inspect CCR column"
    expected: "Table header shows CCR column after W/L. Slots whose model appears in ~/.claude-code-router/config.json show the CCR provider name (e.g., akashml, together). Slots with no CCR route show '—'. When the config file is entirely absent, all slots show '—' with no error banner."
    why_human: "CCR column content depends on the live ~/.claude-code-router/config.json file which is machine-specific and not present in the test environment."
  - test: "Run listAgents() after injecting key_status.status=invalid into qgsd.json and inspect badge"
    expected: "The slot with key_status.status=invalid shows '[key invalid]' appended inline to its slot name in the Slot column. Slots without key_status in qgsd.json show no badge. Restore qgsd.json after test."
    why_human: "Badge behavior requires manually editing ~/.claude/qgsd.json to inject key_status, which is a live user-config file that automated verification must not touch."
---

# Phase v0.10-01: Foundation Verification Report

**Phase Goal:** The manage-agents list view shows quorum W/L, CCR routing, and key-invalid status per slot, and the readQgsdJson/writeQgsdJson helper pair is available for all later phases
**Verified:** 2026-02-24T17:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | readQgsdJson() returns {} when ~/.claude/qgsd.json is absent — no ENOENT thrown | VERIFIED | Function at line 1591; existsSync guard returns {}; live smoke test confirmed {}; unit test at test line 344 passes |
| 2 | writeQgsdJson() writes atomically via tmp-rename (same pattern as writeClaudeJson) | VERIFIED | Function at lines 1605-1610; writes to `p + '.tmp'` then fs.renameSync; roundtrip smoke test passed |
| 3 | slotToFamily('claude-3') returns 'claude', slotToFamily('gemini-1') returns 'gemini' | VERIFIED | Function at line 1616; regex `/-\d+$/` strips suffix; 5 unit tests pass |
| 4 | getWlDisplay returns '—' (em-dash) when scoreboardData is null (absent scoreboard file) | VERIFIED | Function at line 1626; returns '\u2014' on null; unit test for EDGE CASE 1 passes; smoke test confirmed |
| 5 | getCcrProviderForSlot returns null when ccrConfig is null (absent CCR config) | VERIFIED | Function at line 1654; null guard at line 1655; unit test for EDGE CASE 2 analog passes; smoke test confirmed |
| 6 | getKeyInvalidBadge returns empty string when key_status absent from qgsd.json agent_config | VERIFIED | Function at line 1671; slot absent guard returns ''; unit test for EDGE CASE 3 passes; smoke test confirmed |
| 7 | All 7 new pure functions exported via module.exports._pure | VERIFIED | _pure block at lines 1680-1694 lists all 7; smoke test `typeof _pure.X` returned 'function' for all 7 |
| 8 | All absent-file edge cases covered by node:test unit tests | VERIFIED | 48/48 tests pass including EDGE CASE 1 (scoreboard null), EDGE CASE 2 (CCR config absent), EDGE CASE 3 (key_status absent) |
| 9 | listAgents() loads scoreboard with existsSync guard and shows W/L column | VERIFIED | lines 205-212 in listAgents(); existsSync guard present; header at line 233 includes 'W/L'.padEnd(W.wl); row at line 331 includes wlCell |
| 10 | listAgents() calls readCcrConfigSafe() and shows CCR column | VERIFIED | line 215 calls readCcrConfigSafe(); header at line 234 includes 'CCR'.padEnd(W.ccr); row at line 332 includes ccrCell |
| 11 | listAgents() calls getKeyInvalidBadge() and appends badge to slot name | VERIFIED | lines 318-321 call getKeyInvalidBadge() and build slotDisplay = (name + keyBadge).padEnd(W.slot) |
| 12 | listAgents() reads qgsd.json exactly once (no duplicate reads) | VERIFIED | Single readQgsdJson() call at line 200; no other qgsd.json read inside listAgents(); line 482 is in editAgent() |
| 13 | W/L and CCR columns render correctly in terminal (visual) | HUMAN NEEDED | Header at lines 225-236; W object wl:8, ccr:12; row construction verified in code but visual layout requires human review |

**Score:** 12/12 automated truths verified; 1 truth requires human verification

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/manage-agents.cjs` | readQgsdJson, writeQgsdJson, readCcrConfigSafe, slotToFamily, getCcrProviderForSlot, getWlDisplay, getKeyInvalidBadge pure functions | VERIFIED | All 7 functions defined at lines 1591-1678; all exported via _pure at lines 1680-1694 |
| `bin/manage-agents.cjs` | Updated listAgents() with W/L, CCR, and key-invalid display columns | VERIFIED | scoreboard load at 205-212, CCR load at 215, W object expanded at 224, header at 225-236, per-row computation at 307-321, row array at 323-334 |
| `bin/manage-agents.test.cjs` | Unit tests for all 7 new pure functions covering 3 absent-file edge cases | VERIFIED | 22 new test cases added (lines 216-360); 48/48 total tests pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `bin/manage-agents.cjs` | `module.exports._pure` | Object literal spread of all 7 new functions | WIRED | Lines 1680-1694; _pure contains readQgsdJson, writeQgsdJson, slotToFamily, getWlDisplay, readCcrConfigSafe, getCcrProviderForSlot, getKeyInvalidBadge |
| `bin/manage-agents.test.cjs` | `bin/manage-agents.cjs` | `require('./manage-agents.cjs')._pure` with destructured function names | WIRED | Line 219: `const { slotToFamily, getWlDisplay, readCcrConfigSafe, getCcrProviderForSlot, getKeyInvalidBadge, readQgsdJson, writeQgsdJson } = _pure` |
| `listAgents()` | `.planning/quorum-scoreboard.json` | `fs.existsSync(sbPath)` guard + `JSON.parse(fs.readFileSync(...))` | WIRED | Lines 205-212; sbPath built from process.cwd() + '.planning/quorum-scoreboard.json'; existsSync guard verified |
| `listAgents()` | `~/.claude-code-router/config.json` | `readCcrConfigSafe()` call | WIRED | Line 215 calls `readCcrConfigSafe()` with no argument (uses CCR_CONFIG_PATH default defined at line 17) |
| `listAgents()` | `qgsd.json agent_config[slot].key_status` | `getKeyInvalidBadge()` call using qgsd from readQgsdJson() | WIRED | Line 200 reads qgsd via readQgsdJson(); line 203 extracts agentConfig; line 319 calls getKeyInvalidBadge(name, agentConfig, hasKeyFn) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| DISP-01 | v0.10-01-01, v0.10-01-02 | listAgents() shows quorum W/L column per slot (graceful empty state when scoreboard file absent) | SATISFIED | W/L column in header (line 233); wlCell computed via slotToFamily+getWlDisplay per row (lines 308-309); null scoreboard yields em-dash (verified); 48 tests pass |
| DISP-02 | v0.10-01-01, v0.10-01-02 | listAgents() shows CCR provider name per slot when CCR routing applies | SATISFIED | CCR column in header (line 234); ccrCell computed via readCcrConfigSafe+getCcrProviderForSlot per row (lines 313-314); absent config yields '—' (verified) |
| DISP-03 | v0.10-01-01, v0.10-01-02 | listAgents() shows [key invalid] badge when last health probe returned 401 AND a key is configured for that slot | SATISFIED (logic) / HUMAN NEEDED (visual) | getKeyInvalidBadge wired at line 319; slotDisplay includes keyBadge at line 321; logic verified by unit tests; visual appearance requires human inspection |

**Note on REQUIREMENTS.md:** The active REQUIREMENTS.md covers v0.12 (LOG, XST, VAL, TLA, ALY, PRM, PET requirements). DISP-01/02/03 are defined in `.planning/milestones/v0.11-REQUIREMENTS.md` — the v0.11 milestone requirements file. No orphaned requirements from the active REQUIREMENTS.md apply to this phase. This is expected: REQUIREMENTS.md was replaced with v0.12 content after v0.11 completed.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| `bin/manage-agents.cjs` | 156-158 | Comment block noting secretsLib not loaded in listAgents() scope — badge uses `hasKeyFn = (_) => true` as semantic simplification | Info | Not a bug: key_status is only written after editAgent/checkAgentHealth health probe which requires a key to be configured. The simplification is semantically correct. No action needed. |

No blocker or warning anti-patterns found.

### Human Verification Required

#### 1. W/L Column Visual Layout

**Test:** Run `node bin/manage-agents.cjs` and select "1. List agents"
**Expected:** Table header shows W/L column (8 chars wide) after Upd. For claude-family slots with .planning/quorum-scoreboard.json present, cells show values like `114W/0L`. When scoreboard is absent (or running outside a project directory), all W/L cells show `—` with no crash or ENOENT error.
**Why human:** Terminal ANSI column rendering and em-dash character alignment require visual inspection. Automated tests verify the pure function logic; the listAgents() table construction is code-verified but live layout can only be confirmed by running the interactive menu.

#### 2. CCR Column Visual Layout

**Test:** Run `node bin/manage-agents.cjs` and select "1. List agents"
**Expected:** Table header shows CCR column (12 chars wide) after W/L. Slots whose model appears in `~/.claude-code-router/config.json` providers show the CCR provider name (e.g., `akashml`, `together`). Slots with no CCR route show `—`. When `~/.claude-code-router/config.json` is absent, all CCR cells show `—` with no error banner.
**Why human:** CCR column content depends on the live `~/.claude-code-router/config.json` file which is machine-specific and absent in the automated test environment. The readCcrConfigSafe() function is unit-tested; the rendering in the table must be visually confirmed.

#### 3. Key-Invalid Badge Visual Appearance

**Test:** Temporarily add `"key_status": { "status": "invalid" }` to one slot under `agent_config` in `~/.claude/qgsd.json`, run `node bin/manage-agents.cjs` and select "1. List agents", then restore qgsd.json
**Expected:** The modified slot's name appears as `slotname [key invalid]` inline in the Slot column. Other slots show no badge. The badge text is ` [key invalid]` (space-prefixed, 14 chars).
**Why human:** Testing requires modifying `~/.claude/qgsd.json` which is a live user config file. Automated verification cannot safely inject and restore this state. The getKeyInvalidBadge() function is unit-tested; the inline rendering in listAgents() is code-verified but visual confirmation requires live execution.

### Automated Checks Summary

All automated checks passed:

- 48/48 node:test unit tests pass (`node --test bin/manage-agents.test.cjs` exits 0)
- All 7 pure functions present and exported via `module.exports._pure`
- All 3 absent-file edge cases verified by unit tests AND live smoke tests:
  - EDGE CASE 1: `getWlDisplay('claude', null)` returns '\u2014' (em-dash)
  - EDGE CASE 2: `getCcrProviderForSlot('any-model', null)` returns null
  - EDGE CASE 3: `getKeyInvalidBadge('claude-1', {}, () => true)` returns ''
- writeQgsdJson atomic tmp-rename roundtrip verified
- readQgsdJson returns {} on absent file verified
- listAgents() uses single readQgsdJson() call (no duplicate inline reads)
- All 3 documented commit hashes verified in git history:
  - `23975fa` — test(v0.10-01): add failing stubs for display helper functions
  - `0a2beee` — feat(v0.10-01): add readQgsdJson, writeQgsdJson, and display helper pure functions
  - `c9194cd` — feat(v0.10-01-02): integrate W/L, CCR, and key-invalid columns into listAgents()
- QGSD_JSON_PATH constant at line 16, CCR_CONFIG_PATH constant at line 17

---

_Verified: 2026-02-24T17:30:00Z_
_Verifier: Claude (qgsd-verifier)_
