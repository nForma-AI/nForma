---
plan: v0.19-10-02
status: complete
wave: 2
---

# Summary: v0.19-10-02 — Implementation GREEN

## What was built

**Task 1 — `bin/run-prism.cjs` policy wire-up:**
- Removed `const PRISM_PRIOR_TP = 0.85` and `const PRISM_PRIOR_UNAVAIL = 0.15` (lines 108–109)
- Replaced all 5 references with `policy.conservative_priors.tp_rate` and `policy.conservative_priors.unavail`
- `policy` object was already available at that code point (set at line 118 via `readPolicy()`)

**Task 2 — `bin/run-prism.test.cjs` stale test update:**
- Updated first test (`conservative priors used and logged when no scoreboard exists`) to read
  actual values from `policy.yaml` via `readPolicy()` instead of hardcoded `0.85`/`0.15` literals
- CALIB-04 test (RED from Plan 01) now passes GREEN

## Test results

- Before plan: 17 pass, 1 fail (RED CALIB-04 test)
- After plan:  18 pass, 0 fail (all GREEN)

## Verification checks

- `grep "PRISM_PRIOR_TP|PRISM_PRIOR_UNAVAIL" bin/run-prism.cjs` → 0 lines
- `grep "policy.conservative_priors" bin/run-prism.cjs` → 3 lines
- `node --test bin/run-prism.test.cjs` → 18 pass, 0 fail
- No hardcoded `0.85`/`0.15` literals remain in test assertions
- Pre-existing failures in `secrets.test.cjs` unchanged (not introduced by this phase)

## Commits

1. `feat(v0.19-10): wire readPolicy() conservative_priors to run-prism.cjs fallback constants`
2. `test(v0.19-10): GREEN — update prior assertions to read from policy.yaml`

## Self-Check: PASSED

- CALIB-04 contract satisfied: operators changing `formal/policy.yaml` `conservative_priors` values
  will see changes reflected in `run-prism.cjs` Args output at next run
- No regressions in `run-prism.test.cjs` (18/18 green)
