---
phase: v0.21-02-conformance-crisis-fix
plan: "03"
subsystem: testing
tags: [xstate, conformance, attribution, spec-bug, impl-bug, cjs]

# Dependency graph
requires:
  - phase: v0.21-02-conformance-crisis-fix plan 01
    provides: bin/xstate-trace-walker.cjs (replayTrace, evaluateTransitions)
  - phase: v0.21-02-conformance-crisis-fix plan 02
    provides: formal/.divergences.json (TTrace records for attribution input)
provides:
  - bin/attribute-trace-divergence.cjs — root-cause attribution tool (classifyDivergence, analyzeTrace)
  - bin/attribute-trace-divergence.test.cjs — 5 unit tests for attribution logic
affects:
  - v0.21-02-conformance-crisis-fix plan 04 (runs attribution pipeline to identify fix)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Heuristic attribution: 50/50 prior → adjust based on guard context and divergenceType
    - Fail-open exit: exit 0 when .divergences.json missing (diagnostic tool, not a gate)
    - classifyDivergence and analyzeTrace exported for unit testing (not inside if main block)

key-files:
  created:
    - bin/attribute-trace-divergence.cjs
    - bin/attribute-trace-divergence.test.cjs
  modified: []

key-decisions:
  - "classifyDivergence starts at 50/50 prior — context determines which direction to shift"
  - "unmappable_action always 90% impl-bug (hook emitted unknown event type)"
  - "Uninitialized count fields (0/null for Count/Available/Polled) → +30 impl-bug confidence"
  - "Populated context with guard still failing → +20 spec-bug confidence"
  - "Tool exits 0 if .divergences.json missing — not a failure state"
  - "Machine/walker params passed to analyzeTrace/classifyDivergence but not used (future extension point)"

patterns-established:
  - "Pattern 1: Attribution heuristics via confidence deltas from a 50/50 prior"
  - "Pattern 2: classifyDivergence exported for pure unit testing without file I/O"

requirements-completed: [DIAG-02]

# Metrics
duration: 5min
completed: 2026-03-01
---

# Plan v0.21-02-03 Summary

**attribute-trace-divergence.cjs: root-cause attribution tool classifying XState divergences as spec-bug vs impl-bug with heuristic confidence scores**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-01T00:20:00Z
- **Completed:** 2026-03-01T00:25:00Z
- **Tasks:** 2 (Task 1: attribution tool, Task 2: tests)
- **Files modified:** 2

## Accomplishments
- `classifyDivergence` heuristic: 50/50 prior, adjusted by guard context signals (uninitialized fields → impl-bug; populated context + guard failure → spec-bug)
- `unmappable_action` divergences always classified as 90% impl-bug
- `--input`, `--batch-size`, `--output-json` CLI flags implemented
- Fail-open exit 0 when `.divergences.json` missing
- All 5 unit tests GREEN; `classifyDivergence` and `analyzeTrace` exported

## Task Commits

Each task was committed atomically:

1. **Task 1+2: attribution tool + tests** - `ce0a39db` (feat)

## Files Created/Modified
- `bin/attribute-trace-divergence.cjs` — Root-cause attribution CLI (120+ lines), exports classifyDivergence + analyzeTrace
- `bin/attribute-trace-divergence.test.cjs` — 5 unit tests covering impl-bug, spec-bug, unmappable_action, output fields, graceful fallback

## Decisions Made
- Heuristic-based classification (not ML) — simple and auditable for a conformance debugging tool.
- Machine/walker params included in `analyzeTrace` signature for future enhancement (currently unused — TTrace guardEvaluations already contain guard results).

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## Next Phase Readiness
- Attribution pipeline ready: `node bin/attribute-trace-divergence.cjs` reads `formal/.divergences.json` and outputs analysis
- `--output-json` flag produces structured JSON for Plan 04 pipeline

---
*Phase: v0.21-02-conformance-crisis-fix*
*Completed: 2026-03-01*
