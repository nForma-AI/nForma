---
phase: v0.21-02-conformance-crisis-fix
plan: "02"
subsystem: testing
tags: [xstate, conformance, tdd, cjs, ttrace, divergence-export]

# Dependency graph
requires:
  - phase: v0.21-02-conformance-crisis-fix plan 01
    provides: bin/xstate-trace-walker.cjs (evaluateTransitions, replayTrace, evaluateGuard)
provides:
  - bin/validate-traces.cjs buildTTrace helper (session-grouped guard evaluation)
  - formal/.divergences.json (up to 10 TTrace records from most recent validate-traces run)
  - 6 new unit tests for buildTTrace behavior
affects:
  - v0.21-02-conformance-crisis-fix plan 03 (reads formal/.divergences.json for attribution)
  - v0.21-02-conformance-crisis-fix plan 04 (runs attribution pipeline)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Round-grouped TTrace construction — events grouped by round_id before guard evaluation
    - Standalone event approximation — events without round_id treated as singleton groups
    - Atomic .divergences.json write (tmp + rename)
    - Lazy walker require inside if (require.main === module) to preserve module.exports purity

key-files:
  created:
    - formal/.divergences.json
  modified:
    - bin/validate-traces.cjs
    - bin/validate-traces.test.cjs

key-decisions:
  - "buildTTrace placed before the if(require.main === module) block so it is accessible from module.exports"
  - "Walker lazy-loaded inside main block to avoid breaking module.exports usage (walker requires XState machine)"
  - "First pass: parse JSON lines; second pass: validate — allows round-grouping before validation loop"
  - "state_mismatch divergences get full TTrace structure; unmappable_action and mcp_field_validation get plain objects (no guard context to analyze)"
  - "formal/.divergences.json written before writeCheckResult call — data persists even if check-result write fails"

patterns-established:
  - "Pattern 1: Two-pass JSONL processing — parse all lines, group by round_id, then validate each"
  - "Pattern 2: TTrace as the canonical divergence record format for state_mismatch events"

requirements-completed: [DIAG-01]

# Metrics
duration: 8min
completed: 2026-03-01
---

# Plan v0.21-02-02 Summary

**validate-traces.cjs enhanced: buildTTrace with session-grouped guard evaluation writes TTrace records to formal/.divergences.json**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-01T00:12:00Z
- **Completed:** 2026-03-01T00:20:00Z
- **Tasks:** 2 (Task 1: buildTTrace + TTrace export, Task 2: tests)
- **Files modified:** 3 (validate-traces.cjs, validate-traces.test.cjs, formal/.divergences.json)

## Accomplishments
- Added `buildTTrace` helper with round-grouped guard evaluation — guards in event N see context from events 1..N-1
- Standalone events (no round_id) handled as singleton groups — no silent data loss
- `formal/.divergences.json` written atomically (tmp + rename) with up to 10 TTrace records after each run
- All 38 validate-traces tests GREEN (6 existing tests kept + 6 new buildTTrace tests added)

## Task Commits

Each task was committed atomically:

1. **Task 1+2: buildTTrace + TTrace export + tests** - `85584661` (feat)

## Files Created/Modified
- `bin/validate-traces.cjs` — Enhanced with `buildTTrace` function and `formal/.divergences.json` write; `buildTTrace` exported
- `bin/validate-traces.test.cjs` — 6 new buildTTrace unit tests appended (38 total, all GREEN)
- `formal/.divergences.json` — Created by running validate-traces.cjs; 10 TTrace records of `state_mismatch` type

## Decisions Made
- Two-pass approach: parse all lines first → group by round_id → validate each. This allows session context to be available before the guard evaluation loop.
- Walker lazy-loaded inside `if (require.main === module)` to preserve the pure module.exports interface.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- Existing `bin/validate-traces.test.cjs` already had many tests (38 total). Added the 6 new buildTTrace tests as an append rather than replacing the file.

## Next Phase Readiness
- `formal/.divergences.json` exists with 10 TTrace records, ready for Plan 03 (attribute-trace-divergence)
- `buildTTrace` exported from `validate-traces.cjs`

---
*Phase: v0.21-02-conformance-crisis-fix*
*Completed: 2026-03-01*
