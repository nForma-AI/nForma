---
phase: v0.9-05
plan: "02"
type: execute
wave: 2
depends_on:
  - v0.9-05-01
files_modified:
  - agents/qgsd-executor.md
  - agents/qgsd-verifier.md
  - agents/qgsd-roadmapper.md
  - agents/qgsd-debugger.md
  - agents/qgsd-plan-checker.md
  - agents/qgsd-planner.md
  - agents/qgsd-project-researcher.md
  - agents/qgsd-research-synthesizer.md
  - agents/qgsd-phase-researcher.md
  - qgsd-core/templates/phase-prompt.md
  - qgsd-core/templates/codebase/structure.md
  - hooks/qgsd-circuit-breaker.js
autonomous: true
requirements:
  - REN-03

must_haves:
  truths:
    - "No agent file references ~/.claude/get-shit-done/ (50 occurrences replaced across 9 files)"
    - "qgsd-core/templates/phase-prompt.md references ~/.claude/qgsd/ not ~/.claude/get-shit-done/"
    - "hooks/qgsd-circuit-breaker.js message strings reference qgsd-core/ not get-shit-done/"
  artifacts:
    - path: "agents/qgsd-executor.md"
      provides: "Executor agent with updated runtime paths"
      contains: "~/.claude/qgsd/"
    - path: "agents/qgsd-planner.md"
      provides: "Planner agent with updated runtime paths"
      contains: "~/.claude/qgsd/"
    - path: "qgsd-core/templates/phase-prompt.md"
      provides: "Phase prompt template with updated installed-path references"
      contains: "~/.claude/qgsd/"
    - path: "hooks/qgsd-circuit-breaker.js"
      provides: "Circuit breaker hook with updated workflow path in message strings"
      contains: "qgsd-core/workflows/oscillation-resolution-mode.md"
  key_links:
    - from: "agents/*.md"
      to: "~/.claude/qgsd/bin/gsd-tools.cjs"
      via: "path reference in agent instructions"
      pattern: "~/.claude/qgsd/"
    - from: "qgsd-core/templates/phase-prompt.md"
      to: "~/.claude/qgsd/workflows/"
      via: "@-reference in template"
      pattern: "@.*~/.claude/qgsd/"
    - from: "hooks/qgsd-circuit-breaker.js"
      to: "qgsd-core/workflows/oscillation-resolution-mode.md"
      via: "message string in hook output"
      pattern: "qgsd-core/workflows/oscillation-resolution-mode"
---

<objective>
Update all hardcoded `~/.claude/get-shit-done/` runtime path references in agent files and templates, and update the `get-shit-done/workflows/` source path references in the circuit breaker hook's message strings.

Purpose: After the directory rename, the installed runtime path (`~/.claude/qgsd/`) is already correct — the installer maps to it. But the agents, templates, and hook message strings still hardcode the old paths. This plan eliminates every remaining `get-shit-done` string from the actively-used source files.
Output: 6 agent files, 2 templates, and 1 hook with zero `get-shit-done/` path references remaining.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.9-05-rename-get-shit-done-qgsd-core/v0.9-05-RESEARCH.md
@.planning/phases/v0.9-05-rename-get-shit-done-qgsd-core/v0.9-05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agents/*.md — 43 path occurrences across 6 files</name>
  <files>
    agents/qgsd-executor.md
    agents/qgsd-verifier.md
    agents/qgsd-roadmapper.md
    agents/qgsd-debugger.md
    agents/qgsd-plan-checker.md
    agents/qgsd-planner.md
    agents/qgsd-project-researcher.md
    agents/qgsd-research-synthesizer.md
    agents/qgsd-phase-researcher.md
  </files>
  <action>
    In each of the 9 agent files, replace all occurrences of:
    ```
    ~/.claude/get-shit-done/
    ```
    with:
    ```
    ~/.claude/qgsd/
    ```

    Occurrence counts (use these to verify completeness):
    - `agents/qgsd-executor.md` — 13 occurrences
    - `agents/qgsd-verifier.md` — 7 occurrences
    - `agents/qgsd-roadmapper.md` — 2 occurrences
    - `agents/qgsd-debugger.md` — 1 occurrence
    - `agents/qgsd-plan-checker.md` — 5 occurrences
    - `agents/qgsd-planner.md` — 3 occurrences
    - `agents/qgsd-project-researcher.md` — 1 occurrence
    - `agents/qgsd-research-synthesizer.md` — 3 occurrences
    - `agents/qgsd-phase-researcher.md` — 3 occurrences
    Total: 50 occurrences across 9 files

    After each file, spot-check one representative line to confirm replacement succeeded.

    Do NOT change:
    - Any `get-shit-done-cc` npm package name references (none expected in agents/)
    - Any CHANGELOG.md references (not an agent file)
    - The QGSD project identity text — only path references change
  </action>
  <verify>
    ```bash
    grep -r 'get-shit-done/' agents/ | wc -l
    ```
    Must return 0. Any non-zero result means a missed occurrence — re-examine that file.

    Also confirm the replacement happened (not just deletion):
    ```bash
    grep -r '~/.claude/qgsd/' agents/ | wc -l
    ```
    Should return approximately 50.
  </verify>
  <done>
    `grep -r 'get-shit-done/' agents/ | wc -l` returns 0. All 9 agent files updated. `grep -r '~/.claude/qgsd/' agents/` shows ~50 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update qgsd-core/templates/phase-prompt.md + codebase/structure.md</name>
  <files>
    qgsd-core/templates/phase-prompt.md
    qgsd-core/templates/codebase/structure.md
  </files>
  <action>
    **`qgsd-core/templates/phase-prompt.md` — 9 occurrences:**

    Replace all occurrences of `~/.claude/get-shit-done/` with `~/.claude/qgsd/`.

    The specific lines (from research) that must change:
    - `@~/.claude/get-shit-done/workflows/execute-plan.md` (×2) → `@~/.claude/qgsd/workflows/execute-plan.md`
    - `@~/.claude/get-shit-done/templates/summary.md` (×2) → `@~/.claude/qgsd/templates/summary.md`
    - `@~/.claude/get-shit-done/references/checkpoints.md` (×3) → `@~/.claude/qgsd/references/checkpoints.md`
    - `~/.claude/get-shit-done/references/tdd.md` → `~/.claude/qgsd/references/tdd.md`
    - `~/.claude/get-shit-done/templates/user-setup.md` → `~/.claude/qgsd/templates/user-setup.md`
    - `~/.claude/get-shit-done/workflows/verify-phase.md` → `~/.claude/qgsd/workflows/verify-phase.md`

    **`qgsd-core/templates/codebase/structure.md` — structural description:**

    Replace the `get-shit-done/` directory name in structural descriptions with `qgsd-core/`. This file describes the project structure; the directory name changed.

    After edits, confirm:
    ```bash
    grep 'get-shit-done' qgsd-core/templates/phase-prompt.md | wc -l
    grep 'get-shit-done' qgsd-core/templates/codebase/structure.md | wc -l
    ```
    Both must return 0.
  </action>
  <verify>
    ```bash
    grep 'get-shit-done' qgsd-core/templates/phase-prompt.md | wc -l
    grep 'get-shit-done' qgsd-core/templates/codebase/structure.md | wc -l
    ```
    Both must output `0`.

    Spot-check the replacements:
    ```bash
    grep '~/.claude/qgsd/workflows/execute-plan' qgsd-core/templates/phase-prompt.md | wc -l
    ```
    Must return ≥2 (the two @-references).
  </verify>
  <done>
    Zero `get-shit-done` references remain in either template file. `qgsd-core/templates/phase-prompt.md` contains `~/.claude/qgsd/` references for all @-imports.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update hooks/qgsd-circuit-breaker.js message strings</name>
  <files>hooks/qgsd-circuit-breaker.js</files>
  <action>
    In `hooks/qgsd-circuit-breaker.js`, update the two message strings at lines 419 and 456 that reference:
    ```
    get-shit-done/workflows/oscillation-resolution-mode.md
    ```
    Change to:
    ```
    qgsd-core/workflows/oscillation-resolution-mode.md
    ```

    These are message strings shown to Claude when the circuit breaker fires, directing it to the oscillation resolution workflow. Only the path portion within the message changes — surrounding message text stays the same.

    Do NOT touch `hooks/dist/qgsd-circuit-breaker.js` at this stage — the dist sync happens in Plan 03.

    Confirm:
    ```bash
    grep 'get-shit-done/workflows' hooks/qgsd-circuit-breaker.js | wc -l
    ```
    Must return 0.

    ```bash
    grep 'qgsd-core/workflows/oscillation-resolution-mode' hooks/qgsd-circuit-breaker.js | wc -l
    ```
    Must return 2 (lines 419 and 456).
  </action>
  <verify>
    ```bash
    grep 'get-shit-done/workflows' hooks/qgsd-circuit-breaker.js | wc -l
    ```
    Must output `0`.

    ```bash
    diff <(grep 'oscillation-resolution-mode' hooks/qgsd-circuit-breaker.js) \
         <(echo "expected 2 occurrences with qgsd-core path")
    grep 'qgsd-core/workflows/oscillation-resolution-mode' hooks/qgsd-circuit-breaker.js
    ```
    Must show 2 lines with `qgsd-core/`.
  </verify>
  <done>
    `grep 'get-shit-done/workflows' hooks/qgsd-circuit-breaker.js` returns 0. Lines 419 and 456 reference `qgsd-core/workflows/oscillation-resolution-mode.md`. `hooks/dist/` is NOT yet updated (that happens in Plan 03).
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run comprehensive check:

```bash
# Verify no get-shit-done/ path refs remain in agents, templates, or hook source
grep -r 'get-shit-done/' agents/ | wc -l          # → 0
grep 'get-shit-done/' qgsd-core/templates/phase-prompt.md | wc -l   # → 0
grep 'get-shit-done/' qgsd-core/templates/codebase/structure.md | wc -l  # → 0
grep 'get-shit-done/workflows' hooks/qgsd-circuit-breaker.js | wc -l  # → 0

# Verify replacements landed
grep -r '~/.claude/qgsd/' agents/ | wc -l         # → ~43
grep '~/.claude/qgsd/' qgsd-core/templates/phase-prompt.md | wc -l  # → ~9
grep 'qgsd-core/workflows' hooks/qgsd-circuit-breaker.js | wc -l    # → 2
```
</verification>

<success_criteria>
- Zero `get-shit-done/` path occurrences in all 9 agent files
- Zero `get-shit-done/` occurrences in `qgsd-core/templates/phase-prompt.md`
- Zero `get-shit-done/` occurrences in `qgsd-core/templates/codebase/structure.md`
- Zero `get-shit-done/workflows` occurrences in `hooks/qgsd-circuit-breaker.js`
- `~/.claude/qgsd/` appears in agent files (~50 times)
- `qgsd-core/workflows/oscillation-resolution-mode.md` appears 2x in hook source
- `hooks/dist/qgsd-circuit-breaker.js` NOT YET updated (Plan 03 handles this)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.9-05-rename-get-shit-done-qgsd-core/v0.9-05-02-SUMMARY.md`
</output>
