---
phase: v0.13-06
slug: deploy-is-gap-closure-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-25
---

# Phase v0.13-06 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.13-06`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.13-06`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | grep / bash inspection (no test suite — deployment-only phase) |
| **Config file** | none |
| **Quick run command** | `grep -n "IS_GAP_CLOSURE" ~/.claude/qgsd/workflows/transition.md` |
| **Full suite command** | `grep -c '\-A 15.*Phase.*COMPLETED_PHASE' ~/.claude/qgsd/workflows/transition.md && grep -c 'A 4.*\^###.*Phase.*COMPLETED_PHASE' ~/.claude/qgsd/workflows/transition.md` |
| **Estimated runtime** | ~5 seconds |
| **CI pipeline** | none — local install operation |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `grep -n "IS_GAP_CLOSURE" ~/.claude/qgsd/workflows/transition.md`
- **After every plan wave:** Run full grep suite (old pattern count=0, new pattern count=1)
- **Before `/qgsd:verify-work`:** Both grep checks must pass
- **Maximum acceptable task feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.13-06-01-01 | 01 | 1 | LOOP-02 | smoke | `node bin/install.js --claude --global 2>&1 \| grep "Installed qgsd"` | N/A | ⬜ pending |
| v0.13-06-01-02 | 01 | 1 | LOOP-02 | integration | `grep -c '\-A 15.*Phase.*COMPLETED_PHASE' ~/.claude/qgsd/workflows/transition.md` (expect 0) | N/A | ⬜ pending |
| v0.13-06-01-03 | 01 | 1 | LOOP-02 | integration | `grep -c 'A 4.*\^###.*Phase.*COMPLETED_PHASE' ~/.claude/qgsd/workflows/transition.md` (expect 1) | N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

This phase has no code changes. The "test" is running the installer and grepping the output file. No test scaffolding required before execution.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Runtime IS_GAP_CLOSURE routing works end-to-end | LOOP-02 | Requires completing a gap closure phase to trigger the live transition path | Complete a gap closure phase and verify it routes to audit-milestone re-audit instead of complete-milestone |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 5s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.13-06`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | grep checks | — | — | ⬜ pending |

**Phase validation complete:** pending
