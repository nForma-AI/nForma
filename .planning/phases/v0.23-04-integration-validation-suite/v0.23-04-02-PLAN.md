---
phase: v0.23-04-integration-validation-suite
plan: 02
type: execute
wave: 2
depends_on:
  - v0.23-04-01
files_modified:
  - bin/test-formal-integration.test.cjs
autonomous: true
requirements:
  - IVL-02
formal_artifacts: none

must_haves:
  truths:
    - "The test covers the full chain: plan-phase keyword scan populates FORMAL_SPEC_CONTEXT, executor fires run-formal-check.cjs, verifier receives FORMAL_CHECK_RESULT containing actual TLC output with at least one checked property"
    - "verify-formal-results.cjs parseNDJSON and groupByFormalism can consume real run-formal-check output and produce correct pass/fail/skip counts"
    - "The FORMAL_CHECK_RESULT JSON structure emitted by run-formal-check.cjs is parseable by the verifier pipeline without transformation"
    - "Temp NDJSON files are created in os.tmpdir() and reliably cleaned up even when tests fail"
    - "The new test file is discoverable by npm test and integrates with the existing test suite without requiring special flags"
  artifacts:
    - path: "bin/test-formal-integration.test.cjs"
      provides: "Full chain integration tests proving plan-phase -> execute-phase -> verifier handoff works"
      contains: "IVL-02"
  key_links:
    - from: "bin/test-formal-integration.test.cjs"
      to: "bin/run-formal-check.cjs"
      via: "spawnSync invocation capturing FORMAL_CHECK_RESULT"
      pattern: "FORMAL_CHECK_RESULT"
    - from: "bin/test-formal-integration.test.cjs"
      to: "bin/verify-formal-results.cjs"
      via: "require() and calling parseNDJSON/groupByFormalism"
      pattern: "require.*verify-formal-results"
    - from: "bin/run-formal-check.cjs"
      to: "bin/verify-formal-results.cjs"
      via: "FORMAL_CHECK_RESULT JSON structure compatibility"
      pattern: "passed.*failed.*skipped"
---

<objective>
Add full-chain integration tests proving the plan-phase -> execute-phase -> verifier pipeline actually works end-to-end: FORMAL_SPEC_CONTEXT population leads to run-formal-check execution, which produces FORMAL_CHECK_RESULT that verify-formal-results.cjs can consume and embed in VERIFICATION.md.

Purpose: IVL-02 requires proof that the ENTIRE chain works -- not just individual tools in isolation. This plan tests the handoff points: keyword scan output structure, run-formal-check FORMAL_CHECK_RESULT format, and verify-formal-results.cjs consumption of that output.

Output: Extended bin/test-formal-integration.test.cjs with IVL-02 full-chain tests
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.23-04-integration-validation-suite/v0.23-04-RESEARCH.md
@.planning/phases/v0.23-04-integration-validation-suite/v0.23-04-01-SUMMARY.md
@bin/run-formal-check.cjs
@bin/verify-formal-results.cjs
@bin/test-formal-integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IVL-02 full chain structural tests</name>
  <files>bin/test-formal-integration.test.cjs</files>
  <action>
Extend bin/test-formal-integration.test.cjs (created by Plan 01) with a new describe block: `describe('IVL-02: full chain validation', () => { ... })`

**Chain Step 1: FORMAL_SPEC_CONTEXT population test**

Test: `IVL-02 chain: plan-phase keyword scan produces FORMAL_SPEC_CONTEXT structure`

The plan-phase formal scope scan (implemented in v0.23-01) reads formal/spec/{module}/invariants.md files and matches modules by keywords. Simulate this by:
1. Read formal/spec/ directory to get all module names
2. For each module, verify formal/spec/{module}/invariants.md exists and is non-empty
3. Verify at least the known modules (quorum, breaker, deliberation) have invariants.md files
4. Verify invariants.md files contain expected structure: "Property:" and "Config line:" markers
5. This proves the source data for FORMAL_SPEC_CONTEXT is present and well-formed

**Chain Step 2: execute-phase fires run-formal-check test**

Test: `IVL-02 chain: run-formal-check produces FORMAL_CHECK_RESULT with at least one checked property`

1. Invoke `node bin/run-formal-check.cjs --modules=quorum` via spawnSync (skip if Java absent)
2. Parse FORMAL_CHECK_RESULT JSON from stdout
3. Use assertFormalCheckResultShape(result) to cross-validate structure against verifier expectations
4. Verify result has `passed >= 1` (at least one property was actually checked)
5. Verify the stdout also contains the summary line with "checks, X passed"
6. Capture the full stdout for Step 3

**Chain Step 3: verifier receives and parses FORMAL_CHECK_RESULT test**

Test: `IVL-02 chain: verify-formal-results.cjs consumes run-formal-check output`

1. Import { parseNDJSON, groupByFormalism, generateFVSection } from verify-formal-results.cjs
2. Create a temporary NDJSON file from real run-formal-check output. **CRITICAL: Create temp files in os.tmpdir() using a unique filename** (e.g., `path.join(os.tmpdir(), 'qgsd-ivl02-test-' + Date.now() + '.ndjson')`). To get real output: invoke run-formal-check --modules=quorum via spawnSync, then transform the per-check result lines into NDJSON format that verify-formal-results.cjs expects. Each check result from run-formal-check produces a JSON object with: module, tool, status, detail, runtimeMs. Write these as one-JSON-per-line to the temp file.
3. Call parseNDJSON(tempFilePath) -- verify it returns a non-empty array
4. Call groupByFormalism(results) -- verify it returns an object with at least one formalism key (e.g., 'tlc') and that formalism's pass count > 0
5. If generateFVSection is exported, call it and verify it returns a non-empty string containing "pass" or "Formal"
6. **CRITICAL: Clean up temp file using try/finally pattern to ensure deletion even on assertion failure:**
```javascript
const tmpFile = path.join(os.tmpdir(), `qgsd-ivl02-test-${Date.now()}.ndjson`);
try {
  fs.writeFileSync(tmpFile, ndjsonContent);
  // ... assertions ...
} finally {
  try { fs.unlinkSync(tmpFile); } catch (_) { /* ignore if already removed */ }
}
```
7. **Add explicit cleanup verification:** After the finally block, assert that the temp file no longer exists: `assert.equal(fs.existsSync(tmpFile), false, 'temp NDJSON file should be cleaned up');`

**Alternative approach if NDJSON format differs from run-formal-check output:**
If run-formal-check.cjs output format doesn't directly match what parseNDJSON expects, test the JSON structure compatibility instead:
1. Parse FORMAL_CHECK_RESULT from run-formal-check stdout
2. Verify its shape ({passed, failed, skipped, counterexamples}) matches what the verifier expects using assertFormalCheckResultShape
3. Verify verify-formal-results.cjs exports are callable and handle the data

**Chain Step 4: end-to-end round-trip test**

Test: `IVL-02 chain: full round-trip -- spec files exist, tool runs, result is parseable by verifier`

This is the money test that proves the ENTIRE chain:
1. Verify formal/spec/quorum/invariants.md exists (plan-phase scan source)
2. Invoke run-formal-check --modules=quorum (execute-phase equivalent)
3. Parse FORMAL_CHECK_RESULT from stdout
4. Use assertFormalCheckResultShape(result) to validate structure
5. Pass result to verify-formal-results.cjs groupByFormalism (or equivalent)
6. Verify final output has pass count > 0 and fail count === 0
7. This single test spans all three handoff points: spec files -> tool execution -> result parsing

If Java absent, skip all chain tests with descriptive message "Java required for full chain validation".

**Key rules:**
- Do NOT mock any subprocess or module import
- Use real invariants.md files from formal/spec/ (not test fixtures)
- Use real run-formal-check.cjs output (not hardcoded JSON)
- ALL temp files MUST be created in os.tmpdir() with unique names (prevents collisions in parallel test runs) and cleaned up via try/finally (prevents disk accumulation across multiple test runs in CI)
- Use `const os = require('os');` for temp dir
- After each test that creates temp files, assert the temp file no longer exists to catch cleanup bugs early
  </action>
  <verify>
Run: `node --test bin/test-formal-integration.test.cjs`
Expected: All IVL-02 tests pass (Java present) or skip (Java absent). The round-trip test proves spec files exist, tool runs, and result is parseable.
Verify IVL-02 test names appear in output: grep for "IVL-02 chain" in test output.
Verify: `ls /tmp/qgsd-ivl02-test-* 2>/dev/null` returns nothing (all temp files cleaned up).
  </verify>
  <done>
bin/test-formal-integration.test.cjs contains IVL-02 full chain tests. The round-trip test proves: formal/spec/ invariants exist -> run-formal-check.cjs produces real FORMAL_CHECK_RESULT -> verify-formal-results.cjs can parse the output. All handoff points validated with real data. No mocking. All temp NDJSON files created in os.tmpdir() and reliably cleaned up via try/finally, with post-cleanup existence assertions to catch leaks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run complete test suite and confirm all GREEN</name>
  <files>bin/test-formal-integration.test.cjs</files>
  <action>
Run the complete test suite to confirm all tests from Plan 01 (IVL-01, IVL-03, ENF-03) and Plan 02 (IVL-02) pass together.

1. Run: `node --test bin/test-formal-integration.test.cjs`
2. Verify: total test count >= 22 (7 IVL-01/ENF-03 + 11 IVL-03 + 4+ IVL-02)
3. Verify: 0 failures (all pass or skip)
4. Verify: no "mock" or "stub" strings in the test file (`grep -c "mock\|stub" bin/test-formal-integration.test.cjs` should return 0)
5. Verify: FORMAL_CHECK_RESULT is parsed from real stdout in multiple tests (`grep -c "FORMAL_CHECK_RESULT" bin/test-formal-integration.test.cjs` should return >= 5)
6. Verify: assertFormalCheckResultShape is called in structural and regression tests (`grep -c "assertFormalCheckResultShape" bin/test-formal-integration.test.cjs` should return >= 4)
7. Verify: temp file cleanup is present (`grep -c "unlinkSync\|finally" bin/test-formal-integration.test.cjs` should return >= 1)

If any test fails, diagnose and fix:
- If a TLA+ spec fails TLC (counterexample found), that's a real regression -- do NOT suppress the failure. Report it.
- If Java is absent and a structural test crashes instead of skipping, fix the skip guard logic.
- If verify-formal-results.cjs import fails, check the export path.

**npm test integration validation:**

After all tests pass individually, run the project's full test suite to ensure the new test file is discoverable:
1. Run: `npm test 2>&1 | tail -30`
2. Verify: the new test file bin/test-formal-integration.test.cjs appears in the npm test output (it should be auto-discovered by the existing `--test` glob pattern in package.json or by node:test's default discovery)
3. If the file does NOT appear in npm test output, check package.json's `test` script. The existing pattern should be `node --test 'bin/*.test.cjs'` or similar. If the glob doesn't match, report the issue -- do NOT change package.json unless the glob genuinely excludes the new file by mistake.
4. Count the total test count in npm test output. It should now be the existing 71+ tests plus the new 22+ tests from this file.
5. Verify 0 failures across the entire suite.

This confirms the new 22+ test file integrates cleanly with the existing test suite and is visible to CI via the default npm test command.
  </action>
  <verify>
Run: `node --test bin/test-formal-integration.test.cjs` -- all pass or skip, 0 failures
Run: `npm test` -- no regressions in existing tests AND new test file appears in output
Run: `wc -l bin/test-formal-integration.test.cjs` -- should be 120+ lines (substantial test file)
Run: `ls /tmp/qgsd-ivl02-test-* 2>/dev/null` -- no leftover temp files
  </verify>
  <done>
All integration tests pass (or skip gracefully). The test file has 22+ tests with 0 failures. The full npm test suite passes without regressions and includes the new test file in its discovery. bin/test-formal-integration.test.cjs is a complete integration validation suite covering IVL-01, IVL-02, IVL-03, and ENF-03. No leftover temp files on disk.
  </done>
</task>

</tasks>

<verification>
1. `node --test bin/test-formal-integration.test.cjs` -- all tests pass or skip, 0 failures
2. `npm test` -- full suite passes, no regressions from new test file, new file is discoverable
3. Grep for "IVL-02 chain" in test file -- should find 4+ test names
4. Grep for "verify-formal-results" in test file -- should find require() import
5. The round-trip test (IVL-02 chain: full round-trip) validates all three handoff points in a single test
6. `ls /tmp/qgsd-ivl02-test-* 2>/dev/null` returns nothing -- all temp NDJSON files cleaned up
</verification>

<success_criteria>
- IVL-02: Full chain test proves plan-phase scan -> execute-phase run-formal-check -> verifier verify-formal-results pipeline works with real data
- All 22+ tests pass or skip gracefully
- npm test shows no regressions and discovers the new test file
- Zero mocking in entire test file
- Temp NDJSON files created in os.tmpdir() and cleaned up via try/finally even on test failure
- FORMAL_CHECK_RESULT structure cross-validated against verifier expectations via assertFormalCheckResultShape
</success_criteria>

<output>
After completion, create `.planning/phases/v0.23-04-integration-validation-suite/v0.23-04-02-SUMMARY.md`
</output>
