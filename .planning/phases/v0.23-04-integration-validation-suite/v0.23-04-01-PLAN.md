---
phase: v0.23-04-integration-validation-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/test-formal-integration.test.cjs
autonomous: true
requirements:
  - IVL-01
  - IVL-03
formal_artifacts: none

must_haves:
  truths:
    - "Running bin/test-formal-integration.test.cjs completes without error and reports that run-formal-check.cjs actually executed with real stdout/exit codes captured -- not mocked"
    - "All existing TLA+ specs (QGSDDeliberation.tla, MCbreaker.cfg, MCliveness.cfg, and any others in formal/tla/) pass TLC clean -- no regressions introduced by the integration wiring"
    - "If Java is not installed, tests skip gracefully with a descriptive skip message rather than crashing"
    - "FORMAL_CHECK_RESULT JSON structure (passed, failed, skipped, counterexamples keys) is explicitly validated to match verify-formal-results.cjs parseNDJSON expectations"
  artifacts:
    - path: "bin/test-formal-integration.test.cjs"
      provides: "Integration test suite proving formal tools actually run with real output"
      min_lines: 120
      contains: "spawnSync"
  key_links:
    - from: "bin/test-formal-integration.test.cjs"
      to: "bin/run-formal-check.cjs"
      via: "spawnSync invocation with --modules flag"
      pattern: "spawnSync.*run-formal-check"
    - from: "bin/test-formal-integration.test.cjs"
      to: "formal/tla/*.tla"
      via: "run-formal-check MODULE_CHECKS mapping"
      pattern: "modules=.*quorum|breaker|deliberation"
    - from: "bin/test-formal-integration.test.cjs"
      to: "bin/verify-formal-results.cjs"
      via: "FORMAL_CHECK_RESULT structure compatibility validation"
      pattern: "passed.*failed.*skipped.*counterexamples"
---

<objective>
Create the integration test file that proves run-formal-check.cjs executes real formal verification tools (TLC, Alloy, PRISM) with actual stdout/exit code capture -- not mocked -- and that all existing TLA+ specs pass TLC clean with no regressions.

Purpose: IVL-01 requires proof that tools actually ran. IVL-03 requires all TLA+ specs pass without regressions. This plan creates the test file with smoke tests (proving real tool execution), structural tests (validating real TLC output patterns), and regression tests (all modules pass TLC clean).

Output: bin/test-formal-integration.test.cjs with passing tests (or graceful skips if Java absent)
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.23-04-integration-validation-suite/v0.23-04-RESEARCH.md
@bin/run-formal-check.cjs
@bin/verify-formal-results.cjs
@bin/run-tlc.test.cjs
@bin/run-formal-verify.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test file with IVL-01 smoke and structural tests</name>
  <files>bin/test-formal-integration.test.cjs</files>
  <action>
Create bin/test-formal-integration.test.cjs using the project's standard test pattern (node:test + node:assert/strict + child_process.spawnSync).

The file MUST follow these patterns from the codebase:
- Use `const { test, describe } = require('node:test');`
- Use `const assert = require('node:assert/strict');`
- Use `const { spawnSync } = require('child_process');`
- Use `const path = require('path');`
- Use `const fs = require('fs');`

**IVL-01 Smoke Tests (pass even if Java not installed):**

1. `IVL-01 smoke: run-formal-check.cjs loads without syntax error` -- require() the module, verify it exports detectJava, checkJarExists, runCheck, MODULE_CHECKS. This proves the script exists and is parseable.

2. `IVL-01 smoke: MODULE_CHECKS contains all 10 known modules` -- verify MODULE_CHECKS has keys for: quorum, tui-nav, breaker, deliberation, oscillation, convergence, prefilter, recruiting, account-manager, mcp-calls. Use assert.deepStrictEqual on Object.keys().sort().

3. `IVL-01 smoke: detectJava returns string or null` -- call detectJava() and assert result is either a string (Java found) or null (Java absent). No crash either way.

**IVL-01 Structural Tests (require Java; skip if absent):**

4. `IVL-01 structural: run-formal-check --modules=quorum produces real TLC output` -- Use spawnSync to invoke `node bin/run-formal-check.cjs --modules=quorum`. If Java not found (check stderr for "java not found"), mark test as skip using `test.skip()` or `{ skip: 'Java not installed' }`. Otherwise:
   - Verify exit code is 0 (pass) or 1 (fail), NOT a signal/timeout
   - Verify stdout contains "FORMAL_CHECK_RESULT=" (machine-readable output line)
   - Parse the FORMAL_CHECK_RESULT JSON from stdout -- verify it has keys: passed, failed, skipped, counterexamples
   - **CRITICAL: Cross-validate FORMAL_CHECK_RESULT structure against verify-formal-results.cjs expectations.** Read verify-formal-results.cjs to identify what parseNDJSON expects as input shape. Assert that the FORMAL_CHECK_RESULT JSON object has exactly the keys `{ passed: number, failed: number, skipped: number, counterexamples: Array }` -- not just that it parses, but that the value types match what the verifier pipeline consumes. This prevents silent incompatibility where run-formal-check output format could diverge from the verifier's parseNDJSON expectations.
   - Verify stdout contains "[run-formal-check] Results:" summary line
   - This proves run-formal-check.cjs ACTUALLY RAN with real output, not mocked

5. `IVL-01 structural: run-formal-check --modules=breaker produces real TLC output` -- Same pattern as test 4 but for the breaker module. Include the same FORMAL_CHECK_RESULT structure cross-validation. Validates a second module also produces real output (not just quorum working by coincidence).

**Fail-open Tests (ENF-03 compliance):**

6. `ENF-03 fail-open: run-formal-check skips gracefully if Java not found` -- If detectJava() returns null, invoke run-formal-check --modules=quorum via spawnSync and verify: exit code is 0 (not crash), stderr contains "java not found" warning, stdout contains FORMAL_CHECK_RESULT with skipped > 0 and failed === 0. If Java IS found, skip this test (cannot test fail-open when Java is present).

7. `ENF-03 fail-open: run-formal-check skips PRISM if PRISM_BIN not set` -- Invoke run-formal-check --modules=quorum with PRISM_BIN explicitly unset (delete from env). Verify: the FORMAL_CHECK_RESULT JSON has at least one entry with status "skipped" for tool "prism" (quorum module has a PRISM check). Parse stdout to find prism skip message.

**FORMAL_CHECK_RESULT / verify-formal-results.cjs compatibility helper:**

Create a helper function at the top of the test file (inside a describe or at module level) that validates FORMAL_CHECK_RESULT structure:
```javascript
function assertFormalCheckResultShape(result) {
  assert.ok(typeof result.passed === 'number', 'passed must be a number');
  assert.ok(typeof result.failed === 'number', 'failed must be a number');
  assert.ok(typeof result.skipped === 'number', 'skipped must be a number');
  assert.ok(Array.isArray(result.counterexamples), 'counterexamples must be an array');
}
```
Use this helper in every test that parses FORMAL_CHECK_RESULT to enforce consistency between run-formal-check.cjs output and verify-formal-results.cjs expectations.

**Key implementation rules:**
- Use spawnSync with `{ encoding: 'utf8', timeout: 180000, cwd: process.cwd() }` for all subprocess calls
- Parse FORMAL_CHECK_RESULT from stdout by finding line starting with "FORMAL_CHECK_RESULT=" and JSON.parse the rest
- For Java-dependent tests, use `describe('structural (requires Java)', () => { ... })` grouping
- For skip logic: check if `detectJava()` returns null at top of describe block, then use `test({ skip: reason }, ...)` form
- Do NOT mock spawnSync. Do NOT stub detectJava. Real tool execution only.
  </action>
  <verify>
Run: `node --test bin/test-formal-integration.test.cjs`
Expected: All smoke tests pass. Structural tests either pass (Java present) or skip (Java absent). No test crashes.
Verify stdout includes test names starting with "IVL-01" and "ENF-03".
Verify: grep for `assertFormalCheckResultShape` in test file -- should appear in structural tests confirming cross-validation is wired.
  </verify>
  <done>
bin/test-formal-integration.test.cjs exists with 7+ tests. Smoke tests (IVL-01 smoke) pass unconditionally. Structural tests (IVL-01 structural) pass with real TLC output when Java is present, or skip gracefully when absent. Fail-open tests (ENF-03) validate graceful degradation. FORMAL_CHECK_RESULT JSON is parsed and validated from real stdout -- not mocked. Every structural test uses assertFormalCheckResultShape to cross-validate that FORMAL_CHECK_RESULT keys/types match verify-formal-results.cjs parseNDJSON expectations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add IVL-03 regression tests for all TLA+ specs</name>
  <files>bin/test-formal-integration.test.cjs</files>
  <action>
Extend bin/test-formal-integration.test.cjs (created in Task 1) with IVL-03 regression tests that verify ALL existing TLA+ specs pass TLC clean.

**IVL-03 Regression Tests:**

Add a new describe block: `describe('IVL-03: all TLA+ specs pass TLC clean', () => { ... })`

1. `IVL-03 regression: all modules pass run-formal-check` -- Invoke run-formal-check with ALL 10 modules: `node bin/run-formal-check.cjs --modules=quorum,tui-nav,breaker,deliberation,oscillation,convergence,prefilter,recruiting,account-manager,mcp-calls`. Use spawnSync with 300000ms timeout (5 min -- running all modules takes time). If Java not found, skip. Otherwise:
   - Verify exit code is 0 (all passed, none failed)
   - Parse FORMAL_CHECK_RESULT JSON from stdout
   - Use assertFormalCheckResultShape(result) to cross-validate structure
   - Verify `result.failed === 0` (no counterexamples in ANY spec)
   - Verify `result.passed > 0` (at least some checks actually ran)
   - Verify `result.counterexamples` is an empty array
   - Log the pass/fail/skip counts for visibility

2. Per-module regression tests -- Loop through all 10 module names from MODULE_CHECKS. For each module, create a test: `IVL-03 regression: ${module} passes TLC clean`. Each test invokes `node bin/run-formal-check.cjs --modules=${module}` via spawnSync. Verify:
   - Exit code is 0
   - FORMAL_CHECK_RESULT.failed === 0
   - Use assertFormalCheckResultShape(result) on each parsed result
   - If module is 'quorum', also verify FORMAL_CHECK_RESULT has entries for tlc AND alloy (quorum has both)
   - Skip if Java not found

Use dynamic test generation:
```javascript
const { MODULE_CHECKS } = require('./run-formal-check.cjs');
const moduleNames = Object.keys(MODULE_CHECKS);

for (const mod of moduleNames) {
  test(`IVL-03 regression: ${mod} passes TLC clean`, { skip: !javaAvailable && 'Java not installed', timeout: 180000 }, () => {
    // invoke run-formal-check --modules=${mod}
    // verify exit code 0, failed === 0
    // assertFormalCheckResultShape(result)
  });
}
```

**Key rules:**
- Import MODULE_CHECKS from run-formal-check.cjs to get module list dynamically (not hardcoded list)
- Timeout for per-module tests: 180000ms (3 min each)
- Timeout for all-modules test: 300000ms (5 min)
- Do NOT mock any subprocess. Real TLC execution only.
- Distinguish between "spec has counterexample" (FORMAL_CHECK_RESULT.failed > 0) and "spec has syntax error" (exit code non-zero but no FORMAL_CHECK_RESULT line) -- both are failures for IVL-03

After adding regression tests, run the full test file to verify everything passes (or skips gracefully).
  </action>
  <verify>
Run: `node --test bin/test-formal-integration.test.cjs`
Expected: All IVL-03 tests pass (if Java installed) or skip (if Java absent). The all-modules test shows FORMAL_CHECK_RESULT with failed=0. Each per-module test shows individual pass.
Count total tests: should be 7 (from Task 1) + 1 (all-modules) + 10 (per-module) = 18+ tests.
  </verify>
  <done>
bin/test-formal-integration.test.cjs has 18+ tests covering IVL-01, IVL-03, and ENF-03. All TLA+ specs (quorum, tui-nav, breaker, deliberation, oscillation, convergence, prefilter, recruiting, account-manager, mcp-calls) are individually tested for TLC-clean passes. The all-modules regression test proves no regressions exist. All tests use real tool execution via spawnSync -- zero mocking. Every test that parses FORMAL_CHECK_RESULT also calls assertFormalCheckResultShape to guard against format drift from the verifier.
  </done>
</task>

</tasks>

<verification>
1. `node --test bin/test-formal-integration.test.cjs` -- all tests pass or skip gracefully
2. `node -e "require('./bin/test-formal-integration.test.cjs')"` -- file loads without SyntaxError
3. Grep for "mock" or "stub" in test file -- should return 0 matches (no mocking)
4. Grep for "FORMAL_CHECK_RESULT" in test file -- should return multiple matches (real output parsing)
5. Test count >= 18 (7 IVL-01/ENF-03 + 1 all-modules + 10 per-module)
6. Grep for "assertFormalCheckResultShape" in test file -- should appear in structural and regression tests
</verification>

<success_criteria>
- bin/test-formal-integration.test.cjs exists with 18+ tests
- IVL-01: Tests prove run-formal-check.cjs executed with real stdout/exit codes
- IVL-03: All 10 TLA+ module specs pass TLC clean (or skip if Java absent)
- ENF-03: Fail-open behavior validated for missing Java and missing PRISM
- Zero mocking -- all tests use real subprocess execution
- FORMAL_CHECK_RESULT structure is cross-validated against verify-formal-results.cjs expectations in every structural/regression test
</success_criteria>

<output>
After completion, create `.planning/phases/v0.23-04-integration-validation-suite/v0.23-04-01-SUMMARY.md`
</output>
