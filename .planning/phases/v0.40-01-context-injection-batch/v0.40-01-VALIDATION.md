---
phase: v0.40-01
slug: context-injection-batch
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-19
---

# Phase v0.40-01 — Validation Strategy

> Template created by `/nf:plan-phase v0.40-01` (step 5.5) after research.
> Populated by `/nf:plan-phase v0.40-01` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/nf:execute-phase v0.40-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node --test`) |
| **Config file** | none — tests run directly |
| **Quick run command** | `node --test hooks/nf-prompt.test.js` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | .github/workflows/test.yml — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test hooks/nf-prompt.test.js`
- **After every plan wave:** Run `npm test`
- **Before `/nf:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.40-01-01-01 | 01 | 1 | SESSION-01, SESSION-02, SESSION-03, ROOT-01, ROOT-03, CONST-01, CONST-02 | unit | `grep -n 'isFirstMessageOfSession' hooks/nf-prompt.js && grep -n 'ROOT CAUSE ENFORCEMENT' hooks/nf-prompt.js && grep -n 'EDIT CONSTRAINT' hooks/nf-prompt.js && diff hooks/nf-prompt.js hooks/dist/nf-prompt.js` | ✅ | ⬜ pending |
| v0.40-01-01-02 | 01 | 1 | SESSION-01, SESSION-02, SESSION-03, ROOT-01, ROOT-03, CONST-01, CONST-02 | unit | `node --test hooks/nf-prompt.test.js` | ❌ W0 | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

Test file `hooks/nf-prompt.test.js` already exists and will be extended by Task 2.

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `nf-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s ✅
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-19

---

## Execution Tracking

Updated during `/nf:execute-phase v0.40-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | `node --test hooks/nf-prompt.test.js` | — | — | ⬜ pending |

**Phase validation complete:** pending
