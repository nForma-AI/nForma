---
phase: v0.35-02-mcp-setup-slot-classification
plan: 01
subsystem: infra
tags: [mcp, providers, auth_type, quorum, setup]

# Dependency graph
requires:
  - phase: v0.35-01-install-hooks-dist-rebuild
    provides: clean hooks/dist build pipeline
provides:
  - auth_type field in providers.json for all 12 slots
  - mcp-setup workflow writes auth_type to nf.json during first-run and re-run
  - 6 tests proving auth_type schema completeness and correct classification
affects: [v0.35-03, nf-prompt, config-audit, FALLBACK-01]

# Tech tracking
tech-stack:
  added: []
  patterns: [declarative slot classification via auth_type field]

key-files:
  created:
    - test/mcp-setup-auth-type.test.cjs
  modified:
    - bin/providers.json
    - commands/nf/mcp-setup.md

key-decisions:
  - "auth_type uses 'sub' and 'api' as values (matching existing nf-prompt.js convention)"
  - "Re-run flow syncs auth_type for ALL configured agents, not just newly added ones (fixes existing installations)"

patterns-established:
  - "Slot classification via providers.json auth_type field, not name-prefix inference"

requirements-completed: [SETUP-01, SETUP-02]

# Metrics
duration: 3min
completed: 2026-03-12
---

# Phase v0.35-02 Plan 01: MCP Setup Slot Classification Summary

**Declarative auth_type field added to all 12 provider slots in providers.json, wired through mcp-setup first-run and re-run flows to fix T1/T2 tiered dispatch**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-12T23:04:46Z
- **Completed:** 2026-03-12T23:07:25Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `auth_type` field to all 12 providers in `providers.json` (6 "sub" for CLI subscription slots, 6 "api" for ccr-routed API slots)
- Wired auth_type sync into `mcp-setup.md` first-run onboarding flow (Step 3d) and re-run agent menu flow
- Created 6 tests proving schema completeness, value constraints, correct classification, display_type cross-validation, and direct-lookup viability

## Task Commits

Each task was committed atomically:

1. **Task 1: Add auth_type to providers.json and wire auth_type in mcp-setup workflow** - `1c42cfe0` (feat)
2. **Task 2: Add auth_type schema and classification tests** - `441812d8` (test)

## Files Created/Modified
- `bin/providers.json` - Added auth_type field ("sub" or "api") to all 12 provider entries
- `commands/nf/mcp-setup.md` - Added Step 3d (first-run auth_type sync) and re-run auth_type sync section
- `test/mcp-setup-auth-type.test.cjs` - 6 tests for auth_type schema validation and classification correctness

## Decisions Made
- Used "sub" and "api" as auth_type values to match the existing convention in nf-prompt.js (line 506)
- Re-run flow syncs auth_type for ALL configured agents (not just newly added), ensuring existing installations get fixed on next mcp-setup re-run
- Placed auth_type after `"type": "subprocess"` for readability in providers.json

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing `verify-hooks-sync` test failure (from quick-283 gsd-context-monitor rename) prevents `npm test` from passing. This is NOT caused by this plan's changes and is out of scope. Logged to deferred items.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- auth_type schema is complete and tested
- mcp-setup workflow writes auth_type during both first-run and re-run flows
- Ready for v0.35-03 (TUI entry point resolution) which does not depend on this phase

---
*Phase: v0.35-02-mcp-setup-slot-classification*
*Completed: 2026-03-12*
