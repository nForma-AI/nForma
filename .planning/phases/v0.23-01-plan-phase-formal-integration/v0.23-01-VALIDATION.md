---
phase: v0.23-01
slug: plan-phase-formal-integration
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-02
---

# Phase v0.23-01 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.23-01` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.23-01` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.23-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node --test`) |
| **Config file** | none — uses `npm test` script in package.json |
| **Quick run command** | `node --test bin/conformance-schema.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~60 seconds (full suite) |
| **CI pipeline** | .github/workflows/test.yml — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/conformance-schema.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.23-01-01-01 | 01 | 1 | WFI-01 | integration | `grep -n "4\.5\|Formal Scope Scan\|FORMAL_SPEC_CONTEXT" qgsd-core/workflows/plan-phase.md \| head -20` | ✅ N/A | ⬜ pending |
| v0.23-01-01-02 | 01 | 1 | WFI-01 | integration | `grep -n "formal_context\|formal_artifacts\|FORMAL_SPEC_CONTEXT" qgsd-core/workflows/plan-phase.md \| head -20` | ✅ N/A | ⬜ pending |
| v0.23-01-02-01 | 02 | 2 | WFI-02 | integration | `grep -n "check_dimensions\|formal_context\|formal_artifacts.*BLOCKER" qgsd-core/workflows/plan-phase.md \| head -20` | ✅ N/A | ⬜ pending |
| v0.23-01-02-02 | 02 | 2 | ENF-03 | smoke | `grep -c "FORMAL_SPEC_CONTEXT" ~/.claude/qgsd/workflows/plan-phase.md && grep -n "formal_artifacts.*BLOCKER" ~/.claude/qgsd/workflows/plan-phase.md \| head -5` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

All tasks modify `qgsd-core/workflows/plan-phase.md` (a markdown workflow file) and use grep-based verification commands that test the file content directly. No new test files are required.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| plan-phase formal gate skip warning when Java absent | ENF-03 | Java absence cannot block Step 4.5 (pure .md reads) — SC3 satisfied by design; no runtime test needed | Review plan-phase Step 4.5 bash code to confirm no Java invocation |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 60s ✅
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-02

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.23-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | {command} | {N} | {N} | ✅ sampled |
| 2 | 2 | {command} | {N} | {N} | ✅ sampled |

**Phase validation complete:** pending
