---
phase: 02-config-mcp-detection
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - templates/qgsd.json
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - CONF-03
  - MCP-01
  - MCP-03

must_haves:
  truths:
    - "templates/qgsd.json documents that required_models is the canonical field name (not quorum_models)"
    - "templates/qgsd.json documents the two-layer merge behavior and how per-project override works"
    - "A human has verified the full Phase 2 feature set in a live Claude Code session"
  artifacts:
    - path: "templates/qgsd.json"
      provides: "Updated config template with schema documentation"
      contains: "_comment"
  key_links:
    - from: "templates/qgsd.json _comment"
      to: "hooks/config-loader.js behavior"
      via: "documentation of two-layer merge"
      pattern: "_comment"
---

<objective>
Add schema documentation to templates/qgsd.json (using a _comment key since JSON does not support comments), and run a human-verify checkpoint confirming the full Phase 2 feature set works correctly in a live Claude Code session.

Purpose: Users need to understand the config schema when editing qgsd.json. The checkpoint confirms two-layer merge, fail-open, and MCP detection all work end-to-end.
Output: templates/qgsd.json (updated with documentation), human verification of Phase 2 behavior.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/qgsd.json
@.planning/phases/02-config-mcp-detection/02-01-SUMMARY.md
@.planning/phases/02-config-mcp-detection/02-02-SUMMARY.md
@.planning/phases/02-config-mcp-detection/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema documentation to templates/qgsd.json</name>
  <files>templates/qgsd.json</files>
  <action>
    Replace the contents of templates/qgsd.json with an updated version that adds a _comment key documenting the schema.

    The new content should be:

    {
      "_comment": [
        "QGSD Quorum Config — generated by QGSD installer. Safe to edit.",
        "",
        "quorum_commands: GSD planning commands that require quorum before Claude delivers output.",
        "  Add or remove command names to control which /gsd: commands are gated.",
        "",
        "required_models: Dict of quorum model entries. Key = logical name, value = { tool_prefix, required }.",
        "  tool_prefix: MCP tool name prefix — matched with startsWith(). E.g. 'mcp__codex-cli__'",
        "    matches both mcp__codex-cli__review and mcp__codex-cli__codex.",
        "  required: If true, model must be called (or be unavailable per ~/.claude.json) for quorum.",
        "  NOTE: This field was called 'quorum_models' in early requirements. 'required_models' is canonical.",
        "",
        "fail_mode: 'open' (default) — unavailable models are skipped, quorum passes with available models.",
        "  'closed' is reserved for future use.",
        "",
        "Config layering: ~/.claude/qgsd.json (global) is loaded first.",
        "  .claude/qgsd.json in the project directory overrides global values (project takes precedence).",
        "  Merge is SHALLOW: if a project config sets required_models, it entirely replaces global required_models.",
        "  To add a model to the global list, copy the full required_models from global and add your entry.",
        "  Create .claude/qgsd.json in a project to restrict quorum to fewer models or different commands.",
        "",
        "MCP prefixes are auto-detected from ~/.claude.json mcpServers on first install.",
        "  Edit required_models here to override detected values."
      ],
      "quorum_commands": [
        "plan-phase", "new-project", "new-milestone",
        "discuss-phase", "verify-work", "research-phase"
      ],
      "fail_mode": "open",
      "required_models": {
        "codex":    { "tool_prefix": "mcp__codex-cli__",  "required": true },
        "gemini":   { "tool_prefix": "mcp__gemini-cli__", "required": true },
        "opencode": { "tool_prefix": "mcp__opencode__",   "required": true }
      },
      "quorum_instructions": "QUORUM REQUIRED (structural enforcement — Stop hook will verify)\n\nBefore presenting any planning output to the user, you MUST:\n  1. Call mcp__codex-cli__review with the full plan content\n  2. Call mcp__gemini-cli__gemini with the full plan content\n  3. Call mcp__opencode__opencode with the full plan content\n  4. Present all model responses, resolve concerns, then deliver final output\n\nFail-open: if a model is UNAVAILABLE (quota/error), note it and proceed with available models.\nThe Stop hook reads the transcript — skipping quorum will block your response."
    }

    Note: The _comment array approach is the standard JSON documentation pattern. The hooks ignore unknown keys, so _comment does not affect hook behavior.
    Note: The quorum_instructions value must match the string that was in the original template (used by qgsd-prompt.js as a fallback when the field is not overridden in user config).
  </action>
  <verify>
    1. node -e "JSON.parse(require('fs').readFileSync('templates/qgsd.json', 'utf8'))" — no parse error
    2. grep "_comment" templates/qgsd.json — returns a match
    3. grep "required_models" templates/qgsd.json — returns a match with documentation noting it is canonical
    4. grep "quorum_instructions" templates/qgsd.json — returns a match (field preserved)
  </verify>
  <done>templates/qgsd.json is valid JSON with _comment documentation array. Schema is explained: required_models canonical name, tool_prefix semantics, fail_mode, two-layer merge, MCP detection note.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — correct MCP-01 file path and CONF-03/MCP-03 field name</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Two targeted corrections to REQUIREMENTS.md to align with what was verified live and what the
    plans implement:

    1. Fix MCP-01 file path (blocker: plans read ~/.claude.json, not ~/.claude/settings.json):
       Current text: "Installer reads `~/.claude/settings.json` (or `~/.claude/claude_desktop_config.json`)"
       Replace with:  "Installer reads `~/.claude.json` to auto-detect MCP server names"
       Rationale: VERIFIED LIVE — ~/.claude.json contains mcpServers: ['codex-cli', 'gemini-cli',
       'opencode']. ~/.claude/settings.json has no mcpServers field. The plans (02-02, 02-03) are
       correct; REQUIREMENTS.md was wrong.

    2. Fix CONF-03 field name (blocker: all plans use 'required_models', REQUIREMENTS.md says 'quorum_models'):
       Current text: "`quorum_models` (array of MCP tool name patterns)"
       Replace with:  "`required_models` (dict of MCP tool entries: { tool_prefix, required })"
       Rationale: 'required_models' was chosen during planning as a more accurate name (it is a dict,
       not an array, and 'required' matches the field structure). This is an approved divergence from
       the original REQUIREMENTS.md placeholder name.

    3. Fix MCP-03 field name consistently:
       Current text: "Detected names written to `~/.claude/qgsd.json` as `quorum_models` on install"
       Replace with:  "Detected names written to `~/.claude/qgsd.json` as `required_models` on install"
       Rationale: Same as CONF-03 — 'required_models' is the canonical field name implemented in
       all plans and in the templates/qgsd.json schema documentation added in Task 1.

    Also add a note to the bottom of REQUIREMENTS.md (before the closing timestamp line) recording
    these corrections:
       "*Updated: 2026-02-20 — MCP-01: corrected file path from ~/.claude/settings.json to
       ~/.claude.json (verified live). CONF-03/MCP-03: corrected field name from quorum_models to
       required_models (approved divergence during Phase 2 planning).*"
  </action>
  <verify>
    1. grep "quorum_models" .planning/REQUIREMENTS.md — returns NO matches (all occurrences replaced)
    2. grep "required_models" .planning/REQUIREMENTS.md — returns at least 2 matches (CONF-03, MCP-03)
    3. grep "\.claude\.json" .planning/REQUIREMENTS.md — returns a match in MCP-01 line
    4. grep "settings\.json" .planning/REQUIREMENTS.md — returns NO match in MCP-01 line (path corrected)
  </verify>
  <done>REQUIREMENTS.md MCP-01 says ~/.claude.json. CONF-03 and MCP-03 say required_models (not quorum_models). Correction note added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Phase 2 end-to-end</name>
  <files>none (verification only)</files>
  <action>Human verification of Phase 2 feature set — run checks listed in how-to-verify section below.</action>
  <verify>All 6 checks in how-to-verify pass without errors.</verify>
  <done>Human confirms: config loader tests pass, stop hook tests pass (13/13), two-layer merge works, malformed config warns, MCP detection logs correctly, live quorum enforcement still blocks and passes correctly.</done>
  <what-built>
    Phase 2 complete: config-loader.js (two-layer merge + validation + stderr warnings), qgsd-stop.js fail-open unavailability detection, qgsd-prompt.js using shared loader, MCP auto-detection in installer, templates/qgsd.json documentation.
  </what-built>
  <how-to-verify>
    Run these checks in a live Claude Code session:

    **Check 1 — Config loader unit tests:**
    node --test hooks/config-loader.test.js
    Expected: all tests pass, no failures.

    **Check 2 — Stop hook tests (including fail-open):**
    node --test hooks/qgsd-stop.test.js
    Expected: 13 tests pass (TC1-TC10 + TC11-TC13), no failures.

    **Check 3 — Two-layer merge in practice:**
    Create a file /tmp/test-project/.claude/qgsd.json:
      { "fail_mode": "closed", "quorum_commands": ["plan-phase"] }
    Run: node -e "const {loadConfig}=require('./hooks/config-loader'); const c=loadConfig('/tmp/test-project'); console.log(JSON.stringify(c,null,2));"
    Expected: fail_mode is "closed" (from project), quorum_commands is ["plan-phase"] (from project), required_models has 3 entries (from DEFAULT_CONFIG, not overridden).

    **Check 4 — Malformed config warning:**
    Create /tmp/bad-config/.claude/qgsd.json with content: "{ invalid json :"
    Run: node -e "const {loadConfig}=require('./hooks/config-loader'); loadConfig('/tmp/bad-config');" 2>&1
    Expected: stderr shows "[qgsd] WARNING: Malformed config at..." with path. No crash.

    **Check 5 — MCP detection log output:**
    Run: node -e "
      const path=require('path'), os=require('os'), fs=require('fs');
      // Temporarily check what ~/.claude.json has
      const f=path.join(os.homedir(),'.claude.json');
      const d=JSON.parse(fs.readFileSync(f,'utf8'));
      console.log('mcpServers:', Object.keys(d.mcpServers||{}));
    "
    Expected: lists codex-cli, gemini-cli, opencode (or equivalent).
    Then verify that if you were to run the installer fresh, it would detect those servers and write their prefixes.

    **Check 6 — Live /gsd:plan-phase quorum enforcement (already verified in Phase 1, regression check):**
    In Claude Code, run /gsd:plan-phase (or any quorum command) without calling the quorum models.
    Expected: Stop hook blocks with "QUORUM REQUIRED:" message.
    Then run it properly (calling all 3 quorum models). Expected: passes.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
Overall Phase 2 checks (all plans combined):
1. node --test hooks/config-loader.test.js — all tests pass
2. node --test hooks/qgsd-stop.test.js — 13 tests pass
3. node -e "JSON.parse(require('fs').readFileSync('templates/qgsd.json','utf8'))" — valid JSON
4. grep "require('./config-loader')" hooks/qgsd-stop.js hooks/qgsd-prompt.js — both match
5. grep "buildRequiredModelsFromMcp" bin/install.js — match found
6. grep "config-loader.js" scripts/build-hooks.js — match found
7. Human checkpoint passed
</verification>

<success_criteria>
- templates/qgsd.json has valid JSON with _comment documentation array
- _comment documents: required_models is canonical (not quorum_models), tool_prefix semantics, fail_mode, two-layer merge, MCP detection note
- Human has verified: config-loader tests pass, stop hook tests pass (13/13), two-layer merge works, malformed config warns gracefully, MCP detection logs correctly
- All Phase 2 requirements (CONF-01 through MCP-06) confirmed working end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-mcp-detection/02-04-SUMMARY.md`
</output>
