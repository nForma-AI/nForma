---
phase: 02-config-mcp-detection
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - hooks/qgsd-stop.js
  - hooks/qgsd-stop.test.js
autonomous: true
requirements:
  - CONF-04
  - MCP-06

must_haves:
  truths:
    - "qgsd-stop.js uses the shared config-loader module — no inline loadConfig() remains"
    - "When a required model's tool_prefix has no matching entry in ~/.claude.json mcpServers, the Stop hook passes (fail-open) and writes a note to stderr"
    - "When a required model IS in ~/.claude.json mcpServers but has no tool_use evidence in the transcript, the Stop hook blocks with decision:block"
    - "When ~/.claude.json is missing or malformed, unavailability detection returns null and the hook treats all models as potentially available (conservative: block on missing evidence)"
    - "Prefix-based matching (MCP-06) still works correctly after the config-loader migration"
    - "All existing TC1-TC10 tests still pass after the refactor"
  artifacts:
    - path: "hooks/qgsd-stop.js"
      provides: "Stop hook with shared config loader and fail-open unavailability detection"
      min_lines: 180
    - path: "hooks/qgsd-stop.test.js"
      provides: "Test suite with TC11-TC13 covering fail-open behavior"
      min_lines: 380
  key_links:
    - from: "hooks/qgsd-stop.js"
      to: "hooks/config-loader.js"
      via: "require('./config-loader')"
      pattern: "require\\('./config-loader'\\)"
    - from: "hooks/qgsd-stop.js"
      to: "~/.claude.json"
      via: "getAvailableMcpPrefixes() reads mcpServers"
      pattern: "\\.claude\\.json"
    - from: "hooks/qgsd-stop.js missingKeys filter"
      to: "availablePrefixes"
      via: "unavailability check before blocking"
      pattern: "availablePrefixes"
---

<objective>
Migrate qgsd-stop.js to use the shared config-loader module, and add fail-open unavailability detection: when a required model's tool prefix has no matching entry in ~/.claude.json mcpServers, the hook passes instead of blocking. Add TDD tests for the new behavior (TC11-TC13) and confirm all existing TC1-TC10 tests still pass.

Purpose: Separates "model not called" (block) from "model not available" (pass). Makes enforcement resilient to users who don't have all three quorum models installed.
Output: qgsd-stop.js (updated), qgsd-stop.test.js (updated with 3 new tests).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-mcp-detection/2-RESEARCH.md
@hooks/qgsd-stop.js
@hooks/qgsd-stop.test.js
@.planning/phases/02-config-mcp-detection/02-01-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD — Stop hook fail-open unavailability detection (RED → GREEN → REFACTOR)</name>
  <files>hooks/qgsd-stop.js, hooks/qgsd-stop.test.js</files>
  <action>
    RED: Add TC11, TC12, TC13 to hooks/qgsd-stop.test.js for fail-open behavior and MCP-06 renamed prefix regression. Run — new tests must FAIL (hook has no unavailability detection yet).
    GREEN: Migrate hooks/qgsd-stop.js to use require('./config-loader') (remove inline loadConfig/DEFAULT_CONFIG), add getAvailableMcpPrefixes() reading ~/.claude.json (with QGSD_CLAUDE_JSON env override for tests), update missingKeys filter to skip unavailable models. Run — all 13 tests must PASS.
    REFACTOR: Ensure getAvailableMcpPrefixes() writes only to stderr. Run — tests must still PASS.
  </action>
  <feature>
    <name>Fail-open: unavailable models pass, available-but-not-called models block</name>
    <files>hooks/qgsd-stop.js, hooks/qgsd-stop.test.js</files>
    <behavior>
      New behavior added to main() in qgsd-stop.js after findQuorumEvidence():

      getAvailableMcpPrefixes():
      - Reads ~/.claude.json
      - On missing file: returns null (unknown → treat conservatively)
      - On malformed JSON: writes "[qgsd] WARNING: Could not parse ~/.claude.json: {msg}" to stderr, returns null
      - On success: returns array of derived prefixes from mcpServers keys: Object.keys(servers).map(name => "mcp__" + name + "__")

      Updated missingKeys filter logic:
      - availablePrefixes = getAvailableMcpPrefixes()  (null = unknown)
      - For each required model not in foundModels:
        - If availablePrefixes is null: include in missingKeys (conservative — cannot confirm unavailability)
        - If availablePrefixes is non-null AND model's tool_prefix not in availablePrefixes: SKIP (unavailable → fail-open)
        - If availablePrefixes is non-null AND model's tool_prefix IS in availablePrefixes: include in missingKeys (available but not called → block)

      When models are skipped as unavailable and a block is still issued (other models missing):
      - Block reason includes note: "[Note: {modelKey} ({prefix}) was unavailable and skipped per fail-open policy]"
      When models are skipped as unavailable and NO block is issued (all required available models were called):
      - Write stderr note: "[qgsd] INFO: Quorum passed. Note: {modelKey} ({prefix}) was unavailable and skipped."

      Cases (input → expected):
      - opencode not in mcpServers, codex+gemini called → pass (opencode unavailable → skipped)
      - opencode not in mcpServers, codex called but gemini not called, gemini IS in mcpServers → block for gemini only
      - ~/.claude.json missing → treat all as available (null) → block on any missing evidence (existing behavior)
      - All 3 in mcpServers, all 3 called → pass (existing TC5 behavior)
      - All 3 in mcpServers, only codex called → block for gemini+opencode (existing TC6 behavior)
      - Renamed prefix: config has required_models with custom prefix "mcp__my-codex-server__", transcript has tool_use "mcp__my-codex-server__review", mcpServers has "my-codex-server" → match found → pass (MCP-06 regression)

      RED: Write TC11, TC12, TC13 first. Run — must FAIL (hook has no availability logic yet).
      GREEN: Implement getAvailableMcpPrefixes() and updated missingKeys filter. Run — must PASS.
      REFACTOR: Clean up, ensure no stdout contamination from new code paths.
    </behavior>
    <implementation>
      Changes to hooks/qgsd-stop.js:

      1. At the top, replace inline loadConfig() and DEFAULT_CONFIG with:
         const { loadConfig, DEFAULT_CONFIG } = require('./config-loader');
         Remove the inline DEFAULT_CONFIG object and loadConfig() function entirely.

      2. Add getAvailableMcpPrefixes() function after findQuorumEvidence():
         function getAvailableMcpPrefixes() {
           const claudeJsonPath = path.join(os.homedir(), '.claude.json');
           if (!fs.existsSync(claudeJsonPath)) return null;
           try {
             const d = JSON.parse(fs.readFileSync(claudeJsonPath, 'utf8'));
             const servers = d.mcpServers || {};
             return Object.keys(servers).map(name => 'mcp__' + name + '__');
           } catch (e) {
             process.stderr.write('[qgsd] WARNING: Could not parse ~/.claude.json: ' + e.message + '\n');
             return null;
           }
         }

      3. In main(), after findQuorumEvidence() call, update missingKeys derivation:
         const availablePrefixes = getAvailableMcpPrefixes();

         const unavailableKeys = [];
         const missingKeys = Object.entries(config.required_models)
           .filter(([modelKey, modelDef]) => {
             if (!modelDef.required) return false;
             if (foundModels.has(modelKey)) return false;
             // Check unavailability only if we have a definitive server list
             if (availablePrefixes !== null) {
               const isConfigured = availablePrefixes.some(p => p === modelDef.tool_prefix);
               if (!isConfigured) {
                 unavailableKeys.push({ modelKey, prefix: modelDef.tool_prefix });
                 return false; // unavailable → fail-open, skip
               }
             }
             return true; // required, not called, either available or unknown
           })
           .map(([modelKey]) => modelKey);

      4. After the missingKeys check, if missingKeys.length === 0 but unavailableKeys.length > 0, emit stderr note and pass:
         if (missingKeys.length === 0) {
           if (unavailableKeys.length > 0) {
             const note = unavailableKeys.map(u => u.modelKey + ' (' + u.prefix + ')').join(', ');
             process.stderr.write('[qgsd] INFO: Quorum passed. Note: ' + note + ' was unavailable and skipped.\n');
           }
           process.exit(0);
         }

      5. In the BLOCK path, if unavailableKeys.length > 0, append a note to the blockReason:
         const unavailNote = unavailableKeys.length > 0
           ? ' [Note: ' + unavailableKeys.map(u => u.modelKey + ' (' + u.prefix + ')').join(', ') + ' was unavailable and skipped per fail-open policy]'
           : '';
         const blockReason = 'QUORUM REQUIRED: ...' + unavailNote;

      6. Update the loadConfig() call in main() to pass no argument (hook uses process.cwd() for project lookup):
         const config = loadConfig();

      Changes to hooks/qgsd-stop.test.js:

      Add three new test cases after TC10:

      TC11: "fail-open: unavailable model (not in mcpServers) passes even without evidence"
        - Mock ~/.claude.json by writing it to a temp path — but the hook reads the real ~/.claude.json path.
        - Instead: use a custom qgsd.json config in the temp directory where the tool_prefix uses a server name NOT present in the real ~/.claude.json mcpServers.
        - OR: Test the getAvailableMcpPrefixes behavior indirectly — write a transcript where only codex+gemini called, and if the real system has opencode in mcpServers, this will block. Too environment-dependent.
        - BETTER APPROACH for TC11-TC13: Export getAvailableMcpPrefixes() as a module function. In the test, mock it by writing a synthetic config that has a required_models entry with a tool_prefix that does NOT match any real mcpServers entry.
        - ACTUAL APPROACH: Since the hook is spawned as child process, use a config file with a model whose prefix is deliberately unusual (e.g., "mcp__test-unavailable-model__") — this prefix will never appear in the real mcpServers. This is deterministic.

        Concrete TC11 setup:
        - Write a temp qgsd.json with required_models: { test_model: { tool_prefix: "mcp__test-unavailable-model__", required: true } }
        - Write a transcript with /gsd:plan-phase in current turn + NO tool_use for test_model
        - The hook will call getAvailableMcpPrefixes() → real ~/.claude.json → "mcp__test-unavailable-model__" not in any server prefix
        - Expected: exit 0, empty stdout (fail-open pass)
        - BUT: this only works if ~/.claude.json exists. If it doesn't, hook returns null → treats as available → blocks.
        - Document this env dependency in the test.

        Use env var QGSD_TEST_CLAUDE_JSON to allow the test to inject a mock path — then the hook reads it if set.
        This is the cleanest approach for deterministic testing.

        SIMPLEST DETERMINISTIC APPROACH (no env var needed):
        Write TC11 using a tool_prefix that is guaranteed not in any mcpServers:
        "mcp__zzz-no-such-server-qgsd-test__" — if test environment has ~/.claude.json, the prefix will not be there → fail-open. If ~/.claude.json missing → returns null → conservative → test will fail. Mark the test as conditional.

        RECOMMENDATION: Add a QGSD_CLAUDE_JSON env var override in getAvailableMcpPrefixes() for testability:
        const claudeJsonPath = process.env.QGSD_CLAUDE_JSON || path.join(os.homedir(), '.claude.json');
        Tests then pass a temp JSON file path. This is clean and standard.

        TC11: Pass env QGSD_CLAUDE_JSON pointing to temp file with mcpServers: {}. Required model has prefix "mcp__codex-cli__". No evidence in transcript. Expected: block (model configured but not available in empty mcpServers → actually: no match → unavailable → fail-open? No — empty mcpServers means prefix not in availablePrefixes → unavailable → pass). Assert: stdout empty.

        TC12: Pass env QGSD_CLAUDE_JSON pointing to temp file with mcpServers: { "codex-cli": {} }. Required models: codex (mcp__codex-cli__) + gemini (mcp__gemini-cli__). Transcript has codex tool_use but no gemini tool_use. Expected: block for gemini (gemini not in mcpServers → unavailable → skipped; codex in mcpServers → called → pass; but wait — gemini not in mcpServers → unavailable → skip. Only codex required+available+called → pass? No. Gemini is required, not called, NOT in mcpServers → unavailable → skip → pass). Assert: stdout empty (both models either called or unavailable).

        TC13: MCP-06 regression with renamed prefix. Config has required_models: { custom: { tool_prefix: "mcp__my-custom-codex__", required: true } }. Env QGSD_CLAUDE_JSON with mcpServers: { "my-custom-codex": {} }. Transcript has tool_use "mcp__my-custom-codex__review". Expected: exit 0, stdout empty (prefix match found).

      FINAL TEST APPROACH: Use QGSD_CLAUDE_JSON env var in getAvailableMcpPrefixes(). Tests inject a synthetic file for deterministic results. Clearly document this in test file comments.
    </implementation>
  </feature>
  <verify>
    1. node --test hooks/qgsd-stop.test.js — all 13 tests pass (TC1-TC10 unchanged, TC11-TC13 new)
    2. grep "require('./config-loader')" hooks/qgsd-stop.js — returns a match
    3. grep "function loadConfig" hooks/qgsd-stop.js — returns NO match (inline removed)
    4. grep "getAvailableMcpPrefixes" hooks/qgsd-stop.js — returns a match
    5. node --test hooks/config-loader.test.js — still passes (unchanged)
  </verify>
  <done>All 13 stop hook tests pass. qgsd-stop.js uses shared config-loader. Fail-open unavailability detection works: unavailable models pass, available-but-not-called models block.</done>
</task>

</tasks>

<verification>
Overall Plan 02 checks:
1. node --test hooks/qgsd-stop.test.js — 13 tests pass (TC1-TC10 + TC11-TC13)
2. node --test hooks/config-loader.test.js — all tests still pass
3. grep "require('./config-loader')" hooks/qgsd-stop.js — match found
4. grep "getAvailableMcpPrefixes" hooks/qgsd-stop.js — match found
5. grep "function loadConfig\|const DEFAULT_CONFIG" hooks/qgsd-stop.js — NO match (inline removed)
6. No stdout output from getAvailableMcpPrefixes() in any code path — only stderr
</verification>

<success_criteria>
- hooks/qgsd-stop.js uses require('./config-loader') — no inline loadConfig() or DEFAULT_CONFIG
- getAvailableMcpPrefixes() reads ~/.claude.json (or QGSD_CLAUDE_JSON env override) for mcpServers
- Models whose tool_prefix is not in availablePrefixes are skipped (fail-open) when availablePrefixes is non-null
- Models whose tool_prefix IS in availablePrefixes but have no transcript evidence trigger a block
- When availablePrefixes is null (file missing/malformed): conservative — all missing models trigger block
- Unavailability notes written to stderr only (never stdout)
- All 13 tests pass (TC1-TC10 regression + TC11 unavailable model passes, TC12 partial availability, TC13 MCP-06 renamed prefix)
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-mcp-detection/02-02-SUMMARY.md`
</output>
