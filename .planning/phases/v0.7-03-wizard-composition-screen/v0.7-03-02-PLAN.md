---
phase: v0.7-03-wizard-composition-screen
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - commands/qgsd/mcp-setup.md
autonomous: true
requirements: [WIZ-10]

must_haves:
  truths:
    - "The Composition Screen AskUserQuestion includes 'Add new slot — add a slot to ~/.claude.json and quorum_active' as an option (already in Plan 01 stub)"
    - "Selecting 'Add new slot' from the composition screen routes to Step A (EXISTING_SERVERS detection + template selection)"
    - "After Step D (identity ping) completes in the add-slot flow, execution returns to the Composition Screen (re-reading fresh data)"
    - "The newly added slot appears in the Composition Screen table with ● ON status after return"
    - "The success_criteria block in mcp-setup.md lists all three WIZ flows: WIZ-08 (composition menu option), WIZ-09 (toggle screen), WIZ-10 (add slot from composition screen)"
  artifacts:
    - path: "commands/qgsd/mcp-setup.md"
      provides: "Add new slot flow from Composition Screen (WIZ-10)"
      contains: "Add new slot"
  key_links:
    - from: "Composition Screen 'Add new slot' option"
      to: "Step A (EXISTING_SERVERS detection + template selection)"
      via: "If 'Add new slot': route to Step A with return-to-composition marker"
      pattern: "Add new slot"
    - from: "Step D identity ping (end of add-slot flow)"
      to: "Composition Screen (re-read data)"
      via: "Return to composition screen after add"
      pattern: "return to composition screen"
---

<objective>
Wire the "Add new slot" option in the Composition Screen to the existing Step A/B/B-native flow (WIZ-10). After a new slot is added and the identity ping completes, execution returns to the Composition Screen showing the new slot as active. Update the `success_criteria` block to reflect all three WIZ flows.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/v0.7-03-wizard-composition-screen/v0.7-03-RESEARCH.md
@commands/qgsd/mcp-setup.md
</context>

<task type="auto">
<name>Task 1: Wire "Add new slot" routing in the Composition Screen</name>
<files>commands/qgsd/mcp-setup.md</files>
<action>
In the Composition Screen section (added by Plan 01), locate the "Add new slot" handler comment/stub. Replace it with full routing instructions:

```
**Add new slot handler:** When "Add new slot" selected:

Route to **Step A** (EXISTING_SERVERS detection + template selection). This is the same flow as "Add new agent" from the roster menu. After Step D (identity ping) completes:

- Return to **Composition Screen** (re-read fresh `slots` and `active` data from disk).
- The new slot will appear in the table with ● ON status (it was added to quorum_active during Step B-native or Step B flows).
- Re-display the Composition Screen AskUserQuestion.
```

Note: Step B-native already adds the new slot to quorum_active (line ~720 in mcp-setup.md). Step B (claude-mcp-server) also adds to quorum_active via the inline script in its apply flow. So the return path simply re-reads qgsd.json and the slot will be ON.

Confirm the return-to-composition instruction appears at or near the end of both Step D variants (claude-mcp-server path around line 648 and native CLI path around line 741). Add the return path marker: "If invoked from Composition Screen: return to Composition Screen after restart. Otherwise: return to roster display."

Implement this by adding a conditional note after each Step D completion block:
```
**Return path:** If this add-slot flow was entered from the Composition Screen (via "Add new slot"), return to Composition Screen after identity ping. Otherwise, return to roster display.
```
</action>
<verify>
grep -c "Add new slot\|return to.*[Cc]omposition\|Composition Screen" commands/qgsd/mcp-setup.md
# Expected: >= 5 (option in CS + handler routing + 2 Step D return paths + section header)
</verify>
<done>
"Add new slot" routing from Composition Screen to Step A is specified. Both Step D variants include a return-to-composition path.
</done>
</task>

<task type="auto">
<name>Task 2: Update success_criteria block to include WIZ-08/09/10</name>
<files>commands/qgsd/mcp-setup.md</files>
<action>
Locate the `<success_criteria>` block at the bottom of `commands/qgsd/mcp-setup.md`. Add three new bullet points covering the WIZ flows:

```
- Edit Quorum Composition flow (WIZ-08): re-run menu option → composition screen entry
- Composition toggle flow (WIZ-09): slot list with ON/OFF indicators → toggle → apply writes quorum_active to qgsd.json → no restart required
- Add slot from composition flow (WIZ-10): "Add new slot" → Step A/B/B-native → identity ping → return to composition screen showing new slot ON
```
</action>
<verify>
grep -c "WIZ-08\|WIZ-09\|WIZ-10" commands/qgsd/mcp-setup.md
# Expected: >= 3
</verify>
<done>WIZ-08, WIZ-09, WIZ-10 all referenced in success_criteria block.</done>
</task>

<task type="auto">
<name>Task 3: npm test + commit</name>
<files>commands/qgsd/mcp-setup.md</files>
<action>
Run npm test to confirm no regressions:
```bash
npm test 2>&1 | grep -E "pass|fail" | grep -v "async\|at " | tail -5
```

Stage and commit:
```bash
git add commands/qgsd/mcp-setup.md
git commit -m "feat(v0.7-03-02): mcp-setup add-slot-from-composition routing + WIZ-10 success criteria (WIZ-10)"
```
</action>
<verify>
git log --oneline -1 | grep "v0.7-03-02"
npm test 2>&1 | grep "pass " | tail -3
</verify>
<done>Committed. npm test shows no new failures beyond pre-existing 3.</done>
</task>
