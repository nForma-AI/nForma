---
phase: v0.37-03-scanner-fp-tuning
plan: 01
status: complete
started: 2026-03-16
completed: 2026-03-16
---

## Summary

Implemented per-scanner FP rate tracking, auto-threshold tuning for chronic FP offenders, and diagnostics table display in nf-solve.cjs.

## Tasks Completed

| # | Task | Status |
|---|------|--------|
| 1 | Add session_history recording and FP rate computation | Done |
| 2 | Add auto-threshold tuning for chronic FP offenders | Done |
| 3 | Add per-scanner FP rate table to report-only output | Done |
| 4 | Add FPTUNE unit tests | Done |

## Key Changes

- **computeScannerStats()** -- Extracts per-scanner per-category FP/genuine/review counts from classifications
- **recordSessionHistory()** -- Appends session entry to session_history, trims to rolling window of 10
- **computeFPRates()** -- Computes rolling FP rates per scanner across session history
- **applyFPTuning()** -- Auto-raises suppression threshold by 0.1 for scanners with >60% FP over 5+ sessions (cap 0.9)
- **formatFPRateTable()** -- Renders per-scanner FP rate table for diagnostics output
- **main() integration** -- Session history recorded and tuning applied after classification loading
- **formatReport() integration** -- FP rate table appended when session history exists

## Key Files

### Created
- `bin/nf-solve-fp-tuning.test.cjs` -- 38 tests for FPTUNE-01/02/03
- `.planning/phases/v0.37-03-scanner-fp-tuning/v0.37-03-01-SUMMARY.md`

### Modified
- `bin/nf-solve.cjs` -- 5 new exported functions, main() and formatReport() integration

## Self-Check: PASSED

- [x] All 4 tasks executed
- [x] Each task committed individually (2 commits)
- [x] SUMMARY.md created
- [x] session_history appears 10 times in source
- [x] All 38 FPTUNE tests pass (0 failures)
- [x] 5 new functions exported via module.exports

## Decisions

- Used rolling window of 10 sessions (matching FPTUNE-01 requirement) with FIFO trim
- Default suppression threshold set to 0.5 (reasonable baseline -- tuning raises from there)
- FP rate table uses fixed scanner order (ctor, ttor, dtor, dtoc) for consistent output
- applyFPTuning reads/writes the file each call (atomic, no in-memory state between cycles)
