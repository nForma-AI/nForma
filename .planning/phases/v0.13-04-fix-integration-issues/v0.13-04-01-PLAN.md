---
phase: v0.13-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qgsd-core/workflows/plan-milestone-gaps.md
  - qgsd-core/workflows/execute-phase.md
  - qgsd-core/workflows/discuss-phase.md
autonomous: true
requirements:
  - QUORUM-01
  - QUORUM-02
  - QUORUM-03
gap_closure: true

must_haves:
  truths:
    - "All three workflow files use the identical canonical path $HOME/.claude/qgsd-bin/update-scoreboard.cjs"
    - "No workflow file references the non-existent path $HOME/.claude/qgsd/bin/update-scoreboard.cjs"
    - "INT-01 is confirmed closed: current path is correct and consistent across all three files"
  artifacts:
    - path: "qgsd-core/workflows/plan-milestone-gaps.md"
      provides: "Scoreboard invocation using canonical qgsd-bin path"
      contains: "qgsd-bin/update-scoreboard.cjs"
    - path: "qgsd-core/workflows/execute-phase.md"
      provides: "Scoreboard invocation using canonical qgsd-bin path"
      contains: "qgsd-bin/update-scoreboard.cjs"
    - path: "qgsd-core/workflows/discuss-phase.md"
      provides: "Scoreboard invocation using canonical qgsd-bin path"
      contains: "qgsd-bin/update-scoreboard.cjs"
  key_links:
    - from: "qgsd-core/workflows/plan-milestone-gaps.md"
      to: "$HOME/.claude/qgsd-bin/update-scoreboard.cjs"
      via: "node invocation in Step 5 quorum gate"
      pattern: "qgsd-bin/update-scoreboard\\.cjs"
    - from: "qgsd-core/workflows/execute-phase.md"
      to: "$HOME/.claude/qgsd-bin/update-scoreboard.cjs"
      via: "node invocation in gaps_found quorum gate"
      pattern: "qgsd-bin/update-scoreboard\\.cjs"
    - from: "qgsd-core/workflows/discuss-phase.md"
      to: "$HOME/.claude/qgsd-bin/update-scoreboard.cjs"
      via: "node invocation in present_gray_areas quorum gate"
      pattern: "qgsd-bin/update-scoreboard\\.cjs"
---

<objective>
Verify and document that INT-01 (binary path inconsistency) is a non-issue: all three workflow files already use the identical canonical path `$HOME/.claude/qgsd-bin/update-scoreboard.cjs`. No path change is needed. The task produces grep evidence confirming 0 occurrences of the stale path `qgsd/bin/update-scoreboard.cjs` across all three files, and confirms that the current path exists on disk.

Purpose: The v0.13 audit flagged INT-01 as a portability risk. Research confirmed the audit's fix recommendation would break the path (update-scoreboard.cjs does NOT exist at `~/.claude/qgsd/bin/`). This plan closes INT-01 by verifying the current state is correct and documenting the canonical path determination.

Output: Grep verification confirming consistent correct path across all three workflow files. No file content changes required.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.13-04-fix-integration-issues/v0.13-04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify INT-01 — confirm canonical path is consistent and correct across all three workflow files</name>
  <files>qgsd-core/workflows/plan-milestone-gaps.md, qgsd-core/workflows/execute-phase.md, qgsd-core/workflows/discuss-phase.md</files>
  <action>
Run the following verification commands and record the output. No file content changes are expected.

Step 1 — Confirm the canonical path exists on disk:
```bash
ls "$HOME/.claude/qgsd-bin/update-scoreboard.cjs"
```
Expected: file exists. If file does NOT exist, stop and escalate — INT-01 would require an installer fix, which is out of scope for this phase.

Step 2 — Confirm the alternate (incorrect) path does NOT exist on disk:
```bash
ls "$HOME/.claude/qgsd/bin/update-scoreboard.cjs" 2>&1 || echo "CONFIRMED ABSENT"
```
Expected: "CONFIRMED ABSENT" or "No such file or directory".

Step 3 — Confirm all three workflow files use the canonical path (should return 3 matches, one per file):
```bash
grep -l 'qgsd-bin/update-scoreboard\.cjs' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md
```
Expected: all three file paths printed.

Step 4 — Confirm no file uses the incorrect alternate path (should return 0 matches):
```bash
grep -l 'qgsd/bin/update-scoreboard\.cjs' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md 2>&1 || echo "0 matches — correct"
```
Expected: "0 matches — correct".

Step 5 — Show the exact line from each file to confirm identical quoting style:
```bash
grep -n 'update-scoreboard\.cjs' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md
```
Expected: three lines, all using `"$HOME/.claude/qgsd-bin/update-scoreboard.cjs"` with identical quoting and path format.

If all checks pass: INT-01 is confirmed closed. No file edits needed. Record all command outputs in the SUMMARY.

If any check fails: document the failure in the SUMMARY and note what follow-up is needed (likely an installer-scope fix, out of scope for this phase).
  </action>
  <verify>
```bash
# Path exists on disk
ls "$HOME/.claude/qgsd-bin/update-scoreboard.cjs" && echo "EXISTS"

# 0 occurrences of incorrect path across all three files
grep -c 'qgsd/bin/update-scoreboard\.cjs' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md
# Expected output: each file:0

# 3 matches of correct canonical path (one per file)
grep -c 'qgsd-bin/update-scoreboard\.cjs' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/execute-phase.md \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/discuss-phase.md
# Expected output: each file:1
```
  </verify>
  <done>
- `$HOME/.claude/qgsd-bin/update-scoreboard.cjs` exists on disk
- All three workflow files contain exactly 1 match of `qgsd-bin/update-scoreboard.cjs`
- 0 files contain `qgsd/bin/update-scoreboard.cjs`
- Command outputs recorded in SUMMARY confirming INT-01 is closed
  </done>
</task>

</tasks>

<verification>
INT-01 is closed when:
1. Disk check confirms `$HOME/.claude/qgsd-bin/update-scoreboard.cjs` exists
2. grep confirms 0 occurrences of the incorrect path across all three files
3. grep confirms 1 occurrence of the correct path in each file
4. All three lines use identical quoting/format (`"$HOME/.claude/qgsd-bin/update-scoreboard.cjs"`)
</verification>

<success_criteria>
- [ ] `$HOME/.claude/qgsd-bin/update-scoreboard.cjs` confirmed to exist on disk
- [ ] 0 occurrences of `qgsd/bin/update-scoreboard.cjs` in any of the three workflow files
- [ ] Each of the three workflow files contains exactly 1 occurrence of `qgsd-bin/update-scoreboard.cjs`
- [ ] All three occurrences use identical path format confirming portability
- [ ] INT-01 confirmed closed — no path change needed
</success_criteria>

<output>
After completion, create `.planning/phases/v0.13-04-fix-integration-issues/v0.13-04-01-SUMMARY.md`
</output>
