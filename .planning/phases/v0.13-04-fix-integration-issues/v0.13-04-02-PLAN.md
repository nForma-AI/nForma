---
phase: v0.13-04
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - qgsd-core/workflows/plan-milestone-gaps.md
autonomous: true
requirements:
  - QUORUM-01
gap_closure: true

must_haves:
  truths:
    - "plan-milestone-gaps.md Step 2 priority table nice row does not contain unguarded 'Ask user: include or defer?' text"
    - "plan-milestone-gaps.md Step 5 fenced code block template does not contain the bare 'Create these {X} phases? (yes / adjust / defer all optional)' user-dialog line"
    - "plan-milestone-gaps.md success_criteria does not contain stale 'User confirmed phase plan' criterion"
    - "plan-milestone-gaps.md success_criteria contains quorum-compatible criterion referencing GSD_DECISION"
    - "The quorum approval gate (QUORUM-01 section) in Step 5 is intact after the edits"
  artifacts:
    - path: "qgsd-core/workflows/plan-milestone-gaps.md"
      provides: "Auto-mode compatible plan-milestone-gaps workflow"
      contains: "GSD_DECISION"
  key_links:
    - from: "qgsd-core/workflows/plan-milestone-gaps.md line 36"
      to: "auto-mode compatible nice-row text"
      via: "priority table Step 2"
      pattern: "Auto-defer"
    - from: "qgsd-core/workflows/plan-milestone-gaps.md success_criteria"
      to: "quorum-based completion criterion"
      via: "success_criteria block"
      pattern: "GSD_DECISION"
---

<objective>
Fix INT-02: Remove three specific residual user-gate text fragments from `qgsd-core/workflows/plan-milestone-gaps.md` that would cause a general-purpose subagent in autonomous mode to pause awaiting user input. The quorum approval gate (already in Step 5) replaces the user dialog — the surrounding text must reflect this.

Purpose: INT-02 blocks fully autonomous execution. A subagent spawned by audit-milestone in gap-closure mode reads this workflow and may interpret `"Create these {X} phases? (yes / adjust / defer all optional)"` as an instruction to prompt the user. With the quorum gate already written, the three user-gate text fragments are stale artifacts from before QUORUM-01 was implemented.

Output: `plan-milestone-gaps.md` with three targeted text changes; quorum gate and all surrounding workflow logic untouched.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.13-04-fix-integration-issues/v0.13-04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix INT-02 location 1 — replace unguarded nice-row text in Step 2 priority table</name>
  <files>qgsd-core/workflows/plan-milestone-gaps.md</files>
  <action>
Read `/Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md` line 36.

Current text (line 36, inside the Step 2 priority table):
```
| `nice` | Ask user: include or defer? |
```

Replace with:
```
| `nice` | Auto-defer in `--auto` mode; include otherwise |
```

This removes the `Ask user:` instruction which a subagent in autonomous mode may interpret as a directive to prompt the user. The replacement accurately describes the auto-mode behavior (nice gaps are deferred automatically) while preserving the meaning for interactive mode.

Do NOT touch any other rows in the table. Do NOT touch lines outside the table row. Verify the change by reading lines 30–40 after the edit to confirm only line 36 changed.
  </action>
  <verify>
```bash
grep -n 'Ask user: include or defer' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 0 matches

grep -n 'Auto-defer' /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 1 match at or near line 36
```
  </verify>
  <done>
- Line 36 reads `| \`nice\` | Auto-defer in \`--auto\` mode; include otherwise |`
- 0 occurrences of `Ask user: include or defer?` in the file
- Priority table structure intact (must/should/nice rows all present)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix INT-02 locations 2 and 3 — remove bare user-dialog line and update success_criteria</name>
  <files>qgsd-core/workflows/plan-milestone-gaps.md</files>
  <action>
Read `/Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md` focusing on two sections.

**Fix A — Remove bare user-dialog line from Step 5 fenced code block (line ~107):**

The fenced code block (closing ` ``` ` at approximately line 108) contains this line just before the closing fence:
```
Create these {X} phases? (yes / adjust / defer all optional)
```

Also guard the optional nice-to-have section earlier in the same code block (lines ~97-103):

Current text (lines ~97-103):
```
{If nice-to-have gaps exist:}

### Deferred (nice-to-have)

These gaps are optional. Include them?
- {gap description}
- {gap description}
```

Replace with:
```
{If nice-to-have gaps exist AND not running in --auto mode:}

### Deferred (nice-to-have)

These gaps are optional. Include them?
- {gap description}
- {gap description}

{If nice-to-have gaps exist AND running in --auto mode:}
Nice-to-have gaps deferred automatically (auto mode). Include manually if desired.
```

Then delete the bare user-dialog line (line ~107):
```
Create these {X} phases? (yes / adjust / defer all optional)
```

The closing ` ``` ` fence stays. The `**Quorum approval gate (QUORUM-01):**` section starting immediately after the closing fence MUST remain untouched.

**Fix B — Update stale success_criteria (line ~318):**

Current text (inside `<success_criteria>` block):
```
- [ ] User confirmed phase plan
```

Replace with:
```
- [ ] Quorum approved gap closure phases (GSD_DECISION marker present in Step 5 output)
```

After making both edits, read lines 95-115 and 314-326 to confirm:
- The optional section has the auto-mode guard
- The bare dialog line is gone
- The closing fence and quorum gate section below it are intact
- The success_criteria has the updated line

Do NOT remove, modify, or relocate the `**Quorum approval gate (QUORUM-01):**` section or any of its subsections (quorum dispatch, scoreboard update, GSD_DECISION marker, BLOCKED route). That section is the QUORUM-01 implementation and must remain intact.
  </action>
  <verify>
```bash
# Confirm bare user-dialog line is gone (must return 0)
grep -c 'Create these.*phases.*yes.*adjust.*defer' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 0

# Confirm stale success criterion is gone (must return 0)
grep -c 'User confirmed phase plan' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 0

# Confirm quorum gate is still present (must return 1)
grep -c 'Quorum approval gate' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 1

# Confirm GSD_DECISION success criterion is present (must return 1+)
grep -c 'GSD_DECISION' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 2+ (one in Step 5 APPROVED route, one in success_criteria)

# Confirm auto-mode guard added to nice section (must return 1)
grep -c 'not running in --auto mode' \
  /Users/jonathanborduas/code/QGSD/qgsd-core/workflows/plan-milestone-gaps.md
# Expected: 1
```
  </verify>
  <done>
- 0 occurrences of `Create these {X} phases? (yes / adjust / defer all optional)` in the file
- 0 occurrences of `User confirmed phase plan` in the file
- 1 occurrence of `Quorum approval gate` (QUORUM-01 section intact)
- 2+ occurrences of `GSD_DECISION` (Step 5 APPROVED route + success_criteria)
- 1 occurrence of `not running in --auto mode` (nice-to-have section guarded)
- success_criteria item reads: `Quorum approved gap closure phases (GSD_DECISION marker present in Step 5 output)`
  </done>
</task>

</tasks>

<verification>
INT-02 is closed when all four conditions hold in `qgsd-core/workflows/plan-milestone-gaps.md`:

1. `grep -c 'Ask user: include or defer' plan-milestone-gaps.md` → 0
2. `grep -c 'Create these.*phases.*yes.*adjust.*defer' plan-milestone-gaps.md` → 0
3. `grep -c 'User confirmed phase plan' plan-milestone-gaps.md` → 0
4. `grep -c 'Quorum approval gate' plan-milestone-gaps.md` → 1 (gate intact)
5. `grep -c 'GSD_DECISION' plan-milestone-gaps.md` → 2+ (Step 5 route + success_criteria)
</verification>

<success_criteria>
- [ ] `Ask user: include or defer?` removed from Step 2 priority table nice row
- [ ] nice row updated to: `Auto-defer in \`--auto\` mode; include otherwise`
- [ ] nice-to-have section in Step 5 code block guarded with `{If ... AND not running in --auto mode:}` conditional
- [ ] auto-mode nice-to-have deferral message added (`Nice-to-have gaps deferred automatically (auto mode)...`)
- [ ] Bare `Create these {X} phases? (yes / adjust / defer all optional)` line removed from Step 5 code block
- [ ] `User confirmed phase plan` replaced in success_criteria
- [ ] success_criteria item now reads: `Quorum approved gap closure phases (GSD_DECISION marker present in Step 5 output)`
- [ ] Quorum approval gate (QUORUM-01) section in Step 5 is untouched
- [ ] All verify grep commands return expected counts
</success_criteria>

<output>
After completion, create `.planning/phases/v0.13-04-fix-integration-issues/v0.13-04-02-SUMMARY.md`
</output>
