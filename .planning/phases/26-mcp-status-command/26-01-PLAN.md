---
phase: 26-mcp-status-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/qgsd/mcp-status.md
  - ~/.claude/commands/qgsd/mcp-status.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04

must_haves:
  truths:
    - "User can run /qgsd:mcp-status and see a table with all 10 connected agents"
    - "Each row shows name, version, current model, health state, available models, and UNAVAIL count"
    - "Health state is derived from scoreboard: quota-exceeded if UNAVAIL > 0, error if identity failed, available otherwise"
    - "UNAVAIL count is computed from scoreboard rounds[].votes, not from static config"
    - "available_models column shows live data from identity tool response"
    - "Missing scoreboard file does not crash the command — shows UNAVAIL=0 and health=available"
    - "Individual agent identity failure shows error health and dashes — does not abort the whole table"
    - "mcp-status is NOT in quorum_commands (read-only observation, R2.1 compliance)"
  artifacts:
    - path: "commands/qgsd/mcp-status.md"
      provides: "mcp-status command source"
      contains: "mcp__claude-glm__identity"
    - path: "~/.claude/commands/qgsd/mcp-status.md"
      provides: "installed mcp-status command"
      contains: "mcp__claude-glm__identity"
  key_links:
    - from: "mcp-status.md Step 1"
      to: ".planning/quorum-scoreboard.json"
      via: "node -e inline script reading rounds[].votes"
      pattern: "quorum-scoreboard\\.json"
    - from: "mcp-status.md Step 3"
      to: "mcp__claude-glm__identity (and 9 others)"
      via: "sequential identity tool calls"
      pattern: "mcp__.*__identity"
    - from: "scoreboard counts[scoreboardKey]"
      to: "health state derivation"
      via: "explicit model key → MCP server name mapping"
      pattern: "counts\\[scoreboardKey\\]"
---

<objective>
Create the `/qgsd:mcp-status` command file that calls `identity` on all 10 configured MCP agents sequentially, reads UNAVAIL counts from the quorum scoreboard, and renders a formatted table showing name, version, model, health state, available models, and UNAVAIL count.

Purpose: Gives users instant visibility into which quorum agents are healthy, quota-exceeded, or erroring — and what models they're running.
Output: `commands/qgsd/mcp-status.md` (source) + `~/.claude/commands/qgsd/mcp-status.md` (installed)
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mcp-status-command/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/qgsd/mcp-status.md source file</name>
  <files>/Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md</files>
  <action>
Create `/Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md` with the following exact content. The command covers all 10 MCP agents — the 10th is `claude-glm` (Fireworks/GLM-5), which was added after the initial plan was drafted. Include it in both the frontmatter `allowed-tools` and the agent table in Step 3.

The complete file content:

```
---
name: qgsd:mcp-status
description: Show status of all connected quorum agents — name, version, model, health, available models, and UNAVAIL count from the scoreboard
allowed-tools:
  - Read
  - Bash
  - mcp__codex-cli__identity
  - mcp__gemini-cli__identity
  - mcp__opencode__identity
  - mcp__copilot-cli__identity
  - mcp__claude-deepseek__identity
  - mcp__claude-minimax__identity
  - mcp__claude-qwen-coder__identity
  - mcp__claude-kimi__identity
  - mcp__claude-llama4__identity
  - mcp__claude-glm__identity
---

<objective>
Display a formatted status table of all connected MCP quorum agents. For each agent: call its identity tool to get live name/version/model/available_models, read UNAVAIL count from the quorum scoreboard, and derive health state.

This command is read-only (observation only). It does NOT invoke quorum and is NOT in quorum_commands.
</objective>

<process>

## Step 1: Read UNAVAIL counts from scoreboard

Run the following Bash command and store the output as SCOREBOARD_INFO:

node -e "
const fs=require('fs');
const p='.planning/quorum-scoreboard.json';
if(!fs.existsSync(p)){console.log('{}');process.exit(0);}
const d=JSON.parse(fs.readFileSync(p,'utf8'));
const counts={};
for(const r of d.rounds||[]){
  for(const [m,v] of Object.entries(r.votes||{})){
    if(v==='UNAVAIL')counts[m]=(counts[m]||0)+1;
  }
}
const info={counts,totalRounds:(d.rounds||[]).length,lastUpdate:d.team?.captured_at||null};
console.log(JSON.stringify(info));
"

Parse `counts` (object: model key → UNAVAIL count), `totalRounds`, `lastUpdate`.

If scoreboard file does not exist, the script outputs `{}` — treat all counts as 0.

## Step 2: Display banner

Print:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 QGSD ► MCP STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Querying 10 agents...

## Step 3: Call identity on each agent (sequential — one at a time, never parallel)

For each of the 10 agents below, call their identity tool with `{}` as input. Wrap each call in try/catch — on error, mark health=`error` and fill version/model/available_models with `—`. Never let a single agent failure abort the loop.

**Agent list and tool mapping (complete — 10 agents):**

| Display Name      | Identity Tool                 | Scoreboard Key |
|---|---|---|
| codex-cli         | mcp__codex-cli__identity      | codex          |
| gemini-cli        | mcp__gemini-cli__identity     | gemini         |
| opencode          | mcp__opencode__identity       | opencode       |
| copilot-cli       | mcp__copilot-cli__identity    | copilot        |
| claude-deepseek   | mcp__claude-deepseek__identity | deepseek      |
| claude-minimax    | mcp__claude-minimax__identity  | minimax       |
| claude-qwen-coder | mcp__claude-qwen-coder__identity | qwen-coder  |
| claude-kimi       | mcp__claude-kimi__identity    | kimi           |
| claude-llama4     | mcp__claude-llama4__identity  | llama4         |
| claude-glm        | mcp__claude-glm__identity     | glm            |

After each successful identity call, parse the JSON response to extract: `name`, `version`, `model`, `available_models`, `install_method`. If `available_models` has more than 3 entries, display the first 3 joined by ", " followed by ", ..." truncation. If `available_models` is empty or null, display `—`.

Note: `glm` is not yet in the scoreboard VALID_MODELS list — `counts['glm']` will be undefined. Treat as 0 (correct, not an error).

## Step 4: Derive health state per agent

For each agent after the identity call attempt:
- If identity call threw an exception or returned an error → health = `error`
- Else if `(counts[scoreboardKey] || 0) > 0` → health = `quota-exceeded`
- Else → health = `available`

## Step 5: Render formatted table

Display the table with these columns: Agent | Version | Model | Health | Available Models | UNAVAIL

Example output format:

┌─────────────────────┬─────────┬──────────────────────────┬────────────────┬───────────────────────────────┬─────────┐
│ Agent               │ Version │ Model                    │ Health         │ Available Models              │ UNAVAIL │
├─────────────────────┼─────────┼──────────────────────────┼────────────────┼───────────────────────────────┼─────────┤
│ codex-cli           │ 1.2.3   │ codex                    │ quota-exceeded │ codex, o3-mini, gpt-4o, ...   │ 56      │
│ gemini-cli          │ 1.1.0   │ gemini-3-pro-preview     │ quota-exceeded │ gemini-3-pro, flash, ...      │ 34      │
│ opencode            │ 1.0.0   │ claude-sonnet-4-6        │ available      │ claude-sonnet-4-6, gpt-4o     │ 4       │
│ copilot-cli         │ 1.2.0   │ gpt-4.1                  │ available      │ gpt-4.1, gpt-4o, claude-3.5   │ 0       │
│ claude-deepseek     │ 1.0.0   │ deepseek-ai/DeepSeek-V3  │ available      │ —                             │ 0       │
│ claude-minimax      │ 1.0.0   │ MiniMaxAI/MiniMax-M2.5   │ available      │ —                             │ 0       │
│ claude-qwen-coder   │ 1.0.0   │ Qwen/Qwen3-Coder-480B    │ available      │ —                             │ 0       │
│ claude-kimi         │ 1.0.0   │ kimi                     │ available      │ —                             │ 0       │
│ claude-llama4       │ 1.0.0   │ meta-llama/Llama-4-M     │ available      │ —                             │ 0       │
│ claude-glm          │ 1.0.0   │ glm-5                    │ available      │ —                             │ 0       │
└─────────────────────┴─────────┴──────────────────────────┴────────────────┴───────────────────────────────┴─────────┘

Scoreboard: {totalRounds} rounds recorded | Last update: {lastUpdate}

If scoreboard file was absent (empty counts), show instead:
Scoreboard: no data yet (run /qgsd:quorum first to populate)

</process>

<success_criteria>
- All 10 agents queried (or marked error if unavailable)
- UNAVAIL count shown per agent from scoreboard rounds
- Health state shown: available / quota-exceeded / error
- available_models shown (truncated at 3 + "..." if longer)
- Command handles missing scoreboard gracefully (no crash)
- Command handles individual agent identity failures gracefully (no crash)
- claude-glm row present with UNAVAIL=0 (scoreboard does not yet record glm votes)
</success_criteria>
```
  </action>
  <verify>
Run: `grep "mcp__.*__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md | wc -l`
Expected: 10 (one line per identity tool in allowed-tools frontmatter)

Run: `grep "claude-glm" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md | wc -l`
Expected: at least 2 (one in allowed-tools, one in agent table)
  </verify>
  <done>File exists at `/Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md`, contains all 10 identity tool references in frontmatter and the complete agent table including claude-glm row.</done>
</task>

<task type="auto">
  <name>Task 2: Install mcp-status.md and verify structural correctness</name>
  <files>/Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md</files>
  <action>
Copy the source file to the installed location:

```bash
cp /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md /Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md
```

Verify the installed copy matches the source:
```bash
diff /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md /Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md && echo "INSTALLED OK"
```

Run structural checks:

1. Confirm all 10 identity tools are in allowed-tools:
```bash
grep "mcp__.*__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md | wc -l
# Expected: 10
```

2. Confirm claude-glm is present (was missing in original plan):
```bash
grep "claude-glm" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
# Expected: at least 2 lines (frontmatter + agent table)
```

3. Confirm mcp-status is NOT in quorum_commands:
```bash
grep "mcp-status" /Users/jonathanborduas/.claude/qgsd.json 2>/dev/null || echo "NOT IN QUORUM - CORRECT"
# Expected: "NOT IN QUORUM - CORRECT"
```

4. Verify installed file exists and is non-empty:
```bash
ls -la /Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md
# Expected: file with size > 0
```

If all checks pass, commit:
```bash
cd /Users/jonathanborduas/code/QGSD && git add commands/qgsd/mcp-status.md && git commit -m "feat(26-01): add /qgsd:mcp-status command — 10 agents, identity polling, scoreboard UNAVAIL, health state"
```
  </action>
  <verify>
Run: `diff /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md /Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md && echo "OK"`
Expected: "OK" (no diff)

Run: `grep "mcp__.*__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md | wc -l`
Expected: 10

Run: `grep "mcp-status" /Users/jonathanborduas/.claude/qgsd.json 2>/dev/null || echo "NOT IN QUORUM"`
Expected: "NOT IN QUORUM"
  </verify>
  <done>Installed file at `~/.claude/commands/qgsd/mcp-status.md` is byte-for-byte identical to source; 10 identity tools confirmed; mcp-status not in quorum_commands; git commit created.</done>
</task>

</tasks>

<verification>
1. `ls /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md` — source file exists
2. `ls /Users/jonathanborduas/.claude/commands/qgsd/mcp-status.md` — installed file exists
3. `grep "mcp__.*__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md | wc -l` → 10 (all agents including claude-glm)
4. `grep "claude-glm" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md` → at least 2 matches
5. `grep "mcp-status" /Users/jonathanborduas/.claude/qgsd.json 2>/dev/null || echo "NOT IN QUORUM"` → "NOT IN QUORUM"
6. OBS-01: table shows name/version/model — confirmed by table columns in Step 5
7. OBS-02: health state from scoreboard UNAVAIL votes — confirmed by Step 4 derivation logic
8. OBS-03: available_models from identity tool response — confirmed by Step 3 parsing
9. OBS-04: UNAVAIL count from rounds[].votes — confirmed by Step 1 inline node script
</verification>

<success_criteria>
- `/qgsd:mcp-status` command exists in both source and installed locations
- All 10 agents are in the allowed-tools list: codex-cli, gemini-cli, opencode, copilot-cli, claude-deepseek, claude-minimax, claude-qwen-coder, claude-kimi, claude-llama4, claude-glm
- claude-glm appears in both frontmatter and agent table (the critical fix from original 9-agent plan)
- UNAVAIL counts computed dynamically from scoreboard rounds, not hardcoded
- Health state derived: quota-exceeded (UNAVAIL > 0), error (identity failed), available (otherwise)
- available_models displayed from live identity response, truncated at 3 + "..." if longer
- claude-glm shows UNAVAIL=0 without error (correct — glm not yet in scoreboard VALID_MODELS)
- Command handles missing scoreboard file gracefully
- mcp-status NOT in quorum_commands (R2.1 compliance)
- OBS-01, OBS-02, OBS-03, OBS-04 all satisfied by the single command file
</success_criteria>

<output>
After completion, create `.planning/phases/26-mcp-status-command/26-01-SUMMARY.md`
</output>
