---
phase: 07-enforcement-config-integration
plan: "02"
type: tdd
wave: 2
depends_on:
  - "07-01"
files_modified:
  - hooks/qgsd-circuit-breaker.js
  - hooks/qgsd-circuit-breaker.test.js
autonomous: true
requirements:
  - ENFC-01
  - ENFC-02
  - ENFC-03

must_haves:
  truths:
    - "When the circuit breaker state file has active:true and a non-read-only Bash command arrives, the hook outputs hookSpecificOutput.permissionDecision:'deny'"
    - "The block reason message contains: oscillating file set names, 'CIRCUIT BREAKER' confirmation, read-only allowed operations list, root cause analysis instruction, user manual-commit instruction"
    - "When the circuit breaker state file has active:true and a read-only command (git log) arrives, the hook exits 0 with empty stdout"
    - "The hook reads oscillation_depth and commit_window from loadConfig(gitRoot).circuit_breaker rather than hardcoded constants"
    - "A project .claude/qgsd.json with circuit_breaker.oscillation_depth:2 causes oscillation to trigger after 2 matching commits (not 3)"
    - "All 125 existing tests continue to pass after CB-TC7 is updated and CB-TC16 through CB-TC19 are added"
  artifacts:
    - path: "hooks/qgsd-circuit-breaker.js"
      provides: "Enforcement blocking: hookSpecificOutput.permissionDecision:'deny' when state.active and command is not read-only; config-driven thresholds via loadConfig(gitRoot)"
      contains: "permissionDecision"
    - path: "hooks/qgsd-circuit-breaker.test.js"
      provides: "Updated CB-TC7 asserting deny decision; new CB-TC16 (read-only pass), CB-TC17 (block reason content), CB-TC18 (config oscillation_depth), CB-TC19 (config commit_window)"
      min_lines: 520
  key_links:
    - from: "hooks/qgsd-circuit-breaker.js"
      to: "hooks/config-loader.js"
      via: "require('./config-loader') + loadConfig(gitRoot)"
      pattern: "loadConfig\\(gitRoot\\)"
    - from: "hooks/qgsd-circuit-breaker.js"
      to: "hookSpecificOutput"
      via: "process.stdout.write(JSON.stringify({hookSpecificOutput: {...}}))"
      pattern: "permissionDecision.*deny"
    - from: "hooks/qgsd-circuit-breaker.test.js"
      to: "hooks/qgsd-circuit-breaker.js"
      via: "spawnSync hook process with stdin payload"
      pattern: "CB-TC1[6-9]"
---

<objective>
Implement execution blocking in the circuit breaker PreToolUse hook and wire in config-driven thresholds, using TDD.

Purpose: Phase 6 delivered detection-only (always exits 0). Phase 7 flips the enforcement switch: when state.active is true and the incoming command is not read-only, the hook must return a deny decision that Claude Code enforces. Additionally, the hardcoded OSCILLATION_DEPTH and COMMIT_WINDOW constants must be replaced with values from loadConfig(gitRoot) so users can tune thresholds per-project. This plan delivers structural enforcement — Claude cannot execute write Bash commands when oscillation is detected.

Output: Updated hooks/qgsd-circuit-breaker.js with blocking logic and config integration; updated and extended hooks/qgsd-circuit-breaker.test.js with CB-TC7 fix and CB-TC16 through CB-TC19.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-enforcement-config-integration/07-RESEARCH.md
@hooks/qgsd-circuit-breaker.js
@hooks/qgsd-circuit-breaker.test.js
@hooks/config-loader.js
@.planning/phases/07-enforcement-config-integration/07-01-SUMMARY.md
</context>

<feature>
  <name>Circuit breaker enforcement + config-driven thresholds</name>
  <files>hooks/qgsd-circuit-breaker.js, hooks/qgsd-circuit-breaker.test.js</files>
  <behavior>
    ENFORCEMENT (ENFC-01/02/03):
    When state.active===true AND command is NOT read-only:
      Output to stdout: JSON.stringify({ hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: buildBlockReason(state) } })
      Then exit 0.
    When state.active===true AND command IS read-only:
      Exit 0 with empty stdout (read-only passes even when blocked).

    buildBlockReason(state) must include ALL of:
    1. Oscillating file set: state.file_set listed by name
    2. "CIRCUIT BREAKER ACTIVE" — explicit confirmation
    3. Allowed operations: git log, git diff, grep, cat, ls, head, tail, find
    4. Instruction: Claude must perform root cause analysis and map dependencies before resuming
    5. User commit instruction: "You must commit the fix manually — Claude cannot run git commit while the breaker is active. Run 'npx qgsd --reset-breaker' to clear the breaker after committing."

    CONFIG INTEGRATION (CONF-06/07/08/09 consumed):
    Remove module-level constants: OSCILLATION_DEPTH and COMMIT_WINDOW
    In main(), after getting gitRoot and before detection:
      const config = loadConfig(gitRoot);
      const oscillationDepth = config.circuit_breaker.oscillation_depth;
      const commitWindow = config.circuit_breaker.commit_window;
    Use oscillationDepth and commitWindow in place of removed constants.
    loadConfig is already require()'d as unused in Phase 6 — just activate the call.
    If config-loader is NOT yet imported in the hook: add require('./config-loader').

    UPDATED CONTROL FLOW (per RESEARCH.md Pattern 3):
      stdin -> parse JSON
      -> extract cwd, command
      -> getGitRoot(cwd) -> null? exit 0 (DETECT-05)
      -> statePath = path.join(gitRoot, '.claude', 'circuit-breaker-state.json')
      -> state = readState(statePath)
      -> if state && state.active:
           if !isReadOnly(command): BLOCK (emit hookSpecificOutput deny) then exit 0
           else: exit 0 (read-only allowed even when blocked)
      -> if isReadOnly(command): exit 0 (DETECT-04)
      -> config = loadConfig(gitRoot)
      -> hashes = getCommitHashes(gitRoot, config.circuit_breaker.commit_window)
      -> fileSets = getCommitFileSets(gitRoot, hashes)
      -> result = detectOscillation(fileSets, config.circuit_breaker.oscillation_depth)
      -> if result.detected: writeState(statePath, result.fileSet, fileSets)
      -> exit 0

    TEST CASES:
    CB-TC7 (UPDATED): active state + write command ('echo test')
      - stdout must be non-empty
      - parsed JSON must have hookSpecificOutput.permissionDecision === 'deny'
      - permissionDecisionReason must include 'CIRCUIT BREAKER'
      - permissionDecisionReason must include allowed operations
      - permissionDecisionReason must include 'manually commit' or 'manually'
      (Remove old assertion: stdout === '')

    CB-TC16 (NEW): active state + read-only command ('git log --oneline -5')
      - exit 0, stdout empty — read-only passes even when breaker is active

    CB-TC17 (NEW): active state + write command — verify block reason content
      - permissionDecisionReason includes the file names from state.file_set
      - permissionDecisionReason includes 'root cause'
      - permissionDecisionReason includes 'git log' (allowed ops)
      - permissionDecisionReason includes 'npx qgsd --reset-breaker'

    CB-TC18 (NEW): config oscillation_depth integration
      - Create temp git repo
      - Write .claude/qgsd.json with circuit_breaker.oscillation_depth:2
      - Create exactly 2 commits touching same file set (below default depth=3, meets depth=2)
      - Run hook with write command
      - State file written (oscillation detected at depth 2)
      - This verifies hook reads oscillation_depth from config, not hardcoded constant

    CB-TC19 (NEW): config commit_window integration
      - Create temp git repo
      - Write .claude/qgsd.json with circuit_breaker.commit_window:3
      - Create 4 commits: commits 1-3 touch file-A.txt, commit 4 touches file-B.txt (different file)
      - With default commit_window=6: commits 1-4 all in window, file-A.txt set appears 3x → would detect
      - With commit_window=3: only last 3 commits in window; commit 1 (file-A.txt) is excluded
        → file-A.txt set appears only 2x in window → oscillation NOT detected (depth=3)
      - Run hook with write command
      - State file must NOT be written (verifies commit_window=3 used, not default 6)
  </behavior>
  <implementation>
    RED phase:
    1. Update CB-TC7 in hooks/qgsd-circuit-breaker.test.js:
       Remove assertion: assert.strictEqual(stdout, '', ...)
       Add assertions for hookSpecificOutput deny structure and reason content
    2. Add CB-TC16 through CB-TC19 following existing test patterns:
       - Use writeProjectConfig() helper (write .claude/qgsd.json in temp repo): create .claude/ dir, write JSON
       - For CB-TC18/19: helper creates commits then writes config (avoid git add capturing config file)
       - Pipe JSON stdin payload via spawnSync input option (existing pattern)
    3. Run npm test — CB-TC7 fails (hook still emits empty stdout), CB-TC16 through CB-TC19 fail
    4. Commit: test(07-02): update CB-TC7 and add CB-TC16-19 for enforcement and config integration

    GREEN phase:
    1. Add buildBlockReason(state) function to hooks/qgsd-circuit-breaker.js:
       - Returns a multi-line string meeting ENFC-02/03 content requirements
       - Includes all 5 required content elements (file set, CIRCUIT BREAKER confirmation, allowed ops, root cause instruction, user commit instruction with 'npx qgsd --reset-breaker')
    2. Update main() active-state branch:
       - Old: if (state && state.active) { process.exit(0); } // STATE-03: already active, skip detection
       - New: if (state && state.active) {
                if (!isReadOnly(command)) {
                  process.stdout.write(JSON.stringify({ hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: buildBlockReason(state) } }));
                }
                process.exit(0);
              }
    3. Add loadConfig import if not present: const { loadConfig } = require('./config-loader');
    4. Remove module-level constants OSCILLATION_DEPTH and COMMIT_WINDOW
    5. In detection section, replace with config-driven values:
       const config = loadConfig(gitRoot);
       const hashes = getCommitHashes(gitRoot, config.circuit_breaker.commit_window);
       const result = detectOscillation(fileSets, config.circuit_breaker.oscillation_depth);
    6. Update file comment: remove "Hardcoded defaults: oscillation_depth=3, commit_window=6" → "Config-driven defaults via loadConfig(gitRoot)"
    7. Run npm test — all tests pass including updated CB-TC7 and new CB-TC16-19
    8. Commit: feat(07-02): implement circuit breaker enforcement and config-driven thresholds

    REFACTOR phase (if needed):
    1. Ensure block reason is readable and complete
    2. Run npm test — all pass
    3. Commit: refactor(07-02): clean up buildBlockReason message structure

    CRITICAL CONSTRAINTS:
    - WRONG blocking format for PreToolUse: { "decision": "block" } silently allows the command
    - CORRECT format: { hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: '...' } }
    - Read-only check inside the active-state branch MUST come first — isReadOnly before emitting block
    - Call loadConfig(gitRoot) — NOT loadConfig() or loadConfig(process.cwd()) — gitRoot is the correct project dir
    - loadConfig(gitRoot) must be called AFTER getGitRoot() succeeds and AFTER the active-state branch (only run if detection is needed)
    - All 125 existing tests must still pass after changes
    - CB-TC18/19: write .claude/qgsd.json AFTER creating commits to avoid git add contaminating commit file sets
  </implementation>
</feature>

<verification>
Run: npm test
Expected: All tests pass including updated CB-TC7 and new CB-TC16 through CB-TC19. Total should be 130+ passing.

Run: echo '{"tool_name":"Bash","tool_input":{"command":"rm -rf /tmp/test"},"cwd":"/tmp"}' | node hooks/qgsd-circuit-breaker.js
Expected: Empty stdout, exit 0 (no state file in /tmp means no active state — detection runs but no oscillation).

Manual state test:
  mkdir -p /tmp/cb-test/.claude
  echo '{"active":true,"file_set":["src/foo.js"],"activated_at":"2026-01-01T00:00:00Z","commit_window_snapshot":[]}' > /tmp/cb-test/.claude/circuit-breaker-state.json
  cd /tmp/cb-test && git init && git commit --allow-empty -m "init"
  echo '{"tool_name":"Bash","tool_input":{"command":"echo hello"},"cwd":"/tmp/cb-test"}' | node /path/to/hooks/qgsd-circuit-breaker.js
Expected: stdout contains JSON with hookSpecificOutput.permissionDecision === 'deny' and permissionDecisionReason includes 'CIRCUIT BREAKER'.
</verification>

<success_criteria>
- npm test: all tests pass (125 existing + CB-TC7 updated + CB-TC16/17/18/19 new)
- hooks/qgsd-circuit-breaker.js emits hookSpecificOutput.permissionDecision:'deny' when state.active and command is not read-only
- hooks/qgsd-circuit-breaker.js exits 0 with empty stdout when state.active and command is read-only
- hooks/qgsd-circuit-breaker.js uses loadConfig(gitRoot).circuit_breaker.oscillation_depth and .commit_window (no hardcoded constants)
- OSCILLATION_DEPTH and COMMIT_WINDOW module-level constants removed from hook
- Block reason covers all 5 ENFC-02/03 content requirements: file set, CIRCUIT BREAKER confirmation, allowed ops, root cause instruction, user manual-commit + reset-breaker instruction
</success_criteria>

<output>
After completion, create `.planning/phases/07-enforcement-config-integration/07-02-SUMMARY.md`
</output>
