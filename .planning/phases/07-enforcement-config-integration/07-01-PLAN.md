---
phase: 07-enforcement-config-integration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - hooks/config-loader.js
  - hooks/config-loader.test.js
autonomous: true
requirements:
  - CONF-06
  - CONF-07
  - CONF-08
  - CONF-09

must_haves:
  truths:
    - "loadConfig() returns a config object with circuit_breaker.oscillation_depth=3 and circuit_breaker.commit_window=6 by default"
    - "Invalid circuit_breaker.oscillation_depth (non-positive-integer) falls back to 3 with a [qgsd] WARNING on stderr"
    - "Invalid circuit_breaker.commit_window (non-positive-integer) falls back to 6 with a [qgsd] WARNING on stderr"
    - "A project qgsd.json with circuit_breaker.oscillation_depth=5 overrides the global value via shallow merge"
    - "A null or non-object circuit_breaker value falls back to full defaults with a stderr warning"
    - "A circuit_breaker with only oscillation_depth set leaves commit_window validated and defaulted independently"
    - "loadConfig() never writes to stdout when circuit_breaker is invalid"
  artifacts:
    - path: "hooks/config-loader.js"
      provides: "Extended DEFAULT_CONFIG with circuit_breaker sub-object + validateConfig() circuit_breaker branch"
      contains: "circuit_breaker"
    - path: "hooks/config-loader.test.js"
      provides: "7 new TDD test cases TC-CB1 through TC-CB7"
      min_lines: 200
  key_links:
    - from: "hooks/config-loader.js"
      to: "DEFAULT_CONFIG.circuit_breaker"
      via: "exported DEFAULT_CONFIG"
      pattern: "circuit_breaker.*oscillation_depth.*3"
    - from: "hooks/config-loader.js"
      to: "validateConfig()"
      via: "validateConfig branch for circuit_breaker"
      pattern: "circuit_breaker.*commit_window"
    - from: "hooks/config-loader.test.js"
      to: "hooks/config-loader.js"
      via: "require('./config-loader')"
      pattern: "TC-CB[1-7]"
---

<objective>
Extend the shared config loader with a circuit_breaker configuration sub-object, using TDD.

Purpose: The circuit breaker hook (Phase 6) uses hardcoded OSCILLATION_DEPTH=3 and COMMIT_WINDOW=6 constants. Phase 7 makes these user-configurable via qgsd.json. Before the hook can read these values, config-loader.js must export them and validate them correctly. This plan delivers the config foundation that Plan 07-02 builds on.

Output: Extended hooks/config-loader.js with circuit_breaker in DEFAULT_CONFIG and validateConfig(); 7 new passing test cases in hooks/config-loader.test.js.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-enforcement-config-integration/07-RESEARCH.md
@hooks/config-loader.js
@hooks/config-loader.test.js
@.planning/phases/06-circuit-breaker-detection-and-state/06-01-SUMMARY.md
</context>

<feature>
  <name>circuit_breaker config key in config-loader</name>
  <files>hooks/config-loader.js, hooks/config-loader.test.js</files>
  <behavior>
    DEFAULT_CONFIG gains a circuit_breaker sub-object: { oscillation_depth: 3, commit_window: 6 }

    validateConfig() gains a new branch after the existing fail_mode check:
    - If circuit_breaker is missing or not an object → replace with DEFAULT_CONFIG.circuit_breaker, warn stderr
    - If circuit_breaker.oscillation_depth is not a positive integer → replace with 3, warn stderr
    - If circuit_breaker.commit_window is not a positive integer → replace with 6, warn stderr
    - Each sub-key validated independently (missing one sub-key does not invalidate the other)

    loadConfig() behavior unchanged — shallow merge carries circuit_breaker through automatically.
    CONF-09: project circuit_breaker fully replaces global circuit_breaker (consistent with existing required_models behavior).

    Test cases:
    TC-CB1: DEFAULT_CONFIG.circuit_breaker exists with oscillation_depth=3, commit_window=6
    TC-CB2: Valid circuit_breaker in project config (oscillation_depth=5, commit_window=8) → values used as-is
    TC-CB3: circuit_breaker.oscillation_depth is string 'not-a-number' → falls back to 3, stderr WARNING
    TC-CB4: circuit_breaker.commit_window is -1 (negative integer) → falls back to 6, stderr WARNING
    TC-CB5: circuit_breaker is null → entire block falls back to defaults, stderr WARNING
    TC-CB6: circuit_breaker has only oscillation_depth=5 (missing commit_window) → oscillation_depth=5 used, commit_window=6 (default)
    TC-CB7: loadConfig() with invalid circuit_breaker writes nothing to stdout
  </behavior>
  <implementation>
    RED phase:
    1. Add TC-CB1 through TC-CB7 to hooks/config-loader.test.js following existing test structure (temp dirs, stderr capture via process.stderr, finally cleanup)
    2. Run npm test — TC-CB1 fails (DEFAULT_CONFIG has no circuit_breaker), TC-CB2 through TC-CB7 fail
    3. Commit: test(07-01): add failing TC-CB1 through TC-CB7 for circuit_breaker config key

    GREEN phase:
    1. Add circuit_breaker to DEFAULT_CONFIG in hooks/config-loader.js:
       circuit_breaker: { oscillation_depth: 3, commit_window: 6 }
    2. Add circuit_breaker validation branch to validateConfig():
       - Check if circuit_breaker is an object (not null); if not, replace with {...DEFAULT_CONFIG.circuit_breaker} and warn
       - Check oscillation_depth: Number.isInteger(v) && v >= 1; if not, replace with 3 and warn
       - Check commit_window: Number.isInteger(v) && v >= 1; if not, replace with 6 and warn
       - Validate each sub-key independently so missing one sub-key gets its own default
    3. Run npm test — all tests pass including TC-CB1 through TC-CB7
    4. Commit: feat(07-01): add circuit_breaker to DEFAULT_CONFIG and validateConfig

    REFACTOR phase (if needed):
    1. Ensure warning strings follow [qgsd] WARNING: prefix convention exactly
    2. Run npm test — all tests still pass
    3. Commit: refactor(07-01): clean up circuit_breaker validation wording

    Critical constraints:
    - Use process.stderr.write() — NEVER console.log() or console.warn() in hook context
    - Warning format: '[qgsd] WARNING: qgsd.json: circuit_breaker...'
    - Do NOT introduce deep merge — shallow merge is the existing design decision (STATE.md phase 02)
    - TC-CB6 validates that a project config with only oscillation_depth set loses commit_window from global (shallow merge replaces entire circuit_breaker object) but validateConfig() then fills in the missing sub-key independently
    - All existing tests must continue to pass (125 currently passing)
  </implementation>
</feature>

<verification>
Run: npm test
Expected: All tests pass including new TC-CB1 through TC-CB7.
Run: node -e "const {loadConfig, DEFAULT_CONFIG} = require('./hooks/config-loader'); console.log(JSON.stringify(DEFAULT_CONFIG.circuit_breaker))"
Expected: {"oscillation_depth":3,"commit_window":6}
</verification>

<success_criteria>
- npm test passes with all existing + 7 new TC-CB tests
- DEFAULT_CONFIG.circuit_breaker exported from hooks/config-loader.js with correct defaults
- validateConfig() handles null, non-object, and invalid sub-key values with correct fallback + stderr warning
- loadConfig() returns circuit_breaker values when called — hook in Plan 07-02 can safely access config.circuit_breaker.oscillation_depth and config.circuit_breaker.commit_window
- No stdout output during any invalid-config path (TC-CB7)
</success_criteria>

<output>
After completion, create `.planning/phases/07-enforcement-config-integration/07-01-SUMMARY.md`
</output>
