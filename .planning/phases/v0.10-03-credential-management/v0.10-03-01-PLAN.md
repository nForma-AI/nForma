---
phase: v0.10-03-credential-management
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/manage-agents.test.cjs
autonomous: true
requirements:
  - CRED-01
  - CRED-02

must_haves:
  truths:
    - "Failing test stubs exist for classifyProbeResult covering all 6 input cases"
    - "Failing test stubs exist for writeKeyStatus covering the roundtrip to qgsd.json"
    - "Running node --test bin/manage-agents.test.cjs shows RED on the new stubs (expected failure)"
    - "All pre-existing tests remain GREEN"
  artifacts:
    - path: "bin/manage-agents.test.cjs"
      provides: "Failing test stubs for classifyProbeResult and writeKeyStatus"
      contains: "classifyProbeResult"
  key_links:
    - from: "bin/manage-agents.test.cjs"
      to: "bin/manage-agents.cjs"
      via: "require('./manage-agents.cjs')._pure"
      pattern: "_pure\\.classifyProbeResult|_pure\\.writeKeyStatus"
---

<objective>
Wave 0 test scaffolding: add failing stubs for classifyProbeResult and writeKeyStatus before any implementation begins.

Purpose: Nyquist validation requires tests to exist in RED state before implementation. These stubs define the exact contract for the two new functions, making the implementation task in Plan 02 purely a matter of turning RED to GREEN.
Output: bin/manage-agents.test.cjs with 8 new test cases in RED state.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.10-03-credential-management/v0.10-03-RESEARCH.md
@.planning/phases/v0.10-03-credential-management/v0.10-03-VALIDATION.md
@bin/manage-agents.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing stubs for classifyProbeResult to manage-agents.test.cjs</name>
  <files>bin/manage-agents.test.cjs</files>
  <action>
Append a new test section to bin/manage-agents.test.cjs immediately after the buildCloneEntry tests (around line 510). Import classifyProbeResult from _pure at the top of the file alongside the other destructured imports.

Add these 6 test cases under a `// classifyProbeResult` section header:

1. `classifyProbeResult: null probeResult -> 'unreachable'`
   - Input: null
   - Expected: 'unreachable'

2. `classifyProbeResult: healthy=false -> 'unreachable'`
   - Input: `{ healthy: false, latencyMs: 0, statusCode: null, error: 'ECONNREFUSED' }`
   - Expected: 'unreachable'

3. `classifyProbeResult: healthy=true, statusCode=401 -> 'invalid'`
   - Input: `{ healthy: true, latencyMs: 120, statusCode: 401, error: null }`
   - Expected: 'invalid'

4. `classifyProbeResult: healthy=true, statusCode=200 -> 'ok'`
   - Input: `{ healthy: true, latencyMs: 80, statusCode: 200, error: null }`
   - Expected: 'ok'

5. `classifyProbeResult: healthy=true, statusCode=403 -> 'ok'`
   - Input: `{ healthy: true, latencyMs: 95, statusCode: 403, error: null }`
   - Expected: 'ok'

6. `classifyProbeResult: healthy=true, statusCode=422 -> 'ok'`
   - Input: `{ healthy: true, latencyMs: 102, statusCode: 422, error: null }`
   - Expected: 'ok'

These stubs MUST fail at this stage because classifyProbeResult does not yet exist in _pure. The destructured import will throw at require time — use a try/catch guard or add classifyProbeResult to the destructure only after confirming the test runner handles missing exports gracefully.

IMPORTANT: The simplest approach is to add `classifyProbeResult` to the destructure line at the top alongside the existing imports. When the tests run in Wave 0, the require will succeed (module loads) but each test will fail with `TypeError: classifyProbeResult is not a function` or similar — which is the expected RED state.
  </action>
  <verify>node --test bin/manage-agents.test.cjs 2>&1 | grep -E "classifyProbeResult|FAIL|fail" | head -20</verify>
  <done>6 new classifyProbeResult tests appear in output, all FAIL. All pre-existing tests pass (no regressions). Commit: `test(v0.10-03-01): add failing stubs for classifyProbeResult`</done>
</task>

<task type="auto">
  <name>Task 2: Add failing stubs for writeKeyStatus to manage-agents.test.cjs</name>
  <files>bin/manage-agents.test.cjs</files>
  <action>
Append a `// writeKeyStatus` test section immediately after the classifyProbeResult stubs. Add writeKeyStatus to the destructured import at the top of the file.

Add these 2 test cases:

1. `writeKeyStatus: writes {status: 'invalid', checkedAt: ISO} to qgsd.json agent_config[slotName].key_status`
   - Setup: create a tmp file path via `os.tmpdir() + '/qgsd_ws_test_' + Date.now() + '.json'`
   - Call: `writeKeyStatus('claude-1', 'invalid', tmpPath)` — note: writeKeyStatus must accept an optional filePath parameter for testability, matching the readQgsdJson/writeQgsdJson pattern from v0.10-01
   - Assert: `readQgsdJson(tmpPath).agent_config['claude-1'].key_status.status === 'invalid'`
   - Assert: `typeof readQgsdJson(tmpPath).agent_config['claude-1'].key_status.checkedAt === 'string'`
   - Assert: `new Date(readQgsdJson(tmpPath).agent_config['claude-1'].key_status.checkedAt)` is a valid date (not NaN)
   - Cleanup: fs.unlinkSync in finally block

2. `writeKeyStatus: writes {status: 'ok', checkedAt: ISO} to qgsd.json agent_config[slotName].key_status`
   - Same structure as above but status = 'ok' and slotName = 'gemini-1'

These stubs MUST fail at this stage because writeKeyStatus does not yet exist in _pure.

Note on writeKeyStatus design: it will be an impure function (has file I/O side effects). It should be exported via _pure despite being impure — the reason is testability via the optional filePath parameter. This matches the readQgsdJson/writeQgsdJson precedent in the codebase (both accept optional filePath and are exported via _pure for testing). Document this design decision in the test comment.
  </action>
  <verify>node --test bin/manage-agents.test.cjs 2>&1 | grep -E "writeKeyStatus|FAIL|fail" | head -20</verify>
  <done>2 new writeKeyStatus tests appear in output, all FAIL. All pre-existing tests (including classifyProbeResult stubs) still fail as expected. No regressions in prior tests. Commit: `test(v0.10-03-01): add failing stubs for writeKeyStatus`</done>
</task>

</tasks>

<verification>
Run: `node --test bin/manage-agents.test.cjs 2>&1 | tail -20`

Expected output:
- All pre-existing tests: PASS
- classifyProbeResult tests (6): FAIL (function not yet implemented)
- writeKeyStatus tests (2): FAIL (function not yet implemented)

The RED state is correct and required. Do NOT attempt to make stubs pass.
</verification>

<success_criteria>
- bin/manage-agents.test.cjs has 8 new test cases total (6 for classifyProbeResult + 2 for writeKeyStatus)
- All 8 new tests are in RED state (failing as expected)
- All pre-existing tests remain GREEN (no regressions)
- Stubs are committed before any implementation begins
</success_criteria>

<output>
After completion, create `.planning/phases/v0.10-03-credential-management/v0.10-03-01-SUMMARY.md`
</output>
