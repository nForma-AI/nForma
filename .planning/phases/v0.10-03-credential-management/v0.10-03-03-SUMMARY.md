---
phase: v0.10-03-credential-management
plan: "03"
status: complete-pending-human-verify
commit: 1b5efca
date: 2026-02-24
---

# Plan v0.10-03-03 Summary — Integration Wiring

## What Was Built

Three integration changes to `bin/manage-agents.cjs` wiring `classifyProbeResult`/`writeKeyStatus` into the live execution paths and adding the "Batch rotate keys" menu option.

## Tasks Completed

### Task 1: Update checkAgentHealth() to persist key_status
- After `probeProviderUrl()` call (line ~1109), added:
  ```js
  const classification = classifyProbeResult(probe);
  if (classification !== 'unreachable') {
    writeKeyStatus(slotName, classification);
  }
  ```
- Guard: `classification !== 'unreachable'` ensures provider DOWN does not overwrite prior key_status
- All existing console output (statusLine, agent/URL/model display) preserved unchanged

### Task 2: Add batchRotateKeys() async function
- Placed after `checkAgentHealth()`, before subprocess provider helpers section
- Uses `inquirer checkbox` for multi-slot selection (existing v8.2.7 CJS pattern from editAgent())
- Sequential `for...of` loop — never `Promise.all` (keychain concurrency invariant)
- Per-slot `console.log` confirmation printed before next slot prompt
- Single `writeClaudeJson(data)` call AFTER the full loop — never inside loop body
- `writeKeyStatus()` intentionally absent from batchRotateKeys() — key_status only written during health probes
- Early return guard: if `selectedSlots.length === 0`, skips writeClaudeJson entirely

### Task 3: Wire "Batch rotate keys" into mainMenu()
- "11. Batch rotate keys" inserted before "Update coding agents" (renumbered to 12)
- Handler: `else if (action === 'batch-rotate') await batchRotateKeys();`
- Placement: after ccr-keys separator, before update-agents

## Test Results

- 74/74 tests pass after all three tasks
- 0 regressions

## Invariants Verified

- `Promise.all` does NOT appear in `batchRotateKeys()` body
- `writeClaudeJson(data)` appears exactly once (after the loop), not inside loop body
- `writeKeyStatus()` does NOT appear in `batchRotateKeys()` — only in `checkAgentHealth()`
- `classification !== 'unreachable'` guard is the sole condition for calling `writeKeyStatus()`

## Human Verification Required (checkpoint:human-verify — blocking)

The following must be verified manually:

1. `node bin/manage-agents.cjs` → select "11. Batch rotate keys"
2. Verify: checkbox picker shows all configured slots with [key set]/[no key] status
3. Select 1-2 slots, press Enter
4. Verify: password prompt appears for first selected slot
5. Enter a test key, press Enter
6. Verify: confirmation line "claude-1: key updated" appears BEFORE next slot's prompt
7. Complete all slots; verify "✓ N slot(s) updated" appears
8. Select "7. Check agent health", pick a slot, complete probe
9. After probe: `cat ~/.claude/qgsd.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(JSON.stringify(d.agent_config, null, 2));"`
10. Verify: `agent_config[slotName].key_status = { status: 'ok'|'invalid', checkedAt: ISO }`
11. Restart CLI; select "1. List agents"
12. Verify: `[key invalid]` badge persists for any slot whose last probe was 401

**Resume signal:** Type "approved" in chat or describe issues found.
