---
phase: v0.10-03-credential-management
plan: "01"
status: complete
commit: 64f0b8d
date: 2026-02-24
---

# Plan v0.10-03-01 Summary — Failing Test Stubs

## What Was Built

Added 8 failing test stubs to `bin/manage-agents.test.cjs` covering `classifyProbeResult` and `writeKeyStatus`, satisfying the Wave 0 Nyquist requirement (RED state before implementation).

## Tasks Completed

### Task 1: classifyProbeResult stubs (6 tests)
- `null probeResult -> 'unreachable'`
- `healthy=false -> 'unreachable'`
- `healthy=true, statusCode=401 -> 'invalid'`
- `healthy=true, statusCode=200 -> 'ok'`
- `healthy=true, statusCode=403 -> 'ok'`
- `healthy=true, statusCode=422 -> 'ok'`

### Task 2: writeKeyStatus stubs (2 tests)
- Writes `{status: 'invalid', checkedAt: ISO}` to `qgsd.json` at `agent_config['claude-1'].key_status`
- Writes `{status: 'ok', checkedAt: ISO}` to `qgsd.json` at `agent_config['gemini-1'].key_status`
- Both use tmp file path via optional `filePath` parameter (testability pattern from v0.10-01)

## Test Results

- Pre-existing tests: 66 PASS (no regressions)
- New stubs: 8 FAIL (expected — `TypeError: classifyProbeResult is not a function`)
- Total: 74 tests, 8 failing as required by Wave 0

## Decisions

- Added `classifyProbeResult` and `writeKeyStatus` to the destructure import line alongside existing `_pure` exports
- `writeKeyStatus` exported via `_pure` despite being impure (has file I/O) — same pattern as `readQgsdJson`/`writeQgsdJson`; testability via optional `filePath` parameter justifies the export
