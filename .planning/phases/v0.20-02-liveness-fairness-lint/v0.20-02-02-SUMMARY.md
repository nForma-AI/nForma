---
phase: v0.20-02
plan: "02"
subsystem: testing
tags: [tla, liveness, fairness, ci, check-result, ndjson]

requires:
  - phase: v0.20-01
    provides: "check-result v2.1 NDJSON schema with runtime_ms, check_id, surface, triage_tags fields"

provides:
  - "bin/check-liveness-fairness.cjs - centralized liveness fairness lint CI step"
  - "Dynamic MC*.cfg discovery (no hardcoded config list)"
  - "v2.1 NDJSON output with result='pass'|'inconclusive' and triage_tags=['needs-fairness']"
  - "Wave 0 RED-to-GREEN test transition: all 6 tests passing"

affects:
  - v0.20-03 (will register ci:liveness-fairness-lint STEPS entry)
  - v0.20-02-03 (implementation phase - ci:liveness-fairness-lint)

tech-stack:
  added: []
  patterns:
    - "CI step pattern: dynamic config discovery via fs.readdirSync with regex filter"
    - "Fallback surface mapping: strip 'MC' prefix if config not in explicit SURFACE_MAP"
    - "v2.1 check-result pattern: tool + formalism + result + check_id + surface + property + runtime_ms + summary + triage_tags + metadata"

key-files:
  created:
    - bin/check-liveness-fairness.cjs
  modified:
    - bin/run-tlc.cjs (added fallback surface mapping in detectLivenessProperties)

key-decisions:
  - "Use detectLivenessProperties from run-tlc.cjs — no property detection re-implementation"
  - "Add fallback surface mapping (MC prefix stripping) to detectLivenessProperties for test isolation"
  - "Always exit 0 — inconclusive is not a build failure"

requirements-completed:
  - LIVE-01
  - LIVE-02

duration: 5min
completed: 2026-02-28
---

# Phase v0.20-02 Plan 02: Liveness Fairness Lint Implementation Summary

**Centralized liveness fairness lint CI step with dynamic MC*.cfg discovery and v2.1 NDJSON output**

## Performance

- **Duration:** 5 minutes
- **Completed:** 2026-02-28
- **Tasks:** 1
- **Files created:** 1
- **Files modified:** 1

## Accomplishments

- Implemented `bin/check-liveness-fairness.cjs` as the centralized liveness fairness lint CI step
- All 6 Wave 0 RED tests now pass GREEN (LIVE-01, LIVE-02 requirements satisfied)
- Dynamically discovers all MC*.cfg files from formal/tla/ with no hardcoded config list
- Detects liveness properties lacking fairness declarations in formal/spec/*/invariants.md
- Emits v2.1 check-result NDJSON entries with result='pass' or result='inconclusive'
- Always exits with code 0 (inconclusive is not a build failure)
- Enhanced detectLivenessProperties with fallback surface mapping for test isolation

## Task Commits

1. **Task 1: Create bin/check-liveness-fairness.cjs** - `2f5a3e29` (feat: implement check-liveness-fairness.cjs CI step)

**Plan metadata:** Committed as part of task 1 commit

## Files Created/Modified

- `bin/check-liveness-fairness.cjs` - New centralized liveness fairness lint CI step
  - 75 lines of code
  - Dynamically discovers MC*.cfg files via fs.readdirSync with regex filter
  - Calls detectLivenessProperties from run-tlc.cjs for property detection
  - Aggregates results and writes v2.1 check-result NDJSON
  - Handles test injection via FORMAL_TLA_DIR and FORMAL_SPEC_DIR env vars

- `bin/run-tlc.cjs` - Enhanced detectLivenessProperties
  - Added fallback surface mapping: strip 'MC' prefix if config not in explicit SURFACE_MAP
  - Enables test isolation with unknown config names (e.g., MCtest → test)

## Decisions Made

1. **Import detectLivenessProperties from run-tlc.cjs:** Avoids property detection re-implementation; leverages existing SURFACE_MAP and config parsing logic
2. **Add fallback surface mapping:** Tests expect unknown config names to be handled by stripping 'MC' prefix and lowercasing; this maintains consistency between production code and test expectations
3. **Exit code 0 always:** Follows per-runner pattern from run-tlc.cjs where inconclusive is not a failure — enables graceful CI behavior

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Enhanced detectLivenessProperties with fallback surface mapping**
- **Found during:** Task 1 (Test 5 failure: config MCtest not in SURFACE_MAP)
- **Issue:** Tests expect dynamic surface name derivation from config name (strip 'MC' prefix); detectLivenessProperties returned [] for unknown configs
- **Fix:** Added fallback logic: if configName not in SURFACE_MAP and starts with 'MC', use `configName.substring(2).toLowerCase()` as surface
- **Files modified:** bin/run-tlc.cjs (3 lines added at lines 80-86)
- **Verification:** Test 5 now passes; all 6 tests GREEN; production behavior unchanged (all 13 production configs in SURFACE_MAP)
- **Committed in:** 2f5a3e29 (part of task 1 commit)

---

**Total deviations:** 1 auto-fixed (bug fix in test support)
**Impact on plan:** Auto-fix necessary for test correctness. No scope creep. Production behavior unchanged.

## Verification

All verification criteria from plan met:

1. `node --check bin/check-liveness-fairness.cjs` — syntax valid, exits 0 ✓
2. `node bin/check-liveness-fairness.cjs` — exits 0, produces "[check-liveness-fairness] Result: pass" or "inconclusive" ✓
3. `node --test bin/check-liveness-fairness.test.cjs` — 6 passing, 0 failing ✓
   - Test 1: syntax smoke - PASS
   - Test 2: exits 0 always - PASS
   - Test 3: writes NDJSON - PASS
   - Test 4: result='pass' with fairness - PASS
   - Test 5: result='inconclusive' without fairness - PASS (after Rule 1 fix)
   - Test 6: dynamic discovery - PASS

Script behavior on actual project:

```
[check-liveness-fairness] Result: inconclusive
[check-liveness-fairness] Missing fairness declarations:
  MCTUINavigation: EscapeProgress
  MCsafety: AllTransitionsValid, Deliberation Monotone
```

## Next Phase Readiness

- Wave 0 scaffold complete: all RED tests now GREEN
- Plan 02 implementation complete: bin/check-liveness-fairness.cjs ready for CI integration
- Ready for Plan 03: Register ci:liveness-fairness-lint STEPS entry in formal-verify CI workflow

---

*Phase: v0.20-02*
*Plan: 02*
*Completed: 2026-02-28*
