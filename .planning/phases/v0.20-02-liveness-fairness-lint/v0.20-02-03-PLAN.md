---
phase: v0.20-02
plan: "03"
type: execute
wave: 2
depends_on:
  - v0.20-02-02
files_modified:
  - bin/run-formal-verify.cjs
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "run-formal-verify.cjs STEPS array contains an entry with id='ci:liveness-fairness-lint', tool='ci', script='check-liveness-fairness.cjs'"
    - "Running node bin/run-formal-verify.cjs --only=ci executes check-liveness-fairness.cjs and shows it in the summary table"
    - "The RED test added to run-formal-verify.test.cjs in Plan 01 now passes GREEN"
    - "Existing 25 STEPS entries are unchanged (count becomes 26 after addition)"
  artifacts:
    - path: "bin/run-formal-verify.cjs"
      provides: "Updated STEPS with ci:liveness-fairness-lint as the 3rd ci:* entry"
      contains: "ci:liveness-fairness-lint"
  key_links:
    - from: "bin/run-formal-verify.cjs STEPS[ci:liveness-fairness-lint]"
      to: "bin/check-liveness-fairness.cjs"
      via: "type:'node', script:'check-liveness-fairness.cjs' — same pattern as ci:trace-redaction"
      pattern: "script.*check-liveness-fairness\\.cjs"
    - from: "bin/run-formal-verify.test.cjs LIVE-02 test"
      to: "bin/run-formal-verify.cjs STEPS"
      via: "fs.readFileSync + match on ci:liveness-fairness-lint string"
      pattern: "ci:liveness-fairness-lint"
---

<objective>
Wire `ci:liveness-fairness-lint` into the `STEPS` array of `bin/run-formal-verify.cjs`, making the step visible in the formal verification pipeline run summary and satisfying LIVE-02.

Purpose: LIVE-02 requires that `run-formal-verify.cjs` STEPS includes a `ci:liveness-fairness-lint` entry. The script created in Plan 02 does the work; this plan makes it an official, named CI step that appears in the run summary and is callable via `--only=ci`. This also turns the RED test added to `run-formal-verify.test.cjs` in Plan 01 GREEN.

Output: Modified `bin/run-formal-verify.cjs` with one new STEPS entry; STEPS count goes from 25 to 26.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-RESEARCH.md
@bin/run-formal-verify.cjs
@bin/run-formal-verify.test.cjs
@.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ci:liveness-fairness-lint to STEPS in run-formal-verify.cjs</name>
  <files>bin/run-formal-verify.cjs</files>
  <action>
Open `bin/run-formal-verify.cjs` and locate the `// ─ CI enforcement ──` section near the end of the STEPS array (currently contains `ci:trace-redaction` and `ci:trace-schema-drift`).

Add a new STEPS entry AFTER `ci:trace-schema-drift` (append as the last CI entry, before the closing `];` of the STEPS array):

```javascript
  {
    tool: 'ci', id: 'ci:liveness-fairness-lint',
    label: 'Liveness-fairness lint — detect liveness properties without fairness declarations (LIVE-01, LIVE-02)',
    type: 'node', script: 'check-liveness-fairness.cjs', args: [],
  },
```

Also update the file header comment block at the top that shows the coverage summary:
- Change the TLA+ line count from `(8)` to `(9)` (adding the new liveness property coverage)
- Change the PRISM line count from `(2)` to `(3)` (adding the new liveness model entry)
- Change the CI enforce line from `//   CI enforce (2)  — check-trace-redaction.cjs, check-trace-schema-drift.cjs` to:
  `//   CI enforce (3)  — check-trace-redaction.cjs, check-trace-schema-drift.cjs, check-liveness-fairness.cjs`
- Change `//   Total:    25 steps` to `//   Total:    26 steps`

Do NOT change any other STEPS entries. Do NOT change the STEPS array order for existing entries.

After editing, verify syntax: `node --check bin/run-formal-verify.cjs`
  </action>
  <verify>
1. `node --check bin/run-formal-verify.cjs` — exits 0, no SyntaxError
2. `grep "ci:liveness-fairness-lint" bin/run-formal-verify.cjs` — prints the new STEPS entry
3. `grep "check-liveness-fairness.cjs" bin/run-formal-verify.cjs` — prints the script reference
4. `grep "Total:" bin/run-formal-verify.cjs` — prints "Total:    26 steps"
5. `grep -c "tool:" bin/run-formal-verify.cjs` — prints 26 (one 'tool:' per STEPS entry)
6. `node --test bin/run-formal-verify.test.cjs` — the LIVE-02 test now PASSES; all other tests still pass
7. `node bin/run-formal-verify.cjs --only=ci 2>&1 | grep "liveness-fairness"` — output includes liveness-fairness-lint step running and exiting
  </verify>
  <done>
`bin/run-formal-verify.cjs` STEPS array contains `ci:liveness-fairness-lint` as the 26th entry. The LIVE-02 RED test from Plan 01 now passes GREEN. Running `--only=ci` executes check-liveness-fairness.cjs and shows it in the step summary. Header comment updated to reflect 26 steps, TLA+ (9), PRISM (3), and CI enforce (3).
  </done>
</task>

</tasks>

<verification>
1. `node --check bin/run-formal-verify.cjs` — exits 0
2. `grep -c "tool:" bin/run-formal-verify.cjs` — prints 26
3. `node --test bin/run-formal-verify.test.cjs` — all tests pass (including the LIVE-02 test now GREEN)
4. `node --test bin/check-liveness-fairness.test.cjs` — all 6 tests pass (still GREEN from Plan 02)
5. `npm test` — full test suite green
6. `node bin/run-formal-verify.cjs --only=ci 2>&1 | grep "liveness-fairness-lint"` — step executes and appears in output
7. Phase success criteria from ROADMAP:
   - LIVE-01: `node bin/check-liveness-fairness.cjs` exits 0 and writes result=inconclusive for any config missing fairness
   - LIVE-02: `grep "ci:liveness-fairness-lint" bin/run-formal-verify.cjs` confirms step is registered
</verification>

<success_criteria>
- STEPS array has 26 entries (was 25)
- `ci:liveness-fairness-lint` entry has: tool='ci', id='ci:liveness-fairness-lint', type='node', script='check-liveness-fairness.cjs'
- Header comment updated to show 26 steps, TLA+ (9), PRISM (3), CI enforce (3)
- `npm test` fully green
- LIVE-01 satisfied: check-liveness-fairness.cjs emits inconclusive for missing fairness
- LIVE-02 satisfied: ci:liveness-fairness-lint visible in STEPS, runs via --only=ci
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-03-SUMMARY.md`
</output>
