---
phase: v0.20-02
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/check-liveness-fairness.test.cjs
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "bin/check-liveness-fairness.test.cjs exists with RED tests that FAIL against a non-existent script"
    - "Tests specify the contract: exit 0 always, result=pass when all fairness declared, result=inconclusive when any missing"
    - "Tests cover dynamic MC*.cfg discovery (no hardcoded list)"
    - "run-formal-verify.test.cjs gains a RED test asserting ci:liveness-fairness-lint is in STEPS"
  artifacts:
    - path: "bin/check-liveness-fairness.test.cjs"
      provides: "Wave 0 RED test scaffold — 6 failing tests specifying check-liveness-fairness.cjs contract"
      min_lines: 80
  key_links:
    - from: "bin/check-liveness-fairness.test.cjs"
      to: "bin/check-liveness-fairness.cjs"
      via: "require() of script that does not yet exist — tests must fail in RED state"
      pattern: "check-liveness-fairness\\.cjs"
    - from: "bin/run-formal-verify.test.cjs"
      to: "bin/run-formal-verify.cjs STEPS array"
      via: "assertion that ci:liveness-fairness-lint exists in STEPS"
      pattern: "ci:liveness-fairness-lint"
---

<objective>
Wave 0 Nyquist scaffold for Phase v0.20-02: create RED test files that specify the contract for `bin/check-liveness-fairness.cjs` before any implementation exists.

Purpose: Per VALIDATION.md mandate, tests must exist and fail BEFORE implementation. This proves the tests are meaningful (not vacuously passing against a stub). Wave 0 must complete before Plan 02 (implementation) starts.

Output: `bin/check-liveness-fairness.test.cjs` (6 RED tests) and one new RED test added to `bin/run-formal-verify.test.cjs` asserting the STEPS entry exists.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-RESEARCH.md
@.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-VALIDATION.md
@bin/run-tlc.cjs
@bin/run-formal-verify.cjs
@bin/run-formal-verify.test.cjs
@bin/write-check-result.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 RED test scaffold for check-liveness-fairness.cjs</name>
  <files>bin/check-liveness-fairness.test.cjs</files>
  <action>
Create `bin/check-liveness-fairness.test.cjs` as a NEW file. Use the same pattern as `bin/run-tlc.test.cjs` and `bin/write-check-result.test.cjs` for test structure (Node.js built-in test runner: `const { test } = require('node:test')`).

The script under test is `bin/check-liveness-fairness.cjs` which does NOT exist yet. All tests that require() it will fail — that is the correct RED state.

Add the following 6 tests:

**Test 1: script loads without syntax error (will fail — file missing)**
```javascript
test('syntax smoke: script loads without SyntaxError', () => {
  const result = spawnSync(process.execPath, ['--check', SCRIPT], { encoding: 'utf8', timeout: 5000 });
  assert.doesNotMatch(result.stderr || '', /SyntaxError/);
  assert.strictEqual(result.status, 0);
});
```
Where `SCRIPT = path.join(__dirname, 'check-liveness-fairness.cjs')`.

**Test 2: exits with code 0 always (inconclusive is not a failure)**
Spawn `check-liveness-fairness.cjs` and assert `result.status === 0`. Script does not exist yet → this will fail because spawnSync returns non-zero for ENOENT.

**Test 3: writes a check result entry to NDJSON**
Use a tmpDir pattern. Set `CHECK_RESULTS_PATH` env var pointing to a temp file. Spawn the script with that env var. Assert the output file has at least one line that parses as valid JSON with `tool` field.

**Test 4: result is 'pass' when all configs have fairness declarations**
Inject a `FORMAL_TLA_DIR` env var pointing to a tmpDir containing a fake `MCtest.cfg` with `PROPERTY EventualConsensus`. Also inject `FORMAL_SPEC_DIR` pointing to a tmpDir with `formal/spec/test/invariants.md` containing `## EventualConsensus`. Assert written NDJSON has `result === 'pass'`.

**Test 5: result is 'inconclusive' when any config lacks fairness declaration**
Same setup as Test 4 but `invariants.md` does NOT contain `## EventualConsensus`. Assert written NDJSON has `result === 'inconclusive'` and `triage_tags` includes `'needs-fairness'`.

**Test 6: dynamically discovers MC*.cfg files (rejects hardcoded list)**
Inject `FORMAL_TLA_DIR` pointing to a tmpDir containing TWO fake configs: `MCtest1.cfg` (PROPERTY LivA) and `MCtest2.cfg` (no PROPERTY lines). Inject matching `FORMAL_SPEC_DIR` with no invariants.md for either. Assert the NDJSON `metadata.configs_checked` count equals 2 (both discovered, not just one).

After writing all 6 tests, run them: `node --test bin/check-liveness-fairness.test.cjs`. ALL 6 must FAIL (RED state confirmed). Capture and show the output showing failures.

Note: Tests 4–6 require env var injection into a child process spawn. Use the `env` option of spawnSync: `spawnSync(process.execPath, [SCRIPT], { env: { ...process.env, CHECK_RESULTS_PATH: tmpFile, FORMAL_TLA_DIR: tmpDir }, encoding: 'utf8' })`.
  </action>
  <verify>
Run: `node --test bin/check-liveness-fairness.test.cjs`
Expected: All 6 tests FAIL (the implementation script does not exist). Output shows 6 failing, 0 passing.
Also verify the file was created: `ls -la bin/check-liveness-fairness.test.cjs`
  </verify>
  <done>
`bin/check-liveness-fairness.test.cjs` exists with 6 tests. All 6 fail because `bin/check-liveness-fairness.cjs` does not exist. RED state confirmed — Wave 0 scaffold is meaningful.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add RED test for ci:liveness-fairness-lint STEPS entry to run-formal-verify.test.cjs</name>
  <files>bin/run-formal-verify.test.cjs</files>
  <action>
Append ONE new test to `bin/run-formal-verify.test.cjs` AFTER the existing tests (do not modify any existing tests). The test checks that the STEPS array in `bin/run-formal-verify.cjs` contains the new `ci:liveness-fairness-lint` entry.

Read `run-formal-verify.cjs` source content in the test and check for the STEPS entry:

```javascript
test('LIVE-02: STEPS includes ci:liveness-fairness-lint entry', () => {
  const src = fs.readFileSync(path.join(__dirname, 'run-formal-verify.cjs'), 'utf8');
  // Verify the entry exists with correct tool='ci' and script='check-liveness-fairness.cjs'
  assert.match(src, /ci:liveness-fairness-lint/);
  assert.match(src, /check-liveness-fairness\.cjs/);
  assert.match(src, /tool:\s*['"]ci['"]/);
});
```

After adding this test, run the full suite: `npm test`. The new test must FAIL (ci:liveness-fairness-lint not yet in STEPS). All pre-existing tests must still pass.

Confirm RED state for the new test before committing.
  </action>
  <verify>
Run: `node --test bin/run-formal-verify.test.cjs`
Expected: New test "LIVE-02: STEPS includes ci:liveness-fairness-lint entry" FAILS. All pre-existing tests pass. Output shows exactly 1 new failure.
  </verify>
  <done>
`bin/run-formal-verify.test.cjs` has one new test that asserts `ci:liveness-fairness-lint` is in STEPS. Test fails in RED state. All pre-existing tests pass (no regressions introduced by the addition).
  </done>
</task>

</tasks>

<verification>
1. `ls bin/check-liveness-fairness.test.cjs` — file exists
2. `node --test bin/check-liveness-fairness.test.cjs` — 6 failures (RED), 0 passes
3. `node --test bin/run-formal-verify.test.cjs` — exactly 1 new failure (LIVE-02 test), all pre-existing pass
4. `npm test` — pre-existing test suite still green; 2 new failures visible (the scaffold tests)
</verification>

<success_criteria>
- `bin/check-liveness-fairness.test.cjs` exists with exactly 6 tests, all RED
- `bin/run-formal-verify.test.cjs` has one new RED test asserting ci:liveness-fairness-lint in STEPS
- Zero pre-existing tests broken
- Wave 0 contract established: implementation in Plan 02 must satisfy tests 1–6; STEPS wire-up in Plan 03 must satisfy the run-formal-verify test
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-02-liveness-fairness-lint/v0.20-02-01-SUMMARY.md`
</output>
