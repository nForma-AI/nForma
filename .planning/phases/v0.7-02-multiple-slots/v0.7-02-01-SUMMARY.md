---
plan: v0.7-02-01
phase: v0.7-02-multiple-slots
status: complete
requirements: [MULTI-01, MULTI-03]
completed: 2026-02-23
---

# Plan 01: addSlotToQuorumActive helper + install.js incremental quorum_active update

## What Was Built

Added `addSlotToQuorumActive(slotName, qgsdJsonPath, dryRun)` to `bin/migrate-to-slots.cjs` as an idempotent helper that appends a single slot to `quorum_active` in `qgsd.json` without overwriting existing entries. Wired it into `bin/install.js` reinstall path (MULTI-03 block after COMP-04 backfill) so that when a user manually adds a slot to `~/.claude.json` and reruns `npx qgsd@latest`, the new slot is automatically appended to `quorum_active`.

## Tasks Completed

| Task | Status | Notes |
|------|--------|-------|
| Task 1: addSlotToQuorumActive() in migrate-to-slots.cjs | ✓ Complete | Function + export; idempotent; dryRun support |
| Task 2: Wire into install.js reinstall path | ✓ Complete | MULTI-03 block inserts after COMP-04 backfill |
| Task 3: MS-TC-ADD-1..5 unit tests | ✓ Complete | 5/5 pass; package.json test script updated |
| Task 4: Commit | ✓ Complete | `feat(v0.7-02-01): addSlotToQuorumActive helper + install.js incremental quorum_active update` |

## Key Files

- **bin/migrate-to-slots.cjs**: +35 lines — addSlotToQuorumActive function + updated module.exports
- **bin/install.js**: +14 lines — MULTI-03 block in reinstall path
- **bin/migrate-to-slots.test.cjs**: new file, 95 lines — MS-TC-ADD-1..5
- **package.json**: test script extended to include migrate-to-slots.test.cjs

## Verification

- `typeof addSlotToQuorumActive` → `function`
- `grep -c "addSlotToQuorumActive" bin/migrate-to-slots.cjs` → 2 (definition + export)
- `grep -c "addSlotToQuorumActive" bin/install.js` → 2 (require + call)
- MS-TC-ADD-1..5: 5/5 pass
- npm test: 250/253 pass (3 pre-existing failures, 0 new regressions)

## Self-Check: PASSED
