---
phase: v0.10-06
requirements: [PORT-01, PORT-02, PORT-03]
status: auto_verified (human_verify pending)
---

# v0.10-06 VERIFICATION — Import/Export

## Requirements

| Req | Description | Status |
|-----|-------------|--------|
| PORT-01 | Export roster with unconditional API key redaction | auto_verified |
| PORT-02 | Import with schema validation + pre-import backup | auto_verified |
| PORT-03 | Re-prompt for __redacted__ key values on import | auto_verified |

## Automated Checks

### PORT-01 (Export)

| Check | Verification | Result |
|-------|--------------|--------|
| `buildExportData` redacts `_KEY` suffix | test: `buildExportData: redacts env values in mcpServers` | PASS |
| `buildRedactedEnv` covers `_KEY`, `_SECRET`, `_TOKEN`, `_PASSWORD` | 4 unit tests | PASS |
| `buildExportData` preserves non-env fields | test: `buildExportData: preserves non-env fields` | PASS |
| `performExport` does NOT call `syncToClaudeJson` | grep confirms only comments reference syncToClaudeJson | PASS |
| Menu item 16 exists | `grep "16. Export roster" bin/manage-agents.cjs` → line 2120 | PASS |

### PORT-02 (Import + Backup)

| Check | Verification | Result |
|-------|--------------|--------|
| `buildBackupPath` format | 2 unit tests | PASS |
| `validateImportSchema` rejects invalid command | test: `validateImportSchema: invalid command returns error` | PASS |
| `validateImportSchema` rejects absolute home path | test: `validateImportSchema: absolute home path in args returns error` | PASS |
| `validateImportSchema` collects all errors | test: `validateImportSchema: multiple errors all collected before return` | PASS |
| `validateImportSchema` valid schema → empty array | test: `validateImportSchema: valid minimal schema returns no errors` | PASS |
| Menu item 17 exists | `grep "17. Import roster" bin/manage-agents.cjs` → line 2121 | PASS |

### PORT-03 (Redacted Key Re-prompt)

| Check | Verification | Result |
|-------|--------------|--------|
| `__redacted__` in env is not a validation error | test: `validateImportSchema: __redacted__ value in env is not a validation error` | PASS |
| `performImport` re-prompt logic coded as sequential for...of | code review — lines use `for...of` not `Promise.all` | PASS |

## Test Run

```
node --test bin/manage-agents.test.cjs
ℹ tests 140
ℹ pass 140
ℹ fail 0
```

## Implementing Commits

| Plan | Commit | Description |
|------|--------|-------------|
| 01 | `9ea3c4d` | 15 RED test stubs |
| 02 | `946e7d3` | 4 pure functions GREEN |
| 03 | `5d3ee91` | Integration functions + mainMenu wiring |

## Human Verification (Pending)

The following require interactive TTY verification (checkpoint:human-verify):

1. **PORT-01 Export**: Select menu item 16 → enter output path → open file → confirm all `*_KEY`, `*_SECRET`, `*_TOKEN`, `*_PASSWORD` env values are `"__redacted__"`; no real key strings appear
2. **PORT-02 Import validation**: Import file with `command: "python"` → confirm error printed, no backup created, no changes written
3. **PORT-02 Import backup**: Import valid file → confirm `~/.claude.json.pre-import.<timestamp>` created (colons replaced with dashes)
4. **PORT-03 Re-prompt**: Import file with `__redacted__` values → confirm per-slot password prompt appears; blank entry skips without crash
