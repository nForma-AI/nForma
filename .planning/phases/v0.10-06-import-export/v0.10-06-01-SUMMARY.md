---
phase: v0.10-06
plan: "01"
status: complete
completed: 2026-02-25
commit: 9ea3c4d
tests_before: 125 pass / 0 fail
tests_after: 125 pass / 15 fail
---

# v0.10-06-01 SUMMARY — Wave 0 RED Stubs

## What Was Done

Added 15 failing test stubs to `bin/manage-agents.test.cjs` for the 4 import/export pure functions:

- **buildBackupPath** (2 stubs): path + timestamp suffix construction
- **buildRedactedEnv** (4 stubs): _KEY/_SECRET redaction + non-sensitive preservation + empty env
- **buildExportData** (3 stubs): redacts env in mcpServers + preserves non-env fields + _TOKEN redaction
- **validateImportSchema** (6 stubs): non-object root, invalid command, absolute home path, valid schema, multiple errors, `__redacted__` not an error

## Test State

- 125 prior tests: all pass (no regression)
- 15 new stubs: all fail with `TypeError: <fn> is not a function` (functions not yet in `_pure`)

## Commit

`9ea3c4d` — test(v0.10-06-01): add 15 RED stubs for buildBackupPath/buildRedactedEnv/buildExportData/validateImportSchema
