---
phase: v0.10-06
slug: import-export
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-25
---

# Phase v0.10-06 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.10-06`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.10-06`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` (built-in Node.js test runner) |
| **Config file** | none — no config file needed |
| **Quick run command** | `node --test bin/manage-agents.test.cjs` |
| **Full suite command** | `node --test bin/manage-agents.test.cjs bin/migrate-to-slots.test.cjs bin/review-mcp-logs.test.cjs bin/update-scoreboard.test.cjs get-shit-done/bin/gsd-tools.test.cjs hooks/config-loader.test.js hooks/qgsd-circuit-breaker.test.js hooks/qgsd-prompt.test.js hooks/qgsd-statusline.test.js hooks/qgsd-stop.test.js` |
| **Estimated runtime** | ~3 seconds (manage-agents alone) |
| **CI pipeline** | npm test (excludes manage-agents.test.cjs — run standalone) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/manage-agents.test.cjs`
- **After every plan wave:** Run full suite command above
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** ~3 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 06-01-01 | 01 | 0 | PORT-01, PORT-02, PORT-03 | unit stubs (RED) | `node --test bin/manage-agents.test.cjs 2>&1 \| grep -E "fail\|FAIL"` | ❌ W0 | ⬜ pending |
| 06-02-01 | 02 | 1 | PORT-01 | unit (GREEN) | `node --test bin/manage-agents.test.cjs 2>&1 \| tail -5` | ❌ W0 | ⬜ pending |
| 06-02-02 | 02 | 1 | PORT-02 | unit (GREEN) | `node --test bin/manage-agents.test.cjs 2>&1 \| tail -5` | ❌ W0 | ⬜ pending |
| 06-02-03 | 02 | 1 | PORT-03 | unit (GREEN) | `node --test bin/manage-agents.test.cjs 2>&1 \| tail -5` | ❌ W0 | ⬜ pending |
| 06-03-01 | 03 | 2 | PORT-01 | integration + manual | `node --test bin/manage-agents.test.cjs && node bin/manage-agents.cjs` | ✅ N/A | ⬜ pending |
| 06-03-02 | 03 | 2 | PORT-02, PORT-03 | integration + manual | `node --test bin/manage-agents.test.cjs && node bin/manage-agents.cjs` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/manage-agents.test.cjs` — extend with ~15 failing stubs:
  - `buildBackupPath` (2 stubs): happy path + timestamp shape
  - `buildRedactedEnv` (4 stubs): `_KEY` suffix redacted, `_SECRET` suffix redacted, non-sensitive key preserved, empty env returns `{}`
  - `buildExportData` (3 stubs): env redacted in output, non-env fields preserved, `__redacted__` placeholder present
  - `validateImportSchema` (6 stubs): non-object root → error, invalid command → error, absolute home path in args → error, valid minimal schema → no errors, multiple errors collected → all reported, `__redacted__` value in env → no validation error

Stubs MUST fail because functions are not yet in `_pure`. Existing 125 tests continue to pass.

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| "Export roster" menu option writes file with `__redacted__` values | PORT-01 | File system + stdout inspection; no keytar in test env | Run `node bin/manage-agents.cjs`, choose 16, verify output file has no real API keys |
| "Import roster" menu option re-prompts for redacted keys | PORT-02 | TTY `password` prompt interaction | Run `node bin/manage-agents.cjs`, choose 17, import a file with `__redacted__` values, verify per-slot prompts appear |
| Backup file created at `~/.claude.json.pre-import.<timestamp>` | PORT-03 | File system side effect — requires real `~/.claude.json` | After import, verify backup file exists with correct timestamp suffix |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 3s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.10-06`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 3 | `node --test bin/manage-agents.test.cjs` | — | — | ⬜ pending |
| 2 | 2 | `node --test bin/manage-agents.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
