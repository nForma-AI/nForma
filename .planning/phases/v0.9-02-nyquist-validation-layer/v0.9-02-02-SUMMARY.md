---
phase: v0.9-02
plan: 02
status: complete
completed: 2026-02-24
requirements: [NYQ-04]
---

# Plan 02 Summary: gsd-tools.cjs Config Wiring + nyquist_validation Config Fields

## What Was Done

**Task 1 — Added nyquist_validation to gsd-tools.cjs (4 locations)**

Edited `/Users/jonathanborduas/code/QGSD/get-shit-done/bin/gsd-tools.cjs` at 4 targeted locations:

1. **`loadConfig()` defaults object (line 187):** Added `nyquist_validation: true` — QGSD defaults to `true` (stronger than upstream GSD which defaults `false`)
2. **`loadConfig()` return map (line 222):** Added `nyquist_validation: get('nyquist_validation', { section: 'workflow', field: 'nyquist_validation' }) ?? defaults.nyquist_validation`
3. **`cmdInitPlanPhase()` Workflow flags block (line 4408):** Added `nyquist_validation_enabled: config.nyquist_validation` to the result object
4. **`cmdConfigEnsureSection()` hardcoded workflow block (line 668):** Added `nyquist_validation: true` to the hardcoded defaults

File passed `node -c` syntax check.

**Task 2 — Updated config.json files**

- `/Users/jonathanborduas/code/QGSD/get-shit-done/templates/config.json`: Added `"nyquist_validation": false` to `workflow` section — new projects opt in explicitly
- `/Users/jonathanborduas/code/QGSD/.planning/config.json`: Added `"nyquist_validation": true` to `workflow` section — QGSD repo itself has Nyquist enabled (satisfies SC-4)
- Both files pass `JSON.parse` validation

**Task 3 — Install sync**

Ran `node /Users/jonathanborduas/code/QGSD/bin/install.js --claude --global` — successfully propagated:
- `~/.claude/qgsd/bin/gsd-tools.cjs` (4 nyquist locations)
- `~/.claude/qgsd/templates/config.json` (nyquist_validation: false)

## Verification Results

| Check | Result |
|-------|--------|
| `node -c gsd-tools.cjs` | OK |
| nyquist_validation in all 4 locations | 4 lines (pass) |
| `templates/config.json` has `"nyquist_validation": false` | pass |
| `.planning/config.json` has `"nyquist_validation": true` | pass |
| Both configs pass JSON.parse | pass |
| Installed gsd-tools.cjs has 4 nyquist locations | 4 (pass) |
| `init plan-phase "v0.9-02"` outputs `"nyquist_validation_enabled": true` | pass |

## Requirements Covered

- **NYQ-04**: `gsd-tools.cjs init plan-phase` JSON output now includes `nyquist_validation_enabled: true` (boolean, defaults true per QGSD convention)

## End-to-End Confirmation

```
$ node ~/.claude/qgsd/bin/gsd-tools.cjs init plan-phase "v0.9-02" 2>/dev/null | grep nyquist
  "nyquist_validation_enabled": true,
```
