---
phase: v0.12-05
plan: "03"
status: complete
commit: 33a5cbb
date: 2026-02-25
requirements:
  - GAP-2
  - GAP-6
---

# v0.12-05-03 SUMMARY — GREEN Tests + TLC Runner

## What Was Built

- `bin/run-protocol-tlc.cjs` — TLC runner for protocol termination specs:
  - VALID_CONFIGS: ['MCdeliberation', 'MCprefilter']
  - Default config: MCdeliberation
  - Spec dispatch: MCdeliberation → QGSDDeliberation.tla, MCprefilter → QGSDPreFilter.tla
  - workers='1' unconditionally (both configs have liveness properties)
  - Java detection, version check (>=17), JAR check, all gated before TLC invocation
  - Zero external npm dependencies (child_process, fs, path only)

## Test Results

All 5 tests in `bin/run-protocol-tlc.test.cjs` GREEN:
1. JAVA_HOME invalid → exit 1 + stderr JAVA_HOME message ✓
2. JAR missing (skip if JAR present or no Java) ✓
3. Unknown config → exit 1 + descriptive message ✓
4. Invalid config → exit 1 + MCdeliberation|MCprefilter in stderr ✓
5. workers-always-1 assertion → reads source, finds `'1'`, no `'auto'` ✓

Full npm test: pass 298, fail 2 (pre-existing failures in run-alloy and run-tlc JAR-missing tests when tla2tools.jar is downloaded locally — unrelated to v0.12-05).

## Decisions

- workers='1' unconditional (no conditional like run-oscillation-tlc.cjs) because both protocol
  specs (QGSDDeliberation and QGSDPreFilter) have PROPERTY (liveness) declarations
- Pre-existing test failures in run-alloy.test.cjs and run-tlc.test.cjs confirmed before
  v0.12-05 changes — caused by tla2tools.jar being present on disk (gitignored, downloaded locally)
