---
phase: v0.35-01
slug: install-hooks-dist-rebuild
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-12
---

# Phase v0.35-01 — Validation Strategy

> Template created by `/nf:plan-phase v0.35-01` (step 5.5) after research.
> Populated by `/nf:plan-phase v0.35-01` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/nf:execute-phase v0.35-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in) |
| **Config file** | none — node:test discovers test/*.test.cjs by convention |
| **Quick run command** | `node --test --test-bail test/install-fresh-clone.test.cjs` |
| **Full suite command** | `npm run test:install` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | npm test (existing) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test --test-bail test/install-fresh-clone.test.cjs`
- **After every plan wave:** Run `npm run test:install`
- **Before `/nf:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.35-01-01-01 | 01 | 1 | INST-01 | integration | `grep -n 'buildHooksIfMissing' bin/install.js && npm run test:install` | ✅ | ⬜ pending |
| v0.35-01-01-02 | 01 | 1 | INST-02 | unit | `node --test test/install-fresh-clone.test.cjs` | ❌ W0 | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

Test file `test/install-fresh-clone.test.cjs` is created as part of Task 2 (same wave as implementation).

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `nf-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s ✅
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-12

---

## Execution Tracking

Updated during `/nf:execute-phase v0.35-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | `npm run test:install` | TBD | TBD | ⬜ pending |

**Phase validation complete:** {pending / YYYY-MM-DD HH:MM}
