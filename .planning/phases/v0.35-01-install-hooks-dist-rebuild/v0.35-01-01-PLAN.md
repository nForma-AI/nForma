---
phase: v0.35-01-install-hooks-dist-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - test/install-fresh-clone.test.cjs
autonomous: true
requirements:
  - INST-01
  - INST-02
formal_artifacts: none

must_haves:
  truths:
    - "Running `node bin/install.js --claude --global` on a fresh clone (no hooks/dist/) succeeds without error"
    - "If hooks/dist/ is missing or stale, install either auto-rebuilds it or prints a clear actionable message telling the user exactly what command to run"
    - "After install completes, all hooks in ~/.claude/hooks/ are functional (not empty or stale copies)"
  artifacts:
    - path: "bin/install.js"
      provides: "Auto-rebuild logic for hooks/dist/ when missing"
      contains: "buildHooksIfMissing"
    - path: "test/install-fresh-clone.test.cjs"
      provides: "Test proving fresh clone install works"
      min_lines: 40
  key_links:
    - from: "bin/install.js"
      to: "scripts/build-hooks.js"
      via: "HOOKS_TO_COPY manifest shared or referenced"
      pattern: "HOOKS_TO_COPY|build-hooks"
    - from: "bin/install.js"
      to: "hooks/dist/"
      via: "auto-rebuild creates dist/ before copy step"
      pattern: "buildHooksIfMissing|mkdirSync.*dist"
---

<objective>
Add auto-rebuild of hooks/dist/ to the installer so fresh source checkouts work without manual `npm run build:hooks`.

Purpose: GitHub issue #4 -- users cloning from source get a broken install because hooks/dist/ is gitignored and only built by prepublishOnly. The installer must detect missing hooks/dist/ and either rebuild inline or print an actionable error.

Output: Modified bin/install.js with buildHooksIfMissing() function, plus test proving fresh-clone install works.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.35-01-install-hooks-dist-rebuild/v0.35-01-RESEARCH.md

Key source files:
@bin/install.js (lines 2004-2031: current hooks copy logic)
@scripts/build-hooks.js (HOOKS_TO_COPY manifest, lines 13-34)
@test/install-virgin.test.cjs (existing test patterns, setup/teardown)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildHooksIfMissing() to installer and integrate before hooks copy</name>
  <files>bin/install.js</files>
  <action>
Add a `buildHooksIfMissing(hooksDir, distDir)` function to bin/install.js that:

1. Checks if `hooks/dist/` directory exists via `fs.existsSync(distDir)`.
2. If it exists, returns `{ rebuilt: false, success: true }` immediately (preserves idempotency per OverridesPreserved invariant).
3. If missing, runs `scripts/build-hooks.js` as a child process using `require('child_process').execFileSync` with `process.execPath` and the path to `scripts/build-hooks.js`. This reuses the exact same build logic with zero code duplication.
4. After running build-hooks.js, verify hooks/dist/ now exists. If yes: `{ rebuilt: true, success: true }`. If no: `{ rebuilt: false, success: false, error: 'build-hooks.js failed' }`.
5. On failure, print to stderr an actionable message including the exact command `npm run build:hooks` and the retry command `node bin/install.js --claude --global`. Then push 'hooks' to the failures array.

Integration point: Insert the buildHooksIfMissing() call at line ~2004, BEFORE the existing `const hooksSrc = path.join(src, 'hooks', 'dist');` block. The flow becomes:
- Call buildHooksIfMissing(hooksDir, distDir) -- NEW: auto-rebuild if missing
- const hooksSrc = path.join(src, 'hooks', 'dist'); -- existing line
- if (fs.existsSync(hooksSrc)) { ... } -- existing copy logic unchanged

The `hooksDir` is `path.join(src, 'hooks')` and `distDir` is `path.join(src, 'hooks', 'dist')` where `src` is the source directory (already resolved earlier in installRuntime).

IMPORTANT: The existing hooks copy block (lines 2004-2031) must remain UNCHANGED. The new function only ensures hooks/dist/ exists before the copy step runs. This preserves the existing templating logic (`.claude` path replacement) and verification.

Do NOT add console.log on success (silent rebuild per fail-open pattern). Only log errors to stderr.

Idempotency: Running install twice must produce identical results. First run: rebuild + copy. Second run: dist/ exists, skip rebuild, copy (same files).
  </action>
  <verify>
1. `grep -n 'buildHooksIfMissing' bin/install.js` -- function defined and called
2. `grep -n 'npm run build:hooks' bin/install.js` -- actionable error message present
3. Remove hooks/dist/, run `node bin/install.js --claude --global --config-dir /tmp/nf-test-rebuild`, verify it succeeds and hooks/dist/ is recreated
4. Existing test suite passes: `npm run test:install`
  </verify>
  <done>
bin/install.js contains buildHooksIfMissing() that auto-rebuilds hooks/dist/ by calling scripts/build-hooks.js when dist/ is missing. Existing hooks copy logic unchanged. Install succeeds on fresh clone without manual build step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fresh-clone install test proving hooks/dist auto-rebuild works</name>
  <files>test/install-fresh-clone.test.cjs</files>
  <action>
Create `test/install-fresh-clone.test.cjs` following the patterns in `test/install-virgin.test.cjs`:

1. Use `node:test` (describe/test/before/after), `node:assert/strict`, `node:fs`, `node:path`, `node:os`, `child_process.execFileSync`.
2. In `before()`:
   - Create temp dir for config output via `fs.mkdtempSync`
   - Save current state: check if `hooks/dist/` exists, back up knowledge of its presence
   - Remove `hooks/dist/` directory entirely with `fs.rmSync(distDir, { recursive: true, force: true })`
   - Run install via `execFileSync(process.execPath, [INSTALL_SCRIPT, '--claude', '--global', '--config-dir', tmpDir])`
3. In `after()`:
   - Clean up temp dir
   - Restore hooks/dist/ by running `node scripts/build-hooks.js` to ensure repo state is clean for other tests
   - Use try/finally to guarantee cleanup even on failure

Test cases (5 total):
- **'install succeeds when hooks/dist/ is initially missing'**: Assert the install command did not throw (completed without error).
- **'hooks/dist/ is rebuilt after install'**: Assert `fs.existsSync(path.join(__dirname, '..', 'hooks', 'dist'))` is true.
- **'installed hooks directory contains required hooks'**: Assert the temp config dir has `hooks/` with nf-stop.js, nf-prompt.js, nf-circuit-breaker.js present (same assertions as install-virgin.test.cjs lines 119-127).
- **'re-install is idempotent'**: Run install again into a second temp dir, compare file lists -- same hooks present.
- **'hooks/dist/ contains files matching HOOKS_TO_COPY count'**: Read scripts/build-hooks.js content, extract HOOKS_TO_COPY array length via regex, verify hooks/dist/ has at least that many .js files.

Important: This test modifies repo state (removes hooks/dist/) so it MUST restore it in after(). Use try/finally in after() to ensure cleanup even on failure.

The test file should be auto-discovered by the existing `npm test` and `npm run test:install` commands (node:test discovers test/*.test.cjs by convention).
  </action>
  <verify>
1. `node --test test/install-fresh-clone.test.cjs` -- all 5 tests pass
2. `ls hooks/dist/` -- directory restored after test run
3. `npm run test:install` -- existing install tests still pass (no interference)
  </verify>
  <done>
test/install-fresh-clone.test.cjs exists with 5 test cases proving: fresh clone install works, hooks/dist is auto-rebuilt, required hooks are installed, re-install is idempotent, and hook count matches manifest. All tests pass. hooks/dist/ restored after test.
  </done>
</task>

</tasks>

<verification>
1. Fresh clone simulation: Remove hooks/dist/, run install, verify success
2. Idempotency: Run install twice, verify identical output
3. Error path: Temporarily rename a source hook file, run install with missing dist/, verify actionable error message appears
4. Existing tests: `npm run test:install` passes without regression
5. Formal invariant: OverridesPreserved -- re-running install produces same state (verified by idempotency test)
</verification>

<success_criteria>
- `node bin/install.js --claude --global` succeeds on a fresh source clone (no hooks/dist/)
- If auto-rebuild fails, installer prints actionable error message with `npm run build:hooks` command
- All hooks in ~/.claude/hooks/ are functional after install (nf-stop.js, nf-prompt.js, nf-circuit-breaker.js present)
- `node --test test/install-fresh-clone.test.cjs` passes all 5 test cases
- `npm run test:install` passes (no regression)
- InstallIdempotent property preserved (second install produces identical state)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.35-01-install-hooks-dist-rebuild/v0.35-01-01-SUMMARY.md`
</output>
