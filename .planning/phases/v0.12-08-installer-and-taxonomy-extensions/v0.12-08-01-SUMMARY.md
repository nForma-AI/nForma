---
phase: v0.12-08
plan: 01
status: complete
commit: acbaa08
date: 2026-02-25
---

# Plan 01 Summary — Wave 0 RED Test Stubs

## What Was Built

- **bin/run-installer-alloy.test.cjs** — 4 error-path test stubs for the installer Alloy runner
- **package.json** — `scripts.test` updated to append `bin/run-installer-alloy.test.cjs`

## Test Results

- Test 1 (JAVA_HOME nonexistent): RED — `assert.match` fails; Node prints "Cannot find module" not "JAVA_HOME"
- Test 2 (JAR not found): SKIP — JAR is present in `formal/alloy/`, conditional skip fires correctly
- Test 3 (unknown --spec): RED — runner missing, exit 1 but stderr doesn't match `/Unknown spec|bogus/i`
- Test 4 (invalid spec lists install-scope, taxonomy-safety): RED — runner missing, stderr doesn't match

Result: 3 RED, 1 SKIP (expected) — Wave 0 scaffold confirmed.

## Verification

```
node --test bin/run-installer-alloy.test.cjs
# pass 1, fail 3 — tests RED because runner absent
node -e "const p=require('./package.json'); console.log(p.scripts.test.includes('run-installer-alloy.test.cjs'))"
# true
```

## Decisions

- Test 2 uses the same conditional-skip pattern as run-audit-alloy.test.cjs: skip if Java unavailable OR if JAR already present (can't test absent-JAR path when JAR exists)
- Both tasks in single plan per RESEARCH.md Pitfall 4: test file must exist before package.json update
- Test 4 checks for BOTH valid spec names (install-scope and taxonomy-safety) — two-spec runner
