---
phase: v0.12-08
plan: 02
status: complete
commit: 74ac2bd
date: 2026-02-25
---

# Plan 02 Summary — Alloy Specs (GAP-7 + GAP-8)

## What Was Built

- **formal/alloy/install-scope.als** — Extended with FileToken, InstallSnapshot sigs; RollbackSound + ConfigSyncComplete predicates; RollbackSoundCheck + ConfigSyncCompleteCheck assert+check pairs
- **formal/alloy/taxonomy-safety.als** — New file with module taxonomy_safety; TaskDescription, Category, Subcategory, Bool, KnownCategories, Classification, ClassifyFn sigs; NoInjection, TaxonomyClosed, NewCategoryConsistent assertions with check commands at scope 3

## Verification

```
grep "^check NoConflict\|^check AllEquivalence\|^check InstallIdempotent" formal/alloy/install-scope.als | wc -l
# 3 — original checks untouched

grep "^assert RollbackSoundCheck\|^assert ConfigSyncCompleteCheck" formal/alloy/install-scope.als | wc -l
# 2 — new assertions present

grep "^check RollbackSoundCheck\|^check ConfigSyncCompleteCheck" formal/alloy/install-scope.als | wc -l
# 2 — new check commands present

grep "module taxonomy_safety" formal/alloy/taxonomy-safety.als
# module taxonomy_safety

grep "^abstract sig\|^one sig\|^sig " formal/alloy/taxonomy-safety.als | wc -l
# 8 — all sigs present (TaskDescription, Category, Subcategory, Bool, True/False, KnownCategories, Classification, ClassifyFn)

grep "^assert " formal/alloy/taxonomy-safety.als | wc -l
# 3 — NoInjection, TaxonomyClosed, NewCategoryConsistent

grep "^check " formal/alloy/taxonomy-safety.als | wc -l
# 3 — check commands for all three assertions
```

## Decisions

- RollbackSound uses `no post.files` (sound overapproximation): FileToken represents only GSD-installed files, so empty post-snapshot correctly models "all GSD files removed"
- NoInjection framed as functional constraint (`one f.maps[t]`) per RESEARCH.md Open Question 1 resolution — meaningful and verifiable
- TaxonomyClosed/NewCategoryConsistent quantify over ALL KnownCategories (not SOME) per Pitfall 1 guidance
- install-scope.als extension appended after `run AllSelected` — existing three check commands untouched
