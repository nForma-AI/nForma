---
phase: v0.12-08
plan: 03
status: complete
commit: d17bec2
date: 2026-02-25
---

# Plan 03 Summary — Runner Implementation + GREEN Tests

## What Was Built

- **bin/run-installer-alloy.cjs** — Alloy 6 runner for install-scope.als and taxonomy-safety.als; mirrors run-audit-alloy.cjs with VALID_SPECS = ['install-scope', 'taxonomy-safety']

## Test Results

```
node --test bin/run-installer-alloy.test.cjs
# pass 4, fail 0 — all 4 tests GREEN
```

- Test 1 (JAVA_HOME nonexistent): GREEN — exit 1 + stderr matches /JAVA_HOME|java/i
- Test 2 (JAR not found): SKIP — JAR is present, conditional skip fires correctly
- Test 3 (unknown --spec): GREEN — exit 1 + stderr matches /Unknown spec|bogus/i
- Test 4 (invalid spec lists both valid specs): GREEN — exit 1 + stderr matches /install-scope|taxonomy-safety/i

Full suite (npm test): 309/312 pass — 3 pre-existing failures from other test files (run-audit-alloy JAR test, run-tlc JAR test, TC1 days test), none from the new installer tests.

## Verification

```
node bin/run-installer-alloy.cjs --spec=bogus 2>&1
# [run-installer-alloy] Unknown spec: bogus. Valid: install-scope, taxonomy-safety

JAVA_HOME=/nonexistent node bin/run-installer-alloy.cjs 2>&1
# [run-installer-alloy] JAVA_HOME is set but java binary not found at: /nonexistent/bin/java

grep "VALID_SPECS" bin/run-installer-alloy.cjs
# const VALID_SPECS = ['install-scope', 'taxonomy-safety'];

grep "Counterexample" bin/run-installer-alloy.cjs
# if (/Counterexample/i.test(stdout)) {
```

## Decisions

- Mirrored run-audit-alloy.cjs exactly — only VALID_SPECS, default spec, and binary name in error messages differ
- stdio: 'pipe' (not 'inherit') for stdout counterexample scan — RESEARCH.md State of Art requirement
- Pre-existing failures in npm test are from unrelated test files; not introduced by this plan
