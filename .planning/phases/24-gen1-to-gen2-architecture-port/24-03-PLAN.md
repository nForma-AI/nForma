---
phase: 24-gen1-to-gen2-architecture-port
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/registry.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/index.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/ask.tool.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/suggest.tool.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/explain.tool.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/utils/copilotExecutor.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/types.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/server.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/definitions.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/handlers.ts
autonomous: true
requirements:
  - STD-02

must_haves:
  truths:
    - "copilot-mcp-server src/tools/ has registry.ts, index.ts, simple-tools.ts, ask.tool.ts, suggest.tool.ts, explain.tool.ts — no definitions.ts or handlers.ts remains"
    - "npm run build succeeds in copilot-mcp-server with zero TypeScript errors"
    - "All existing tests pass after import path updates"
    - "Shared copilot helpers (buildCopilotArgs, classifyCommandError, extractResponse) live in src/utils/copilotExecutor.ts and are imported by all three domain tools"
  artifacts:
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/tools/registry.ts"
      provides: "UnifiedTool interface + toolRegistry + executeTool + getToolDefinitions + toolExists"
      contains: "export interface UnifiedTool"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/utils/copilotExecutor.ts"
      provides: "Shared helpers: buildCopilotArgs, validateAddDir, classifyCommandError, extractResponse, buildTimeBudgetPrefix"
      contains: "buildCopilotArgs"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/tools/ask.tool.ts"
      provides: "ask tool using copilot executor"
      contains: "export const askTool"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/server.ts"
      provides: "Gen2 dispatch via executeTool() + toolExists()"
      contains: "executeTool"
  key_links:
    - from: "/Users/jonathanborduas/code/copilot-mcp-server/src/tools/ask.tool.ts"
      to: "/Users/jonathanborduas/code/copilot-mcp-server/src/utils/copilotExecutor.ts"
      via: "import { buildCopilotArgs, extractResponse }"
      pattern: "copilotExecutor"
    - from: "/Users/jonathanborduas/code/copilot-mcp-server/src/server.ts"
      to: "/Users/jonathanborduas/code/copilot-mcp-server/src/tools/index.ts"
      via: "import { getToolDefinitions, executeTool, toolExists }"
      pattern: "executeTool"
---

<objective>
Port copilot-mcp-server from Gen1 (definitions.ts + handlers.ts) to Gen2 (per-tool *.tool.ts + registry.ts) architecture.

Purpose: copilot-mcp-server has 5 tools with shared copilot execution helpers that currently live in handlers.ts. Extracting shared helpers to src/utils/copilotExecutor.ts prevents code duplication across the three domain tools (ask, suggest, explain) while enabling the Gen2 per-tool pattern.
Output: src/tools/ with registry.ts, index.ts, simple-tools.ts, ask.tool.ts, suggest.tool.ts, explain.tool.ts; src/utils/copilotExecutor.ts; updated server.ts; deleted definitions.ts and handlers.ts; all tests passing; npm run build succeeds.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-gen1-to-gen2-architecture-port/24-RESEARCH.md

# Gen2 reference implementations (read before starting)
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/registry.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/index.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/simple-tools.ts

# Gen1 source to preserve (read before modifying)
@/Users/jonathanborduas/code/copilot-mcp-server/src/tools/definitions.ts
@/Users/jonathanborduas/code/copilot-mcp-server/src/tools/handlers.ts
@/Users/jonathanborduas/code/copilot-mcp-server/src/server.ts
@/Users/jonathanborduas/code/copilot-mcp-server/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gen2 tool files and shared executor helper for copilot-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/copilot-mcp-server/src/utils/copilotExecutor.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/registry.ts
    /Users/jonathorborduas/code/copilot-mcp-server/src/tools/ask.tool.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/suggest.tool.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/explain.tool.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/simple-tools.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/index.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/types.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/definitions.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/handlers.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/copilot-mcp-server

Read the Gen1 source files first (definitions.ts, handlers.ts, types.ts). copilot-mcp-server has no session storage but has shared helpers in handlers.ts: buildCopilotArgs, validateAddDir, classifyCommandError, extractResponse, buildTimeBudgetPrefix. These are used by AskToolHandler, SuggestToolHandler, and ExplainToolHandler.

**Step 1: Add ToolArguments to src/types.ts (Pitfall 2 fix)**
```typescript
export type ToolArguments = Record<string, unknown>;
```

**Step 2: Create src/utils/copilotExecutor.ts**
Move ALL shared copilot helpers from handlers.ts into this file. This prevents duplication across three tool files. Export each function:
- buildCopilotArgs(...)
- validateAddDir(...)
- classifyCommandError(...)
- extractResponse(...)
- buildTimeBudgetPrefix(...)
Preserve exact function signatures and implementations from Gen1 handlers.ts.

**Step 3: Create src/tools/registry.ts**
Copy from gemini-mcp-server/src/tools/registry.ts. Adapt category union: `'simple' | 'copilot' | 'utility'`.

**Step 4: Create src/tools/ask.tool.ts**
Extract AskToolHandler logic. Points:
- Import helpers from '../utils/copilotExecutor.js'
- zodSchema: match Gen1 definitions.ts inputSchema for ask tool
- execute() returns Promise<string> — adapt return value from ToolResult to string
- Inside execute(): `const typed = args as z.infer<typeof askArgsSchema>`
- category: 'copilot'

**Step 5: Create src/tools/suggest.tool.ts**
Extract SuggestToolHandler. Import shared helpers from copilotExecutor.ts. Returns Promise<string>. category: 'copilot'.

**Step 6: Create src/tools/explain.tool.ts**
Extract ExplainToolHandler. Import shared helpers from copilotExecutor.ts. Returns Promise<string>. category: 'copilot'.

**Step 7: Create src/tools/simple-tools.ts**
Two simple tools: ping, identity (match Gen1 behavior). category: 'simple'.

**Step 8: Create src/tools/index.ts**
```typescript
import { toolRegistry } from './registry.js';
import { askTool } from './ask.tool.js';
import { suggestTool } from './suggest.tool.js';
import { explainTool } from './explain.tool.js';
import { pingTool, identityTool } from './simple-tools.js';

toolRegistry.push(askTool, suggestTool, explainTool, pingTool, identityTool);

export * from './registry.js';
```

**Step 9: Delete Gen1 files**
Delete src/tools/definitions.ts and src/tools/handlers.ts.
  </action>
  <verify>
cd /Users/jonathanborduas/code/copilot-mcp-server && ls src/tools/ | grep -E "(registry|index|ask\.tool|suggest\.tool|explain\.tool|simple-tools)\.ts" | wc -l
# Expect: 6
ls src/tools/definitions.ts src/tools/handlers.ts 2>&1 | grep "No such"
# Expect: both not found
ls src/utils/copilotExecutor.ts
# Expect: file exists
  </verify>
  <done>
registry.ts, index.ts, simple-tools.ts, ask.tool.ts, suggest.tool.ts, explain.tool.ts exist in src/tools/. src/utils/copilotExecutor.ts exists. definitions.ts and handlers.ts are deleted. types.ts exports ToolArguments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update server.ts to Gen2 dispatch and fix test imports; verify build passes</name>
  <files>
    /Users/jonathanborduas/code/copilot-mcp-server/src/server.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/index.test.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/default-model.test.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/edge-cases.test.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/error-scenarios.test.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/mcp-stdio.test.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/__tests__/model-selection.test.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/copilot-mcp-server

**Step 1: Update src/server.ts**
Read server.ts fully first. Replace Gen1 dispatch with Gen2.

Replace imports:
```typescript
// REMOVE:
import { toolDefinitions } from './tools/definitions.js';
import { toolHandlers } from './tools/handlers.js';

// ADD:
import { getToolDefinitions, executeTool, toolExists } from './tools/index.js';
```

Replace ListToolsRequestSchema handler:
```typescript
return { tools: getToolDefinitions() as unknown as Tool[] };
```

Replace CallToolRequestSchema handler:
```typescript
if (!toolExists(name)) { throw new Error(`Unknown tool: ${name}`); }
const args = (request.params.arguments as ToolArguments) || {};
const result = await executeTool(name, args);
return { content: [{ type: 'text', text: result }], isError: false };
```

Remove isValidToolName() (Pitfall 5). Remove TOOLS import if only used for this check.

**Step 2: Update test files**
Read each test file and update imports:
- `from '../tools/definitions.js'` → use getToolDefinitions() from `'../tools/index.js'`
- `from '../tools/handlers.js'` (AskToolHandler, etc.) → import from specific tool file
- Any handler class instantiation → test via UnifiedTool.execute() or executeTool()
- Behavioral assertions must still pass

**Step 3: Build and test**
```bash
cd /Users/jonathanborduas/code/copilot-mcp-server
npm run build
npm test
```

Fix any TypeScript errors (missing types, return type mismatches) before marking done.
  </action>
  <verify>
cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build 2>&1 | tail -5
# Expect: exit 0
npm test 2>&1 | tail -10
# Expect: all tests pass
grep -n "toolHandlers\|toolDefinitions\|isValidToolName\|definitions\.js\|handlers\.js" src/server.ts
# Expect: zero matches
grep -n "executeTool\|toolExists\|getToolDefinitions" src/server.ts
# Expect: all three present
  </verify>
  <done>
npm run build exits 0. npm test passes all tests. src/server.ts uses Gen2 dispatch. No Gen1 references remain in server.ts.
  </done>
</task>

</tasks>

<verification>
1. ls /Users/jonathanborduas/code/copilot-mcp-server/src/tools/ — shows Gen2 files; no definitions.ts or handlers.ts
2. ls /Users/jonathanborduas/code/copilot-mcp-server/src/utils/copilotExecutor.ts — exists
3. cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build — exits 0
4. cd /Users/jonathanborduas/code/copilot-mcp-server && npm test — all tests pass
5. grep "executeTool" /Users/jonathanborduas/code/copilot-mcp-server/src/server.ts — matches found
</verification>

<success_criteria>
copilot-mcp-server uses Gen2 architecture: 5 tools in individual *.tool.ts files, shared helpers in copilotExecutor.ts, registry.ts wires them, server.ts dispatches via executeTool(). Build and all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/24-gen1-to-gen2-architecture-port/24-03-SUMMARY.md`
</output>
