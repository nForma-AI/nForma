---
phase: 24-gen1-to-gen2-architecture-port
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/registry.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/index.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/claude.tool.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/review.tool.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/session/index.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/types.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/server.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/definitions.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/handlers.ts
autonomous: true
requirements:
  - STD-02

must_haves:
  truths:
    - "claude-mcp-server src/tools/ has registry.ts, index.ts, simple-tools.ts, claude.tool.ts, review.tool.ts — no definitions.ts or handlers.ts remains"
    - "npm run build succeeds in claude-mcp-server with zero TypeScript errors"
    - "All existing tests pass after import path updates (no behavioral regressions)"
    - "Session storage singleton is shared between claude.tool.ts and listSessions tool — listSessions returns real sessions"
  artifacts:
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/registry.ts"
      provides: "UnifiedTool interface + toolRegistry array + executeTool() + getToolDefinitions() + toolExists()"
      contains: "export interface UnifiedTool"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/index.ts"
      provides: "Tool registration + re-exports from registry"
      contains: "toolRegistry.push"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/claude.tool.ts"
      provides: "claude tool with streaming, fallback providers, session storage"
      contains: "export const claudeTool"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/session/index.ts"
      provides: "Shared InMemorySessionStorage singleton"
      contains: "export const sessionStorage"
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/server.ts"
      provides: "Gen2 dispatch via executeTool() + toolExists()"
      contains: "executeTool"
  key_links:
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/server.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/index.ts"
      via: "import { getToolDefinitions, executeTool, toolExists }"
      pattern: "executeTool"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/claude.tool.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/session/index.ts"
      via: "import { sessionStorage }"
      pattern: "sessionStorage"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/index.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/registry.ts"
      via: "export * from './registry.js'"
      pattern: "export \\* from"
---

<objective>
Port claude-mcp-server from Gen1 (definitions.ts + handlers.ts) to Gen2 (per-tool *.tool.ts + registry.ts) architecture.

Purpose: claude-mcp-server is the most complex Gen1 repo (5 tools, streaming execution, session storage, fallback providers). Porting it to Gen2 makes adding new tools a one-file operation and matches the gemini/opencode reference implementations.
Output: src/tools/ with registry.ts, index.ts, simple-tools.ts, claude.tool.ts, review.tool.ts; src/session/index.ts singleton; updated server.ts; deleted definitions.ts and handlers.ts; all tests passing; npm run build succeeds.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-gen1-to-gen2-architecture-port/24-RESEARCH.md

# Gen2 reference implementations (read before starting)
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/registry.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/index.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/simple-tools.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/ask-gemini.tool.ts

# Gen1 source to preserve (read before modifying)
@/Users/jonathanborduas/code/claude-mcp-server/src/tools/definitions.ts
@/Users/jonathanborduas/code/claude-mcp-server/src/tools/handlers.ts
@/Users/jonathanborduas/code/claude-mcp-server/src/server.ts
@/Users/jonathanborduas/code/claude-mcp-server/src/types.ts
@/Users/jonathanborduas/code/claude-mcp-server/src/session/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gen2 tool files and session singleton for claude-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/src/session/index.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/registry.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/claude.tool.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/review.tool.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/simple-tools.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/index.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/types.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/definitions.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/handlers.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/claude-mcp-server

Read the Gen1 source files first (definitions.ts, handlers.ts, server.ts, types.ts, session/storage.ts) to understand what must be preserved. Then read the Gen2 reference files from gemini-mcp-server (registry.ts, index.ts, simple-tools.ts, ask-gemini.tool.ts) to understand the target pattern.

**Step 1: Create src/session/index.ts**
Export the InMemorySessionStorage singleton so both claude.tool.ts and simple-tools.ts (listSessions) share the same instance. This prevents the Pitfall 1 split-storage bug.
```typescript
// src/session/index.ts
import { InMemorySessionStorage } from './storage.js';
export const sessionStorage = new InMemorySessionStorage();
```

**Step 2: Add ToolArguments to src/types.ts**
Gen1 types.ts does not export ToolArguments. Add it (Pitfall 2 fix):
```typescript
export type ToolArguments = Record<string, unknown>;
```

**Step 3: Create src/tools/registry.ts**
Copy verbatim from gemini-mcp-server/src/tools/registry.ts. Adapt:
- category union: `'simple' | 'claude' | 'utility'` (not 'gemini')
- Import ToolArguments from '../types.js' (verify the import path matches what claude-mcp-server uses)
- Import ToolAnnotations from '../types.js' if present; otherwise from '@modelcontextprotocol/sdk/types.js'

**Step 4: Create src/tools/claude.tool.ts**
Extract ClaudeToolHandler logic from handlers.ts into a UnifiedTool. Key points:
- Import sessionStorage from '../session/index.js' (NOT creating a new instance)
- Import all streaming/execution helpers that ClaudeToolHandler currently uses (executeCommandStreaming, buildProviderArgs, etc.)
- zodSchema: extract the Zod schema that was in ClaudeToolHandler.execute() — match it to the Gen1 definitions.ts inputSchema exactly (Pitfall 3)
- execute() returns Promise<string>: adapt the streaming output accumulator to return the final string result
- Preserve fallback provider logic and session storage (read/write sessionId) exactly as in Gen1 handler
- category: 'claude'

**Step 5: Create src/tools/review.tool.ts**
Extract ReviewToolHandler logic from handlers.ts. Returns Promise<string>. Preserve model selection and streaming logic. category: 'claude'.

**Step 6: Create src/tools/simple-tools.ts**
Three simple tools: ping, help, listSessions.
- ping: returns 'pong' or echoes message (match Gen1 behavior)
- help: returns help text string (match Gen1 behavior)
- listSessions: imports sessionStorage from '../session/index.js'; calls sessionStorage.listSessions(); returns JSON string or 'No active sessions'
- All three are category: 'simple'

**Step 7: Create src/tools/index.ts**
```typescript
import { toolRegistry } from './registry.js';
import { claudeTool } from './claude.tool.js';
import { reviewTool } from './review.tool.js';
import { pingTool, helpTool, listSessionsTool } from './simple-tools.js';

toolRegistry.push(claudeTool, reviewTool, pingTool, helpTool, listSessionsTool);

export * from './registry.js';
```

**Step 8: Delete Gen1 files**
Delete src/tools/definitions.ts and src/tools/handlers.ts — do NOT keep them alongside the new files (Pitfall anti-pattern: having both causes import confusion and TypeScript errors).
  </action>
  <verify>
cd /Users/jonathanborduas/code/claude-mcp-server && ls src/tools/ | grep -E "(registry|index|claude\.tool|review\.tool|simple-tools)\.ts" | wc -l
# Expect: 5 lines
ls src/tools/definitions.ts src/tools/handlers.ts 2>&1 | grep "No such"
# Expect: both files not found
ls src/session/index.ts
# Expect: file exists
  </verify>
  <done>
registry.ts, index.ts, simple-tools.ts, claude.tool.ts, review.tool.ts exist in src/tools/. src/session/index.ts exists. definitions.ts and handlers.ts are deleted. types.ts exports ToolArguments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update server.ts to Gen2 dispatch and fix test imports; verify build passes</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/src/server.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/index.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/session.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/context-building.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/default-model.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/edge-cases.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/error-scenarios.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/mcp-stdio.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/model-selection.test.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/__tests__/resume-functionality.test.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/claude-mcp-server

**Step 1: Update src/server.ts**
Switch from Gen1 dispatch to Gen2 dispatch. Read the current server.ts fully before editing.

Replace imports:
```typescript
// REMOVE:
import { toolDefinitions } from './tools/definitions.js';
import { toolHandlers } from './tools/handlers.js';

// ADD:
import { getToolDefinitions, executeTool, toolExists } from './tools/index.js';
```

Replace ListToolsRequestSchema handler:
```typescript
// BEFORE (Gen1):
return { tools: toolDefinitions };

// AFTER (Gen2):
return { tools: getToolDefinitions() as unknown as Tool[] };
```

Replace CallToolRequestSchema handler:
```typescript
// BEFORE (Gen1):
if (!this.isValidToolName(name)) { throw new Error(`Unknown tool: ${name}`); }
const result = await toolHandlers[name].execute(args, context);
return result; // returned ToolResult directly

// AFTER (Gen2):
if (!toolExists(name)) { throw new Error(`Unknown tool: ${name}`); }
const args = (request.params.arguments as ToolArguments) || {};
const result = await executeTool(name, args, (newOutput) => { /* optional progress */ });
return { content: [{ type: 'text', text: result }], isError: false };
```

Remove isValidToolName() private method (Pitfall 5 fix). Remove import of TOOLS enum if it was only used for isValidToolName().

Preserve: Keep Gen1-style progress notification infrastructure (streaming via extra.sendNotification) if present — do NOT replace with Gen2 polling pattern. The key correctness requirement is tool dispatch, not progress parity. The onProgress callback in executeTool can be used to accumulate and forward progress, or simplified to a no-op if the streaming is fully inside the tool's execute() implementation.

**Step 2: Update test files**
Read each test file and update import paths:
- Any import from '../tools/definitions.js' → import from '../tools/index.js' (getToolDefinitions()) or from the specific tool file
- Any import from '../tools/handlers.js' (ClaudeToolHandler, etc.) → import from the new per-tool file (../tools/claude.tool.ts, etc.)
- Any direct handler instantiation (new ClaudeToolHandler()) → test via the UnifiedTool's execute() or via executeTool() from index
- Behavioral assertions (tool names, required input fields, response format) must still pass — only import paths change

Check each test for which symbols it imports from Gen1 files and update accordingly. If a test previously tested ClaudeToolHandler class methods, redirect to testing claudeTool.execute() with the same inputs.

**Step 3: Build and test**
```bash
cd /Users/jonathanborduas/code/claude-mcp-server
npm run build
npm test
```

Fix any TypeScript compilation errors before moving on. Common fixes:
- Missing ToolArguments import → add to types.ts import
- ToolAnnotations not found → import from @modelcontextprotocol/sdk/types.js
- Return type mismatch (execute() returning ToolResult instead of string) → adapt to return the text string
  </action>
  <verify>
cd /Users/jonathanborduas/code/claude-mcp-server && npm run build 2>&1 | tail -5
# Expect: exit 0, no TypeScript errors
npm test 2>&1 | tail -10
# Expect: all tests pass (same pass count as before the port)
grep -n "toolHandlers\|toolDefinitions\|isValidToolName\|definitions\.js\|handlers\.js" src/server.ts
# Expect: zero matches (all Gen1 references removed)
grep -n "executeTool\|toolExists\|getToolDefinitions" src/server.ts
# Expect: all three Gen2 functions referenced
  </verify>
  <done>
npm run build exits 0 with zero TypeScript errors. npm test passes all tests. src/server.ts imports from tools/index.js and uses executeTool/toolExists/getToolDefinitions. No Gen1 imports remain in server.ts.
  </done>
</task>

</tasks>

<verification>
1. ls /Users/jonathanborduas/code/claude-mcp-server/src/tools/ — shows registry.ts, index.ts, simple-tools.ts, claude.tool.ts, review.tool.ts; no definitions.ts or handlers.ts
2. ls /Users/jonathanborduas/code/claude-mcp-server/src/session/index.ts — exists
3. cd /Users/jonathanborduas/code/claude-mcp-server && npm run build — exits 0
4. cd /Users/jonathanborduas/code/claude-mcp-server && npm test — all tests pass
5. grep "executeTool" /Users/jonathanborduas/code/claude-mcp-server/src/server.ts — matches found
</verification>

<success_criteria>
claude-mcp-server uses Gen2 architecture: 5 tools in individual *.tool.ts files, registry.ts wires them, server.ts dispatches via executeTool(). Build and all tests pass. Session storage singleton is shared correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/24-gen1-to-gen2-architecture-port/24-01-SUMMARY.md`
</output>
