---
phase: 24-gen1-to-gen2-architecture-port
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/registry.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/index.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/codex.tool.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/review.tool.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/session/index.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/types.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/server.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/definitions.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/handlers.ts
autonomous: true
requirements:
  - STD-02

must_haves:
  truths:
    - "codex-mcp-server src/tools/ has registry.ts, index.ts, simple-tools.ts, codex.tool.ts, review.tool.ts — no definitions.ts or handlers.ts remains"
    - "npm run build succeeds in codex-mcp-server with zero TypeScript errors"
    - "All existing tests pass after import path updates (no behavioral regressions)"
    - "Session storage singleton shared between codex.tool.ts and listSessions — session continuity preserved"
  artifacts:
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/tools/registry.ts"
      provides: "UnifiedTool interface + toolRegistry array + executeTool() + getToolDefinitions() + toolExists()"
      contains: "export interface UnifiedTool"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/tools/codex.tool.ts"
      provides: "codex tool with session storage, soft timeout, sandbox mode"
      contains: "export const codexTool"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/session/index.ts"
      provides: "Shared InMemorySessionStorage singleton"
      contains: "export const sessionStorage"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/server.ts"
      provides: "Gen2 dispatch via executeTool() + toolExists()"
      contains: "executeTool"
  key_links:
    - from: "/Users/jonathanborduas/code/codex-mcp-server/src/server.ts"
      to: "/Users/jonathanborduas/code/codex-mcp-server/src/tools/index.ts"
      via: "import { getToolDefinitions, executeTool, toolExists }"
      pattern: "executeTool"
    - from: "/Users/jonathanborduas/code/codex-mcp-server/src/tools/codex.tool.ts"
      to: "/Users/jonathanborduas/code/codex-mcp-server/src/session/index.ts"
      via: "import { sessionStorage }"
      pattern: "sessionStorage"
---

<objective>
Port codex-mcp-server from Gen1 (definitions.ts + handlers.ts) to Gen2 (per-tool *.tool.ts + registry.ts) architecture.

Purpose: codex-mcp-server has 6 tools including session storage, soft timeout handling, and sandbox mode — all must be preserved exactly in the Gen2 per-tool files. Porting enables single-file tool additions matching the gemini/opencode reference pattern.
Output: src/tools/ with registry.ts, index.ts, simple-tools.ts, codex.tool.ts, review.tool.ts; src/session/index.ts singleton; updated server.ts; deleted definitions.ts and handlers.ts; all tests passing; npm run build succeeds.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-gen1-to-gen2-architecture-port/24-RESEARCH.md

# Gen2 reference implementations (read before starting)
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/registry.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/index.ts
@/Users/jonathanborduas/code/gemini-mcp-server/src/tools/simple-tools.ts

# Gen1 source to preserve (read before modifying)
@/Users/jonathanborduas/code/codex-mcp-server/src/tools/definitions.ts
@/Users/jonathanborduas/code/codex-mcp-server/src/tools/handlers.ts
@/Users/jonathanborduas/code/codex-mcp-server/src/server.ts
@/Users/jonathanborduas/code/codex-mcp-server/src/types.ts
@/Users/jonathanborduas/code/codex-mcp-server/src/session/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gen2 tool files and session singleton for codex-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/codex-mcp-server/src/session/index.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/registry.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/codex.tool.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/review.tool.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/simple-tools.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/index.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/types.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/definitions.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/handlers.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/codex-mcp-server

Read the Gen1 source files first (definitions.ts, handlers.ts, server.ts, types.ts, session/storage.ts) to understand what must be preserved. codex-mcp-server has 6 tools (codex, review, ping, help, listSessions, identity) and uses soft timeout, sandbox mode, and session storage.

**Step 1: Create src/session/index.ts**
Shared singleton to prevent Pitfall 1 (split storage breaking session continuity):
```typescript
import { InMemorySessionStorage } from './storage.js';
export const sessionStorage = new InMemorySessionStorage();
```

**Step 2: Add ToolArguments to src/types.ts (Pitfall 2 fix)**
```typescript
export type ToolArguments = Record<string, unknown>;
```

**Step 3: Create src/tools/registry.ts**
Copy verbatim from gemini-mcp-server/src/tools/registry.ts. Adapt:
- category union: `'simple' | 'codex' | 'utility'`
- Import paths: '../types.js' for ToolArguments; '../types.js' for ToolAnnotations if present

**Step 4: Create src/tools/codex.tool.ts**
Extract CodexToolHandler from handlers.ts. Key points:
- Import sessionStorage from '../session/index.js' (shared singleton, NOT new instance)
- Preserve soft timeout logic, sandbox mode, and all Codex CLI invocation exactly
- zodSchema: match the Zod schema to the Gen1 definitions.ts inputSchema for codex (Pitfall 3)
- execute() returns Promise<string> — accumulate any streaming/stdout output and return as string
- Inside execute(), cast args: `const typed = args as z.infer<typeof codexArgsSchema>`
- category: 'codex'

**Step 5: Create src/tools/review.tool.ts**
Extract ReviewToolHandler. Returns Promise<string>. Preserve model selection logic. category: 'codex'.

**Step 6: Create src/tools/simple-tools.ts**
Four simple tools: ping, help, listSessions, identity.
- ping: returns 'pong' or echo
- help: returns help text
- listSessions: import sessionStorage from '../session/index.js'; return JSON or 'No active sessions'
- identity: return server name/version/model info (match Gen1 identity tool behavior)
- All category: 'simple'

**Step 7: Create src/tools/index.ts**
```typescript
import { toolRegistry } from './registry.js';
import { codexTool } from './codex.tool.js';
import { reviewTool } from './review.tool.js';
import { pingTool, helpTool, listSessionsTool, identityTool } from './simple-tools.js';

toolRegistry.push(codexTool, reviewTool, pingTool, helpTool, listSessionsTool, identityTool);

export * from './registry.js';
```

**Step 8: Delete Gen1 files**
Delete src/tools/definitions.ts and src/tools/handlers.ts.
  </action>
  <verify>
cd /Users/jonathanborduas/code/codex-mcp-server && ls src/tools/ | grep -E "(registry|index|codex\.tool|review\.tool|simple-tools)\.ts" | wc -l
# Expect: 5
ls src/tools/definitions.ts src/tools/handlers.ts 2>&1 | grep "No such"
# Expect: both not found
ls src/session/index.ts
# Expect: file exists
  </verify>
  <done>
registry.ts, index.ts, simple-tools.ts, codex.tool.ts, review.tool.ts exist in src/tools/. src/session/index.ts exists. definitions.ts and handlers.ts are deleted. types.ts exports ToolArguments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update server.ts to Gen2 dispatch and fix test imports; verify build passes</name>
  <files>
    /Users/jonathanborduas/code/codex-mcp-server/src/server.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/index.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/session.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/context-building.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/default-model.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/edge-cases.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/error-scenarios.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/mcp-stdio.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/model-selection.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/resume-functionality.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/soft-timeout-command.test.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/__tests__/soft-timeout.test.ts
  </files>
  <action>
Working directory: /Users/jonathanborduas/code/codex-mcp-server

**Step 1: Update src/server.ts**
Read server.ts fully first. Replace Gen1 dispatch with Gen2.

Replace imports:
```typescript
// REMOVE:
import { toolDefinitions } from './tools/definitions.js';
import { toolHandlers } from './tools/handlers.js';

// ADD:
import { getToolDefinitions, executeTool, toolExists } from './tools/index.js';
```

Replace ListToolsRequestSchema handler body:
```typescript
return { tools: getToolDefinitions() as unknown as Tool[] };
```

Replace CallToolRequestSchema handler body:
```typescript
if (!toolExists(name)) { throw new Error(`Unknown tool: ${name}`); }
const args = (request.params.arguments as ToolArguments) || {};
const result = await executeTool(name, args);
return { content: [{ type: 'text', text: result }], isError: false };
```

Remove isValidToolName() private method (Pitfall 5). Remove TOOLS import if only used for isValidToolName().

**Step 2: Update test files**
Read each test file. Update imports:
- `from '../tools/definitions.js'` → use `getToolDefinitions()` from `'../tools/index.js'`
- `from '../tools/handlers.js'` → import from specific tool file (codex.tool.ts, etc.)
- Any handler class instantiation → test via UnifiedTool.execute() or executeTool()
- soft-timeout.test.ts and soft-timeout-command.test.ts: check what they import; adapt to test codex.tool.ts execute() directly if they test CodexToolHandler internals
- Behavioral assertions (tool names, required fields, response shape, session behavior) must still pass

**Step 3: Build and test**
```bash
cd /Users/jonathanborduas/code/codex-mcp-server
npm run build
npm test
```

Fix TypeScript errors (return type, missing imports, etc.) before marking complete.
  </action>
  <verify>
cd /Users/jonathanborduas/code/codex-mcp-server && npm run build 2>&1 | tail -5
# Expect: exit 0
npm test 2>&1 | tail -10
# Expect: all tests pass
grep -n "toolHandlers\|toolDefinitions\|isValidToolName\|definitions\.js\|handlers\.js" src/server.ts
# Expect: zero matches
grep -n "executeTool\|toolExists\|getToolDefinitions" src/server.ts
# Expect: all three present
  </verify>
  <done>
npm run build exits 0. npm test passes all tests. src/server.ts uses Gen2 dispatch. No Gen1 references remain in server.ts.
  </done>
</task>

</tasks>

<verification>
1. ls /Users/jonathanborduas/code/codex-mcp-server/src/tools/ — shows Gen2 files; no definitions.ts or handlers.ts
2. ls /Users/jonathanborduas/code/codex-mcp-server/src/session/index.ts — exists
3. cd /Users/jonathanborduas/code/codex-mcp-server && npm run build — exits 0
4. cd /Users/jonathanborduas/code/codex-mcp-server && npm test — all tests pass
5. grep "executeTool" /Users/jonathanborduas/code/codex-mcp-server/src/server.ts — matches found
</verification>

<success_criteria>
codex-mcp-server uses Gen2 architecture: 6 tools in individual *.tool.ts files, registry.ts wires them, server.ts dispatches via executeTool(). Build and all tests pass. Session storage singleton shared correctly. Soft timeout and sandbox mode preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/24-gen1-to-gen2-architecture-port/24-02-SUMMARY.md`
</output>
