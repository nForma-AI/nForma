---
phase: v0.14-05
slug: watch-mode
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-26
---

# Phase v0.14-05 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.14-05`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.14-05`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (built-in, already used throughout project) |
| **Config file** | `package.json` `"test"` script |
| **Quick run command** | `node --test bin/run-formal-verify.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30s (no Java/TLC/Alloy/PRISM needed for watch tests) |
| **CI pipeline** | `.github/workflows/formal-verify.yml` — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/run-formal-verify.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| Wave 0 scaffold | 01 | 0 | DX-01 | integration (placeholder) | `node --test bin/run-formal-verify.test.cjs` | ❌ W0 add to existing | ⬜ pending |
| Task 1: Extract runOnce() | 01 | 1 | DX-01 | unit (smoke) | `node --test bin/run-formal-verify.test.cjs` | ✅ existing tests cover | ⬜ pending |
| Task 2: --watch loop + watcher | 01 | 1 | DX-01 | integration (spawn+SIGINT) | `node --test bin/run-formal-verify.test.cjs` | ❌ W0 add placeholders | ⬜ pending |
| Task 3: Full test suite regression | 01 | 1 | DX-01 | smoke (full suite) | `npm test` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] Add 3 placeholder watch-mode tests to `bin/run-formal-verify.test.cjs` (they will fail until `--watch` is implemented):
  - `watch mode (DX-01): --watch flag is accepted without error` — placeholder
  - `watch mode (DX-01): starts and does not exit immediately` — placeholder
  - `watch mode (DX-01): exits cleanly on SIGINT` — placeholder

**Why placeholders first:** The full spawn+SIGINT integration tests require the feature to exist. Placeholders use `spawnSync` with `--help` or check `--watch` doesn't crash CLI parsing — runnable immediately, fail cleanly, signal intent.

---

## Test Isolation Requirements

Watch mode tests MUST:
- Use `os.tmpdir()` + `fs.mkdtempSync()` for isolated temp dirs with a copy of `src/machines/qgsd-workflow.machine.ts`
- Use `--only=generate` so tests only run the 2 generate steps (no Java/TLC/Alloy/PRISM required)
- Set `cwd: tmpDir` so `bin/` scripts resolve relative to tmpdir with the machine file
- Include `timeout: 30000` per test
- Clean up tmpdir in `finally` blocks

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real-world save with vim/neovim | DX-01 | Editor integration can't be automated in CI | Open XState machine in vim; run `node bin/run-formal-verify.cjs --watch --only=generate`; save with `:w`; verify re-run appears within 3s |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 30s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.14-05`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 3 | `node --test bin/run-formal-verify.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
