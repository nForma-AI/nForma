---
plan: v0.14-05-02
phase: v0.14-05
status: complete
wave: 1
requirements: [DX-01]
tasks_completed: 2
files_modified: 2
commits:
  - 4b7a77c
  - 32d54b6
date: 2026-02-26
key-files:
  created: []
  modified:
    - bin/run-formal-verify.cjs
    - bin/run-formal-verify.test.cjs
---

# Plan v0.14-05-02 SUMMARY: Watch Mode Implementation

## What Was Built

Implemented `--watch` mode in `bin/run-formal-verify.cjs` and replaced the 3 placeholder tests from Plan 01 with 3 real spawn+SIGINT integration tests. DX-01 is now fully satisfied.

## Tasks Completed

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Extract runOnce() and add --watch branch to run-formal-verify.cjs | Complete | 4b7a77c |
| 2 | Replace placeholder tests with real integration tests | Complete | 32d54b6 |

## Implementation

### Task 1 — run-formal-verify.cjs changes

- Extracted the async IIFE body into `async function runOnce()` returning `failed` count
- Added `results.length = 0` at top of `runOnce()` to reset for each re-run
- Added `--watch` branch: `fs.watch(process.cwd()/src/machines/)` watching `qgsd-workflow.machine.ts`
- 300ms debounce timer before re-running
- SIGINT handler: closes watcher, prints "Exiting watch mode.", calls `process.exit(0)`
- Non-watch mode unchanged: `runOnce().then(failed => process.exit(failed > 0 ? 1 : 0))`
- Used `process.cwd()` (not `__dirname`) for `machineDir` — critical for test isolation

### Task 2 — run-formal-verify.test.cjs changes

- Added `spawn`, `fs`, `os` imports
- Replaced 3 placeholder tests with real integration tests
- All 3 tests spawn with `cwd: tmpDir`, copy machine file to `tmpDir/src/machines/`
- Tests wait for initial SUMMARY before sending SIGINT (avoids spawnSync-blocking race)
- `--only=generate` — no Java/TLC/Alloy/PRISM needed

## Test Results

- `node --test bin/run-formal-verify.test.cjs`: 8/8 pass
- `npm test`: 340/340 pass (was 337 before v0.14-04, gained 3 from watch-mode tests)

## Success Criteria

- [x] `node bin/run-formal-verify.cjs --watch` starts and does not exit — watches XState machine file
- [x] File save triggers re-run within seconds (Change detected + re-run prints SUMMARY)
- [x] SIGINT terminates cleanly: exit code 0, "Exiting watch mode." printed
- [x] Non-watch behavior unchanged: existing 5 tests still pass
- [x] 8/8 tests pass; 340/340 full suite

## Self-Check: PASSED

- `node --check bin/run-formal-verify.cjs` exits 0
- `node bin/run-formal-verify.cjs --only=generate` prints SUMMARY + Wall-clock (regression: ok)
- `node bin/run-formal-verify.cjs --only=bogus-invalid` exits 1 (regression: ok)
- All 8 tests pass, npm test 340/340
- DX-01 satisfied
