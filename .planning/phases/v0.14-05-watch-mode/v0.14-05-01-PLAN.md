---
phase: v0.14-05
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/run-formal-verify.test.cjs
autonomous: true
requirements:
  - DX-01

must_haves:
  truths:
    - "3 placeholder watch-mode tests exist in bin/run-formal-verify.test.cjs"
    - "Running node --test bin/run-formal-verify.test.cjs succeeds (test suite runs; placeholder tests fail with assertion errors, not crashes)"
    - "Placeholder test names contain 'DX-01' to signal intent"
  artifacts:
    - path: "bin/run-formal-verify.test.cjs"
      provides: "Placeholder watch-mode tests (3 tests, expected to fail until --watch implemented)"
      contains: "watch mode (DX-01)"
  key_links:
    - from: "bin/run-formal-verify.test.cjs"
      to: "bin/run-formal-verify.cjs"
      via: "spawnSync with --watch and --check flags"
      pattern: "spawnSync.*run-formal-verify.*--watch"
---

<objective>
Add 3 placeholder watch-mode tests to bin/run-formal-verify.test.cjs BEFORE any implementation exists. These tests fail until --watch is implemented (Wave 1), following the Nyquist Wave 0 requirement from v0.14-05-VALIDATION.md.

Purpose: Encode DX-01 test intent before touching the implementation, so Wave 1 has a clear RED-state baseline to turn GREEN.
Output: 3 new test cases in bin/run-formal-verify.test.cjs that fail with assertion errors (not crashes).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.14-05-watch-mode/v0.14-05-RESEARCH.md
@.planning/phases/v0.14-05-watch-mode/v0.14-05-VALIDATION.md
@bin/run-formal-verify.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 placeholder watch-mode tests to bin/run-formal-verify.test.cjs</name>
  <files>bin/run-formal-verify.test.cjs</files>
  <action>
Append 3 new tests to bin/run-formal-verify.test.cjs after the existing 5 tests. These are placeholder tests that:
- Use spawnSync (not spawn) so they complete synchronously
- Do NOT require --watch to be fully implemented to run (the test suite does not hang)
- Will fail with assertion errors once run against the current code (before --watch exists) because they check for watch-mode behavior
- Will turn GREEN in Plan 02 after implementation

Test 1 — flag acceptance:
```javascript
test('watch mode (DX-01): --watch flag is accepted without error', () => {
  // Placeholder: once --watch is handled, argv parsing must not print
  // "unknown flag" or crash with a non-zero exit on --help-style invocation.
  // We use --check (syntax only) to verify the script is parseable, then
  // spawnSync with --watch + a deliberate timeout sentinel.
  // This test will be replaced by a full integration test in Plan 02.
  const result = spawnSync(process.execPath, ['--check', RUN_FV], {
    encoding: 'utf8',
    timeout: 5000,
  });
  assert.strictEqual(result.status, 0, '--check must pass (syntax valid after --watch added)');
  assert.doesNotMatch(result.stderr || '', /SyntaxError/);
  // Placeholder assertion: verify RUN_FV path resolves
  assert.ok(require('fs').existsSync(RUN_FV), 'run-formal-verify.cjs must exist');
});
```

Test 2 — starts and persists (placeholder using known-good argv):
```javascript
test('watch mode (DX-01): starts and does not exit immediately — placeholder', () => {
  // Placeholder until --watch is implemented.
  // Verifies --only=generate is still accepted (existing behavior, non-watch).
  // Plan 02 replaces this with a spawn + SIGINT integration test.
  const result = spawnSync(process.execPath, [RUN_FV, '--only=generate'], {
    encoding: 'utf8',
    timeout: 30000,
  });
  assert.doesNotMatch(result.stderr || '', /Unknown --only value/i);
  // Placeholder: assert watching message NOT yet present (confirms feature not yet shipped)
  const output = (result.stdout || '') + (result.stderr || '');
  // This assertion intentionally FAILS until --watch is implemented in Plan 02
  assert.match(output, /Watch mode enabled|Watching:/i,
    'PLACEHOLDER: will fail until --watch is implemented in Plan 02 — expected to be RED');
});
```

Test 3 — SIGINT clean exit (placeholder):
```javascript
test('watch mode (DX-01): exits cleanly on SIGINT — placeholder', () => {
  // Placeholder until --watch is implemented.
  // Plan 02 replaces this with a spawn() + child.kill('SIGINT') integration test.
  // For now: assert that the script exits 0 on normal completion with --only=generate
  // and that 'Exiting watch mode' is NOT in output (confirming feature not yet present).
  const result = spawnSync(process.execPath, [RUN_FV, '--only=generate'], {
    encoding: 'utf8',
    timeout: 30000,
  });
  const output = (result.stdout || '') + (result.stderr || '');
  // This assertion intentionally FAILS until Plan 02 implementation adds SIGINT handling
  assert.match(output, /Exiting watch mode/i,
    'PLACEHOLDER: will fail until --watch SIGINT handler is implemented in Plan 02 — expected to be RED');
});
```

Important notes:
- Do NOT add `const { spawn } = require('child_process')` — these placeholders use only spawnSync (already imported).
- Do NOT add `const os = require('os')` or `const fs = require('fs')` if not already present — check existing requires first.
- Add `require('fs')` inline in the first test (as shown above) if fs is not already required at the top.
- Preserve all existing tests exactly — only APPEND after line 95.
  </action>
  <verify>node --test bin/run-formal-verify.test.cjs 2>&1 | tail -30</verify>
  <done>
- bin/run-formal-verify.test.cjs now has 8 tests total (5 existing + 3 new)
- All 3 new tests have names containing "watch mode (DX-01)"
- Running node --test bin/run-formal-verify.test.cjs completes without crashing
- Tests 6 and 7 (the PLACEHOLDER ones with "expected to be RED" messages) fail with AssertionError (not with syntax/crash errors)
- Test 5 (flag acceptance using --check) passes GREEN (syntax check still works)
- The 5 original tests remain GREEN
  </done>
</task>

</tasks>

<verification>
node --test bin/run-formal-verify.test.cjs 2>&1 | grep -E "(pass|fail|watch mode|DX-01|✓|✗|▶)"

Expected: 3 new tests appear, 2 of the 3 placeholder tests fail with assertion errors containing "PLACEHOLDER", test suite completes (does not hang).
</verification>

<success_criteria>
- bin/run-formal-verify.test.cjs has 8 tests (5 existing + 3 DX-01 placeholders)
- node --test bin/run-formal-verify.test.cjs completes without hanging
- Wave 0 scaffold committed: test intent for --watch encoded before implementation
- VALIDATION.md wave_0_complete field can be set to true
</success_criteria>

<output>
After completion, create .planning/phases/v0.14-05-watch-mode/v0.14-05-01-SUMMARY.md
</output>
