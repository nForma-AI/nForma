---
phase: v0.25-01-schema-foundation
verified: 2026-03-03T09:30:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase v0.25-01: Schema Foundation Verification Report

**Phase Goal:** Every formal artifact (model registry, check results, runners, requirements envelope) carries structured requirement linkage fields that downstream tools can query
**Verified:** 2026-03-03T09:30:00Z
**Status:** passed
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `formal/model-registry.json` has a `requirements: string[]` field on all 24 model entries; 21 non-empty, 3 empty (TUINavigation, QGSDQuorum_xstate, test entry) | VERIFIED | Node check: 21 with requirements, 3 empty, 0 missing field |
| 2 | `formal/requirements.json` has `formal_models: string[]` on 56 requirements with known formal coverage | VERIFIED | Node check: 56 requirements with `formal_models`; QUORUM-02 correctly shows `["formal/tla/QGSDQuorum.tla", "formal/alloy/quorum-votes.als"]` |
| 3 | Bidirectional consistency: every model->req and req->model mapping is symmetric | VERIFIED | Bidirectional check: 56 OK, 0 mismatches |
| 4 | The test model entry has an empty requirements array (consistent with TUINavigation, QGSDQuorum_xstate) | VERIFIED | `../../../../tmp/promote-test/formal/tla/QGSDQuorum.tla` -> `[]` |
| 5 | `formal/check-result.schema.json` has an optional `requirement_ids: string[]` field with pattern `^[A-Z]`; `uppaal` in formalism enum | VERIFIED | Schema check: `requirement_ids` type=array, items.type=string, pattern=^[A-Z], not in required array; formalism enum includes `uppaal` |
| 6 | `write-check-result.cjs` emits `requirement_ids` (defaults to `[]`), validates elements are strings, rejects non-array/non-string inputs | VERIFIED | 21 tests pass; runtime checks confirmed: default `[]`, provided array emitted, non-string rejected, non-array rejected |
| 7 | `bin/requirement-map.cjs` exports `CHECK_ID_TO_REQUIREMENTS` (28 entries) and `getRequirementIds(checkId)` with fail-open semantics | VERIFIED | 27 tests pass; `getRequirementIds('unknown:check')` returns `[]`; returns copy via `.slice()` to prevent mutation |
| 8 | All 18 formal verification runners import `requirement-map.cjs` and pass `requirement_ids` in every `writeCheckResult()` call | VERIFIED | Code scan: 18/18 runners contain both `requirement-map` import and `requirement_ids` field |
| 9 | `bin/aggregate-requirements.cjs` captures and merges back `formal_models` enrichment data across re-aggregation runs | VERIFIED | Aggregator has pre-aggregation capture (`existingFormalModels`) and post-aggregation merge-back pattern at lines 240-283 |

**Score:** 9/9 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `formal/model-registry.json` | Model-level requirement linkage for all 24 models | VERIFIED | 24 entries, all have `requirements` array; 21 non-empty seeded from TRACEABILITY_RESEARCH.md Section 5 |
| `formal/requirements.json` | Requirement-level formal model linkage | VERIFIED | 56 requirements have `formal_models` arrays; 154 without formal coverage correctly omit the field |
| `formal/check-result.schema.json` | JSON schema with `requirement_ids` field | VERIFIED | Contains `requirement_ids` (optional array of strings matching `^[A-Z]`); `additionalProperties: false` still in force |
| `bin/write-check-result.cjs` | NDJSON writer that emits `requirement_ids` | VERIFIED | Validates, defaults to `[]`, emits in record; 21 tests pass |
| `bin/write-check-result.test.cjs` | Tests for `requirement_ids` acceptance and rejection | VERIFIED | 5 new tests (tests 16-20) covering default empty, provided array, non-string rejection, non-array rejection, backward compatibility; all 21 tests pass |
| `bin/requirement-map.cjs` | Centralized check_id to requirement_ids mapping (28 entries) | VERIFIED | Exports `CHECK_ID_TO_REQUIREMENTS` and `getRequirementIds`; 28 check_ids covering TLA+/Alloy/PRISM/UPPAAL/CI; fail-open; mutation-safe |
| `bin/requirement-map.test.cjs` | 27 tests for requirement-map module | VERIFIED | 27/27 tests pass across 7 describe blocks |
| `bin/aggregate-requirements.cjs` | Preserves `formal_models` across re-runs | VERIFIED | Pre-aggregation capture + merge-back pattern implemented; `formal_models` survives re-aggregation |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `formal/model-registry.json` | `formal/requirements.json` | Bidirectional requirement-model mapping | WIRED | Bidirectional check: 56 requirements consistent, 0 mismatches |
| `bin/write-check-result.cjs` | `formal/check-result.schema.json` | NDJSON output conforms to schema (`requirement_ids`) | WIRED | Schema declares `requirement_ids` optional; writer emits it with same semantics |
| `bin/write-check-result.test.cjs` | `bin/write-check-result.cjs` | `require('./write-check-result.cjs')` | WIRED | Test file imports and exercises the writer |
| `bin/run-tlc.cjs` | `bin/requirement-map.cjs` | `require('./requirement-map.cjs')` + `requirement_ids: getRequirementIds(checkId)` | WIRED | Confirmed by code scan (18/18 runners) |
| All 18 runners | `bin/write-check-result.cjs` | `requirement_ids` field passed through `writeCheckResult()` | WIRED | 18/18 runners have both `requirement-map` import and `requirement_ids` in `writeCheckResult` calls |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SCHEMA-01 | v0.25-01-01 | `model-registry.json` entries gain a `requirements: string[]` array seeded from property-to-requirement map | SATISFIED | 24 entries with `requirements` arrays; 21 non-empty; specific checks (QGSDStopHook.tla -> STOP-01..STOP-07, SPEC-01) pass |
| SCHEMA-02 | v0.25-01-02 | `check-result.schema.json` gains `requirement_ids: string[]` field; validation updated | SATISFIED | Schema has `requirement_ids` (type=array, items.type=string, pattern=^[A-Z], optional); `uppaal` in formalism enum; 21 writer tests pass |
| SCHEMA-03 | v0.25-01-03 | Each verification runner emits `requirement_ids` in NDJSON output | SATISFIED | 18/18 runners wired; centralized `requirement-map.cjs` with 28 check_ids; 27/27 tests pass |
| SCHEMA-04 | v0.25-01-01 | `requirements.json` envelope gains optional `formal_models: string[]` per requirement | SATISFIED | 56 requirements have `formal_models`; aggregator preserves field across re-runs; bidirectional consistency confirmed |

No orphaned requirements: all four SCHEMA-01..04 were claimed by plans and have corresponding implementation evidence.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `formal/requirements.json` | 1548, 1556 | text "placeholder" | Info | Domain text describing the `__redacted__` placeholder in API key export feature -- not a code stub |

No blockers or warnings found. The two "placeholder" hits are legitimate requirement text describing a security design (API key redaction), not placeholder implementations.

---

## Human Verification Required

None. All truths are verifiable programmatically via:
- JSON structure inspection (model-registry.json, requirements.json, check-result.schema.json)
- Node.js test execution (21 write-check-result tests pass, 27 requirement-map tests pass)
- Code pattern scanning (18/18 runners wired)
- Bidirectional consistency check (0 mismatches across 56 requirements)
- Git commit existence (all 7 documented commits verified)

---

## Gaps Summary

No gaps. All 9 observable truths are verified. All 8 required artifacts exist, are substantive (not stubs), and are wired. All 4 requirements (SCHEMA-01 through SCHEMA-04) are satisfied with implementation evidence.

The one notable deviation from the original plan that was correctly handled: `run-phase-tlc.cjs` was excluded from wiring because it does not call `writeCheckResult()` (it is a plan verification tool, not a formal check runner). The plan listed 19 files; 18 were wired, 1 excluded for documented reason. This does not affect goal achievement.

---

_Verified: 2026-03-03T09:30:00Z_
_Verifier: Claude (qgsd-verifier)_
