---
phase: v0.25-01-schema-foundation
plan: "02"
subsystem: formal-verification
tags: [json-schema, ndjson, requirement-traceability, write-check-result]

# Dependency graph
requires:
  - phase: v0.25-01-01
    provides: model-registry and requirements.json seeding (foundational schema infrastructure)
provides:
  - requirement_ids field in check-result.schema.json (type=array, items.type=string, pattern=^[A-Z])
  - write-check-result.cjs emits requirement_ids in NDJSON (defaults to [] for backward compat)
  - 5 new tests covering acceptance and rejection of requirement_ids
  - "uppaal" added to formalism enum in schema (was missing, already in VALID_FORMALISMS)
affects: [v0.25-01-03, v0.25-02, v0.25-03, v0.25-04, v0.25-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional field backward compat: new fields default to [] via `Array.isArray(x) ? x : []`"
    - "Validation before record construction: validate optional arrays before building output record"

key-files:
  created: []
  modified:
    - formal/check-result.schema.json
    - bin/write-check-result.cjs
    - bin/write-check-result.test.cjs

key-decisions:
  - "requirement_ids is optional (not in required array) for backward compatibility with existing runners"
  - "requirement_ids items validated as strings in writer, not just schema -- belt-and-suspenders for runtime safety"
  - "uppaal added to formalism enum to align schema with VALID_FORMALISMS already in write-check-result.cjs"

patterns-established:
  - "requirement_ids: string[] pattern: optional field, defaults to [], elements must be strings starting with uppercase"

requirements-completed: [SCHEMA-02]

# Metrics
duration: 2min
completed: 2026-03-03
---

# Phase v0.25-01 Plan 02: check-result Schema + Writer requirement_ids Extension Summary

**JSON schema and NDJSON writer extended with optional `requirement_ids: string[]` field that links check results to requirements, with full validation and 5 new tests (21 total)**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-03T08:53:56Z
- **Completed:** 2026-03-03T08:55:37Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Extended `formal/check-result.schema.json` with `requirement_ids` property: optional array of strings matching `^[A-Z]` pattern, not in required array for backward compatibility
- Added `"uppaal"` to formalism enum in schema (was missing; VALID_FORMALISMS in write-check-result.cjs already had it)
- Updated `bin/write-check-result.cjs` to validate and emit `requirement_ids` in NDJSON output, defaulting to `[]` when absent
- Added 5 new tests covering: default empty array, provided array, non-string rejection, non-array rejection, backward compatibility (21 tests total, all pass)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend check-result.schema.json with requirement_ids field** - `3e70299c` (feat)
2. **Task 2: Update write-check-result.cjs to emit requirement_ids + add tests** - `e9c9fe38` (feat)

**Plan metadata:** (pending docs commit)

## Files Created/Modified

- `formal/check-result.schema.json` - Added requirement_ids property (array of strings, pattern ^[A-Z], optional); added "uppaal" to formalism enum
- `bin/write-check-result.cjs` - Added requirement_ids validation and emission; added JSDoc parameter
- `bin/write-check-result.test.cjs` - Added 5 new tests (tests 16-20) for requirement_ids behavior

## Decisions Made

- `requirement_ids` is optional (not in `required` array) — existing runners don't emit it, so making it required would break backward compatibility
- Writer validates `requirement_ids` type at runtime (not just relying on JSON schema) — belt-and-suspenders for safety
- `"uppaal"` added to schema formalism enum to match `VALID_FORMALISMS` in writer — schema and writer must agree on valid values

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added "uppaal" to formalism enum in schema**
- **Found during:** Task 1 (schema extension)
- **Issue:** Plan instructed to verify "uppaal" is in formalism enum and add if missing. The schema had `["tla", "alloy", "prism", "trace", "redaction"]` but `VALID_FORMALISMS` in write-check-result.cjs already included `"uppaal"`. Schema was out of sync.
- **Fix:** Added `"uppaal"` to formalism enum in schema — aligns schema with writer's VALID_FORMALISMS
- **Files modified:** `formal/check-result.schema.json`
- **Verification:** `node -e` confirms uppaal in enum; existing uppaal test (Test 15a) still passes
- **Committed in:** 3e70299c (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - schema/writer enum mismatch bug)
**Impact on plan:** Fix was explicitly called out in the plan as a conditional action. No scope creep.

## Issues Encountered

None - plan executed cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `requirement_ids` field is now available in the check-result schema and writer
- Plan 03 (runner wiring) can now add `requirement_ids` arrays to runner calls
- All 21 tests pass, no regressions
- Schema and writer are in sync on formalism enum values

---
*Phase: v0.25-01-schema-foundation*
*Completed: 2026-03-03*
