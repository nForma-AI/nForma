---
phase: v0.25-01-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formal/model-registry.json
  - formal/requirements.json
  - bin/aggregate-requirements.cjs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-04
formal_artifacts:
  update:
    - formal/model-registry.json
    - formal/requirements.json

must_haves:
  truths:
    - "Running `cat formal/model-registry.json | jq '.models | to_entries[] | select(.value.requirements != null) | .value.requirements'` shows a non-empty string array for every model entry that has known requirement coverage (seeded from TRACEABILITY_RESEARCH.md Section 5 map)"
    - "Running `cat formal/requirements.json | jq '.requirements[] | select(.formal_models != null)'` shows requirements that have formal model paths linked back to them"
    - "The test model entry (../../../../tmp/promote-test/...) has no requirements array (excluded from seeding)"
    - "TUINavigation.tla has an empty requirements array (no matching requirement IDs)"
    - "Auto-generated models (QGSDQuorum, quorum-votes, quorum.pm) have requirements arrays seeded from the property-to-requirement map"
  artifacts:
    - path: "formal/model-registry.json"
      provides: "Model-level requirement linkage for all 22 models"
      contains: "requirements"
    - path: "formal/requirements.json"
      provides: "Requirement-level formal model linkage"
      contains: "formal_models"
  key_links:
    - from: "formal/model-registry.json"
      to: "formal/requirements.json"
      via: "bidirectional requirement-model mapping"
      pattern: "requirements.*QUORUM|formal_models.*tla"
---

<objective>
Seed requirement linkage fields into model-registry.json and requirements.json, establishing the bidirectional mapping foundation for formal traceability.

Purpose: SCHEMA-01 and SCHEMA-04 create the data layer that all downstream traceability tools (matrix generator, coverage dashboards, CI guards) will query. Without these fields, no automated traceability is possible.
Output: model-registry.json with `requirements: string[]` per model; requirements.json with `formal_models: string[]` per requirement.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.25-01-schema-foundation/v0.25-01-RESEARCH.md
@formal/model-registry.json
@formal/requirements.json
@bin/aggregate-requirements.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements arrays to model-registry.json (SCHEMA-01)</name>
  <files>formal/model-registry.json</files>
  <action>
Read formal/model-registry.json and add a `requirements` field (array of strings) to each model entry. Seed the arrays using the property-to-requirement map from `.planning/research/TRACEABILITY_RESEARCH.md` Section 5.

Use this exact seed data (from Research Section 5.1, 5.2, 5.3):

```
"formal/tla/QGSDQuorum.tla":        ["QUORUM-01", "QUORUM-02", "QUORUM-03", "QUORUM-04", "SAFE-01", "SAFE-02", "SAFE-03", "LOOP-01", "RECV-01"]
"formal/tla/QGSDCircuitBreaker.tla": ["DETECT-01", "DETECT-02", "DETECT-03"]
"formal/tla/QGSDOscillation.tla":    ["DETECT-04", "DETECT-05", "DETECT-06"]
"formal/tla/QGSDConvergence.tla":    ["ORES-01", "ORES-02", "ORES-03", "ORES-04", "ORES-05"]
"formal/tla/QGSDDeliberation.tla":   ["PLAN-01", "PLAN-02", "SAFE-03", "IMPR-01", "LOOP-02"]
"formal/tla/QGSDPreFilter.tla":      ["PLAN-03", "PLAN-04", "PLAN-05", "PLAN-06", "LOOP-03"]
"formal/tla/QGSDAccountManager.tla": ["CRED-01", "CRED-02", "CRED-03", "CRED-04", "CRED-05", "CRED-06"]
"formal/tla/QGSDMCPEnv.tla":         ["MCPENV-01", "MCPENV-02", "MCPENV-03"]
"formal/tla/QGSDStopHook.tla":       ["STOP-01", "STOP-02", "STOP-03", "STOP-04", "STOP-05", "STOP-06", "STOP-07", "SPEC-01"]
"formal/tla/QGSDRecruiting.tla":     ["SLOT-02", "SLOT-03", "SLOT-04", "SLOT-05"]
"formal/tla/TUINavigation.tla":      []
"formal/tla/QGSDQuorum_xstate.tla":  []
"formal/alloy/quorum-votes.als":          ["QUORUM-02", "SAFE-01", "SAFE-04"]
"formal/alloy/quorum-composition.als":    ["SPEC-03", "COMP-01"]
"formal/alloy/scoreboard-recompute.als":  ["SCBD-01", "SCBD-02", "SCBD-03", "SCBD-04"]
"formal/alloy/availability-parsing.als":  ["CALIB-01", "CALIB-02", "CALIB-03"]
"formal/alloy/transcript-scan.als":       ["STOP-08", "STOP-09", "STOP-10", "STOP-11"]
"formal/alloy/install-scope.als":         ["INST-01", "INST-02", "INST-03", "INST-04", "INST-05"]
"formal/alloy/taxonomy-safety.als":       ["SCBD-05", "SCBD-06", "SCBD-07"]
"formal/alloy/account-pool-structure.als":["CRED-07", "CRED-08", "CRED-09", "CRED-10", "CRED-11"]
"formal/prism/quorum.pm":            ["PRM-01", "QUORUM-04", "LOOP-01"]
"formal/prism/oauth-rotation.pm":    ["PRM-AM-01", "CRED-12"]
"formal/prism/mcp-availability.pm":  ["MCPENV-04", "FAIL-01"]
```

For the test model entry (`../../../../tmp/promote-test/formal/tla/QGSDQuorum.tla`), do NOT add a requirements field -- leave it untouched or set `"requirements": []`.

Place the `requirements` field after the `description` field in each entry to keep JSON ordering consistent. Preserve all existing fields (version, last_updated, update_source, source_id, session_id, description).

Validate the output is valid JSON: `node -e "JSON.parse(require('fs').readFileSync('formal/model-registry.json','utf8')); console.log('OK')"`.
  </action>
  <verify>
Run: `node -e "const r=JSON.parse(require('fs').readFileSync('formal/model-registry.json','utf8')); const m=r.models; let ok=0,empty=0; for(const[k,v]of Object.entries(m)){if(Array.isArray(v.requirements)){if(v.requirements.length>0)ok++;else empty++}} console.log('Models with requirements:',ok,'Empty:',empty)"`

Expected: Models with requirements >= 20, Empty >= 2 (TUINavigation, QGSDQuorum_xstate, test entry).

Also verify a specific entry: `node -e "const r=JSON.parse(require('fs').readFileSync('formal/model-registry.json','utf8')); console.log(r.models['formal/tla/QGSDStopHook.tla'].requirements)"` -- should show `["STOP-01", "STOP-02", "STOP-03", "STOP-04", "STOP-05", "STOP-06", "STOP-07", "SPEC-01"]`.
  </verify>
  <done>
All 22 model entries in model-registry.json have a `requirements` array. 20 models have non-empty arrays seeded from the property-to-requirement map. TUINavigation.tla and QGSDQuorum_xstate.tla have empty arrays. The test model entry is excluded. JSON is valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add formal_models to requirements.json and update aggregator (SCHEMA-04)</name>
  <files>formal/requirements.json, bin/aggregate-requirements.cjs</files>
  <action>
Two sub-steps:

**Step A: Enrich requirements.json with formal_models arrays**

Read formal/requirements.json. For each requirement that has formal model coverage (based on the inverse of the model-registry seed data from Task 1), add a `formal_models` field containing an array of model file paths.

Build the inverse map from the seed data:
- QUORUM-01 -> ["formal/tla/QGSDQuorum.tla"]
- QUORUM-02 -> ["formal/tla/QGSDQuorum.tla", "formal/alloy/quorum-votes.als"]
- QUORUM-03 -> ["formal/tla/QGSDQuorum.tla"]
- QUORUM-04 -> ["formal/tla/QGSDQuorum.tla", "formal/prism/quorum.pm"]
- SAFE-01 -> ["formal/tla/QGSDQuorum.tla", "formal/alloy/quorum-votes.als"]
- SAFE-02 -> ["formal/tla/QGSDQuorum.tla"]
- SAFE-03 -> ["formal/tla/QGSDQuorum.tla", "formal/tla/QGSDDeliberation.tla"]
- SAFE-04 -> ["formal/alloy/quorum-votes.als"]
- LOOP-01 -> ["formal/tla/QGSDQuorum.tla", "formal/prism/quorum.pm"]
- LOOP-02 -> ["formal/tla/QGSDDeliberation.tla"]
- LOOP-03 -> ["formal/tla/QGSDPreFilter.tla"]
- RECV-01 -> ["formal/tla/QGSDQuorum.tla"]
- DETECT-01..DETECT-03 -> ["formal/tla/QGSDCircuitBreaker.tla"]
- DETECT-04..DETECT-06 -> ["formal/tla/QGSDOscillation.tla"]
- ORES-01..ORES-05 -> ["formal/tla/QGSDConvergence.tla"]
- PLAN-01, PLAN-02 -> ["formal/tla/QGSDDeliberation.tla"]
- PLAN-03..PLAN-06 -> ["formal/tla/QGSDPreFilter.tla"]
- IMPR-01 -> ["formal/tla/QGSDDeliberation.tla"]
- CRED-01..CRED-06 -> ["formal/tla/QGSDAccountManager.tla"]
- CRED-07..CRED-11 -> ["formal/alloy/account-pool-structure.als"]
- CRED-12 -> ["formal/prism/oauth-rotation.pm"]
- MCPENV-01..MCPENV-03 -> ["formal/tla/QGSDMCPEnv.tla"]
- MCPENV-04 -> ["formal/prism/mcp-availability.pm"]
- FAIL-01 -> ["formal/prism/mcp-availability.pm"]
- STOP-01 -> ["formal/tla/QGSDStopHook.tla"]
- STOP-02..STOP-07 -> ["formal/tla/QGSDStopHook.tla"]
- STOP-08..STOP-11 -> ["formal/alloy/transcript-scan.als"]
- SPEC-01 -> ["formal/tla/QGSDStopHook.tla"]
- SPEC-03 -> ["formal/alloy/quorum-composition.als"]
- COMP-01 -> ["formal/alloy/quorum-composition.als"]
- SCBD-01..SCBD-04 -> ["formal/alloy/scoreboard-recompute.als"]
- SCBD-05..SCBD-07 -> ["formal/alloy/taxonomy-safety.als"]
- CALIB-01..CALIB-03 -> ["formal/alloy/availability-parsing.als"]
- INST-01..INST-05 -> ["formal/alloy/install-scope.als"]
- SLOT-02..SLOT-05 -> ["formal/tla/QGSDRecruiting.tla"]
- PRM-01 -> ["formal/prism/quorum.pm"]
- PRM-AM-01 -> ["formal/prism/oauth-rotation.pm"]

For requirements that have NO formal model coverage (e.g., ACT-*, DASH-*, KEY-*, etc.), do NOT add the `formal_models` field -- leave them unchanged so the field remains optional.

Place the `formal_models` field after the `background` field in each requirement object.

**Step B: Update aggregate-requirements.cjs to preserve formal_models**

Read bin/aggregate-requirements.cjs. The aggregator rebuilds requirements.json from milestone files. It needs to preserve the `formal_models` field during re-aggregation. Two approaches (choose the simpler one):

1. If the aggregator builds requirements from scratch: after aggregation, read the previously-saved formal_models data and merge it back into the new output.
2. If the aggregator reads existing requirements.json first: ensure `formal_models` is preserved as a pass-through field.

The key constraint: `formal_models` is enrichment data, not milestone-sourced. The aggregator must not discard it when re-running.

Validate JSON: `node -e "JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); console.log('OK')"`.
  </action>
  <verify>
Run: `node -e "const r=JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); const covered=r.requirements.filter(x=>x.formal_models&&x.formal_models.length>0); console.log('Requirements with formal_models:',covered.length); console.log('Sample:',covered[0].id,covered[0].formal_models)"`

Expected: Requirements with formal_models >= 60 (based on the inverse map above). Sample should show a valid requirement ID and model paths array.

Also verify a specific case: `node -e "const r=JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); const q=r.requirements.find(x=>x.id==='QUORUM-02'); console.log(q.formal_models)"` -- should show `["formal/tla/QGSDQuorum.tla", "formal/alloy/quorum-votes.als"]`.

Verify aggregator preserves the field: `node bin/aggregate-requirements.cjs 2>&1; node -e "const r=JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); const q=r.requirements.find(x=>x.id==='QUORUM-02'); console.log('After re-aggregate:',q.formal_models)"` -- formal_models should still be present.
  </verify>
  <done>
requirements.json contains `formal_models: string[]` for all requirements with known formal coverage (60+ requirements). Requirements without formal coverage have no `formal_models` field. The aggregator preserves `formal_models` across re-runs. JSON is valid.
  </done>
</task>

</tasks>

<verification>
1. `cat formal/model-registry.json | jq '.models | to_entries[] | select(.value.requirements | length > 0) | .key'` lists 20+ model paths
2. `cat formal/requirements.json | jq '[.requirements[] | select(.formal_models != null)] | length'` returns 60+
3. Bidirectional check: if model X claims requirement Y, then requirement Y's formal_models includes model X's path
</verification>

<success_criteria>
- model-registry.json: all 22 models have `requirements` arrays, 20 with non-empty data
- requirements.json: 60+ requirements have `formal_models` arrays with model paths
- Bidirectional consistency: model->req and req->model mappings are symmetric
- Both JSON files are valid and pass `JSON.parse()`
- Aggregator preserves formal_models across re-runs
</success_criteria>

<output>
After completion, create `.planning/phases/v0.25-01-schema-foundation/v0.25-01-01-SUMMARY.md`
</output>
