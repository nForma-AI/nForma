---
phase: v0.10-08
plan: "02"
subsystem: testing
tags: [plcy-03, map, manage-agents, bug-fix, regression-guard, dependency-injection, providers-json]
dependency_graph:
  requires:
    - phase: v0.10-08-01
      provides: Wave 0 TDD RED stubs (stub 3 intentionally RED until _pure export added)
    - phase: v0.10-05
      provides: runAutoUpdateCheck() implementation (broken), getUpdateStatuses() usage pattern
  provides:
    - PLCY-03 critical bug fixed — statuses.get(binName) with providerMap slot→binary lookup
    - getStatusesFn dependency injection for testability
    - runAutoUpdateCheck exported via module.exports._pure
    - All 3 Wave 0 regression stubs GREEN (125 pass, 0 fail)
    - v0.10-05-VERIFICATION.md and v0.10-MILESTONE-AUDIT.md updated to clear critical bug flag
  affects:
    - bin/manage-agents.cjs
    - .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md
    - .planning/v0.10-MILESTONE-AUDIT.md

tech_stack:
  added: []
  patterns:
    - "Dependency injection via default parameter (getStatusesFn = getUpdateStatuses) — backwards-compatible, enables test injection without fs mocking"
    - "providerMap build before slot loop — same pattern as listAgents(); fail-open with try/catch"
    - "Map.get(binName) with providers.json slot→binary resolution — authoritative mapping pattern"

key_files:
  created: []
  modified:
    - bin/manage-agents.cjs
    - .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md
    - .planning/v0.10-MILESTONE-AUDIT.md

key_decisions:
  - "providerMap[slot] → p.cli → path.basename() for binary name resolution — follows existing listAgents() pattern exactly, no new abstraction needed"
  - "getStatusesFn default parameter is backwards-compatible — production call await runAutoUpdateCheck() unchanged"
  - "Field name corrections (status==='update-available', .latest, .current) confirmed against getUpdateStatuses() return shape in update-agents.cjs"
  - "MILESTONE-AUDIT.md status changed to gaps_found_plcy03_fixed — PLCY-03 satisfied at code level; remaining 9 unsatisfied are PORT-01/02/03 + PRST/CRED/DASH orphans"

requirements_completed:
  - PLCY-03

metrics:
  duration: "6min"
  completed: "2026-02-25"
  tasks_completed: 2
  files_modified: 3
---

# Phase v0.10-08 Plan 02: PLCY-03 Bug Fix Summary

**PLCY-03 Map bracket notation bug fixed in runAutoUpdateCheck() — statuses.get(binName) with providers.json slot-to-binary lookup, getStatusesFn injection, _pure export, 125 tests GREEN**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-02-25T~08:00Z
- **Completed:** 2026-02-25
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Fixed `runAutoUpdateCheck()` to use `statuses.get(binName)` instead of `statuses[slot]` — the original bracket notation on a Map always returned `undefined`, causing every auto-update check to log `SKIP`
- Added `providerMap` build block using `readProvidersJson()` before the slot loop to resolve slot names to CLI binary names (same pattern as `listAgents()` lines 397-401)
- Added `getStatusesFn = getUpdateStatuses` default parameter for dependency injection — stub 3 can now inject a fake statuses Map; production code unchanged
- Corrected field name mismatches: `statusEntry.status === 'update-available'` (was `.updateAvailable`), `statusEntry.latest` (was `.latestVersion`), `statusEntry.current` (was `.currentVersion`)
- Exported `runAutoUpdateCheck` via `module.exports._pure` — turns stub 3 from RED to GREEN
- Cleared critical bug flag in `v0.10-05-VERIFICATION.md` (PLCY-03 row + fix note appended) and `v0.10-MILESTONE-AUDIT.md` (severity resolved, flow fixed, score updated 10→9 unsatisfied)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix runAutoUpdateCheck() — Map.get(binName) + provider lookup + getStatusesFn injection + field name fixes + _pure export** - `1f69cec` (fix)
2. **Task 2: Update v0.10-05-VERIFICATION.md and v0.10-MILESTONE-AUDIT.md to clear critical bug flag** - `ee0f70f` (docs)

## Files Created/Modified

- `bin/manage-agents.cjs` — Fixed `runAutoUpdateCheck()`: getStatusesFn injection, providerMap build, statuses.get(binName), field name corrections, _pure export added
- `.planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md` — PLCY-03 row updated with fix reference (commit 1f69cec); fix note appended at bottom
- `.planning/v0.10-MILESTONE-AUDIT.md` — PLCY-03 moved to satisfied_code_fix_pending_human; PLCY-03-BUG severity changed to resolved; flow status changed to fixed; score updated to 9/16 unsatisfied

## Decisions Made

- **providerMap[slot] → p.cli → path.basename()**: Binary name resolution follows the exact same pattern as `listAgents()` at lines 397-401. No new abstraction needed — consistent with existing code.
- **getStatusesFn default parameter**: `async function runAutoUpdateCheck(getStatusesFn = getUpdateStatuses)` — zero-cost backwards compatibility; production entry point call `await runAutoUpdateCheck()` is unchanged; test injection works by passing a fake function.
- **Field name corrections confirmed**: `getUpdateStatuses()` in `update-agents.cjs` returns entries with `{ current, latest, status }` fields (not `currentVersion`, `latestVersion`, `updateAvailable`). All three corrected.
- **MILESTONE-AUDIT status**: `gaps_found_plcy03_fixed` documents that the critical bug is resolved while other gaps (PORT-01/02/03 + orphaned PRST/CRED/DASH) remain. Honest accounting.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. The stub 3 test correctly turned GREEN after adding `runAutoUpdateCheck` to `_pure` — exactly as predicted by Plan 01's TDD RED design.

## Self-Check: PASSED

Files exist:
- FOUND: bin/manage-agents.cjs (modified)
- FOUND: .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md (modified)
- FOUND: .planning/v0.10-MILESTONE-AUDIT.md (modified)

Key assertions:
- `statuses.get(binName)` present in manage-agents.cjs at line 1455: FOUND
- `statuses[slot]` bracket notation: 0 occurrences (REMOVED)
- `runAutoUpdateCheck` in _pure export at line 2559: FOUND
- Test suite: 125 pass, 0 fail: CONFIRMED
- PLCY-03-BUG severity: "resolved" in MILESTONE-AUDIT.md: CONFIRMED
- "bug fixed in v0.10-08" in VERIFICATION.md: CONFIRMED

Commits exist:
- FOUND: 1f69cec — fix(v0.10-08-02): PLCY-03 runAutoUpdateCheck Map bracket notation bug
- FOUND: ee0f70f — docs(v0.10-08-02): clear PLCY-03 critical bug flag in verification docs

## Next Phase Readiness

- PLCY-03 is fully satisfied at the code level. The last unsatisfied requirement for the v0.10 Roster Toolkit milestone (at code-verification level) is resolved.
- 4 TTY interactive flows for PLCY-03 remain human_needed (unchanged from v0.10-05 scope — no regression).
- Remaining 9 unsatisfied v0.10 requirements are PORT-01/02/03 (phase not started) and PRST/CRED/DASH orphans (no VERIFICATION.md for those phases).
- v0.10-08 phase is complete.

---
*Phase: v0.10-08-plcy03-auto-update-bug-fix*
*Completed: 2026-02-25*
