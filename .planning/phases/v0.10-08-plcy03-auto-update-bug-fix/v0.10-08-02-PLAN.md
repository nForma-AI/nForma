---
phase: v0.10-08
plan: 02
type: execute
wave: 1
depends_on:
  - v0.10-08-01
files_modified:
  - bin/manage-agents.cjs
  - .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md
  - .planning/v0.10-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - PLCY-03
must_haves:
  truths:
    - "runAutoUpdateCheck() reads update status via statuses.get(binName) — not statuses[slot]"
    - "Slot-to-binary-name mapping uses providers.json lookup, matching the pattern in listAgents() at line 477-479"
    - "All 3 Wave 0 stubs from Plan 01 pass GREEN after the fix"
    - "v0.10-05-VERIFICATION.md PLCY-03 row no longer shows a critical bug — it references the fix commit"
    - "v0.10-MILESTONE-AUDIT.md PLCY-03-BUG entry is cleared from the integration issues list"
    - "Total test count is at least 125 pass, 0 fail after the fix"
  artifacts:
    - path: "bin/manage-agents.cjs"
      provides: "Fixed runAutoUpdateCheck() with getStatusesFn injection + providerMap lookup + statuses.get(binName)"
      contains: "statuses.get(binName)"
    - path: ".planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md"
      provides: "PLCY-03 bug cleared, fix commit referenced"
      contains: "PLCY-03.*bug fixed"
    - path: ".planning/v0.10-MILESTONE-AUDIT.md"
      provides: "PLCY-03 moved from unsatisfied to satisfied/human_needed"
      contains: "satisfied"
  key_links:
    - from: "bin/manage-agents.cjs runAutoUpdateCheck()"
      to: "providers.json via readProvidersJson()"
      via: "providerMap build block before slot loop"
      pattern: "providerMap\\[slot\\]"
    - from: "bin/manage-agents.cjs runAutoUpdateCheck()"
      to: "getUpdateStatuses Map"
      via: "statuses.get(binName)"
      pattern: "statuses\\.get\\(binName\\)"
---

<objective>
Fix the PLCY-03 critical bug in `runAutoUpdateCheck()`: replace `statuses[slot]` (Map bracket notation, always `undefined`) with `statuses.get(binName)` via a `providers.json` slot-to-binary lookup. Also fix co-located field name mismatches. Add `getStatusesFn` dependency injection so the integration regression guard from Plan 01 (stub 3) turns GREEN. Update the two documentation files to clear the critical bug flag.

Purpose: Close the PLCY-03 critical bug — the last unsatisfied requirement for the v0.10 Roster Toolkit milestone. After this plan, the PLCY-03 auto-update log chain is fully functional and permanently regression-guarded.
Output: Fixed `bin/manage-agents.cjs`, updated `v0.10-05-VERIFICATION.md`, updated `v0.10-MILESTONE-AUDIT.md`, ≥125 tests pass.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.10-08-plcy03-auto-update-bug-fix/v0.10-08-RESEARCH.md
@.planning/phases/v0.10-08-plcy03-auto-update-bug-fix/v0.10-08-VALIDATION.md
@.planning/phases/v0.10-08-plcy03-auto-update-bug-fix/v0.10-08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix runAutoUpdateCheck() — Map.get(binName) + provider lookup + getStatusesFn injection + field name fixes</name>
  <files>bin/manage-agents.cjs</files>
  <action>
Read `bin/manage-agents.cjs` lines 1416-1471 (`runAutoUpdateCheck` full body) before making any changes.

**Change 1 — Add getStatusesFn dependency injection to the function signature:**

Find the current function declaration:
```javascript
async function runAutoUpdateCheck() {
```
Replace with:
```javascript
async function runAutoUpdateCheck(getStatusesFn = getUpdateStatuses) {
```

This default-parameter injection is backwards-compatible — the production entry point call `await runAutoUpdateCheck()` is unchanged.

**Change 2 — Replace the internal `getUpdateStatuses()` call:**

Inside `runAutoUpdateCheck`, find the line that calls `getUpdateStatuses()` (without arguments). Replace it to use the injected function:
```javascript
// BEFORE:
const statuses = await getUpdateStatuses();
// AFTER:
const statuses = await getStatusesFn();
```

**Change 3 — Add providerMap build block before the autoSlots loop:**

Find the section just before `for (const slot of autoSlots)`. Add this block immediately before that loop (using the EXACT same pattern as `listAgents()` lines 397-401):
```javascript
// Build slot→provider map for binary name resolution (Map key is binary name, not slot name)
let providerMap = {};
try {
  const pdata = readProvidersJson();
  for (const p of (pdata.providers || [])) providerMap[p.name] = p;
} catch (_) {}
```

**Change 4 — Fix the inner loop: replace statuses[slot] with statuses.get(binName):**

Inside `for (const slot of autoSlots)`, find the BROKEN line:
```javascript
const statusEntry = statuses && statuses[slot];
```
Replace the entire status lookup and branching logic with the corrected version:
```javascript
// Resolve slot → CLI binary name (providers.json is authoritative; Map is keyed by binary name)
const p = providerMap[slot];
const binName = p && p.cli ? path.basename(p.cli) : null;
const statusEntry = binName ? statuses.get(binName) : undefined;
let status, detail;
if (!statusEntry) {
  status = 'SKIP';
  detail = binName ? 'no update info available for slot' : 'no provider record for slot';
} else if (statusEntry.error) {
  status = 'ERROR';
  detail = statusEntry.error;
} else if (statusEntry.status === 'update-available') {
  status = 'UPDATE_AVAILABLE';
  detail = statusEntry.latest || null;
} else {
  status = 'OK';
  detail = statusEntry.current || null;
}
```

Note: The original code checked `statusEntry.updateAvailable` and `statusEntry.latestVersion`/`statusEntry.currentVersion` — those field names are wrong. `getUpdateStatuses()` returns `{ current, latest, status }`. The corrected field names are `statusEntry.status === 'update-available'`, `statusEntry.latest`, `statusEntry.current`.

**Change 5 — Export runAutoUpdateCheck via _pure:**

Find the `module.exports._pure` block (around line 2518-2545). Add `runAutoUpdateCheck` to the exported object:
```javascript
module.exports._pure = {
  // ... existing exports ...
  runAutoUpdateCheck,
};
```

This allows stub 3 from Plan 01 to import it successfully and makes the function unit-testable.

**After all changes, run:**
```
node --test bin/manage-agents.test.cjs
```

Expected: All 3 Wave 0 stubs from Plan 01 now PASS. Total should be ≥125 pass, 0 fail.

If stub 3 still fails, the issue is likely: (a) `runAutoUpdateCheck` not exported via `_pure`, or (b) the stub's import path is wrong. Check the stub and fix the export.

The stub 3 from Plan 01 asserts `typeof runAutoUpdateCheck === 'function'`. After adding `runAutoUpdateCheck` to `_pure`, this assertion passes — stub 3 turns GREEN.
  </action>
  <verify>
Run: `node --test bin/manage-agents.test.cjs 2>&1 | tail -10`

Expected:
- All 3 REGRESSION PLCY-03 stubs PASS (including stub 3 which was RED in Plan 01)
- Total: ≥125 pass, 0 fail
- No test output shows "statuses[slot]" in any failing assertion

Also verify the fix is in place:
```
grep -n "statuses.get(binName)" bin/manage-agents.cjs
grep -n "statuses\[slot\]" bin/manage-agents.cjs
```
First grep must produce at least 1 result. Second grep must produce 0 results inside `runAutoUpdateCheck` (the old broken line is gone).
  </verify>
  <done>
- `runAutoUpdateCheck()` signature accepts `getStatusesFn = getUpdateStatuses` default parameter
- `providerMap` is built from `readProvidersJson()` before the slot loop, with fail-open catch block
- `statuses.get(binName)` is used — `statuses[slot]` bracket notation is gone from this function
- Field names corrected: `statusEntry.status === 'update-available'` (not `.updateAvailable`), `statusEntry.latest` (not `.latestVersion`), `statusEntry.current` (not `.currentVersion`)
- `runAutoUpdateCheck` is exported via `module.exports._pure`
- `node --test bin/manage-agents.test.cjs` shows ≥125 pass, 0 fail
  </done>
</task>

<task type="auto">
  <name>Task 2: Update v0.10-05-VERIFICATION.md and v0.10-MILESTONE-AUDIT.md to clear critical bug flag</name>
  <files>
    .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md
    .planning/v0.10-MILESTONE-AUDIT.md
  </files>
  <action>
Read both files before editing. Make targeted, minimal edits — do not reformat or restructure.

**Edit 1 — v0.10-05-VERIFICATION.md:**

In the Requirements Coverage table, find the PLCY-03 row:
```
| PLCY-03 | ROADMAP success criteria 3+4 | ... | SATISFIED (code) / NEEDS HUMAN (UI) | Entry point restructured; ... |
```

Update the Status column to indicate the bug is fixed and reference the fix commit. The commit hash is not known yet at plan time — the executor must fill in the actual commit hash from the Plan 02 / Task 1 commit.

Replace the PLCY-03 status cell with:
```
SATISFIED (code, bug fixed in v0.10-08) / NEEDS HUMAN (UI)
```

Replace the Evidence cell to add:
```
; Map bracket notation bug fixed in v0.10-08-02 (commit: <COMMIT_HASH>): statuses.get(binName) with providerMap lookup replaces statuses[slot]
```

Also update the phase header `status:` in the YAML frontmatter from `human_needed` to `human_needed` (no change — the overall status remains `human_needed` because TTY flows are still unconfirmed interactively). Add a note in the Audit Report section (near line 98-106 "Anti-Patterns Found") or at the bottom of the file:

```markdown
## v0.10-08 Fix Note

**PLCY-03 critical bug fixed (2026-02-25):** `runAutoUpdateCheck()` Map bracket notation bug at line 1445 was fixed in phase v0.10-08. The function now uses `statuses.get(binName)` with `providers.json` provider lookup. Regression test added in `bin/manage-agents.test.cjs`. All 3 Wave 0 regression stubs pass GREEN.
```

**Edit 2 — v0.10-MILESTONE-AUDIT.md:**

In the YAML frontmatter, find:
```yaml
status: gaps_found
```
Change to:
```yaml
status: gaps_found_plcy03_fixed
```
(This documents that the critical bug is fixed but other gaps remain — PRST, CRED, DASH orphaned requirements and PORT-01/02/03 not started.)

In the `gaps.requirements` array, find the PLCY-03 entry:
```yaml
    - id: "PLCY-03"
      status: "unsatisfied"
      ...
      evidence: "CRITICAL BUG at bin/manage-agents.cjs line 1445: ..."
```
Replace with:
```yaml
    - id: "PLCY-03"
      status: "satisfied_code_fix_pending_human"
      phase: "v0.10-05"
      claimed_by_plans: ["v0.10-05-01-PLAN.md", "v0.10-05-02-PLAN.md", "v0.10-05-03-PLAN.md"]
      completed_by_plans: ["v0.10-08-02-SUMMARY.md (bug fix + regression guard)"]
      verification_status: "bug_fixed"
      evidence: "Map bracket notation bug fixed in v0.10-08-02: statuses.get(binName) with providerMap lookup. Regression test in manage-agents.test.cjs (3 PLCY-03 stubs, all GREEN). Remaining: 4 TTY interactive flows require human terminal testing (unchanged from v0.10-05-VERIFICATION.md)."
```

In the `integration` array, find the `PLCY-03-BUG` entry:
```yaml
    - id: "PLCY-03-BUG"
      severity: "critical"
      ...
```
Replace with:
```yaml
    - id: "PLCY-03-BUG"
      severity: "resolved"
      location: "bin/manage-agents.cjs (was line 1445, now fixed)"
      description: "FIXED in v0.10-08-02: runAutoUpdateCheck() now uses statuses.get(binName) with providers.json providerMap lookup. Regression guard: 3 PLCY-03 stubs in manage-agents.test.cjs."
      affected_requirements: ["PLCY-03"]
```

In the `flows` array, find `PLCY-03 auto-update log chain`:
```yaml
    - name: "PLCY-03 auto-update log chain"
      status: "broken"
```
Change `status` to `"fixed"` and add:
```yaml
      fixed_in: "v0.10-08-02"
      fix_description: "statuses.get(binName) with providerMap lookup"
```

Also update the Requirements Coverage table (markdown body, around line 157-176) PLCY-03 row from:
```
| PLCY-03 | v0.10-05 | human_needed + critical bug | not checked | `[ ]` | **unsatisfied** |
```
to:
```
| PLCY-03 | v0.10-05 | human_needed (bug fixed v0.10-08) | not checked | `[ ]` | **satisfied (code) / human_needed (UI)** |
```

Finally, update the phase-level Requirements Coverage score comment at line ~176:
Change `10/16 unsatisfied` references to reflect that PLCY-03 has moved from `unsatisfied` to `satisfied_code`. The score is now `9/16 unsatisfied` (PORT-01/02/03 + PRST-01/02 + CRED-01/02 + DASH-01/02/03 = 10 minus PLCY-03 = 9).
  </action>
  <verify>
1. Read `.planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md` — confirm PLCY-03 row contains "bug fixed in v0.10-08" and the commit hash is present.
2. Read `.planning/v0.10-MILESTONE-AUDIT.md` — confirm:
   - PLCY-03-BUG entry has `severity: "resolved"` (not `"critical"`)
   - PLCY-03 requirement entry has `status: "satisfied_code_fix_pending_human"` (not `"unsatisfied"`)
   - PLCY-03 flow has `status: "fixed"` (not `"broken"`)
  </verify>
  <done>
- `v0.10-05-VERIFICATION.md` PLCY-03 row references the fix commit and no longer shows a critical bug
- `v0.10-MILESTONE-AUDIT.md` PLCY-03 is moved from unsatisfied to satisfied_code, PLCY-03-BUG severity changed to resolved, flow status changed to fixed
- Both files are committed as part of the Plan 02 commit
  </done>
</task>

</tasks>

<verification>
After both tasks are complete:
1. `node --test bin/manage-agents.test.cjs` — ≥125 pass, 0 fail
2. `grep -n "statuses.get(binName)" bin/manage-agents.cjs` — shows at least 1 match in `runAutoUpdateCheck`
3. `grep -n "statuses\[slot\]" bin/manage-agents.cjs` — shows 0 matches (bug line removed)
4. `grep "runAutoUpdateCheck" bin/manage-agents.cjs | grep "_pure"` — confirms the function is exported
5. `grep "resolved" .planning/v0.10-MILESTONE-AUDIT.md` — confirms PLCY-03-BUG is resolved
6. `grep "bug fixed" .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md` — confirms VERIFICATION.md updated
</verification>

<success_criteria>
Phase v0.10-08 is complete when:
- `runAutoUpdateCheck()` uses `statuses.get(binName)` with providers.json providerMap lookup
- `getStatusesFn` dependency injection is in place (production call unchanged)
- Field name mismatches corrected: `status === 'update-available'`, `latest`, `current`
- `runAutoUpdateCheck` exported via `module.exports._pure`
- All 3 PLCY-03 Wave 0 regression stubs pass GREEN
- Total tests: ≥125 pass, 0 fail
- `v0.10-05-VERIFICATION.md` PLCY-03 row references fix commit
- `v0.10-MILESTONE-AUDIT.md` PLCY-03-BUG severity is "resolved", flow status is "fixed"
</success_criteria>

<output>
After completion, create `.planning/phases/v0.10-08-plcy03-auto-update-bug-fix/v0.10-08-02-SUMMARY.md`
</output>
