---
phase: v0.10-08
plan: "01"
subsystem: testing
tags: [regression, tdd, plcy-03, map, manage-agents]
dependency_graph:
  requires: []
  provides: [PLCY-03-wave0-regression-stubs]
  affects: [bin/manage-agents.test.cjs]
tech_stack:
  added: []
  patterns: [node:test regression stubs, RED-GREEN TDD, dependency injection]
key_files:
  created: []
  modified:
    - bin/manage-agents.test.cjs
decisions:
  - "Stub 3 uses _pure.runAutoUpdateCheck existence check as RED trigger — simplest assertion that fails before Plan 02 adds the export"
  - "Test total is 125 (not 124+1=125 as stated in plan prose) — plan count was consistent, test runner reports 125 total = 122 + 3 stubs"
metrics:
  duration: "49s"
  completed: "2026-02-25"
  tasks_completed: 1
  files_modified: 1
requirements_satisfied: [PLCY-03]
---

# Phase v0.10-08 Plan 01: PLCY-03 Wave 0 Regression Stubs Summary

Wave 0 TDD RED phase: three regression test stubs added to `bin/manage-agents.test.cjs` documenting the PLCY-03 Map bracket notation bug and proving the fix direction before any implementation change.

## What Was Built

Three test cases appended after existing v0.10-07 stubs in `bin/manage-agents.test.cjs`:

1. **Stub 1 — Bug proof (PASSES):** Demonstrates that `Map['key']` always returns `undefined`, proving the original broken code path in `runAutoUpdateCheck()`.

2. **Stub 2 — Fix proof (PASSES):** Demonstrates that `Map.get('key')` correctly retrieves the entry, documenting the correct API to use after the fix.

3. **Stub 3 — Integration regression guard (FAILS — RED):** Attempts to destructure `runAutoUpdateCheck` from `_pure` and asserts it is a function. Since `_pure` does not yet export `runAutoUpdateCheck`, the assertion fails with `AssertionError: runAutoUpdateCheck must be exported via _pure`.

## Test Run Results

```
tests 125
pass  124
fail    1   (stub 3 — intentional RED)
```

- Baseline 122 pre-existing tests: all pass, no regression.
- Stub 1 + Stub 2: PASS (document bug and fix direction).
- Stub 3: FAILS (RED state confirmed — will turn GREEN after Plan 02 adds `_pure.runAutoUpdateCheck`).

## Deviations from Plan

None — plan executed exactly as written.

Note: The plan mentions "124 pass, 1 fail" as the expected outcome. The actual test runner reports "tests 125, pass 124, fail 1" — the difference is that `tests` counts total (125 = 122 + 3 stubs), while the plan's "124" referred to passing tests only. The outcome is correct.

## Self-Check: PASSED

Files exist:
- FOUND: bin/manage-agents.test.cjs (modified, 28 lines added)

Commits exist:
- FOUND: 4951f47 — test(v0.10-08-01): add Wave 0 regression stubs for PLCY-03 Map bracket notation bug
