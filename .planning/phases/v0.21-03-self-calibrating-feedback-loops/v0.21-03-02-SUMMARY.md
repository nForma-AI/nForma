---
plan: v0.21-03-02
phase: v0.21-03
status: complete
completed: 2026-03-01
---

# Summary: v0.21-03-02 — LOOP-01: export-prism-constants pre-step

## What Was Built

Wired `export-prism-constants.cjs` as a fail-open pre-step in `bin/run-prism.cjs`. The pre-step runs before PRISM is invoked, ensuring `rates.const` is always refreshed from the scoreboard.

## Key Files

### Modified
- `bin/run-prism.cjs` — added LOOP-01 pre-step block (lines 147-166) using `spawnSync(process.execPath, [exportConstantsPath], { cwd: process.cwd(), timeout: 10000 })`
- `bin/run-prism.test.cjs` — fixed LOOP-01 test fixture to use JSON scoreboard with 30+ rounds and `PRISM_BIN=prism` (PATH sentinel to bypass binary existence check)

## Notable Deviations

The Plan 01 LOOP-01 test fixture used `quorum-scoreboard.md` (wrong format). The `export-prism-constants.cjs` script reads `.planning/quorum-scoreboard.json`. Fixed the test to use a proper JSON scoreboard with 30+ rounds (above the MIN_ROUNDS=30 threshold) so the pre-step writes real data to `rates.const`.

The test also needed `PRISM_BIN=prism` (not `/nonexistent/prism`) because `run-prism.cjs` performs a binary existence check and exits before reaching the pre-step when a non-existent path is provided.

## Verification

```
node --test bin/run-prism.test.cjs → 19/19 tests pass, LOOP-01 GREEN
grep -n "export-prism-constants" bin/run-prism.cjs → 3 lines (comment, path, spawnSync call)
```

## Self-Check: PASSED
