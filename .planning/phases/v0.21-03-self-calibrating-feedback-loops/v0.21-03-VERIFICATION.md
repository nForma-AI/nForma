---
phase: v0.21-03
status: PASSED
verified: 2026-03-01
---

# Verification: v0.21-03 — Self-Calibrating Feedback Loops

## Goal

The formal verification pipeline updates its own inputs from what it observes — PRISM calibrates from scoreboard, specs regenerate from XState changes, and debug sessions produce invariant candidates.

## Success Criteria Verification

### SC1 — LOOP-01: run-prism.cjs always uses latest scoreboard rates

**PASS**

- `bin/run-prism.cjs` line 147: `// ── LOOP-01: export-prism-constants pre-step` block present
- Pre-step calls `export-prism-constants.cjs` via `spawnSync` before PRISM is invoked
- Test confirms `rates.const` is written before PRISM is invoked (fail: if pre-step absent)
- Evidence: `node --test bin/run-prism.test.cjs` → LOOP-01 test GREEN

### SC2 — LOOP-02: PostToolUse hook auto-regenerates TLA+/Alloy specs on XState machine changes

**PASS**

- `hooks/qgsd-spec-regen.js` exists (PostToolUse hook, fail-open)
- Watches for `tool_name === 'Write'` + `file_path.includes('qgsd-workflow.machine.ts')`
- Registered in `~/.claude/settings.json` PostToolUse array: `node "~/.claude/hooks/qgsd-spec-regen.js"`
- `bin/install.js` has registration in both install and uninstall paths (4 occurrences)
- Evidence: `node --test hooks/qgsd-spec-regen.test.js` → 4/4 GREEN

### SC3 — LOOP-03: Empirical rate deviation triggers PRISM re-run; CI fails on new threshold violation

**PASS**

- `bin/sensitivity-sweep-feedback.cjs` exists (194 lines)
- `DEVIATION_THRESHOLD = 0.05` named constant
- Algorithm: reads `formal/sensitivity-report.ndjson`, computes empirical rate from `quorum-scoreboard.md`, finds nearest tested value, exits 1 (CI fail) when flip-to-fail record within 2×threshold of empirical rate
- Evidence: `node --test bin/sensitivity-sweep-feedback.test.cjs` → 3/3 GREEN

### SC4 — LOOP-04: Post-session TLA+ PROPERTY candidates proposed from debug state transitions

**PASS**

- `bin/propose-debug-invariants.cjs` exists (165 lines)
- Three mining heuristics: state transition patterns → `PROPERTY TransitionHint_*`, `root_cause:` lines → `INVARIANT RootCauseHint_*`, invariant candidate lines → `PROPERTY InvariantCandidate_N`
- `--non-interactive` mode: prints all candidates, exits 0 (CI safe)
- `~/.claude/commands/qgsd/debug.md` Step H added at line 166, instructs running `propose-debug-invariants.cjs` after each debug session
- Evidence: `node --test bin/propose-debug-invariants.test.cjs` → 3/3 GREEN

## Test Suite Results

```
node --test bin/run-prism.test.cjs bin/sensitivity-sweep-feedback.test.cjs \
  bin/propose-debug-invariants.test.cjs hooks/qgsd-spec-regen.test.js

tests  32
pass   32
fail    0
```

All 32 tests GREEN.

## Verdict: PASSED

All four LOOPs implemented, tested, and verified. The formal verification pipeline is now self-calibrating.
