---
plan: v0.21-03-03
phase: v0.21-03
status: complete
completed: 2026-03-01
---

# Summary: v0.21-03-03 — LOOP-02: qgsd-spec-regen PostToolUse Hook

## What Was Built

PostToolUse hook that auto-regenerates TLA+/Alloy specs whenever `qgsd-workflow.machine.ts` is written. Eliminates manual `generate-formal-specs.cjs` runs after XState machine edits.

## Key Files

### Created
- `hooks/qgsd-spec-regen.js` — PostToolUse hook, exits 0 always (fail-open). Checks `tool_name === 'Write'` and `file_path.includes('qgsd-workflow.machine.ts')`. Runs `generate-formal-specs.cjs` via spawnSync. Returns `hookSpecificOutput.additionalContext` with result message.
- `hooks/dist/qgsd-spec-regen.js` — identical copy (gitignored, synced via cp)

### Modified
- `bin/install.js` — added spec-regen registration in install path (line ~1887) and uninstall path (line ~1184)

## Activation
- Hook file installed to `~/.claude/hooks/qgsd-spec-regen.js`
- Registered in `~/.claude/settings.json` PostToolUse array

## Notable Deviations

The `installForClaude` function writes to `~/.claude/settings.json` (not `~/.claude.json`). Verification command updated to use the correct file path.

## Verification

```
node --test hooks/qgsd-spec-regen.test.js → 4/4 tests GREEN
grep -c "qgsd-spec-regen" bin/install.js → 4 (install + uninstall x2 each)
ls ~/.claude/hooks/qgsd-spec-regen.js → exists
qgsd-spec-regen registered: true (in settings.json PostToolUse)
```

## Self-Check: PASSED
