---
plan: v0.20-06-02
status: complete
wave: 1
type: execute
completed: 2026-02-28
---

# Summary: v0.20-06-02 — Implement generate-triage-bundle.cjs

## What was built

Created `bin/generate-triage-bundle.cjs` — the triage bundle generator that satisfies TRIAGE-01.
Reads `formal/check-results.ndjson`, writes `formal/diff-report.md` and `formal/suspects.md`.

## Key files

### Created
- `bin/generate-triage-bundle.cjs` — triage bundle generator (253 lines)

## Key exports

- `parseCurrentNDJSON()` — reads NDJSON from cwd/formal/check-results.ndjson (fail-open)
- `generateSuspects(results)` — filters result=fail OR non-empty triage_tags, sorted by priority
- `computeDeltas(currentResults, previousSnapshot)` — detects transitioned, new, removed checks
- `loadPreviousSnapshot()` — extracts embedded JSON block from diff-report.md
- `buildCurrentSnapshot(currentResults)` — builds check_id->result map for next-run embedding
- `formatDiffReport(currentResults, deltas, isFirstRun)` — full markdown diff report
- `formatSuspectsReport(suspects)` — prioritized suspects markdown

## Tasks

| # | Task | Status |
|---|------|--------|
| 1 | Implement generate-triage-bundle.cjs — parse, suspects, diff, snapshot | ✓ Done |

## Verification

```
node --test bin/generate-triage-bundle.test.cjs 2>&1 | grep "pass\|fail"
```

Result: 6 pass, 1 fail — tests 1-6 GREEN, test 7 RED (awaiting Plan 03 STEPS wiring).

```
node bin/generate-triage-bundle.cjs
```

Result: formal/diff-report.md and formal/suspects.md created. First-run note present.

## Self-Check: PASSED

6/7 tests GREEN. Test 7 correctly remains RED. No regressions in npm test.
TRIAGE-01 requirement satisfied.
