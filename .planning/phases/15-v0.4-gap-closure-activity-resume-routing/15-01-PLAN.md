---
phase: 15-v0.4-gap-closure-activity-resume-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/oscillation-resolution-mode.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md
  - get-shit-done/workflows/resume-project.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [ACT-02, ACT-04]
gap_closure: true

must_haves:
  truths:
    - "When oscillation resolution mode writes circuit_breaker activity, the JSON payload includes a phase field so resume-project can render /qgsd:execute-phase {phase} with a real phase number"
    - "When a new-milestone workflow is interrupted during researching, resume-project routes to /qgsd:new-milestone (not /qgsd:plan-phase)"
    - "When a new-milestone workflow is interrupted during creating_roadmap, resume-project routes to /qgsd:new-milestone"
    - "The existing researching row in resume-project.md is unambiguously scoped to plan_phase activity to prevent false matches"
    - "ACT-02 and ACT-04 are marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "get-shit-done/workflows/oscillation-resolution-mode.md"
      provides: "circuit_breaker activity-set calls with phase field at Steps 4 and 5"
      contains: '"phase":"${PHASE_NUMBER}"'
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md"
      provides: "installed copy with same phase field addition"
      contains: '"phase":"${PHASE_NUMBER}"'
    - path: "get-shit-done/workflows/resume-project.md"
      provides: "routing table with new_milestone rows and disambiguated researching row"
      contains: "researching (activity=plan_phase)"
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md"
      provides: "installed copy with same routing table fixes"
      contains: "researching (activity=new_milestone)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ACT-02 and ACT-04 marked [x] complete"
      contains: "[x] **ACT-02**"
  key_links:
    - from: "oscillation-resolution-mode.md Step 4 activity-set"
      to: "resume-project.md oscillation_diagnosis row"
      via: '"phase":"${PHASE_NUMBER}"'
      pattern: 'activity.*circuit_breaker.*phase.*PHASE_NUMBER'
    - from: "new-milestone.md researching activity-set"
      to: "resume-project.md researching (activity=new_milestone) row"
      via: "activity=new_milestone disambiguation"
      pattern: "researching .activity=new_milestone."
---

<objective>
Close two ACT-02 and ACT-04 gaps found in the v0.4 milestone audit — surgical edits to two workflow markdown files (source + installed copies) plus REQUIREMENTS.md checkbox updates.

Purpose: Two E2E flows are currently broken: (1) context reset during oscillation resolution renders `/qgsd:execute-phase undefined` because the circuit_breaker activity-set calls omit `phase`; (2) context reset during new-milestone workflow routes the user to `/qgsd:plan-phase` instead of `/qgsd:new-milestone` because the routing table has no new_milestone rows.

Output: Four patched workflow files (2 source, 2 installed) and updated REQUIREMENTS.md + ROADMAP.md.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-v0.4-gap-closure-activity-resume-routing/15-RESEARCH.md
@.planning/v0.4-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase field to oscillation-resolution-mode.md activity-set calls (source + installed)</name>
  <files>
    get-shit-done/workflows/oscillation-resolution-mode.md
    /Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md
  </files>
  <action>
    Edit TWO files — source and installed copy. Both files need identical JSON payload changes (only the gsd-tools.cjs path differs between source and installed; the JSON payload edit is the same).

    **Source file:** `/Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md`
    **Installed file:** `/Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md`

    **Edit 1 — Step 4 (line ~65 in source):**
    Find the activity-set call in the `quorum_diagnosis` step (Step 4). The current JSON payload is:
    ```
    "{\"activity\":\"circuit_breaker\",\"sub_activity\":\"oscillation_diagnosis\"}"
    ```
    Change it to:
    ```
    "{\"activity\":\"circuit_breaker\",\"sub_activity\":\"oscillation_diagnosis\",\"phase\":\"${PHASE_NUMBER}\"}"
    ```

    **Edit 2 — Step 5 (line ~93 in source):**
    Find the activity-set call in the `on_consensus` step (Step 5). The current JSON payload is:
    ```
    "{\"activity\":\"circuit_breaker\",\"sub_activity\":\"awaiting_approval\"}"
    ```
    Change it to:
    ```
    "{\"activity\":\"circuit_breaker\",\"sub_activity\":\"awaiting_approval\",\"phase\":\"${PHASE_NUMBER}\"}"
    ```

    Apply BOTH edits to BOTH files (4 total edits). The installed file uses absolute path `/Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs` in the bash call — the JSON payload edit is identical regardless of path format.

    Do NOT change anything else in these files.
  </action>
  <verify>
    Run grep to confirm both payloads updated in both files:
    ```bash
    grep -n 'PHASE_NUMBER' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
    grep -n 'PHASE_NUMBER' /Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md
    ```
    Both commands must return exactly 2 lines each — one for `oscillation_diagnosis` and one for `awaiting_approval`.
  </verify>
  <done>
    Both oscillation-resolution-mode.md files (source and installed) contain `"phase":"${PHASE_NUMBER}"` in both the Step 4 and Step 5 activity-set JSON payloads. grep returns 2 matches per file (4 total).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix resume-project.md routing table — add new_milestone rows and disambiguate researching row (source + installed)</name>
  <files>
    get-shit-done/workflows/resume-project.md
    /Users/jonathanborduas/.claire/qgsd/workflows/resume-project.md
  </files>
  <action>
    Edit TWO files — source and installed copy. The routing table content is identical between them (path substitution does not affect table rows).

    **Source file:** `/Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md`
    **Installed file:** `/Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md`

    **Edit 1 — Disambiguate the existing `researching` row:**
    Find this row in the routing table:
    ```
    | researching | `/qgsd:plan-phase {phase}` — researcher was running, re-trigger with --research flag |
    ```
    Change it to:
    ```
    | researching (activity=plan_phase) | `/qgsd:plan-phase {phase}` — researcher was running, re-trigger with --research flag |
    ```

    **Edit 2 — Add new_milestone routing rows:**
    After the `quorum` row and BEFORE the `oscillation_diagnosis` row, insert these two new rows:
    ```
    | researching (activity=new_milestone) | `/qgsd:new-milestone` — milestone research was running |
    | creating_roadmap | `/qgsd:new-milestone` — roadmapper was spawning |
    ```

    The resulting routing table section should look like (order matters for disambiguation — plan_phase row must come before new_milestone row):
    ```
    | researching (activity=plan_phase) | `/qgsd:plan-phase {phase}` — researcher was running, re-trigger with --research flag |
    | planning | `/qgsd:plan-phase {phase}` — planner was running, re-trigger plan-phase |
    | checking_plan | `/qgsd:plan-phase {phase}` — checker was running, re-trigger plan-phase |
    | quorum | `/qgsd:plan-phase {phase}` — QUORUM was in progress (round {quorum_round}), re-trigger plan-phase |
    | researching (activity=new_milestone) | `/qgsd:new-milestone` — milestone research was running |
    | creating_roadmap | `/qgsd:new-milestone` — roadmapper was spawning |
    | oscillation_diagnosis | `/qgsd:execute-phase {phase}` — oscillation resolution quorum was running |
    | awaiting_approval | `/qgsd:execute-phase {phase}` — unified solution is ready, user approval is needed |
    | executing | `/qgsd:quick` — quick task execution was in progress |
    | planning (activity=quick) | `/qgsd:quick` — quick task planning was in progress |
    ```

    Apply BOTH edits to BOTH files. Do NOT change anything else.

    NOTE: `creating_roadmap` row intentionally omits `{phase}` — new-milestone.md writes this sub_activity BEFORE any phase has been defined, so the phase field is absent from the stored activity payload.
  </action>
  <verify>
    Run grep to confirm all changes in both files:
    ```bash
    grep -n 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    grep -n 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
    ```
    Each command must return 3 lines: one for `researching (activity=plan_phase)`, one for `researching (activity=new_milestone)`, one for `creating_roadmap`.

    Also confirm the old bare `| researching |` row is gone:
    ```bash
    grep -n '^| researching |' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    ```
    Must return 0 matches.
  </verify>
  <done>
    Both resume-project.md files (source and installed) have: (1) `researching (activity=plan_phase)` row pointing to /qgsd:plan-phase, (2) `researching (activity=new_milestone)` row pointing to /qgsd:new-milestone, (3) `creating_roadmap` row pointing to /qgsd:new-milestone. The bare `| researching |` row no longer exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark ACT-02 and ACT-04 complete in REQUIREMENTS.md; update ROADMAP.md Phase 15 plan list</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
    **REQUIREMENTS.md edits:**

    1. Find the ACT-02 checkbox (currently `[ ]`):
    ```
    - [ ] **ACT-02**: Activity schema: `{ activity, sub_activity, phase?, plan?, wave?, debug_round?, checkpoint?, quorum_round?, updated }` — all fields except `activity` and `updated` are optional; unknown fields are preserved
    ```
    Change `[ ]` to `[x]`.

    2. Find the ACT-04 checkbox (currently `[ ]`):
    ```
    - [ ] **ACT-04**: `resume-work` reads current-activity.json and routes to the exact recovery point — displaying the interrupted state before resuming execution
    ```
    Change `[ ]` to `[x]`.

    3. Find the traceability table entry for ACT-02:
    ```
    | ACT-02 | Phase 15 (gap closure for Phase 14) | Pending |
    ```
    Change `Pending` to `Complete`.

    4. Find the traceability table entry for ACT-04:
    ```
    | ACT-04 | Phase 15 (gap closure for Phase 14) | Pending |
    ```
    Change `Pending` to `Complete`.

    5. Update the pending count in the coverage summary at the bottom. Find:
    ```
    - Pending (awaiting verification): 20 (v0.2: ORES-01..05 + v0.3: RLS-01..04 + v0.4: ACT-02, ACT-04)
    ```
    Change to:
    ```
    - Pending (awaiting verification): 18 (v0.2: ORES-01..05 + v0.3: RLS-01..04)
    ```

    **ROADMAP.md edits:**

    Find the Phase 15 Plans list (currently showing `0/1 planned`):
    ```
    Plans:
    - [ ] 15-01-PLAN.md — Fix oscillation-resolution-mode.md (add phase field to circuit_breaker activity-set calls at Step 4 and Step 5) + fix resume-project.md routing table (add new_milestone rows; note oscillation_diagnosis/awaiting_approval phase limitation)
    ```

    The plan file has been created. Update the plans count in the Phase 15 progress table from `0/1 planned` to `1/1 planned` and mark in-progress:
    Find in the progress table:
    ```
    | 15. v0.4 Gap Closure — Activity Resume Routing | 0/1 planned | Planned | — |
    ```
    Change to:
    ```
    | 15. v0.4 Gap Closure — Activity Resume Routing | 1/1 planned | In Progress | — |
    ```
  </action>
  <verify>
    ```bash
    grep -n 'ACT-02\|ACT-04' /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep -E '\[x\]|Complete'
    ```
    Must return 4 lines: both `[x]` checkbox lines and both `Complete` traceability rows.
  </verify>
  <done>
    REQUIREMENTS.md has ACT-02 and ACT-04 marked `[x]` in both the checkbox section and the traceability table with status `Complete`. ROADMAP.md Phase 15 progress table updated to `1/1 planned` and `In Progress`.
  </done>
</task>

<task type="checkpoint:verify">
  <name>Task 4: Automated verification — confirm all 4 file pairs patched correctly</name>
  <files>
    get-shit-done/workflows/oscillation-resolution-mode.md
    /Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md
    get-shit-done/workflows/resume-project.md
    /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
    .planning/REQUIREMENTS.md
  </files>
  <action>Run /qgsd:quorum-test to verify all edits from Tasks 1-3 are correct. Use the grep commands in the verify section to check each file pair. If any check fails, enter /qgsd:debug loop (capped at 3 rounds) per R1 checkpoint:verify definition.</action>
  <what-built>
    Three auto tasks made 8 total file edits:
    - oscillation-resolution-mode.md source: 2 JSON payload additions
    - oscillation-resolution-mode.md installed: 2 JSON payload additions
    - resume-project.md source: 1 rename + 2 row insertions
    - resume-project.md installed: 1 rename + 2 row insertions
    - REQUIREMENTS.md: 4 checkbox/status changes
    - ROADMAP.md: 1 progress table update
  </what-built>
  <verify>
    Run the following verification checks using /qgsd:quorum-test with these must_haves:

    **ACT-02 Fix verification (oscillation-resolution-mode.md):**
    ```bash
    # Source copy — must have exactly 2 PHASE_NUMBER occurrences in activity-set calls
    grep -c 'PHASE_NUMBER' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
    # Expected: 2

    # Installed copy — same check
    grep -c 'PHASE_NUMBER' /Users/jonathanborduas/.claude/qgsd/workflows/oscillation-resolution-mode.md
    # Expected: 2

    # Confirm oscillation_diagnosis has phase field
    grep 'oscillation_diagnosis.*PHASE_NUMBER\|PHASE_NUMBER.*oscillation_diagnosis' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
    # Must return 1 match

    # Confirm awaiting_approval has phase field
    grep 'awaiting_approval.*PHASE_NUMBER\|PHASE_NUMBER.*awaiting_approval' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
    # Must return 1 match
    ```

    **ACT-04 Fix verification (resume-project.md):**
    ```bash
    # Source copy — must have all 3 new/renamed rows
    grep -c 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    # Expected: 3

    # Installed copy — same check
    grep -c 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
    # Expected: 3

    # Bare researching row must be gone from source
    grep -c '^| researching |' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    # Expected: 0

    # new-milestone rows point to correct command
    grep 'new_milestone.*new-milestone\|creating_roadmap.*new-milestone' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
    # Must return 2 matches
    ```

    **REQUIREMENTS.md verification:**
    ```bash
    grep '\[x\].*ACT-02\|\[x\].*ACT-04' /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
    # Must return 2 lines (both checkboxes marked [x])
    ```

    If any check fails, enter /qgsd:debug loop (capped at 3 rounds) to fix the specific failing check.
  </verify>
  <done>All grep counts return expected values: oscillation-resolution-mode.md source and installed each have 2 PHASE_NUMBER matches; resume-project.md source and installed each have 3 disambiguation row matches; REQUIREMENTS.md has 2 [x] matches for ACT-02 and ACT-04.</done>
  <resume-signal>quorum-test PASS — all verification checks return expected counts</resume-signal>
</task>

</tasks>

<verification>
Phase 15 is complete when:
1. `grep -c 'PHASE_NUMBER' get-shit-done/workflows/oscillation-resolution-mode.md` returns 2
2. `grep -c 'PHASE_NUMBER' ~/.claude/qgsd/workflows/oscillation-resolution-mode.md` returns 2
3. `grep -c 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' get-shit-done/workflows/resume-project.md` returns 3
4. `grep -c 'activity=plan_phase\|activity=new_milestone\|creating_roadmap' ~/.claude/qgsd/workflows/resume-project.md` returns 3
5. `grep '\[x\].*ACT-02\|\[x\].*ACT-04' .planning/REQUIREMENTS.md` returns 2 lines
6. E2E flow test: `cat` a simulated activity JSON `{"activity":"circuit_breaker","sub_activity":"oscillation_diagnosis","phase":"15-v0.4-gap-closure"}` — resume-project routing table row `oscillation_diagnosis` now renders `/qgsd:execute-phase 15-v0.4-gap-closure` (not `undefined`)
7. E2E flow test: simulated activity `{"activity":"new_milestone","sub_activity":"researching"}` — resume-project routes to `/qgsd:new-milestone` (not `/qgsd:plan-phase`)
</verification>

<success_criteria>
Both broken E2E flows from the v0.4 audit are fixed:
- Flow 1 (oscillation resolution context reset): `/qgsd:execute-phase {phase}` renders with a real phase number, not `undefined`
- Flow 2 (new-milestone context reset): user is routed to `/qgsd:new-milestone`, not `/qgsd:plan-phase`
ACT-02 and ACT-04 requirements are marked complete in REQUIREMENTS.md.
All 4 workflow files (source + installed copies of both) reflect the changes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-v0.4-gap-closure-activity-resume-routing/15-01-SUMMARY.md`
</output>
