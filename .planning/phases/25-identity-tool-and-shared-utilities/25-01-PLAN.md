---
phase: 25-identity-tool-and-shared-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/claude-mcp-server/src/constants.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/constants.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/constants.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/src/constants.ts
autonomous: true
requirements:
  - STD-08

must_haves:
  truths:
    - "All 4 Gen1-ported repos have src/constants.ts with SERVER_NAME, LOG_PREFIX, ERROR_MESSAGES, STATUS_MESSAGES, and PROTOCOL constants"
    - "No circular imports — constants.ts re-exports from types.ts but types.ts never imports from constants.ts"
    - "npm run build succeeds in all 4 repos after constants.ts is added"
  artifacts:
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/constants.ts"
      provides: "Server identity constants for claude-mcp-server"
      contains: "SERVER_NAME"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/constants.ts"
      provides: "Server identity constants for codex-mcp-server"
      contains: "SERVER_NAME"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/constants.ts"
      provides: "Server identity constants for copilot-mcp-server"
      contains: "SERVER_NAME"
    - path: "/Users/jonathanborduas/code/openhands-mcp-server/src/constants.ts"
      provides: "Server identity constants for openhands-mcp-server"
      contains: "SERVER_NAME"
  key_links:
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/constants.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/types.ts"
      via: "re-export of ToolArguments"
      pattern: "export.*from.*types"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/constants.ts"
      via: "LOG_PREFIX import (used in Plan 02)"
      pattern: "LOG_PREFIX.*from.*constants"
---

<objective>
Create src/constants.ts in the 4 Gen1-ported repos (claude, codex, copilot, openhands) that currently lack it. These constants files eliminate magic strings and are the prerequisite for the Logger utility (Plan 02) and the identity tool SERVER_NAME reference (Plan 03).

Purpose: STD-08 requires all 6 repos to have constants.ts. gemini and opencode already have it. This plan creates it for the 4 remaining repos. The Logger in Plan 02 imports LOG_PREFIX from constants.ts — constants must exist first.

Output: 4 new constants.ts files, builds passing in all 4 repos.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-identity-tool-and-shared-utilities/25-RESEARCH.md

# Canonical reference for constants.ts structure (copy pattern from here):
@/Users/jonathanborduas/code/opencode-mcp-server/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.ts for claude-mcp-server and codex-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/src/constants.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/constants.ts
  </files>
  <action>
Read the opencode-mcp-server canonical reference at `/Users/jonathanborduas/code/opencode-mcp-server/src/constants.ts` to understand the full structure before writing.

Read `/Users/jonathanborduas/code/claude-mcp-server/src/types.ts` to confirm what ToolArguments and model constants are exported from there.
Read `/Users/jonathanborduas/code/codex-mcp-server/src/types.ts` similarly.

Create `/Users/jonathanborduas/code/claude-mcp-server/src/constants.ts`:
- `export { ToolArguments } from './types.js';`
- `export const SERVER_NAME = 'claude-mcp-server' as const;`
- `export const LOG_PREFIX = '[CMCP]';`
- `export const ERROR_MESSAGES = { TOOL_NOT_FOUND: 'not found in registry', NO_PROMPT_PROVIDED: 'Please provide a prompt.' } as const;`
- `export const STATUS_MESSAGES = { PROCESSING_START: 'Starting Claude analysis...', PROCESSING_COMPLETE: 'Analysis completed successfully' } as const;`
- `export const PROTOCOL = { ROLES: { USER: 'user', ASSISTANT: 'assistant' }, CONTENT_TYPES: { TEXT: 'text' }, STATUS: { SUCCESS: 'success', ERROR: 'error', FAILED: 'failed', REPORT: 'report' }, NOTIFICATIONS: { PROGRESS: 'notifications/progress' }, KEEPALIVE_INTERVAL: 25000 } as const;`

CRITICAL: Do NOT import anything from constants.ts back into types.ts — one-way dependency only (constants.ts → types.ts).

Create `/Users/jonathanborduas/code/codex-mcp-server/src/constants.ts` using the same structure:
- `export { ToolArguments } from './types.js';`
- `export const SERVER_NAME = 'codex-mcp-server' as const;`
- `export const LOG_PREFIX = '[CDMCP]';`
- Same ERROR_MESSAGES, STATUS_MESSAGES, PROTOCOL shape (adapt message text for codex: "Starting Codex analysis...", etc.)

Run `npm run build` in both repos to confirm no TypeScript errors:
- `cd /Users/jonathanborduas/code/claude-mcp-server && npm run build`
- `cd /Users/jonathanborduas/code/codex-mcp-server && npm run build`
  </action>
  <verify>
`cd /Users/jonathanborduas/code/claude-mcp-server && npm run build` exits 0.
`cd /Users/jonathanborduas/code/codex-mcp-server && npm run build` exits 0.
`grep -l "SERVER_NAME" /Users/jonathanborduas/code/claude-mcp-server/src/constants.ts /Users/jonathanborduas/code/codex-mcp-server/src/constants.ts` returns both paths.
`grep "import.*constants" /Users/jonathanborduas/code/claude-mcp-server/src/types.ts` returns nothing (no circular import).
  </verify>
  <done>
Both constants.ts files exist with SERVER_NAME, LOG_PREFIX, and PROTOCOL. Both repos build successfully. types.ts does not import from constants.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create constants.ts for copilot-mcp-server and openhands-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/copilot-mcp-server/src/constants.ts
    /Users/jonathanborduas/code/openhands-mcp-server/src/constants.ts
  </files>
  <action>
Read `/Users/jonathanborduas/code/copilot-mcp-server/src/types.ts` to understand what ToolArguments is exported from there.
Read `/Users/jonathanborduas/code/openhands-mcp-server/src/types.ts` similarly.

Create `/Users/jonathanborduas/code/copilot-mcp-server/src/constants.ts`:
- `export { ToolArguments } from './types.js';`
- `export const SERVER_NAME = 'copilot-mcp-server' as const;`
- `export const LOG_PREFIX = '[CPMCP]';`
- `export const ERROR_MESSAGES = { TOOL_NOT_FOUND: 'not found in registry', NO_PROMPT_PROVIDED: 'Please provide a prompt.' } as const;`
- `export const STATUS_MESSAGES = { PROCESSING_START: 'Starting Copilot analysis...', PROCESSING_COMPLETE: 'Analysis completed successfully' } as const;`
- `export const PROTOCOL = { ROLES: { USER: 'user', ASSISTANT: 'assistant' }, CONTENT_TYPES: { TEXT: 'text' }, STATUS: { SUCCESS: 'success', ERROR: 'error', FAILED: 'failed', REPORT: 'report' }, NOTIFICATIONS: { PROGRESS: 'notifications/progress' }, KEEPALIVE_INTERVAL: 25000 } as const;`

Create `/Users/jonathanborduas/code/openhands-mcp-server/src/constants.ts`:
- `export { ToolArguments } from './types.js';` (check that ToolArguments is exported from types.ts; if not, skip this line)
- `export const SERVER_NAME = 'openhands-mcp-server' as const;`
- `export const LOG_PREFIX = '[OHMCP]';`
- Same ERROR_MESSAGES, STATUS_MESSAGES, PROTOCOL shape (adapt text for openhands).

NOTE: If types.ts in either repo does not export ToolArguments (openhands may differ), skip that re-export line and only include the named constants.

Run builds:
- `cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build`
- `cd /Users/jonathanborduas/code/openhands-mcp-server && npm run build`

Also verify gemini and opencode already have SERVER_NAME in their constants.ts (read them). If SERVER_NAME is missing from either, add it:
- gemini: `export const SERVER_NAME = 'gemini-mcp-server' as const;`
- opencode: `export const SERVER_NAME = 'opencode-mcp-server' as const;`
Then rebuild if modified.
  </action>
  <verify>
`cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build` exits 0.
`cd /Users/jonathanborduas/code/openhands-mcp-server && npm run build` exits 0.
`grep "SERVER_NAME" /Users/jonathanborduas/code/copilot-mcp-server/src/constants.ts` shows the constant.
`grep "SERVER_NAME" /Users/jonathanborduas/code/openhands-mcp-server/src/constants.ts` shows the constant.
`grep "SERVER_NAME" /Users/jonathanborduas/code/gemini-mcp-server/src/constants.ts` shows the constant.
`grep "SERVER_NAME" /Users/jonathanborduas/code/opencode-mcp-server/src/constants.ts` shows the constant.
  </verify>
  <done>
All 6 repos have constants.ts with SERVER_NAME. All 4 Gen1-ported repos now have constants.ts where it was missing. Builds pass in copilot and openhands repos.
  </done>
</task>

</tasks>

<verification>
Run builds in all 4 newly modified repos:
- `cd /Users/jonathanborduas/code/claude-mcp-server && npm run build` — should exit 0
- `cd /Users/jonathanborduas/code/codex-mcp-server && npm run build` — should exit 0
- `cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build` — should exit 0
- `cd /Users/jonathanborduas/code/openhands-mcp-server && npm run build` — should exit 0

Confirm no circular imports: `grep -r "import.*constants" /Users/jonathanborduas/code/claude-mcp-server/src/types.ts` returns nothing.

Confirm all 6 repos have SERVER_NAME: search each constants.ts for the string.
</verification>

<success_criteria>
- 4 new constants.ts files created (claude, codex, copilot, openhands)
- gemini and opencode constants.ts updated with SERVER_NAME if missing
- All 6 repos: constants.ts defines SERVER_NAME, LOG_PREFIX at minimum
- All 4 modified repos build cleanly (npm run build exits 0)
- Zero circular imports introduced (types.ts never imports from constants.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/25-identity-tool-and-shared-utilities/25-01-SUMMARY.md` documenting:
- Which files were created/modified in each repo
- The LOG_PREFIX value assigned to each repo
- Whether gemini/opencode needed SERVER_NAME added
- Build results for all repos
</output>
