---
phase: 25-identity-tool-and-shared-utilities
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - /Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts
  - /Users/jonathanborduas/code/claude-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/utils/logger.ts
  - /Users/jonathanborduas/code/codex-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/utils/logger.ts
  - /Users/jonathanborduas/code/copilot-mcp-server/src/tools/simple-tools.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/src/utils/logger.ts
  - /Users/jonathanborduas/code/openhands-mcp-server/src/tools/simple-tools.ts
autonomous: true
requirements:
  - STD-08

must_haves:
  truths:
    - "All 4 Gen1-ported repos have src/utils/logger.ts with the Logger class using console.warn for stderr output"
    - "No console.log calls remain in non-startup src files in any of the 4 repos — all replaced with Logger calls"
    - "npm run build AND npm test pass in all 4 repos after Logger integration"
    - "MCP stdout protocol is not corrupted — Logger routes all output to stderr via console.warn/console.error"
  artifacts:
    - path: "/Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts"
      provides: "Logger utility for claude-mcp-server"
      contains: "class Logger"
    - path: "/Users/jonathanborduas/code/codex-mcp-server/src/utils/logger.ts"
      provides: "Logger utility for codex-mcp-server"
      contains: "class Logger"
    - path: "/Users/jonathanborduas/code/copilot-mcp-server/src/utils/logger.ts"
      provides: "Logger utility for copilot-mcp-server"
      contains: "class Logger"
    - path: "/Users/jonathanborduas/code/openhands-mcp-server/src/utils/logger.ts"
      provides: "Logger utility for openhands-mcp-server"
      contains: "class Logger"
  key_links:
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/constants.ts"
      via: "LOG_PREFIX import"
      pattern: "LOG_PREFIX.*from.*constants"
    - from: "/Users/jonathanborduas/code/claude-mcp-server/src/tools/simple-tools.ts"
      to: "/Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts"
      via: "Logger.log() calls replacing console.log"
      pattern: "Logger\\.(log|warn|error)"
---

<objective>
Create src/utils/logger.ts in the 4 Gen1-ported repos (claude, codex, copilot, openhands) and replace operational console.log calls with Logger calls. The Logger class routes output to stderr (console.warn) to protect the MCP JSON-RPC stdout protocol from corruption.

Purpose: STD-08 requires all 6 repos to have src/utils/logger.ts. gemini and opencode already have it. This plan creates it for the 4 remaining repos and integrates it into existing tool files.

Output: 4 new logger.ts files, console.log calls replaced in tool files, builds and tests passing.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-identity-tool-and-shared-utilities/25-RESEARCH.md
@.planning/phases/25-identity-tool-and-shared-utilities/25-01-SUMMARY.md

# Canonical Logger reference — copy verbatim:
@/Users/jonathanborduas/code/opencode-mcp-server/src/utils/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logger.ts and replace console.log in claude-mcp-server and codex-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/simple-tools.ts
    /Users/jonathanborduas/code/claude-mcp-server/src/tools/claude.tool.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/utils/logger.ts
    /Users/jonathanborduas/code/codex-mcp-server/src/tools/simple-tools.ts
  </files>
  <action>
Read the canonical logger at `/Users/jonathanborduas/code/opencode-mcp-server/src/utils/logger.ts` in full before writing.

Step 1 — Create src/utils/ directory and logger.ts for claude-mcp-server:
- Create `/Users/jonathanborduas/code/claude-mcp-server/src/utils/logger.ts` as a verbatim copy of the opencode logger.
- The only change: import path is `'../constants.js'` (same as opencode). No other changes.

Step 2 — Replace console.log in claude-mcp-server:
- Read all src files to find console.log calls: `grep -rn "console\.log" /Users/jonathanborduas/code/claude-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"`
- For each console.log call in non-test source files, replace with `Logger.log(...)` — import Logger from `'../utils/logger.js'` (or `'./utils/logger.js'` depending on file location).
- KEEP: `console.error(...)` calls in `index.ts` startup handler (before server start — these are safe on stderr).
- KEEP: `console.error(...)` calls in `server.ts` for failed progress notifications.
- DO NOT REPLACE: Any `console.error` in startup paths. Only replace `console.log`.
- Add `import { Logger } from '../utils/logger.js';` to each file where replacement occurs.

Step 3 — Create logger.ts for codex-mcp-server:
- Create `/Users/jonathanborduas/code/codex-mcp-server/src/utils/logger.ts` — verbatim copy of opencode logger.
- Same import: `'../constants.js'`

Step 4 — Replace console.log in codex-mcp-server:
- `grep -rn "console\.log" /Users/jonathanborduas/code/codex-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"`
- Replace operational console.log with Logger.log. Keep console.error in startup/server.ts.

Step 5 — Build and test both repos:
- `cd /Users/jonathanborduas/code/claude-mcp-server && npm run build && npm test`
- `cd /Users/jonathanborduas/code/codex-mcp-server && npm run build && npm test`
  </action>
  <verify>
`cd /Users/jonathanborduas/code/claude-mcp-server && npm run build && npm test` — both exit 0.
`cd /Users/jonathanborduas/code/codex-mcp-server && npm run build && npm test` — both exit 0.
`grep -r "console\.log" /Users/jonathanborduas/code/claude-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"` returns nothing (all replaced).
`grep -r "console\.log" /Users/jonathanborduas/code/codex-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"` returns nothing.
Both logger.ts files exist and contain `class Logger`.
  </verify>
  <done>
Logger created for claude and codex. No console.log in operational source. Builds and tests pass in both repos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create logger.ts and replace console.log in copilot-mcp-server and openhands-mcp-server</name>
  <files>
    /Users/jonathanborduas/code/copilot-mcp-server/src/utils/logger.ts
    /Users/jonathanborduas/code/copilot-mcp-server/src/tools/simple-tools.ts
    /Users/jonathanborduas/code/openhands-mcp-server/src/utils/logger.ts
    /Users/jonathanborduas/code/openhands-mcp-server/src/tools/simple-tools.ts
  </files>
  <action>
Apply the same Logger creation and console.log replacement process to copilot and openhands repos.

Step 1 — Create `/Users/jonathanborduas/code/copilot-mcp-server/src/utils/logger.ts`:
- Verbatim copy from opencode logger. Import: `'../constants.js'`.

Step 2 — Replace console.log in copilot-mcp-server:
- `grep -rn "console\.log" /Users/jonathanborduas/code/copilot-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"`
- Replace operational console.log with Logger.log.
- Keep console.error in startup/server.ts.

Step 3 — Create `/Users/jonathanborduas/code/openhands-mcp-server/src/utils/logger.ts`:
- Verbatim copy from opencode logger. Import: `'../constants.js'`.

Step 4 — Replace console.log in openhands-mcp-server:
- `grep -rn "console\.log" /Users/jonathanborduas/code/openhands-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"`
- Replace operational console.log with Logger.log.
- Keep console.error in startup/server.ts.

Step 5 — Build and test both:
- `cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build && npm test`
- `cd /Users/jonathanborduas/code/openhands-mcp-server && npm run build && npm test`

Also verify the 2 repos that already have logger (gemini, opencode) still build cleanly — run `npm run build` in each if any constants.ts modification was made in Plan 01.
  </action>
  <verify>
`cd /Users/jonathanborduas/code/copilot-mcp-server && npm run build && npm test` — both exit 0.
`cd /Users/jonathanborduas/code/openhands-mcp-server && npm run build && npm test` — both exit 0.
`grep -r "console\.log" /Users/jonathanborduas/code/copilot-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"` returns nothing.
`grep -r "console\.log" /Users/jonathanborduas/code/openhands-mcp-server/src/ --include="*.ts" --exclude-dir="__tests__"` returns nothing.
All 4 logger.ts files exist: `ls /Users/jonathanborduas/code/{claude,codex,copilot,openhands}-mcp-server/src/utils/logger.ts`
  </verify>
  <done>
Logger created for all 4 Gen1-ported repos. No console.log in operational source across any of the 4 repos. All builds and tests pass.
  </done>
</task>

</tasks>

<verification>
Confirm STD-08 completion across all 6 repos:
- All 6 repos have `src/constants.ts` (4 created in Plan 01; gemini/opencode already had it)
- All 6 repos have `src/utils/logger.ts` (4 created in this plan; gemini/opencode already had it)
- No console.log in operational src files for the 4 newly updated repos
- All 4 repos: `npm run build && npm test` pass
- Logger consistently uses `console.warn` internally (not console.log) to protect stdout JSON-RPC
</verification>

<success_criteria>
- 4 new logger.ts files exist at src/utils/logger.ts in claude, codex, copilot, openhands repos
- All operational console.log calls replaced with Logger.log() in those 4 repos
- console.error calls in index.ts startup handlers preserved (safe)
- Builds pass in all 4 repos
- Tests pass in all 4 repos (existing test counts unchanged — no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/25-identity-tool-and-shared-utilities/25-02-SUMMARY.md` documenting:
- Which files were created and which had console.log replaced
- Count of console.log → Logger.log replacements per repo
- Test results (pass counts) for all 4 repos
- Any console.error calls that were intentionally preserved and why
</output>
