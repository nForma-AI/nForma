---
phase: v0.37-05
phase_name: Hypothesis Targeting
status: passed
verified_at: 2026-03-16T23:20:00Z
verifier: nf-verifier
formal_check:
  passed: 0
  failed: 0
  skipped: 0
  counterexamples: []
  note: "No formal modules matched this phase (formal scope scan returned empty)"
must_haves:
  - id: SC1
    description: "When a hypothesis transitions from UNMEASURABLE to CONFIRMED or VIOLATED, its parent layer receives +1 priority weight in the next solve remediation wave"
    status: passed
    evidence: "computeLayerPriorityWeights returns {c_to_r: 1} for a single oscillation hypothesis transition; loadHypothesisTransitions filters to only UNMEASURABLE->CONFIRMED/VIOLATED"
  - id: SC2
    description: "solve-wave-dag.cjs reads hypothesis measurements and adjusts wave ordering so layers with recent transitions execute before layers without"
    status: passed
    evidence: "computeWaves with priorityWeights={git_heatmap: 1} puts git_heatmap first in wave 1 (before d_to_c alphabetically); without weights, d_to_c is first (backward compatible)"
requirements_checked:
  - HTARGET-01
  - HTARGET-02
---

## Verification Report -- Phase v0.37-05: Hypothesis Targeting

### Goal
Hypothesis measurement transitions directly influence which layers get remediated first in solve waves.

### Formal Check
No formal modules matched this phase. Formal scope scan returned empty (fail-open). The changes are additive (new module + optional parameter extension) and do not modify any existing formal spec behavior.

### Success Criteria Verification

**SC1: Hypothesis transition -> layer +1 priority weight** -- PASSED
- `mapSourceToLayer` maps TLA+ filenames to DAG layer keys via keyword matching
- `loadHypothesisTransitions` detects only UNMEASURABLE -> CONFIRMED/VIOLATED transitions (not CONFIRMED->CONFIRMED or CONFIRMED->VIOLATED)
- `computeLayerPriorityWeights` adds +1 per transition per mapped layer (additive)
- TC-PREC-MATCH tests in hypothesis-layer-map.test.cjs confirm (22/22 pass)

**SC2: solve-wave-dag adjusts wave ordering** -- PASSED
- `computeWaves` extended with optional `priorityWeights` parameter
- Layers with higher weight appear first within their wave (intra-wave ordering)
- Dependency topology (cross-wave ordering) is NEVER changed by weights
- Alphabetical tiebreaker preserved when weights are equal
- Fully backward compatible: no weights = identical behavior
- solve-wave-dag.test.cjs confirms (16/16 pass)

### Test Results
- `bin/hypothesis-layer-map.test.cjs`: 22/22 pass
- `bin/solve-wave-dag.test.cjs`: 16/16 pass
- Zero regressions

### Requirements Traceability
| Requirement | Covered By | Status |
|-------------|-----------|--------|
| HTARGET-01 | hypothesis-layer-map.cjs: transition detection + weight computation | Satisfied |
| HTARGET-02 | solve-wave-dag.cjs: priority-weighted intra-wave sorting | Satisfied |
