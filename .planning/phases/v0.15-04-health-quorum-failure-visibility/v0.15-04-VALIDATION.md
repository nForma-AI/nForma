---
phase: v0.15-04
slug: health-quorum-failure-visibility
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-27
---

# Phase v0.15-04 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.15-04`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.15-04`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (Node.js built-in) with `node:assert` |
| **Config file** | none — built-in runner |
| **Quick run command** | `node --test qgsd-core/bin/gsd-tools.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | none configured |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test qgsd-core/bin/gsd-tools.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** ~15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| Wave 0: test scaffolding | 01 | 0 | VIS-01 | unit | `node --test qgsd-core/bin/gsd-tools.test.cjs 2>&1 \| grep -E "VIS-01\|pass\|fail"` | ❌ W0 | ⬜ pending |
| Add Check 9 to gsd-tools.cjs | 01 | 1 | VIS-01 | unit | `node --test qgsd-core/bin/gsd-tools.test.cjs` | ✅ after W0 | ⬜ pending |
| Add W008 to health.md error table | 01 | 1 | VIS-01 | manual | doc-only, no automated test | ✅ N/A | ⬜ pending |
| Install sync | 01 | 1 | VIS-01 | smoke | `node /Users/jonathanborduas/code/QGSD/qgsd-core/bin/gsd-tools.cjs validate health \| node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));const w008=((d.warnings\|\|[]).filter(x=>x.code==='W008'));if(w008.length===0){console.error('FAIL');process.exit(1);}console.log('PASS');"` | ✅ N/A | ⬜ pending |
| Regression check | 01 | 1 | VIS-01 | unit | `npm test` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `qgsd-core/bin/gsd-tools.test.cjs` — append VIS-01 describe block with 3 failing test stubs (TC-01, TC-02, TC-03)

Wave 0 verification command:
```bash
node --test qgsd-core/bin/gsd-tools.test.cjs 2>&1 | grep -E "VIS-01|pass|fail"
```
Expected: VIS-01-TC-01 FAIL (W008 never emitted), pre-existing 151 tests still pass.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| W008 row present in health.md error-code table | VIS-01 | Documentation table, not runtime behavior | Open `qgsd-core/workflows/health.md`, confirm `\| W008 \| warning \| Quorum slot has 3+ recurring failures \| No \|` row exists in `<error_codes>` section |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 15s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.15-04`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 4 | `npm test` | — | — | ⬜ pending |

**Phase validation complete:** pending
