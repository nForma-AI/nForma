---
phase: 13-circuit-breaker-oscillation-resolution-mode
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/qgsd-circuit-breaker.js
  - hooks/qgsd-circuit-breaker.test.js
autonomous: true
requirements:
  - ORES-04
  - ORES-05

must_haves:
  truths:
    - "When the circuit breaker blocks, the deny message includes the commit_window_snapshot rendered as a readable commit graph"
    - "The deny message explicitly tells Claude to invoke Oscillation Resolution Mode per R5"
    - "buildBlockReason() reads commit_window_snapshot from state and formats it as a commit table"
    - "Tests verify the enhanced deny message format"
  artifacts:
    - path: "hooks/qgsd-circuit-breaker.js"
      provides: "Enhanced buildBlockReason() with commit graph and R5 reference"
      contains: "Oscillation Resolution Mode per R5"
    - path: "hooks/qgsd-circuit-breaker.test.js"
      provides: "Tests for enhanced deny message"
      contains: "Oscillation Resolution Mode"
  key_links:
    - from: "hooks/qgsd-circuit-breaker.js buildBlockReason()"
      to: "CLAUDE.md R5"
      via: "deny message references R5 by name"
      pattern: "Oscillation Resolution Mode per R5"
    - from: "hooks/qgsd-circuit-breaker.js writeState()"
      to: "buildBlockReason()"
      via: "commit_window_snapshot stored in state and read back in deny message"
      pattern: "commit_window_snapshot"
---

<objective>
Enhance the circuit breaker hook's deny message to include the commit graph (commit_window_snapshot) and explicitly tell Claude to invoke Oscillation Resolution Mode per R5, then add tests for the enhanced format.

Purpose: The hook blocks execution but its current deny message only says "CIRCUIT BREAKER ACTIVE" with a static list. With resolution mode, the hook needs to provide the commit graph data that Claude needs for Step 3 of the workflow — so Claude doesn't have to re-run git log separately. The message also needs to reference "Oscillation Resolution Mode per R5" so Claude knows which workflow to follow.

Output: Updated buildBlockReason() in hooks/qgsd-circuit-breaker.js + new/updated tests in hooks/qgsd-circuit-breaker.test.js.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hooks/qgsd-circuit-breaker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance buildBlockReason() with commit graph and R5 reference</name>
  <files>hooks/qgsd-circuit-breaker.js</files>
  <action>
    Modify buildBlockReason(state) in hooks/qgsd-circuit-breaker.js to:

    1. **Include commit graph**: The state object has `commit_window_snapshot` which is an array of file arrays (one per commit, in most-recent-first order, matching the git log output order). Format this as a readable table in the deny message. The commit hashes are not in state (only file sets), so format it as:

    ```
    Commit Graph (most recent first):
    | # | Files Changed |
    |---|---------------|
    | 1 | file-a.js, file-b.js |
    | 2 | file-c.js |
    | 3 | file-a.js, file-b.js |
    ```

    If commit_window_snapshot is absent or empty, omit the table and note "(commit graph unavailable)".

    2. **Add R5 reference**: Change the action instructions block from the current hardcoded text to explicitly say:
    "Invoke Oscillation Resolution Mode per R5 in CLAUDE.md — see get-shit-done/workflows/oscillation-resolution-mode.md for the full procedure."

    3. **Preserve existing content**: Keep the oscillating file set line, CIRCUIT BREAKER ACTIVE header, allowed read-only operations list, and "npx qgsd --reset-breaker" instruction.

    4. **Keep function signature identical**: buildBlockReason(state) — no signature change, no new parameters.

    The updated buildBlockReason should produce output like:
    ```
    CIRCUIT BREAKER ACTIVE

    Oscillating file set detected: file-a.js, file-b.js

    Commit Graph (most recent first):
    | # | Files Changed |
    |---|---------------|
    | 1 | file-a.js, file-b.js |
    | 2 | file-c.js |
    | 3 | file-a.js, file-b.js |
    | 4 | file-c.js |
    | 5 | file-a.js, file-b.js |
    | 6 | file-d.js |

    Invoke Oscillation Resolution Mode per R5 in CLAUDE.md — see get-shit-done/workflows/oscillation-resolution-mode.md for the full procedure.

    Allowed read-only operations: git log, git diff, grep, cat, ls, head, tail, find

    After committing the fix manually, run 'npx qgsd --reset-breaker' to clear the circuit breaker.
    ```

    IMPORTANT: The hookSpecificOutput format must remain unchanged — only the permissionDecisionReason string content changes. The outer JSON structure `{ hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason } }` is CRITICAL and must not be touched.
  </action>
  <verify>
    node -e "
      const { buildBlockReason } = require('./hooks/qgsd-circuit-breaker.js');
    " 2>&1 | head -5
    grep -n "Oscillation Resolution Mode per R5" /Users/jonathanborduas/code/QGSD/hooks/qgsd-circuit-breaker.js
    grep -n "commit_window_snapshot" /Users/jonathanborduas/code/QGSD/hooks/qgsd-circuit-breaker.js
    grep -n "Commit Graph" /Users/jonathanborduas/code/QGSD/hooks/qgsd-circuit-breaker.js
  </verify>
  <done>
    buildBlockReason() produces a deny reason containing: "CIRCUIT BREAKER ACTIVE", the oscillating file set, a commit graph table built from commit_window_snapshot, the "Oscillation Resolution Mode per R5" reference, and the --reset-breaker instruction.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests for enhanced deny message format</name>
  <files>hooks/qgsd-circuit-breaker.test.js</files>
  <action>
    Update hooks/qgsd-circuit-breaker.test.js to cover the enhanced buildBlockReason() output. Read the existing test file first to understand its structure.

    Add or update the following test cases (within the existing test framework — do not change the test runner or test helper patterns):

    **CB-TC-BR1: Deny message includes commit graph when snapshot present**
    - Input state: `{ active: true, file_set: ['a.js', 'b.js'], activated_at: '2026-01-01T00:00:00Z', commit_window_snapshot: [['a.js','b.js'], ['c.js'], ['a.js','b.js']] }`
    - Expected: deny reason contains "Commit Graph" and "a.js, b.js" in the table
    - Expected: deny reason contains "Oscillation Resolution Mode per R5"

    **CB-TC-BR2: Deny message handles missing snapshot gracefully**
    - Input state: `{ active: true, file_set: ['x.js'], activated_at: '2026-01-01T00:00:00Z' }` (no commit_window_snapshot)
    - Expected: deny reason does NOT throw; contains "CIRCUIT BREAKER ACTIVE"
    - Expected: contains "(commit graph unavailable)" or gracefully omits the table

    **CB-TC-BR3: Deny message still references --reset-breaker**
    - Input state: any active state
    - Expected: deny reason contains "npx qgsd --reset-breaker"

    If buildBlockReason is not exported from qgsd-circuit-breaker.js (it is currently an internal function), test it by running the full hook via spawnSync with a pre-existing state file (pattern already used in the test suite — follow existing CB-TC integration test patterns). The deny reason is in the parsed output's `hookSpecificOutput.permissionDecisionReason`.

    Run `npm test` after adding tests to confirm all existing tests still pass and new tests pass.

    Existing test count baseline: 19 tests passing. New count after additions: expect 22+ passing.
  </action>
  <verify>
    cd /Users/jonathanborduas/code/QGSD && npm test 2>&1 | tail -20
  </verify>
  <done>
    npm test passes with all pre-existing tests (19) still passing, plus new CB-TC-BR1, CB-TC-BR2, CB-TC-BR3 passing. The enhanced deny message format is verified by tests.
  </done>
</task>

</tasks>

<verification>
1. grep -n "Oscillation Resolution Mode per R5" hooks/qgsd-circuit-breaker.js — confirms R5 reference in deny message
2. grep -n "commit_window_snapshot\|Commit Graph" hooks/qgsd-circuit-breaker.js — confirms snapshot rendering
3. npm test — all tests pass (expect 22+)
4. grep -n "CB-TC-BR" hooks/qgsd-circuit-breaker.test.js — confirms new test cases added
5. The hookSpecificOutput JSON structure is unchanged: verify with grep -n "permissionDecision.*deny" hooks/qgsd-circuit-breaker.js
</verification>

<success_criteria>
- buildBlockReason() renders commit_window_snapshot as a markdown table when present
- buildBlockReason() handles missing snapshot gracefully (no crash, informative fallback)
- Deny message contains "Oscillation Resolution Mode per R5" and workflow doc reference
- All 19 pre-existing tests pass
- 3 new tests (CB-TC-BR1, CB-TC-BR2, CB-TC-BR3) pass
- hookSpecificOutput format unchanged (permissionDecision: 'deny' still works)
</success_criteria>

<output>
After completion, create .planning/phases/13-circuit-breaker-oscillation-resolution-mode/13-02-SUMMARY.md
</output>
