---
phase: 13-circuit-breaker-oscillation-resolution-mode
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - get-shit-done/workflows/oscillation-resolution-mode.md
autonomous: true
requirements:
  - ORES-01
  - ORES-02
  - ORES-03

must_haves:
  truths:
    - "CLAUDE.md R5 describes oscillation resolution mode (not immediate hard-stop) with fast-path, quorum diagnosis, and human approval gate"
    - "A workflow document exists at get-shit-done/workflows/oscillation-resolution-mode.md with the exact step-by-step process Claude follows"
    - "The hard-stop still exists as last resort (no consensus after 4 rounds → hard-stop with escalation)"
    - "Environmental file fast-path is documented: config/env/lock files trigger immediate human escalation, skipping quorum"
  artifacts:
    - path: "CLAUDE.md"
      provides: "R5 updated to oscillation resolution mode"
      contains: "Oscillation Resolution Mode"
    - path: "get-shit-done/workflows/oscillation-resolution-mode.md"
      provides: "Step-by-step workflow for Claude to follow when hook blocks execution"
      min_lines: 40
  key_links:
    - from: "CLAUDE.md R5"
      to: "get-shit-done/workflows/oscillation-resolution-mode.md"
      via: "R5.2 references the workflow document"
      pattern: "oscillation-resolution-mode"
    - from: "CLAUDE.md R5"
      to: "R3.3 deliberation rules"
      via: "R5 invokes R3.3 for quorum rounds"
      pattern: "R3\\.3"
---

<objective>
Update CLAUDE.md R5 to replace the immediate hard-stop with oscillation resolution mode, and create the workflow document Claude follows when the circuit breaker hook blocks execution.

Purpose: The hook detects oscillation and blocks, but currently CLAUDE.md R5 just says hard-stop. With resolution mode, Claude instead enters a structured diagnosis workflow — building a commit graph, running quorum, and presenting a unified solution for user approval before execution resumes.

Output: Updated CLAUDE.md R5 section + new get-shit-done/workflows/oscillation-resolution-mode.md workflow document.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@hooks/qgsd-circuit-breaker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md R5 — oscillation resolution mode</name>
  <files>CLAUDE.md</files>
  <action>
    Replace the current R5 section (R5.1, R5.2, R5.3) with the new oscillation resolution mode design. The updated R5 must:

    **R5.1 — OSCILLATION Detection** (unchanged content — keep as-is):
    - Same two-condition definition (≥3 of last 6 commits + no net stability improvement)

    **R5.2 — Oscillation Resolution Mode** (replaces current "When Circuit Breaker Triggers"):
    When the PreToolUse hook blocks with CIRCUIT BREAKER ACTIVE, Claude MUST enter Oscillation Resolution Mode. Claude MUST NOT hard-stop immediately. The resolution mode steps are:

    1. **Fast-path check**: If the oscillating file set (reported in the hook's deny message) contains environmental/external files — config files (.env, *.config.js, *.config.ts), lock files (package-lock.json, yarn.lock, Pipfile.lock, poetry.lock), or external API schema files — Claude MUST escalate immediately to the user with the file list and reason "Environmental oscillation detected — this is likely an external dependency issue, not a structural coupling issue. Human intervention required." Skip quorum to avoid wasted cycles on environmental issues.

    2. **Build commit graph**: Using git log (read-only, allowed during breaker active state), display the last N commits (N = commit_window, default 6) in chronological order showing which files changed per commit. This reveals the A→B→A→B ping-pong pattern.

    3. **Invoke quorum diagnosis**: Present the commit graph to all available quorum models (per R3.3 rules, up to 4 rounds). The quorum prompt MUST use this exact framing: "Diagnose the STRUCTURAL COUPLING causing this oscillation — not surface symptoms. Propose a UNIFIED solution that resolves both sides simultaneously. Partial or incremental fixes are NOT acceptable."

    4. **Quorum deliberation**: Apply R3.3 rules (up to 4 rounds). Partial/incremental fixes are REJECTED — only unified solutions accepted. EXECUTION is still single-model only (R2.2) — quorum handles diagnosis and planning only.

    5. **On consensus**: Claude presents the unified solution plan to the user for approval. Claude MUST NOT execute anything until the user explicitly approves the plan. After user approval: user manually commits the fix, then runs `npx qgsd --reset-breaker` to clear state.

    6. **No consensus after 4 rounds**: Claude MUST hard-stop and escalate to user with all model positions.

    **R5.3 — Allowed Actions During Resolution Mode** (replaces current "Allowed Actions Under Circuit Breaker"):
    - Read-only Bash commands pass through: git log, git diff, grep, cat, ls, head, tail, find
    - Quorum model calls (diagnosis and planning, NOT execution)
    - Displaying commit graph to user
    - Presenting unified solution plan for user approval

    Claude MUST NOT execute write Bash commands until user approves the plan AND manually runs `npx qgsd --reset-breaker`.

    Also add a reference to the workflow document: "See get-shit-done/workflows/oscillation-resolution-mode.md for the full step-by-step procedure."

    Preserve all other CLAUDE.md sections exactly (R0, R1, R2, R3, R4, R6, R7, R8, Appendix). Only modify R5.
  </action>
  <verify>
    grep -n "Oscillation Resolution Mode" /Users/jonathanborduas/code/QGSD/CLAUDE.md
    grep -n "Fast-path check\|fast-path" /Users/jonathanborduas/code/QGSD/CLAUDE.md
    grep -n "oscillation-resolution-mode.md" /Users/jonathanborduas/code/QGSD/CLAUDE.md
    grep -n "R3\.3" /Users/jonathanborduas/code/QGSD/CLAUDE.md | grep -i "R5\|resolution"
  </verify>
  <done>
    CLAUDE.md R5 contains: oscillation resolution mode heading, fast-path check for environmental files, commit graph step, quorum diagnosis step with exact framing, consensus path with user approval gate, no-consensus hard-stop path, and reference to workflow document.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create oscillation-resolution-mode.md workflow document</name>
  <files>get-shit-done/workflows/oscillation-resolution-mode.md</files>
  <action>
    Create get-shit-done/workflows/oscillation-resolution-mode.md as the step-by-step procedural reference that Claude follows when the PreToolUse circuit breaker hook blocks execution.

    The document should follow the same style as other workflow docs in get-shit-done/workflows/ (imperative steps, numbered, no prose fluff). Structure:

    ```
    # Oscillation Resolution Mode

    Triggered when: The PreToolUse hook returns CIRCUIT BREAKER ACTIVE deny message.

    ## Step 1 — Read the Deny Message
    Extract from the hook's deny message:
    - The oscillating file set (listed under "Oscillating file set detected:")
    - The commit_window_snapshot data (if present in the deny payload)

    ## Step 2 — Fast-Path: Environmental File Check
    Check if oscillating file set contains environmental/external files:
    - Config files: .env, *.env, *.config.js, *.config.ts, *.config.mjs
    - Lock files: package-lock.json, yarn.lock, Pipfile.lock, poetry.lock, Cargo.lock, go.sum
    - External API schema files: openapi.json, swagger.json, schema.graphql, *.schema.json

    IF YES: Immediately escalate to user. Message:
    "Environmental oscillation detected in: [file list]
    This is likely an external dependency conflict, not a structural coupling issue.
    Human intervention required — quorum diagnosis is not appropriate here."
    STOP — do not proceed to Step 3.

    IF NO: Continue to Step 3.

    ## Step 3 — Build Commit Graph
    Run: git log --oneline --name-only -[N] (N = commit_window, default 6)
    Display as a table:
    | Commit | Message | Files Changed |
    |--------|---------|---------------|
    | abc1234 | fix: ... | file-a.js, file-b.js |
    | def5678 | fix: ... | file-c.js |
    | ghi9012 | fix: ... | file-a.js, file-b.js |

    This makes the A→B→A→B ping-pong pattern visually obvious.

    ## Step 4 — Quorum Diagnosis
    Form your own diagnosis first (structural coupling analysis).
    Then query each available quorum model with:

    Context: [paste commit graph table]
    Oscillating file set: [file list]

    Prompt: "Diagnose the STRUCTURAL COUPLING causing this oscillation — not surface symptoms. Propose a UNIFIED solution that resolves both sides simultaneously. Partial or incremental fixes are NOT acceptable."

    Apply R3.3 deliberation rules (up to 4 rounds, stop on consensus).

    ## Step 5 — On Consensus
    Present the unified solution plan to the user with:
    - Summary of structural coupling diagnosed
    - The unified solution (files to change, what to change)
    - Instructions: "To implement: [steps]. After committing the fix, run `npx qgsd --reset-breaker` to clear the circuit breaker."

    Wait for explicit user approval before any implementation.

    ## Step 6 — No Consensus After 4 Rounds
    Hard-stop. Present to user:
    - Each model's final position (1-2 sentences)
    - Core point of disagreement
    - Claude's recommendation

    User must make the final call.

    ## Constraints
    - EXECUTION is single-model only (R2.2) — quorum diagnoses and plans, never executes
    - Read-only Bash commands (git log, grep, cat) are allowed throughout
    - No write Bash commands until user approves AND runs --reset-breaker
    ```

    Adapt the exact markdown formatting to match other workflow docs in the directory (check one or two for style reference).
  </action>
  <verify>
    ls -la /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
    grep -c "Step" /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/oscillation-resolution-mode.md
  </verify>
  <done>
    File exists at get-shit-done/workflows/oscillation-resolution-mode.md with at least 6 numbered steps, fast-path check, commit graph step, quorum diagnosis with exact prompt framing, consensus path, and no-consensus hard-stop path.
  </done>
</task>

</tasks>

<verification>
1. grep -n "Oscillation Resolution Mode" CLAUDE.md — confirms R5 heading updated
2. grep -n "fast-path\|Fast-path\|environmental" CLAUDE.md — confirms fast-path present
3. grep -n "oscillation-resolution-mode.md" CLAUDE.md — confirms workflow reference present
4. ls get-shit-done/workflows/oscillation-resolution-mode.md — confirms file created
5. grep -c "Step [0-9]" get-shit-done/workflows/oscillation-resolution-mode.md — confirms steps present (expect >= 6)
6. grep "R3\.3" CLAUDE.md | grep -i "5\|resolution" — confirms R5 references R3.3
</verification>

<success_criteria>
- CLAUDE.md R5 completely replaced with oscillation resolution mode (no remaining text about "immediately STOP execution")
- R5 references get-shit-done/workflows/oscillation-resolution-mode.md
- Workflow document created with all 6 steps
- Fast-path check for environmental files present in both documents
- Exact quorum framing ("STRUCTURAL COUPLING", "UNIFIED solution") present in both documents
- Hard-stop as last resort (after no consensus) preserved
</success_criteria>

<output>
After completion, create .planning/phases/13-circuit-breaker-oscillation-resolution-mode/13-01-SUMMARY.md
</output>
