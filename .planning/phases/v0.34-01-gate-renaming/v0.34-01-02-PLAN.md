---
phase: v0.34-01
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - bin/nForma.cjs
  - bin/solve-tui.cjs
  - bin/cross-layer-dashboard.cjs
  - bin/cross-layer-dashboard.test.cjs
  - bin/compute-per-model-gates.cjs
  - bin/nf-solve.cjs
  - bin/nf-solve.test.cjs
  - bin/escalation-classifier.cjs
  - bin/requirement-map.cjs
  - test/golden/tui-agents.txt
autonomous: true
formal_artifacts: none
requirements:
  - NAME-03

must_haves:
  truths:
    - "The TUI gate scoring page and solve report display 'Wiring:Evidence', 'Wiring:Purpose', 'Wiring:Coverage' labels instead of 'Gate A', 'Gate B', 'Gate C'"
    - "All scripts that consume gate scores (run-formal-verify.cjs, nf-solve, cross-layer dashboard) work correctly with renamed fields"
    - "Golden test snapshots reflect the new gate labels"
  artifacts:
    - path: "test/golden/tui-agents.txt"
      provides: "Updated golden snapshot with Wiring:Evidence/Purpose/Coverage labels"
  key_links:
    - from: "bin/nForma.cjs"
      to: "TUI menu and display"
      via: "label strings for gate menu items and gate pass counts"
      pattern: "Wiring:Evidence"
    - from: "bin/cross-layer-dashboard.cjs"
      to: "Dashboard display output"
      via: "Gate label strings in formatted output"
      pattern: "Wiring:Evidence"
    - from: "bin/nf-solve.cjs"
      to: "Solve report console output"
      via: "Gate label strings in console.log"
      pattern: "Wiring:Evidence"
---

<objective>
Update all display labels from opaque "Gate A/B/C" to descriptive "Wiring:Evidence/Purpose/Coverage" across TUI, solve report, cross-layer dashboard, and supporting scripts.

Purpose: Users see semantically meaningful gate names in every interface — TUI menus, gate scoring pages, solve reports, and dashboards — instead of arbitrary A/B/C labels. This is the user-facing half of the gate renaming.

Output: All display strings updated, golden test snapshots regenerated, display tests updated.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.34-01-gate-renaming/v0.34-01-RESEARCH.md
@.planning/phases/v0.34-01-gate-renaming/v0.34-01-01-SUMMARY.md
@bin/nForma.cjs (TUI: gate menu items around lines 633-635, gate views around 3016-3018, gate pass counts around 3370-3372, sweep labels around 3632-3634)
@bin/cross-layer-dashboard.cjs (display: Gate A/B/C labels around lines 231-262)
@bin/nf-solve.cjs (display: header comments and console output referencing Gate A/B/C)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename gate labels in TUI, dashboard, and solve scripts</name>
  <files>
    bin/nForma.cjs
    bin/solve-tui.cjs
    bin/cross-layer-dashboard.cjs
    bin/nf-solve.cjs
    bin/compute-per-model-gates.cjs
    bin/escalation-classifier.cjs
    bin/requirement-map.cjs
  </files>
  <action>
**Part A: Update bin/nForma.cjs TUI labels**

1. Menu items (around lines 633-635):
   - `'  L1->L2 Gate A'` → `'  L1->L2 Wiring:Evidence'`
   - `'  L2->L3 Gate B'` → `'  L2->L3 Wiring:Purpose'`
   - `'  L3->TC Gate C'` → `'  L3->TC Wiring:Coverage'`

2. Solve residual view calls (around lines 3016-3018):
   - `'L1→L2 Gate A Grounding'` → `'L1→L2 Wiring:Evidence'`
   - `'L2→L3 Gate B Traceability'` → `'L2→L3 Wiring:Purpose'`
   - `'L3→TC Gate C Validation'` → `'L3→TC Wiring:Coverage'`

3. Gate pass counts (around lines 3370-3372):
   - `'Gate A pass:'` → `'Wiring:Evidence pass:'`
   - `'Gate B pass:'` → `'Wiring:Purpose pass:'`
   - `'Gate C pass:'` → `'Wiring:Coverage pass:'`

4. Sweep labels (around lines 3632-3634):
   - `'L1→L2 Gate A'` → `'L1→L2 Wiring:Evidence'`
   - `'L2→L3 Gate B'` → `'L2→L3 Wiring:Purpose'`
   - `'L3→TC Gate C'` → `'L3→TC Wiring:Coverage'`

**Part B: Update bin/cross-layer-dashboard.cjs display strings**

Around lines 231-262, update the display labels:
- `'  Gate A:'` → `'  Wiring:Evidence:'`
- `'  Gate B:'` → `'  Wiring:Purpose:'`
- `'  Gate C:'` → `'  Wiring:Coverage:'`
- Update section comments from `// Gate A` to `// Wiring:Evidence`, etc.

**Part C: Update bin/nf-solve.cjs display strings**

1. Header comments (around lines 21-22):
   - `'L1->L2: Gate A grounding alignment score'` → `'L1->L2: Wiring:Evidence alignment score'`
   - `'L2->L3: Gate B traceability alignment score'` → `'L2->L3: Wiring:Purpose alignment score'`

2. Any other console.log strings referencing "Gate A/B/C" in the solve report output.

**Part D: Update bin/compute-per-model-gates.cjs display strings**

Console.log lines (around lines 733-735, 746-748) — update the human-readable labels:
- `'Gate A (grounding) pass:'` → `'Wiring:Evidence pass:'`
- `'Gate B (abstraction) pass:'` → `'Wiring:Purpose pass:'`
- `'Gate C (validation) pass:'` → `'Wiring:Coverage pass:'`
- `'Gate A grounding_score:'` → `'Wiring:Evidence score:'`  (note: the field names in code were already renamed in Plan 01)
- `'Gate B gate_b_score:'` → `'Wiring:Purpose score:'`
- `'Gate C gate_c_score:'` → `'Wiring:Coverage score:'`

Also update section comments (lines ~126, ~180, ~208):
- `'Gate A: Grounding'` → `'Wiring:Evidence (Grounding L1→L2)'`
- `'Gate B: Abstraction'` → `'Wiring:Purpose (Abstraction L2→L3)'`
- `'Gate C: Validation'` → `'Wiring:Coverage (Validation L3→TC)'`

**Part E: Update bin/escalation-classifier.cjs and bin/requirement-map.cjs**

Search for any "Gate A/B/C" references in these files and update to Wiring:Evidence/Purpose/Coverage. The reference in requirement-map.cjs (line ~50 comment) should say "Wiring:Evidence" instead of "Gate A grounding".

**Part F: Update bin/solve-tui.cjs**

Update "Recent Gate Changes" header (line ~571) to "Recent Wiring Gate Changes" or leave as-is if it's a generic label. Search for any "Gate A/B/C" specific strings and update.

**Comprehensive sweep:** Run `grep -rn 'Gate A\|Gate B\|Gate C' bin/` after all updates and verify zero matches remain (excluding git-ignored or generated files). The only acceptable remaining matches would be in comments explaining the historical renaming.
  </action>
  <verify>
grep -c 'Gate A\|Gate B\|Gate C' bin/nForma.cjs returns 0
grep -c 'Gate A\|Gate B\|Gate C' bin/cross-layer-dashboard.cjs returns 0
grep -c 'Gate A\|Gate B\|Gate C' bin/nf-solve.cjs returns 0
grep -c 'Gate A\|Gate B\|Gate C' bin/compute-per-model-gates.cjs returns 0
grep 'Wiring:Evidence' bin/nForma.cjs returns matches (new labels present)
grep 'Wiring:Purpose' bin/cross-layer-dashboard.cjs returns matches
grep 'Wiring:Coverage' bin/nf-solve.cjs returns matches
  </verify>
  <done>
All display strings across nForma.cjs (TUI menus, gate views, pass counts, sweep labels), cross-layer-dashboard.cjs (dashboard output), nf-solve.cjs (solve report), compute-per-model-gates.cjs (CLI output), and supporting scripts updated from "Gate A/B/C" to "Wiring:Evidence/Purpose/Coverage". No instances of old labels remain in bin/ scripts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update display tests and golden test snapshots</name>
  <files>
    bin/cross-layer-dashboard.test.cjs
    bin/nf-solve.test.cjs
    test/golden/tui-agents.txt
  </files>
  <action>
**Part A: Update bin/cross-layer-dashboard.test.cjs**

Around lines 88-90, update the display label assertions:
- `assert.ok(out.includes('Gate A'), 'contains Gate A')` → `assert.ok(out.includes('Wiring:Evidence'), 'contains Wiring:Evidence')`
- `assert.ok(out.includes('Gate B'), 'contains Gate B')` → `assert.ok(out.includes('Wiring:Purpose'), 'contains Wiring:Purpose')`
- `assert.ok(out.includes('Gate C'), 'contains Gate C')` → `assert.ok(out.includes('Wiring:Coverage'), 'contains Wiring:Coverage')`

**Part B: Update bin/nf-solve.test.cjs display assertions**

Around lines 210-211, update label assertions if they reference "Gate B"/"Gate C" display strings. Check for any other display label assertions that need updating.

**Part C: Update golden test snapshots**

In `test/golden/tui-agents.txt`, replace all occurrences of:
- `L1->L2 Gate A` → `L1->L2 Wiring:Evidence`
- `L2->L3 Gate B` → `L2->L3 Wiring:Purpose`
- `L3->TC Gate C` → `L3->TC Wiring:Coverage`
- `L1→L2 Gate A` → `L1→L2 Wiring:Evidence`
- `L2→L3 Gate B` → `L2→L3 Wiring:Purpose`
- `L3→TC Gate C` → `L3→TC Wiring:Coverage`

These appear at multiple points in the golden file (lines ~1830-1832, 1854-1856, 1897-1899, 1921-1923, 1964-1966, 1988-1990, 2031-2033, 2055-2057).

**Important:** Preserve exact spacing and alignment in the golden file — only change the label text, maintaining column widths. The labels are slightly longer ("Wiring:Evidence" = 15 chars vs "Gate A" = 6 chars), so column padding will need adjustment in the golden file to match what the updated TUI code produces. The safest approach is to regenerate the golden file by running the TUI test generator if one exists, or carefully adjusting padding manually.

Run the full test suite after updates to confirm no regressions.
  </action>
  <verify>
node --test bin/cross-layer-dashboard.test.cjs 2>&1 | tail -5 (all tests pass)
node --test bin/nf-solve.test.cjs 2>&1 | tail -5 (all tests pass)
grep -c 'Gate A\|Gate B\|Gate C' test/golden/tui-agents.txt returns 0
grep 'Wiring:Evidence' test/golden/tui-agents.txt returns matches
npm test 2>&1 | tail -10 (full suite passes)
  </verify>
  <done>
Display test assertions updated to check for new Wiring:Evidence/Purpose/Coverage labels. Golden test snapshot tui-agents.txt updated with new labels throughout. Full test suite passes with zero references to old Gate A/B/C labels in tests or golden files.
  </done>
</task>

</tasks>

<verification>
- `grep -rn 'Gate A\|Gate B\|Gate C' bin/*.cjs` returns zero matches (no old labels in any script)
- `grep -rn 'Gate A\|Gate B\|Gate C' test/golden/` returns zero matches (golden files updated)
- `grep 'Wiring:Evidence' bin/nForma.cjs` confirms TUI labels updated
- `grep 'Wiring:Purpose' bin/cross-layer-dashboard.cjs` confirms dashboard labels
- `grep 'Wiring:Coverage' bin/compute-per-model-gates.cjs` confirms CLI output labels
- `node --test bin/cross-layer-dashboard.test.cjs` passes with new label assertions
- `npm test` full suite passes
</verification>

<success_criteria>
- TUI menus show "L1->L2 Wiring:Evidence", "L2->L3 Wiring:Purpose", "L3->TC Wiring:Coverage"
- Cross-layer dashboard displays "Wiring:Evidence", "Wiring:Purpose", "Wiring:Coverage"
- Solve report uses new gate labels in all output
- compute-per-model-gates.cjs CLI output uses new labels
- Golden test snapshots contain new labels, zero old labels
- All display tests pass with new label assertions
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/v0.34-01-gate-renaming/v0.34-01-02-SUMMARY.md`
</output>
