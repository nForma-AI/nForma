---
phase: v0.34-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/compute-per-model-gates.cjs
  - bin/compute-per-model-gates.test.cjs
  - bin/nf-solve.cjs
  - bin/nf-solve.test.cjs
  - bin/cross-layer-dashboard.cjs
  - bin/solve-trend-helpers.cjs
  - bin/solve-trend.test.cjs
  - .planning/formal/gates/gate-a-grounding.json
  - .planning/formal/gates/gate-b-abstraction.json
  - .planning/formal/gates/gate-c-validation.json
  - bin/gate-field-compatibility.test.cjs
  - test/fixtures/gate-a-old-format.json
  - test/fixtures/gate-a-new-format.json
  - test/fixtures/gate-b-old-format.json
  - test/fixtures/gate-b-new-format.json
  - test/fixtures/gate-c-old-format.json
  - test/fixtures/gate-c-new-format.json
autonomous: true
formal_artifacts: none
requirements:
  - NAME-01
  - NAME-02
  - NAME-04

must_haves:
  truths:
    - "Running compute-per-model-gates.cjs produces gate JSON files with wiring_evidence_score, wiring_purpose_score, wiring_coverage_score fields (not grounding_score/gate_b_score/gate_c_score)"
    - "All scripts that consume gate scores (nf-solve.cjs, cross-layer-dashboard.cjs, solve-trend-helpers.cjs) read with fallback: prefer wiring_*_score, fall back to old field names"
    - "Old gate JSON files with legacy field names (grounding_score, gate_b_score, gate_c_score) are still readable without errors — existing data is not lost during transition"
    - "Gate JSON files contain schema_version '2' and new field names after compute-per-model-gates.cjs runs"
  artifacts:
    - path: "bin/gate-field-compatibility.test.cjs"
      provides: "Central compatibility tests for old/new gate field fallback logic"
      min_lines: 60
    - path: "test/fixtures/gate-a-old-format.json"
      provides: "Gate A fixture with legacy grounding_score field"
    - path: "test/fixtures/gate-a-new-format.json"
      provides: "Gate A fixture with new wiring_evidence_score field"
  key_links:
    - from: "bin/compute-per-model-gates.cjs"
      to: ".planning/formal/gates/gate-a-grounding.json"
      via: "fs.writeFileSync with wiring_evidence_score"
      pattern: "wiring_evidence_score"
    - from: "bin/nf-solve.cjs"
      to: ".planning/formal/gates/gate-a-grounding.json"
      via: "fallback read: wiring_evidence_score || grounding_score"
      pattern: "wiring_evidence_score.*grounding_score"
    - from: "bin/solve-trend-helpers.cjs"
      to: ".planning/formal/gates/"
      via: "fallback read for trend extraction"
      pattern: "wiring_evidence_score.*grounding_score"
---

<objective>
Rename gate score field names from legacy (grounding_score, gate_b_score, gate_c_score) to descriptive wiring names (wiring_evidence_score, wiring_purpose_score, wiring_coverage_score) across all producers and consumers, with backward-compatible migration.

Purpose: Gate score fields currently use inconsistent, opaque naming. Unified wiring_*_score naming provides semantic clarity and prepares the schema for Phase v0.34-02 semantic scoring. Backward compatibility ensures existing gate JSON data remains readable.

Output: compute-per-model-gates.cjs writes new field names. All consumer scripts read with fallback. Gate JSON files carry schema_version "2". Compatibility test suite validates both old and new formats.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.34-01-gate-renaming/v0.34-01-RESEARCH.md
@bin/compute-per-model-gates.cjs (producer: writes grounding_score, gate_b_score, gate_c_score)
@bin/nf-solve.cjs (consumer: reads grounding_score, gate_b_score, gate_c_score around lines 2199-2290)
@bin/cross-layer-dashboard.cjs (consumer: reads scores around line 165-179)
@bin/solve-trend-helpers.cjs (consumer: reads scores around lines 35-50)
@.planning/formal/gates/gate-a-grounding.json (current schema_version: "1")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gate score producer and create compatibility fixtures</name>
  <files>
    bin/compute-per-model-gates.cjs
    test/fixtures/gate-a-old-format.json
    test/fixtures/gate-a-new-format.json
    test/fixtures/gate-b-old-format.json
    test/fixtures/gate-b-new-format.json
    test/fixtures/gate-c-old-format.json
    test/fixtures/gate-c-new-format.json
  </files>
  <action>
**Part A: Update compute-per-model-gates.cjs to write new field names**

In compute-per-model-gates.cjs, update the aggregate gate output objects (around lines 320-345) to write new field names:

1. Gate A output (line ~322): Change `grounding_score: groundingScore` to `wiring_evidence_score: groundingScore`
2. Gate B output (line ~334): Change `gate_b_score: gateBScore` to `wiring_purpose_score: gateBScore`
3. Gate C output (line ~342): Change `gate_c_score: gateCScore` to `wiring_coverage_score: gateCScore`
4. In all three output objects, change `schema_version: "1"` to `schema_version: "2"`

Update console.log lines (around lines 746-748) to reference the new field names:
- Line 746: `aggregate.gate_a.wiring_evidence_score` (was `grounding_score`)
- Line 747: `aggregate.gate_b.wiring_purpose_score` (was `gate_b_score`)
- Line 748: `aggregate.gate_c.wiring_coverage_score` (was `gate_c_score`)

Do NOT rename the `gate_a`, `gate_b`, `gate_c` object keys in per-model-gates.json — those are structural keys, not score fields.

**Part B: Create test fixture files**

Create `test/fixtures/` directory if it doesn't exist. Create 6 fixture files:

`gate-a-old-format.json`:
```json
{ "schema_version": "1", "generated": "2026-01-01T00:00:00.000Z", "grounding_score": 0.95, "target": 0.8, "target_met": true, "explained": 19, "total": 20, "unexplained_counts": { "instrumentation_bug": 0, "model_gap": 1, "genuine_violation": 0 }, "scope": { "mode": "per-model-aggregate" } }
```

`gate-a-new-format.json`:
```json
{ "schema_version": "2", "generated": "2026-01-01T00:00:00.000Z", "wiring_evidence_score": 0.95, "target": 0.8, "target_met": true, "explained": 19, "total": 20, "unexplained_counts": { "instrumentation_bug": 0, "model_gap": 1, "genuine_violation": 0 }, "scope": { "mode": "per-model-aggregate" } }
```

`gate-b-old-format.json`:
```json
{ "schema_version": "1", "generated": "2026-01-01T00:00:00.000Z", "gate_b_score": 0.98, "total_entries": 100, "grounded_entries": 98, "orphaned_entries": 2, "target": 1, "target_met": false }
```

`gate-b-new-format.json`:
```json
{ "schema_version": "2", "generated": "2026-01-01T00:00:00.000Z", "wiring_purpose_score": 0.98, "total_entries": 100, "grounded_entries": 98, "orphaned_entries": 2, "target": 1, "target_met": false }
```

`gate-c-old-format.json`:
```json
{ "schema_version": "1", "generated": "2026-01-01T00:00:00.000Z", "gate_c_score": 0.80, "total_entries": 100, "validated_entries": 80, "unvalidated_entries": 20, "target": 0.8, "target_met": true }
```

`gate-c-new-format.json`:
```json
{ "schema_version": "2", "generated": "2026-01-01T00:00:00.000Z", "wiring_coverage_score": 0.80, "total_entries": 100, "validated_entries": 80, "unvalidated_entries": 20, "target": 0.8, "target_met": true }
```
  </action>
  <verify>
grep 'wiring_evidence_score' bin/compute-per-model-gates.cjs returns match
grep 'wiring_purpose_score' bin/compute-per-model-gates.cjs returns match
grep 'wiring_coverage_score' bin/compute-per-model-gates.cjs returns match
grep -c 'grounding_score' bin/compute-per-model-gates.cjs returns 0 (no old field writes remaining)
ls test/fixtures/gate-a-old-format.json test/fixtures/gate-a-new-format.json (both exist)
node -e "const f = require('./test/fixtures/gate-a-new-format.json'); console.log(f.wiring_evidence_score)" prints 0.95
  </verify>
  <done>
compute-per-model-gates.cjs writes wiring_evidence_score, wiring_purpose_score, wiring_coverage_score with schema_version "2". All 6 fixture files created with valid JSON in old and new formats. No references to old field names remain in the producer script.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add backward-compatible fallback reads in all consumers and create compatibility tests</name>
  <files>
    bin/nf-solve.cjs
    bin/nf-solve.test.cjs
    bin/cross-layer-dashboard.cjs
    bin/solve-trend-helpers.cjs
    bin/solve-trend.test.cjs
    bin/compute-per-model-gates.test.cjs
    bin/gate-field-compatibility.test.cjs
  </files>
  <action>
**Part A: Update nf-solve.cjs consumer reads (around lines 2199-2290)**

Update the three gate-reading sections to use fallback pattern:

1. Gate A read (line ~2199): Change `const score = gateA.grounding_score || 0;` to `const score = gateA.wiring_evidence_score || gateA.grounding_score || 0;`
2. Gate A write (line ~2204): Change `grounding_score: score` to `wiring_evidence_score: score`
3. Gate B read (line ~2232): Change `const score = gateB.gate_b_score || 0;` to `const score = gateB.wiring_purpose_score || gateB.gate_b_score || 0;`
4. Gate B write (line ~2239): Change `gate_b_score: score` to `wiring_purpose_score: score`
5. Gate C read (line ~2283): Change `const score = gateC.gate_c_score || 0;` to `const score = gateC.wiring_coverage_score || gateC.gate_c_score || 0;`
6. Gate C write (line ~2288): Change `gate_c_score: score` to `wiring_coverage_score: score`

**Part B: Update cross-layer-dashboard.cjs (around lines 165-179)**

1. Line ~165: Change `score: gateA.grounding_score` to `score: gateA.wiring_evidence_score || gateA.grounding_score`
2. Line ~172: Change `score: gateB.gate_b_score` to `score: gateB.wiring_purpose_score || gateB.gate_b_score`
3. Line ~179: Change `score: gateC.gate_c_score` to `score: gateC.wiring_coverage_score || gateC.gate_c_score`

**Part C: Update solve-trend-helpers.cjs (around lines 35-50)**

1. Gate A (line ~38): Change `result.a = { score: raw.grounding_score, ...}` to `result.a = { score: raw.wiring_evidence_score || raw.grounding_score, ...}`
2. Gate B (line ~44): Change `result.b = { score: raw.gate_b_score, ...}` to `result.b = { score: raw.wiring_purpose_score || raw.gate_b_score, ...}`
3. Gate C (line ~50): Change `result.c = { score: raw.gate_c_score, ...}` to `result.c = { score: raw.wiring_coverage_score || raw.gate_c_score, ...}`

Update comments from "uses grounding_score" etc. to mention both field names.

**Part D: Update existing test files**

In `bin/compute-per-model-gates.test.cjs`:
- Update field name assertions to check for new field names (wiring_evidence_score, wiring_purpose_score, wiring_coverage_score)
- Update the gateAFields, gateBFields, gateCFields arrays to list new field names
- Update assertClose() calls to reference new field names

In `bin/nf-solve.test.cjs`:
- Update assertions on lines ~733, ~746, ~759 from `'grounding_score' in result.detail` to `'wiring_evidence_score' in result.detail`, and similarly for gate_b_score and gate_c_score

In `bin/solve-trend.test.cjs`:
- Update test fixture objects (around lines 46-52) to use new field names
- Keep old field name fixtures as a separate test case to verify backward compat

**Part E: Create gate-field-compatibility.test.cjs**

Create `bin/gate-field-compatibility.test.cjs` using `node:test` + `node:assert/strict` pattern (matching existing test files like compute-per-model-gates.test.cjs).

Test cases:
1. Read old `grounding_score` when `wiring_evidence_score` absent — should return the old value
2. Read new `wiring_evidence_score` when present — should prefer new over old
3. Read old `gate_b_score` when `wiring_purpose_score` absent
4. Read new `wiring_purpose_score` when present — prefer new
5. Read old `gate_c_score` when `wiring_coverage_score` absent
6. Read new `wiring_coverage_score` when present — prefer new
7. Both old and new JSON fixtures parse without errors (load all 6 fixture files)
8. A file with BOTH old and new field names prefers the new one

For each test, implement the same fallback logic used in consumer scripts:
```javascript
const readGateScore = (data, gate) => {
  if (gate === 'a') return data.wiring_evidence_score || data.grounding_score || 0;
  if (gate === 'b') return data.wiring_purpose_score || data.gate_b_score || 0;
  if (gate === 'c') return data.wiring_coverage_score || data.gate_c_score || 0;
  return 0;
};
```

Register the test in package.json test:ci script if a test:ci script exists, otherwise verify it runs standalone with `node --test bin/gate-field-compatibility.test.cjs`.
  </action>
  <verify>
node --test bin/gate-field-compatibility.test.cjs 2>&1 | tail -5 (all tests pass, 0 failures)
node --test bin/compute-per-model-gates.test.cjs 2>&1 | tail -5 (existing tests updated and pass)
node --test bin/nf-solve.test.cjs 2>&1 | tail -5 (updated assertions pass)
node --test bin/solve-trend.test.cjs 2>&1 | tail -5 (updated tests pass)
grep 'wiring_evidence_score' bin/nf-solve.cjs returns matches
grep 'wiring_evidence_score.*grounding_score' bin/cross-layer-dashboard.cjs returns matches (fallback pattern)
grep 'wiring_evidence_score.*grounding_score' bin/solve-trend-helpers.cjs returns matches (fallback pattern)
  </verify>
  <done>
All consumer scripts (nf-solve.cjs, cross-layer-dashboard.cjs, solve-trend-helpers.cjs) read gate scores with fallback: prefer wiring_*_score, fall back to legacy field names. All existing tests updated to reference new field names. gate-field-compatibility.test.cjs validates both old and new format fixtures load correctly and fallback logic works. Backward compatibility preserved: old gate JSON files with grounding_score/gate_b_score/gate_c_score are still readable.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'grounding_score' bin/compute-per-model-gates.cjs` returns 0 (producer no longer writes old fields)
- `grep 'wiring_evidence_score' bin/compute-per-model-gates.cjs` confirms new field written
- `grep 'wiring_evidence_score.*grounding_score' bin/nf-solve.cjs` confirms fallback read pattern
- `grep 'wiring_purpose_score.*gate_b_score' bin/cross-layer-dashboard.cjs` confirms fallback read pattern
- `grep 'wiring_coverage_score.*gate_c_score' bin/solve-trend-helpers.cjs` confirms fallback read pattern
- `node --test bin/gate-field-compatibility.test.cjs` passes all 8+ tests
- `node --test bin/compute-per-model-gates.test.cjs` passes with new field names
- All 6 fixture files exist in test/fixtures/ and parse as valid JSON
</verification>

<success_criteria>
- compute-per-model-gates.cjs writes wiring_evidence_score, wiring_purpose_score, wiring_coverage_score with schema_version "2"
- nf-solve.cjs, cross-layer-dashboard.cjs, solve-trend-helpers.cjs all use fallback reads (new || old || 0)
- Old format gate JSON files remain readable (backward compat)
- All tests pass: gate-field-compatibility, compute-per-model-gates, nf-solve, solve-trend
- 6 fixture files created for old/new format validation
</success_criteria>

<output>
After completion, create `.planning/phases/v0.34-01-gate-renaming/v0.34-01-01-SUMMARY.md`
</output>
