---
phase: v0.34-01
plan: "01"
subsystem: gate-scoring
tags:
  - gate-renaming
  - backward-compatibility
  - schema-migration
  - field-naming

dependency_graph:
  requires: []
  provides:
    - gate-score-fields-renamed
    - backward-compatible-fallback-reads
    - gate-field-compatibility-test-suite
  affects:
    - bin/compute-per-model-gates.cjs
    - bin/nf-solve.cjs
    - bin/cross-layer-dashboard.cjs
    - bin/solve-trend-helpers.cjs

tech_stack:
  added: []
  patterns:
    - "Fallback field read: new_field || legacy_field || default"
    - "Schema versioning: schema_version field bumped from '1' to '2'"
    - "Fixture-driven compatibility tests for old/new field formats"

key_files:
  created:
    - bin/gate-field-compatibility.test.cjs
    - test/fixtures/gate-a-old-format.json
    - test/fixtures/gate-a-new-format.json
    - test/fixtures/gate-b-old-format.json
    - test/fixtures/gate-b-new-format.json
    - test/fixtures/gate-c-old-format.json
    - test/fixtures/gate-c-new-format.json
  modified:
    - bin/compute-per-model-gates.cjs
    - bin/nf-solve.cjs
    - bin/cross-layer-dashboard.cjs
    - bin/solve-trend-helpers.cjs
    - bin/compute-per-model-gates.test.cjs
    - bin/nf-solve.test.cjs
    - bin/solve-trend.test.cjs

decisions:
  - "Schema version bumped to '2' in all three gate output files (gate-a-grounding.json, gate-b-abstraction.json, gate-c-validation.json)"
  - "Consumer fallback pattern: prefer wiring_*_score (new), fall back to legacy field names, then 0 — ensures zero data loss during transition"
  - "gate-a, gate-b, gate-c object keys in per-model-gates.json left unchanged — those are structural keys, not score fields"
  - "21 compatibility tests created to validate both old/new format fixtures and fallback logic"

metrics:
  duration: "~35 minutes"
  completed: "2026-03-11"
  tasks_completed: 2
  files_created: 7
  files_modified: 7
---

# Phase v0.34-01 Plan 01: Gate Score Field Renaming Summary

Gate score field names renamed from legacy opaque names (grounding_score, gate_b_score, gate_c_score) to descriptive wiring names (wiring_evidence_score, wiring_purpose_score, wiring_coverage_score) with full backward compatibility across producer and all consumers.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Update gate score producer and create compatibility fixtures | 10adab48 | compute-per-model-gates.cjs, 6 fixture files |
| 2 | Add backward-compatible fallback reads in all consumers and create compatibility tests | b01db438 | nf-solve.cjs, cross-layer-dashboard.cjs, solve-trend-helpers.cjs, 3 test files, gate-field-compatibility.test.cjs |

## What Was Built

**Producer update (compute-per-model-gates.cjs):**
- `grounding_score` renamed to `wiring_evidence_score` in Gate A output
- `gate_b_score` renamed to `wiring_purpose_score` in Gate B output
- `gate_c_score` renamed to `wiring_coverage_score` in Gate C output
- `schema_version` bumped from `'1'` to `'2'` in all three gate file outputs
- Console log lines updated to reference new field names

**Consumer updates (all with fallback reads):**
- `nf-solve.cjs`: reads `wiring_*_score || legacy_score || 0` for all three gates; writes new field names in detail objects
- `cross-layer-dashboard.cjs`: reads `wiring_*_score || legacy_score` for all three gates
- `solve-trend-helpers.cjs`: reads `wiring_*_score || legacy_score` for all three gates; comments updated

**Test fixture files (6 created):**
- `gate-a-old-format.json` / `gate-a-new-format.json`
- `gate-b-old-format.json` / `gate-b-new-format.json`
- `gate-c-old-format.json` / `gate-c-new-format.json`

**Test suite updates:**
- `compute-per-model-gates.test.cjs`: 57 tests, all assertions updated to new field names (57 pass)
- `nf-solve.test.cjs`: wiring_*_score keys checked in detail assertions
- `solve-trend.test.cjs`: fixtures updated to schema v2, backward compat test added for legacy fields (16 pass)
- `gate-field-compatibility.test.cjs`: 21 tests validating old/new format and fallback precedence

## Verification Results

- `grep -c 'grounding_score' bin/compute-per-model-gates.cjs` = 0 (no legacy field writes in producer)
- `grep 'wiring_evidence_score' bin/compute-per-model-gates.cjs` = confirmed
- `grep 'wiring_evidence_score.*grounding_score' bin/nf-solve.cjs` = confirmed fallback
- `grep 'wiring_purpose_score.*gate_b_score' bin/cross-layer-dashboard.cjs` = confirmed fallback
- `grep 'wiring_coverage_score.*gate_c_score' bin/solve-trend-helpers.cjs` = confirmed fallback
- `node bin/gate-field-compatibility.test.cjs`: 21 pass, 0 fail
- `node bin/compute-per-model-gates.test.cjs`: 57 pass, 0 fail
- `node --test bin/solve-trend.test.cjs`: 16 pass, 0 fail
- All 6 fixture files created and parse as valid JSON

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

Files created verified:
- bin/gate-field-compatibility.test.cjs: FOUND
- test/fixtures/gate-a-old-format.json: FOUND
- test/fixtures/gate-a-new-format.json: FOUND
- test/fixtures/gate-b-old-format.json: FOUND
- test/fixtures/gate-b-new-format.json: FOUND
- test/fixtures/gate-c-old-format.json: FOUND
- test/fixtures/gate-c-new-format.json: FOUND

Commits verified:
- 10adab48: Task 1 — producer update + fixtures
- b01db438: Task 2 — consumer fallbacks + compatibility tests
