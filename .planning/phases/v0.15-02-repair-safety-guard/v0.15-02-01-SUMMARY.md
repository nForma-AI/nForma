---
phase: v0.15-02
plan: 01
subsystem: testing
tags: [gsd-tools, validate, repair, safety-guard, tdd]

# Dependency graph
requires:
  - phase: v0.15-01
    provides: phasePattern fixes for ROADMAP health checks; baseline 145 tests
provides:
  - Content-length safety gate in validate health --repair preventing silent STATE.md overwrite
  - --force flag for validate health subcommand (opt-in overwrite of rich STATE.md)
  - 3 new SAFE-01 tests confirming guard behavior
affects: [validate-health, repair-workflow, state-management]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Content-length guard: read existing file, check split('\\n').length > threshold before overwrite"
    - "--force flag pattern: args.includes('--force') at arg handler, pass as options.force to cmdFunction"
    - "TDD Wave 0: write failing tests first, confirm RED before implementing fix"

key-files:
  created: []
  modified:
    - qgsd-core/bin/gsd-tools.test.cjs
    - qgsd-core/bin/gsd-tools.cjs

key-decisions:
  - "Threshold hardcoded as SAFE_LINE_THRESHOLD=50 constant inside regenerateState case (not a runtime flag)"
  - "Warning output in repairs_performed JSON array only — no stderr/console.warn (consistent with rest of file)"
  - "Guard placed inside case 'regenerateState' only — createConfig and resetConfig unaffected"
  - "W002 trigger used for tests: STATE.md references v0.99-99 (phantom) while only v0.99-01-test exists on disk"

patterns-established:
  - "W002 phantom-phase trigger: reference v0.99-NN in STATE.md while creating v0.99-01-test dir on disk"

requirements-completed: [SAFE-01]

# Metrics
duration: 4min
completed: 2026-02-27
---

# Phase v0.15-02 Plan 01: Repair Safety Guard Summary

**Content-length guard in validate health --repair that blocks silent STATE.md overwrite if file >50 lines, requiring explicit --force to proceed**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-27T00:59:04Z
- **Completed:** 2026-02-27T01:03:48Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Added SAFE_LINE_THRESHOLD=50 guard in regenerateState case that pushes `{ action: 'regenerateState', success: false, skipped: true, reason: 'STATE.md has N lines (threshold: 50). Re-run with --force to overwrite.' }` and breaks without touching the file
- Added `--force` flag parsing in validate health arg handler and passed as `options.force` to cmdValidateHealth
- Smoke test confirmed: `validate health --repair` on real QGSD STATE.md (76 lines) produces skipped entry and leaves file unchanged
- 3 new SAFE-01 TDD tests all green (148 total: 145 baseline + 3 SAFE-01), zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1 (Wave 0): Write failing test scaffold for SAFE-01** - `9baedf5` (test)
2. **Task 2 (Wave 1): Implement --force flag parsing and content-length guard** - `39551fb` (feat)
3. **Task 3 (Wave 2): Install sync, smoke test, and full regression** - (no source change; install copies to ~/.claude/)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `qgsd-core/bin/gsd-tools.test.cjs` - Appended SAFE-01 describe block with 3 tests (TC-01/TC-02/TC-03)
- `qgsd-core/bin/gsd-tools.cjs` - Added --force flag parsing at arg handler + content-length guard in regenerateState case

## Decisions Made
- Threshold is 50 lines (per SAFE-01 requirement), hardcoded as named constant inside the case block
- Used W002 trigger for tests (phantom phase v0.99-99 referenced in STATE.md, only v0.99-01-test dir on disk) rather than E004 (missing STATE.md) to allow line-count guard to read existing content
- Phantom phase must match regex `v\d+\.\d+-\d{2}` — test initially used "v0.phantom-01" which did not match the extractor regex; corrected to "v0.99-99"

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected phantom phase format in test setup**
- **Found during:** Task 1 (Wave 0 test scaffold)
- **Issue:** Test STATE.md used `Phase v0.phantom-01` which does not match the phase-reference extractor regex `/[Pp]hase\s+(v\d+\.\d+-\d{2}...)/g` — W002 never fired, so regenerateState was never queued, all 3 tests failed with "file unchanged" (not the expected RED/GREEN pattern)
- **Fix:** Changed phantom phase to `v0.99-99` which matches regex; only `v0.99-01-test` dir exists on disk so W002 fires correctly
- **Files modified:** `qgsd-core/bin/gsd-tools.test.cjs`
- **Verification:** Wave 0 re-run: TC-01 RED, TC-02 PASS, TC-03 PASS (147 pass, 1 fail as expected)
- **Committed in:** 9baedf5 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 bug — incorrect test setup)
**Impact on plan:** Fix was essential for correct TDD wave 0 behavior. No scope creep.

## Issues Encountered
- npm test shows 2 pre-existing failures (TC-PROMPT-N-CAP and TC-DEFAULT-CEIL-PASS in qgsd-stop hook tests) that existed before this plan (baseline was 341 pass, 6 fail). These are out of scope. The gsd-tools.cjs suite shows 148 pass, 0 fail as required.

## Next Phase Readiness
- SAFE-01 requirement fully satisfied: guard in place, install sync complete, real STATE.md protected
- v0.15-02 phase complete; next milestone planning can proceed

---
*Phase: v0.15-02*
*Completed: 2026-02-27*
