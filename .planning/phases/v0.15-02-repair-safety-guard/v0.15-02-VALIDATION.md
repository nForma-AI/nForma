---
phase: v0.15-02
slug: repair-safety-guard
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-27
---

# Phase v0.15-02 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.15-02`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.15-02`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (Node.js built-in) + `node:assert` |
| **Config file** | none — built-in runner |
| **Quick run command** | `node --test qgsd-core/bin/gsd-tools.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~5 seconds |
| **CI pipeline** | none |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test qgsd-core/bin/gsd-tools.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.15-02-01-01 | 01 | 0 | SAFE-01 | unit | `node --test qgsd-core/bin/gsd-tools.test.cjs` | ❌ W0 | ⬜ pending |
| v0.15-02-01-02 | 01 | 1 | SAFE-01 | unit | `node --test qgsd-core/bin/gsd-tools.test.cjs` | ✅ | ⬜ pending |
| v0.15-02-01-03 | 01 | 1 | SAFE-01 | unit | `node --test qgsd-core/bin/gsd-tools.test.cjs` | ✅ | ⬜ pending |
| v0.15-02-01-04 | 01 | 2 | SAFE-01 | smoke | `node bin/install.js --claude --global && node ~/.claude/qgsd/bin/gsd-tools.cjs validate health --repair 2>/dev/null` | ✅ N/A | ⬜ pending |
| v0.15-02-01-05 | 01 | 2 | SAFE-01 | unit | `npm test` | ✅ | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `qgsd-core/bin/gsd-tools.test.cjs` — append `describe('SAFE-01: --repair safety guard for rich STATE.md', ...)` with 3 test stubs:
  - TC-01: `--repair` without `--force` does NOT overwrite STATE.md with >50 lines (must FAIL before fix)
  - TC-02: `--repair --force` proceeds and overwrites STATE.md with >50 lines
  - TC-03: `--repair` without `--force` overwrites STATE.md with ≤50 lines normally

*TC-01 must be RED before implementation. TC-02 and TC-03 may already pass with current code.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Smoke test against real QGSD STATE.md (160 lines) | SAFE-01 | Requires live install + real repo | Run `node ~/.claude/qgsd/bin/gsd-tools.cjs validate health --repair` from QGSD root; confirm STATE.md line count unchanged |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 5s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.15-02`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 2 | `node --test qgsd-core/bin/gsd-tools.test.cjs` | — | — | ⬜ sampled |
| 2 | 2 | `npm test` | — | — | ⬜ sampled |

**Phase validation complete:** pending
