---
phase: v0.24-03-quorum-observability
plan: 02
type: execute
wave: 1
depends_on: [v0.24-03-01]
files_modified:
  - bin/call-quorum-slot.cjs
autonomous: true
requirements: [OBS-01]
formal_artifacts: none

must_haves:
  truths:
    - "After a quorum round completes, a per-session log file contains structured telemetry for every slot that was dispatched: slot name, round number, verdict, latency in milliseconds, and provider status"
    - "Telemetry logging is fail-open: a failure in recordTelemetry never blocks or crashes the quorum dispatch — the slot call still succeeds or fails on its own merits"
    - "Session ID is resolved from CLAUDE_SESSION_ID environment variable with a fallback to session-<timestamp> when the variable is absent"
    - "Latency is measured for the final successful attempt only; retry_count is recorded separately so flakiness scoring can distinguish retried-but-succeeded from timed-out"
  artifacts:
    - path: "bin/call-quorum-slot.cjs"
      provides: "recordTelemetry function appending JSONL to .planning/quorum-rounds-<session>.jsonl"
      contains: "recordTelemetry"
    - path: ".planning/quorum-rounds-*.jsonl"
      provides: "Per-session structured telemetry log (created at runtime)"
  key_links:
    - from: "bin/call-quorum-slot.cjs"
      to: ".planning/quorum-rounds-<session>.jsonl"
      via: "fs.appendFileSync in recordTelemetry"
      pattern: "appendFileSync.*quorum-rounds"
    - from: "bin/call-quorum-slot.cjs"
      to: "bin/providers.json"
      via: "provider field lookup for telemetry record"
      pattern: "provider\\.provider|provider\\.name"
---

<objective>
Implement per-slot telemetry logging in call-quorum-slot.cjs so every slot dispatch produces a
structured JSONL record with slot name, round number, verdict, latency, provider status, and
retry count.

Purpose: OBS-01 — enables root-cause analysis for timeout patterns, provider degradation, and
retry storms. This telemetry is the foundation for delivery rate tracking (OBS-02) and flakiness
scoring (OBS-03).
Output: Modified call-quorum-slot.cjs with recordTelemetry function; runtime JSONL log files.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.24-03-quorum-observability/v0.24-03-RESEARCH.md
@.planning/phases/v0.24-03-quorum-observability/v0.24-03-01-SUMMARY.md
@bin/call-quorum-slot.cjs
@bin/providers.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recordTelemetry function and integrate into dispatch flow</name>
  <files>bin/call-quorum-slot.cjs</files>
  <action>
    Add a recordTelemetry function to bin/call-quorum-slot.cjs following the exact same fail-open
    pattern as the existing appendTokenSentinel function (lines 37-53).

    1. Add recordTelemetry function near the top of the file (after appendTokenSentinel):

    function recordTelemetry(slotName, round, verdict, latencyMs, provider, providerStatus, retryCount, errorType):
    - Build a JSON record with fields: ts (ISO timestamp), session_id, round (parsed int, default 0),
      slot, verdict, latency_ms, provider, provider_status, retry_count, error_type
    - Resolve session_id from process.env.CLAUDE_SESSION_ID with fallback to 'session-' + Date.now()
    - Compute logPath as path.join(findProjectRoot(), '.planning', 'quorum-rounds-' + sessionId + '.jsonl')
    - fs.mkdirSync(path.dirname(logPath), { recursive: true })
    - fs.appendFileSync(logPath, record + '\n', 'utf8')
    - Wrap entire body in try/catch — catch writes to stderr and does NOT rethrow

    2. Add --round CLI argument parsing (after existing getArg calls):
    const roundNum = getArg('--round');

    3. Modify main() to capture timing and call recordTelemetry:

    In the success path (after result is obtained, before process.stdout.write(result)):
    - Capture start time before the dispatch call: const startMs = Date.now();
    - Compute latency after dispatch: const latencyMs = Date.now() - startMs;
    - Extract verdict from the result using simple regex:
      const verdict = (/APPROVE|BLOCK|FLAG/.exec(result) || [])[0] || 'UNKNOWN';
    - Get provider name from provider object: provider.provider || provider.name
    - Call recordTelemetry with all fields, retryCount = 0, errorType = null

    In the error/catch path (after writeFailureLog, before process.exit(1)):
    - Call recordTelemetry with verdict = 'UNAVAIL', latency from startMs to now,
      retryCount from retry attempts, errorType from error classification.

    4. Track retry count: Add a let retryAttempts = 0 variable in main() scope.
    Wrap the dispatch call so that retryWithBackoff increments this counter on each retry.
    The simplest approach: pass a callback to retryWithBackoff that increments the counter
    on each attempt after the first. Or extract the count after retryWithBackoff completes
    by comparing attempts.

    IMPORTANT constraints:
    - Do NOT change the existing behavior of retryWithBackoff, appendTokenSentinel, or writeFailureLog
    - recordTelemetry is purely additive — observational, never blocking
    - The --round argument is optional. If not provided, default to 0 for backward compatibility
    - For verdict extraction, use simple regex on result output. Do NOT import quorum-slot-dispatch.cjs
    - Keep call-quorum-slot.cjs self-contained — no new require() calls needed
  </action>
  <verify>
    Run the TDD structural tests from Plan 01 — they should now pass:
    ```bash
    node --test bin/quorum-telemetry.test.cjs 2>&1 | grep -E "(not ok|ok )"
    ```
    All structural tests should show "ok" (GREEN). Unit tests should remain GREEN.

    Manual smoke test:
    ```bash
    echo "test prompt" | CLAUDE_SESSION_ID=test-smoke node bin/call-quorum-slot.cjs --slot gemini-1 --round 1 --timeout 5000 2>/dev/null; cat .planning/quorum-rounds-test-smoke.jsonl 2>/dev/null | head -1
    ```
    (The slot call may fail due to no real provider, but the telemetry record should still be written in the error path.)
  </verify>
  <done>
    - recordTelemetry function exists in bin/call-quorum-slot.cjs
    - Function appends JSONL records to .planning/quorum-rounds-SESSION.jsonl
    - Each record has all required fields: ts, session_id, round, slot, verdict, latency_ms, provider, provider_status, retry_count, error_type
    - Session ID falls back to 'session-TIMESTAMP' when CLAUDE_SESSION_ID is absent
    - Fail-open: telemetry errors are caught and logged to stderr, never throw
    - All structural tests in bin/quorum-telemetry.test.cjs pass (GREEN)
    - Integration test gate: All structural + unit tests from Plan 01 pass GREEN (0 failures): node --test bin/quorum-telemetry.test.cjs bin/scoreboard-observability.test.cjs
  </done>
</task>

</tasks>

<verification>
```bash
# Run all OBS-01 tests
node --test bin/quorum-telemetry.test.cjs 2>&1 | tail -5

# Verify recordTelemetry function exists
grep -c 'recordTelemetry' bin/call-quorum-slot.cjs

# Verify JSONL fields in source
grep -c 'quorum-rounds' bin/call-quorum-slot.cjs
grep -c 'session_id' bin/call-quorum-slot.cjs
grep -c 'latency_ms' bin/call-quorum-slot.cjs
```
</verification>

<success_criteria>
- All tests in bin/quorum-telemetry.test.cjs pass (0 failures)
- bin/call-quorum-slot.cjs contains recordTelemetry function
- Telemetry records contain all required fields (ts, session_id, round, slot, verdict, latency_ms, provider, provider_status, retry_count, error_type)
- Fail-open: recordTelemetry wrapped in try/catch, errors logged to stderr
- Session ID resolves from CLAUDE_SESSION_ID with fallback
- --round CLI argument accepted (optional, defaults to 0)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.24-03-quorum-observability/v0.24-03-02-SUMMARY.md`
</output>
