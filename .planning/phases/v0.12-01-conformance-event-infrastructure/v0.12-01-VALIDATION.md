---
phase: v0.12-01
slug: conformance-event-infrastructure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-24
---

# Phase v0.12-01 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.12-01`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.12-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `node:test` |
| **Config file** | none — direct invocation |
| **Quick run command** | `node --test bin/validate-traces.test.cjs` |
| **Full suite command** | `node scripts/lint-isolation.js && node --test hooks/qgsd-stop.test.js hooks/config-loader.test.js get-shit-done/bin/gsd-tools.test.cjs hooks/qgsd-circuit-breaker.test.js hooks/qgsd-prompt.test.js bin/update-scoreboard.test.cjs hooks/qgsd-statusline.test.js bin/review-mcp-logs.test.cjs bin/migrate-to-slots.test.cjs bin/validate-traces.test.cjs` |
| **Estimated runtime** | ~20 seconds (existing ~18s + new test file) |

> Note: `bin/validate-traces.test.cjs` must be added to `package.json` `"test"` script as part of Wave 0.

---

## Nyquist Sampling Rate

- **After every task commit:** Run `node --test bin/validate-traces.test.cjs`
- **After every plan wave:** Run full suite command above
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** ~20 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| W0-01 | 01 | 0 | LOG-01, LOG-03, VAL-01, VAL-02 | scaffold | `node --test bin/validate-traces.test.cjs` | ❌ W0 | ⬜ pending |
| 01-01 | 01 | 1 | LOG-01 | unit | `node --test bin/validate-traces.test.cjs` | ✅ W0 | ⬜ pending |
| 01-02 | 01 | 1 | LOG-02, LOG-03 | integration | `node --test hooks/qgsd-stop.test.js` | ✅ existing | ⬜ pending |
| 01-03 | 01 | 1 | LOG-02, LOG-03 | integration | `node --test hooks/qgsd-prompt.test.js` | ✅ existing | ⬜ pending |
| 02-01 | 02 | 2 | XST-01, XST-02, XST-03 | smoke | `npx tsc --project tsconfig.formal.json --noEmit && npx tsup src/machines/qgsd-workflow.machine.ts --format cjs --out-dir dist/machines --no-splitting --tsconfig tsconfig.formal.json && test -f dist/machines/qgsd-workflow.machine.cjs` | ❌ W0 | ⬜ pending |
| 03-01 | 03 | 3 | VAL-01, VAL-02, VAL-03 | unit + integration | `node --test bin/validate-traces.test.cjs` | ✅ W0 | ⬜ pending |
| 03-02 | 03 | 3 | VAL-03 | manual | `node bin/install.js --claude --global && ls ~/.claude/qgsd-bin/validate-traces.cjs` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/validate-traces.test.cjs` — failing stubs for:
  - Schema module exports (LOG-01): `VALID_ACTIONS`, `VALID_PHASES`, `VALID_OUTCOMES`, `schema_version` exist
  - Event field shape (LOG-03): stub checks `{ ts, phase, action, slots_available, vote_result, outcome }`
  - Deviation score formula (VAL-02): stub asserts correct percentage computation
  - Exit code 0 on empty log (VAL-01): stub asserts validator exits 0 with no lines
  - Exit code 1 on divergence (VAL-01): stub asserts validator exits 1 when a trace mismatches
- [ ] `package.json` `"test"` script updated to include `bin/validate-traces.test.cjs`

XState machine and tsup build do NOT require Wave 0 stubs — they are TypeScript compile-time artifacts verified by `tsc --noEmit` + file-exists checks.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `validate-traces.cjs` ships to `~/.claude/qgsd-bin/` | VAL-03 | Requires running installer and inspecting filesystem | Run `node bin/install.js --claude --global` then `ls ~/.claude/qgsd-bin/validate-traces.cjs` |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 20s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.12-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 2 | — | — | — | scaffold |
| 1 | 3 | `node --test bin/validate-traces.test.cjs` + hook tests | — | — | ⬜ pending |
| 2 | 1 | tsc + tsup smoke | — | — | ⬜ pending |
| 3 | 2 | `node --test bin/validate-traces.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
