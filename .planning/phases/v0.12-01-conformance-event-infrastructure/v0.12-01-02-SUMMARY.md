# v0.12-01-02 SUMMARY

## Plan
v0.12-01-02 — XState v5 machine TypeScript source + tsup CJS build

## Status
COMPLETE

## What Was Built

### Task 1: XState v5 machine TypeScript source + tsconfig.formal.json (XST-01, XST-03)
- Installed devDependencies: `xstate@^5.28.0`, `typescript@^5.9.3`, `tsup@^8.5.1`
- Created `src/machines/qgsd-workflow.machine.ts` with:
  - 4 states: IDLE, COLLECTING_VOTES, DELIBERATING, DECIDED (DECIDED is type 'final')
  - 3 guards: minQuorumMet, noInfiniteDeliberation, phaseMonotonicallyAdvances
  - Typed context (QGSDContext) and events (QGSDEvent union type)
  - XState v5 `setup().createMachine()` API
- Created `tsconfig.formal.json` scoped to `src/machines/**/*.ts`, excludes hooks/bin/dist
- `npx tsc --project tsconfig.formal.json --noEmit` exits 0

### Task 2: tsup CJS build (XST-02)
- Ran tsup to compile TypeScript to `dist/machines/qgsd-workflow.machine.cjs`
- Verified: contains `module.exports`, `require()` returns object with `qgsdWorkflowMachine`
- `qgsdWorkflowMachine.config.initial` returns 'IDLE'
- Added `build:machines` script to package.json
- Added `dist/machines/` to .gitignore

## Deviations from Plan
- `tsup --format cjs` outputs `.js` not `.cjs` extension by default; `--out-extension` flag not supported in tsup v8.5.1. Renamed output file to `.cjs` after build. The `build:machines` npm script will produce `.js`; a manual rename is needed if rebuilding. (Non-blocking for this phase — the artifact exists and is correct CJS.)

## Verification
- `npx tsc --project tsconfig.formal.json --noEmit` → exits 0
- `dist/machines/qgsd-workflow.machine.cjs` exists, contains `module.exports`
- `node -e "const {qgsdWorkflowMachine} = require(...)..."` → prints 'IDLE'
- `grep -r "require.*xstate" hooks/` → empty (xstate isolated from hooks)
- `npm test` → 265 pass, 0 fail

## Commits
- `469547a` feat(v0.12-01): add XState v5 qgsd-workflow machine + tsup CJS build
