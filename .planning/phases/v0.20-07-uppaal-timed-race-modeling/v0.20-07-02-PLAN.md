---
phase: v0.20-07
plan: "02"
type: execute
wave: 1
depends_on:
  - v0.20-07-01
files_modified:
  - bin/write-check-result.cjs
  - bin/write-check-result.test.cjs
autonomous: true
requirements:
  - UPPAAL-02

must_haves:
  truths:
    - "bin/write-check-result.cjs VALID_FORMALISMS array includes 'uppaal'"
    - "writeCheckResult({ formalism: 'uppaal', ... }) does not throw"
    - "write-check-result.test.cjs asserts 'uppaal' is valid formalism"
    - "npm test passes — no regressions"
  artifacts:
    - path: "bin/write-check-result.cjs"
      provides: "VALID_FORMALISMS with 'uppaal' added"
      contains: "'uppaal'"
    - path: "bin/write-check-result.test.cjs"
      provides: "Test asserting uppaal formalism valid"
      contains: "uppaal"
  key_links:
    - from: "bin/write-check-result.cjs"
      to: "bin/run-uppaal.cjs"
      via: "require('./write-check-result.cjs') — writeCheckResult with formalism: 'uppaal'"
      pattern: "uppaal"
---

<objective>
Add 'uppaal' to VALID_FORMALISMS in write-check-result.cjs to unblock run-uppaal.cjs.

Purpose: The v2.1 schema validation in write-check-result.cjs rejects unknown formalisms. Without 'uppaal' in the list, any attempt by run-uppaal.cjs to call writeCheckResult will throw. This plan makes the additive change and adds a test guard to prevent future removal.

Output: Modified write-check-result.cjs + write-check-result.test.cjs.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/v0.20-07-uppaal-timed-race-modeling/v0.20-07-RESEARCH.md
@bin/write-check-result.cjs
@bin/write-check-result.test.cjs
@.planning/phases/v0.20-07-uppaal-timed-race-modeling/v0.20-07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'uppaal' to VALID_FORMALISMS in write-check-result.cjs</name>
  <files>bin/write-check-result.cjs</files>
  <action>
    Modify bin/write-check-result.cjs.

    **Step 1: Locate VALID_FORMALISMS.**
    Line: `const VALID_FORMALISMS = ['tla', 'alloy', 'prism', 'trace', 'redaction'];`

    **Step 2: Append 'uppaal' to the array.**
    Change to:
    ```javascript
    const VALID_FORMALISMS = ['tla', 'alloy', 'prism', 'trace', 'redaction', 'uppaal'];
    ```

    **Step 3: Verify inline.**
    Run: `grep "VALID_FORMALISMS" bin/write-check-result.cjs`
    Expected: array includes 'uppaal'.

    Run: `node --check bin/write-check-result.cjs`
    Expected: exits 0 (no syntax errors).

    IMPORTANT — Anti-patterns:
    - Do NOT modify any other line in write-check-result.cjs
    - Do NOT change VALID_RESULTS
    - Do NOT change the export line — `VALID_FORMALISMS` must remain exported
  </action>
  <verify>
    1. `node -e "const {VALID_FORMALISMS} = require('./bin/write-check-result.cjs'); console.log(VALID_FORMALISMS.includes('uppaal'))"` — outputs `true`
    2. `node --check bin/write-check-result.cjs` — exits 0
  </verify>
  <done>
    VALID_FORMALISMS includes 'uppaal'. writeCheckResult with formalism: 'uppaal' does not throw.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test asserting 'uppaal' formalism is valid in write-check-result.test.cjs</name>
  <files>bin/write-check-result.test.cjs</files>
  <action>
    Modify bin/write-check-result.test.cjs to add a guard test for the 'uppaal' formalism.

    **Step 1: Read the file.** Open bin/write-check-result.test.cjs and find the end of the test list.

    **Step 2: Add test.** After the last existing test, add:
    ```javascript
    test('VALID_FORMALISMS includes uppaal (UPPAAL-02)', () => {
      const { VALID_FORMALISMS } = require(MODULE_PATH);
      assert.ok(
        VALID_FORMALISMS.includes('uppaal'),
        'VALID_FORMALISMS must include "uppaal" for run-uppaal.cjs writeCheckResult calls'
      );
    });
    ```

    **Step 3: Verify no regressions.**
    Run: `node --test bin/write-check-result.test.cjs 2>&1 | tail -5`
    Expected: all tests pass.

    IMPORTANT: Do NOT modify any other test in write-check-result.test.cjs.
    The existing test `writeCheckResult throws on invalid formalism` uses `formalism: 'bad'` — adding 'uppaal' to the valid list does NOT break this test (it still throws for 'bad').
  </action>
  <verify>
    1. `node --test bin/write-check-result.test.cjs 2>&1 | tail -5` — all pass
    2. `grep "uppaal" bin/write-check-result.test.cjs` — shows the new test
    3. `npm test 2>&1 | tail -5` — full suite passes
  </verify>
  <done>
    write-check-result.test.cjs has test for 'uppaal' formalism.
    npm test passes with no regressions.
    UPPAAL-02 prerequisite (formalism validation) satisfied.
  </done>
</task>

</tasks>

<verification>
1. `node -e "const {VALID_FORMALISMS} = require('./bin/write-check-result.cjs'); console.log(VALID_FORMALISMS)"` — includes 'uppaal'
2. `node --test bin/write-check-result.test.cjs 2>&1 | grep "pass\|fail"` — all pass
3. `npm test 2>&1 | tail -5` — suite passes
</verification>

<success_criteria>
- VALID_FORMALISMS in write-check-result.cjs includes 'uppaal'
- writeCheckResult({ formalism: 'uppaal', ... }) does not throw
- write-check-result.test.cjs guards the 'uppaal' entry
- npm test passes — no regressions
- UPPAAL-02 formalism prerequisite satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-07-uppaal-timed-race-modeling/v0.20-07-02-SUMMARY.md`
</output>
