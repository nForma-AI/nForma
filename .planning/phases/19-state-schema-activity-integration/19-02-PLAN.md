---
phase: 19-state-schema-activity-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/resume-project.md
  - ~/.claude/get-shit-done/workflows/resume-project.md
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "Running /qgsd:resume-work after an interrupted discover step routes to '/qgsd:fix-tests — re-trigger discovery'"
    - "Running /qgsd:resume-work after an interrupted running_batch (activity=maintain_tests) step routes to '/qgsd:fix-tests — load state file, re-run batch N of M'"
    - "Running /qgsd:resume-work after an interrupted categorizing_batch (activity=maintain_tests) step routes to the categorization recovery command"
    - "Running /qgsd:resume-work after an interrupted actioning_batch (activity=maintain_tests) step routes to the action dispatch recovery command"
    - "Running /qgsd:resume-work after a verifying_batch (activity=maintain_tests) step routes to verification recovery"
    - "Running /qgsd:resume-work on a complete (activity=maintain_tests) session routes to summary/clear action"
    - "Both source (get-shit-done/workflows/resume-project.md) and installed (~/.claude/get-shit-done/workflows/resume-project.md) copies are identical"
  artifacts:
    - path: "get-shit-done/workflows/resume-project.md"
      provides: "6 new routing rows for maintain_tests sub-activities"
      contains: "discovering_tests"
    - path: "~/.claude/get-shit-done/workflows/resume-project.md"
      provides: "Installed copy of routing table with 6 new rows"
      contains: "discovering_tests"
  key_links:
    - from: "resume-project.md routing table"
      to: "discovering_tests sub_activity"
      via: "activity=maintain_tests qualifier row"
      pattern: "discovering_tests"
    - from: "resume-project.md routing table"
      to: "running_batch (activity=maintain_tests)"
      via: "qualified sub_activity row to disambiguate from other running_batch activities"
      pattern: "running_batch.*maintain_tests"
---

<objective>
Add 6 new routing rows to the resume-project.md routing table so that interrupted /qgsd:fix-tests sessions route correctly back to the exact interrupted step when the user runs /qgsd:resume-work.

Purpose: Without these rows, an interrupted maintain-tests session appears orphaned in resume-work — the user sees no recovery option and must manually determine where to restart. The routing rows close INTG-02 and make the fix-tests tool production-safe for long-running 20k+ suite maintenance sessions.

Output: Updated source and installed copies of resume-project.md, each with 6 new routing rows covering all maintain_tests sub_activity values defined in the Phase 19 research.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-state-schema-activity-integration/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 maintain_tests routing rows to resume-project.md (source and installed)</name>
  <files>get-shit-done/workflows/resume-project.md, ~/.claude/get-shit-done/workflows/resume-project.md</files>
  <action>
Read the current routing table in get-shit-done/workflows/resume-project.md. The table is inside the "determine_next_action" step and begins with:

  | sub_activity | Recovery |
  |---|---|

The table currently ends with the row for "planning (activity=quick)". After that row (before the closing paragraph "Present the matched recovery option..."), insert the following 6 new rows:

  | discovering_tests (activity=maintain_tests) | /qgsd:fix-tests — re-trigger discovery; state file absent means fresh run |
  | running_batch (activity=maintain_tests) | /qgsd:fix-tests — load state file, re-run batch {batch} of {batch_total} |
  | categorizing_batch (activity=maintain_tests) | /qgsd:fix-tests — load state, batch results on disk, re-enter categorization for batch {batch} |
  | actioning_batch (activity=maintain_tests) | /qgsd:fix-tests — load state, dispatch quick tasks for batch {batch} failures |
  | verifying_batch (activity=maintain_tests) | /qgsd:fix-tests — load state, re-run verification for actioned tests in batch {batch} |
  | complete (activity=maintain_tests) | /qgsd:fix-tests — session already complete; print summary and clear activity state |

Disambiguation notes (critical for correctness):
- running_batch, categorizing_batch, actioning_batch, verifying_batch MUST carry the "(activity=maintain_tests)" qualifier — future activities may use the same sub_activity names, and the qualifier prevents mis-routing (this pattern was established in Phase 15 for the ACT-04 fix)
- discovering_tests and complete also carry "(activity=maintain_tests)" for consistency and future safety, even though they are currently unique

After editing the source file at get-shit-done/workflows/resume-project.md, copy the identical content to the installed copy:

  cp get-shit-done/workflows/resume-project.md ~/.claude/get-shit-done/workflows/resume-project.md

This two-file update pattern is required per the Phase 19 research (Pitfall 3) and was established in Phases 14 and 15. The installed copy is what resume-work actually reads at runtime — updating only the source has no effect on live behavior.

Verify both files are identical after the copy.

Important: do NOT remove or reorder any existing routing rows. The 6 new rows are inserted after the last existing row in the table ("planning (activity=quick) | ..."), before the closing paragraph. Do not add section headers or blank lines inside the table — keep it as a contiguous markdown table.
  </action>
  <verify>
1. New rows present in source file:
   grep -c 'maintain_tests\|maintaining_tests\|discovering_tests' get-shit-done/workflows/resume-project.md
   Expected: 6 or more matches (one per new row)

2. Specific rows present:
   grep 'discovering_tests' get-shit-done/workflows/resume-project.md
   Expected: one line containing "discovering_tests" and "/qgsd:fix-tests"

   grep 'running_batch.*maintain_tests' get-shit-done/workflows/resume-project.md
   Expected: one line containing the running_batch maintain_tests row

3. Installed copy matches source:
   diff get-shit-done/workflows/resume-project.md ~/.claude/get-shit-done/workflows/resume-project.md
   Expected: no output (files are identical)

4. Existing rows still intact:
   grep 'executing_plan\|planning (activity=quick)' get-shit-done/workflows/resume-project.md
   Expected: both original rows still present
  </verify>
  <done>
- 6 new routing rows present in the routing table in get-shit-done/workflows/resume-project.md
- Each row covers one maintain_tests sub_activity: discovering_tests, running_batch, categorizing_batch, actioning_batch, verifying_batch, complete
- All ambiguous sub_activity values carry (activity=maintain_tests) qualifier
- Source and installed copies are byte-for-byte identical (diff returns nothing)
- All pre-existing routing rows are unchanged
  </done>
</task>

</tasks>

<verification>
After task completes:

1. Source file has 6 new rows:
   grep -c 'maintain_tests' get-shit-done/workflows/resume-project.md
   Expected: 6

2. Installed copy is identical to source:
   diff get-shit-done/workflows/resume-project.md ~/.claude/get-shit-done/workflows/resume-project.md
   Expected: exit 0 with no output

3. Original rows still present:
   grep 'executing_plan' get-shit-done/workflows/resume-project.md
   Expected: match found

4. All 6 sub_activities covered:
   grep -E 'discovering_tests|running_batch.*maintain|categorizing_batch|actioning_batch|verifying_batch|complete.*maintain' get-shit-done/workflows/resume-project.md | wc -l
   Expected: 6
</verification>

<success_criteria>
- 6 new routing rows present in the routing table in get-shit-done/workflows/resume-project.md (source copy)
- ~/.claude/get-shit-done/workflows/resume-project.md (installed copy) is identical to the source copy
- Each maintain_tests sub_activity (discovering_tests, running_batch, categorizing_batch, actioning_batch, verifying_batch, complete) has a routing row with a /qgsd:fix-tests recovery action
- All ambiguous sub_activities carry the (activity=maintain_tests) qualifier
- No pre-existing routing rows were removed or reordered
</success_criteria>

<output>
After completion, create .planning/phases/19-state-schema-activity-integration/19-02-SUMMARY.md
</output>
