---
phase: v0.20-08
type: verification
status: passed
date: 2026-02-28
---

# Verification — v0.20-08 Sensitivity Analysis

## Requirements Verified

| Requirement | Status | Evidence |
|-------------|--------|----------|
| SENS-01 | PASS | `bin/run-sensitivity-sweep.cjs` sweeps MaxSize [1,2,3] and tp_rate [0.5,0.75,0.95]; records written to `sensitivity-report.ndjson` with v2.1 schema including `metadata.parameter`, `metadata.value`, `metadata.delta` |
| SENS-02 | PASS | `plan-phase.md` step 8.3 inserted; `SENSITIVITY_CONTEXT` injected into quorum `review_context`; test 7 GREEN |
| SENS-03 | PASS | `bin/sensitivity-report.cjs` generates `formal/sensitivity-report.md` ranked by outcome-flip count with code paths, test cases, monitoring recommendations |

## Test Results

```
node --test bin/run-sensitivity-sweep.test.cjs

✔ run-sensitivity-sweep.cjs loads without syntax errors (SENS-01)
✔ run-sensitivity-sweep.cjs exits 0 when TLC and PRISM not in PATH (SENS-01)
✔ run-sensitivity-sweep.cjs writes records to sensitivity-report.ndjson (SENS-01)
✔ run-sensitivity-sweep.cjs sweeps ≥2 parameters with ≥3 values each (SENS-01)
✔ run-sensitivity-sweep.cjs records include metadata.parameter and metadata.value (SENS-01)
✔ sensitivity-report.cjs generates formal/sensitivity-report.md (SENS-03)
✔ plan-phase.md step 8.3 exists and references run-sensitivity-sweep.cjs (SENS-02)

tests 7 | pass 7 | fail 0
```

## Formal Verification

Sensitivity sweep is itself a meta-formal verification tool. Graceful degradation verified:
- TLC absent: `result=inconclusive`, `triage_tags=['no-tlc']`, exits 0
- PRISM absent: `result=inconclusive`, `triage_tags=['no-prism']`, exits 0
- Both absent: all 6 records written as inconclusive, no crash

When TLC is available (tla2tools.jar present in environment): MaxSize sweep runs live checks, records `result=pass`/`fail` with `delta=stable`/`flip-to-X`.

## Artifacts

- `bin/run-sensitivity-sweep.cjs` — sweep runner
- `bin/sensitivity-report.cjs` — report generator
- `bin/run-sensitivity-sweep.test.cjs` — 7-test suite (Wave 0 RED → GREEN)
- `~/.claude/qgsd/workflows/plan-phase.md` — step 8.3 inserted (user-space, not in QGSD repo)
