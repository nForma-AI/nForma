---
phase: v0.20-08
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/run-sensitivity-sweep.test.cjs
autonomous: true
requirements:
  - SENS-01
  - SENS-02
  - SENS-03

must_haves:
  truths:
    - "bin/run-sensitivity-sweep.test.cjs exists with 7+ RED tests that all FAIL before implementation"
    - "Tests cover: syntax check, graceful degradation (no TLC/PRISM), NDJSON write, parameter sweep count, metadata fields, report generation, plan-phase.md step 8.3"
    - "Running `node --test bin/run-sensitivity-sweep.test.cjs` exits non-zero — no 'ok' lines"
  artifacts:
    - path: "bin/run-sensitivity-sweep.test.cjs"
      provides: "7 Wave 0 RED tests for SENS-01, SENS-02, SENS-03"
      min_lines: 100
      contains: "sensitivity-report.ndjson"
  key_links:
    - from: "bin/run-sensitivity-sweep.test.cjs"
      to: "bin/run-sensitivity-sweep.cjs"
      via: "spawnSync or require()"
      pattern: "run-sensitivity-sweep"
    - from: "bin/run-sensitivity-sweep.test.cjs"
      to: "~/.claude/qgsd/workflows/plan-phase.md"
      via: "fs.readFileSync + includes('run-sensitivity-sweep.cjs')"
      pattern: "step 8.3"
---

<objective>
Scaffold Wave 0 RED tests for SENS-01, SENS-02, and SENS-03 in bin/run-sensitivity-sweep.test.cjs.

Purpose: Lock the implementation contract before any code is written. RED tests specify that `run-sensitivity-sweep.cjs` must exit 0 when TLC/PRISM absent (graceful degradation), write inconclusive records to `formal/sensitivity-report.ndjson`, sweep ≥2 parameters with ≥3 values each, and that `plan-phase.md` includes a step 8.3 referencing the sweep tool. The tools are created in Plans 02-04 — tests turn GREEN then.

Output: New file `bin/run-sensitivity-sweep.test.cjs` with 7 failing test cases.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/v0.20-08-sensitivity-analysis-fv-parameter-sweep-identifies-high-impact-variables-for-planning-signal/v0.20-08-RESEARCH.md
</context>

<feature>
  <name>Sensitivity Analysis Wave 0 RED Test Scaffold</name>
  <files>bin/run-sensitivity-sweep.test.cjs</files>
  <behavior>
    All 7 tests must FAIL when run against the current repo (run-sensitivity-sweep.cjs and sensitivity-report.cjs do not exist yet).

    Use `node:test` + `node:assert` with `.cjs` suffix — the project's established test pattern.

    Helper pattern:
    ```javascript
    const { spawnSync } = require('child_process');
    const path = require('path');
    const fs = require('fs');
    const os = require('os');

    const TOOL_PATH = path.join(__dirname, 'run-sensitivity-sweep.cjs');
    const REPORT_PATH = path.join(__dirname, 'sensitivity-report.cjs');
    const PLAN_PHASE_PATH = path.join(
      os.homedir(), '.claude', 'qgsd', 'workflows', 'plan-phase.md'
    );

    function runSweep(env) {
      const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'sens-test-'));
      fs.mkdirSync(path.join(tmpDir, 'formal'), { recursive: true });
      const ndjsonOut = path.join(tmpDir, 'formal', 'sensitivity-report.ndjson');
      const result = spawnSync(process.execPath, [TOOL_PATH], {
        cwd: tmpDir,
        encoding: 'utf8',
        timeout: 30000,
        env: {
          ...process.env,
          ...env,
          SENSITIVITY_REPORT_PATH: ndjsonOut,
          PATH: '/nonexistent:' + (process.env.PATH || ''),
        },
      });
      return { tmpDir, ndjsonOut, result };
    }
    ```

    Test cases (7 total, all must RED):

    1. "run-sensitivity-sweep.cjs loads without syntax errors (SENS-01)"
       - `spawnSync(process.execPath, ['--check', TOOL_PATH], { encoding: 'utf8' })`
       - assert.strictEqual(result.status, 0, ...)
       - Will RED because run-sensitivity-sweep.cjs does not exist

    2. "run-sensitivity-sweep.cjs exits 0 when TLC and PRISM not in PATH (SENS-01)"
       - Spawn with PATH=/nonexistent
       - assert.strictEqual(result.status, 0, 'must exit 0 even when TLC/PRISM missing')

    3. "run-sensitivity-sweep.cjs writes records to sensitivity-report.ndjson (SENS-01)"
       - Spawn with SENSITIVITY_REPORT_PATH pointing to tmpDir
       - Read the ndjson output file
       - assert.ok(ndjson.includes('sensitivity'), 'records must have surface=sensitivity or check_id containing sens:')
       - assert.ok(ndjson.includes('"result"'), 'records must have result field')

    4. "run-sensitivity-sweep.cjs sweeps ≥2 parameters with ≥3 values each (SENS-01)"
       - Spawn and read ndjson output
       - Parse all records, extract unique metadata.parameter values
       - assert.ok(uniqueParams.length >= 2, 'must sweep at least 2 parameters')
       - For each parameter, count records; assert.ok(count >= 3, 'must sweep at least 3 values per param')

    5. "run-sensitivity-sweep.cjs records include metadata.parameter and metadata.value (SENS-01)"
       - Parse ndjson records
       - assert.ok(records.every(r => r.metadata && 'parameter' in r.metadata), ...)
       - assert.ok(records.every(r => r.metadata && 'value' in r.metadata), ...)

    6. "sensitivity-report.cjs generates formal/sensitivity-report.md (SENS-03)"
       - const tmpDir = mkdtempSync(...)
       - Write sample sensitivity-report.ndjson to tmpDir
       - Spawn REPORT_PATH in tmpDir
       - assert.ok(fs.existsSync(path.join(tmpDir, 'formal', 'sensitivity-report.md')), ...)

    7. "plan-phase.md step 8.3 exists and references run-sensitivity-sweep.cjs (SENS-02)"
       - const src = fs.readFileSync(PLAN_PHASE_PATH, 'utf8')
       - assert.ok(src.includes('run-sensitivity-sweep.cjs'), 'plan-phase.md must reference run-sensitivity-sweep.cjs (SENS-02)')
       - assert.ok(src.includes('8.3') || src.includes('SENSITIVITY_CONTEXT'), 'plan-phase.md must have step 8.3')

    Cleanup: add fs.rmSync(tmpDir, { recursive: true, force: true }) at end of spawnSync tests.
  </behavior>
  <implementation>
    Do NOT implement run-sensitivity-sweep.cjs in this plan — that is Plan 02.
    Do NOT implement sensitivity-report.cjs in this plan — that is Plan 03.
    Do NOT modify plan-phase.md in this plan — that is Plan 04.
    Only create the test file. Confirm all 7 tests fail (RED).

    RED state verification command:
    `node --test bin/run-sensitivity-sweep.test.cjs 2>&1 | tail -10`
    Expected: 7 failures. No 'ok' lines.
    Acceptable RED failures: ENOENT (file not found), assertion errors on non-existent files.
    NOT acceptable: syntax errors in the test file itself.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 RED — 7 contract tests for SENS-01/02/03</name>
  <files>bin/run-sensitivity-sweep.test.cjs</files>
  <action>
    Create bin/run-sensitivity-sweep.test.cjs as a NEW file.

    File header:
    ```javascript
    #!/usr/bin/env node
    'use strict';
    // bin/run-sensitivity-sweep.test.cjs
    // Wave 0 RED tests for SENS-01, SENS-02, SENS-03.
    // Requirements: SENS-01, SENS-02, SENS-03

    const { test } = require('node:test');
    const assert = require('node:assert');
    const { spawnSync } = require('child_process');
    const path = require('path');
    const fs = require('fs');
    const os = require('os');
    ```

    Add helper functions and 7 tests exactly as specified in the feature section above.

    After writing the file, verify RED state:
    - Run: `node --test bin/run-sensitivity-sweep.test.cjs 2>&1 | tail -10`
    - Expected: 7 failures
    - NOT acceptable: syntax errors in the test file
  </action>
  <verify>
    Run: `node --test bin/run-sensitivity-sweep.test.cjs 2>&1 | grep -E "✓|✖|pass|fail" | head -15`

    Expected RED state:
    - All 7 tests show ✖ (fail)
    - No ✓ lines (no accidental green tests)
    - Exit code non-zero

    Also run: `node --test bin/run-sensitivity-sweep.test.cjs 2>&1 | tail -5`
    Expected: summary line showing "7 fail".
  </verify>
  <done>
    `node --test bin/run-sensitivity-sweep.test.cjs` exits non-zero with all 7 tests failing (RED).
    No syntax errors in the test file.
    Wave 0 RED state committed: `test(v0.20-08-01): add 7 RED tests for sensitivity sweep SENS-01/02/03`.
  </done>
</task>

</tasks>

<verification>
Run: `node --test bin/run-sensitivity-sweep.test.cjs 2>&1 | grep -cE "✖"` — must output 7.
Run: `node --check bin/run-sensitivity-sweep.test.cjs` — must produce no syntax errors (exit 0).
</verification>

<success_criteria>
- bin/run-sensitivity-sweep.test.cjs exists with 7 test cases
- All 7 tests FAIL (RED) when run against current repo (tools not yet implemented)
- Test names match VALIDATION.md Wave 0 requirements
- No syntax errors in test file
- Wave 0 RED state committed before Plan 02/03/04 implementation begins
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-08-sensitivity-analysis-fv-parameter-sweep-identifies-high-impact-variables-for-planning-signal/v0.20-08-01-SUMMARY.md`
</output>
