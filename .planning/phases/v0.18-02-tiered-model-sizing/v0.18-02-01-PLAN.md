---
phase: v0.18-02-tiered-model-sizing
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - hooks/config-loader.js
  - hooks/config-loader.test.js
autonomous: true
requirements:
  - TIER-03

must_haves:
  truths:
    - "loadConfig() returns model_tier_planner: 'opus' and model_tier_worker: 'haiku' from DEFAULT_CONFIG when no config file is present"
    - "A qgsd.json with model_tier_worker: 'sonnet' overrides the default to 'sonnet'"
    - "A qgsd.json with model_tier_planner: 'gpt-4' is rejected with a stderr WARNING and the key is deleted from the config"
    - "A qgsd.json with model_tier_worker: 42 (non-string) is rejected with a stderr WARNING and the key is deleted"
  artifacts:
    - path: "hooks/config-loader.js"
      provides: "DEFAULT_CONFIG with tier keys + validateConfig() tier validation"
      contains: "model_tier_planner"
    - path: "hooks/config-loader.test.js"
      provides: "TIER validation tests covering defaults, valid override, invalid string, non-string"
      min_lines: 4
  key_links:
    - from: "hooks/config-loader.test.js"
      to: "hooks/config-loader.js"
      via: "require('./config-loader')"
      pattern: "model_tier_planner.*sonnet|model_tier_worker.*haiku"
    - from: "hooks/config-loader.js"
      to: "DEFAULT_CONFIG"
      via: "flat key merge"
      pattern: "model_tier_planner.*opus.*model_tier_worker.*haiku"
---

<objective>
Add model_tier_planner and model_tier_worker flat keys to hooks/config-loader.js DEFAULT_CONFIG with validation, via TDD. Tests go RED first, then implementation makes them GREEN.

Purpose: TIER-03 requires users to override tier assignments via flat config keys in qgsd.json. The hook config-loader is the validation layer for ~/.claude/qgsd.json. Adding defaults here ensures any install reading this file gets the correct tier defaults.

Output: hooks/config-loader.js with tier key defaults (model_tier_planner: 'opus', model_tier_worker: 'haiku') and validateConfig() validation; hooks/config-loader.test.js with 4 TIER test cases passing.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-RESEARCH.md
@hooks/config-loader.js
@hooks/config-loader.test.js
</context>

<feature>
  <name>Tier key validation in config-loader.js</name>
  <files>hooks/config-loader.js, hooks/config-loader.test.js</files>
  <behavior>
    Test cases (input -> expected output):
    - No qgsd.json → config.model_tier_planner === 'opus', config.model_tier_worker === 'haiku' (from DEFAULT_CONFIG)
    - { model_tier_planner: 'sonnet' } → config.model_tier_planner === 'sonnet' (valid override preserved)
    - { model_tier_worker: 'haiku' } → config.model_tier_worker === 'haiku' (valid override preserved)
    - { model_tier_planner: 'gpt-4' } → config.model_tier_planner === undefined, stderr contains '[qgsd] WARNING' and 'model_tier_planner'
    - { model_tier_worker: 42 } → config.model_tier_worker === undefined, stderr contains '[qgsd] WARNING' and 'model_tier_worker'

    Valid tier values: 'haiku', 'sonnet', 'opus' only.
    Invalid types (non-string) must also be rejected with the same WARNING pattern.
  </behavior>
  <implementation>
    RED phase:
    1. Append 4 test cases to hooks/config-loader.test.js (after existing TC tests):
       - TIER-TC1: No config → defaults are 'opus' and 'haiku'
       - TIER-TC2: Valid model_tier_planner: 'sonnet' → preserved
       - TIER-TC3: Invalid model_tier_planner: 'gpt-4' → deleted + stderr WARNING
       - TIER-TC4: Invalid model_tier_worker: 42 (number) → deleted + stderr WARNING
    2. Run: node --test hooks/config-loader.test.js
       All 4 TIER tests MUST FAIL (missing impl). Existing tests must still pass.
    3. Commit: test(v0.18-02-01): add failing TIER validation tests to config-loader.test.js

    GREEN phase:
    1. In hooks/config-loader.js DEFAULT_CONFIG (after quorum_active):
       Add two flat keys:
         // model_tier_planner: model tier for planner agents (gsd-planner, gsd-roadmapper).
         // model_tier_worker: model tier for worker agents (researcher, checker, executor, etc.).
         // Valid values: 'haiku' | 'sonnet' | 'opus'. Flat keys required — nested objects lost in shallow merge.
         model_tier_planner: 'opus',
         model_tier_worker: 'haiku',
    2. In validateConfig() (after context_monitor validation block, before return config):
       Add validation block for model_tier_planner and model_tier_worker following the EXACT pattern
       from RESEARCH.md Example 1 (type check first, then whitelist check, delete + stderr on failure).
       Allowed values whitelist: ['haiku', 'sonnet', 'opus']
       WARNING format: '[qgsd] WARNING: qgsd.json: model_tier_planner must be "haiku", "sonnet", or "opus"; removing\n'
    3. Run: node --test hooks/config-loader.test.js
       All 4 TIER tests MUST PASS. All existing tests must still pass.
    4. Run: node --test hooks/config-loader.test.js hooks/qgsd-stop.test.js
       Confirm no regressions.
    5. Commit: feat(v0.18-02-01): add model_tier_planner/worker defaults and validation to config-loader.js

    IMPORTANT — Do NOT modify hooks/dist/config-loader.js in this plan. The dist sync is Plan 03.
    Do NOT add model_tier keys to gsd-tools.cjs in this plan. That is Plan 02.
  </implementation>
</feature>

<verification>
1. node --test hooks/config-loader.test.js → all tests pass (including 4 new TIER tests)
2. grep "model_tier_planner" hooks/config-loader.js → shows in DEFAULT_CONFIG and validateConfig()
3. grep "model_tier_worker" hooks/config-loader.js → shows in DEFAULT_CONFIG and validateConfig()
4. git log --oneline -3 → shows both commits: test(v0.18-02-01) then feat(v0.18-02-01)
</verification>

<success_criteria>
- 4 new TIER test cases pass in hooks/config-loader.test.js
- All pre-existing config-loader tests continue to pass
- DEFAULT_CONFIG has model_tier_planner: 'opus' and model_tier_worker: 'haiku'
- validateConfig() rejects invalid tier values with '[qgsd] WARNING' to stderr
- 2 atomic commits: RED (failing tests) then GREEN (implementation)
</success_criteria>

<output>
After completion, create .planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-01-SUMMARY.md
</output>
