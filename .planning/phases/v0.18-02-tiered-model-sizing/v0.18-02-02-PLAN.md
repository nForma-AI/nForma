---
phase: v0.18-02-tiered-model-sizing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - qgsd-core/bin/gsd-tools.cjs
  - qgsd-core/bin/gsd-tools.test.cjs
autonomous: true
requirements:
  - TIER-01
  - TIER-02

must_haves:
  truths:
    - "resolveModelInternal('gsd-phase-researcher') returns 'haiku' by default (no config override)"
    - "resolveModelInternal('gsd-plan-checker') returns 'haiku' by default (no config override)"
    - "resolveModelInternal('gsd-planner') returns 'inherit' (opus) by default — planner tier unchanged"
    - "A .planning/config.json with model_tier_worker: 'sonnet' causes resolveModelInternal('gsd-phase-researcher') to return 'sonnet'"
    - "Per-agent model_overrides still take priority over tier keys"
  artifacts:
    - path: "qgsd-core/bin/gsd-tools.cjs"
      provides: "tier key defaults in loadConfig + tier lookup in resolveModelInternal()"
      contains: "agentToTier"
    - path: "qgsd-core/bin/gsd-tools.test.cjs"
      provides: "TIER unit tests for resolveModelInternal tier lookup"
      min_lines: 4
  key_links:
    - from: "qgsd-core/bin/gsd-tools.cjs resolveModelInternal()"
      to: "loadConfig() config.model_tier_worker"
      via: "agentToTier lookup + tierKey construction"
      pattern: "agentToTier\\[agentType\\]|model_tier_"
    - from: "qgsd-core/bin/gsd-tools.cjs init command (plan-phase)"
      to: "resolveModelInternal()"
      via: "researcher_model and checker_model fields in init JSON"
      pattern: "researcher_model.*resolveModelInternal|checker_model.*resolveModelInternal"
---

<objective>
Extend gsd-tools.cjs loadConfig defaults with model_tier_planner/model_tier_worker keys and add tier lookup logic in resolveModelInternal(), so the `init plan-phase` command returns haiku for researcher_model and checker_model by default.

Purpose: TIER-01 and TIER-02 require the researcher and plan-checker sub-agents dispatched in plan-phase.md to use haiku model by default. The init JSON output (researcher_model, checker_model) is what plan-phase.md passes to Task() calls. This plan wires tier keys through the resolution chain.

Output: gsd-tools.cjs with tier-aware resolveModelInternal() + tier keys in loadConfig defaults; gsd-tools.test.cjs with TIER unit tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-RESEARCH.md
@qgsd-core/bin/gsd-tools.cjs
@qgsd-core/bin/gsd-tools.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier key defaults to gsd-tools.cjs loadConfig</name>
  <files>qgsd-core/bin/gsd-tools.cjs</files>
  <action>
    In gsd-tools.cjs, the loadConfig function at line ~172 returns a hardcoded defaults object for
    .planning/config.json. Add model_tier_planner and model_tier_worker to this defaults object.

    In the `defaults` const at the top of loadConfig() (around line 174), add:
      model_tier_planner: 'opus',   // Planner agents (gsd-planner, gsd-roadmapper) default to opus
      model_tier_worker: 'haiku',   // Worker agents (researcher, checker, executor) default to haiku

    In the returned config object (the try block that reads config.json), add these two fields:
      model_tier_planner: get('model_tier_planner') ?? defaults.model_tier_planner,
      model_tier_worker: get('model_tier_worker') ?? defaults.model_tier_worker,

    And in the catch block fallback return (return defaults), the defaults object already includes
    the tier keys, so no additional change needed there.

    IMPORTANT: Only add to the loadConfig that reads .planning/config.json (around line 172-227).
    Do NOT modify any other loadConfig variant or any hook file in this plan.
    Do NOT validate tier values here — gsd-tools does not validate config (that is config-loader.js's job).
    Tier values are passed through as-is from config.json; invalid values fall through to profile lookup.
  </action>
  <verify>
    node -e "const g = require('./qgsd-core/bin/gsd-tools.cjs'); // triggers parse, check for syntax errors"
    grep -n "model_tier_planner\|model_tier_worker" qgsd-core/bin/gsd-tools.cjs | head -10
  </verify>
  <done>
    grep output shows model_tier_planner and model_tier_worker in loadConfig defaults and the returned config object.
    Node require completes without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier lookup in resolveModelInternal() and unit tests</name>
  <files>qgsd-core/bin/gsd-tools.cjs, qgsd-core/bin/gsd-tools.test.cjs</files>
  <action>
    PART A — Modify resolveModelInternal() in gsd-tools.cjs (currently at line ~4160):

    After the per-agent override check and before the profile fallback lookup, insert the tier lookup:

    ```javascript
    // NEW: Tier lookup (after per-agent override, before profile fallback)
    // Maps agent types to cost tiers: planner (high-value) vs worker (repetitive/cheap)
    const agentToTier = {
      'gsd-planner': 'planner',
      'gsd-roadmapper': 'planner',
      'gsd-phase-researcher': 'worker',
      'gsd-plan-checker': 'worker',
      'gsd-executor': 'worker',
      'gsd-verifier': 'worker',
      'gsd-codebase-mapper': 'worker',
      'gsd-project-researcher': 'worker',
      'gsd-research-synthesizer': 'worker',
      'gsd-debugger': 'worker',
    };
    const tier = agentToTier[agentType] || 'worker';
    const tierKey = 'model_tier_' + tier; // 'model_tier_planner' or 'model_tier_worker'
    const tierModel = config[tierKey];
    if (tierModel && ['haiku', 'sonnet', 'opus'].includes(tierModel)) {
      return tierModel === 'opus' ? 'inherit' : tierModel;
    }
    ```

    The lookup order becomes:
    1. Per-agent model_overrides (existing, unchanged)
    2. Tier key (new: model_tier_planner or model_tier_worker from config)
    3. Profile model (existing MODEL_PROFILES lookup, unchanged)
    4. Default: 'sonnet'

    Note: 'opus' in tier returns 'inherit' (consistent with existing override behavior for planner).

    PART B — Add unit tests to qgsd-core/bin/gsd-tools.test.cjs:

    Add a new describe block 'resolveModelInternal tier lookup' with these test cases:
    - TIER-01: default config → researcher resolves to 'haiku'
      Create temp project with no config.json. Run: node gsd-tools.cjs resolve-model gsd-phase-researcher --cwd <tmpDir>
      Expect output contains 'haiku'
    - TIER-02: default config → checker resolves to 'haiku'
      Same pattern with gsd-plan-checker. Expect 'haiku'.
    - TIER-03a: default config → planner resolves to 'inherit' (opus tier)
      gsd-planner resolves to 'inherit'
    - TIER-03b: config with model_tier_worker: 'sonnet' → researcher resolves to 'sonnet'
      Create temp project with .planning/config.json containing { "model_tier_worker": "sonnet" }
      Run resolve-model gsd-phase-researcher. Expect 'sonnet'.
    - TIER-03c: per-agent override beats tier
      Create config with model_overrides: { "gsd-phase-researcher": "sonnet" } AND model_tier_worker: "haiku"
      Run resolve-model gsd-phase-researcher. Expect 'sonnet' (override wins).

    Use the existing runGsdTools() and createTempProject() helpers. Use the same describe/test/assert
    pattern as existing tests in gsd-tools.test.cjs.

    After writing tests, run:
    node --test qgsd-core/bin/gsd-tools.test.cjs
    All 5 new TIER tests must pass. All existing gsd-tools tests must pass.
  </action>
  <verify>
    node --test qgsd-core/bin/gsd-tools.test.cjs
    node qgsd-core/bin/gsd-tools.cjs resolve-model gsd-phase-researcher --cwd /tmp
    node qgsd-core/bin/gsd-tools.cjs resolve-model gsd-plan-checker --cwd /tmp
    node qgsd-core/bin/gsd-tools.cjs resolve-model gsd-planner --cwd /tmp
  </verify>
  <done>
    resolve-model gsd-phase-researcher returns 'haiku' (TIER-01 verified).
    resolve-model gsd-plan-checker returns 'haiku' (TIER-02 verified).
    resolve-model gsd-planner returns 'inherit' (planner tier untouched).
    All gsd-tools tests pass including 5 new TIER tests.
  </done>
</task>

</tasks>

<verification>
1. node --test qgsd-core/bin/gsd-tools.test.cjs → all tests pass including new TIER describe block
2. node qgsd-core/bin/gsd-tools.cjs init plan-phase --cwd . | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); console.log(j.researcher_model, j.checker_model, j.planner_model)"
   Expected: researcher_model=haiku, checker_model=haiku, planner_model=inherit (or sonnet if project has no config)
3. grep -n "agentToTier\|model_tier_" qgsd-core/bin/gsd-tools.cjs → shows tier lookup in resolveModelInternal
</verification>

<success_criteria>
- resolveModelInternal('gsd-phase-researcher') returns 'haiku' with default config (TIER-01)
- resolveModelInternal('gsd-plan-checker') returns 'haiku' with default config (TIER-02)
- resolveModelInternal('gsd-planner') returns 'inherit' with default config (planner tier = opus)
- model_tier_worker: 'sonnet' in .planning/config.json causes researcher to return 'sonnet'
- per-agent override still beats tier key (existing behavior preserved)
- init plan-phase JSON output has researcher_model=haiku and checker_model=haiku
- All existing gsd-tools tests continue to pass
</success_criteria>

<output>
After completion, create .planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-02-SUMMARY.md
</output>
