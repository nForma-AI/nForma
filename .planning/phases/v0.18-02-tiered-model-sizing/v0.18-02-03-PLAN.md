---
phase: v0.18-02-tiered-model-sizing
plan: "03"
type: execute
wave: 2
depends_on:
  - v0.18-02-01
  - v0.18-02-02
files_modified:
  - hooks/dist/config-loader.js
autonomous: true
requirements:
  - TIER-01
  - TIER-02
  - TIER-03

must_haves:
  truths:
    - "hooks/dist/config-loader.js is identical to hooks/config-loader.js (diff shows no differences)"
    - "~/.claude/hooks/config-loader.js contains model_tier_planner and model_tier_worker (deployed)"
    - "npm test passes all tests with no failures"
  artifacts:
    - path: "hooks/dist/config-loader.js"
      provides: "Deployed config-loader with tier key support"
      contains: "model_tier_planner"
  key_links:
    - from: "hooks/config-loader.js"
      to: "hooks/dist/config-loader.js"
      via: "cp (manual sync)"
      pattern: "model_tier_planner.*opus"
    - from: "hooks/dist/config-loader.js"
      to: "~/.claude/hooks/config-loader.js"
      via: "node bin/install.js --claude --global"
      pattern: "model_tier_planner"
---

<objective>
Sync the updated hooks/config-loader.js (with tier key validation from Plan 01) to hooks/dist/ and run install.js to deploy it to ~/.claude/hooks/. Then run the full npm test suite to confirm no regressions.

Purpose: CLAUDE.md mandates: "edits to hook source files MUST sync to hooks/dist/ first, then run install.js". Without this step, the running hook at ~/.claude/hooks/config-loader.js remains stale and tier validation never executes in production. This plan closes the install sync loop for TIER-01, TIER-02, TIER-03.

Output: hooks/dist/config-loader.js synced; ~/.claude/hooks/config-loader.js updated with tier keys; npm test green.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-01-SUMMARY.md
@.planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync config-loader.js to dist and deploy via install.js</name>
  <files>hooks/dist/config-loader.js</files>
  <action>
    Run the following three commands as a single atomic sequence (all must succeed):

    1. Verify source has tier keys (pre-flight check):
       grep "model_tier_planner" hooks/config-loader.js
       Must return a match. If not, stop — Plan 01 was not completed correctly.

    2. Sync to dist:
       cp hooks/config-loader.js hooks/dist/config-loader.js

    3. Verify diff is clean:
       diff hooks/config-loader.js hooks/dist/config-loader.js
       Must produce NO output (files are identical). If diff shows differences, the cp failed.

    4. Run install.js to deploy to ~/.claude/hooks/:
       node bin/install.js --claude --global
       This updates ~/.claude/hooks/config-loader.js from hooks/dist/config-loader.js.
       Install output will show config-loader sync confirmation.

    5. Verify deployed file has tier keys:
       grep "model_tier_planner" ~/.claude/hooks/config-loader.js
       Must return a match. If not, the install did not deploy correctly.

    Do NOT modify any other files. This task is purely cp + install.
  </action>
  <verify>
    diff hooks/config-loader.js hooks/dist/config-loader.js
    grep "model_tier_planner" ~/.claude/hooks/config-loader.js
    grep "model_tier_worker" ~/.claude/hooks/config-loader.js
  </verify>
  <done>
    diff shows no differences between source and dist.
    ~/.claude/hooks/config-loader.js contains both model_tier_planner and model_tier_worker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full npm test suite regression check</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions from Plan 01 and Plan 02 changes:

    npm test

    The suite runs: lint-isolation.js + node --test across all test files including:
    - hooks/config-loader.test.js (includes new TIER tests from Plan 01)
    - qgsd-core/bin/gsd-tools.test.cjs (includes new TIER tests from Plan 02)
    - All other existing tests

    Expected: all tests pass. If any test fails:
    1. Read the failure message
    2. If failure is in config-loader.test.js → regression from Plan 01, fix in config-loader.js
    3. If failure is in gsd-tools.test.cjs → regression from Plan 02, fix in gsd-tools.cjs
    4. For any fix to config-loader.js → re-run cp + install (Task 1) before re-running npm test
    5. Do NOT modify test files to make tests pass — fix the implementation

    After npm test passes, check that the Nyquist VALIDATION.md wave tracking can be updated:
    - Count of passing tests should be >= the count before v0.18-02 (no net regression)
  </action>
  <verify>
    npm test
    echo "Exit code: $?"
  </verify>
  <done>
    npm test exits with code 0.
    All pre-existing tests pass.
    4 new TIER tests in config-loader.test.js pass.
    5 new TIER tests in gsd-tools.test.cjs pass.
  </done>
</task>

</tasks>

<verification>
1. diff hooks/config-loader.js hooks/dist/config-loader.js → no output
2. grep "model_tier_planner" ~/.claude/hooks/config-loader.js → match found
3. npm test → exit code 0, all tests pass
4. node qgsd-core/bin/gsd-tools.cjs resolve-model gsd-phase-researcher → 'haiku'
5. node qgsd-core/bin/gsd-tools.cjs resolve-model gsd-plan-checker → 'haiku'
</verification>

<success_criteria>
- hooks/dist/config-loader.js is byte-for-byte identical to hooks/config-loader.js
- ~/.claude/hooks/config-loader.js contains model_tier_planner and model_tier_worker
- npm test exits 0 with no failures
- All three TIER requirements are verifiable:
  - TIER-01: resolve-model gsd-phase-researcher → haiku
  - TIER-02: resolve-model gsd-plan-checker → haiku
  - TIER-03: config-loader validates model_tier_planner/worker with warnings on bad values
</success_criteria>

<output>
After completion, create .planning/phases/v0.18-02-tiered-model-sizing/v0.18-02-03-SUMMARY.md
</output>
