---
phase: v0.12-09-verification-quick-fixes
plan: "05"
subsystem: formal-verification
tags: [petri-net, graphviz, dot, cli, deadlock-detection, PET-03]

# Dependency graph
requires:
  - phase: v0.12-09-verification-quick-fixes
    provides: generate-petri-net.cjs with PET-01/PET-02/PET-03 requirements
provides:
  - --min-quorum=N CLI flag on generate-petri-net.cjs making PET-03 deadlock check exercisable at runtime
affects: [v0.12-09, formal-verification, petri-net]

# Tech tracking
tech-stack:
  added: []
  patterns: ["CLI arg override pattern: effectiveX = argValue ?? computedDefault"]

key-files:
  created: []
  modified:
    - bin/generate-petri-net.cjs

key-decisions:
  - "effectiveMinQuorum replaces MIN_QUORUM_SIZE in deadlock check and buildDot call — zero regression on default path (3>5=false unchanged)"
  - "CLI flag pattern --min-quorum=N overrides threshold without changing buildDot signature — avoids test breakage"
  - "Pre-existing run-alloy.test.cjs failure confirmed not introduced by this change (verified via git stash round-trip)"

patterns-established:
  - "Hardcoded-constant deadlock check pattern: always-false guard made exercisable via CLI override without changing defaults"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-02-25
---

# Phase v0.12-09 Plan 05: PET-03 Deadlock Check Fix Summary

**`--min-quorum=N` CLI override added to generate-petri-net.cjs, making the permanently-inert PET-03 deadlock guard exercisable at runtime without changing default behavior**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-25T18:32:16Z
- **Completed:** 2026-02-25T18:35:28Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `effectiveMinQuorum` derivation from optional `--min-quorum=N` CLI argument
- Updated PET-03 deadlock check condition to use `effectiveMinQuorum` instead of hardcoded `MIN_QUORUM_SIZE`
- Updated warning message value to display `effectiveMinQuorum`
- Updated `buildDot` call to pass `effectiveMinQuorum` so DOT label reflects the override
- Default behavior unchanged: no `--min-quorum` flag => `effectiveMinQuorum = 3`, `3 > 5 = false`, no warning
- Deadlock warning now reachable: `--min-quorum=6` => `effectiveMinQuorum = 6`, `6 > 5 = true`, WARNING fires to stderr

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix PET-03 permanently-inert deadlock check via --min-quorum CLI flag** - `f8a7e76` (fix)

## Files Created/Modified
- `bin/generate-petri-net.cjs` - Added effectiveMinQuorum CLI arg parsing and updated deadlock check + buildDot call

## Decisions Made
- Used `effectiveMinQuorum` as an intermediate variable rather than mutating `MIN_QUORUM_SIZE` — keeps the computed default readable and the override clearly separated
- `buildDot` pure function signature left unchanged — only the call site changes, preserving all existing unit tests that call `buildDot` directly
- Pre-existing `run-alloy.test.cjs` failure confirmed not introduced by this change (verified via `git stash` round-trip)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing test failure in `bin/run-alloy.test.cjs:24` ("exits non-zero and prints Alloy JAR download URL when JAR not found") was present before this change. Confirmed via `git stash && npm test` round-trip. Not in scope for this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- PET-03 deadlock check is now exercisable at runtime
- All 4 generate-petri-net tests pass (4/4 GREEN)
- v0.12-09 verification quick fixes can continue with remaining plans (01, 03, 04)

---
*Phase: v0.12-09-verification-quick-fixes*
*Completed: 2026-02-25*
