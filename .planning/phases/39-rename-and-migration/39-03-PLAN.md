---
phase: 39-rename-and-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 39-01
  - 39-02
files_modified:
  - commands/qgsd/mcp-status.md
  - commands/qgsd/mcp-set-model.md
  - commands/qgsd/mcp-restart.md
  - commands/qgsd/mcp-update.md
  - commands/qgsd/mcp-setup.md
  - commands/qgsd/quorum.md
  - commands/qgsd/quorum-test.md
  - commands/qgsd/debug.md
  - agents/qgsd-quorum-orchestrator.md
autonomous: true
requirements:
  - SLOT-01
  - SLOT-03
  - SLOT-04

must_haves:
  truths:
    - "Running /qgsd:mcp-status shows agents as codex-cli-1, gemini-cli-1, opencode-1, copilot-1, claude-1 through claude-6 in the status table"
    - "Running /qgsd:mcp-set-model claude-1 <model> does not produce an 'Unknown agent' error"
    - "Running /qgsd:mcp-restart codex-cli-1 does not produce a 'not in valid list' error"
    - "Running /qgsd:mcp-update gemini-cli-1 finds the agent in KNOWN_AGENTS"
    - "The quorum orchestrator display-name logic does not strip 'claude-' prefix (slot name is the display name)"
    - "grep for any old model-based agent name across all updated files returns zero matches"
  artifacts:
    - path: "commands/qgsd/mcp-status.md"
      provides: "Updated allowed-tools block + agent table with slot names"
      contains: "mcp__claude-1__identity"
    - path: "commands/qgsd/mcp-set-model.md"
      provides: "Updated allowed-tools + valid agent list with slot names"
      contains: "claude-1"
    - path: "commands/qgsd/mcp-restart.md"
      provides: "Updated allowed-tools + valid agent list with slot names"
      contains: "codex-cli-1"
    - path: "commands/qgsd/mcp-update.md"
      provides: "Updated KNOWN_AGENTS array and valid agent list with slot names"
      contains: "codex-cli-1"
    - path: "agents/qgsd-quorum-orchestrator.md"
      provides: "Updated native agent tool calls + display-name logic using slot names"
      contains: "mcp__codex-cli-1__identity"
  key_links:
    - from: "commands/qgsd/mcp-status.md allowed-tools"
      to: "Claude Code tool allowlist"
      via: "frontmatter allowed-tools block"
      pattern: "mcp__claude-1__identity"
    - from: "commands/qgsd/mcp-set-model.md valid agent list"
      to: "user-entered $AGENT argument"
      via: "Step 2 validation check"
      pattern: "claude-1.*claude-2.*claude-3"
    - from: "agents/qgsd-quorum-orchestrator.md Step 2"
      to: "native CLI agent identity tools"
      via: "sequential tool calls"
      pattern: "mcp__codex-cli-1__identity"
---

<objective>
Update all 8 command .md files and the quorum orchestrator agent to use slot-based names throughout — in allowed-tools frontmatter blocks, in agent validation lists, in inline tool call instructions, and in display tables. This is the user-facing surface of the rename: after this plan, no QGSD command or agent shows old model-based names to the user.

Purpose: SLOT-01 (user sees slot names in all output), SLOT-03 (commands and orchestrator updated), SLOT-04 (mcp-status/mcp-set-model/mcp-update/mcp-restart accept slot names). Runs after Plans 01 and 02 so the configuration layer is already correct.

Output: 9 .md files fully updated.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-rename-and-migration/39-RESEARCH.md
@.planning/phases/39-rename-and-migration/39-01-SUMMARY.md
@.planning/phases/39-rename-and-migration/39-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mcp-status.md, mcp-set-model.md, mcp-restart.md, mcp-update.md — SLOT-04 commands</name>
  <files>
    commands/qgsd/mcp-status.md
    commands/qgsd/mcp-set-model.md
    commands/qgsd/mcp-restart.md
    commands/qgsd/mcp-update.md
  </files>
  <action>
Update the four SLOT-04 commands. For each file, read it first, then apply edits.

**commands/qgsd/mcp-status.md changes:**

1. `allowed-tools` block (lines 4–16): Replace all 10 identity tool entries:
   - `mcp__codex-cli__identity` → `mcp__codex-cli-1__identity`
   - `mcp__gemini-cli__identity` → `mcp__gemini-cli-1__identity`
   - `mcp__opencode__identity` → `mcp__opencode-1__identity`
   - `mcp__copilot-cli__identity` → `mcp__copilot-1__identity`
   - `mcp__claude-deepseek__identity` → `mcp__claude-1__identity`
   - `mcp__claude-minimax__identity` → `mcp__claude-2__identity`
   - `mcp__claude-qwen-coder__identity` → `mcp__claude-3__identity`
   - `mcp__claude-kimi__identity` → `mcp__claude-4__identity`
   - `mcp__claude-llama4__identity` → `mcp__claude-5__identity`
   - `mcp__claude-glm__identity` → `mcp__claude-6__identity`

2. Agent table (Step 3) — update Display Name, Identity Tool, Scoreboard Key columns:
   Replace the table body with:
   ```
   | Display Name    | Identity Tool                   | Scoreboard Key |
   |---|---|---|
   | codex-cli-1     | mcp__codex-cli-1__identity      | codex          |
   | gemini-cli-1    | mcp__gemini-cli-1__identity     | gemini         |
   | opencode-1      | mcp__opencode-1__identity       | opencode       |
   | copilot-1       | mcp__copilot-1__identity        | copilot        |
   | claude-1        | mcp__claude-1__identity         | deepseek       |
   | claude-2        | mcp__claude-2__identity         | minimax        |
   | claude-3        | mcp__claude-3__identity         | qwen-coder     |
   | claude-4        | mcp__claude-4__identity         | kimi           |
   | claude-5        | mcp__claude-5__identity         | llama4         |
   | claude-6        | mcp__claude-6__identity         | glm            |
   ```

3. Example output table (Step 5): Replace all old agent names in the example table with slot names. Replace row Display Names and old tool references to match. E.g.:
   - `codex-cli` → `codex-cli-1`, `gemini-cli` → `gemini-cli-1`, `opencode` → `opencode-1`, `copilot-cli` → `copilot-1`, `claude-deepseek` → `claude-1`, `claude-minimax` → `claude-2`, `claude-qwen-coder` → `claude-3`, `claude-kimi` → `claude-4`, `claude-llama4` → `claude-5`, `claude-glm` → `claude-6`

4. Note at the bottom about `claude-glm` not in scoreboard: Update to say `claude-6` (the slot name that replaced claude-glm).

**commands/qgsd/mcp-set-model.md changes:**

1. `allowed-tools` block: Apply the same 10 identity tool renames as in mcp-status.md.

2. Step 1 usage message valid agents list:
   Replace:
   ```
   codex-cli, gemini-cli, opencode, copilot-cli,
   claude-deepseek, claude-minimax, claude-qwen-coder,
   claude-kimi, claude-llama4, claude-glm
   ```
   With:
   ```
   codex-cli-1, gemini-cli-1, opencode-1, copilot-1,
   claude-1, claude-2, claude-3, claude-4, claude-5, claude-6
   ```

3. Step 2 validation list (check $AGENT against known list): Apply same replacement.

4. Step 2 error message valid agents display: Apply same replacement.

5. Step 3 identity tool derivation comment (around "Replace hyphens..."):
   Update the example: `codex-cli-1` → `mcp__codex-cli-1__identity`. Update any surrounding text to use slot names.

**commands/qgsd/mcp-restart.md changes:**

1. `allowed-tools` block: Apply the same 10 identity tool renames.

2. Step 1 usage message valid agents list: Apply the same slot-name replacement.

3. Step 2 validation list: Apply same replacement.

4. Step 2 error message display: Apply same replacement.

5. Step 3 inline node script — find the AGENT lookup block (reads ~/.claude.json to find the agent entry). Update any hardcoded agent name references in that script. The lookup is dynamic (uses the agent name argument), but the valid list check at line ~43–55 uses the old names.

**commands/qgsd/mcp-update.md changes:**

1. Step 1 usage message valid agents list: Apply slot-name replacement.

2. Step 2 validation list: Apply slot-name replacement.

3. Step 2 error message display: Apply slot-name replacement.

4. Find the `KNOWN_AGENTS` constant in the inline node script (around line ~163):
   Replace:
   ```javascript
   const KNOWN_AGENTS = [
     'codex-cli', 'gemini-cli', 'opencode', 'copilot-cli',
     'claude-deepseek', 'claude-minimax', 'claude-qwen-coder',
     'claude-kimi', 'claude-llama4', 'claude-glm'
   ];
   ```
   With:
   ```javascript
   const KNOWN_AGENTS = [
     'codex-cli-1', 'gemini-cli-1', 'opencode-1', 'copilot-1',
     'claude-1', 'claude-2', 'claude-3', 'claude-4', 'claude-5', 'claude-6'
   ];
   ```

5. Example output table in success criteria section: Replace old agent names with slot names.
  </action>
  <verify>
    # Zero old names in all 4 commands
    grep -n "codex-cli[^-1]\|gemini-cli[^-1]\|opencode[^-1]\|copilot-cli\|claude-deepseek\|claude-minimax\|claude-qwen-coder\|claude-kimi\|claude-llama4\|claude-glm" \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-set-model.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-restart.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-update.md
    # Must return ZERO matches
    # Slot names present
    grep "claude-1" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md
    grep "codex-cli-1" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-update.md
  </verify>
  <done>
    All 4 SLOT-04 commands reference slot names exclusively in allowed-tools, validation lists, inline scripts, and display tables; zero old model-based names remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update mcp-setup.md, quorum.md, quorum-test.md, debug.md, and qgsd-quorum-orchestrator.md</name>
  <files>
    commands/qgsd/mcp-setup.md
    commands/qgsd/quorum.md
    commands/qgsd/quorum-test.md
    commands/qgsd/debug.md
    agents/qgsd-quorum-orchestrator.md
  </files>
  <action>
Read each file before editing. Apply the following changes:

**commands/qgsd/mcp-setup.md changes:**

This command has a claude-mcp-server roster table showing agent names. Read the file and:
1. Find the first-run onboarding section listing claude-mcp-server agent names (e.g. `claude-deepseek`, `claude-minimax`, etc.) — replace with `claude-1`, `claude-2`, `claude-3`, `claude-4`, `claude-5`, `claude-6`.
2. Find the re-run menu display section showing the roster — replace with slot names.
3. Any inline `require()` or script references using old agent names: replace with slot names.
4. Find the agent table or list showing the copilot and other non-claude agents:
   - `copilot-cli` → `copilot-1`
   - `codex-cli` → `codex-cli-1`
   - `gemini-cli` → `gemini-cli-1`
   - `opencode` → `opencode-1`

**commands/qgsd/quorum.md changes:**

1. Step 2 — Team identity section: The 4 native agent identity tool calls use old names:
   - `mcp__codex-cli__identity` → `mcp__codex-cli-1__identity`
   - `mcp__gemini-cli__identity` → `mcp__gemini-cli-1__identity`
   - `mcp__opencode__identity` → `mcp__opencode-1__identity`
   - `mcp__copilot-cli__identity` → `mcp__copilot-1__identity`

2. The display name stripping comment (around line 116): currently says `claude-deepseek → deepseek`. Since slot names are the display names now, update to:
   - Remove or update the `claude-` stripping logic description
   - New display name rule: for claude-mcp servers, the display name IS the slot name (`claude-1`, `claude-2`, etc.) — no prefix stripping

3. Model query section tool calls for native agents:
   - `mcp__codex-cli__review` → `mcp__codex-cli-1__review`
   - `mcp__gemini-cli__gemini` → `mcp__gemini-cli-1__gemini`
   - `mcp__opencode__opencode` → `mcp__opencode-1__opencode`
   - `mcp__copilot-cli__ask` → `mcp__copilot-1__ask`

4. Scoreboard `--model` derivation comment (around line 284): currently says strip `claude-` prefix from server name. Update to say: for claude-mcp servers, use the `model` field from the identity response as the scoreboard key (not derived from the server name).

5. Any Mode B Task() dispatch lines referencing old tool names: update to slot-based names.

**commands/qgsd/quorum-test.md changes:**

1. Find the 4 inline Task() calls with old tool names and update:
   - `mcp__gemini-cli__gemini` → `mcp__gemini-cli-1__gemini`
   - `mcp__opencode__opencode` → `mcp__opencode-1__opencode`
   - `mcp__copilot-cli__ask` → `mcp__copilot-1__ask`
   - `mcp__codex-cli__review` → `mcp__codex-cli-1__review`

**commands/qgsd/debug.md changes:**

1. Find the 4 inline Task() calls with old tool names and update (same mapping as quorum-test.md).

**agents/qgsd-quorum-orchestrator.md changes:**

1. Step 2 — Team identity capture, native CLI agents (lines ~63–66):
   - `mcp__codex-cli__identity` → `mcp__codex-cli-1__identity`
   - `mcp__gemini-cli__identity` → `mcp__gemini-cli-1__identity`
   - `mcp__opencode__identity` → `mcp__opencode-1__identity`
   - `mcp__copilot-cli__identity` → `mcp__copilot-1__identity`

2. Display name logic (line ~76): Currently reads:
   `Display name = server name with 'claude-' prefix stripped (e.g. 'claude-deepseek' → 'deepseek')`
   Replace with:
   `Display name = slot name as-is (e.g. 'claude-1', 'claude-2') — no prefix stripping. For native CLI agents: 'codex-cli-1' → display as 'codex-cli-1', 'gemini-cli-1' → 'gemini-cli-1', etc.`

3. Mode A query section — native CLI agents call order (lines ~132–135):
   - `mcp__codex-cli__review` → `mcp__codex-cli-1__review`
   - `mcp__gemini-cli__gemini` → `mcp__gemini-cli-1__gemini`
   - `mcp__opencode__opencode` → `mcp__opencode-1__opencode`
   - `mcp__copilot-cli__ask` → `mcp__copilot-1__ask`

4. Mode B dispatch Task() calls (if present in file): update any old tool name references.

5. Scoreboard `--model` key derivation: if the file currently says to strip `claude-` prefix to get the model key for scoreboard, update to: use the `model` field from the identity/health_check response as the `--model` argument (not derived from the server name). This preserves the scoreboard's current model-shorthand keys (deepseek, minimax, etc.) by reading the actual model string.
  </action>
  <verify>
    # Zero old names across all 5 files
    grep -rn "mcp__codex-cli__\|mcp__gemini-cli__\|mcp__opencode__\|mcp__copilot-cli__\|claude-deepseek\|claude-minimax\|claude-qwen-coder\|claude-kimi\|claude-llama4\|claude-glm" \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-setup.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum-test.md \
      /Users/jonathanborduas/code/QGSD/commands/qgsd/debug.md \
      /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md
    # Must return ZERO matches
    # Slot names present in orchestrator
    grep "mcp__codex-cli-1__identity\|mcp__opencode-1__identity" /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md
    # Must return matches
  </verify>
  <done>
    All 5 files (mcp-setup.md, quorum.md, quorum-test.md, debug.md, qgsd-quorum-orchestrator.md) use slot names exclusively; orchestrator display-name logic updated to use slot name as-is; zero old model-based names remain.
  </done>
</task>

</tasks>

<verification>
Final verification across the full QGSD source:

1. `grep -rn "claude-deepseek\|claude-minimax\|claude-qwen-coder\|claude-kimi\|claude-llama4\|claude-glm" /Users/jonathanborduas/code/QGSD/commands/ /Users/jonathanborduas/code/QGSD/agents/ /Users/jonathanborduas/code/QGSD/hooks/ /Users/jonathanborduas/code/QGSD/templates/` returns ZERO matches

2. `grep -rn "mcp__codex-cli__\b\|mcp__gemini-cli__\b\|mcp__opencode__\b\|mcp__copilot-cli__\b" /Users/jonathanborduas/code/QGSD/commands/ /Users/jonathanborduas/code/QGSD/agents/ /Users/jonathanborduas/code/QGSD/hooks/ /Users/jonathanborduas/code/QGSD/templates/` returns ZERO matches

3. `grep -n "mcp__claude-1__identity" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-status.md` returns at least 1 match

4. `grep -n "codex-cli-1" /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md` returns at least 1 match

5. `grep -n "copilot-1" /Users/jonathanborduas/code/QGSD/commands/qgsd/mcp-set-model.md` returns at least 1 match
</verification>

<success_criteria>
- SLOT-01: All user-facing surfaces (status tables, quorum instructions, scoreboard display, wizard menus) show slot names
- SLOT-03 (completion): All QGSD commands and orchestrator agent updated — no old model-based names remain anywhere in commands/ or agents/
- SLOT-04: mcp-status, mcp-set-model, mcp-update, mcp-restart all accept and display slot names in their validation lists, tool calls, and output tables
</success_criteria>

<output>
After completion, create `.planning/phases/39-rename-and-migration/39-03-SUMMARY.md`
</output>
