---
phase: v0.13-02
plan: 03
status: complete
requirements_closed: [QUORUM-03]
files_modified:
  - qgsd-core/workflows/discuss-phase.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md
---

# Plan 03 Summary — QUORUM-03

## What Was Built

One surgical insertion into `discuss-phase.md` (source + installed copy):

### Task 1: Second quorum pass on for_user[] survivors at present_gray_areas (QUORUM-03)

Inserted a new sub-section immediately after `<step name="present_gray_areas">` opening line and `Present the domain boundary and gray areas to user.`, BEFORE the "First, display auto-resolved assumptions" block.

The new section implements a second quorum attempt on `for_user[]` survivors when in auto mode:

**Gate**: `--auto` flag present OR `workflow.auto_advance=true` AND `for_user[]` is non-empty

**Per-question loop** (sequential, not parallel):
1. Claude forms position: APPROVE (quorum can decide) or BLOCK (needs user) with rationale
2. Dispatch all active slots as sibling `qgsd-quorum-slot-worker` Tasks (Mode A)
3. Deliberate up to **3 rounds** (R4 cap — this is a secondary pre-filter, not full R3 10 rounds)
4. Update scoreboard BEFORE moving to next question (ordering guarantee)
5. Route: APPROVED → add to `auto_resolved[]`, remove from `for_user[]`; BLOCKED/ESCALATED → keep in `for_user[]`

**After all items processed:**
- `for_user[]` empty → skip AskUserQuestion, fast-path to `write_context` with combined assumption list
- `for_user[]` still has items → continue to standard AskUserQuestion below (unchanged)

**Interactive mode**: If neither `--auto` nor `AUTO_CFG` is true, skip entire sub-section — AskUserQuestion behavior completely unchanged.

## Must-Haves Verified

- [x] qgsd-quorum-slot-worker present: 2 matches (R4 pre-filter line 208 + QUORUM-03 line 256)
- [x] update-scoreboard present in QUORUM-03 section: match at line 261
- [x] "Auto mode second quorum pass (QUORUM-03)" header present at line 237
- [x] "second quorum pass" present in body text at line 247
- [x] r4_pre_filter step heading unchanged: still at line 194
- [x] AUTO_CFG present in QUORUM-03 section at line 242
- [x] Interactive mode bypass guard present: "If neither `--auto` nor `AUTO_CFG` is true (interactive mode)" at line 284
- [x] Source and installed copies in sync: diff shows only path differences

## Decisions

- Deliberation cap set to 3 rounds (not 10) to match R4 pre-filter cap — this is a secondary filter, not a full R3 quorum
- Second pass fires at `present_gray_areas` AFTER `r4_pre_filter` has already run — not a replacement
- r4_pre_filter step left completely untouched (verified via grep)
- Interactive mode (no --auto, no workflow.auto_advance) receives zero behavioral change
