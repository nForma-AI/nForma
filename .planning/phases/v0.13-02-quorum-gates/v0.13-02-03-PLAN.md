---
phase: v0.13-02
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - qgsd-core/workflows/discuss-phase.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md
autonomous: true
requirements:
  - QUORUM-03

must_haves:
  truths:
    - "In auto mode (--auto flag OR workflow.auto_advance=true), discuss-phase runs a second quorum pass on for_user[] survivors before presenting to user"
    - "Second quorum pass fires at present_gray_areas entry, AFTER r4_pre_filter has already run — not a replacement for r4_pre_filter"
    - "Scoreboard updated via update-scoreboard.cjs after each per-question quorum round, before moving to next question"
    - "Questions where quorum APPROVEs with answer are moved to auto_resolved[] and removed from for_user[]"
    - "Questions where quorum BLOCKs remain in for_user[] and are presented to user as before"
    - "In interactive mode (no --auto, no workflow.auto_advance), discuss-phase behavior is completely unchanged"
    - "R4 pre-filter step (r4_pre_filter) is completely unchanged"
    - "Source and installed copies of discuss-phase.md are identical (diff shows only tilde-vs-absolute path differences)"
  artifacts:
    - path: "qgsd-core/workflows/discuss-phase.md"
      provides: "Second quorum pass on for_user[] survivors in auto mode (QUORUM-03)"
      contains: "qgsd-quorum-slot-worker"
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md"
      provides: "Installed copy — identical to source"
      contains: "qgsd-quorum-slot-worker"
  key_links:
    - from: "discuss-phase present_gray_areas (auto mode)"
      to: "R3 quorum per for_user[] item"
      via: "qgsd-quorum-slot-worker Tasks (Mode A)"
      pattern: "qgsd-quorum-slot-worker"
    - from: "discuss-phase per-question quorum"
      to: "update-scoreboard.cjs"
      via: "node bin/update-scoreboard.cjs"
      pattern: "update-scoreboard"
    - from: "discuss-phase quorum APPROVE"
      to: "auto_resolved[]"
      via: "move from for_user[] to auto_resolved[]"
      pattern: "auto_resolved"
---

<objective>
Wire QUORUM-03 into discuss-phase.md by adding a second quorum pass on for_user[] survivors in auto mode, at the present_gray_areas step entry point.

Purpose: When discuss-phase runs in auto mode, questions the R4 pre-filter couldn't resolve get one more quorum attempt before being presented to the user. This further reduces interruptions without removing user agency (interactive mode is fully unchanged).

Key design: This is NOT a replacement for R4 pre-filter. The r4_pre_filter step runs at analyze_phase and processes all candidate questions. The second pass here runs on the subset that survived R4 (for_user[] survivors). Gate: auto mode only.

Output: Modified discuss-phase.md (source + installed) with QUORUM-03 implemented.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/v0.13-02-quorum-gates/v0.13-02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add second quorum pass on for_user[] survivors at present_gray_areas in auto mode (QUORUM-03)</name>
  <files>
    qgsd-core/workflows/discuss-phase.md
    /Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md
  </files>
  <action>
Read `qgsd-core/workflows/discuss-phase.md` first (required before editing).

Locate the `<step name="present_gray_areas">` section (around line 234). The step begins with:

```
<step name="present_gray_areas">
Present the domain boundary and gray areas to user.

**First, display auto-resolved assumptions (from r4_pre_filter):**
```

The step then shows auto-resolved assumptions and presents for_user[] items via AskUserQuestion (multiSelect).

**Insertion point:** Add a new sub-section BEFORE the "First, display auto-resolved assumptions" block. Insert immediately after the `<step name="present_gray_areas">` opening line and the `Present the domain boundary and gray areas to user.` line:

```
**Auto mode second quorum pass (QUORUM-03):**

Before presenting to the user, check if auto mode is active:

```bash
AUTO_CFG=$(node ~/.claude/qgsd/bin/gsd-tools.cjs config-get workflow.auto_advance 2>/dev/null || echo "true")
```

**If `--auto` flag present OR `AUTO_CFG` is `"true"` AND `for_user[]` is non-empty:**

Run a second quorum pass on each surviving item in `for_user[]`. This is distinct from the R4 pre-filter (which already ran at analyze_phase) — this is a second attempt on the survivors.

For each question still in `for_user[]` (process sequentially):

1. Form Claude's position: can quorum decide this question, or does it genuinely require the user's vision/preference? State APPROVE (quorum can decide) or BLOCK (needs user) with 1-2 sentence rationale.

2. Run R3 quorum inline (dispatch_pattern from `commands/qgsd/quorum.md`):
   - Mode A — pure question
   - Question: "Should '[question text]' be decided by quorum now (removing it from user presentation), or does it genuinely require the user's vision/preference? Phase: {phase_name}. Goal: {phase_goal}. Context: {any relevant patterns from STATE.md}. If quorum can decide: provide the recommended answer biased toward the long-term solution."
   - Dispatch all active slots as sibling `qgsd-quorum-slot-worker` Tasks (one per slot)
   - Deliberate up to 3 rounds per R4 (secondary pre-filter, not full R3 10 rounds)

3. After quorum vote for this question, update scoreboard BEFORE moving to next question:
   ```bash
   node "$HOME/.claude/qgsd-bin/update-scoreboard.cjs" \
     --model <model_name_or_slot> \
     --result <vote_code> \
     --task "discuss-phase-{PHASE_NUMBER}" \
     --round <round_number> \
     --verdict <APPROVE|BLOCK> \
     --task-description "Discuss-phase gray area: {question text (first 80 chars)}"
   ```

4. Route on quorum_result:
   - **APPROVED**: Record as auto-resolved assumption. Add to `auto_resolved[]` with the consensus answer. Remove from `for_user[]`.
   - **BLOCKED or ESCALATED**: Keep in `for_user[]`. Mark for user presentation.

**After processing all for_user[] items:**

- If `for_user[]` is now empty: Skip AskUserQuestion presentation. Go directly to `write_context` (same fast-path as R4 pre-filter empty case). Display:
  ```
  All gray areas resolved by quorum (including second-pass R4 survivors). Proceeding to context capture.

  [list each auto-resolved assumption from both R4 pre-filter and second pass]
  ```
- If `for_user[]` still has items: Continue to the standard AskUserQuestion presentation below.

**If neither `--auto` nor `AUTO_CFG` is true (interactive mode):** Skip this entire sub-section. Go directly to the standard display and AskUserQuestion below (unchanged).

```

After this insertion, the rest of the `present_gray_areas` step continues exactly as before: display auto-resolved assumptions, then AskUserQuestion for for_user[] items.

Do NOT modify:
- The `<step name="r4_pre_filter">` step — it is completely unchanged
- The `check_existing` step — unchanged
- The `analyze_phase` step — unchanged
- The AskUserQuestion multiSelect call for for_user[] items — it stays, but is now only reached when for_user[] is non-empty and in interactive mode or quorum couldn't resolve all items

After editing the source file, apply the identical change to the installed copy at `/Users/jonathanborduas/.claude/qgsd/workflows/discuss-phase.md`.

Verify: the r4_pre_filter step content (lines ~194-232 in source) is completely unchanged after your edit. Run `grep -n "r4_pre_filter" qgsd-core/workflows/discuss-phase.md` to confirm the step heading is still there.
  </action>
  <verify>
    Run: `grep -n "qgsd-quorum-slot-worker" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 2 matches (one from existing R4 pre-filter + one new from QUORUM-03 second pass).

    Run: `grep -n "update-scoreboard" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 1 match in the QUORUM-03 second pass section.

    Run: `grep -n "QUORUM-03\|second quorum pass\|Auto mode second" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 1 match in the present_gray_areas step.

    Run: `grep -n "r4_pre_filter" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 1 match — r4_pre_filter step still present (unchanged).

    Run: `grep -n "AUTO_CFG" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 1 match in the QUORUM-03 section (new) — may also be present in auto_advance step.

    Run: `grep -n "interactive mode.*unchanged\|Skip.*interactive\|neither.*auto" qgsd-core/workflows/discuss-phase.md`
    Expected: At least 1 match — interactive mode bypass guard present.

    Run: `diff qgsd-core/workflows/discuss-phase.md ~/.claude/qgsd/workflows/discuss-phase.md | grep "^[<>]" | grep -v "~/.claude\|/Users/jonathanborduas" | head -5`
    Expected: No significant diffs beyond path substitutions.
  </verify>
  <done>
    discuss-phase.md present_gray_areas step adds a second quorum pass on for_user[] survivors when in auto mode. R4 pre-filter step unchanged. Interactive mode completely unchanged. Scoreboard updated per question before moving to next. Source and installed copies in sync.
  </done>
</task>

</tasks>

<verification>
Full verification after task complete:

1. `grep -n "qgsd-quorum-slot-worker" qgsd-core/workflows/discuss-phase.md` → at least 2 matches (R4 pre-filter + QUORUM-03)
2. `grep -n "update-scoreboard" qgsd-core/workflows/discuss-phase.md` → present in QUORUM-03 section
3. `grep -n "QUORUM-03\|second quorum pass\|Auto mode second" qgsd-core/workflows/discuss-phase.md` → present
4. `grep -n "r4_pre_filter" qgsd-core/workflows/discuss-phase.md` → still present (step heading unchanged)
5. `grep -n "neither.*auto\|interactive mode" qgsd-core/workflows/discuss-phase.md` → bypass guard present
6. `diff qgsd-core/workflows/discuss-phase.md ~/.claude/qgsd/workflows/discuss-phase.md` → only tilde-vs-absolute path diffs
</verification>

<success_criteria>
- QUORUM-03: In auto mode, for_user[] survivors get a second quorum pass at present_gray_areas entry
- QUORUM-03: Second pass fires AFTER r4_pre_filter — not a replacement, a second attempt
- QUORUM-03: Scoreboard updated per question before moving to next (per-question ordering guarantee)
- QUORUM-03: APPROVE moves question to auto_resolved[], removes from for_user[]
- QUORUM-03: BLOCK keeps question in for_user[] for user presentation
- QUORUM-03: Interactive mode (no --auto, no workflow.auto_advance) completely unchanged
- R4 pre-filter step unchanged
- Source and installed copies are in sync
</success_criteria>

<output>
After completion, create `.planning/phases/v0.13-02-quorum-gates/v0.13-02-03-SUMMARY.md`
</output>
