---
phase: v0.13-02
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - qgsd-core/workflows/execute-phase.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md
autonomous: true
requirements:
  - QUORUM-02

must_haves:
  truths:
    - "execute-phase verify_phase_goal step gaps_found branch no longer presents a manual /qgsd:plan-phase {X} --gaps suggestion to the user"
    - "When gaps_found, quorum runs inline with a compact prompt (gap summaries, not full VERIFICATION.md) before any action"
    - "Scoreboard is updated via update-scoreboard.cjs BEFORE the plan-phase Task is spawned (ordering guarantee)"
    - "After quorum APPROVE, an inline plan-phase Task is spawned autonomously"
    - "After quorum BLOCK, user is escalated to with the gap report PLUS quorum diagnosis"
    - "execute-phase offer_next Exception note updated to reflect quorum-based gaps_found routing"
    - "passed and human_needed branches in verify_phase_goal are unchanged"
    - "Source and installed copies of execute-phase.md are identical (diff shows only tilde-vs-absolute path differences)"
  artifacts:
    - path: "qgsd-core/workflows/execute-phase.md"
      provides: "gaps_found quorum gate (QUORUM-02) in verify_phase_goal step"
      contains: "qgsd-quorum-slot-worker"
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md"
      provides: "Installed copy — identical to source"
      contains: "qgsd-quorum-slot-worker"
  key_links:
    - from: "execute-phase verify_phase_goal gaps_found"
      to: "R3 quorum diagnosis"
      via: "qgsd-quorum-slot-worker Tasks (compact Mode A)"
      pattern: "qgsd-quorum-slot-worker"
    - from: "execute-phase gaps_found quorum"
      to: "update-scoreboard.cjs"
      via: "node bin/update-scoreboard.cjs"
      pattern: "update-scoreboard"
    - from: "execute-phase gaps_found quorum APPROVE"
      to: "plan-phase --gaps Task"
      via: "Task(subagent_type='general-purpose')"
      pattern: "plan-phase.*--gaps"
---

<objective>
Wire QUORUM-02 into execute-phase.md by replacing the gaps_found manual suggestion in the verify_phase_goal step with inline R3 quorum diagnosis and auto-resolution.

Purpose: After this plan, when execute-phase detects verification gaps, quorum diagnoses whether they are auto-resolvable. If APPROVE, plan-phase --gaps is spawned autonomously. If BLOCK, the user gets the gap report with quorum diagnosis attached — not just a bare suggestion.

Key constraint: execute-phase orchestrator context is ~10-15% — quorum prompt must be compact (1-sentence-per-gap summaries, not full VERIFICATION.md text).

Output: Modified execute-phase.md (source + installed) with QUORUM-02 implemented.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/v0.13-02-quorum-gates/v0.13-02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gaps_found manual suggestion with quorum diagnosis + auto-spawn (QUORUM-02)</name>
  <files>
    qgsd-core/workflows/execute-phase.md
    /Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md
  </files>
  <action>
Read `qgsd-core/workflows/execute-phase.md` first (required before editing).

Locate the `<step name="verify_phase_goal">` section. Within it, find the `**If gaps_found:**` block (around line 426):

```
**If gaps_found:**
```
## ⚠ Phase {X}: {Name} — Gaps Found

**Score:** {N}/{M} must-haves verified
**Report:** {phase_dir}/{phase_num}-VERIFICATION.md

### What's Missing
{Gap summaries from VERIFICATION.md}

---
## ▶ Next Up

`/qgsd:plan-phase {X} --gaps`

<sub>`/clear` first → fresh context window</sub>

Also: `cat {phase_dir}/{phase_num}-VERIFICATION.md` — full report
Also: `/qgsd:verify-work {X}` — manual testing first
```

Gap closure cycle: `/qgsd:plan-phase {X} --gaps` reads VERIFICATION.md → creates gap plans with `gap_closure: true` → user runs `/qgsd:execute-phase {X} --gaps-only` → verifier re-runs.
```

Replace the entire `**If gaps_found:**` block with:

```
**If gaps_found:**

Read the gaps section from `${PHASE_DIR}/${PHASE_NUM}-VERIFICATION.md`. Extract each gap as a 1-sentence summary (keep compact — orchestrator context budget is ~10-15%).

Form your own position: are these gaps auto-resolvable via plan-phase --gaps, or do they require human review? State your vote APPROVE (auto-resolvable) or BLOCK (needs human) with 1-2 sentence rationale.

Run R3 quorum inline (dispatch_pattern from `commands/qgsd/quorum.md`):
- Mode A — compact prompt to preserve context budget
- Question: "Phase {PHASE_NUMBER} verification found {N} gaps. Are these auto-resolvable via plan-phase --gaps Task spawn, or do they require human review? Gaps: {1-sentence-per-gap — max 20 words each}. Vote APPROVE (auto-resolvable) or BLOCK (needs human)."
- Dispatch all active slots as sibling `qgsd-quorum-slot-worker` Tasks (one per slot)
- Synthesize results inline, deliberate up to 10 rounds per R3.3

After quorum vote completes, update the scoreboard BEFORE spawning any Task:
```bash
node "$HOME/.claude/qgsd-bin/update-scoreboard.cjs" \
  --model <model_name_or_slot> \
  --result <vote_code> \
  --task "execute-phase-{PHASE_NUMBER}" \
  --round <round_number> \
  --verdict <APPROVE|BLOCK> \
  --task-description "Phase {PHASE_NUMBER} verification gaps: auto-resolvable vs human-needed"
```

Route on quorum_result:
- **APPROVED**: Auto-spawn plan-phase --gaps Task:
  ```
  Task(
    prompt="Run /qgsd:plan-phase {PHASE_NUMBER} --gaps --auto",
    subagent_type="general-purpose",
    description="Plan gap closure for phase {PHASE_NUMBER}"
  )
  ```
  Log: `⚡ Quorum approved auto-fix — spawning plan-phase {PHASE_NUMBER} --gaps`

- **BLOCKED**: Escalate to user with gap report AND quorum diagnosis:
  ```
  ## ⚠ Phase {X}: {Name} — Gaps Found

  **Score:** {N}/{M} must-haves verified
  **Report:** {phase_dir}/{phase_num}-VERIFICATION.md

  ### What's Missing
  {Gap summaries from VERIFICATION.md}

  ### Quorum Diagnosis
  Quorum could not confirm auto-resolution:
  {quorum_block_reason — specific model objection(s)}

  ---
  ## ▶ Next Up

  `/qgsd:plan-phase {X} --gaps`

  <sub>`/clear` first → fresh context window</sub>

  Also: `cat {phase_dir}/{phase_num}-VERIFICATION.md` — full report
  Also: `/qgsd:verify-work {X}` — manual testing first
  ```

Gap closure cycle: `/qgsd:plan-phase {X} --gaps` reads VERIFICATION.md → creates gap plans with `gap_closure: true` → user runs `/qgsd:execute-phase {X} --gaps-only` → verifier re-runs.
```

Do NOT modify anything else in verify_phase_goal. The `passed` and `human_needed` branches stay unchanged.

After editing the source file, apply the identical change to the installed copy at `/Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md`.
  </action>
  <verify>
    Run: `grep -n "qgsd-quorum-slot-worker" qgsd-core/workflows/execute-phase.md`
    Expected: At least 1 match in the gaps_found section of verify_phase_goal.

    Run: `grep -n "update-scoreboard" qgsd-core/workflows/execute-phase.md`
    Expected: At least 2 matches (one from existing human_needed quorum block + one new in gaps_found).

    Run: `grep -n "plan-phase.*--gaps.*--auto" qgsd-core/workflows/execute-phase.md`
    Expected: At least 1 match in the APPROVED auto-spawn block.

    Run: `grep -n "Quorum Diagnosis" qgsd-core/workflows/execute-phase.md`
    Expected: At least 1 match in the BLOCKED escalation block.

    Run: `grep -n "human_needed\|If human_needed" qgsd-core/workflows/execute-phase.md`
    Expected: Still present (unchanged).

    Run: `diff qgsd-core/workflows/execute-phase.md ~/.claude/qgsd/workflows/execute-phase.md | grep "^[<>]" | grep -v "~/.claude\|/Users/jonathanborduas" | head -5`
    Expected: No significant diffs beyond path substitutions.
  </verify>
  <done>
    execute-phase.md gaps_found branch runs R3 quorum. APPROVE spawns plan-phase --gaps Task autonomously. BLOCK escalates to user with quorum diagnosis. passed/human_needed branches unchanged. Source and installed copies in sync.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update offer_next Exception note for gaps_found (QUORUM-02 follow-on)</name>
  <files>
    qgsd-core/workflows/execute-phase.md
    /Users/jonathanborduas/.claude/qgsd/workflows/execute-phase.md
  </files>
  <action>
In `qgsd-core/workflows/execute-phase.md`, locate the `<step name="offer_next">` section. Find the Exception note (around line 479):

```
**Exception:** If `gaps_found`, the `verify_phase_goal` step already presents the gap-closure path (`/qgsd:plan-phase {X} --gaps`). No additional routing needed — skip auto-advance.
```

Replace it with:

```
**Exception:** If `gaps_found`, the `verify_phase_goal` step handles routing via quorum (QUORUM-02). If quorum approved auto-fix, plan-phase --gaps was already spawned inline. If quorum blocked (escalated to user), the user sees the gap report — no additional routing needed. In both cases, skip auto-advance.
```

Apply the same change to the installed copy.
  </action>
  <verify>
    Run: `grep -n "QUORUM-02\|quorum.*QUORUM\|quorum approved auto-fix" qgsd-core/workflows/execute-phase.md`
    Expected: At least 1 match in the offer_next Exception note.

    Run: `grep -n "already presents the gap-closure path" qgsd-core/workflows/execute-phase.md`
    Expected: Zero matches — old Exception text removed.
  </verify>
  <done>
    offer_next Exception note updated to describe QUORUM-02 routing. Old "already presents gap-closure path" text removed.
  </done>
</task>

</tasks>

<verification>
Full verification after both tasks complete:

1. `grep -n "qgsd-quorum-slot-worker" qgsd-core/workflows/execute-phase.md` → present in gaps_found block
2. `grep -n "update-scoreboard" qgsd-core/workflows/execute-phase.md` → present in gaps_found block
3. `grep -n "plan-phase.*--gaps.*--auto\|plan-phase {PHASE_NUMBER} --gaps --auto" qgsd-core/workflows/execute-phase.md` → present
4. `grep -n "Quorum Diagnosis" qgsd-core/workflows/execute-phase.md` → present in BLOCKED escalation
5. `grep -n "human_needed" qgsd-core/workflows/execute-phase.md` → present (unchanged)
6. `grep -n "already presents the gap-closure path" qgsd-core/workflows/execute-phase.md` → zero matches
7. `diff qgsd-core/workflows/execute-phase.md ~/.claude/qgsd/workflows/execute-phase.md` → only tilde-vs-absolute path diffs
</verification>

<success_criteria>
- QUORUM-02: execute-phase gaps_found branch runs R3 quorum before any action
- QUORUM-02: Compact prompt used (1-sentence-per-gap summaries, max 20 words each)
- QUORUM-02: Scoreboard updated before plan-phase Task spawn
- QUORUM-02: APPROVE path auto-spawns plan-phase --gaps Task
- QUORUM-02: BLOCK path escalates to user with gap report + quorum diagnosis
- offer_next Exception note updated to reflect quorum routing
- passed and human_needed branches in verify_phase_goal unchanged
- Source and installed copies are in sync
</success_criteria>

<output>
After completion, create `.planning/phases/v0.13-02-quorum-gates/v0.13-02-02-SUMMARY.md`
</output>
