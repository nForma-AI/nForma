# v0.12-02 Plan 02 — SUMMARY

**Phase:** v0.12-02
**Plan:** 02 — TLA+ formal specification files
**Status:** COMPLETE
**Commits:** bddb07e (QGSDQuorum.tla), ee42f27 (cfg files + gitignore + README)
**Date:** 2026-02-25

## What Was Built

- `formal/tla/QGSDQuorum.tla` — TLA+ formal specification with:
  - 4 states mirroring XState machine: IDLE, COLLECTING_VOTES, DELIBERATING, DECIDED
  - AgentSymmetry == Permutations(Agents) (required for SYMMETRY in MCsafety.cfg)
  - AgentsSet5 and AgentsSet3 named constant sets for cfg CONSTANTS overrides
  - MinQuorumMet safety invariant: successCount * 2 >= N (correct integer majority)
  - NoInvalidTransition temporal property ([][...]_vars — NOT INVARIANT; primed vars safe)
  - EventualConsensus liveness property: <>(phase = "DECIDED")
  - WF_vars(Decide) + WF_vars(StartQuorum) fairness in Spec

- `formal/tla/MCsafety.cfg` — N=5, SYMMETRY AgentSymmetry, INVARIANT TypeOK + MinQuorumMet, PROPERTY NoInvalidTransition, CHECK_DEADLOCK FALSE. No liveness (incompatible with SYMMETRY).

- `formal/tla/MCliveness.cfg` — N=3, NO SYMMETRY, PROPERTY EventualConsensus, CHECK_DEADLOCK FALSE.

- `formal/tla/.gitignore` — Excludes tla2tools.jar (~50MB).

- `formal/tla/README.md` — Setup instructions: download tla2tools.jar v1.8.0, run node bin/run-tlc.cjs.

## Verification Results

All 5 grep checks passed:
1. COLLECTING_VOTES appears 6 times in QGSDQuorum.tla (>= 2 required) — PASS
2. MCsafety.cfg has `SYMMETRY AgentSymmetry` — PASS
3. MCliveness.cfg has no SYMMETRY directive — PASS
4. MCliveness.cfg has `PROPERTY EventualConsensus` — PASS
5. formal/tla/.gitignore excludes tla2tools.jar — PASS
6. formal/tla/README.md has tlaplus/releases download URL — PASS

npm test: 268 pass, 4 fail (run-tlc stubs still RED — correct at this wave).

## Success Criteria Verification

1. formal/tla/QGSDQuorum.tla: all required identifiers present, correct TLA+ syntax, WF_vars(Decide) in Spec — PASS
2. formal/tla/MCsafety.cfg: SYMMETRY AgentSymmetry, N=5, both INVARIANTs, PROPERTY, CHECK_DEADLOCK FALSE, no EventualConsensus — PASS
3. formal/tla/MCliveness.cfg: N=3, EventualConsensus, CHECK_DEADLOCK FALSE, NO SYMMETRY — PASS
4. formal/tla/.gitignore excludes tla2tools.jar — PASS
5. formal/tla/README.md documents download URL v1.8.0 and run-tlc.cjs usage — PASS
6. npm test pre-existing tests unaffected (268/268 pass) — PASS

## Deviations

None.
