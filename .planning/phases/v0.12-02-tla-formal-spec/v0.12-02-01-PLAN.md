---
phase: v0.12-02
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/run-tlc.test.cjs
  - package.json
autonomous: true
requirements:
  - TLA-04

must_haves:
  truths:
    - "bin/run-tlc.test.cjs exists with failing stubs covering the three TLA-04 error paths (Java not found, JAR missing, invalid config)"
    - "npm test fails on run-tlc.test.cjs stubs because bin/run-tlc.cjs does not yet exist"
    - "package.json test script includes bin/run-tlc.test.cjs so Wave 0 stubs run as part of the full suite"
  artifacts:
    - path: "bin/run-tlc.test.cjs"
      provides: "Wave 0 RED stubs for TLA-04 error-path coverage (no Java invoked)"
      min_lines: 40
    - path: "package.json"
      provides: "Updated test script with bin/run-tlc.test.cjs appended"
      contains: "run-tlc.test.cjs"
  key_links:
    - from: "bin/run-tlc.test.cjs"
      to: "bin/run-tlc.cjs"
      via: "spawnSync(process.execPath, [path.join(__dirname, 'run-tlc.cjs')], ...)"
      pattern: "run-tlc\\.cjs"
---

<objective>
Write TDD RED stubs for bin/run-tlc.test.cjs covering TLA-04 error paths and add the test file to the package.json test script. At this stage bin/run-tlc.cjs does not yet exist — stubs must fail at invocation.

Purpose: Wave 0 scaffolding ensures test coverage is committed before any implementation starts. The stubs cover the three no-Java error paths that npm test must exercise without a JVM installed. This is required by the VALIDATION.md Wave 0 requirement.
Output: bin/run-tlc.test.cjs (4 RED stubs), updated package.json test script.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.12-02-tla-formal-spec/v0.12-02-RESEARCH.md
@.planning/phases/v0.12-02-tla-formal-spec/v0.12-02-VALIDATION.md
@bin/validate-traces.test.cjs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 RED stubs — bin/run-tlc.test.cjs</name>
  <files>bin/run-tlc.test.cjs</files>
  <action>
Create bin/run-tlc.test.cjs with 4 failing stubs using Node.js built-in node:test. Each stub invokes run-tlc.cjs via spawnSync(process.execPath, [...]) — no actual Java is invoked. All tests must FAIL at this stage because bin/run-tlc.cjs does not yet exist.

Pattern: mirror bin/validate-traces.test.cjs structure (spawnSync the script with controlled env/args, assert on exit code and stderr).

Stubs required (4 tests, in this order):

1. "exits non-zero and prints clear error when JAVA_HOME points to nonexistent path"
   - spawnSync run-tlc.cjs with env: { ...process.env, JAVA_HOME: '/nonexistent/java/path' }
   - assert.strictEqual(result.status, 1)
   - assert.match(result.stderr, /JAVA_HOME|java/i)

2. "exits non-zero and prints download URL when tla2tools.jar is not found"
   - spawnSync run-tlc.cjs with env: { ...process.env, JAVA_HOME: undefined, PATH: '' } (no Java on PATH either)
   - Note: since bin/run-tlc.cjs won't exist yet, the test will fail at spawn time — that is the expected RED state
   - assert.strictEqual(result.status, 1)
   - assert.match(result.stderr, /tla2tools\.jar|github\.com\/tlaplus/i)

3. "exits non-zero with descriptive message for unknown --config value"
   - spawnSync run-tlc.cjs with args: ['--config=bogus']
   - assert.strictEqual(result.status, 1)
   - assert.match(result.stderr, /Unknown config|bogus/i)

4. "exits non-zero and lists valid configs in error output for invalid config"
   - spawnSync run-tlc.cjs with args: ['--config=invalid']
   - assert.strictEqual(result.status, 1)
   - assert.match(result.stderr, /MCsafety|MCliveness/i)

File header:
```javascript
#!/usr/bin/env node
'use strict';
// bin/run-tlc.test.cjs
// Wave 0 RED stubs for bin/run-tlc.cjs error paths.
// All tests check error conditions only — no Java or TLC invocation.
// Requirements: TLA-04

const { test } = require('node:test');
const assert   = require('node:assert');
const { spawnSync } = require('child_process');
const path = require('path');

const RUN_TLC = path.join(__dirname, 'run-tlc.cjs');
```

Run after writing: `node --test bin/run-tlc.test.cjs` — ALL 4 tests must FAIL (bin/run-tlc.cjs not found yet).
Commit: `test(v0.12-02): add Wave 0 RED stubs for run-tlc error paths`
  </action>
  <verify>node --test bin/run-tlc.test.cjs 2>&1 | grep -E "fail|Error|Cannot find|ENOENT"; echo "exit: $?"</verify>
  <done>All 4 tests in bin/run-tlc.test.cjs fail (bin/run-tlc.cjs not found or assertion errors). Wave 0 commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add run-tlc.test.cjs to package.json test script</name>
  <files>package.json</files>
  <action>
Append bin/run-tlc.test.cjs to the space-separated list of test files in the package.json "test" script. The current script ends with `bin/validate-traces.test.cjs` — add ` bin/run-tlc.test.cjs` after it (space-separated, before the closing quote).

Read package.json first to confirm exact current ending. Append in place without disrupting the other entries.

After editing, run: `npm test` — expect the suite to fail only on the 4 new run-tlc stubs (the rest pass). Confirm the error is the missing run-tlc.cjs, not a syntax error or path issue.
  </action>
  <verify>node -e "const p = require('./package.json'); console.log(p.scripts.test.includes('run-tlc.test.cjs'))"</verify>
  <done>package.json test script includes bin/run-tlc.test.cjs. npm test runs the stubs and fails only on the 4 new tests (all pre-existing tests continue to pass).</done>
</task>

</tasks>

<verification>
Run: `npm test`
Expected: All pre-existing tests pass; 4 new run-tlc stubs fail with module-not-found or assertion errors. Zero regressions in existing suite.
Grep check: `grep "run-tlc.test.cjs" package.json` — must match.
</verification>

<success_criteria>
1. bin/run-tlc.test.cjs exists with 4 test stubs: JAVA_HOME nonexistent path, JAR missing, unknown config, invalid config
2. All 4 stubs fail (bin/run-tlc.cjs does not yet exist)
3. package.json test script includes bin/run-tlc.test.cjs
4. All pre-existing npm test cases continue to pass
5. Wave 0 commit exists: `test(v0.12-02): add Wave 0 RED stubs for run-tlc error paths`
</success_criteria>

<output>
After completion, create `.planning/phases/v0.12-02-tla-formal-spec/v0.12-02-01-SUMMARY.md`
</output>
