# v0.12-02 Plan 03 — SUMMARY

**Phase:** v0.12-02
**Plan:** 03 — GREEN phase: implement bin/run-tlc.cjs
**Status:** COMPLETE
**Commit:** 1608b76
**Date:** 2026-02-25

## What Was Built

- `bin/run-tlc.cjs` — Node.js TLC wrapper implementing all 4 error paths:
  1. Unknown `--config` value: exits 1, stderr shows `Unknown config: X. Valid: MCsafety, MCliveness`
  2. JAVA_HOME set to nonexistent path: exits 1, stderr shows JAVA_HOME path error
  3. Java not on PATH (no JAVA_HOME): exits 1, stderr shows download URL (adoptium.net)
  4. tla2tools.jar not found: exits 1, stderr shows v1.8.0 download command from github.com/tlaplus

  Additional behaviors: Java >=17 version check, MCliveness uses `-workers 1`, MCsafety uses `-workers auto`. Zero external npm dependencies (child_process, fs, path only).

## Deviation: Version Regex Fix

The plan's regex `/(?:openjdk|java version\s+[""]?)(\d+)/i` did not match `openjdk 25.0.1` (no space captured between prefix and major version). Fixed to `/(?:openjdk\s+|java version\s+[""]?)(\d+)/i`. This is a correctness fix required to pass test 2 — the plan regex was incorrect for the `openjdk X.Y.Z` format produced by modern OpenJDK. All 4 tests now pass GREEN.

## Test Results

- All 4 run-tlc.test.cjs stubs: GREEN (were RED in Plan 01)
- Full suite: 272 tests pass, 0 fail (268 pre-existing + 4 new)
- Zero regressions

## Success Criteria Verification

1. bin/run-tlc.cjs handles all 4 error cases — PASS
2. All 4 tests in bin/run-tlc.test.cjs pass GREEN — PASS
3. npm test passes with zero regressions — PASS (272/272)
4. bin/run-tlc.cjs has zero external npm dependencies — PASS
5. MCliveness uses -workers 1; MCsafety uses -workers auto — PASS
6. JAR not-found error includes v1.8.0 download URL — PASS

## Manual Verification Required (Java needed — deferred to /qgsd:verify-work)

- TLC MCsafety run: `node bin/run-tlc.cjs MCsafety` — requires Java >=17 + tla2tools.jar
- TLC MCliveness run: `node bin/run-tlc.cjs MCliveness` — requires Java >=17 + tla2tools.jar
- XState state name alignment: compare QGSDQuorum.tla states with src/machines/qgsd-workflow.machine.ts
