---
phase: v0.21-05
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/generate-proposed-changes.cjs
  - bin/generate-proposed-changes.test.cjs
autonomous: true
requirements:
  - PLAN-01

must_haves:
  truths:
    - "Running generate-proposed-changes.cjs on a PLAN.md with must_haves: truths: produces a valid proposed-changes.tla file in the phase formal/ subdirectory"
    - "Each truth in the PLAN.md frontmatter appears as an INVARIANT or PROPERTY stub in proposed-changes.tla with its original text as a comment"
    - "Truths with safety keywords (never, always, threshold, count, max) are classified as INVARIANT; truths with liveness keywords (eventually, progress, future, after) are classified as PROPERTY"
    - "The generator also produces MCProposedChanges.cfg alongside the .tla file for TLC invocation"
    - "Running on a PLAN.md with no truths produces no output files and exits cleanly"
  artifacts:
    - path: "bin/generate-proposed-changes.cjs"
      provides: "TLA+ delta synthesis from PLAN.md frontmatter truths"
      exports: ["generateProposedChanges", "generateTlaCfg"]
    - path: "bin/generate-proposed-changes.test.cjs"
      provides: "Unit tests for PLAN-01"
      min_lines: 80
  key_links:
    - from: "bin/generate-proposed-changes.cjs"
      to: "bin/generate-phase-spec.cjs"
      via: "require — reuses parsePlanFrontmatter, classifyTruth"
      pattern: "require.*generate-phase-spec"
    - from: "bin/generate-proposed-changes.cjs"
      to: ".planning/phases/<phase>/formal/proposed-changes.tla"
      via: "fs.writeFileSync"
      pattern: "proposed-changes\\.tla"
---

<objective>
Create a TLA+ delta synthesizer that reads a PLAN.md file's `must_haves: truths:` block and generates a `proposed-changes.tla` scratch spec with INVARIANT/PROPERTY stubs, plus a `MCProposedChanges.cfg` config file for TLC.

Purpose: PLAN-01 requires that plans auto-synthesize TLA+ deltas. This is the foundation for the verification loop (PLAN-02) and quorum enrichment (PLAN-03).
Output: `bin/generate-proposed-changes.cjs` + tests
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.21-05-planning-integration/v0.21-05-RESEARCH.md
@bin/generate-phase-spec.cjs
@bin/generate-phase-spec.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate-proposed-changes.cjs — TLA+ delta synthesis from plan truths</name>
  <files>bin/generate-proposed-changes.cjs</files>
  <action>
Create `bin/generate-proposed-changes.cjs` that:

1. **Reuses** `parsePlanFrontmatter` and `classifyTruth` from `bin/generate-phase-spec.cjs` via `require()`. Do NOT duplicate these functions.

2. **CLI interface:**
   - Usage: `node bin/generate-proposed-changes.cjs <path-to-PLAN.md> [--dry-run]`
   - Accepts a single PLAN.md file path (NOT a directory — unlike generate-phase-spec.cjs which handles directories).
   - `--dry-run` prints output to stdout without writing files.

3. **`generateProposedChanges(planFilePath)`** function:
   - Reads the PLAN.md file content.
   - Calls `parsePlanFrontmatter(content)` to extract `phase` and `truths`.
   - If `truths` is empty, returns `{ generated: false, reason: 'no truths' }`.
   - Calls `classifyTruth(truth)` for each truth to determine INVARIANT vs PROPERTY.
   - Generates a TLA+ MODULE named `ProposedChanges` (fixed name for TLC config stability).
   - The generated spec must include:
     - Module header with phase, timestamp, source file path as comments.
     - `EXTENDS Naturals, FiniteSets, TLC`
     - Minimal state machine: `VARIABLES state`, `Init == state = "INIT"`, `Next == state' = state` (trivial model so TLC can enumerate; the INVARIANT/PROPERTY stubs default to TRUE).
     - `Spec == Init /\ [][Next]_<<state>>`
     - For each truth: `{INVARIANT|PROPERTY} ReqNN == TRUE \* "truth text here"` (stubbed to TRUE — these are placeholder properties).
   - Determines output directory: derive `phaseDir` from planFilePath (its parent directory), then target is `{phaseDir}/formal/proposed-changes.tla`.
   - Creates `{phaseDir}/formal/` directory if it doesn't exist.
   - Writes the .tla file.
   - Returns `{ generated: true, specPath, truthCount, classifications: [{truth, kind, propName}] }`.

4. **`generateTlaCfg(specPath)`** function:
   - Writes `MCProposedChanges.cfg` in the same directory as specPath.
   - Content: `SPECIFICATION Spec` followed by `INVARIANT ReqNN` for each INVARIANT and `PROPERTY ReqNN` for each PROPERTY (parsed from the .tla file by reading back the generated content and extracting INVARIANT/PROPERTY lines).
   - Returns `{ cfgPath }`.

5. **CLI entrypoint** (guarded by `require.main === module`):
   - Parse args, validate path exists.
   - Call `generateProposedChanges(path)`.
   - If not generated (no truths), print message to stderr and exit 0.
   - Call `generateTlaCfg(specPath)`.
   - Print summary: spec path, cfg path, truth count, classifications.

6. **Exports:** `module.exports = { generateProposedChanges, generateTlaCfg }`.

**Follow existing patterns from `generate-phase-spec.cjs`**: CommonJS, `'use strict'`, `#!/usr/bin/env node`, error handling via `process.stderr.write()`, 2-space indentation.

**Key difference from generate-phase-spec.cjs:** This writes to the phase's `formal/` subdirectory (scoped to planning), NOT to `formal/tla/scratch/` (which is the canonical scratch area). This ensures proposed-changes.tla is ephemeral and phase-scoped per RESEARCH.md Pitfall 3.
  </action>
  <verify>
node -e "const m = require('./bin/generate-proposed-changes.cjs'); console.log(Object.keys(m))"
# Should print: [ 'generateProposedChanges', 'generateTlaCfg' ]
  </verify>
  <done>
bin/generate-proposed-changes.cjs exists, exports generateProposedChanges and generateTlaCfg, requires generate-phase-spec.cjs for parsePlanFrontmatter/classifyTruth, and can be invoked as CLI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create generate-proposed-changes.test.cjs — TDD tests for PLAN-01</name>
  <files>bin/generate-proposed-changes.test.cjs</files>
  <action>
Create `bin/generate-proposed-changes.test.cjs` using `node:test` and `node:assert` (following existing patterns from `generate-phase-spec.test.cjs`).

Tests to implement:

1. **"generateProposedChanges returns generated: false for plan with no truths"**
   - Create tmp dir with a PLAN.md containing frontmatter but empty `truths: []`.
   - Call `generateProposedChanges(planPath)`.
   - Assert result.generated is false.

2. **"generateProposedChanges produces proposed-changes.tla with correct INVARIANT/PROPERTY stubs"**
   - Create tmp dir with a PLAN.md containing 3 truths: one safety ("count never exceeds 5"), one liveness ("eventually reaches DONE"), one default.
   - Call `generateProposedChanges(planPath)`.
   - Assert result.generated is true, result.truthCount is 3.
   - Read the generated .tla file and assert it contains `INVARIANT Req01`, `PROPERTY Req02`.
   - Assert the .tla file contains `MODULE ProposedChanges`.

3. **"generateProposedChanges writes to phaseDir/formal/ subdirectory"**
   - Create tmp dir mimicking `.planning/phases/v0.21-05-test/v0.21-05-01-PLAN.md`.
   - Call `generateProposedChanges(planPath)`.
   - Assert `{tmpDir}/.planning/phases/v0.21-05-test/formal/proposed-changes.tla` exists.

4. **"generateTlaCfg creates MCProposedChanges.cfg with correct SPECIFICATION and checks"**
   - Generate a proposed-changes.tla first (from test 2's setup).
   - Call `generateTlaCfg(specPath)`.
   - Read the .cfg file and assert it contains `SPECIFICATION Spec`.
   - Assert it contains `INVARIANT Req01` for the safety truth.
   - Assert it contains `PROPERTY Req02` for the liveness truth.

5. **"CLI dry-run mode prints to stdout without writing files"**
   - Use `execSync` to run `node bin/generate-proposed-changes.cjs <tmpPlanPath> --dry-run`.
   - Assert stdout contains "DRY-RUN" or spec content.
   - Assert no proposed-changes.tla file was created.

6. **"each truth appears as a comment in the generated TLA+ spec"**
   - Create a plan with distinctive truth strings.
   - Generate spec.
   - Assert each truth string appears in the .tla file content (as a TLA+ comment).

**Test infrastructure:** Use `beforeEach`/`afterEach` with `fs.mkdtempSync`/`fs.rmSync` for tmp directories. Follow the exact pattern from `generate-phase-spec.test.cjs`.
  </action>
  <verify>node --test bin/generate-proposed-changes.test.cjs</verify>
  <done>All 6 tests pass. generate-proposed-changes.cjs correctly synthesizes TLA+ deltas from PLAN.md frontmatter truths with proper INVARIANT/PROPERTY classification and MCProposedChanges.cfg generation.</done>
</task>

</tasks>

<verification>
1. `node --test bin/generate-proposed-changes.test.cjs` — all tests GREEN
2. `node -e "const m = require('./bin/generate-proposed-changes.cjs'); console.log(Object.keys(m))"` — exports present
3. `node bin/generate-proposed-changes.cjs --help` or no-args shows usage
</verification>

<success_criteria>
- bin/generate-proposed-changes.cjs exists and exports generateProposedChanges + generateTlaCfg
- bin/generate-proposed-changes.test.cjs exists with 6+ passing tests
- Generated proposed-changes.tla contains valid TLA+ MODULE with INVARIANT/PROPERTY stubs
- Generated MCProposedChanges.cfg contains SPECIFICATION + all checks
- Output is scoped to {phaseDir}/formal/ (NOT formal/tla/scratch/)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.21-05-planning-integration/v0.21-05-01-SUMMARY.md`
</output>
