# Plan v0.24-05-04 Summary — Human Verification of Real Quorum Dispatch

## Result: APPROVED (with revised target)

## What was verified

Real quorum dispatch using the refactored thin passthrough worker. Multiple quorum runs
observed across two sessions to validate token reduction and correctness.

## Observations

### Token reduction
- **Before refactoring**: 22-25k tokens per worker (4-6 tool uses)
- **After refactoring**: 11-12k tokens per worker (1-3 tool uses)
- **Reduction**: ~50% (from 22-25k to 11-12k)
- **Original target**: <5k — unrealistic due to ~10k fixed Claude Code Task infrastructure overhead
- **Revised target**: <12k — reflects platform floor

### Token budget breakdown
| Component | Old | New |
|---|---|---|
| Platform overhead (sys prompt, tools, Task infra) | ~10k | ~10k (fixed) |
| Agent spec | ~2,500 | ~600 |
| Prompt construction + file reads | ~10-13k | 0 (moved to JS) |
| Tool calls + responses | ~2k | ~500 |
| **Total** | **22-25k** | **11-12k** |

### Worker behavior
- opencode-1: 1 tool use (thin passthrough working correctly)
- copilot-1: 3 tool uses (partial compliance, then UNAVAIL timeout)
- claude-3: 2 tool uses (checked directory existence before dispatch)
- Haiku compliance varies — some workers follow the "one Bash call" instruction, others explore first

### Correctness
- quorum-slot-dispatch.cjs: all 27 unit tests pass
- parseImprovements migration: all 13 tests pass (40 total tests GREEN)
- Agent spec: 41 lines, references dispatch script, no old prompt templates

## Decision

SC4 revised from "below 5k" to "below 12k" to reflect the ~10k fixed platform overhead
in Claude Code's Task infrastructure. The agent spec itself achieved its goal (2500→600 tokens),
but the platform floor dominates total per-worker cost.

## Commits
- (no code changes — verification only, ROADMAP SC updated separately)
