---
phase: v0.34-04
slug: semantic-scoring-pipeline-wiring
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-11
---

# Phase v0.34-04 — Validation Strategy

> Template created by `/nf:plan-phase v0.34-04` (step 5.5) after research.
> Populated by `/nf:plan-phase v0.34-04` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/nf:execute-phase v0.34-04`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (node --test) |
| **Config file** | none (uses node --test directly) |
| **Quick run command** | `node --test bin/compute-per-model-gates.test.cjs --test-reporter=spec` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds |
| **CI pipeline** | .github/workflows/test.yml — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/compute-per-model-gates.test.cjs --test-reporter=spec`
- **After every plan wave:** Run `npm test`
- **Before `/nf:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.34-04-01-01 | 01 | 1 | SEM-03, SEM-04 | unit | `node --test bin/compute-per-model-gates.test.cjs --test-reporter=spec` | yes | pending |
| v0.34-04-01-02 | 01 | 1 | SEM-03, SEM-04 | smoke | `grep -n 'compute-semantic-scores' bin/nf-solve.cjs && node -e "require('./bin/nf-solve.cjs')" 2>&1 \| head -5` | N/A | pending |

*Status values: pending / green / red / flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

Existing test files:
- `bin/compute-per-model-gates.test.cjs` — covers gate writing logic (already exists from v0.34-02)
- `bin/compute-semantic-scores.test.cjs` — covers semantic score aggregation (already exists from v0.34-02)

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `nf-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-11

---

## Execution Tracking

Updated during `/nf:execute-phase v0.34-04`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | TBD | TBD | TBD | pending |

**Phase validation complete:** pending
