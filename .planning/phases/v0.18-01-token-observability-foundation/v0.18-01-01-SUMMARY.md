---
phase: v0.18-01
plan: "01"
status: complete
completed: 2026-02-27
---

# v0.18-01 Plan 01 Summary — Token Collection Hooks + CLI Sentinel

## What Was Built

**Task 1: Test scaffold files**
- `hooks/qgsd-token-collector.test.js` — 7 test cases using node:test + node:assert/strict
- `hooks/qgsd-slot-correlator.test.js` — 3 test cases

**Task 2: SubagentStop and SubagentStart hooks**
- `hooks/qgsd-token-collector.js` — SubagentStop hook. Guards on agent_type === 'qgsd-quorum-slot-worker'. Reads agent_transcript_path, sums input_tokens/output_tokens/cache tokens across non-sidechain, non-error assistant entries. Appends JSONL record to .planning/token-usage.jsonl. Null-token record written when transcript absent (fail-open).
- `hooks/qgsd-slot-correlator.js` — SubagentStart hook. Writes correlation stub `{ agent_id, ts, slot: null }` to .planning/quorum-slot-corr-<agent_id>.json. Slot is null at SubagentStart time; token collector resolves slot from last_assistant_message preamble ("slot: <name>") and deletes the correlation file.

**Task 3: call-quorum-slot.cjs + dist sync**
- Added `appendTokenSentinel(slotName)` function (null-token record with all fields null)
- 3 call sites in main(): success path, catch path, unknown-provider path
- `hooks/dist/qgsd-token-collector.js` and `hooks/dist/qgsd-slot-correlator.js` synced

## Verification Results

| Check | Result |
|-------|--------|
| `node --test hooks/qgsd-token-collector.test.js` | 7/7 pass |
| `node --test hooks/qgsd-slot-correlator.test.js` | 3/3 pass |
| OBSV-02 smoke test (slot: claude-1, input: 500, output: 50) | PASS |
| `grep -c appendTokenSentinel bin/call-quorum-slot.cjs` | 4 (def + 3 call sites) |
| `diff hooks/qgsd-token-collector.js hooks/dist/qgsd-token-collector.js` | identical |
| `diff hooks/qgsd-slot-correlator.js hooks/dist/qgsd-slot-correlator.js` | identical |

## Requirements Satisfied

- **OBSV-02**: SubagentStop hook reads agent_transcript_path, sums usage fields, appends correct record
- **OBSV-03**: Slot name resolved from last_assistant_message preamble "slot: <name>"; correlation file fallback path works
- **OBSV-04**: call-quorum-slot.cjs writes null-token sentinel after every CLI slot dispatch (success or failure)

## Commit

`363f4e7` — feat(v0.18-01): implement token collection hooks and CLI sentinel (Plan 01)
