---
phase: v0.10-05-policy-uis
verified: 2026-02-24T23:45:00Z
status: human_needed
score: 4/4 automated truths verified
human_verification:
  - test: "Tune timeouts flow end-to-end"
    expected: "Selecting choice 15 shows per-slot timeout input; entering a new ms value and confirming produces a yellow 'Timeout changes require a Claude Code restart to take effect.' message; cat bin/providers.json confirms the new quorum_timeout_ms value"
    why_human: "TTY-interactive inquirer prompts cannot be driven programmatically — restart-required note conditional on user input"
  - test: "Set update policy flow end-to-end"
    expected: "Selecting choice 16 shows slot picker then auto/prompt/skip list with '← current' annotation on the current value; saving writes agent_config[slot].update_policy to ~/.claude/qgsd.json"
    why_human: "TTY-interactive inquirer two-step prompt — slot selection then policy selection"
  - test: "Startup auto-update check for auto-policy slots"
    expected: "After setting one slot to update_policy=auto and restarting, a brief delay may appear then ~/.claude/qgsd-update.log gains a new timestamped NDJSON entry for that slot before the menu appears"
    why_human: "Requires live startup sequence with a real or mock update-agents.cjs getUpdateStatuses() — cannot drive without TTY"
  - test: "listAgents() warning banner with injected ERROR entry"
    expected: "After echo-ing a recent ERROR JSON line into ~/.claude/qgsd-update.log, selecting '1. List agents' shows a yellow warning banner naming the slot above the agent table"
    why_human: "Visual output verification of conditional console.log — requires TTY + log file state"
---

# Phase v0.10-05: Policy UIs Verification Report

**Phase Goal:** Users can configure quorum timeout and update policy per slot from the main menu, and slots set to auto are checked for updates on startup
**Verified:** 2026-02-24T23:45:00Z
**Status:** human_needed (all automated checks PASSED; 4 interactive flows require human TTY testing)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Derived from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | "Tune timeouts" in main menu; per-slot timeout input; restart-required note mandatory after save | ? NEEDS HUMAN (code wired) | `mainMenu()` line 2107: `{ name: '15. Tune timeouts', value: 'tune-timeouts' }`; dispatch line 2129; `tuneTimeouts()` line 1360-1363 writes providers.json then prints yellow restart note; code substantive |
| 2 | "Set update policy" in main menu; auto/prompt/skip per slot; persisted to `agent_config[slot].update_policy` in qgsd.json via `writeQgsdJson()` | ? NEEDS HUMAN (code wired) | `mainMenu()` line 2108; dispatch line 2130; `setUpdatePolicy()` calls `writeUpdatePolicy()` line 1405; `writeUpdatePolicy` at line 2290-2295 calls `writeQgsdJson`; path confirmed |
| 3 | `runAutoUpdateCheck()` runs on startup for auto-policy slots; writes timestamped NDJSON to `~/.claude/qgsd-update.log` | ? NEEDS HUMAN (code wired) | Entry point line 2142-2156: `async run()` IIFE; line 2146 `await runAutoUpdateCheck()` before `await mainMenu()` in TTY branch; `runAutoUpdateCheck()` lines 1416-1471: reads `agent_config`, filters `update_policy==='auto'` slots, calls `buildUpdateLogEntry()`, `fs.appendFileSync(UPDATE_LOG_PATH, ...)` |
| 4 | `listAgents()` shows yellow warning banner when log has recent ERROR entries | ? NEEDS HUMAN (code wired) | `listAgents()` lines 351-363: `fs.existsSync(UPDATE_LOG_PATH)` + `readFileSync` tail 500 lines + `parseUpdateLogErrors(logContent)` + conditional `console.log` yellow banner with `\u26a0` warning symbol |

All 4 truths have substantive, non-stub implementation. Human verification needed only for TTY-interactive confirmation that the output is visually correct and flows work end-to-end.

**Automated Score:** 4/4 truths have correct code wiring.
**Human Score:** 0/4 truths have been interactively confirmed in this run. (SUMMARY-03 notes human gate was approved by quorum-test PASS on 2026-02-24 — see Commits section.)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/manage-agents.cjs` | All integration functions + pure functions + `_pure` export + entry point | VERIFIED | File is 2545 lines; all required functions present and substantive |
| `bin/manage-agents.cjs` `UPDATE_LOG_PATH` | Constant pointing to `~/.claude/qgsd-update.log` | VERIFIED | Line 18: `const UPDATE_LOG_PATH = path.join(os.homedir(), '.claude', 'qgsd-update.log')` |
| `bin/manage-agents.cjs` `tuneTimeouts()` | Async function with per-slot inquirer prompt + `writeProvidersJson` + restart note | VERIFIED | Lines 1324-1367; calls `buildTimeoutChoices`, `applyTimeoutUpdate`, `writeProvidersJson`; yellow restart note at line 1363 is unconditional on `changed===true` |
| `bin/manage-agents.cjs` `setUpdatePolicy()` | Async function with slot picker + policy list + `writeUpdatePolicy` | VERIFIED | Lines 1374-1407; calls `buildPolicyChoices`, `writeUpdatePolicy` |
| `bin/manage-agents.cjs` `writeUpdatePolicy()` | Helper that calls `writeQgsdJson` to persist `agent_config[slot].update_policy` | VERIFIED | Lines 2290-2295; reads qgsd.json, mutates `agent_config[slotName].update_policy`, calls `writeQgsdJson` |
| `bin/manage-agents.cjs` `runAutoUpdateCheck()` | Fail-open startup hook with 20s Promise.race; writes NDJSON to UPDATE_LOG_PATH | VERIFIED | Lines 1416-1471; `Promise.race([check(), timeout])` at line 1469; `fs.appendFileSync(UPDATE_LOG_PATH, ...)` at lines 1438 and 1461 |
| `bin/manage-agents.cjs` `listAgents()` warning banner | PLCY-03 banner reading log, parsing errors, conditional yellow print | VERIFIED | Lines 351-363; calls `parseUpdateLogErrors`, prints yellow `\x1b[33m\u26a0 Auto-update errors...` |
| `bin/manage-agents.cjs` `buildTimeoutChoices` | Pure function exported via `_pure` | VERIFIED | Lines 2427-2440; in `_pure` at line 2540 |
| `bin/manage-agents.cjs` `applyTimeoutUpdate` | Pure function exported via `_pure` | VERIFIED | Lines 2451-2456; in `_pure` at line 2541 |
| `bin/manage-agents.cjs` `buildPolicyChoices` | Pure function with `← current` marker; exported via `_pure` | VERIFIED | Lines 2471-2478; ANSI `\x1b[90m\u2190 current\x1b[0m` annotation; in `_pure` at line 2542 |
| `bin/manage-agents.cjs` `buildUpdateLogEntry` | Pure function returning NDJSON string ending `\n`; exported via `_pure` | VERIFIED | Lines 2488-2495; `JSON.stringify({ts, slot, status, detail}) + '\n'`; in `_pure` at line 2543 |
| `bin/manage-agents.cjs` `parseUpdateLogErrors` | Pure function filtering ERROR entries within maxAgeMs window; exported via `_pure` | VERIFIED | Lines 2507-2516; default 24h window; in `_pure` at line 2544 |
| `bin/manage-agents.test.cjs` | 24 test stubs + implementation tests; 118 pass, 0 fail | VERIFIED | Test run confirmed: `pass 118`, `fail 0` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `mainMenu()` | `tuneTimeouts()` | `else if (action === 'tune-timeouts')` | WIRED | Line 2129 |
| `mainMenu()` | `setUpdatePolicy()` | `else if (action === 'update-policy')` | WIRED | Line 2130 |
| `mainMenu()` choices array | `'tune-timeouts'` and `'update-policy'` values | `{ name: '15. Tune timeouts', value: 'tune-timeouts' }` | WIRED | Lines 2107-2108 |
| Entry point `run()` | `runAutoUpdateCheck()` | `await runAutoUpdateCheck()` before `await mainMenu()` in TTY branch | WIRED | Lines 2143-2147 |
| `listAgents()` | `~/.claude/qgsd-update.log` via `parseUpdateLogErrors` | `fs.existsSync(UPDATE_LOG_PATH)` + `readFileSync` + `parseUpdateLogErrors(logContent)` | WIRED | Lines 352-363 |
| `tuneTimeouts()` | `bin/providers.json` | `readProvidersJson()` -> `applyTimeoutUpdate()` -> `writeProvidersJson()` | WIRED | Lines 1333, 1354, 1361 |
| `setUpdatePolicy()` | `~/.claude/qgsd.json` | `writeUpdatePolicy(slotName, policy)` -> `writeQgsdJson(qgsd, filePath)` | WIRED | Lines 1405, 2290-2295 |
| `runAutoUpdateCheck()` | `UPDATE_LOG_PATH` | `fs.appendFileSync(UPDATE_LOG_PATH, buildUpdateLogEntry(...))` | WIRED | Lines 1438, 1461 |
| `module.exports._pure` | All 5 new pure functions | Object spread with explicit names | WIRED | Lines 2540-2544 |

---

### Requirements Coverage

PLCY requirement IDs (PLCY-01, PLCY-02, PLCY-03) do not appear in `REQUIREMENTS.md` — that file tracks v0.12 milestone requirements (LOG-*, XST-*, VAL-*, TLA-*, etc.) only. The PLCY requirements are defined in ROADMAP.md success criteria for phase v0.10-05 and are cross-referenced here against those criteria.

| Requirement | Derived From | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PLCY-01 | ROADMAP success criterion 1 | "Tune timeouts" in main menu; shows current timeout per slot; restart-required note after save | SATISFIED (code) / NEEDS HUMAN (UI) | `tuneTimeouts()` wired to choice 15; `writeProvidersJson` called on change; restart note at line 1363 unconditionally shown when `changed===true` |
| PLCY-02 | ROADMAP success criterion 2 | "Set update policy" in main menu; auto/prompt/skip; persisted to `agent_config[slot].update_policy` in qgsd.json via `writeQgsdJson()` | SATISFIED (code) / NEEDS HUMAN (UI) | `setUpdatePolicy()` wired to choice 16; `writeUpdatePolicy` -> `writeQgsdJson` confirmed at lines 2290-2295 |
| PLCY-03 | ROADMAP success criteria 3+4 | `runAutoUpdateCheck()` runs on startup for auto-policy slots; NDJSON log; `listAgents()` warning banner on ERROR entries | SATISFIED (code) / NEEDS HUMAN (UI) | Entry point restructured; `runAutoUpdateCheck` called before `mainMenu`; log writes confirmed; `listAgents` banner wired |

No orphaned requirements found — all three PLCY IDs are claimed by all three plans and have corresponding implementation.

---

### Anti-Patterns Found

No blockers or stub patterns found.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| N/A | — | No TODO/FIXME/placeholder comments in modified code | None | — |
| N/A | — | No stub `return null`/`return {}` in integration functions | None | — |
| N/A | — | All handlers have substantive implementations (not just `e.preventDefault()` or `console.log`) | None | — |

---

### Test Suite Status

```
node --test bin/manage-agents.test.cjs
pass 118
fail 0
```

- 94 pre-existing tests: all pass (no regression)
- 24 TDD RED stubs (Plan 01): all GREEN after Plan 02 implementation
- Total: 118 pass, 0 fail

Note: Plan 01 SUMMARY documented 24 stubs vs the plan frontmatter's stated 22 — this is a documentation discrepancy only. The 118 total is correct (94 + 24 = 118).

---

### Human Verification Required

Plan 03 included a `checkpoint:human-verify` gate (Task 3). The SUMMARY-03 reports this gate was approved via quorum-test PASS consensus on 2026-02-24 (3 models: Gemini PASS, OpenCode PASS, Copilot PASS; Codex UNAVAILABLE). Commits `3e3e368` and `ca9cdda` are the implementation artifacts.

For completeness, the following human tests should be confirmed if this is a fresh verification pass:

#### 1. Tune Timeouts (PLCY-01)

**Test:** Run `node bin/manage-agents.cjs`; select "15. Tune timeouts"; enter a new ms value (e.g., 45000) for one slot; press Enter past others.
**Expected:** Yellow message "Timeout changes require a Claude Code restart to take effect." appears after saving. Running `python3 -c "import json; d=json.load(open('bin/providers.json')); [print(p.get('name'), p.get('quorum_timeout_ms')) for p in d.get('providers',[])]"` shows the new value.
**Why human:** Conditional on user entering a new value in TTY inquirer prompt — cannot automate without a TTY driver.

#### 2. Set Update Policy (PLCY-02)

**Test:** From main menu, select "16. Set update policy"; pick any slot; select "auto".
**Expected:** Green checkmark message "slot: update_policy = auto" appears. Running `python3 -c "import json,os; d=json.load(open(os.path.expanduser('~/.claude/qgsd.json'))); print(d.get('agent_config',{}))"` shows the slot's `update_policy = "auto"`.
**Why human:** Two-step TTY-interactive inquirer prompt; policy persistence requires live qgsd.json write.

#### 3. Startup Auto-Update Check (PLCY-03)

**Test:** With at least one slot having `update_policy=auto` (from test 2), exit and restart: `node bin/manage-agents.cjs`. Observe startup delay. Run `tail -5 ~/.claude/qgsd-update.log`.
**Expected:** New timestamped JSON entry for the auto slot appears in the log before the menu shows. Entry contains `ts`, `slot`, `status` fields.
**Why human:** Requires live startup sequence and real or mock `getUpdateStatuses()` response; log file state is ephemeral.

#### 4. listAgents Warning Banner (PLCY-03)

**Test:** Inject a recent ERROR entry: `echo '{"ts":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'","slot":"claude-1","status":"ERROR","detail":"test"}' >> ~/.claude/qgsd-update.log`. Then select "1. List agents" from main menu.
**Expected:** Yellow warning banner with `⚠ Auto-update errors in last 24h for: claude-1` appears above the agent table. Banner includes path to the log file.
**Why human:** Visual output verification; requires injected log state + TTY display.

---

### Commits Verified

The following commits are referenced in SUMMARY files and were in git log:

| Commit | Description | Plan |
|--------|-------------|------|
| `c8d560d` | TDD RED stubs (24 stubs added) | 01 |
| `758f72c` | `buildTimeoutChoices` + `applyTimeoutUpdate` implemented | 02 |
| `b77f259` | `buildPolicyChoices`, `buildUpdateLogEntry`, `parseUpdateLogErrors` implemented | 02 |
| `3e3e368` | `UPDATE_LOG_PATH`, `tuneTimeouts()`, `setUpdatePolicy()` added | 03 |
| `ca9cdda` | `runAutoUpdateCheck()`, `listAgents()` banner, mainMenu wiring, entry point | 03 |
| `2cdcc1f` | STATE.md updated at human-verify checkpoint | 03 |

---

## Summary

Phase v0.10-05 goal achievement is **structurally complete**. Every component required by the 4 success criteria is:

1. Implemented with substantive, non-stub code in `bin/manage-agents.cjs`
2. Wired end-to-end from entry point through main menu dispatch to pure functions and I/O
3. Tested with 118 passing unit tests, 0 failing
4. Committed with traceable commit hashes

The `human_needed` status reflects that 4 TTY-interactive flows cannot be verified programmatically without a terminal driver. The Plan 03 SUMMARY reports these were approved by a quorum-test human-verify gate on 2026-02-24. If this is a re-verification after that gate, the status is effectively **passed**.

---

_Verified: 2026-02-24T23:45:00Z_
_Verifier: Claude (qgsd-verifier)_
