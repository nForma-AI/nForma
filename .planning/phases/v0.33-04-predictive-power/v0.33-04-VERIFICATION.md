---
phase: v0.33-04
status: passed
verified_at: 2026-03-10
requirements: [PRED-01, PRED-02, TRACK-03]
formal_check: skipped
---

# Phase v0.33-04: Predictive Power — Verification

## Must-Haves Checklist

### PRED-01: Bug-to-property linking
- [x] `linkBugsToProperties()` reads debt.json and model-registry.json
- [x] Uses requirement ID overlap (formal_refs JOIN model requirements) as matching mechanism
- [x] Produces mapping with schema_version, total_bugs, total_linked, mappings array
- [x] `writeBugToProperty()` writes mapping to bug-to-property.json with timestamp
- [x] Entries without formal_refs appear as unlinked (predicted: false)
- [x] Fail-open for missing debt.json and model-registry.json

### PRED-02: Per-model recall scoring
- [x] `computePerModelRecall()` computes bugs_predicted / total_relevant_bugs per model
- [x] Uses gate_a pass from per-model-gates.json to determine "predicted"
- [x] Fail-open with empty object when per-model-gates.json missing
- [x] Models absent from per-model-gates.json get recall=0 (not skipped)
- [x] Recall is INFORMATIONAL ONLY — not wired as gate input

### TRACK-03: Convergence velocity estimation
- [x] `fitExponentialDecay()` implements linearized OLS on log-transformed data
- [x] Requires >= 10 data points (returns INSUFFICIENT_DATA below threshold)
- [x] Handles edge cases: zeros, -1 (fast-mode), stable, diverging, degenerate
- [x] `computeConvergenceVelocity()` reads JSONL via readTrendWindow, per-layer status
- [x] Returns all INSUFFICIENT_DATA when JSONL missing (first-run)
- [x] Sessions-to-convergence computed with threshold 0.5

### Integration
- [x] `updatePredictivePower()` orchestrates linking + recall + velocity in one call
- [x] nf-solve.cjs calls updatePredictivePower after updateVerdicts, before persistSessionSummary
- [x] Predictive power summary appears in solve report as informational output
- [x] No changes to convergence logic, exit codes, or gate evaluation

## Artifact Verification

| Artifact | Exists | Min Lines | Actual |
|----------|--------|-----------|--------|
| bin/predictive-power.cjs | Yes | 150 | 406 |
| bin/predictive-power.test.cjs | Yes | 60 | 493 |

## Key Links Verification

| From | To | Pattern | Verified |
|------|----|---------|----------|
| predictive-power.cjs | debt.json | debt_entries\|formal_refs | Yes |
| predictive-power.cjs | model-registry.json | model-registry\.json | Yes |
| predictive-power.cjs | bug-to-property.json | bug-to-property\.json | Yes |
| predictive-power.cjs | oscillation-detector.cjs | require.*oscillation-detector | Yes |
| nf-solve.cjs | predictive-power.cjs | predictive-power | Yes |

## Test Results

- 33 tests pass, 0 failures
- Test file registered in package.json test:ci
- npm run test:ci passes (no regressions)

## Formal Check

Formal scope scan: no modules matched this phase (no formal spec covers predictive power). Skipped.

## Score

3/3 requirements verified (PRED-01, PRED-02, TRACK-03)
