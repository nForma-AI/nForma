---
status: passed
phase: v0.38-03
verified: 2026-03-18
formal_check:
  passed: 0
  failed: 0
  skipped: 1
  note: "agent-loop module not registered in run-formal-check.cjs — skip-only, no counterexample"
---

# Phase v0.38-03: Model Refinement Workflow — Verification

## Goal

When a bug has no matching model or the model fails to reproduce, nForma can create or refine a model that captures the failure mode through a prescriptive 6-phase cycle.

## Success Criteria Verification

### 1. close-formal-gaps.md accepts `--bug-context` and biases spec generation

**Status:** PASSED

- `core/workflows/close-formal-gaps.md` Step 1 parses `--bug-context` flag with normalization via `bin/refinement-loop.cjs --normalize`
- Step 5 injects `<bug_context>` block into spec generation prompt for all formalisms
- `commands/nf/close-formal-gaps.md` argument-hint includes `--bug-context`
- Evidence: `grep -c 'bug-context' core/workflows/close-formal-gaps.md` = 5

### 2. Model refinement loop verifies FAILS and iterates up to 2 additional times

**Status:** PASSED

- `bin/refinement-loop.cjs` implements `verifyBugReproduction()` with inverted semantics
- Non-zero exit (checker found violation) = reproduced = success
- Zero exit (checker passed) = not captured = retry
- Default maxAttempts = 3 (initial + 2 retries)
- Fail-open on checker subprocess errors
- 11 unit tests in `test/refinement-loop.test.cjs` all pass

### 3. model-driven-fix.md orchestrates 6-phase prescriptive cycle

**Status:** PASSED

- `core/workflows/model-driven-fix.md` implements all 6 phases:
  1. Discovery (formal-scope-scan.cjs --bug-mode)
  2. Reproduction (--run-checkers)
  3. Refinement (close-formal-gaps --bug-context)
  4. Constraint extraction (model-constrained-fix.cjs)
  5. Constrained fix (presents constraints as guidance)
  6. Pre-verification (runs ONLY new/refined model)
- `commands/nf/model-driven-fix.md` command definition with --skip-fix, --verbose, --formalism flags
- 8 integration tests in `test/model-driven-fix-orchestrator.test.cjs` all pass

## Requirement Coverage

| ID | Description | Status |
|----|-------------|--------|
| MRF-01 | --bug-context flag biases spec generation | PASSED |
| MRF-02 | Refinement loop with inverted verification, up to 2 retries | PASSED |
| MRF-03 | 6-phase prescriptive cycle end-to-end | PASSED |

## Test Summary

| Test File | Tests | Pass | Fail |
|-----------|-------|------|------|
| test/bug-context-normalization.test.cjs | 9 | 9 | 0 |
| test/refinement-loop.test.cjs | 11 | 11 | 0 |
| test/model-driven-fix-orchestrator.test.cjs | 8 | 8 | 0 |
| **Total** | **28** | **28** | **0** |

## Formal Check

Formal scope scan matched module `agent-loop`. Module not registered in run-formal-check.cjs (skipped). No counterexamples found. The refinement loop's bounded iteration count (max 3) naturally satisfies the EventuallyTerminates liveness property.

## Verdict

**PASSED** — All 3 success criteria met, all 3 requirements covered, 28/28 tests pass.
