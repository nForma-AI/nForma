---
plan: v0.7-01-03
phase: v0.7-01-composition-architecture
status: complete
completed: 2026-02-23
requirements: [COMP-02]
---

## Summary

Wired `quorum_active` dynamically into all three consumer files: fixed INT-04 in `quorum.md` Mode B, added `$QUORUM_ACTIVE` read step to both `quorum.md` and orchestrator provider pre-flight, and implemented dynamic fallback generation in `qgsd-prompt.js`.

## What Was Built

- `commands/qgsd/quorum.md`:
  - INT-04 fix: line 433 updated from "strip the `claude-` prefix" to `--slot` + `--model-id` instruction matching Mode A
  - Added `$QUORUM_ACTIVE` read step to provider pre-flight section (bash node -e snippet reading from global + project qgsd.json)
- `agents/qgsd-quorum-orchestrator.md`:
  - Added `$QUORUM_ACTIVE` read step to provider pre-flight section
  - Replaced hardcoded native CLI call-order list with quorum_active-driven iteration block: "All quorum slots — driven by `$QUORUM_ACTIVE`"
- `hooks/qgsd-prompt.js`:
  - Added `SLOT_TOOL_SUFFIX` map and `slotToolCall()` helper for tool suffix derivation
  - Dynamic fallback step generation from `config.quorum_active` when present and `quorum_instructions` is absent
  - `DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK` retained for zero-config installs

## Key Files Created/Modified

- `commands/qgsd/quorum.md` — INT-04 fix + quorum_active pre-flight step
- `agents/qgsd-quorum-orchestrator.md` — quorum_active pre-flight + dynamic call order
- `hooks/qgsd-prompt.js` — dynamic fallback from quorum_active

## Verification

- `grep -n "strip the" commands/qgsd/quorum.md` → no output (INT-04 fixed)
- `grep -c "quorum_active" agents/qgsd-quorum-orchestrator.md` → 1
- `grep -c "quorum_active" hooks/qgsd-prompt.js` → 5
- `grep -c "DEFAULT_QUORUM_INSTRUCTIONS_FALLBACK" hooks/qgsd-prompt.js` → 2
- Smoke test with `quorum_active: ["codex-cli-1","gemini-cli-1","claude-1"]` → dynamic steps generated correctly
- npm test: 241/244 passing (no regressions)

## Self-Check: PASSED
