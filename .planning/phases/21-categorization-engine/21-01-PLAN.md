---
phase: 21-categorization-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/fix-tests.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md
autonomous: true
requirements: [CATG-01, CATG-02]

must_haves:
  truths:
    - "Each confirmed failure (status=failed, flaky=false) is classified into exactly one of: valid-skip, adapt, isolate, real-bug, or fixture"
    - "Failures with context_score < 2 are placed in deferred_tests, not auto-actioned"
    - "adapt-classified failures have a pickaxe_context field in their verdict (commits array may be empty but field must be present)"
    - "state.categorization_verdicts contains one entry per classified failure with file, category, confidence, context_score, reason, error_type, and pickaxe_context fields"
    - "Phase 20 stub state (categorization_verdicts empty but real_bug non-empty) is detected and cleared before re-classification"
  artifacts:
    - path: "get-shit-done/workflows/fix-tests.md"
      provides: "Step 6d: real categorization replacing stub"
      contains: "context_score"
    - path: "get-shit-done/workflows/fix-tests.md"
      provides: "git pickaxe enrichment for adapt"
      contains: "git log -S"
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md"
      provides: "Installed workflow with live categorization"
      contains: "context_score"
  key_links:
    - from: "fix-tests.md Step 6d"
      to: "maintain-tests-batch-result.json results[].status"
      via: "confirmed failure filter (status=failed AND flaky=false)"
      pattern: "status.*failed.*flaky.*false"
    - from: "fix-tests.md categorization"
      to: "state.categorization_verdicts"
      via: "save-state after each batch categorization"
      pattern: "categorization_verdicts"
    - from: "fix-tests.md adapt branch"
      to: "git log -S"
      via: "Bash spawnSync pickaxe after category verdict"
      pattern: "git.*log.*-S"
---

<objective>
Replace the Phase 20 stub categorization in fix-tests.md Step 6d with a real AI classification engine: context assembly (test file source + top-2 stack trace source files), inline Claude 5-category reasoning, context_score gating, and git pickaxe enrichment for adapt failures.

Purpose: CATG-01 and CATG-02 — the fix-tests loop must classify confirmed failures correctly so downstream dispatch (Plan 02) has accurate verdicts. Without real classification, the loop cannot distinguish bugs that need developer attention from environment issues that can be auto-fixed.

Output:
- get-shit-done/workflows/fix-tests.md — Step 6d replaced with full categorization engine (~120 lines added)
- /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md — identical installed copy (absolute paths already in file)
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-workflow-orchestrator/20-01-SUMMARY.md
@get-shit-done/workflows/fix-tests.md
@.planning/phases/21-categorization-engine/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend state schema and replace Step 6d in source workflow</name>
  <files>get-shit-done/workflows/fix-tests.md</files>
  <action>
Replace Step 5 initial state JSON to include the four new Phase 21 fields. The current JSON in Step 5 ends with `"deferred_tests":[]`. Extend it:

```json
{
  "schema_version": 1,
  "session_id": "<ISO timestamp>",
  "manifest_path": ".planning/maintain-tests-manifest.json",
  "total_tests": <total_files>,
  "batches_complete": 0,
  "batch_status": {},
  "processed_files": [],
  "results_by_category": {"valid_skip":[],"adapt":[],"isolate":[],"real_bug":[],"fixture":[]},
  "flaky_tests": [],
  "iteration_count": 0,
  "last_unresolved_count": <total_files>,
  "consecutive_no_progress": 0,
  "deferred_tests": [],
  "categorization_verdicts": [],
  "dispatched_tasks": [],
  "deferred_report": {"real_bug":[], "low_context":[]}
}
```

Replace the entire Step 6d block (currently "Stub-categorize results (Phase 20 placeholder — Phase 21 replaces this)" through the final "DO NOT dispatch" line) with the following full categorization engine:

---

### 6d. Categorize confirmed failures (Phase 21 — AI classification)

**Phase 20 stub detection:** Before processing, check if `categorization_verdicts` is empty AND any `results_by_category` array is non-empty. If true: this is Phase 20 stub state. Clear all category arrays and re-classify:
```bash
# Clear stub: reset results_by_category, categorization_verdicts, deferred_report in state
node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs maintain-tests save-state \
  --state-json '<current state with results_by_category all empty arrays, categorization_verdicts=[], dispatched_tasks=[], deferred_report={"real_bug":[],"low_context":[]}>'
```
Print: `QGSD fix-tests: Clearing Phase 20 stub state — re-classifying all failures`

**Sort batch results:**
- `passed_or_skipped` = results where status == "passed" OR status == "skipped"
- `flaky` = results where status == "flaky"
- `confirmed_failures` = results where status == "failed" AND flaky == false

Append `passed_or_skipped` files to `state.processed_files`.
Append `flaky` files to `state.flaky_tests`.

**Skip already-classified failures (resume safety):**
Before classifying a failure, check if `result.file` already appears in any `results_by_category` array OR in `categorization_verdicts[].file`. If found: skip classification, reuse existing verdict for dispatch grouping.

**Context assembly for each confirmed failure:**
For each failure in `confirmed_failures` in groups of 20 (to avoid context overflow):

  1. Read the test file:
     ```
     Read(result.file)  — truncate mentally at 4000 chars if very large
     ```

  2. Extract source file paths from `result.error_summary`:
     - Lines matching pattern: `at .* \((src|lib|app)/` or `File "(src|lib|app)/`
     - Take first 2 unique paths that are NOT node_modules and NOT the test file itself
     - Read each: `Read(stack_path_1)`, `Read(stack_path_2)` — skip if file does not exist

  3. Compute `context_score`:
     - +1 if test file source is non-empty
     - +1 if at least 1 stack trace source file was read successfully
     - +1 if `result.error_summary` is non-null and non-empty
     - Range: 0–3

  4. If `context_score < 2`: add to `state.deferred_tests` AND `state.deferred_report.low_context`. Do NOT classify. Continue to next failure.

**5-category classification (inline Claude reasoning for context_score >= 2):**

For the current group of up to 20 failures with their assembled context, produce a JSON verdict array. Use the following decision rules:

| Category | Classify when |
|----------|--------------|
| `valid-skip` | Test was already skipped/pending in test file source; tests a removed/deprecated feature; checks `process.env.CI` to skip itself |
| `adapt` | Failure caused by a real code change that mutated asserted behavior; error_summary shows assertion mismatch ("expected X got Y") clearly traceable to a code change; no environment dependency |
| `isolate` | Fails only due to environment/ordering dependency with no real code change; error shows missing env var, port conflict, race condition, or depends on another test's side effects |
| `real-bug` | Failure reveals a genuine defect requiring developer judgment; stack trace shows panic/crash/wrong logic not explainable by environment or code change |
| `fixture` | Fails because a fixture file, test data, snapshot, or generated mock is stale/missing/mismatched |

If uncertain: classify as `real-bug` (conservative — better to defer than to auto-action incorrectly).

Produce for each classified failure:
```json
{
  "file": "path/to/test.test.js",
  "category": "<one of the 5>",
  "confidence": "high|medium|low",
  "context_score": <0-3>,
  "reason": "<one sentence explaining classification>",
  "error_type": "<assertion_mismatch|import_error|snapshot_mismatch|fixture_missing|env_missing|port_conflict|timeout|unknown>",
  "pickaxe_context": null
}
```

Append each verdict to `state.categorization_verdicts`.
Append each file to the matching `state.results_by_category.<category>` array.

**Git pickaxe enrichment for adapt failures (CATG-02):**
After producing verdicts for a group, for each verdict where `category == "adapt"`:

  1. Extract the primary identifier from the test source:
     - Try regex: `describe\(['"](\w[\w\s]+)['"]` → first match
     - Fallback: primary import name from `import .* from ['"](.+)['"]` → last path segment
     - Keep identifier <= 60 chars; strip quotes

  2. Run pickaxe (scoped search first):
     ```bash
     PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
     git -C "$PROJECT_ROOT" log -S"<identifier>" --oneline --diff-filter=M -- src/ lib/ app/ 2>/dev/null | head -10
     ```

  3. If empty, run broader fallback:
     ```bash
     git -C "$PROJECT_ROOT" log -S"<identifier>" --oneline -10 2>/dev/null
     ```

  4. Set `pickaxe_context`:
     ```json
     {
       "identifier": "<identifier>",
       "commits": ["<hash> <message>", ...],
       "command_run": "git log -S\"<identifier>\" --oneline -10"
     }
     ```
     If git is unavailable or project is not a git repo: set `pickaxe_context = null` — still categorize as adapt.
     If no commits returned: set `commits = []` — still dispatch as adapt (pickaxe is enhancement, not gating).

  5. Update the verdict in `state.categorization_verdicts` with the `pickaxe_context` value.

**Update state after each group of 20:**
```bash
node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs maintain-tests save-state \
  --state-json '<state with updated results_by_category, categorization_verdicts, deferred_tests, deferred_report>'
```

**Note on dispatch:** Adapt/fixture/isolate dispatch happens in Step 6h (added by Plan 02). This step (6d) only produces verdicts and updates state.

---

Also update the Step 9 terminal summary to remove the Phase 20 stub disclaimer and replace with real category counts:

Replace the `Results:` block and stub note in Step 9 with:
```
 Results by category:
   valid-skip:  {len(valid_skip)}
   adapt:       {len(adapt)}
   isolate:     {len(isolate)}
   real-bug:    {len(real_bug)}   ← deferred (see report below)
   fixture:     {len(fixture)}
   flaky:       {len(flaky_tests)}
   deferred:    {len(deferred_tests)}   ← context_score < 2
   Passed/skipped: {len(processed_files)}
   Dispatched tasks: {len(dispatched_tasks)}
```

Remove: `Note: Phase 20 stub — all failures marked as real_bug.` and `Run Phase 21 categorization to replace with accurate classification.`
  </action>
  <verify>
    1. `grep -n "context_score" get-shit-done/workflows/fix-tests.md` — shows context_score appears in 6d section
    2. `grep -n "git log -S" get-shit-done/workflows/fix-tests.md` — shows pickaxe step present
    3. `grep -n "Phase 20 placeholder" get-shit-done/workflows/fix-tests.md` — returns nothing (stub removed)
    4. `grep -n "categorization_verdicts" get-shit-done/workflows/fix-tests.md` — shows multiple occurrences (state schema + save step)
    5. `grep -n "deferred_report" get-shit-done/workflows/fix-tests.md` — shows field in Step 5 JSON and 6d update
    6. `grep -c "valid-skip\|adapt\|isolate\|real-bug\|fixture" get-shit-done/workflows/fix-tests.md` — returns >= 5 (all 5 categories present)
  </verify>
  <done>Step 6d is fully replaced: context assembly, 5-category inline classification, git pickaxe for adapt, state save with categorization_verdicts and deferred_report. Step 5 state JSON includes all 4 new fields. Phase 20 stub text is gone. Step 9 summary shows real category counts.</done>
</task>

<task type="auto">
  <name>Task 2: Write installed workflow copy (absolute paths)</name>
  <files>/Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md</files>
  <action>
Write the installed copy at `/Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` with the same categorization engine as the source file.

IMPORTANT: Per Phase 20 decision, source and installed copies are written INDEPENDENTLY (not via cp). The installed copy already uses absolute paths for gsd-tools.cjs (`/Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs`) — keep those absolute paths unchanged. Do not introduce tilde paths.

The installed file is identical in content to the source file EXCEPT:
- gsd-tools.cjs path remains `/Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs` (already absolute in installed copy — keep as-is)
- No other path differences; the categorization engine text is identical

Read the source file first, then write the installed copy, replacing any gsd-tools.cjs references with the installed absolute path if they differ.
  </action>
  <verify>
    1. `grep -n "context_score" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — shows context_score in installed copy
    2. `grep -n "git log -S" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — shows pickaxe step
    3. `grep "Phase 20 placeholder" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — returns nothing
    4. `diff <(grep -c "context_score" get-shit-done/workflows/fix-tests.md) <(grep -c "context_score" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md)` — same count
  </verify>
  <done>Installed workflow at /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md is an exact functional copy of the source with categorization engine. No stub text remains. Installed copy uses absolute gsd-tools.cjs path throughout.</done>
</task>

</tasks>

<verification>
1. `grep -n "Phase 20 placeholder" get-shit-done/workflows/fix-tests.md` → no output (stub removed)
2. `grep -c "context_score" get-shit-done/workflows/fix-tests.md` → >= 3 occurrences
3. `grep -c "git log -S" get-shit-done/workflows/fix-tests.md` → >= 1 occurrence
4. `grep "categorization_verdicts" get-shit-done/workflows/fix-tests.md` → present in Step 5 JSON, Step 6d save-state, and Step 9 summary
5. Source file line count > 242 (was 242 pre-Phase 21) — new categorization engine adds ~120 lines
6. Installed and source files both pass the above grep checks
</verification>

<success_criteria>
- Step 6d is fully replaced with 5-category AI classification
- Context assembly: test source + top-2 stack trace files + context_score computed per failure
- context_score < 2 → deferred_tests + deferred_report.low_context (never classified)
- adapt verdicts include pickaxe_context (field present even if commits=[])
- categorization_verdicts in state JSON has one entry per classified failure
- Phase 20 stub detection logic is present and clears stale real_bug entries
- All 5 categories appear in the taxonomy decision table in the workflow
- Both source and installed workflow files updated
</success_criteria>

<output>
After completion, create `.planning/phases/21-categorization-engine/21-01-SUMMARY.md`
</output>
