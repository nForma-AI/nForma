---
phase: 21-categorization-engine
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - get-shit-done/workflows/fix-tests.md
  - /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md
autonomous: true
requirements: [CATG-03]

must_haves:
  truths:
    - "adapt, fixture, and isolate failures are grouped by category+error_type+directory_prefix and dispatched as /qgsd:quick Task agents"
    - "Each dispatch batch contains at most 20 test files; groups > 20 are chunked with batch suffix in description"
    - "real-bug failures appear only in the deferred user report printed at Step 9, never dispatched"
    - "dispatched_tasks in state JSON records every dispatched Task with category, error_type, directory, test_count, test_files, and dispatched_at"
    - "State is saved with dispatched_tasks record BEFORE spawning each Task agent"
    - "Terminal condition is evaluated BEFORE dispatch — terminal-triggering batch does not dispatch further tasks"
    - "Step 9 terminal summary prints the deferred report (real-bug + low-context) in a formatted block"
  artifacts:
    - path: "get-shit-done/workflows/fix-tests.md"
      provides: "Step 6h: grouping + dispatch + deferred accumulation"
      contains: "dispatched_tasks"
    - path: "get-shit-done/workflows/fix-tests.md"
      provides: "Step 9 deferred report block"
      contains: "deferred_report"
    - path: "/Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md"
      provides: "Installed workflow with dispatch logic"
      contains: "dispatched_tasks"
  key_links:
    - from: "fix-tests.md Step 6h"
      to: "state.categorization_verdicts"
      via: "read verdicts for current batch, filter actionable categories"
      pattern: "categorization_verdicts"
    - from: "fix-tests.md Step 6h"
      to: "Task() spawn"
      via: "qgsd-planner subagent with quick planning_context"
      pattern: "subagent_type.*qgsd-planner"
    - from: "fix-tests.md Step 6h dispatch"
      to: "state.dispatched_tasks"
      via: "save-state after each task dispatch record"
      pattern: "dispatched_tasks"
    - from: "fix-tests.md Step 9"
      to: "state.deferred_report"
      via: "print formatted report of real_bug + low_context at terminal summary"
      pattern: "deferred_report"
---

<objective>
Add Step 6h to the fix-tests workflow: group actionable categorization verdicts (adapt, fixture, isolate) by category+error_type+directory_prefix, chunk into batches of max 20, spawn /qgsd:quick Task agents for each chunk, and record dispatched_tasks in state. Add deferred user report to the Step 9 terminal summary for real-bug and low-context failures.

Purpose: CATG-03 — the categorization engine produces verdicts (Plan 01), but without dispatch they accumulate in state with no action. This plan closes the loop: actionable failures get auto-fixed via quick tasks; real bugs get surfaced to the user at the end.

Output:
- get-shit-done/workflows/fix-tests.md — Step 6h added after 6g; Step 9 deferred report added
- /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md — installed copy updated identically
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-categorization-engine/21-01-SUMMARY.md
@get-shit-done/workflows/fix-tests.md
@.planning/phases/21-categorization-engine/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 6h grouping and dispatch logic to source workflow</name>
  <files>get-shit-done/workflows/fix-tests.md</files>
  <action>
After Step 6g (termination condition check), insert a new Step 6h block. It must come AFTER the termination check — if a terminal condition fired, skip dispatch for this batch.

Insert between Step 6g and the loop continuation logic:

---

### 6h. Group and dispatch actionable failures (CATG-03)

**Skip if terminal condition fired in 6g.** Only dispatch if the loop will continue after this batch.

**Collect new verdicts from this batch:**
Identify all entries in `state.categorization_verdicts` that correspond to files in the current batch's `confirmed_failures` AND have not yet been recorded in `state.dispatched_tasks[].test_files`. These are the new verdicts eligible for dispatch.

**Filter actionable categories:**
- Actionable: `adapt`, `fixture`, `isolate`
- Not actionable: `real-bug` (→ `state.deferred_report.real_bug`), `deferred` / context_score < 2 (already in `state.deferred_report.low_context`)

For each new verdict with category == "real-bug": append `result.file` to `state.deferred_report.real_bug` (if not already present).

**Grouping algorithm:**
Group actionable verdicts by composite key:
```
group_key = category + "_" + error_type + "_" + directory_prefix
```
Where `directory_prefix` = first 2 path segments of `verdict.file`
(e.g., file "src/auth/user.test.js" → directory_prefix = "src/auth")

For each group, collect all verdict files with that group_key.

**Chunking (max 20 per task):**
For each group, split files into chunks of at most 20.
If a group has N chunks (N > 1), number them: batch 1/N, batch 2/N, etc.

**Deduplication check:**
Before dispatching a chunk, check `state.dispatched_tasks` for an entry with the same `category`, `error_type`, `directory`, and `test_files` list. If found: skip (already dispatched, likely a resume).

**For each chunk: save state record THEN dispatch Task:**

1. Build task description:
```
Fix {count} {category} test failures in {directory_prefix} — {error_type}{chunk_suffix}

Test files:
- {file1} (reason: {verdict1.reason})
- {file2} (reason: {verdict2.reason})
[...up to 20 files]

Category: {category}
Error pattern: {error_type}
{if category == "adapt" AND pickaxe_context.commits is non-empty:
"Git context (recent commits touching code under test):
{pickaxe_context.commits[0]}
{pickaxe_context.commits[1] if exists}"}
```
Where `{chunk_suffix}` = "" if only 1 chunk, or " (batch {n}/{total})" if multiple chunks.

2. Build dispatched_task record:
```json
{
  "task_id": "<ISO timestamp + chunk index>",
  "category": "<category>",
  "error_type": "<error_type>",
  "directory": "<directory_prefix>",
  "test_count": <chunk file count>,
  "test_files": ["<file1>", "<file2>", ...],
  "dispatched_at": "<ISO timestamp>"
}
```

3. Save state with this record appended to `dispatched_tasks` BEFORE spawning the Task:
```bash
node /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs maintain-tests save-state \
  --state-json '<state with new dispatched_task appended>'
```

4. Spawn the /qgsd:quick Task agent:
```
Task(
  prompt="First, read ~/.claude/agents/qgsd-planner.md for your role and instructions.

<planning_context>
Mode: quick
Description: {task_description}

<files_to_read>
- .planning/STATE.md
- ./CLAUDE.md (if exists)
</files_to_read>
</planning_context>
",
  subagent_type="qgsd-planner",
  description="Fix {category}/{error_type}: {directory_prefix}{chunk_suffix}"
)
```

Print after each dispatch: `QGSD fix-tests: Dispatched task — {category}/{error_type} in {directory_prefix} ({test_count} tests)`

**Print dispatch summary after all chunks dispatched:**
```
QGSD fix-tests: Batch {B+1} dispatch complete — {N} tasks dispatched, {M} real-bug failures deferred
```

---

Also add Step 6h note to the loop continuation comment: "After 6h, proceed to next batch or loop termination."
  </action>
  <verify>
    1. `grep -n "Step 6h" get-shit-done/workflows/fix-tests.md` — shows Step 6h is present
    2. `grep -n "dispatched_tasks" get-shit-done/workflows/fix-tests.md` — shows in state schema (Step 5), 6h save-state, and Step 9
    3. `grep -n "group_key" get-shit-done/workflows/fix-tests.md` — shows grouping algorithm present
    4. `grep -n "subagent_type" get-shit-done/workflows/fix-tests.md` — shows Task spawn pattern
    5. `grep -n "chunk_suffix\|batch 1/" get-shit-done/workflows/fix-tests.md` — shows chunking with batch suffix
    6. `grep -n "deferred_report.real_bug" get-shit-done/workflows/fix-tests.md` — shows real-bug accumulation
    7. `grep -n "Skip if terminal" get-shit-done/workflows/fix-tests.md` — shows terminal condition gate before dispatch
  </verify>
  <done>Step 6h is present after Step 6g in fix-tests.md. Grouping by category+error_type+directory_prefix is defined. Chunking at max 20 with batch suffix is specified. State saved before Task spawn. real-bug goes to deferred_report only. Terminal condition gates dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Add deferred report to Step 9 and update installed copy</name>
  <files>
    get-shit-done/workflows/fix-tests.md
    /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md
  </files>
  <action>
**Part A — Update Step 9 terminal summary in source file:**

After the existing `Results by category:` block in Step 9 (added by Plan 01), append the deferred user report block:

```
{if state.deferred_report.real_bug is non-empty OR state.deferred_report.low_context is non-empty:}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 QGSD ► FIX-TESTS: Deferred Failures (Action Required)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Real bugs (requires developer judgment):
{for each file in state.deferred_report.real_bug:}
  - {file} — {matching verdict.reason from categorization_verdicts}

Low context (could not classify reliably):
{for each file in state.deferred_report.low_context:}
  - {file} — context_score: {matching verdict.context_score or "<1"}

These failures were NOT auto-actioned. Review manually.
Full details: .planning/maintain-tests-state.json → deferred_report

{if state.deferred_report.real_bug is empty AND state.deferred_report.low_context is empty:}
 All failures were classified and dispatched.
```

Also update the `State saved to:` line in Step 9 to read:
```
 State saved to:  .planning/maintain-tests-state.json
 Dispatched:      {len(dispatched_tasks)} quick tasks
```

**Part B — Write installed copy:**

Write the full updated fix-tests.md (with both Plan 01 categorization and Plan 02 dispatch changes) to `/Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md`.

Per Phase 20 pattern: write independently, not via cp. The installed copy uses absolute paths for gsd-tools.cjs (`/Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs`) — keep all absolute paths unchanged. All other content is identical to the source file.
  </action>
  <verify>
    1. `grep -n "Deferred Failures" get-shit-done/workflows/fix-tests.md` — shows deferred report block in Step 9
    2. `grep -n "deferred_report.real_bug" get-shit-done/workflows/fix-tests.md` — appears in Step 6h (accumulation) and Step 9 (report)
    3. `grep -n "Deferred Failures" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — installed copy has deferred report
    4. `grep -n "Step 6h" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — dispatch logic in installed copy
    5. `grep -c "dispatched_tasks" get-shit-done/workflows/fix-tests.md` — source has >= 3 occurrences
    6. `grep -c "dispatched_tasks" /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md` — installed has same count
    7. Line count of installed file >= line count of source file (installed is functionally identical)
  </verify>
  <done>Step 9 terminal summary shows the deferred report block with real-bug and low-context lists. Installed workflow at /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md is a complete functional copy including Steps 6d, 6h, and updated Step 9. Both files have identical categorization and dispatch behavior.</done>
</task>

</tasks>

<verification>
1. `grep -n "Step 6h" get-shit-done/workflows/fix-tests.md` → present after Step 6g
2. `grep "Terminal condition" get-shit-done/workflows/fix-tests.md | head -5` → shows terminal gate in 6g AND dispatch skip in 6h
3. `grep "subagent_type.*qgsd-planner" get-shit-done/workflows/fix-tests.md` → Task spawn pattern present
4. `grep "batch 1/" get-shit-done/workflows/fix-tests.md` → chunk suffix pattern present
5. `grep "Deferred Failures" get-shit-done/workflows/fix-tests.md` → Step 9 deferred report present
6. `grep "real_bug\|low_context" get-shit-done/workflows/fix-tests.md | wc -l` → >= 4 occurrences (6h accumulation + Step 9 report)
7. Source line count: `wc -l get-shit-done/workflows/fix-tests.md` → substantially > 242 (Phase 21 adds 180+ lines)
8. Installed copy line count matches source: `wc -l /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md`
</verification>

<success_criteria>
- Step 6h present and comes after Step 6g (terminal condition check gates dispatch)
- Grouping key = category + "_" + error_type + "_" + directory_prefix
- Max 20 tests per Task; chunks numbered when group > 20
- State saved with dispatched_task record BEFORE Task spawn
- Task prompt follows qgsd-planner quick planning_context format
- real-bug failures NOT dispatched — accumulated in deferred_report.real_bug only
- Step 9 prints deferred report block when deferred_report is non-empty
- Installed workflow at /Users/jonathanborduas/.claude/qgsd/workflows/fix-tests.md is complete and functional
- INTG-03 compliance maintained: no quorum workers called anywhere in the workflow
</success_criteria>

<output>
After completion, create `.planning/phases/21-categorization-engine/21-02-SUMMARY.md`
</output>
