# Requirements Coverage

> Auto-generated by qgsd:solve R->D remediation.
> 243 requirements documented.

## Agent Behavior

### AGT-01: Subagents spawned by QGSD skills MUST loop autonomously until a terminal conditi

**Requirement:** Subagents spawned by QGSD skills MUST loop autonomously until a terminal condition is reached. Valid terminal conditions: (1) success — the goal is verifiably met, (2) iteration cap exhausted (default max 5), (3) unrecoverable — all available paths are low-confidence or unavailable. Returning a message instructing the user to "run again" or "re-invoke" instead of looping is a violation.

**Formal models:** .planning/formal/tla/QGSDAgentLoop.tla

**Status:** Pending

## Architecture

### ARCH-10: QGSD, as a Claude Code plugin, MUST NOT bundle LLM SDKs; Haiku/Sonnet/Opus calls

**Requirement:** QGSD, as a Claude Code plugin, MUST NOT bundle LLM SDKs; Haiku/Sonnet/Opus calls MUST use the Agent tool's model parameter

**Formal models:** .planning/formal/alloy/codebase-arch-constraints.als

**Status:** Pending

## CI/CD

### CI-01: Automated test suite runs on every pull request and merge to main is blocked whe

**Requirement:** Automated test suite runs on every pull request and merge to main is blocked when tests fail

**Formal models:** .planning/formal/alloy/ci-pipeline-gates.als

**Status:** Pending

### CI-02: Linting and formatting checks run in CI and block merge on violations

**Requirement:** Linting and formatting checks run in CI and block merge on violations

**Formal models:** .planning/formal/alloy/ci-pipeline-gates.als

**Status:** Pending

### CI-03: Type checking (if applicable to the language) runs in CI and blocks merge on typ

**Requirement:** Type checking (if applicable to the language) runs in CI and blocks merge on type errors

**Formal models:** .planning/formal/alloy/ci-pipeline-gates.als

**Status:** Pending

### CI-04: Full-suite test fallback enumerates files via find (never raw globs), enforces -

**Requirement:** Full-suite test fallback enumerates files via find (never raw globs), enforces --test-timeout=15000 per file and a 5-minute Bash timeout, and treats timeout as pass-with-warning when task-specific tests already passed.

**Formal models:** .planning/formal/alloy/ci-test-fallback.als

**Status:** Complete

## Configuration

### COMP-01: User can define a `quorum.active` array in `qgsd.json` listing which slots parti

**Requirement:** User can define a `quorum.active` array in `qgsd.json` listing which slots participate in quorum

**Formal models:** .planning/formal/alloy/quorum-composition.als

**Status:** Complete

### COMP-02: Quorum orchestrator reads `quorum.active` from config instead of a hardcoded age

**Requirement:** Quorum orchestrator reads `quorum.active` from config instead of a hardcoded agent list; only active slots are called

**Formal models:** .planning/formal/alloy/quorum-policy.als

**Status:** Complete

### COMP-03: `check-provider-health.chs` and scoreboard tooling derive agent list from `quoru

**Requirement:** `check-provider-health.chs` and scoreboard tooling derive agent list from `quorum.active` rather than hardcoded arrays

**Formal models:** .planning/formal/alloy/quorum-policy.als

**Status:** Complete

### COMP-04: Default `quorum.active` is auto-populated at install/migration time based on dis

**Requirement:** Default `quorum.active` is auto-populated at install/migration time based on discovered slots in `~/.claude.json`

**Formal models:** .planning/formal/alloy/quorum-policy.als

**Status:** Complete

### CONF-01: Global config at `~/.claude/qgsd.json` — installed once, applies to all projects

**Requirement:** Global config at `~/.claude/qgsd.json` — installed once, applies to all projects

**Status:** Complete

### CONF-02: Per-project override at `.claude/nf.json` — merged with global, project values

**Requirement:** Per-project override at `.claude/nf.json` — merged with global, project values take precedence

**Status:** Complete

### CONF-03: Config contains: `quorum_commands` (array of command names), `required_models` (

**Requirement:** Config contains: `quorum_commands` (array of command names), `required_models` (dict of MCP tool entries: { tool_prefix, required }), `fail_mode` (open|closed, default: open)

**Status:** Complete

### CONF-04: Fail-open behavior: when a quorum model is unavailable, Stop hook passes and log

**Requirement:** Fail-open behavior: when a quorum model is unavailable, Stop hook passes and logs reduced quorum notification

**Status:** Complete

### CONF-05: Config validates on read — malformed config falls back to hardcoded defaults wit

**Requirement:** Config validates on read — malformed config falls back to hardcoded defaults with warning

**Status:** Complete

### CONF-06: qgsd.json schema extended with `circuit_breaker.oscillation_depth` (integer, def

**Requirement:** qgsd.json schema extended with `circuit_breaker.oscillation_depth` (integer, default: 3) — minimum commits touching same file set to trigger breaker

**Formal models:** .planning/formal/alloy/config-two-layer.als

**Status:** Complete

### CONF-07: qgsd.json schema extended with `circuit_breaker.commit_window` (integer, default

**Requirement:** qgsd.json schema extended with `circuit_breaker.commit_window` (integer, default: 6) — number of recent commits to analyze

**Formal models:** .planning/formal/alloy/config-two-layer.als

**Status:** Complete

### CONF-08: Circuit breaker config values validated on load; invalid values fall back to def

**Requirement:** Circuit breaker config values validated on load; invalid values fall back to defaults with stderr warning

**Status:** Complete

### CONF-09: Two-layer config merge (global + project) applies to `circuit_breaker` settings 

**Requirement:** Two-layer config merge (global + project) applies to `circuit_breaker` settings identically to existing merge behavior

**Formal models:** .planning/formal/alloy/config-two-layer.als

**Status:** Complete

### CONF-10: Configuration layer validates and gracefully degrades when providers.json contai

**Requirement:** Configuration layer validates and gracefully degrades when providers.json contains zero providers, falling back to solo quorum mode with clear diagnostic messages

**Formal models:** .planning/formal/alloy/config-zero-providers.als

**Status:** Complete

### CONF-11: Global polyrepo registry at ~/.claude/polyrepos/<name>.json and per-repo marker 

**Requirement:** Global polyrepo registry at ~/.claude/polyrepos/<name>.json and per-repo marker at .planning/polyrepo.json enable named groups of related repositories with cross-repo awareness support

**Formal models:** .planning/formal/alloy/polyrepo-config.als

**Status:** Complete

### CONF-12: Per-repo marker at .planning/polyrepo.json supports an optional `docs` field wit

**Requirement:** Per-repo marker at .planning/polyrepo.json supports an optional `docs` field with merge-semantic key-value pairs (user, developer, examples, or custom) identifying documentation and example locations relative to the repo root

**Formal models:** .planning/formal/alloy/polyrepo-config.als

**Status:** Complete

### KEY-01: User can set or update the API key for any agent through the wizard

**Requirement:** User can set or update the API key for any agent through the wizard

**Formal models:** .planning/formal/tla/QGSDKeyManagement.tla

**Status:** Complete

### KEY-02: Key is stored securely via keytar (bin/secrets.cjs)

**Requirement:** Key is stored securely via keytar (bin/secrets.cjs)

**Formal models:** .planning/formal/tla/QGSDKeyManagement.tla

**Status:** Complete

### KEY-03: Wizard writes key from keytar to `~/.claude.json` mcpServers env block during ap

**Requirement:** Wizard writes key from keytar to `~/.claude.json` mcpServers env block during apply

**Formal models:** .planning/formal/tla/QGSDKeyManagement.tla

**Status:** Complete

### KEY-04: Wizard automatically restarts the agent after key changes take effect

**Requirement:** Wizard automatically restarts the agent after key changes take effect

**Formal models:** .planning/formal/tla/QGSDKeyManagement.tla

**Status:** Complete

### PLCY-01: User can set quorum timeout (ms) per slot from a dedicated menu shortcut — not b

**Requirement:** User can set quorum timeout (ms) per slot from a dedicated menu shortcut — not buried inside editAgent

**Status:** Pending

### PLCY-02: User can configure update policy per slot: auto / prompt / skip

**Requirement:** User can configure update policy per slot: auto / prompt / skip

**Status:** Pending

## Formal Verification

### ANNOT-01: All 11 TLA+ model files contain `@requirement` structured comments on each prope

**Requirement:** All 11 TLA+ model files contain `@requirement` structured comments on each property/invariant, mapping it to specific requirement IDs

**Status:** Complete

### ANNOT-02: All 8 Alloy model files contain `@requirement` structured comments on each asser

**Requirement:** All 8 Alloy model files contain `@requirement` structured comments on each assertion/check, mapping it to specific requirement IDs

**Status:** Complete

### ANNOT-03: All 3 PRISM .props files contain `@requirement` structured comments on each prop

**Requirement:** All 3 PRISM .props files contain `@requirement` structured comments on each property, mapping it to specific requirement IDs

**Status:** Complete

### ANNOT-04: `bin/extract-annotations.cjs` parses `@requirement` comments from TLA+, Alloy, a

**Requirement:** `bin/extract-annotations.cjs` parses `@requirement` comments from TLA+, Alloy, and PRISM files and returns a structured JSON map of `{ model_file: [{ property, requirement_ids }] }`

**Status:** Complete

### ANNOT-05: The traceability matrix generator reads extracted annotations as a primary data 

**Requirement:** The traceability matrix generator reads extracted annotations as a primary data source, with model-registry `requirements` arrays as fallback

**Status:** Complete

### EVID-01: `never_observed` path entries in `validate-traces.cjs` evidence output carry `co

**Requirement:** `never_observed` path entries in `validate-traces.cjs` evidence output carry `confidence: low|medium|high` based on trace volume and window duration

**Formal models:** .planning/formal/alloy/evidence-triage.als

**Status:** Complete

### EVID-02: `observation_window` metadata (window_start, window_end, n_traces, n_events, win

**Requirement:** `observation_window` metadata (window_start, window_end, n_traces, n_events, window_days) written to `check-results.ndjson` for evidence-driven checks

**Status:** Complete

### LIVE-01: CI step detects liveness properties lacking a fairness declaration in `invariant

**Requirement:** CI step detects liveness properties lacking a fairness declaration in `invariants.md` and emits `result=inconclusive` instead of `pass`

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### LIVE-02: `run-formal-verify.cjs` STEPS includes a `ci:liveness-fairness-lint` step that e

**Requirement:** `run-formal-verify.cjs` STEPS includes a `ci:liveness-fairness-lint` step that enforces LIVE-01

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### SCHEMA-01: `model-registry.json` entries gain a `requirements` array (string[]) listing the

**Requirement:** `model-registry.json` entries gain a `requirements` array (string[]) listing the requirement IDs each model covers; existing entries seeded from the property-to-requirement map in TRACEABILITY_RESEARCH.md

**Formal models:** .planning/formal/alloy/schema-extensions.als

**Status:** Complete

### SCHEMA-03: Each verification runner (run-tlc.cjs, run-alloy.cjs, run-prism.cjs, CI lint ste

**Requirement:** Each verification runner (run-tlc.cjs, run-alloy.cjs, run-prism.cjs, CI lint steps) emits `requirement_ids` in its NDJSON output, extracted from model-registry or inline annotations

**Formal models:** .planning/formal/alloy/schema-extensions.als

**Status:** Complete

### SCHEMA-04: `requirements.json` envelope gains an optional `formal_models` array (string[]) 

**Requirement:** `requirements.json` envelope gains an optional `formal_models` array (string[]) per requirement, listing model file paths that verify it

**Formal models:** .planning/formal/alloy/schema-extensions.als

**Status:** Complete

### SENS-01: `bin/run-sensitivity-sweep.cjs` varies key model parameters (at minimum: quorum 

**Requirement:** `bin/run-sensitivity-sweep.cjs` varies key model parameters (at minimum: quorum size N across its full range, timeout threshold T across low/medium/high values) across ≥3 values each, running TLA+/PRISM checks for each configuration, and records outcome deltas (pass→fail, pass→inconclusive transitions) in `.planning/formal/sensitivity-report.ndjson` using v2.1 schema — each record annotated with which parameter value caused the transition.

**Formal models:** .planning/formal/tla/QGSDSensitivity.tla

**Status:** Complete

### SENS-02: `plan-phase.md` step 8.3 (FV gate) is extended to also run `run-sensitivity-swee

**Requirement:** `plan-phase.md` step 8.3 (FV gate) is extended to also run `run-sensitivity-sweep.cjs` (fail-open) and inject `SENSITIVITY_CONTEXT` into the quorum `review_context`, surfacing the top-3 highest-impact parameters as planning recommendations: which code paths control them, which boundary values to test, and which metrics to monitor.

**Formal models:** .planning/formal/tla/QGSDSensitivity.tla

**Status:** Complete

### SENS-03: `bin/sensitivity-report.cjs` generates `.planning/formal/sensitivity-report.md` — a human

**Requirement:** `bin/sensitivity-report.cjs` generates `.planning/formal/sensitivity-report.md` — a human-readable ranked list of sensitive parameters by outcome-flip count, each annotated with (a) the code path or configuration variable that controls it, (b) recommended unit/integration test cases for boundary values, (c) recommended monitoring metrics to track in production.

**Formal models:** .planning/formal/tla/QGSDSensitivity.tla

**Status:** Complete

### SOLVE-01: Execute `/nf:solve` command to sweep all 7 layer transitions (Requirements→For

**Requirement:** Execute `/nf:solve` command to sweep all 7 layer transitions (Requirements→Formal, Formal→Tests, Code→Formal, Tests→Code, Formal→Code, Requirements→Docs, Docs→Code) and compute residual vectors showing gaps at each boundary, with optional auto-closing of gaps through iteration until convergence

**Formal models:** .planning/formal/alloy/solve-consistency.als

**Status:** Complete

### SOLVE-02: Parse all Alloy default constants from newline-separated constraint blocks and c

**Requirement:** Parse all Alloy default constants from newline-separated constraint blocks and clear formalTestSyncCache at each solver iteration to ensure fresh residual computation after file mutations

**Formal models:** .planning/formal/alloy/solve-consistency.als

**Status:** Complete

### SOLVE-03: Solver discovers documentation files via config (docs_paths in .planning/config.

**Requirement:** Solver discovers documentation files via config (docs_paths in .planning/config.json) with convention fallback (README.md, docs/**/*.md), checks R→D coverage by literal ID and keyword matching, and verifies D→C structural claims (file paths, CLI commands, dependencies) with false-positive filtering for template variables, example headings, home paths, and fenced code blocks

**Formal models:** .planning/formal/alloy/solve-consistency.als, .planning/formal/alloy/solver-doc-layers.als

**Status:** Complete

### SOLVE-04: Scope R→D gap detection to developer-category documentation files (docs/dev/) an

**Requirement:** Scope R→D gap detection to developer-category documentation files (docs/dev/) and automatically dispatch /nf:quick batches to generate requirement-to-documentation entries into docs/dev/requirements-coverage.md, while preserving user-controlled docs (docs/) as manual-only

**Formal models:** .planning/formal/alloy/solver-doc-layers.als

**Status:** Complete

### SOLVE-05: When qgsd:solve runs in another project, run-formal-verify.cjs discovers and ver

**Requirement:** When qgsd:solve runs in another project, run-formal-verify.cjs discovers and verifies that project's formal models (from ROOT/.formal/{tla,alloy,prism,petri}/) rather than hardcoding QGSD-internal model names. All child runners (run-tlc.cjs, run-alloy.cjs, run-prism.cjs, and variants) must honor --project-root for spec file resolution, JAR/tool lookup, and NDJSON output. QGSD-specific checks (XState machine extraction, provider roster constants) are gracefully skipped when their prerequisites do not exist in the target project.

**Formal models:** .planning/formal/alloy/solver-portability.als

**Status:** Complete

### SOLVE-11: The formal verification result schema SHALL distinguish infrastructure errors

**Requirement:** The formal verification result schema SHALL distinguish infrastructure errors (binary not found, model missing, Java version wrong) from requirement violations (counterexample, divergence, property violation) using separate result values, and only requirement violations SHALL inflate the F->C residual.

**Formal models:** .planning/formal/alloy/result-classification.als

**Implementation:** `write-check-result.cjs` defines `VALID_RESULTS = ['pass', 'fail', 'error', 'warn', 'inconclusive']`. Runner scripts (run-alloy.cjs, run-prism.cjs, run-tlc.cjs, etc.) classify infrastructure failures as `result:'error'` and verification violations as `result:'fail'`. In `nf-solve.cjs`, `sweepFtoC()` tracks `errorCount` separately from `failedCount` — only `failedCount` drives the F→C residual.

**Source files:** bin/write-check-result.cjs, bin/nf-solve.cjs, bin/run-alloy.cjs, bin/run-prism.cjs, bin/run-tlc.cjs

**Status:** Complete

### SPEC-01: Stop hook formalized in `QGSDStopHook.tla` + `MCStopHook.cfg`, TLC verifies safe

**Requirement:** Stop hook formalized in `QGSDStopHook.tla` + `MCStopHook.cfg`, TLC verifies safety + liveness

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### SPEC-03: Quorum composition verified in `quorum-composition.als` — 3 composition rules ho

**Requirement:** Quorum composition verified in `quorum-composition.als` — 3 composition rules hold

**Formal models:** .planning/formal/alloy/quorum-composition.als

**Status:** Complete

### SPEC-04: `generate-phase-spec.cjs` translates PLAN.md `must_haves: truths:` to TLA+ PROPE

**Requirement:** `generate-phase-spec.cjs` translates PLAN.md `must_haves: truths:` to TLA+ PROPERTY stubs

**Formal models:** .planning/formal/tla/QGSDSpecGeneration.tla

**Status:** Complete

### TRACE-01: `bin/generate-traceability-matrix.cjs` reads model-registry, requirements.json, 

**Requirement:** `bin/generate-traceability-matrix.cjs` reads model-registry, requirements.json, and check-results.ndjson to produce `.planning/formal/traceability-matrix.json` with property-level links between requirements and formal properties

**Status:** Complete

### TRACE-02: The traceability matrix includes a `coverage_summary` section: total requirement

**Requirement:** The traceability matrix includes a `coverage_summary` section: total requirements, covered count, coverage percentage, list of uncovered requirements, list of orphan properties (properties with no requirement mapping)

**Formal models:** .planning/formal/alloy/traceability-annotations.als

**Status:** Complete

### TRACE-03: The traceability matrix is generated as a step in `run-formal-verify.cjs` after 

**Requirement:** The traceability matrix is generated as a step in `run-formal-verify.cjs` after all checks complete

**Status:** Complete

### TRACE-04: Bidirectional validation detects asymmetric links (model claims requirement X bu

**Requirement:** Bidirectional validation detects asymmetric links (model claims requirement X but requirement X does not claim that model) and emits warnings

**Formal models:** .planning/formal/alloy/traceability-annotations.als

**Status:** Complete

### TRACE-05: CI guard warns when formal coverage percentage drops below a configurable thresh

**Requirement:** CI guard warns when formal coverage percentage drops below a configurable threshold (default 15%) compared to the previous traceability matrix

**Formal models:** .planning/formal/alloy/traceability-annotations.als

**Status:** Complete

### TRACE-06: The /nf:formal-test-sync command cross-references formal model invariants with

**Requirement:** The /nf:formal-test-sync command cross-references formal model invariants with unit test coverage, validates formal constants against runtime config defaults, reports coverage gaps, generates test stubs, and integrates unit test coverage into the traceability matrix

**Formal models:** .planning/formal/alloy/formal-test-trace.als

**Status:** Complete

## Hooks & Enforcement

### DETECT-01: PreToolUse hook intercepts Bash tool calls and checks whether the current contex

**Requirement:** PreToolUse hook intercepts Bash tool calls and checks whether the current context has an active circuit breaker before running detection

**Formal models:** .planning/formal/tla/QGSDCircuitBreaker.tla

**Status:** Complete

### DETECT-02: Hook retrieves last N commits' changed files via `git log --name-only` (N = comm

**Requirement:** Hook retrieves last N commits' changed files via `git log --name-only` (N = commit_window config) when detection is needed

**Formal models:** .planning/formal/tla/QGSDCircuitBreaker.tla

**Status:** Complete

### DETECT-03: Hook identifies oscillation when the exact same file set (strict set equality, n

**Requirement:** Hook identifies oscillation when the exact same file set (strict set equality, not intersection) appears in ≥ oscillation_depth of the last commit_window commits — strict equality prevents false positives during TDD cycles where different files are touched per commit

**Formal models:** .planning/formal/tla/QGSDCircuitBreaker.tla

**Status:** Complete

### DETECT-04: Read-only Bash commands (git log, git diff, grep, cat, ls, head, tail, find) pas

**Requirement:** Read-only Bash commands (git log, git diff, grep, cat, ls, head, tail, find) pass through without detection or blocking

**Formal models:** .planning/formal/tla/QGSDOscillation.tla

**Status:** Complete

### DETECT-05: Detection is skipped (returns pass) when no git repository exists in the current

**Requirement:** Detection is skipped (returns pass) when no git repository exists in the current working directory

**Formal models:** .planning/formal/tla/QGSDOscillation.tla

**Status:** Complete

### DRIFT-01: `check-trace-schema-drift.cjs` detects schema drift and emits `check-results.ndj

**Requirement:** `check-trace-schema-drift.cjs` detects schema drift and emits `check-results.ndjson`

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### DRIFT-02: Schema drift check runs as CI step in `run-formal-verify.cjs`

**Requirement:** Schema drift check runs as CI step in `run-formal-verify.cjs`

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### ENFC-01: When circuit breaker is active, hook returns `hookSpecificOutput.permissionDecis

**Requirement:** When circuit breaker is active, hook returns `hookSpecificOutput.permissionDecision:'deny'` blocking Bash execution

**Formal models:** .planning/formal/tla/QGSDEnforcement.tla

**Status:** Complete

### ENFC-02: Block reason names the oscillating file set, confirms circuit breaker is active,

**Requirement:** Block reason names the oscillating file set, confirms circuit breaker is active, and lists allowed operations (read-only Bash)

**Formal models:** .planning/formal/tla/QGSDEnforcement.tla

**Status:** Complete

### ENFC-03: Block reason instructs Claude to perform root cause analysis and map dependencie

**Requirement:** Block reason instructs Claude to perform root cause analysis and map dependencies before resuming; explicitly instructs the user to manually commit the fix (since Claude cannot run git commit while blocked)

**Formal models:** .planning/formal/tla/QGSDEnforcement.tla

**Status:** Complete

### HEAL-01: After each deliberation round, system computes P(consensus | remaining rounds); 

**Requirement:** After each deliberation round, system computes P(consensus | remaining rounds); if P < threshold (default 10%), escalation fires early instead of exhausting maxDeliberation

**Formal models:** .planning/formal/prism/deliberation-healing.props

**Status:** Complete

### HEAL-02: When verify-quorum-health detects P(consensus) < 95%, it recommends and auto-adj

**Requirement:** When verify-quorum-health detects P(consensus) < 95%, it recommends and auto-adjusts maxDeliberation in qgsd.json (with user approval)

**Formal models:** .planning/formal/prism/deliberation-healing.props

**Status:** Complete

### ORES-01: When oscillation is detected and the oscillating file set contains only internal

**Requirement:** When oscillation is detected and the oscillating file set contains only internal code files, Claude MUST enter oscillation resolution mode instead of hard-stopping

**Formal models:** .planning/formal/tla/QGSDConvergence.tla

**Status:** Complete

### ORES-02: Oscillation resolution mode presents oscillation evidence (file set, commit grap

**Requirement:** Oscillation resolution mode presents oscillation evidence (file set, commit graph) to all available quorum models with structural-coupling framing

**Formal models:** .planning/formal/tla/QGSDConvergence.tla

**Status:** Complete

### ORES-03: Quorum deliberates (R3.3, up to 4 rounds) and may only approve unified solutions

**Requirement:** Quorum deliberates (R3.3, up to 4 rounds) and may only approve unified solutions — partial/incremental fixes are rejected

**Formal models:** .planning/formal/tla/QGSDConvergence.tla

**Status:** Complete

### ORES-04: On consensus, Claude presents the unified solution plan to the user for approval

**Requirement:** On consensus, Claude presents the unified solution plan to the user for approval before any execution

**Formal models:** .planning/formal/tla/QGSDConvergence.tla

**Status:** Complete

### ORES-05: If no consensus after 4 rounds, Claude hard-stops and escalates to the human wit

**Requirement:** If no consensus after 4 rounds, Claude hard-stops and escalates to the human with all model positions

**Formal models:** .planning/formal/tla/QGSDConvergence.tla

**Status:** Complete

### REDACT-01: `check-trace-redaction.cjs` checks traces for PII/secret patterns and emits `che

**Requirement:** `check-trace-redaction.cjs` checks traces for PII/secret patterns and emits `check-results.ndjson`

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### REDACT-02: Redaction check runs as CI step in `run-formal-verify.cjs`

**Requirement:** Redaction check runs as CI step in `run-formal-verify.cjs`

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### REDACT-03: Redaction violations emit `result=fail` (not warn)

**Requirement:** Redaction violations emit `result=fail` (not warn)

**Formal models:** .planning/formal/tla/QGSDCIChecks.tla

**Status:** Complete

### STOP-01: Stop hook reads transcript JSONL for tool_use entries matching configured quorum

**Requirement:** Stop hook reads transcript JSONL for tool_use entries matching configured quorum model names

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-02: Stop hook checks `stop_hook_active` flag first — if true, exits 0 immediately (i

**Requirement:** Stop hook checks `stop_hook_active` flag first — if true, exits 0 immediately (infinite loop prevention)

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-03: Stop hook checks `hook_event_name` — if `SubagentStop`, exits 0 immediately (sub

**Requirement:** Stop hook checks `hook_event_name` — if `SubagentStop`, exits 0 immediately (subagent exclusion)

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-04: Stop hook scopes transcript search to current turn only (lines since last user m

**Requirement:** Stop hook scopes transcript search to current turn only (lines since last user message boundary) — survives context compaction

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-05: Stop hook reads transcript JSONL as the authoritative source of quorum evidence 

**Requirement:** Stop hook reads transcript JSONL as the authoritative source of quorum evidence — no fast-path pre-check (design decision: last_assistant_message substring matching is not a reliable signal; JSONL parse is synchronous and correct for all transcript sizes)

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-06: Stop hook verifies quorum only when a configured planning command was issued in 

**Requirement:** Stop hook verifies quorum only when a configured planning command was issued in the current turn (scope filtering)

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-07: Stop hook blocks with `{"decision": "block", "reason": "..."}` when quorum is mi

**Requirement:** Stop hook blocks with `{"decision": "block", "reason": "..."}` when quorum is missing — reason includes exact tool names and instructions

**Formal models:** .planning/formal/tla/QGSDStopHook.tla

**Status:** Complete

### STOP-08: Block reason message format: "QUORUM REQUIRED: Before completing this /nf:[com

**Requirement:** Block reason message format: "QUORUM REQUIRED: Before completing this /nf:[command] response, call [tool1], [tool2], [tool3] with your current plan. Present their responses, then deliver your final output."

**Formal models:** .planning/formal/alloy/transcript-scan.als

**Status:** Complete

### STOP-09: Stop hook passes (exits 0, no decision field) when quorum evidence found or no p

**Requirement:** Stop hook passes (exits 0, no decision field) when quorum evidence found or no planning command in scope

**Formal models:** .planning/formal/alloy/transcript-scan.als

**Status:** Complete

### UPS-01: UserPromptSubmit hook detects GSD planning commands via explicit allowlist regex

**Requirement:** UserPromptSubmit hook detects GSD planning commands via explicit allowlist regex match against prompt field

**Formal models:** .planning/formal/tla/QGSDPromptHook.tla

**Status:** Complete

### UPS-02: Allowlist contains exactly 6 commands: new-project, plan-phase, new-milestone, d

**Requirement:** Allowlist contains exactly 6 commands: new-project, plan-phase, new-milestone, discuss-phase, verify-work, research-phase

**Formal models:** .planning/formal/tla/QGSDPromptHook.tla

**Status:** Complete

### UPS-03: UserPromptSubmit hook injects quorum instructions via `hookSpecificOutput.additi

**Requirement:** UserPromptSubmit hook injects quorum instructions via `hookSpecificOutput.additionalContext` (not systemMessage — goes into Claude's context window)

**Formal models:** .planning/formal/tla/QGSDPromptHook.tla

**Status:** Complete

### UPS-04: Injected context names the exact MCP tools to call and instructs Claude to prese

**Requirement:** Injected context names the exact MCP tools to call and instructs Claude to present model responses before delivering final output

**Formal models:** .planning/formal/tla/QGSDPromptHook.tla

**Status:** Complete

### UPS-05: UserPromptSubmit hook never fires on execute-phase or other non-planning command

**Requirement:** UserPromptSubmit hook never fires on execute-phase or other non-planning commands

**Formal models:** .planning/formal/tla/QGSDPromptHook.tla

**Status:** Complete

## Installation & Toolchain

### INST-11: Bin scripts resolve ROOT from process.cwd() (the invoking project directory) rat

**Requirement:** Bin scripts resolve ROOT from process.cwd() (the invoking project directory) rather than __dirname (the script install location). __dirname resolves to ~/.claude/ when installed, breaking all .formal/ and .planning/ path lookups. The --project-root= flag provides an explicit override but the default must be process.cwd().

**Formal models:** .planning/formal/alloy/bin-path-resolution.als

**Status:** Complete

### INST-12: Baseline requirements are merged into .formal/requirements.json using an idempot

**Requirement:** Baseline requirements are merged into .formal/requirements.json using an idempotent CLI tool that matches on text field to prevent duplicates and assigns next-available IDs per prefix namespace.

**Formal models:** .planning/formal/alloy/baseline-merge-idempotent.als

**Status:** Complete

### PLAT-01: QGSD installs and operates correctly on macOS, Ubuntu, and Windows without platf

**Requirement:** QGSD installs and operates correctly on macOS, Ubuntu, and Windows without platform-specific workarounds

**Formal models:** .planning/formal/alloy/platform-install-compat.als

**Status:** Pending

## Installer & CLI

### INST-01: Installer detects no configured quorum agents and prompts user to run `/nf:mcp

**Requirement:** Installer detects no configured quorum agents and prompts user to run `/nf:mcp-setup`

**Formal models:** .planning/formal/alloy/install-scope.als

**Status:** Complete

### INST-02: QGSD's package.json pins GSD version — version lockstep ensures hook compatibili

**Requirement:** QGSD's package.json pins GSD version — version lockstep ensures hook compatibility

**Formal models:** .planning/formal/alloy/install-scope.als

**Status:** Complete

### INST-03: Installer writes hooks to `~/.claude/settings.json` directly (not plugin.json ho

**Requirement:** Installer writes hooks to `~/.claude/settings.json` directly (not plugin.json hooks — stdout is silently discarded per GitHub #10225)

**Formal models:** .planning/formal/alloy/install-scope.als

**Status:** Complete

### INST-04: Installer adds UserPromptSubmit and Stop hook entries to `~/.claude/settings.jso

**Requirement:** Installer adds UserPromptSubmit and Stop hook entries to `~/.claude/settings.json` hooks section

**Formal models:** .planning/formal/alloy/install-scope.als

**Status:** Complete

### INST-05: Installer performs validation before registering hooks: checks MCPs are configur

**Requirement:** Installer performs validation before registering hooks: checks MCPs are configured in Claude Code settings, warns if Codex/Gemini/OpenCode not found

**Formal models:** .planning/formal/alloy/install-scope.als

**Status:** Complete

### INST-06: Installer is idempotent — running `npx qgsd@latest` again updates hooks and conf

**Requirement:** Installer is idempotent — running `npx qgsd@latest` again updates hooks and config without duplicating entries

**Formal models:** .planning/formal/tla/QGSDInstallerIdempotency.tla

**Status:** Complete

### INST-07: Installer respects existing per-project `.claude/nf.json` overrides during upd

**Requirement:** Installer respects existing per-project `.claude/nf.json` overrides during updates

**Formal models:** .planning/formal/tla/QGSDInstallerIdempotency.tla

**Status:** Complete

### INST-08: Installer registers PreToolUse circuit breaker hook in `~/.claude/settings.json`

**Requirement:** Installer registers PreToolUse circuit breaker hook in `~/.claude/settings.json` alongside existing hooks

**Formal models:** .planning/formal/tla/QGSDInstallerIdempotency.tla

**Status:** Complete

### INST-09: Installer writes default `circuit_breaker` config block to qgsd.json on first in

**Requirement:** Installer writes default `circuit_breaker` config block to qgsd.json on first install

**Formal models:** .planning/formal/tla/QGSDInstallerIdempotency.tla

**Status:** Complete

### INST-10: Reinstall (idempotent) adds missing `circuit_breaker` config block without overw

**Requirement:** Reinstall (idempotent) adds missing `circuit_breaker` config block without overwriting user-modified values

**Formal models:** .planning/formal/tla/QGSDInstallerIdempotency.tla

**Status:** Complete

### PORT-02: User can import roster config from JSON file — validates schema, prompts to re-e

**Requirement:** User can import roster config from JSON file — validates schema, prompts to re-enter any redacted key, confirms before applying

**Formal models:** .planning/formal/tla/QGSDConfigPortability.tla

**Status:** Pending

### PORT-03: Import creates a timestamped backup of `~/.claude.json` before applying any chan

**Requirement:** Import creates a timestamped backup of `~/.claude.json` before applying any changes

**Formal models:** .planning/formal/tla/QGSDConfigPortability.tla

**Status:** Pending

### REN-03: All hardcoded `get-shit-done/` path strings removed from `bin/gsd-tools.cjs`, wo

**Requirement:** All hardcoded `get-shit-done/` path strings removed from `bin/gsd-tools.cjs`, workflow files, agent files, and template files

**Formal models:** .planning/formal/alloy/codebase-arch-constraints.als

**Status:** Pending

### WIZ-01: User can run `/nf:mcp-setup` to start the MCP configuration wizard

**Requirement:** User can run `/nf:mcp-setup` to start the MCP configuration wizard

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-02: First run (no configured agents) presents a guided linear onboarding flow step b

**Requirement:** First run (no configured agents) presents a guided linear onboarding flow step by step

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-03: Re-run shows the current agent roster as a navigable menu

**Requirement:** Re-run shows the current agent roster as a navigable menu

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-04: Each agent in the menu shows current model, provider, and key status (present/mi

**Requirement:** Each agent in the menu shows current model, provider, and key status (present/missing)

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-05: User confirms before changes are applied; wizard restarts affected agents after 

**Requirement:** User confirms before changes are applied; wizard restarts affected agents after apply

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-08: `/nf:mcp-setup` re-run menu includes an "Edit Quorum Composition" option

**Requirement:** `/nf:mcp-setup` re-run menu includes an "Edit Quorum Composition" option

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-09: Composition screen shows all discovered slots with on/off toggle for `quorum.act

**Requirement:** Composition screen shows all discovered slots with on/off toggle for `quorum.active` inclusion

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-10: User can add a new slot for any family (claude, copilot, opencode, codex-cli, ge

**Requirement:** User can add a new slot for any family (claude, copilot, opencode, codex-cli, gemini-cli) from within the wizard, which writes the new entry to `~/.claude.json` and triggers restart

**Formal models:** .planning/formal/tla/QGSDSetupWizard.tla

**Status:** Complete

### WIZ-11: User can run /nf:settings to access a guided project manager hub with state-aw

**Requirement:** User can run /nf:settings to access a guided project manager hub with state-aware dashboard (milestone, progress, phase, config summary) and categorized main menu (Continue Working, Project Management, Configuration, Quick Task) with smart routing and profile/baselines management

**Formal models:** .planning/formal/alloy/settings-hub.als

**Status:** Complete

## MCP & Agents

### AGENT-01: User can add a new claude-mcp-server instance (name, provider, model, key)

**Requirement:** User can add a new claude-mcp-server instance (name, provider, model, key)

**Formal models:** .planning/formal/tla/QGSDAgentProvisioning.tla

**Status:** Complete

### AGENT-02: User can remove an existing agent from the roster

**Requirement:** User can remove an existing agent from the roster

**Formal models:** .planning/formal/tla/QGSDAgentProvisioning.tla

**Status:** Complete

### AGENT-03: Wizard runs identity ping to verify connectivity after provisioning new agent

**Requirement:** Wizard runs identity ping to verify connectivity after provisioning new agent

**Status:** Complete

### HLTH-01: `validate health` produces zero W005 false positives for versioned phase directo

**Requirement:** `validate health` produces zero W005 false positives for versioned phase directories (`v0.X-YY-name` format)

**Formal models:** .planning/formal/alloy/health-validation.als

**Status:** Complete

### HLTH-02: `validate health` produces zero W007 false positives for versioned phase directo

**Requirement:** `validate health` produces zero W007 false positives for versioned phase directories

**Formal models:** .planning/formal/alloy/health-validation.als

**Status:** Complete

### HLTH-03: `validate health` produces zero W002 false positives for versioned phase referen

**Requirement:** `validate health` produces zero W002 false positives for versioned phase references in STATE.md

**Status:** Complete

### MCP-01: Installer reads `~/.claude.json` to auto-detect MCP server names for Codex, Gemi

**Requirement:** Installer reads `~/.claude.json` to auto-detect MCP server names for Codex, Gemini, OpenCode

**Status:** Complete

### MCP-02: Detection matches server names containing "codex", "gemini", "opencode" (case-in

**Requirement:** Detection matches server names containing "codex", "gemini", "opencode" (case-insensitive keyword match)

**Status:** Complete

### MCP-03: Detected names written to `~/.claude/qgsd.json` as `required_models` on install

**Requirement:** Detected names written to `~/.claude/qgsd.json` as `required_models` on install

**Status:** Complete

### MCP-04: If detection finds no matching servers, installer falls back to hardcoded defaul

**Requirement:** If detection finds no matching servers, installer falls back to hardcoded defaults: `mcp__codex-cli__`, `mcp__gemini-cli__`, `mcp__opencode__`

**Formal models:** .planning/formal/alloy/mcp-detection.als

**Status:** Complete

### MCP-05: User can manually edit `qgsd.json` to override detected names

**Requirement:** User can manually edit `qgsd.json` to override detected names

**Status:** Complete

### MCP-06: Stop hook matches tool_use names by prefix (e.g. `mcp__codex-cli__` matches both

**Requirement:** Stop hook matches tool_use names by prefix (e.g. `mcp__codex-cli__` matches both `mcp__codex-cli__codex` and `mcp__codex-cli__review`)

**Formal models:** .planning/formal/alloy/mcp-detection.als

**Status:** Complete

### MCPENV-01: `.formal/spec/mcp-calls/` contains TLA+ model of MCP environment behavior

**Requirement:** `.formal/spec/mcp-calls/` contains TLA+ model of MCP environment behavior

**Formal models:** .planning/formal/tla/QGSDMCPEnv.tla

**Status:** Complete

### MCPENV-03: MCP availability rates from `run-prism.cjs` feed into the MCP environment model

**Requirement:** MCP availability rates from `run-prism.cjs` feed into the MCP environment model

**Formal models:** .planning/formal/tla/QGSDMCPEnv.tla

**Status:** Complete

### MCPENV-04: `readMCPAvailabilityRates` exported with composite-key filter; `module.exports` 

**Requirement:** `readMCPAvailabilityRates` exported with composite-key filter; `module.exports` always reachable via require.main guard; prism:mcp-availability STEPS entry and CI step added

**Formal models:** .planning/formal/prism/mcp-availability.pm, .planning/formal/prism/mcp-availability.props

**Status:** Complete

### MULTI-01: User can have multiple `claude-*` slots (`claude-1` through `claude-N`) each run

**Requirement:** User can have multiple `claude-*` slots (`claude-1` through `claude-N`) each running a different model or provider

**Status:** Complete

### MULTI-02: User can have multiple `copilot-N`, `opencode-N`, `codex-cli-N`, and `gemini-cli

**Requirement:** User can have multiple `copilot-N`, `opencode-N`, `codex-cli-N`, and `gemini-cli-N` slots as separate `~/.claude.json` entries

**Formal models:** .planning/formal/alloy/multi-slot-structure.als

**Status:** Complete

### MULTI-03: Adding a new slot of any family is supported by both direct config edit and via 

**Requirement:** Adding a new slot of any family is supported by both direct config edit and via the mcp-setup wizard

**Status:** Complete

### PROV-01: User can change the base URL (provider) for an existing agent

**Requirement:** User can change the base URL (provider) for an existing agent

**Formal models:** .planning/formal/tla/QGSDAgentProvisioning.tla

**Status:** Complete

### PROV-02: Wizard offers curated provider list (AkashML, Together.xyz, Fireworks) + custom 

**Requirement:** Wizard offers curated provider list (AkashML, Together.xyz, Fireworks) + custom entry

**Status:** Complete

### PROV-03: Wizard updates `~/.claude.json` ANTHROPIC_BASE_URL and restarts agent on apply

**Requirement:** Wizard updates `~/.claude.json` ANTHROPIC_BASE_URL and restarts agent on apply

**Status:** Complete

### SLOT-01: User sees all quorum agents referred to by slot name (`claude-1`, `copilot-1`, `

**Requirement:** User sees all quorum agents referred to by slot name (`claude-1`, `copilot-1`, `gemini-cli-1`, etc.) in all QGSD output and commands

**Formal models:** .planning/formal/alloy/multi-slot-structure.als, .planning/formal/tla/QGSDQuorum.tla

**Status:** Complete

### SLOT-02: Migration script renames existing `~/.claude.json` mcpServers entries from model

**Requirement:** Migration script renames existing `~/.claude.json` mcpServers entries from model-based names to slot names automatically (non-destructive, invertible)

**Formal models:** .planning/formal/tla/QGSDRecruiting.tla

**Status:** Complete

### SLOT-04: `mcp-status`, `mcp-set-model`, `mcp-update`, `mcp-restart` accept and display sl

**Requirement:** `mcp-status`, `mcp-set-model`, `mcp-update`, `mcp-restart` accept and display slot names correctly

**Formal models:** .planning/formal/tla/QGSDRecruiting.tla

**Status:** Complete

## Observability

### OBS-05: CLI tools exit with appropriate codes (0 = success, non-zero = failure) and writ

**Requirement:** CLI tools exit with appropriate codes (0 = success, non-zero = failure) and write errors to stderr, output to stdout

**Formal models:** .planning/formal/alloy/cli-exit-conventions.als

**Status:** Pending

## Observability & Diagnostics

### OBS-01: Each quorum round emits structured telemetry (slot, round, verdict, latency_ms, 

**Requirement:** Each quorum round emits structured telemetry (slot, round, verdict, latency_ms, provider status) to a per-session log file

**Formal models:** .planning/formal/prism/observability-delivery.props

**Status:** Complete

### OBS-02: Scoreboard tracks quorum delivery rate -- percentage of calls that achieved targ

**Requirement:** Scoreboard tracks quorum delivery rate -- percentage of calls that achieved target vote count (3/3 vs degraded 2/3)

**Formal models:** .planning/formal/prism/observability-delivery.props

**Status:** Complete

### OBS-03: Each slot gets a flakiness score from recent UNAVAIL/timeout frequency; high-fla

**Requirement:** Each slot gets a flakiness score from recent UNAVAIL/timeout frequency; high-flakiness slots deprioritized in dispatch ordering

**Formal models:** .planning/formal/prism/observability-delivery.props

**Status:** Complete

### OBS-04: Status shows recent UNAVAIL count per agent from quorum scoreboard

**Requirement:** Status shows recent UNAVAIL count per agent from quorum scoreboard

**Formal models:** .planning/formal/prism/observability-delivery.props

**Status:** Complete

### OBS-06: Scan local project state for unfinished quick tasks, stale debug sessions, and u

**Requirement:** Scan local project state for unfinished quick tasks, stale debug sessions, and unverified milestone phases; surface them as always-on issues in /nf:observe without requiring configuration.

**Formal models:** .planning/formal/alloy/observability-analysis.als

**Status:** Complete

### OBS-07: The analyze-assumptions CLI parses TLA+, Alloy, and PRISM formal models to extra

**Requirement:** The analyze-assumptions CLI parses TLA+, Alloy, and PRISM formal models to extract assumptions, thresholds, and invariants, cross-references them against the debt ledger and observe handler registry, and outputs a gap report with proposed qgsd_-prefixed metrics and instrumentation snippets for every uncovered assumption.

**Formal models:** .planning/formal/alloy/observability-analysis.als

**Status:** Complete

### OBS-08: The analyze-assumptions CLI classifies each extracted assumption into priority t

**Requirement:** The analyze-assumptions CLI classifies each extracted assumption into priority tiers (Tier 1: directly monitorable numeric constants, Tier 2: named invariants/assertions checkable via probes, Tier 3: structural constraints not runtime-observable), sorts gap reports by tier, supports --actionable filtering to Tier 1 only, and generates Prometheus gauge/histogram instrumentation snippets for Tier 1 assumptions.

**Formal models:** .planning/formal/alloy/observability-analysis.als

**Status:** Complete

### SCBD-01: Scoreboard tracks performance by slot name (`claude-1`, `copilot-1`) — slot is t

**Requirement:** Scoreboard tracks performance by slot name (`claude-1`, `copilot-1`) — slot is the stable key

**Formal models:** .planning/formal/alloy/scoreboard-recompute.als

**Status:** Complete

### SCBD-02: Each scoreboard entry displays the current model loaded in that slot as context

**Requirement:** Each scoreboard entry displays the current model loaded in that slot as context

**Formal models:** .planning/formal/alloy/scoreboard-recompute.als

**Status:** Complete

### SCBD-03: When a slot's model changes, a new scoreboard row is created for that slot

**Requirement:** When a slot's model changes, a new scoreboard row is created for that slot

**Formal models:** .planning/formal/alloy/scoreboard-recompute.als

**Status:** Complete

### SIG-01: `detect-coverage-gaps.cjs` diffs TLC states vs conformance traces to produce `.f

**Requirement:** `detect-coverage-gaps.cjs` diffs TLC states vs conformance traces to produce `.planning/formal/coverage-gaps.md`

**Formal models:** .planning/formal/alloy/signal-analysis-tools.als

**Status:** Complete

### SIG-02: `generate-petri-net.cjs --roadmap` models phase dependencies as Petri net with c

**Requirement:** `generate-petri-net.cjs --roadmap` models phase dependencies as Petri net with critical path analysis

**Formal models:** .planning/formal/alloy/signal-analysis-tools.als

**Status:** Complete

### SIG-03: `prism-priority.cjs` ranks roadmap items by PRISM failure probability

**Requirement:** `prism-priority.cjs` ranks roadmap items by PRISM failure probability

**Status:** Complete

### SIG-04: `quorum-consensus-gate.cjs` gates quorum rounds by Poisson binomial consensus pr

**Requirement:** `quorum-consensus-gate.cjs` gates quorum rounds by Poisson binomial consensus probability threshold

**Formal models:** .planning/formal/alloy/signal-analysis-tools.als

**Status:** Complete

### TRIAGE-01: `bin/generate-triage-bundle.cjs` reads `check-results.ndjson` and writes `.forma

**Requirement:** `bin/generate-triage-bundle.cjs` reads `check-results.ndjson` and writes `.planning/formal/diff-report.md` (per-check delta from last run) and `.planning/formal/suspects.md` (checks with `result=fail` or `triage_tags` set)

**Formal models:** .planning/formal/alloy/evidence-triage.als

**Status:** Complete

### TRIAGE-02: `run-formal-verify.cjs` calls `generate-triage-bundle.cjs` as the final step aft

**Requirement:** `run-formal-verify.cjs` calls `generate-triage-bundle.cjs` as the final step after all checks complete

**Status:** Complete

### VIS-01: `/nf:health` surfaces W008 warning when a quorum slot has ≥3 failures recorded

**Requirement:** `/nf:health` surfaces W008 warning when a quorum slot has ≥3 failures recorded in `quorum-failures.json`

**Formal models:** .planning/formal/alloy/health-validation.als

**Status:** Complete

## Planning & Tracking

### ACT-01: `.planning/current-activity.json` is written atomically at every major workflow 

**Requirement:** `.planning/current-activity.json` is written atomically at every major workflow state transition (execute-phase, plan-phase, debug, quorum, circuit-breaker, new-milestone stages)

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-02: Activity schema: `{ activity, sub_activity, phase?, plan?, wave?, debug_round?, 

**Requirement:** Activity schema: `{ activity, sub_activity, phase?, plan?, wave?, debug_round?, checkpoint?, quorum_round?, updated }` — all fields except `activity` and `updated` are optional; unknown fields are preserved

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-03: `gsd-tools.cjs activity-set <json>` CLI command writes the current-activity.json

**Requirement:** `gsd-tools.cjs activity-set <json>` CLI command writes the current-activity.json file with atomic replace; `activity-clear` removes it

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-04: `resume-work` reads current-activity.json and routes to the exact recovery point

**Requirement:** `resume-work` reads current-activity.json and routes to the exact recovery point — displaying the interrupted state before resuming execution

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-05: `execute-phase` writes activity at every stage boundary: entering/exiting plan e

**Requirement:** `execute-phase` writes activity at every stage boundary: entering/exiting plan execution, checkpoint:verify, debug loop rounds, awaiting_human_verify, and verifying_phase

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-06: `plan-phase`, `new-milestone`, `debug`, `quorum`, and `circuit-breaker` resoluti

**Requirement:** `plan-phase`, `new-milestone`, `debug`, `quorum`, and `circuit-breaker` resolution workflows write activity at every stage boundary

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### ACT-07: Activity file is cleared on successful completion of any top-level workflow; it 

**Requirement:** Activity file is cleared on successful completion of any top-level workflow; it persists across context resets when mid-workflow

**Formal models:** .planning/formal/tla/QGSDActivityTracking.tla

**Status:** Complete

### DECOMP-01: `bin/analyze-state-space.cjs` reads TLA+ .cfg files and model variables to estim

**Requirement:** `bin/analyze-state-space.cjs` reads TLA+ .cfg files and model variables to estimate state-space size per model and classifies risk as MINIMAL/LOW/MODERATE/HIGH based on configurable thresholds

**Status:** Complete

### DECOMP-02: The state-space analyzer flags models using unbounded domains (Nat, Int without 

**Requirement:** The state-space analyzer flags models using unbounded domains (Nat, Int without constraints) as HIGH risk

**Formal models:** .planning/formal/alloy/state-space-analysis.als

**Status:** Complete

### DECOMP-03: When a model is split into sub-models, the traceability matrix validates that no

**Requirement:** When a model is split into sub-models, the traceability matrix validates that no requirement loses coverage (pre-split coverage >= post-split coverage for every affected requirement)

**Formal models:** .planning/formal/alloy/state-space-analysis.als

**Status:** Complete

### DECOMP-04: The state-space analysis report is included in the traceability matrix output as

**Requirement:** The state-space analysis report is included in the traceability matrix output as a `state_space` section per model

**Formal models:** .planning/formal/alloy/state-space-analysis.als

**Status:** Complete

### DECOMP-05: `analyze-state-space.cjs` identifies model pairs sharing source files or require

**Requirement:** `analyze-state-space.cjs` identifies model pairs sharing source files or requirement prefixes, estimates the merged state space, and recommends merge when combined TLC runtime < 5 minutes — or flags the interface contract needed when merge would exceed the time budget

**Formal models:** .planning/formal/alloy/decomp-cross-model.als

**Status:** Pending

### INIT-01: Load and filter baseline requirements by project profile (web/mobile/desktop/api

**Requirement:** Load and filter baseline requirements by project profile (web/mobile/desktop/api/cli/library), present profile-filtered defaults during new-project and new-milestone workflows with opt-out controls, allowing users to deselect baseline requirements that don't apply

**Formal models:** .planning/formal/alloy/baseline-requirements-filter.als

**Status:** Complete

### LOOP-01: PRISM always uses current scoreboard rates via `export-prism-constants` pre-step

**Requirement:** PRISM always uses current scoreboard rates via `export-prism-constants` pre-step calibration

**Formal models:** .planning/formal/prism/quorum.pm, .planning/formal/prism/quorum.props, .planning/formal/tla/QGSDQuorum.tla

**Status:** Complete

### LOOP-02: PostToolUse hook `qgsd-spec-regen.js` auto-regenerates TLA+/Alloy specs on XStat

**Requirement:** PostToolUse hook `qgsd-spec-regen.js` auto-regenerates TLA+/Alloy specs on XState machine changes

**Formal models:** .planning/formal/tla/QGSDDeliberation.tla

**Status:** Complete

### LOOP-03: `sensitivity-sweep-feedback.cjs` detects threshold violations and triggers PRISM

**Requirement:** `sensitivity-sweep-feedback.cjs` detects threshold violations and triggers PRISM re-run

**Formal models:** .planning/formal/tla/QGSDPreFilter.tla

**Status:** Complete

### LOOP-04: `propose-debug-invariants.cjs` mines TLA+ invariant candidates from debug sessio

**Requirement:** `propose-debug-invariants.cjs` mines TLA+ invariant candidates from debug sessions

**Formal models:** .planning/formal/tla/QGSDSpecGeneration.tla

**Status:** Complete

### PLAN-01: `generate-proposed-changes.cjs` auto-synthesizes TLA+ deltas from PLAN.md truths

**Requirement:** `generate-proposed-changes.cjs` auto-synthesizes TLA+ deltas from PLAN.md truths

**Formal models:** .planning/formal/tla/QGSDDeliberation.tla

**Status:** Complete

### PLAN-02: `run-phase-tlc.cjs` provides iterative TLC verification loop gating planning (3-

**Requirement:** `run-phase-tlc.cjs` provides iterative TLC verification loop gating planning (3-attempt cap)

**Formal models:** .planning/formal/tla/QGSDDeliberation.tla

**Status:** Complete

### PLAN-03: `quorum-formal-context.cjs` generates formal evidence blocks for quorum slots

**Requirement:** `quorum-formal-context.cjs` generates formal evidence blocks for quorum slots

**Formal models:** .planning/formal/tla/QGSDPreFilter.tla

**Status:** Complete

### STATE-01: audit-milestone updates STATE.md "Stopped at" and "Current Position" fields with

**Requirement:** audit-milestone updates STATE.md "Stopped at" and "Current Position" fields with the audit result (passed / gaps_found / tech_debt) after writing the MILESTONE-AUDIT.md artifact

**Formal models:** .planning/formal/tla/QGSDBreakerState.tla

**Status:** Complete

### STATE-02: State schema: `{ active, file_set[], activated_at, commit_window_snapshot[] }` —

**Requirement:** State schema: `{ active, file_set[], activated_at, commit_window_snapshot[] }` — captures what triggered the breaker

**Formal models:** .planning/formal/tla/QGSDBreakerState.tla

**Status:** Complete

### STATE-03: Hook reads existing state first — if active, applies enforcement immediately wit

**Requirement:** Hook reads existing state first — if active, applies enforcement immediately without re-running git log detection

**Formal models:** .planning/formal/tla/QGSDBreakerState.tla

**Status:** Complete

### STATE-04: State file created silently if absent; failure to write logs to stderr but never

**Requirement:** State file created silently if absent; failure to write logs to stderr but never blocks execution

**Formal models:** .planning/formal/tla/QGSDBreakerState.tla

**Status:** Complete

### SYNC-01: QGSD ships as separate npm package (`qgsd` or `get-shit-done-quorum`) that wraps

**Requirement:** QGSD ships as separate npm package (`qgsd` or `get-shit-done-quorum`) that wraps GSD

**Formal models:** .planning/formal/alloy/gsd-sync-invariants.als

**Status:** Complete

### SYNC-02: When GSD releases a new planning command, QGSD releases a patch update adding th

**Requirement:** When GSD releases a new planning command, QGSD releases a patch update adding the command to the default `quorum_commands` list

**Formal models:** .planning/formal/alloy/gsd-sync-invariants.als

**Status:** Complete

### SYNC-03: QGSD changelog explicitly tracks which GSD version it is compatible with

**Requirement:** QGSD changelog explicitly tracks which GSD version it is compatible with

**Status:** Complete

### SYNC-04: No QGSD code modifies any GSD source files — all additions are in separate files

**Requirement:** No QGSD code modifies any GSD source files — all additions are in separate files (`hooks/qgsd-stop.js`, `hooks/qgsd-prompt.js`, `bin/qgsd-install.js`)

**Formal models:** .planning/formal/alloy/gsd-sync-invariants.als

**Status:** Complete

## Quorum & Dispatch

### ARCH-01: All formal models declared in `.planning/formal/model-registry.json` as single source of 

**Requirement:** All formal models declared in `.planning/formal/model-registry.json` as single source of truth with provenance tracking

**Formal models:** .planning/formal/alloy/architecture-registry.als

**Status:** Complete

### ARCH-02: `promote-model.cjs` provides atomic promotion from per-phase specs to canonical 

**Requirement:** `promote-model.cjs` provides atomic promotion from per-phase specs to canonical specs with duplicate detection

**Formal models:** .planning/formal/alloy/architecture-registry.als

**Status:** Complete

### ARCH-03: `accept-debug-invariant.cjs` writes debug-discovered invariants directly to cano

**Requirement:** `accept-debug-invariant.cjs` writes debug-discovered invariants directly to canonical specs with session provenance

**Status:** Complete

### CALIB-01: `.planning/formal/policy.yaml` is the single authoritative source for PRISM calibration p

**Requirement:** `.planning/formal/policy.yaml` is the single authoritative source for PRISM calibration parameters

**Formal models:** .planning/formal/alloy/availability-parsing.als

**Status:** Complete

### CALIB-02: `run-prism.cjs` reads `policy.yaml` and writes `writeCheckResult` with `observat

**Requirement:** `run-prism.cjs` reads `policy.yaml` and writes `writeCheckResult` with `observation_window` metadata

**Formal models:** .planning/formal/alloy/availability-parsing.als

**Status:** Complete

### CALIB-03: `read-policy.cjs` exposes all policy fields with typed interface

**Requirement:** `read-policy.cjs` exposes all policy fields with typed interface

**Formal models:** .planning/formal/alloy/availability-parsing.als

**Status:** Complete

### CALIB-04: `policy.conservative_priors.tp_rate` and `policy.conservative_priors.unavail` fr

**Requirement:** `policy.conservative_priors.tp_rate` and `policy.conservative_priors.unavail` from `.planning/formal/policy.yaml` wire directly to `run-prism.cjs` fallback constants (v0.19-10 fix)

**Formal models:** .planning/formal/alloy/provider-failure.als

**Status:** Complete

### DISP-01: qgsd-prompt.js runs a fast health probe (<3s) per provider before building the d

**Requirement:** qgsd-prompt.js runs a fast health probe (<3s) per provider before building the dispatch list -- dead providers' slots excluded from DISPATCH_LIST

**Formal models:** .planning/formal/tla/QGSDDispatchPipeline.tla

**Status:** Complete

### DISP-02: qgsd-prompt.js reads scoreboard `availability` windows and excludes slots whose 

**Requirement:** qgsd-prompt.js reads scoreboard `availability` windows and excludes slots whose `available_at` is in the future from dispatch

**Formal models:** .planning/formal/tla/QGSDDispatchPipeline.tla

**Status:** Complete

### DISP-03: Dispatch list ordered by recent success rate (from scoreboard slot stats) rather

**Requirement:** Dispatch list ordered by recent success rate (from scoreboard slot stats) rather than static FALLBACK-01 tier sequence -- most reliable slots dispatched first

**Formal models:** .planning/formal/tla/QGSDDispatchPipeline.tla

**Status:** Complete

### DISP-04: Prompt construction (Mode A/B, Round 1/2+, artifact injection, prior_positions, 

**Requirement:** Prompt construction (Mode A/B, Round 1/2+, artifact injection, prior_positions, review_context, improvements request) happens deterministically in JavaScript (call-quorum-slot.cjs or wrapper), not via LLM string manipulation in the slot worker agent

**Formal models:** .planning/formal/tla/QGSDDispatchPipeline.tla

**Status:** Complete

### DISP-05: Output parsing (verdict, reasoning, citations, improvements extraction from raw 

**Requirement:** Output parsing (verdict, reasoning, citations, improvements extraction from raw CLI output) happens in JavaScript with structured YAML output -- the slot worker agent returns script output verbatim without LLM post-processing

**Formal models:** .planning/formal/tla/QGSDDispatchPipeline.tla

**Status:** Complete

### DISP-06: Quorum dispatch prompts inject a matched subset of formal requirements (from .fo

**Requirement:** Quorum dispatch prompts inject a matched subset of formal requirements (from .formal/requirements.json) based on keywords extracted from the question and artifact path, enabling agents to evaluate changes against concrete acceptance criteria.

**Formal models:** .planning/formal/alloy/dispatch-formal-context.als

**Status:** Complete

### META-01: GSD planning commands within this repo (new-project, plan-phase, etc.) auto-reso

**Requirement:** GSD planning commands within this repo (new-project, plan-phase, etc.) auto-resolve questions via quorum before escalating to user

**Status:** Complete

### META-02: Only questions where quorum fails to reach consensus are presented to the user

**Requirement:** Only questions where quorum fails to reach consensus are presented to the user

**Formal models:** .planning/formal/alloy/architecture-registry.als

**Status:** Complete

### META-03: Auto-resolved questions are presented as a list of assumptions before escalated 

**Requirement:** Auto-resolved questions are presented as a list of assumptions before escalated questions

**Status:** Complete

### QUORUM-01: plan-milestone-gaps proposed gap closure phases are submitted to R3 quorum for a

**Requirement:** plan-milestone-gaps proposed gap closure phases are submitted to R3 quorum for approval before ROADMAP.md is updated (replaces AskUserQuestion confirmation gate)

**Formal models:** .planning/formal/tla/QGSDQuorum.tla, .planning/formal/tla/QGSDQuorum_xstate.tla

**Status:** Complete

### QUORUM-02: execute-phase gaps_found triggers quorum diagnosis and auto-resolution (replaces

**Requirement:** execute-phase gaps_found triggers quorum diagnosis and auto-resolution (replaces chain halt + manual suggestion)

**Formal models:** .planning/formal/tla/QGSDQuorum.tla, .planning/formal/alloy/quorum-votes.als

**Status:** Complete

### QUORUM-03: discuss-phase remaining user_questions (surviving R4 pre-filter) are routed to q

**Requirement:** discuss-phase remaining user_questions (surviving R4 pre-filter) are routed to quorum in auto mode (replaces AskUserQuestion for gray areas)

**Formal models:** .planning/formal/tla/QGSDQuorum.tla

**Status:** Complete

### QUORUM-04: A quorum round reaches consensus when all voting members return the same verdict

**Requirement:** A quorum round reaches consensus when all voting members return the same verdict AND none propose further improvements; no additional iteration is required once consensus is achieved

**Formal models:** .planning/formal/tla/QGSDQuorum.tla

**Status:** Pending

### QUORUM-05: All quorum members (including Claude) MUST review prior-round positions from oth

**Requirement:** All quorum members (including Claude) MUST review prior-round positions from other voters and revise their own position before each deliberation round. No voter's position may be frozen across rounds — every participant re-evaluates after seeing peer arguments.

**Formal models:** .planning/formal/tla/QGSDDeliberationRevision.tla

**Status:** Pending

### UNIF-01: All FV checkers append normalized JSON to `check-results.ndjson` — `{ tool, chec

**Requirement:** All FV checkers append normalized JSON to `check-results.ndjson` — `{ tool, check_id, result, detail, ts }`

**Formal models:** .planning/formal/alloy/unified-check-results.als

**Status:** Complete

### UNIF-02: `run-formal-verify.cjs` generates `check-results.ndjson` as canonical output art

**Requirement:** `run-formal-verify.cjs` generates `check-results.ndjson` as canonical output artifact

**Status:** Complete

### UNIF-03: Triage bundle reads from `check-results.ndjson`, not tool stdout; CI enforcement

**Requirement:** Triage bundle reads from `check-results.ndjson`, not tool stdout; CI enforcement steps run inside orchestrator before summary read (v0.19-11 timing fix)

**Formal models:** .planning/formal/alloy/unified-check-results.als

**Status:** Complete

### UNIF-04: CI step exits non-zero when any `result=fail` entry exists in `check-results.ndj

**Requirement:** CI step exits non-zero when any `result=fail` entry exists in `check-results.ndjson`

**Formal models:** .planning/formal/alloy/unified-check-results.als

**Status:** Complete

## Reliability

### REL-01: Failures in external services (APIs, databases, third-party SDKs) are caught and

**Requirement:** Failures in external services (APIs, databases, third-party SDKs) are caught and handled gracefully — the application degrades functionality rather than crashing

**Formal models:** .planning/formal/alloy/reliability-patterns.als

**Status:** Pending

### REL-02: Long-running operations (file uploads, data processing, API calls >2s) show prog

**Requirement:** Long-running operations (file uploads, data processing, API calls >2s) show progress indication and can be cancelled by the user

**Formal models:** .planning/formal/alloy/reliability-patterns.als

**Status:** Pending

## Security

### SEC-01: Pre-commit hook runs secret scanning (e.g., Gitleaks) to block commits containin

**Requirement:** Pre-commit hook runs secret scanning (e.g., Gitleaks) to block commits containing API keys, tokens, passwords, or credentials

**Formal models:** .planning/formal/alloy/security-scanning.als

**Status:** Pending

### SEC-02: CI pipeline runs deep secret scanning (e.g., TruffleHog) across full repo histor

**Requirement:** CI pipeline runs deep secret scanning (e.g., TruffleHog) across full repo history on every PR

**Formal models:** .planning/formal/alloy/security-scanning.als

**Status:** Pending

### SEC-03: All external input (user input, API request bodies, query parameters, file uploa

**Requirement:** All external input (user input, API request bodies, query parameters, file uploads) is validated and sanitized at system boundaries before processing

**Formal models:** .planning/formal/alloy/security-scanning.als

**Status:** Pending

### SEC-04: Dependencies are scanned for known vulnerabilities in CI (e.g., npm audit, Depen

**Requirement:** Dependencies are scanned for known vulnerabilities in CI (e.g., npm audit, Dependabot, Snyk) and critical/high findings block merge

**Formal models:** .planning/formal/alloy/security-scanning.als

**Status:** Pending

## TUI Navigation

### NAV-01: Main TUI menu is organized into 3 modules (Agents, Requirements, Config) with a 

**Requirement:** Main TUI menu is organized into 3 modules (Agents, Requirements, Config) with a 6-character activity bar sidebar showing icons, user switches modules via F1/F2/F3 hotkeys or Tab/Shift+Tab cycling, and only module-relevant menu items are displayed at any time.

**Formal models:** .planning/formal/tla/QGSDTUIModules.tla

**Status:** Complete

### NAV-02: Sessions module (F4) in nForma TUI embeds interactive Claude Code terminal sessi

**Requirement:** Sessions module (F4) in nForma TUI embeds interactive Claude Code terminal sessions via blessed-xterm, supporting creation, connection, and termination of multiple PTY-backed sessions with full focus management and module switching.

**Formal models:** .planning/formal/tla/QGSDTUISessions.tla

**Status:** Complete

### NAV-03: Sessions terminal widget uses pure-JavaScript @xterm/headless terminal emulation

**Requirement:** Sessions terminal widget uses pure-JavaScript @xterm/headless terminal emulation with child_process.spawn, eliminating native addon dependencies and enabling compatibility across all Node.js versions

**Formal models:** .planning/formal/alloy/terminal-emulation-purity.als

**Status:** Complete

## Testing & Quality

### FAIL-01: call-quorum-slot.cjs retries a failed slot call up to 2 times with exponential b

**Requirement:** call-quorum-slot.cjs retries a failed slot call up to 2 times with exponential backoff (1s, 3s) before recording UNAVAIL in quorum-failures.json

**Formal models:** .planning/formal/prism/mcp-availability.pm, .planning/formal/prism/mcp-availability.props

**Status:** Complete

### FAIL-02: providers.json contains explicit slot-to-provider mapping; when a provider probe

**Requirement:** providers.json contains explicit slot-to-provider mapping; when a provider probe returns DOWN, all slots on that provider skipped in a single dispatch decision

**Formal models:** .planning/formal/alloy/provider-failure.als

**Status:** Complete

### RECV-01: `npx qgsd --reset-breaker` CLI flag clears `.claude/circuit-breaker-state.json` 

**Requirement:** `npx qgsd --reset-breaker` CLI flag clears `.claude/circuit-breaker-state.json` and logs confirmation — enables manual recovery when circuit breaker deadlocks due to blocked git commit

**Formal models:** .planning/formal/tla/QGSDQuorum.tla

**Status:** Complete

### SAFE-01: `--repair` cannot silently overwrite a rich STATE.md (>50 lines) without explici

**Requirement:** `--repair` cannot silently overwrite a rich STATE.md (>50 lines) without explicit `--force` flag; guard fires and repair is skipped with a message

**Formal models:** .planning/formal/tla/QGSDQuorum.tla, .planning/formal/alloy/quorum-votes.als

**Status:** Complete

### STD-10: gemini-mcp-server npm package name is unscoped (`gemini-mcp-server`, not `@tuann

**Requirement:** gemini-mcp-server npm package name is unscoped (`gemini-mcp-server`, not `@tuannvm/gemini-mcp-server`) — `~/.claude.json` mcpServers["gemini-cli"].args reflects the unscoped name

**Formal models:** .planning/formal/alloy/codebase-arch-constraints.als

**Status:** Complete

### VERIFY-01: `qgsd-verifier` agent runs `run-formal-verify` after implementation and includes

**Requirement:** `qgsd-verifier` agent runs `run-formal-verify` after implementation and includes `check-results.ndjson` digest in `VERIFICATION.md`

**Formal models:** .planning/formal/alloy/verification-integration.als

**Status:** Complete

### VERIFY-02: `VERIFICATION.md` template gains a `## Formal Verification` section summarizing 

**Requirement:** `VERIFICATION.md` template gains a `## Formal Verification` section summarizing pass/fail/warn counts per formalism (tla, alloy, prism, ci)

**Formal models:** .planning/formal/alloy/verification-integration.als

**Status:** Complete

## Traceability & Verification

### SOLVE-06: qgsd-solve diagnostic engine provides reverse traceability sweeps (C→R, T→R, D→R

**Requirement:** qgsd-solve diagnostic engine provides reverse traceability sweeps (C→R, T→R, D→R) that discover implementation artifacts without requirement backing, assembled via deduplication and acknowledged-not-required filtering, presented through a two-step candidate-then-approve pattern that prevents unbounded requirement expansion

**Formal models:** .planning/formal/alloy/solver-portability.als

**Status:** Complete

## UX Heuristics

### UX-01: Every user-initiated action produces immediate feedback (loading/disabled state)

**Requirement:** Every user-initiated action produces immediate feedback (loading/disabled state) and completion feedback (result message, navigation change, or visible state update)

**Formal models:** .planning/formal/alloy/ux-feedback-safety.als

**Status:** Pending

### UX-02: Destructive actions (delete, reset, remove, overwrite) require explicit confirma

**Requirement:** Destructive actions (delete, reset, remove, overwrite) require explicit confirmation or provide undo within a reasonable window

**Formal models:** .planning/formal/alloy/ux-feedback-safety.als

**Status:** Pending

### UX-03: Error messages are human-readable, explain what went wrong, and suggest a next s

**Requirement:** Error messages are human-readable, explain what went wrong, and suggest a next step or recovery action

**Formal models:** .planning/formal/alloy/ux-feedback-safety.als

**Status:** Pending

## Solver-Discovered

### SOLVE-18: Code module: bin/unified-mcp-server.mjs

**Requirement:** Code module: bin/unified-mcp-server.mjs

**Implementation:** The unified MCP server (`bin/unified-mcp-server.mjs`) is the single entrypoint for all 11 MCP provider servers. It handles provider routing, health checks, and ping/identity operations through a shared server infrastructure.

**Source files:** bin/unified-mcp-server.mjs

**Formal models:** .planning/formal/alloy/requirements-aggregation.als

**Status:** Complete

### SOLVE-19: Code module: hooks/config-loader.js

**Requirement:** Code module: hooks/config-loader.js

**Implementation:** The config loader (`hooks/config-loader.js`) implements the two-layer configuration loading system. It reads both global (`~/.claude/nf.json`) and project-local (`.nf.json`) configuration, merging them with project-local overrides taking precedence.

**Source files:** hooks/config-loader.js

**Formal models:** .planning/formal/alloy/hook-module-existence.als

**Status:** Complete

### SOLVE-20: Code module: hooks/nf-circuit-breaker.js

**Requirement:** Code module: hooks/nf-circuit-breaker.js

**Implementation:** The circuit breaker hook (`hooks/nf-circuit-breaker.js`) is a PreToolUse hook that detects oscillation patterns in consecutive commits. It uses a run-collapse algorithm to identify alternating edit groups and triggers a Haiku reviewer to classify patterns as GENUINE vs REFINEMENT before blocking.

**Source files:** hooks/nf-circuit-breaker.js

**Formal models:** .planning/formal/alloy/hook-module-existence.als

**Status:** Complete

### SOLVE-21: Code module: hooks/nf-prompt.js

**Requirement:** Code module: hooks/nf-prompt.js

**Implementation:** The prompt hook (`hooks/nf-prompt.js`) is a UserPromptSubmit hook that handles quorum injection for nForma skills and circuit breaker recovery. When the breaker is active, it injects oscillation-resolution-mode.md as additional context.

**Source files:** hooks/nf-prompt.js

**Formal models:** .planning/formal/alloy/hook-module-existence.als

**Status:** Complete

### SOLVE-22: Code module: hooks/nf-stop.js

**Requirement:** Code module: hooks/nf-stop.js

**Implementation:** The stop hook (`hooks/nf-stop.js`) is a Stop hook that blocks planning responses that lack quorum consensus. It enforces the quorum requirement (R3.1) by validating that responses have been approved through the multi-model quorum process.

**Source files:** hooks/nf-stop.js

**Formal models:** .planning/formal/alloy/hook-module-existence.als

**Status:** Complete

### SOLVE-23: Code module: hooks/nf-mcp-dispatch-guard.js

**Requirement:** Code module: hooks/nf-mcp-dispatch-guard.js

**Implementation:** The MCP dispatch guard (`hooks/nf-mcp-dispatch-guard.js`) enforces the prohibition on direct MCP tool calls for quorum dispatch. It ensures that quorum slot workers are spawned via parallel sibling Task calls rather than sequential direct MCP invocations.

**Source files:** hooks/nf-mcp-dispatch-guard.js

**Formal models:** .planning/formal/alloy/hook-module-existence.als

**Status:** Complete


## RDME-01: TUI section promoted from `<details>` to visible section with hero screenshot ab

**Requirement:** TUI section promoted from `<details>` to visible section with hero screenshot above the fold

**Category:** README Structure

**Implementation:** README structure requirement implemented directly in README.md content organization and formatting.

**Source files:** README.md

## RLS-01: `package.json` version field bumped from `0.1.0` to `0.2.0`

**Requirement:** `package.json` version field bumped from `0.1.0` to `0.2.0`

**Category:** Installer & CLI

**Implementation:** Release process requirement tracked via package.json version field, MILESTONES.md archive, and git tagging workflow.

**Source files:** package.json, MILESTONES.md

## SAFE-02: Legacy pre-versioning numeric phase dirs 18-39 are archived to `.planning/archiv

**Requirement:** Legacy pre-versioning numeric phase dirs 18-39 are archived to `.planning/archive/legacy/`

**Category:** Testing & Quality

**Implementation:** Safety requirement enforced by bin/install.js archive logic for legacy numeric phase directories.

**Source files:** bin/install.js

## SPEC-02: `QGSDOscillation.tla` audited against `qgsd-circuit-breaker.js` — no drift detec

**Requirement:** `QGSDOscillation.tla` audited against `qgsd-circuit-breaker.js` — no drift detected

**Category:** Formal Verification

**Implementation:** Specification audit requirement verified by formal verification pipeline comparing TLA+ models against source code.

**Source files:** .planning/formal/tla/NFOscillation.tla, hooks/nf-circuit-breaker.js

## Governance & DX

### ADAPT-01: Config values normalized through bidirectional adapter

**Requirement:** Config values are normalized through a bidirectional adapter (e.g., profile names, boolean strings, nested vs flat keys) so internal types never leak to config files and vice versa

**Implementation:** The config-loader module (`hooks/config-loader.js`) provides bidirectional normalization between nf.json config format and internal runtime types. Profile names, boolean strings, and nested keys are normalized on load and serialized back on write, ensuring config files remain portable across different config formats.

**Source files:** hooks/config-loader.js, bin/install.js

### ADR-01: Quorum debates follow consistent template

**Requirement:** Quorum debates in `.planning/quorum/debates/` follow a consistent template (Context, Question, Positions, Decision, Consequences) that downstream tools can parse

**Implementation:** Quorum debate files in `.planning/quorum/debates/` follow a structured markdown template with standard sections: Context, Question, Positions (per-model), Decision, and Consequences. This template is enforced by the quorum dispatch workflow and consumed by debate analysis tools.

**Source files:** core/references/quorum-dispatch.md, .planning/quorum/debates/

### BLD-01: hooks/dist/ rebuilt from current source

**Requirement:** `hooks/dist/` rebuilt from current source — includes all circuit breaker hook code from Phases 6-8 and GUARD 5 code from Phase 5

**Implementation:** The install script (`bin/install.js`) reads hook source files from `hooks/dist/` and installs them to `~/.claude/hooks/`. The build process copies from `hooks/` to `hooks/dist/` ensuring the distribution directory always contains current source including circuit breaker hooks (nf-circuit-breaker.js) and guard hooks.

**Source files:** bin/install.js, hooks/dist/, hooks/nf-circuit-breaker.js

## PAIR-04: Rejected pairings cached to avoid re-evaluating in subsequent runs

**Requirement:** Rejected pairings cached to avoid re-evaluating in subsequent runs

**Implementation:** The `bin/resolve-pairings.cjs` module maintains a rejection cache that persists rejected N:N pairing candidates across solve sessions. When `bin/candidate-pairings.cjs` generates pairing proposals, previously rejected pairings are filtered out before evaluation, preventing redundant re-evaluation of user-rejected or auto-rejected matches.

**Source files:** bin/resolve-pairings.cjs, bin/candidate-pairings.cjs

## PROMO-03: Auto-promotion from SOFT_GATE to HARD_GATE

**Requirement:** Models auto-promoted SOFT_GATE -> HARD_GATE when consecutive_clean_sessions >= 3 AND cooldown satisfied (gate-stability.cjs)

**Implementation:** The `bin/promote-gate-maturity.cjs` module implements the auto-promotion logic. When a model accumulates 3 or more consecutive clean sessions (no verification failures) and any active cooldown timer has expired, the model's gate maturity level is automatically promoted from SOFT_GATE to HARD_GATE. The `bin/compute-per-model-gates.cjs` module tracks per-model consecutive clean session counts and integrates with `bin/nf-solve.cjs` to evaluate promotion eligibility during each solve iteration.

**Source files:** bin/promote-gate-maturity.cjs, bin/compute-per-model-gates.cjs, bin/nf-solve.cjs

## PROMO-04: Promotion changelog logging

**Requirement:** All promotions logged to promotion-changelog.json with timestamp, session_id, from_level, to_level

**Implementation:** The `bin/promote-gate-maturity.cjs` module writes every promotion and demotion event to `.planning/formal/promotion-changelog.json`. Each entry records a timestamp, session_id, the originating maturity level (from_level), and the target level (to_level). The `bin/nForma.cjs` orchestrator and `bin/solve-tui.cjs` TUI both read this changelog for display. Entries are deduplicated per session to prevent redundant log writes.

**Source files:** bin/promote-gate-maturity.cjs, bin/nForma.cjs, bin/solve-tui.cjs, .planning/formal/promotion-changelog.json

